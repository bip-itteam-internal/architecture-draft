## Deskripsi

*Penilaian kinerja berbasis **skor KPI**. **Penting dipisahkan**: engine **skor→insentif tim marketing SUDAH ADA** di kode ([[Microservices - Insentive Service]]); sedangkan **appraisal/KPI bulanan menyeluruh per-karyawan semua departemen** (dijelaskan di "Fitur" bawah) masih **konsep**.*

[Contoh dari sistem ini](https://drive.google.com/drive/folders/15fGQNX5usiMXIk2GY8DAqahHFF5ahPpA)

- **Status**: ⚠️ Sebagian — engine KPI→insentif (marketing) ✅ di [[Microservices - Insentive Service]]; appraisal per-karyawan lintas-departemen ✅ **sebagian** di [[Microservices - Employee Service]] (`/kpi/*`: template per posisi + scoring); siklus bulanan wajib & workflow review supervisor→HR 🟡 konsep.

## Sudah Diimplementasikan — KPI engine insentif (marketing)

> Grounded ke [[Microservices - Insentive Service]] (✅ production). Cakupan: **9 role marketing** (Supervisor, ADV Leader TikTok, ADV Marketplace, ADV Meta, Host Live, Affiliate, CRM, CS, ICC) — **bukan** seluruh karyawan/departemen.

- `master-kpi` (CRUD; bobot total 100) · `POST /calculate` (scoring bertingkat per-role) · hasil + workflow approve/override · cron harian menarik metrik iklan (TikTok GMV-Max / Shopee GMS) dari [[Microservices - Integration Service]].
- Skor → **insentif** ([[Finance - Incentive]] / [[Sales - Incentive]]). Koleksi: `master_kpis`, `kpi_score`, `incentive_results` ([[DB - Overview and Notes]]).

## Sudah Diimplementasikan — appraisal per-karyawan (employee-service)

> Grounded ke [[Microservices - Employee Service]] (`/kpi/*`) + FE [[APP - Web ERP]] (`src/features/hris/kpi/`). Sebagian dari konsep appraisal menyeluruh di bawah kini **sudah berjalan** untuk penilaian per-karyawan lintas departemen (bukan hanya marketing).

- **Template per posisi**: `GET/POST/DELETE /kpi/templates` (filter `?department=&position=`). Tiap template = `{name, department, position, metrics[]}`; metrik `{label, weight, description}` (nilai 0–100, bobot total tiap template).
	- **`employee_count`** (✅ live 2026-08-18, bip-erp `462ba514` + FE `3d807952`): `GET /kpi/templates` menyertakan jumlah karyawan yang **memakai** tiap template = **assignment aktual**, bukan kelayakan posisi. Aturannya: karyawan AKTIF yang **skor KPI periode TERAKHIR**-nya memakai template itu (dikunci `template._id`, sumber `kpi_score`) **dan** posisinya **sekarang** cocok dengan (department, position) template (menyingkirkan yang sudah pindah posisi). Sebabnya penting: **satu posisi boleh punya beberapa template** ([[HRIS - Key Performance Index]] mencatat Finance/AR Staff punya tiga), tetapi **satu karyawan dinilai dengan SATU template** — menghitung per pemegang posisi (kelayakan) menggandakan angka di tiap template posisi yang sama (dua template ICC sama-sama menampilkan 19 untuk 19 orang). Konsekuensi yang disengaja: **template yang belum pernah dipakai = 0** (mis. "ICC NEW"), dan karyawan yang belum pernah dinilai tak terhitung ke mana pun. Berbeda dari `karyawan_aktif` Otomasi KPI yang memang KELAYAKAN posisi. WorkData & kpi_score dibaca proyeksi sempit (hindari decode utuh yang jatuh pada dokumen tanggal-string). FE menampilkannya sebagai kolom **"Karyawan"** di tabel KPI Templates; fallback "–" bila field belum ada (BE deploy dulu), 0 tetap 0. Terverifikasi dev lewat gateway: INTERNAL CONTENT CREATOR = 19, ICC NEW = 0.
- **Scoring per-karyawan+periode** (`YYYY-MM`): submit `template_id` + `values{label→0..100}`. Template di-**snapshot** ke `kpi_score` (skor lampau beku — edit master template tidak retroaktif); final = Σ(weight×value).
- **Aturan kunci — posisi template WAJIB = posisi karyawan PADA PERIODE ITU**: submit ditolak `400 "template position … does not match employee position … for period …"`. Sejak 2026-08-10 pembandingnya **bukan lagi** `work_data.position` hari ini, melainkan jabatan yang dipegang pada **akhir periode yang dinilai**, dijawab dari `employee_movement` ([[ADR - 0044 Mutasi Antar-Tenant Mempertahankan employee_id]]).
	- Sebabnya: karyawan yang pindah 1 September membuat penilaian Agustus-nya **mustahil diisi benar** dengan patokan lama — template yang benar untuk Agustus ditolak, dan satu-satunya yang diterima justru template jabatan yang belum dipegangnya sepanjang bulan itu. Bukan kasus langka: KPI memang sering baru diisi setelah bulannya lewat.
	- Perpindahan yang berlaku **di tengah** periode dianggap sudah terjadi; penilaian bulanan harus punya satu jawaban, bukan dua. Catatan `scheduled`/`cancelled` diabaikan, dan gagal baca riwayat jatuh ke perilaku lama alih-alih memblokir penilaian.
	- **Sisi FE mengikuti** sejak 2026-08-10 (bip-erp [#1146](https://github.com/bip-itteam-internal/bip-erp/pull/1146) + erp-frontend [#963](https://github.com/bip-itteam-internal/erp-frontend/pull/963)). Modal Score KPI dulu menyaring dropdown ke posisi karyawan **saat ini**, sehingga untuk periode sebelum perpindahan template yang benar justru tersembunyi walau backend menerimanya.
		- Jawabannya diambil dari server lewat **`posisi_periode`** yang menumpang di respons `GET /kpi/score` — rute yang **sudah** dipanggil modal itu dengan pasangan `(employee_id, period)` yang sama persis, jadi nol permintaan tambahan dan nol endpoint baru.
		- Aturannya sengaja **tidak** disalin ke TypeScript. Dua sumber kebenaran pasti menyimpang, dan yang menyimpang di sini membuat penilai melihat dropdown yang isinya ditolak server — pola yang sudah ditandai sebagai langkah mundur waktu aturan kelompok departemen sempat digandakan ke frontend.
		- Field **opsional**: selama backend belum ter-deploy, penyaringan jatuh ke jabatan terkini persis seperti perilaku sebelumnya. Dikunci uji.
- **RBAC per departemen** (`RequireKPIDepartmentRBAC`). `department` boleh berisi **beberapa departemen dipisah koma** — dalam hal itu **SEMUA** harus terjangkau role pemanggil; satu saja tak berhak berarti ditolak, agar tampilan gabungan tak membocorkan departemen yang bukan haknya. Role `hris` tetap lolos untuk departemen mana pun.
- **Cakupan TIM untuk Leader** (`GET /kpi?scope=team`, branch `feat/atasan-langsung-kpi-leader` + `feat/kpi-leader-portal`, **belum merge**): menyaring ke **bawahan langsung** pemanggil (`work_data.supervisor_id`), bukan ke departemen. Menu KPI di Portal Saya yang dulu khusus supervisor kini juga terbuka bagi mereka.
	- **Gerbangnya keberadaan bawahan, BUKAN role, nama jabatan, maupun jenjang.** Leader ber-role `staff` sehingga selalu ditolak `RequireKPIDepartmentRBAC`; sementara pola judul `Supervisor|^Leader$` tak cocok untuk "Leader Production", "AR Leader", atau "QA Leader" yang benar-benar ada di data, dan jenjang jabatan belum tercatat di mana pun. Konsekuensi yang disengaja: begitu bawahan seseorang dipindahkan, menunya ikut hilang.
	- Cabang gerbangnya **dipisah**, bukan menambal `RequireKPIDepartmentRBAC` — gerbang itu meloloskan seluruh staf `hris` untuk departemen mana pun, dan pelebaran itu tak boleh terbawa ke jalur baru. Tanpa bawahan **aktif**, `scope=team` ditolak 403 supaya tak jadi pintu belakang.
	- **Supervisor menang** bila seseorang kebetulan keduanya: ia tetap melihat seluruh departemennya, termasuk orang yang sudah punya Leader.
	- Penyaringan keaktifan memakai `system_authentication.is_active`, sama seperti pipeline, supaya gerbang dan isi halaman sepakat.
- **Tampilan gabungan lintas departemen**: departemen yang **satu tim** ditampilkan sebagai satu kelompok berlabel pendek (saat ini **HRGA** = Human Resource + General Affair), departemen lain tetap terpisah. Sumbernya **master data** `master_department.supervised_by` + `supervision_label` — bukan konfigurasi frontend, bukan hardcode (lihat [[HRIS - Organization Structure]]).
	- **Berlaku untuk SIAPA PUN yang berhak melihat kedua departemen**, bukan hanya supervisornya. Penggabungan adalah sifat organisasi, bukan sifat penontonnya: staf HR (`hris:staff`) melihat HRGA menyatu sama seperti SPV-nya. Staf GA tak terpengaruh karena tanpa role `hris` mereka tak sampai ke menu HRIS.
	- **Data karyawan tidak diubah**: `work_data.department` tiap orang tetap di departemen masing-masing. Yang disatukan hanya tampilannya, sehingga pemisahan kembali **tanpa migrasi data** dan tanpa deploy — cukup ubah master data.
	- Penggabungan dihitung di **level orang**, bukan menjumlahkan ringkasan antar-departemen, supaya rata-rata tertimbang menurut jumlah anggota (HR 6 orang vs GA 15 orang memberi hasil berbeda jauh bila salah cara).
	- Hanya **mengelompokkan ulang** baris yang sudah lolos filter `department` + RBAC, tidak pernah menarik baris baru. Sebuah kelompok batal bila kurang dari dua anggotanya ikut difilter, dan labelnya kembali ke nama departemen asli.
	- Query `merge=<daftar>` + `merge_label=<nama>` tetap diterima untuk penggabungan **ad-hoc**, dan menimpa pengelompokan master data bila dikirim.
	- Beberapa kelompok bisa hidup berdampingan (mis. HR+GA dan Finance+Procurement) tanpa perubahan kode.
- **Bentuk respons**: satu departemen (atau beberapa yang seluruhnya digabung) → `{department, summary, members}`; selain itu → `{summary, departments[]}`.
- **Dashboard/analitik**: agregasi per departemen (rata-rata, coverage) + daftar *need training* (<60) & *top performer* (≥80).
- **Belum**: penegakan siklus bulanan & workflow review supervisor→HR (lihat konsep di bawah) = **TBD**.
- **Tampilan FE** (✅ live 2026-08-06, PR erp-frontend [#813](https://github.com/bip-itteam-internal/erp-frontend/pull/813)): halaman KPI mengikuti **pola satu card** yang sudah dipakai seluruh layar daftar HRIS lain — banner, tab departemen, ringkasan, dan tabel dalam satu card. Sebelumnya tiga card bertumpuk dan itu membuat KPI jadi satu-satunya layar HRIS yang tertinggal. Satu komponen (`KpiPageContent`) melayani **11 rute**: `/hris/kpi`, `/hris/kpi/templates`, `/portal/kpi`, serta KPI milik Finance, IT, GA, Quality, Procurement, Kesekretariatan, dan Manufaktur — dibedakan hanya oleh tiga props (`lockedDepartment`, `teamScope`, `view`). Rincian polanya di [[APP - Web ERP]].
	- **Kartu ringkasan tanpa warna ambang.** Total / sudah dinilai / belum dinilai / rata-rata semuanya bernada netral, berbeda dari kartu di Kelola Karyawan atau Resign yang berubah warna menurut target. Alasannya: **ambang KPI belum pernah disepakati siapa pun**, dan mewarnai kartu tanpa ambang berarti menyatakan penilaian yang tak ada dasarnya. Begitu ambang ditetapkan, `StatSummary` sudah punya prop `tone` dan tinggal diisi.
	- **Tab "Kesimpulan"** (✅ live 2026-08-13, erp-frontend commit `d4356c9c`, `feat/workspace-position`): **tab default & pertama** di semua rute ber-tab (KPI Tim + KPI per-modul). Menerjemahkan tiap baris skor jadi tindak lanjut untuk SPV — ≥80 = **Pertahankan**; di bawahnya = keterangan apa yang perlu ditingkatkan (dorong ke 80 / pendampingan / telusuri penurunan tren / lengkapi bukti); belum dinilai = ajakan menilai. Logika di `src/features/hris/kpi/lib/kesimpulan.ts`.
		- **Band de-facto, BUKAN ambang resmi.** Memakai `<60` / `60–79` / `≥80` yang **sudah** dipakai dashboard/analitik (`need training <60`, `top performer ≥80`), bukan menetapkan ambang baru. Ini tak menabrak butir "kartu ringkasan tanpa warna ambang" di atas: kartu tetap netral, keterangan tekstual sengaja tak mengklaim itu target resmi. Begitu ambang resmi ditetapkan, ubah dua konstanta `AMBANG_KURANG`/`AMBANG_BAIK`.
		- **Turun 100% dari data `GET /kpi` yang sama** — tak ada endpoint/kontrak baru (versi ringan; rincian metrik-lemah spesifik sengaja di luar lingkup).
		- **Grafik**: **donut** distribusi 4 band (warna status + legend jumlah/persen) berdampingan dengan **line chart rata-rata KPI per bulan** (6 bulan mundur dari periode aktif; bulan tanpa penilaian = jeda, bukan 0). Line memakai `useKpiTrend` (`useQueries`, satu `GET /kpi` per bulan, queryKey `kpi-trend` terpisah agar filter status tabel tak memotong rata-rata). **Garis mengikuti tab departemen aktif**: fetch tetap se-cakupan halaman, tapi tiap bulan MEMILIH ringkasan `activeDept` dari `departments[]` (`rataRataBulanDept`), jadi ganti tab = pilih ulang dari cache tanpa refetch, dan grup gabungan (mis. HRGA) ikut karena merge dihitung backend per bulan. recharts via `ChartContainer` shared.
		- **Fix hydration (dua lapis)**: (1) `KpiPageContent` (pemakai `useSearchParams`) dibungkus `<Suspense>` — tanpa batas itu Next App Router membail seluruh route ke render klien; berlaku ke-11 rute tanpa menyentuh berkas page. (2) **`/portal/kpi` (`PortalKpiPage`) bercabang pada auth** (`system_roles`/`department` dari **cookie yang tak terbaca saat SSR**), jadi server merender cabang berbeda dari klien — mismatch yang di Next 16 muncul sampai ke `Next.MetadataOutlet`. Digate `useMounted`: server & render pertama klien sama-sama skeleton, cabang peran menyusul setelah mount (erp-frontend commit `9ff3320d`).
	- **Pintasan Otomasi KPI → form konfigurasi** (✅ live 2026-08-13, erp-frontend commit `31958b70`): di halaman **Otomasi KPI** (`/hris/kpi/otomasi`), metrik berstatus `belum` (belum ada konfigurasi otomatis) kini punya tombol **"Isi konfigurasi"** yang deep-link ke **`/portal/kpi?edit=<template_id>`** — Portal Saya tempat SPV mengakses KPI, bukan area HRGA. `KpiPageContent` mengaktifkan **tab Template** saat `?edit=` muncul (di rute ber-tab default-nya Kesimpulan), lalu `KpiTemplatesTable` membuka modal edit template (form konfigurasi otomasi via `konfigurasi-otomatis-field`) **sekali** dan membersihkan param supaya menutup modal / refresh tak membukanya lagi. Navigasi murni FE; id basi tetap membersihkan URL.
	- **Laman "KPI Saya" untuk yang BUKAN penilai** (✅ merged 15 Agustus 2026, erp-frontend PR [#1051](https://github.com/bip-itteam-internal/erp-frontend/pull/1051)): cabang keempat `/portal/kpi`. Sebelum ini karyawan tanpa bawahan hanya melihat kalimat "Anda belum punya bawahan langsung", padahal KPI dirinya sendiri sudah tersedia di backend — cuma bisa dibuka lewat MyBharata. Berlaku **semua posisi & departemen**: templatenya diambil backend dari posisi orangnya sendiri, jadi tak ada nama departemen di jalur ini. **Tanpa perubahan backend**; memakai `GET /me/kpi-score` yang sudah ada (`?preview=true` dan `?graph=true`), lihat [[Microservices - Employee Service]].
		- **Periode belum dinilai TIDAK menampilkan total.** Metrik otomatis baru menutupi sebagian bobot template, sehingga total dari sebagian itu salah ke DUA arah sekaligus — menganggap sisanya nol terlalu rendah, menormalisasi ke yang ada terlalu tinggi — dan keduanya sama meyakinkannya. Yang tampil hanya nilai per-metrik + cacahan metrik yang menunggu atasan. Dikunci test (`kpi-saya-view.test.tsx`).
		- **Alasan gagal hitung tidak ditampilkan ke karyawan**, mengikuti keputusan backend: kalimat seperti "belum ada tiket bertenggat" ditulis untuk penilai yang bisa menindaklanjuti. Bagi yang dinilai, metrik belum-berangka dan metrik manual sama saja — menunggu atasan. **Basis** perhitungan justru diteruskan APA ADANYA supaya angkanya bisa ditelusuri sendiri.
		- **Metrik final membedakan `otomatis` / `disesuaikan atasan` / `dinilai atasan`** dari `auto_value` vs `value` (toleransi 0,005 agar selisih pembulatan JSON tak dituduh penyesuaian). Penyeragam dua bentuk respons ada di `lib/kpi-saya.ts` (pure + 11 test).
		- ✅ **Realtime + popup "Lihat rincian"** (✅ 17 Agustus 2026, bip-erp PR [#1252](https://github.com/bip-itteam-internal/bip-erp/pull/1252) + erp-frontend PR [#1066](https://github.com/bip-itteam-internal/erp-frontend/pull/1066)). `staleTime` klien **dihapus** (`use-kpi-saya.ts`) — rute `/me/kpi-score` sudah dikecualikan dari cache gateway 3 menit, jadi tiap mount memuat ulang, sama seperti `useFetchKpiAutoValues`. Metrik `cakupan` (PR #1246, sudah ada sebelumnya) kini didampingi **`rincian`** — breakdown terstruktur `[]{label,nilai}` di balik `basis`, dibuka lewat tombol "Lihat rincian" (`RincianTrigger`, dialog Radix) yang dipasang sama di modal Score KPI (penilai) dan laman ini (karyawan) — keduanya membaca breakdown yang identik dari backend. **Beda dari `cakupan`**: `rincian` TETAP diteruskan pada skor final (bukan `null`), sebab breakdown angka lampau tetap berguna menelusuri kenapa skor itu keluar, sementara `cakupan` sudah tak relevan begitu beku. Komponen tak merender apa pun bila `rincian` kosong/tak ada — sumber yang tak mengisinya cukup tak menampilkan tombol.
		- ⚠️ **Gerbang menu Portal Saya dibuka untuk semua** (`portal-menu.ts`, `URL_PORTAL_KPI` → `true`) dan judulnya "KPI Tim" → **"KPI"**. Tanpa itu halamannya tak terjangkau justru oleh audiens cabang barunya, dan tertutupnya SENYAP. Efek samping yang disengaja: KPI kini entri "dipegang semua orang" yang menggenapkan grup **Manajemen HR** ke `AMBANG_SARANG`, menggantikan peran "Onboarding" yang sudah dibuang — grup itu kini bertahan bagi Cost Control, dan diratakan bagi staf biasa (satu anak). Cakupan data tetap dijaga backend, bukan menu ([[ADR - 0031 Prefix internal Bukan Batas Keamanan]]).
	- ⚠️ **Mode supervisor di halaman ini mustahil menyala** — cacat gerbang yang **pra-ada** dan belum diperbaiki; lihat butir lengkapnya di [[APP - Web ERP]] bagian "Belum Diimplementasikan / Catatan". **TBD**, karena perbaikannya butuh keputusan siapa "HR" versus "supervisor" lebih dulu.
	- **Ringkasan "Per Tim/Leader" — donut per tim + garis tren multi-tim** (⚠️ erp-frontend branch `feature/workspace-position`, **belum merge**; scope ini hanya muncul untuk departemen ber-staf **ICC/marketing**). Scope "Per Tim/Leader" di `KpiBlueprintView` mengelompokkan anggota menurut **atasan langsung** (`supervisor_id`, dari `GET /employee/list?with_supervisor=true`), dasar sama dengan kartu leader ICC.
		- **Donut per tim**: tiap kartu tim menampilkan distribusi 4-band skor **bulan berjalan** (Pertahankan ≥80 / Perlu dijaga 60–79 / Perlu ditingkatkan <60 / Belum). Warna irisan = **status/severity** (token sama dengan donut Kesimpulan), diklasifikasi dari skor **gabungan** (tersimpan → usulan otomasi) supaya sepakat dengan rata-rata & daftar anggota di kartu yang sama.
		- **Garis tren multi-tim**: satu grafik area di atas kartu, **rata-rata KPI per bulan tiap tim**, ditumpuk & dibedakan **warna identitas** `--fb-seri-*` (palet kategorikal tervalidasi dua tema — **bukan** `--chart-1..5` yang bergeser hue antar tema). Warna garis = titik identitas di kartu (`warnaByKey`). Deret per bulan ditarik `useKpiTrend` (satu `GET /kpi` per bulan; bulan berjalan `sertakan_otomasi=1`, jadi `member.score` per bulan dipakai apa adanya), dikelompokkan per supervisor oleh `bangunTrenTim` (pure, teruji); bulan tanpa penilaian = **jeda (null), bukan 0**.
		- Pengelompokan diekstrak ke `src/features/hris/kpi/lib/kelompok-tim.ts` (key = `supervisor_id`, identitas stabil lintas bulan) supaya kartu & garis memakai dasar sama. **Keterbatasan yang diterima**: tim = potret hierarki **kini** diterapkan mundur — perpindahan atasan historis tak dilacak. Komponen: `blueprint/ringkasan-tim-leader.tsx`, `blueprint/tim-donut.tsx`, `blueprint/tim-tren-chart.tsx`, hook `hooks/use-tim-trend.ts`.
		- **Semua grafik tren KPI kini `AreaChart` bergradasi**: tren Kesimpulan (`kpi-kesimpulan-trend-chart.tsx`), tren KPI Saya (`kpi-saya-tren-chart.tsx`), dan tren per tim memakai gradasi warna seri → transparan di bawah garis; `connectNulls=false` (jeda bulan tanpa nilai) tetap dipertahankan.
- **Menu KPI dirombak ke tampilan "blueprint" `KpiBlueprintView`** (⚠️ erp-frontend branch `feature/workspace-position`, **belum merge**; menggantikan tabel padat di rute KPI). Filter berporos **scope** empat pilihan: **Per Karyawan · Per Tim/Leader · Per Departemen · Per Divisi**. Default **Per Departemen** (Ringkasan Tim) supaya `/portal/kpi` tak membuka layar kosong yang menunggu entitas dipilih. Nilai dari OTOMASI (auto-scores/auto-values); scorecard per entitas (`blueprint/kpi-scorecard.tsx`) menggantikan tabel skor.
	- **Per Tim/Leader & Per Divisi HANYA untuk departemen marketing**, dideteksi dari DATA — departemen ber-staf posisi `icc` (`GET /employee/list?type=employee&position=icc` → {Beauty Hacks, Kyura}), **bukan hardcode nama** (`hooks/use-icc-departments.ts`). Departemen lain hanya Per Karyawan + Per Departemen; berpindah ke departemen non-marketing mengembalikan scope ke Per Departemen.
	- **Per Divisi = GABUNGAN seluruh departemen ICC** (Beauty Hacks + Kyura) lewat satu `GET /kpi?department=<dipisah,koma>`, karena `master_department` **tidak** menautkan keduanya sebagai grup (satu-satunya grup di sana = HRGA). Ditampilkan: satu angka divisi + grafik + rincian per departemen.
	- **Bugfix daftar karyawan tercampur**: karena `/kpi` memekarkan filter ke seluruh grup supervisi, memilih satu departemen memuat anggota grupnya; di mode pemilih (HR/super) anggota kini disaring ke departemen yang dipilih (mode terkunci supervisor tetap utuh agar ringkasan divisi tak pincang). Pemilih karyawan jadi **combobox ber-search** yang menampilkan skor bulan aktif sebagai badge berwarna per band.
	- **Klik anggota di ringkasan → scorecard-nya, in-page** (⚠️ branch `feature/workspace-position`, **belum merge**): mengklik kartu anggota di **Per Departemen** atau baris anggota di **Per Tim/Leader** mengalihkan scope ke **Per Karyawan** dan memilih orang itu (`pilihKaryawan` di `blueprint/kpi-blueprint-view.tsx`: `setEntityId` lalu `setScope("karyawan")`), membuka `blueprint/kpi-scorecard.tsx` **tanpa route/halaman baru**. Karyawan yang diklik selalu ada di `membersScoped`, jadi efek auto-pilih mempertahankannya alih-alih menimpanya dengan anggota pertama. Kartu/baris jadi `<button>` ber-`aria-label` (`hris.kpiBlueprint.lihatRingkasan`, id+en). Scope **Per Divisi** dikecualikan (menampilkan agregat departemen, bukan karyawan). **Tanpa perubahan backend**; dikunci `kpi-blueprint-view.test.tsx` (klik → scorecard orang itu) & `ringkasan-tim-leader.test.tsx` (klik baris → `onSelectEmployee`).
	- **Fix donut "Belum dinilai (100%)" padahal skor otomatis sudah muncul** (⚠️ branch `feature/workspace-position`): donut distribusi di **Per Departemen** (`RingkasanTim`) & **Per Divisi** memakai `distribusiKesimpulan`→`klasifikasiKpi` yang mengklasifikasi dari `m.score`/`m.status` **mentah**, sehingga anggota yang skornya cuma dari usulan otomasi (`auto_score`, `m.score===null`) jatuh ke band "belum" — padahal kartu, StatTile rata-rata/tercapai, dan combobox sudah memakai fallback `m.score ?? auto_score`. Kini kedua call-site mengirim **skor efektif** ke donut lewat `denganSkorEfektif(members, autoByEmp)` (`src/features/hris/kpi/lib/skor-efektif.ts`: utamakan tersimpan, fallback otomasi, set `status:"scored"`), jadi donut sepakat dengan angka di sekitarnya. **Per Tim/Leader tidak kena** (donut-nya `distribusiBand(skorAnggota)` yang sudah ber-fallback); tab **Kesimpulan** lama & marketing tak memakai `auto_score` sama sekali (donut & tabelnya sama-sama dari `m.score`, konsisten). Dikunci `lib/skor-efektif.test.ts` (termasuk kontrol negatif distribusi mentah vs efektif).
- **Atur Target — sumber otomasi dipecah PER-METRIK + tipe isian mengikuti sumber** (⚠️ branch `feature/workspace-position`, **belum merge**). Sumber multi-metrik (mis. `kinerja_toko`, 8 metrik) dulu satu pilihan tanpa pemilih metrik sehingga nilainya ambigu. Karena katalog backend sudah mengekspos daftar `metrik` per sumber, dropdown Sumber **dipecah di tampilan** (`blueprint/atur-target-inline.tsx`): tiap pasangan (sumber, metrik) jadi satu pilihan berdiri sendiri (Penjualan toko, ROAS toko, Profit bersih toko, Retur toko, Tiket tepat waktu, dst) yang menyetel `auto.sumber`+`auto.metrik` sekaligus. **Tanpa perubahan backend/scoring.**
	- **Tipe isian (satuan target) terisi otomatis dari metrik** (`satuanDariMetrik`: nominal/persen/jumlah/nilai) dan **DIKUNCI** (dropdown → chip read-only ringkas) supaya tak bisa diubah ke satuan salah; metrik yang satuannya belum dikenal tetap bisa dipilih manual. Melengkapi penurunan berbasis formula (`satuanTarget`).
	- **Metrik "Profit bersih toko"** = `settlement − hpp − iklan − retur − opex PER ICC` — sudah dihitung backend (`kinerja_toko` metrik `profit_bersih`, `services/employee/kpi_sumber_kinerja_toko.go`); opex per-ICC dari `incentive_opex_accurate` (`proyek_no = employee_id`) ditempel marketing-analytics. Kini punya entri sumber sendiri berketerangan rumus.
- **Scorecard menampilkan SKOR TERSIMPAN walau posisi tak punya template otomasi** (⚠️ branch): gerbang `if (!templateId)` dulu membuang lebih dulu sehingga bulan yang sudah dinilai (mis. sebelum otomasi) tampil kosong. Kini skor tersimpan (`GET /kpi/score`) diutamakan tanpa butuh template; empty state "tanpa template" hanya saat BELUM dinilai DAN tanpa template. Dikunci `kpi-scorecard.test.tsx`.
- **Scorecard menampilkan HASIL DATA DARI SUMBER (breakdown komponen), bukan cuma skor** (⚠️ branch; bip-erp `5930c4dc` + erp-frontend `b1de1ffc`, deploy DEV 2026-08-21). Sumber `kinerja_toko` (`services/employee/kpi_sumber_kinerja_toko.go`) dulu hanya mengembalikan satu angka akhir; kini tiap `Cuplikan` mengisi **`Rincian`** dengan komponen sumbernya (display-only, TIDAK mengubah nilai/skor) yang diteruskan reduksi ke `auto_rincian`: `profit_bersih` → Settlement · HPP · Biaya iklan · Retur · Opex per ICC; `roas`/`roas_bersih` → Revenue (− Retur) · Biaya iklan; `retur_persen` → Retur · Revenue; `gross_profit` → Revenue · HPP. Frontend (`blueprint/kpi-scorecard.tsx`) merender rincian **INLINE** ("Dari data sumber") di kartu metrik, bukan lagi di balik dialog "Lihat rincian". Terverifikasi lewat gateway (ICC Kyura, Agustus 2026): Profit membawa Settlement Rp 31.965.281, HPP Rp 15.617.923, Biaya iklan Rp 16.449.640. Sesuai mockup blueprint "KPI Marketing".
- **Otomasi KPI (`/hris/kpi/otomasi`) dirombak jadi dashboard** (⚠️ branch): dari tabel diagnostik pasif jadi kartu **Cakupan otomasi** (donut distribusi status + persen terotomasi + persen bobot), **Kesehatan penilaian** (persen penilaian otomatis berhasil), panel **Perlu perhatian** yang bisa ditindak (metrik gagal-hitung/manual/tanpa-karyawan + tombol Sesuaikan), lalu tabel rincian per posisi tetap di bawah.
- **Persetujuan target KPI Marketing (Direktur) kini muncul di Ruang Direktur** (⚠️ branch): alur target berjenjang (Finance tetapkan total → SPV breakdown → Direktur setuju/tolak; endpoint `/kpi/target-marketing`, lihat [[Microservices - Employee Service]]) tadinya tak punya surface UI yang bisa diakses (`TargetMarketingView`/`PanelDirektur` orphaned). Ditambah antrean **"Target KPI Marketing"** di tab Persetujuan **Ruang Direktur** (`features/direktur/`), keputusan Setuju/Tolak di tempat, digerbang peran `direktur`/super-akses (Finance/SPV yang lolos GET tetap dikosongkan). Detail Ruang Direktur: [[APP - Web ERP]].
- **Pengisian nilai 100% manual**: `ApplyKPIValues` hanya menerima map `label → 0..100` dari body request; tidak ada jalur auto-fill dari service lain. Analisis kelayakan otomasi per metrik (data production 2026-07-31: 70 template, 311 metrik, 73 metrik sumber datanya sudah siap) ada di [[HRIS - Otomasi Skor KPI]].

## Konsep — appraisal bulanan menyeluruh (sebagian sudah — lihat bagian di atas)

Rancangan KPI/appraisal per-karyawan **semua departemen** (form bulanan diisi supervisor → diteruskan ke HR), **beda** dari engine insentif marketing di atas. Detailnya:

## Fitur

Dokumen ini seharusnya diisi oleh supervisor untuk setiap karyawan, namun hal tersebut tidak menutup kemungkinan bagi karyawan itu sendiri untuk mengisi dokumen lalu meminta review dari supervisor mereka masing-masing

Dokumen perlu diisi dan dikumpulkan pada hari pertama setiap bulan (kasus khusus jika hari pertama jatuh pada hari minggu/hari libur maka akan menjadi hari berikutnya)

Kami menginginkan cara yang mudah untuk mengisi catatan dan kalkulasi otomatis untuk setiap skor, di-review oleh supervisor lalu diteruskan langsung ke HR, di mana HR dapat melihat laporan seperti berikut:

- Dashboard
	- Ikhtisar segala hal yang sangat baik untuk pelaporan kepada stakeholder
	- Tampilan catatan per-departemen
	- Tampilan catatan per-orang
- Siklus form scoring key performance index
	- Ini adalah skor per-orang
	- Setiap departemen memiliki template scoring yang berbeda, namun sekali lagi, kita memerlukan standarisasi dokumen

## Detail yang Tertunda

- [ ] Informasi lebih lanjut tentang hal-hal yang mungkin mengubah payroll karyawan, contoh: overtime, insentif, asuransi

## Kebutuhan

- [ ] Master data karyawan (referensi lookup)
- [ ] Pembuatan form key performance index berdasarkan departemen (terhubung ke data karyawan)

## Dokumen Terkait

- **Implementasi**: [[Microservices - Insentive Service]] (engine KPI→insentif marketing) · [[Finance - Incentive]] · [[Sales - Incentive]]
- [[HRIS - Otomasi Skor KPI]] — analisis kelayakan mengisi skor otomatis dari data ERP (peta 311 metrik ke sumber datanya, modul yang ada tapi datanya kosong, dan rencana bertahap)
- [[HRIS - Work Review]] — penilaian kualitatif (KPI = sisi kuantitatif); pertimbangkan **berbagi satu Review Cycle** ketimbang sistem terpisah
- [[HRIS - Career & Promotion]] — masukan keputusan promosi
- [[HRIS - Analysis]] · [[HRIS - Big Pictures]] · [[HRIS - Interrelationship Matrices]]
