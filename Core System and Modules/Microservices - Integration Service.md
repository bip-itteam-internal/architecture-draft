# Microservices - Integration Service

## Deskripsi

*Integration Service adalah service integrasi marketplace e-commerce sekaligus bridge akuntansi. Service ini menghubungkan ERP ke marketplace Indonesia (TikTok Shop, TikTok Business/Ads, Shopee) — order masuk realtime via webhook **direct** per platform — lalu menormalkan order dan laporan iklan menjadi model "transaction" terpadu. Tujuan akhirnya adalah bridging finansial ke Accurate Online: membuat invoice, menyusun ringkasan transaksi harian per-shop/channel, serta melakukan push Sales Invoice dan Sales Return ke Accurate. Selain itu service ini menangani automasi auto-ship/auto-approve yang sadar-holiday, manajemen kredensial OAuth per marketplace, dan job sync terjadwal. Jalur middleware [[External - Desty]] **soft-disabled per 2026-07-12** (traffic mati; registrasi route/processor/task dicabut dari `main.go`, file kode tetap).*

- **Stack**: Go + Fiber v2 + MongoDB + Redis
- **Path**: `services/integration` (service terbesar)
- **Status**: ✅ Implemented (mostly) — masih ada bagian legacy yang di-disable dan target bridging non-Accurate belum tersedia
- **Referensi API**: dok endpoint vendor (129 endpoint, 7 modul: Auth, TikTok, Shopee, Transactions, Webhooks, Workers, Accurate) → [docs-api-greget.vercel.app](https://docs-api-greget.vercel.app/) (Nextra). Dibangun vendor **Greget**, telah diserahterimakan ke BIP (maintenance kini internal). ⚠️ **Angka 129 historis** (snapshot vendor); kode kini meregistrasi **≈214 rute** di `main.go` — daftar grounded terkini di [[API - Integration Service]].

## Endpoint / Fitur (Sudah Diimplementasikan)

### Webhooks
- `GET /webhooks/logs`, `GET /webhooks/logs/:id` — list & detail log webhook
- `POST /webhooks/logs/:id/retry` — retry pemrosesan webhook
- `GET /webhooks/tasks` — list task hasil webhook
- ~~`GET /webhooks/accounts/desty`~~ · ~~`POST /webhooks/services/desty`~~ — **dicabut 2026-07-12** ([[External - Desty]] soft-disabled; sekarang 404)
- `POST /webhooks/services/shopee` — ingest **Shopee Push Mechanism** (publik via gateway `/ext/webhook/shopee`). Auth **HMAC-SHA256** atas string `URL|body` lewat header `Authorization`; key dipilih per query `app_type` (`ads`/`order`/`marketing`/`one`/`affiliate` → `SHOPEE_ADS_WEBHOOK_KEY` / `SHOPEE_ORDER_WEBHOOK_KEY` / `SHOPEE_MARKETING_WEBHOOK_KEY` / `SHOPEE_ONE_WEBHOOK_KEY` / `SHOPEE_AFFILIATE_WEBHOOK_KEY`). ⚠️ Verifikasi signature **sementara di-bypass** — mismatch hanya di-log lalu tetap 200 (workaround bug Shopee console saat simpan key baru; `webhook_usecase.go`). Payload disimpan ke `webhook_logs` lalu diproses async oleh `ShopeePushProcessor` (create-on-missing — lihat *Observability & Ketahanan Shopee* di bawah).
- `POST /webhooks/services/tiktok` — ingest webhook **TikTok direct/push** (`TiktokDirectProcessor` — selalu tarik order detail dari TikTok, bisa buat order baru)

### Credentials & Holidays
- `POST /credentials` — simpan/registrasi kredensial integrasi
- `POST /holidays`, `GET /holidays`, `DELETE /holidays/:id` — manajemen hari libur untuk automasi auto-ship/auto-approve yang sadar-holiday

### TikTok Business / Ads
- OAuth: `/tiktok/business/auth` (+ callback)
- Advertisers: list advertiser
- Integrated Reports: `/report/integrated`, report daily/ad (+ sync), list, summary
- GMV Max: performance & product (direct + sync), summary, campaigns/items
- **GMV Max Monitoring**: `GET /tiktok/business/report/gmv_max/monitoring` — ranking performa per-campaign (GMV/ROI/orders/cost) lintas account & shop untuk dashboard marketing-insight, dengan flag **GMV winner** = ROI ≥ 3.2 & order ≥ 15 (aturan khusus ICC 2026 di [[Finance - Incentive]]; ambang `roi_threshold`/`min_orders` bisa di-override). Agregasi **menjumlahkan semua baris report** per campaign — termasuk bucket atribusi `item_id="-1"` yang **terpisah** (bukan rollup dari baris per-creative), sehingga total ROI realistis (~4) bukan menyesatkan (~42 bila pakai `-1` saja). Grounded: `tiktok_business_handler.go` (`ListGMVMaxMonitoring`), `usecase/gmv_max_monitoring.go`, `entity.GMVMaxCampaignMonitoringItem.ComputeDerived`. ⚠️ Baru — sudah di kode (unit test + tervalidasi data prod), **belum deploy**.
- **GMV Max Overview (per produk)**: `GET /tiktok/business/report/gmv_max/overview` — agregasi `tt_business_gmv_max_performance_reports` **group by `dimensions.item_group_id`** (buang `item_id="0"`) untuk dashboard **Product Performance** (marketing-insight). Per produk: spend (Σcost), revenue (Σgross_revenue), orders (Σorders), cost/order, **ROAS = rev/spend**, **ROI(%) = (rev−spend)/spend×100** (ad-level, exclude HPP), `no_spend`; totals (spend/revenue/orders/ROAS/product_count/store_count). Join nama produk `tt_business_store_products.title` + nama toko `tt_business_stores.store_name`. **SKU**: kumpulkan `item_id` kreatif (buang bucket `-1`) → `$lookup product_sku_mappings` via `tiktok_item_id` → set `master_sku` unik (`skus[]`; 1 produk = beberapa SKU, coverage ~74%). Query: `start_date`/`end_date` (wajib), `store_ids` (opsional JSON array; multi-toko didukung langsung di server, beda dari Shopee yang client-side). **Fix ROI**: sempat pakai `$avg(metrics.roi)` (rata-rata rasio per-row → ~558% ngaco), diperbaiki ke agregat dari total. Grounded: `tiktok_business_handler.go` (`GetGMVMaxOverview`), `tiktok_business_repo.go`. ⚠️ Baru — belum deploy.
- **ICC Video Metrics**: `GET /tiktok/business/insight/icc-video-metrics` — metrik per-video untuk insentif **ICC** (`ctr`, `watch25`, `roas`, `orders`, `post_date`, `creator`): agregasi GMV Max ad-creative + join `tt_business_campaign_items` (video↔creator) + join `tt_shop_video_performances` (post_date). Dikonsumsi service insentif via HTTP untuk `EvaluateICCVideoIncentive`/`IsICCVideoEligible` (lihat [[Finance - Incentive]]). Grounded: `tiktok_business_handler.go` (`ListICCVideoMetrics`), `entity.ICCVideoMetric.ComputeDerived`. ⚠️ Baru — di kode + tervalidasi prod (1.138 video), **belum deploy**; per-video ROAS diketahui kurang andal (atribusi biaya ke bucket campaign).
- Stores & Products
- **Cron master data** (`SyncMasterData`, setiap 00:00): sync campaigns + stores per advertiser. Sebelumnya kena rate limit TikTok (`40000: Request too frequent`) akibat burst concurrency (advertiser goroutine limit 5, campaign page limit 10). **Fix 2026-07-09**: tambah `getCampaignsWithRetry` (4 attempts, exponential backoff 5s→10s→20s + jitter ≤3s), turunkan concurrency advertiser 5→2 dan page 10→3. Campaigns wajib ada agar cron GMV Max 01:00 bisa berjalan (filter `store_id` dari campaigns).
- **`ListGMVMaxPerformanceReport`** — filter `advertiser_id` sebelumnya salah pakai field path `metrics.advertiser_id` (nested di sub-dokumen); **fix 2026-07-09** ke `advertiser_id` (field top-level di koleksi `tt_business_gmv_max_performance_reports`). Bug ini menyebabkan semua query performance per advertiser return 0 hasil di FE.
- `/sync/master-data` — sinkronisasi master data
- Accounts: CRUD

### Meta Ads

> 🟡 **Bagian ini KONSEP/RENCANA — belum ada kode.** Ditulis untuk grounding rencana implementasi client Meta Marketing API (pola ads/reporting seperti TikTok Business/Ads di atas, **bukan** pola order-sync Shopee/TikTok Shop). Update marker ke ✅ per sub-bagian begitu client Meta Ads nyata di-merge. Detail teknis di bawah bersumber dari dokumentasi publik Meta for Developers (`developers.facebook.com`) per Juli 2026 — **bukan dari kode BIP** — jadi wajib diverifikasi ulang sebelum implementasi (API vendor bisa berubah).

- **Kondisi saat ini**: `TIDAK ada client Meta Ads` di backend `services/integration`. Konsepnya sudah eksis di sisi lain: FE Finance/Incentive (`erp-frontend`) punya role `adv_meta`, `PLATFORMS = ["tiktok", "shopee", "meta"]`, setting "Meta Ads PPN (%)", dan Master Integration (mapping employee↔platform/campaign) — tapi semua **manual entry**, bukan API sync. [[Finance - Incentive]] §Arsitektur & Sumber Data eksplisit hanya menyebut `[[Sales - GMV Creative]]`/Dashboard TikTok sebagai sumber "Jumlah konversi" — Meta **tidak** disebut sebagai sumber otomatis sama sekali, konsisten dengan skema "ADV META DAN MARKETPLACE SHOPEE" di dokumen yang sama yang juga tidak punya jalur data otomatis.
- **Rencana App/Auth**: buat app tipe **Business** di Meta for Developers → hubungkan ke **Business Manager** Bharata → buat **System User** di Business Manager → assign ad account + permission (`ads_read` minimal, `ads_management` bila perlu tulis, `business_management` untuk kelola aset) → generate token System User (**tidak expired**, beda dari token user biasa yang cuma 60 hari). **App Review hanya perlu bila akses ad account di luar Business Manager Bharata sendiri** — untuk kebutuhan internal saat ini kemungkinan tidak perlu App Review sama sekali (lebih sederhana dari OAuth per-shop Shopee/TikTok). Detail langkah operasional: [[RUN - Onboarding Meta Ads]].
- **Kredensial**: rencana reuse entity generik `PlatformToken`/`Credential` yang sudah ada — komentar kode entity saat ini baru sebut "tiktokshop, shopee, lazada, ..." (belum sebut "meta" eksplisit, perlu ditambah saat implementasi) — **bukan** entity baru dari nol.
- **Data pull**: `GET /act_{ad_account_id}/insights` (fields `campaign_name`, `impressions`, `clicks`, `spend`, `actions`, `cost_per_action_type`) — pola mirip `/report/integrated` TikTok Business/Ads (laporan/reporting), **BUKAN** order sync. Tidak ada order/transaksi dari Meta, jadi **tidak** menyentuh `TransactionChannel`/`transaction_orders` sama sekali (beda dari Shopee/TikTok Shop/rencana Lazada sebelumnya).
- **Konsumen data**: `Microservices - Insentive Service` (skor KPI/konversi/CPA untuk skema insentif ADV Meta, lihat [[Finance - Incentive]]) — bukan model transaksi terpadu. Rekomendasi pola alur: Integration Service tarik data Insights dulu (satu pintu, konsisten pola TikTok GMV-Max) → baru dikonsumsi Insentif Service, alih-alih Insentif Service manggil Meta API langsung.
- **Rate limit**: mekanisme Meta berbasis tier akses (header `X-FB-Ads-Insights-Throttle`, `app_id_util_pct`/`acc_id_util_pct`), beda dari window-reset Shopee/Lazada. Tetap rekomendasikan pacing/circuit-breaker serupa pola ketahanan Shopee (lihat §Observability & Ketahanan Shopee di bawah) begitu jumlah ad account bertambah.
- **Open question (belum diputuskan)**: apakah data Insights Meta Ads perlu disimpan sebagai koleksi tersendiri di Integration Service (mirror `affiliate_orders`) sebelum dikonsumsi Insentif Service, atau cukup pass-through tanpa persist? Perlu keputusan eksplisit sebelum implementasi, bukan asumsi dokumen ini.

### TikTok Shop
- OAuth: `/auth` (+ callback), `/authorized-shops`
- **Model multi-account & multi-toko** — hierarki `1 open platform app → N credential → N authorized shop`:
  - **App tunggal**: `TIKTOK_SHOP_APP_KEY`/`TIKTOK_SHOP_APP_SECRET` dibaca dari env (`tiktok_usecase.go` ctor) — satu app TikTok Shop Open Platform dipakai semua akun. Konsekuensi: rate limit API dihitung per-app, makin banyak akun/toko makin cepat kena limit (analog kasus kuota Shopee di §Observability & Ketahanan Shopee).
  - **Credential per akun seller**: tiap otorisasi OAuth (`/auth/callback` → `ExchangeAccessToken`) membuat 1 dokumen `TiktokShopCredential` (`_id` UUID, name, access/refresh token + expiry, open_id) — 1 credential = 1 akun TikTok seller yang meng-authorize app.
  - **Shop per credential**: `GetAuthorizedShops` (API `/authorization/202309/shops`) mengembalikan array shops per credential; disimpan sebagai `TiktokShopAuthorizedShop` (id, name, region, cipher, `credential_id` sebagai link ke credential). Tiap call API per-toko memakai `shop_cipher` toko tsb.
  - **Cron multi-account otomatis**: task sync (orders, master data, affiliate) loop `ListCredentials` → per credential loop authorized shops — akun/toko baru cukup di-authorize via OAuth, tanpa perubahan kode/env.
- Orders: list/detail, direct, sync. `TransactionOrder` mencakup field `PackageID string` (diambil dari `PackageID` line-item pertama saat `TransformFromTiktok`) — dipropagasi ke `warehouseEventPayload` via `NotifyWarehouse` sehingga `fulfillment_orders` bisa menyimpan `package_id` untuk RTS TikTok.
- **Settlement per-order** (detail penyelesaian pembayaran): client `GetTransactionsByOrder` panggil TikTok **Finance API** `GET /finance/202501/orders/{order_id}/statement_transactions`. Ditarik saat sync order (`GetTransactionByOrderID` → fallback `GetTransactionsByOrder` bila belum ada), disimpan via `UpsertTransactionByOrder` ke entity `TiktokShopTransactionByOrder`, lalu ikut dikembalikan di response `/orders/detail` (field `transaction_orders`). Breakdown **per-SKU** mencakup semua komponen layar TikTok Shop: subtotal (`revenue_breakdown.subtotal_before_discount_amount` − `seller_discount_amount`), komisi platform (`fee.platform_commission_amount`/`referral_fee_amount`), ongkir (`shipping_cost_breakdown.*`), komisi afiliasi (`fee.affiliate_commission_amount` + partner/ads), komisi dinamis (co-funded/flash-sale/voucher-xtra fee), cashback bonus (`fee.bonus_cashback_service_fee_amount`), biaya pemrosesan (`fee.transaction_fee_amount`/`sfp_service_fee_amount`), dan `settlement_amount`. Transform `TransformToOrderIncome()` agregat ke `TransactionIncome` (revenue, service fee, shipping, discount, affiliate commission, settlement). **Catatan versi**: `202501` di-hardcode di client — verifikasi versi masih aktif sebelum diandalkan.
- GMV Winning Content: `GET /tiktok/shop/insight/gmv-winning-content` — daftar konten pemenang GMV (handle `ErrShopNotFound` → HTTP 400)
- **Shop Video Performance (persist)**: worker harian `sync-tt-shop-video-performance` (02:30) mem-persist performa video shop ke koleksi **`tt_shop_video_performances`** (video_id, creator, **post_date** dari `PublishTime`, views) — sumber **tanggal upload** (eligibility ICC 7–30 hari) & **hitung produksi video/creator/bulan**. Endpoint `GET /tiktok/shop/insight/creator-video-count?month=YYYY-MM`. Grounded: `usecase/icc_shop_video.go` (`SyncShopVideoPerformance`/`GetCreatorVideoCount`), `worker/tasks/tt_shop_video_performance_task.go`. ⚠️ Baru, **belum deploy** (koleksi terisi setelah worker jalan live; alignment `video_id` shop↔business dikonfirmasi pada sync pertama).
- Accounts: CRUD

### Affiliate (TikTok Seller)
- **Affiliate orders** — client `SearchSellerAffiliateOrders` panggil TikTok Affiliate Seller API `POST /affiliate_seller/202410/orders/search` (reuse `generateSign`/`buildQueryString`; versi **202410** terverifikasi live). Cron 8-jam tarik → koleksi `affiliate_orders` (grain order_id+sku_id, upsert idempotent), terpisah dari `transaction_orders` (link via order_id). Data per-SKU: creator_username, content_id/type, commission_model/rate, price, est/actual commission, settlement_status.
- **Dashboard API**: `GET /affiliate/orders` (list+filter+paginate, incl. `validation_status`) · `/affiliate/summary/{totals,creators,products,validation}` (agregasi Mongo untuk KPI + top creator/produk). FE: [[APP - Web ERP]] modul marketing-insight/affiliate. Detail keputusan: [[ADR - 0009 Affiliate via Search Seller Affiliate Orders API]].

### Shopee
- OAuth: `/auth` (+ callback) — per `account_type` (**ADS_SERVICE** / **ERP_SYSTEM** / **AMS**), lihat *Dual/Tri-app* di bawah
- **Affiliate (AMS)**: `GET /shopee/affiliate/performance` (per-affiliate, **baca DB**) + `POST /shopee/affiliate/sync` (backfill manual) + `GET /shopee/affiliate/recommended` (proxy live) — via app AMS, lihat *AMS affiliate* di *Observability & Ketahanan Shopee*
- **Affiliate v2 (AMS order-level)**: `GET /shopee/affiliate/conversions` (ledger order+item, **baca DB**, rollup per-affiliate) + `POST /shopee/affiliate/conversions/sync` (background) + `GET /shopee/affiliate/validations` (tagihan komisi bulanan + rekonsiliasi) + `POST /shopee/affiliate/validations/sync` (background) — lihat *AMS affiliate v2* di *Observability & Ketahanan Shopee*
- Orders: list, v2 list, detail, **sync (chunked ≤15 hari, mendukung histori ~3 bulan)**, **escrow backfill** (`/orders/escrow/backfill`) — lihat *Manual order sync histori panjang* di *Observability & Ketahanan Shopee*
- **Returns/Refund ingestion**: `GET /shopee/returns/sync` (chunked ≤15 hari) + `GET /shopee/returns/list` — tarik return/refund (item **parsial**, **tanggal proses**, refund) via Returns API, lihat *Shopee Returns ingestion* di *Observability & Ketahanan Shopee*
- Shops
- Products / Items
- GMS Analytics: item & campaign performance (+ sync + summary)
- Accounts: CRUD
- **Filter Toko & Client-Side Aggregation (Shopee vs TikTok GMV Max)**: Berbeda dengan TikTok GMV Max yang mendukung filter `store_ids` (array opsional) untuk agregasi lintas-toko secara native di level server/MongoDB, seluruh endpoint Shopee GMS (`/shopee/gms/*`) dan Shopee Affiliate (`/shopee/affiliate/*`) **mewajibkan parameter `shop_id` tunggal** (`shop_id > 0`). Untuk mendukung fitur filter **"All Shop"** di frontend (ERP Web), aplikasi menggunakan strategi **Client-Side Aggregation**: frontend secara paralel mengeksekusi request (`Promise.all`) ke seluruh `shop_id` yang terotorisasi dan menggabungkan hasilnya secara live di browser. Strategi ini menghindari kompleksitas perombakan skema query `$match` dan kontrak interface di puluhan repositori backend yang berisiko memicu *silent bugs*.

### Transactions (model terpadu)
- `/transactions/orders/list`, summary (+ shops / + products), `/transactions/orders/:id`
- **Export Order Management**: `GET /transactions/orders/export` — stream `.xlsx` (1 baris per item) untuk daftar order; filter identik `/orders/list` (channel/status/canceled_by/time range/shop_id/order_id), tanpa paginasi (cap 10.000 order). Handler `ExportTransactionOrders` (`transaction_handler.go`) → `writeExcelExport` (`excel_export.go`); nama file dibangun `buildOrdersExportFilename` dari filter aktif. FE: [[APP - Web ERP]] tombol Export di halaman Order Management (butuh gateway CORS `ExposeHeaders: Content-Disposition` agar nama file terbaca browser lintas-origin)
- `GET /transactions/returns` — **feed retur lintas-channel (Shopee + TikTok)** untuk WMS gudang, satu baris per event retur. Diseleksi lewat **keberadaan sub-dok `return`** (`TransactionOrderFilter.HasReturn` → `{"return": {$exists, $ne: null}}`), **bukan** `status=RETURNED` — [[ADR - 0022 Retur via Sales Return per Mode + Keep Invoice Line]] membuktikan 39% retur penuh tetap `COMPLETED`. **Sejak 22 Juli 2026 feed JUGA menyertakan order CANCELLED pasca-kirim** (COD gagal — barang fisik balik tapi TAK punya objek retur marketplace): filter `IncludeCancelledReturn` memperluas query jadi `$or` (sub-return ADA **atau** `CANCELLED` + `shipped_at` ada), dan `buildReturnRows` memakai `usecase.EffectiveReturn` (sintesis retur dari `order.Items` — **fungsi yang SAMA** dgn jalur pembukuan, bukan salinan). **Kenapa kritis:** tanpa ini 17.257 order batal tak satu pun muncul di feed, sehingga gudang tak bisa menerimanya — padahal gerbang gudang (di bawah) menahan retur cancel PENDING **menunggu gudang scan**. Feed buta = retur cancel macet PENDING selamanya. Cancel PRA-kirim (`shipped_at` nil) tetap dikecualikan (barang tak pernah keluar). Menyatukan kedua channel yang datanya asimetris: Shopee punya koleksi `shopee_returns`, TikTok **tidak** (hanya `transaction_orders.return` via `FetchAndSetTikTokReturn`). Konsumen: manufacture `GET /returns` (lihat [[API - Manufacture Service]]). **Tidak** ter-gate `RTS_RETURN_ENABLED` — rem itu hanya menahan booking Accurate, sub-dok `return` tetap terisi. **Resi parsel BALIK (22 Juli 2026)**: baris feed kini membawa `reverse_tracking_no` (+ `reverse_logistics_status`, `reverse_channel`) — nomor pada LABEL paket retur yang diterima gudang, BEDA dari resi FORWARD di Master Resi. Sumber Shopee = **`returns.get_return_detail.tracking_number`** (bukan `get_reverse_tracking_info`): terbukti empiris 22 Juli lebih andal — `get_return_detail` **tak pernah error** & memberi resi bahkan utk kurir NON-INTEGRATED (mis. JNE) yang bikin `get_reverse_tracking_info` gagal (biang ~44% "Fail Call" Shopee di dashboard — perilaku wajar utk refund-only/Return-on-Spot/non-integrated, bukan bug). Diisi saat ingestion (`SyncReturnBySN` → `transformClientReturn` → `TransactionReturn.ReverseTrackingNo`); retur pra-fitur di-backfill `cmd/returnresibackfill` (Juni+Juli: 168 diperiksa, 62 dapat resi, targeted `$set` tanpa efek samping). Ini yang memungkinkan gudang **men-scan paket balik** & mencocokkannya langsung ke retur (resi balik tak ada di Master Resi).
  - **Status pembukuan Accurate per baris (`accurate_booking`, ✅ 2026-07-22)** — ditambahkan setelah insiden operator gudang "resi tak bisa di-approve" (JY1079597252/JX9597522110): keduanya ADA & valid, tapi salah satu **sudah terbukukan** (era sebelum gerbang gudang) dan satu lagi **pra-cutover** (kirim < `autoSyncReturnCutoverDateWIB`, sengaja di luar Accurate) — bukan gap API seperti dugaan awal. Tiap baris kini membawa `accurate_booking` (`"booked"` / `"pending"` / `"pre_cutover"` / kosong) + `return_number`, dihitung `usecase.BookingStateByOrderIDs` (satu query batch `GetByMemberOrderIDs` ke `accurate_daily_returns` per order_id yang dikirim, memetakan tiap MEMBER grup — bukan cuma order representatif — supaya order ke-N dalam grup bundle tetap ketemu) dan pra-cutover diturunkan dari `shipped_at` vs cutover. Dipakai manufacture untuk mode **catatan-saja**: retur `booked`/`pre_cutover` tetap bisa dicatat kondisinya (dashboard + stok WMS) tapi TIDAK memicu POST `warehouse-confirm` (lihat [[Microservices - Manufacture Service]]). Grounded: `usecase/accurate_rts_usecase.go` (`BookingStateByOrderIDs`, `ReturnBookingState`), `domain/repository/accurate_daily_return_repository.go` (`GetByMemberOrderIDs`), `interface/http/transaction_return_handler.go` (`buildReturnRows`).
- **Packet Tracking (checkpoint pengiriman + deteksi mandek + retur)** ✅ (2026-07-17..18) — modul pelacakan paket forward (gudang→buyer) & retur (buyer→gudang), sumber data checkpoint API resmi marketplace (bukan pihak-3). Sub-dokumen di `transaction_orders`: **`tracking`** (forward: `events[]{time,status,raw_code,description}` cap 50, `status`/`tracking_no`/`provider`/`last_event_at`/`final_synced`), **`stuck`** (flag mandek: `is_stuck`/`stage`/`reason`/`since`/`cleared_at`), **`return_tracking`** (retur: `events[]`/`status`/`raw_status`/`tracking_no`/`provider`/`requested_at`/`has_checkpoint`). Normalisasi pure-func di `internal/domain/tracking/` (`normalize.go`+`detect.go`, unit-test).
  - **Sumber checkpoint (verified live via harness + Playwright)**: **forward** Shopee `GET /api/v2/logistics/get_tracking_info` (update_time **detik**, enum `logistics_status`) & TikTok `GET /fulfillment/202309/orders/{id}/tracking` (**action_code int** + description berlokasi, update_time_millis **MILIdetik**; endpoint versi **202309**, 202604 out-of-scope; WAJIB kirim `shop_cipher`). **retur** Shopee `get_reverse_tracking_info` (checkpoint hub penuh) — TikTok **TAK ada checkpoint hub**, hanya `Search Returns` (`return_tracking_number`+`return_provider_name`+`create_time`+milestone `return_status`); checkpoint lokasi retur TikTok hanya via lacak resi ke API kurir (di luar scope). Enum action_code TikTok dinormalisasi dari data prod (`50101`=DELIVERED — sempat salah FAILED; `40601/40801`=FAILED; `70xxx/80101`=RETURNED; unknown→UNKNOWN, `raw_code` disimpan → remap tanpa re-sync).
  - **Kredensial TikTok**: `ResolveTrackingCreds` reuse jalur `ListAuthorizedShops→GetAccessToken(CredentialID)` + `shop.Cipher` + env `app_key/secret` (SAMA dgn jalur retur; koleksi `credentials`/`platform_tokens` KOSONG — jebakan "get app_key: no documents" yang sama).
  - **Endpoint**: `GET /transactions/orders/tracking` (list, filter `date_from`/`date_to` WAJIB + `status`/`marketplace`/`tracking_status`/`provider`/`stuck`/`has_return`/`search`(resi/order)/`min_age_hours`(umur diam sejak `last_event_at`) + pagination; **TO_PROCESS & TO_SHIP di-exclude** — belum pasti dikirim, bisa cancel), `GET /transactions/orders/tracking/stats` ($facet agregasi by status/tracking_status/provider + stuck/return total), `POST /transactions/orders/tracking/backfill?date_from&date_to` (async fire-and-forget, loop batch 500 utk isi backlog historis; goroutine tak survive restart → re-trigger via `BIP-Gateway-ID`+`BIP-System-Roles`).
  - **Cron** (`worker/tasks/`): `sync-tracking` (30 mnt; kandidat non-terminal belum DELIVERED + final-sync 1× saat COMPLETED + enrich resi/provider via `ResiByOrderIDs`), `detect-stuck` (jam:15; **hanya SHIPPED/IN_TRANSIT**, window `order_date ≥ now-14hr` + `ClearStuckOlderThan`; threshold per-stage config KV `stuck_thresholds` default 24/48j), `sync-return-tracking` (jam:20; SELALU tulis `LastSyncAt` bahkan saat fetch gagal → backoff, cegah re-sync storm refund-only). Rate-limiter terpisah forward/retur per-MP. **Anti-penalti Shopee (✅ 2026-07-25)**: `get_reverse_tracking_info` gagal utk retur TANPA logistik-balik (refund-only/In-transit RR/Return-on-Spot/non-integrated) — dulu di-retry TIAP JAM karena `final_synced` hanya di-set bila status retur final, padahal mayoritas ACCEPTED (tak final) → terukur prod **57,7% success, 2.788 gagal/hari** (98,7% dari SEMUA Fail Call app) → penalti error-rate Shopee. Fix: (a) **refund-only (`solution=1`) di-SKIP total** — tak panggil API sama sekali (`fetchShopeeReturn` set `final_synced` + `raw_status=refund_only_no_reverse`); (b) error "no reverse logistics" dibungkus sentinel `clients.ErrShopeeNoReverseLogistics` → `SyncReturnTracking` set `final_synced=true` (kondisi STABIL, retry sia-sia). Reverse resi tetap dari `get_return_detail` (jalur sync retur) — tak ada data hilang; hanya timeline checkpoint retur tanpa-logistik-balik yang dilewati. Terverifikasi live: 58 refund-only langsung terkuras dari pool re-fetch. Grounded: `usecase/tracking_usecase.go`, `clients/shopee_returns_client.go` (`reverseTrackingErr` + `_test.go`).
  - **Backfill 1 Jun–17 Jul SELESAI ~99%** (129.232/130.126; sisa ~894 order lama gagal-fetch permanen — shop tak authorized / detail MP kosong). FE: [[APP - Web ERP]] halaman `/integration/packet-tracking` (list+detail timeline+retur; asal retur = `buyer.city/province` TikTok tak-mask / hub checkpoint Shopee yang di-mask `****`).
- ⚠️ **Auto-arrange (Gap 2, pengganti auto-ship Desty)** — worker **`auto-arrange`** (`worker/tasks/auto_arrange.go`, tiap 15 mnt, MongoDB lock `lock:auto-arrange`) yang mengambil alih fungsi "atur pengiriman" Desty: per channel cek gerbang window sadar-holiday (`arrange_window.go`, reuse aturan `shouldSkipAutoApprove`, **fail-closed** saat holiday-check error) → cari order **siap-diatur-kirim** (`raw_status ∈ {AWAITING_SHIPMENT, READY_TO_SHIP, RETRY_SHIP}`, termasuk **COD**; umur ≤7 hari; cap 200/run) via `FindArrangeCandidates` → `ShipBatch` (TO_PROCESS→TO_SHIP) → set `arrange_status=arranged`, atau `arrange_attempts++` → 3× → `needs_review`+Telegram batch. Order jadi TO_SHIP → masuk WMS. ✅ **Celah "arrange sendiri tak sampai ke WMS" diperbaiki 2026-07-24** (PR #638) — `ShipBatch` kini mendorong order ke WMS lewat dua jalur lepas-webhook, jadi worker dorman ini otomatis ikut terperbaiki (memakai `ShipBatch` yang sama); lihat catatan di *Jembatan WMS "Perlu Diproses"*. `AUTO_ARRANGE_ENABLED` tetap default off — dinyalakan hanya saat cutover. **Field baru `transaction_orders`**: `raw_status` (status mentah MP, diisi `Upsert` saat sync — pre-warm), `arrange_status`, `arrange_attempts`. Index `channel_raw_status_order_date`. **⚠️ DORMANT** di balik env **`AUTO_ARRANGE_ENABLED`** (default off) — task hanya didaftarkan bila `true`; nyalakan hanya saat **cutover Desty→WMS**. Follow-up ditunda: TikTok "already arranged"→success belum dipetakan (bounded noise). Grounded: `usecase/auto_arrange_usecase.go`, `usecase/arrange_window.go`. Lihat [[External - Desty]] & [[IT - Background Jobs & Schedulers]].
- **Jembatan WMS "Perlu Diproses"** — `GET /fulfillment/pending-arrange`: daftar order **belum berresi** (`raw_status ∈ {AWAITING_SHIPMENT, READY_TO_SHIP, RETRY_SHIP}` **AND** `arrange_status ≠ arranged`), urut `order_date` ASC, ber-paginasi (limit default 50 / maks 200), filter `shop_ids`/`date_from`/`date_to` (WIB, `date_to` inklusif; format tak dikenal → 400)/`q` (regex ke `order_id`, `items.name`, `items.sku`; nilai di-`QuoteMeta`). **Respons di-projection** hanya field yang dibutuhkan gudang (tanpa `buyer` PII, `income`, `tracking`). Index pendukung: `raw_status_order_date`. Dipakai menu WMS lewat proxy warehouse-service — lihat [[Microservices - Warehouse Service]].
  - ✅ **`raw_status` Shopee kini disegarkan di jalur push webhook (ditemukan & DIPERBAIKI 2026-07-24, PR #638 / commit `14b9795c`).** **Gejala (sebelum fix):** `ShopeePushProcessor` memperbarui status lewat `order.Status = newStatus` + `orderRepo.Update()` — jalur ini **tidak** melewati `TransformFromShopee`, sehingga `raw_status` tertinggal di nilai lama (beda dari `TiktokDirectProcessor` yang memakai `Transform`+`Upsert`). Order Shopee yang sudah ter-arrange tetap ber-`raw_status = READY_TO_SHIP` dan **tetap lolos filter "Perlu Diproses"** padahal sudah ada di WMS. Terukur 24 Juli 2026: **198 order** Shopee `status=TO_SHIP` **AND** `raw_status=READY_TO_SHIP` (8/8 sampel dikonfirmasi sudah di `fulfillment_orders`); nol order ber-`raw_status = PROCESSED`. Menggelembungkan daftar "Perlu Diproses" Shopee ~198 dibanding Desty. **Perbaikan:** tambah `order.RawStatus = payload.Data.Status` bersebelahan dengan `order.Status = newStatus` (`shopee_push.go`) — kelas bug identik dengan `raw_status`/`package_id` yang hilang dari `setFields` `Upsert` (diperbaiki 2026-07-23). Regresi dijaga `shopee_push_test.go` (`TestShopeePushMenyegarkanRawStatus`). **Order yang telanjur** (198 hantu) belum di-backfill — perlu tandai `arrange_status=arranged` (lihat *pemulihan data* pasca-deploy).
  - ⚠️ **`ShipBatch` kini juga menandai `arrange_status`**: `arranged` saat sukses, dan `arrange_attempts++` saat gagal (keduanya best-effort, tak memengaruhi hasil). Ini **efek samping pada endpoint lama** — jalur `POST /fulfillment/rts` dari warehouse ikut menandai. Dinilai benar secara semantik (RTS memang meng-arrange) dan hanya dibaca oleh filter "Perlu Diproses" + scheduler dorman.
  - ✅ **Guard urutan webhook Shopee — `UpdateIfNewer` watermark (DIPERBAIKI 2026-07-27, PR #710).** **Gejala (sebelum fix):** `ShopeePushProcessor` menerapkan status push **secara literal** (`order.Status = newStatus` → `orderRepo.Update()`, 0 API) — beda dari `TiktokDirectProcessor` yang **selalu re-fetch** order detail (self-correcting). Push Shopee yang tiba **out-of-order** (dispatcher 5 worker konkuren) menimpa status terbaru dengan status basi. Terukur 27 Jul 2026: **12 order** Shopee kembali `TO_SHIP` setelah sudah `SHIPPED`. **Perbaikan:** `orderRepo.Update()` di jalur push → **`UpdateIfNewer`** — tulis bersyarat watermark: hanya menimpa bila `order_update_date` existing **≤** event masuk **ATAU** null/absen (order pra-guard tetap boleh diisi; event sama-`update_time` tetap diterapkan agar refresh `raw_status` di atas **tidak regresi**); `MatchedCount=0` → `applied=false`. Bila basi → **lewati semua efek samping** (`NotifyWarehouse`, sync Accurate, hook cancel/return) & balas 200. Insert history di-*gate* oleh `applied` (tak ada baris timeline palsu). **TikTok tak perlu guard** (self-correcting via re-fetch). Regresi dijaga `shopee_push_test.go` (`TestShopeePushGuardEventBasi`) + `transaction_update_if_newer_test.go` (`updateIfNewerFilter`). **12 order basi** dikoreksi pasca-deploy via trigger guardian `sync-shopee-orders`. Grounded: `webhook/processor/shopee_push.go`, `infrastructure/repository/transaction_repo.go` (`UpdateIfNewer`, `updateIfNewerFilter`), `domain/repository/transaction_repository.go`.
  - ✅ **Order hasil arrange sendiri kini didorong ke WMS (ditemukan & DIPERBAIKI 2026-07-24, PR #638 / commit `14b9795c`).** **Gejala (sebelum fix):** `ShipBatch` tidak memanggil `NotifyWarehouse`; satu-satunya jalur masuk WMS adalah webhook marketplace. 2 order TikTok (`585164962491827677`, `585164975041185551`) sukses arrange (resi terbit, terverifikasi di Desty) tapi **nol webhook** sesudahnya ≥35 menit, tak pernah masuk `fulfillment_orders`. Toko yang sama menerima 153 webhook `AWAITING_COLLECTION`/24 jam dari arrange lewat Desty — jadi hipotesis kuat (2/2 sampel, belum dikonfirmasi dok TikTok): **marketplace tidak menggemakan balik perubahan ke aplikasi yang menyebabkannya sendiri**. **Perbaikan (tidak bergantung pada hipotesis itu):** `ShipBatch` kini memanggil `markAndPublishArranged` yang menghidupkan **dua jalur lepas-webhook** — **(a) tarik:** `MarkArranged` mengubah `status`→`TO_SHIP` **dan** menaikkan `order_update_date` (agar reconciler 60 detik menemukannya; tanpa menaikkan watermark order tetap di bawah cursor — kelas bug `resi_wms_safety_net_task.go`), **(b) dorong:** `NotifyWarehouse` async (goroutine + `context.Background()`, BUKAN sinkron — 100 order × timeout 5 dtk akan melampaui anggaran 5 mnt warehouse). Bila (b) gagal, (a) menangkap dalam 60 detik. **Penjagaan:** `MarkArranged` hanya memajukan status bila order masih pra-kirim (`TO_PROCESS`/`TO_SHIP`) via pipeline update atomik — daftar "Perlu Diproses" disaring dari `raw_status` yang bisa basi, sehingga order `CANCELLED`/`SHIPPED` bisa ikut tampil (terukur 4 order CANCELLED); tanpa penjagaan ini sekali klik membangkitkan order mati ke antrian gudang. `raw_status` **tidak** disentuh (status mentah baru tak diketahui tanpa menarik ulang). **Hook dipindah** dari `webhook/processor/warehouse_hook.go` ke paket netral `internal/warehousehook/hook.go` — `processor` meng-import `usecase`, jadi tanpa pemindahan `usecase` tak bisa memanggil `NotifyWarehouse` (circular import). **Terverifikasi live:** order TikTok di-arrange lewat menu → muncul di "Pesanan Baru" dalam <60 detik tanpa webhook marketplace. Worker `auto-arrange` dorman ikut terperbaiki (memakai `ShipBatch` yang sama). Grounded: `usecase/warehouse_bridge_usecase.go` (`markAndPublishArranged`, `asyncNotifyWarehouse`), `infrastructure/repository/transaction_repo.go` (`MarkArranged`), `internal/warehousehook/hook.go`.
- Master: `/master/shops`, `/master/channels`, `/master/status`
- Summary Reports: list, `POST` trigger, group-by-status, get, items, **invoices** (`/summary/reports/:id/invoices`), retry, delete
- `POST /summary/reports/:id/send/:service` — kirim ringkasan ke Accurate
- **Filter tanggal Summary Report (`start_time`/`end_time`)**: order yang di-include ke summary ditentukan lewat `buildTransactionOrdersWithDateRangeStatusFilter` (`transaction_repo.go`), dipanggil dari `TriggerSummaryReport` (pre-check) dan `CreateSummaryReport` (generate async via Redis queue), keduanya di `transaction_usecase.go`. **Kini unconditional pakai satu field `rts_time`** untuk SEMUA kombinasi channel × report type (Sales/Income/Return, TikTok & Shopee) — mapping per-status lama (`shipped_at`/`completed_at`/`returned_at`/`cancelled_at`) sudah tidak dipakai lagi untuk filter tanggal ini. Sumber `rts_time` beda per channel (`TransformFromTiktok`/`TransformFromShopee`, `transaction.go`): **TikTok** = field native API `rts_time` ("ready to ship time", `TiktokShopOrderDetail.RTSTime`), dengan fallback ke `UpdateTime` untuk status TO_SHIP tanpa `CollectionTime`; **Shopee** = `pay_time` (waktu pembayaran) — jadi `rts_time` Shopee secara efektif = waktu bayar, `rts_time` TikTok = waktu siap-kirim (dua semantik berbeda disatukan di satu nama field). Backfill `paid_at` order Shopee historis: `cmd/paidatbackfill` (DB-only, join dari `pay_time` di `shopee_order_details`).
  ⚠️ **Catatan/gap**: `TransactionOrderFilter.FilterByPaidAt` + helper usecase `shouldFilterOrdersByPaidAt` (awalnya dibuat utk scope Shopee+SALES_INVOICE saja) masih di-set di `TriggerSummaryReport`/`CreateSummaryReport` tapi **sudah tidak dibaca lagi** oleh `buildTransactionOrdersWithDateRangeStatusFilter` — dead code, belum dibersihkan per observasi terakhir. Entity `TransactionOrder.PaidAt` juga masih ada terpisah dari `RTSTime` (untuk Shopee keduanya diisi nilai yang sama). FE [[APP - Web ERP]] halaman `integration-accurate/sales/shopee`: label "Payment Date" pada modal Create Summary & kolom tabel saat ini masih di-gate `channel==SHOPEE && reportType==SALES_INVOICE` — perlu diverifikasi ulang apakah masih sesuai setelah generalisasi `rts_time` ini (TBD, belum dicek ulang sejak perubahan backend).
- **Income reporting**: agregasi income invoice + field income pada transaction order (laporan pemasukan)
- **Demography Insight**: `GET /transactions/insight/demography` — insight demografi dari transaksi
- **Status history**: pelacakan riwayat status (status history) transaction order
- **Revenue Comparison**: `GET /transactions/orders/summary/comparison` — perbandingan performa dua periode kustom. Query param: `start_date`/`end_date` (periode aktif, YYYY-MM-DD, default hari ini), `comparison_start`/`comparison_end` (periode pembanding, default kemarin), `channel` (SHOPEE|TIKTOK, **opsional** — omit untuk semua channel), `shop_id`, `timezone` (IANA, default `Asia/Jakarta`). Granularity otomatis: `hourly` (rentang 1 hari → 24 slot label `HH:00`) atau `daily` (multi-hari → 1 slot per hari label `DD/MM`). Respons: `current`, `previous` (masing-masing `total_revenue`, `total_orders` TO_SHIP; `total_completed_revenue`, `total_completed_orders`, `total_products` COMPLETED; + array `slots`), `comparison` (% perubahan ke-5 metrik), `granularity`. Dua query MongoDB dijalankan **concurrent** via `errgroup` (TO_SHIP + COMPLETED).
- **Dashboard Status Summary**: `GET /transactions/orders/dashboard/summary` — jumlah order aktif saat ini **tanpa filter tanggal** (semua order di DB), dikelompokkan per kategori kartu dashboard. Param: `channel`, `shop_id` (opsional). Respons: `pesanan_baru` (TO_PROCESS), `siap_dikirim` (TO_SHIP), `belum_di_proses` (TO_PROCESS + TO_SHIP), `pesanan_selesai` (COMPLETED).

- 🟡 **Kas Toko (wallet marketplace)** — *branch `feat/kas-toko` (PR #380), belum merge*: sync **penarikan dana TikTok** (`GET /finance/202309/withdrawals`, types WAJIB comma-joined satu param — repeated gagal sign 106001) + **mutasi wallet Shopee** (`get_wallet_transaction_list`, window sync 14 hari — docs bilang max 15 tapi API prod menolak 15 hari persis (`wallet.time_invalid`, PR #393)) → koleksi `tt_withdrawals`, `shopee_wallet_transactions` (unique `(shop_id, transaction_id)`), `wallet_sync_state`, `wallet_opening_balances`. Worker **`wallet-sync` harian 07:45 WIB**: incremental (overlap 24 jam) + **re-poll baris non-final** (status berubah in-place tanpa ubah `create_time` — tanpa re-poll status menggantung selamanya) + guard infinite-loop pagination + fail-streak per toko (alert Telegram ≥3, cancel-aware). One-off `cmd/walletbackfill` (default sejak 2026-01-01, `-count-only` dry-run) & harness `cmd/wallettest`. **Fakta probe live yang MENGOREKSI docs resmi** (lihat `bip-erp/docs/superpowers/plans/wallettest-findings.md`): `transaction_type` Shopee = STRING (bukan kode numerik); mutasi saldo penarikan di baris `WITHDRAWAL_CREATED` (negatif), baris `COMPLETED` amount=0; amount TikTok string selalu positif (arah dari `type`); `current_balance` = saldo sesudah mutasi. Endpoint `/wallet/*` (7 rute — lihat [[API - Integration Service]]); saldo TikTok = estimasi anchor+Σ (anchor via `PUT /wallet/opening-balance/:shopId`), saldo Shopee aktual dari `current_balance`; laporan rekonsiliasi uang masuk per toko basis `income.paid_at` (cross-periode) + export Excel. FE: menu **Kas Toko** 3 tab di erp-frontend (branch `feat/kas-toko`, belum merge). Konteks bisnis: blueprint finance "Uang Masuk" (settle → kas toko → penarikan → bank).

### Accurate
- Shop Settings: CRUD (`/accurate/settings/shops`)
- **Product Management**: CRUD `/accurate/products` + validasi item (mapping produk Accurate)
- **Bank Account**: CRUD `/accurate/bank-accounts`
- **KV Config** (key-value): CRUD `/accurate/settings/kv-configs`
- **OAuth Connect Accurate (⚠️ 2026-07-14, belum live e2e)** — admin menghubungkan Accurate via OAuth dari UI ([[APP - Web ERP]] tab `integration/oauth`, kartu status tunggal) alih-alih menempel token manual ke `.env`. Endpoint `/accurate/{auth,auth/callback,connection,disconnect}` (admin-only `RequireIntegrationAdmin`): tukar `code`→token **server-side** (`client_secret` env) → `db-list.do`/`open-db.do` pilih database → simpan kredensial **AES-encrypted** di koleksi `accurate_credentials` (single doc) + nonce CSRF `accurate_oauth_states` (TTL). `AccurateClient` memakai token DB (auto-refresh near-expiry, mutex) via `WithTokenSource`; **fallback env** (`ACCURATE_BEARER_TOKEN`+`ACCURATE_DB_ID`) bila belum connect / DB blip → prod aman. Access_token OAuth = UUID → butuh session (open-db + `X-Session-ID`; `ACCURATE_ACCOUNT_URL` wajib `/accurate`). Keputusan [[ADR - 0014 Accurate Token DB-backed via OAuth]]; prosedur [[RUN - Accurate API Access Token (OAuth)]]. **Ops:** daftarkan `ACCURATE_REDIRECT_URI` = URL FE.
- **Auto-Sync → Accurate (✅ live 2026-07-10/11, PR #328–#338)** — order yang bertransisi ke `SHIPPED` (Shopee `SHIPPED` / TikTok `IN_TRANSIT`, **dibawa kurir**) otomatis di-push sebagai **Sales Invoice** ke Accurate tanpa tombol (keputusan #338: trigger pindah dari TO_SHIP ke SHIPPED — TO_SHIP masih bisa batal → koreksi minim; stok Accurate berkurang saat barang benar-benar keluar):
  - **Trigger**: hook async (goroutine + `recover`) di webhook processor (`shopee_push.go`, `tiktok_direct.go`) **dan jalur cron sync** (`tiktok_usecase.go`, `shopee_new_usecase.go` — mayoritas transisi status nyatanya lewat sync batch, bukan webhook) setelah persist order berstatus `SHIPPED`; `shipped_at` terisi dari `UpdateTime` (Shopee) / `CollectionTime` (TikTok); order yang lompat langsung ke `SHIPPED`/`COMPLETED` via cron sync juga terisi (fix PR #360: `TransformFromShopee` case SHIPPED & COMPLETED + guard TikTok diperluas ke ToShip/Shipped/Completed — tanpa ini order tak masuk snapshot faktur; terukur 258 order Shopee/hari lolos sebelum fix). **Hook completed (PR #363)**: order yang lompat `TO_SHIP → COMPLETED` antar-interval sync (tak pernah terlihat SHIPPED — terukur 83 order Shopee 2026-07-12, 0 faktur terbentuk; sweep 22:00 tak menolong karena hanya menyapu baris yang sudah ada) memicu `OnOrderCompleted` di kedua jalur cron sync → delegasi ke `OnOrderToShip` dgn 2 guard: hari lampau tanpa baris `accurate_daily_invoices` di-skip (anti-dobel dgn faktur manual finance era pra-fitur), dan snapshot unchanged (hash lokal `lines_hash`, nol call Accurate) di-skip; order `shipped_at` null di-skip. **Status TO_CONFIRM_RECEIVE (PR #378, temuan rekonsiliasi finance 2026-07-13)**: switch status Shopee hanya menangani typo `TO_COMFIRM_RECEIVE` → status resmi `TO_CONFIRM_RECEIVE` jatuh ke UNDEFINED dan ordernya hilang dari snapshot faktur (terukur 854 order; 3 di antaranya terbukti hilang dari INV/2026/07/11/001-BH). Fix: kedua ejaan diterima → SHIPPED; repair data = updateMany UNDEFINED→SHIPPED + retry faktur terdampak. **Freeze shipped_at (PR #367)**: `shipped_at` pertama yang tercatat = anchor hari faktur dan TIDAK ditimpa observasi berikutnya (`preserveShippedAt` di `Upsert` repo transaksi) — sebelum fix, transform Shopee men-set ShippedAt=UpdateTime tiap sync sehingga order shipped→completed lintas hari "pindah hari" faktur & dobel (terukur 84 order Shopee 2026-07-12; 2 faktur 11 Jul jadi snapshot kosong → isi lama di Accurate tak ter-edit). **SKU varian Shopee (PR #415, temuan finance 2026-07-14)**: transform memakai `item_sku` listing — listing generik "BundlingIsi2" bervarian beda komposisi (`model_sku` PJG-002+PJG-004 dst) → SKU asing terkirim, 5 faktur KY+GB FAILED berhari-hari. Fix: `model_sku` (varian) diprioritaskan, fallback `item_sku`; audit 4rb order = satu-satunya pasangan beda memang BundlingIsi2. **Guard webhook (PR #409, temuan tracking user 2026-07-14)**: case SHIPPED processor Shopee mencakup event paket-SAMPAI (TO_CONFIRM_RECEIVE) dan menimpa shipped_at (freeze #367 hanya di jalur Upsert sync) → order pindah ke faktur hari sampai (kasus 260710ANKHWRRF: kirim 10 Jul, sampai 13 Jul). Fix: processor isi shipped_at hanya bila belum ada. Repair: kembalikan shipped_at dari event webhook SHIPPED pertama + retry faktur. Repair data SELESAI 2026-07-12: 81 order dikembalikan ke hari kirim asli dari event SHIPPED di `webhook_logs` (`payload.data.ordersn/status/update_time`), 17 tanpa log dibiarkan di hari completion; 6 faktur 10–11 Jul di-retry ulang → dept/proyek/keterangan terisi, dobel era-manual keluar dari faktur 12 Jul. **Race Update webhook vs sync (PR #459, temuan investigator piutang 2026-07-16)**: repo `Update()` menulis $set seluruh dokumen TANPA `preserveShippedAt` (pengaman hanya di Upsert); pemanggil satu-satunya = webhook processor Shopee pola read-modify-write → guard "isi bila nil" menilai salinan basi, sync yang menulis pickup_done_time di sela bisa tertimpa balik → order pindah hari faktur. Fix: `Update()` menerapkan `preserveShippedAt` terhadap state DB terkini tepat sebelum menulis (existing sudah dibaca utk history, nol query tambahan). **shipped_at otoritatif (PR #372, feedback finance 2026-07-13)**: Shopee `pickup_done_time` (waktu asli dibawa kurir; sudah diminta di `response_optional_fields` tapi field-nya tak tertangkap struct) kini = sumber utama `shipped_at`; proxy `UpdateTime` tinggal fallback. Pengecualian freeze: nilai otoritatif (pickup_done_time / CollectionTime TikTok, flag transient `ShippedAtAuthoritative`) boleh menimpa proxy tersimpan — stabil antar-sync, tak bisa drift hari.
  - **Model faktur — FAKTUR PERMANEN** (lihat [[ADR - 0018 Faktur Permanen - Semua Pembalikan via Retur]]): 1 invoice per `(shop_id, channel, hari WIB)` multi-line = **snapshot semua order ber-`shipped_at` hari itu, APA PUN statusnya kini**. `listSnapshotOrders` kini **tanpa filter `Statuses`** — sekali order pernah `shipped`, barisnya **tetap selamanya**; CANCELLED/RETURNED **tidak** lagi dikeluarkan, dan `includeReturnedWithSentReturn` **tak terpakai** lagi. Konsekuensinya **setiap** pembalikan (retur maupun batal) lewat **Retur Penjualan** — model bisnisnya: `penjualan = income (faktur) + retur`. Order batal **sebelum** shipped (`shipped_at` NULL) tak pernah masuk faktur → tak ada yang perlu dikoreksi. *(Menggantikan mekanisme drop-snapshot/keep-line di [[ADR - 0022 Retur via Sales Return per Mode + Keep Invoice Line]].)* (filter `shipped_at` via `TransactionOrderFilter.FilterByShippedAt`); nomor stabil disimpan di koleksi `accurate_daily_invoices` (unique index `(shop_id, channel, date_wib)` + lock DB `locked_until`, aman lintas instance). Baris di-split per `(itemNo, unitPrice)` (harga promo intra-hari = baris terpisah); SKU/bundle di-translate via `accurate_products` (`item_sku` → `product_code`, fallback SKU mentah). **Harga item = BASE PRICE master Product ERP (KEPUTUSAN FINAL finance 2026-07-13, PR #391)**: harga baris faktur diambil dari `items.base_price` per SKU (menu Master Data → Product; 88 SKU termasuk bundle — terverifikasi = pola faktur manual finance, mis. bundle PJB-007+PJB-002 162.500), fallback harga order marketplace bila SKU tak terdaftar. `makeBasePriceResolver` terpasang di semua 5 call site snapshot; sejak PR #417 resolver memilih harga dari **riwayat harga efektif** (`item_price_histories`, `SelectEffectivePrice` per HARI KIRIM faktur) → perubahan base price bertanggal-efektif tidak mengubah faktur hari sebelumnya saat resync; fallback `items.base_price` lalu harga order. BUKAN harga order: original_price marketplace bisa flat salah (kasus INV/2026/07/13/020-BH 130rb semua) dan sale_price ditolak finance (PR #390 ditutup tanpa merge).
  - **Cutover TERPISAH: faktur vs retur (split 2026-07-16)** — dua konstanta beda basis:
    - **FAKTUR `autoSyncCutoverDateWIB = "20260710"`** (basis **hari KIRIM**) — hari WIB sebelum 10 Jul = era faktur **manual finance**; auto-sync DILARANG membuat/meng-edit faktur. Dijaga: `OnOrderToShip`, `SweepDailyInvoice`, `RetryDailyInvoice`, `BackfillPreCutoverInvoice`, **`OnOrderCancelledOrReturned`** (guard ini SEBELUMNYA HILANG di jalur koreksi RETURNED/CANCELLED — ditambah 2026-07-16, mencegah re-snapshot menimpa faktur IMPORTED finance).
    - **RETUR `autoSyncReturnCutoverDateWIB = "20260701"`** (basis **HARI KIRIM** sejak 2026-07-19 — dulu tanggal-retur; [[ADR - 0024 Retur Gerbang Payout + Tanggal per-Solution]] #3) — retur MENGIKUTI era faktur sumbernya: order kirim ≥ 1 Jul = ranah auto-sync; **kirim < 1 Jul = era manual finance** (faktur IMPORTED/tak-ada, income skip) → returnya JUGA manual, tak auto. Dulu basis tanggal-retur menarik retur order kirim Mei/Juni (faktur manual) ke auto → retur membalik pembukuan manual (tak konsisten + dobel); retur < 1 Jul yang terlanjur di-book **di-descope** (`cmd/returndescope`, 2026-07-19: 1565 baris, 1007 dok Accurate dihapus). Aman terhadap faktur manual karena retur hanya **MERUJUK** faktur (tak mengedit). Dijaga: `SyncOrderReturn`, `RetryDailyReturn`, `RecordReturnFetchFailure`. Dobel dgn catch-up manual finance dijaga sanity over-retur + dedupe `return_sn`. **Latar — insiden 2026-07-14**: repair `shipped_at` mengembalikan order lama ke 6–9 Jul → hook membuat faktur era-manual yang **nomornya menabrak seri faktur manual finance**, lalu **me-replace isinya** via protokol edit. Retur belum dijaga sampai 15 Jul → retur era manual tetap dicoba dibukukan, ditolak Accurate (*"Pelanggan pada Retur dan Faktur berbeda"*) dan menumpuk sbg baris merah membingungkan; kini di-skip sebelum baris dibuat. ⚠️ **Sisa insiden (per 2026-07-15, BELUM diperbaiki)**: **12 baris** `accurate_daily_invoices` tanggal **6–9 Jul** ber-`accurate_id` menunjuk faktur **milik pelanggan LAIN** (mayoritas toko TikTok) — terverifikasi satu per satu ke Accurate. Guard cutover membuatnya **inert** (tak tersentuh sweep/hook/retry), tapi datanya masih salah & faktur manual finance kemungkinan sudah tertimpa → **butuh verifikasi finance + rebind**; faktur yang benar untuk ke-12 shop-day itu **semuanya masih ada** di Accurate. Tanggal **≥10 Jul: 18/18 baris terverifikasi BENAR** (Shopee & TikTok) — operasi harian sehat.
  - **HIGH-D — rem anti timpa-menimpa (✅ 2026-07-15)** — **nomor faktur TIDAK menjamin kepemilikan**. Akar insiden di atas: `syncInvoice` mengikat `accurate_id` lewat `detail.do?number=` **tanpa cek pemilik**, jadi nomor yang menabrak faktur toko lain membuat baris ini menunjuk faktur orang lain → edit berikutnya **menimpanya**. Kini `SalesInvoiceDetail` membawa `CustomerNo` (dari `customer.customerNo`) dan dicek di 3 titik: **mengikat** id (`accID==0` & pasca-create) butuh **bukti positif** cocok (`invoiceOwnedBy`, fail-**closed** — tak terbukti = jangan ikat); **sebelum EDIT** ditolak bila **terbukti** milik pelanggan lain (`invoiceOwnedByOther`, fail-**open** saat `customerNo` kosong supaya respons tanpa field itu tak melumpuhkan seluruh sync). Cek edit memakai detail yang **sudah** diambil → nol call API tambahan. Grounded: `accurate_rts_usecase.go` (`invoiceOwnedBy`/`invoiceOwnedByOther`/`syncInvoice`), `clients/accurate_client.go` (`SalesInvoiceDetail.CustomerNo`), tes `accurate_invoice_ownership_test.go`.
  - **Protokol edit resmi Accurate** (`usecase/accurate_rts_usecase.go` — `syncInvoice`): create → simpan `accurate_id` dari respons `save.do` (fallback `detail.do?number=` — **kini wajib lolos cek pemilik**, lihat HIGH-D); edit → `detail.do?id=` ambil baris lama → satu call `save.do` dgn header `id` + baris lama `{id, _status: delete}` + snapshot baru. **Faktur tidak pernah di-delete, dan sejak PR #511 tidak pernah dikoreksi juga**: order pernah-shipped **permanen** di faktur. `OnOrderCancelledOrReturned` **tak menyentuh faktur** (no-op utk faktur), tapi sejak 2026-07-18 ia **membukukan Retur Penjualan untuk cancel pasca-kirim** (CANCELLED + `shipped_at`): mensintesis retur dari `order.Items` lalu `SyncOrderReturn` (lihat *Auto-Sync Retur* & [[ADR - 0018 Faktur Permanen - Semua Pembalikan via Retur]]). Retur marketplace normal tetap lewat jalur ingest (`SetOrderReturn → SyncOrderReturn`). ⚠️ Komentar interface `OnOrderCancelledOrReturned` sempat **tertinggal** menyebut "re-snapshot/auto-drop" padahal implementasinya sudah no-op-untuk-faktur — cukup untuk menyesatkan pembaca (termasuk AI agent) sampai salah membaca arsitektur. Faktur diisi **Keterangan** "Dikirim <tanggal WIB>" + **Departemen/Proyek per baris** dari config toko `accurate_shops.department`/`.project` (PR #361 — menyamakan isian dgn jalur manual; kolom config per toko wajib diisi via Master Data → Config Accurate; koleksi master `departments`/`projects` hanya registry FE — TIDAK dibaca auto-sync). Nilai ter-backfill 2026-07-12 mengikuti **brand** (bukan gudang; pola diverifikasi dari faktur manual finance): `invoice_code` BH → dept `MARKETING - BEAUTYHACKS` + proyek `BIP - BH`; KY+GB → `MARKETING - KY + GB` + `BIP - KY + GB` (`ProjectNo` = kode proyek). Faktur juga dikirim **`taxable:false`** eksplisit (PR #371, feedback finance — tanpa field ini Accurate memakai default pelanggan "Kena Pajak", menyimpang dari seluruh faktur manual yang non-pajak).
  - **Ketahanan**: retry kirim 3× backoff; lock retry berbatas (`acquireLockBlocking`); status persist di `accurate_daily_invoices` (`last_status` SENT/FAILED/PENDING, `last_error` utuh, `attempts`); notif Telegram (sent/failed/return) via notifier existing (grup IT). **Rate limit Accurate (PR #395)**: batas resmi 8 paralel/8 rps per token — burst hook paralel sempat 429 (`get detail for edit`, faktur FAILED). Client Accurate kini punya limiter global 6 rps burst 6 + semaphore 6 paralel + retry-sekali utk 429, berlaku utk semua call (faktur/retur/penyesuaian/stok). **Dedup redelivery (PR #370)**: `OnOrderToShip` skip diam-diam bila status SENT & hash snapshot == `lines_hash` tersimpan (webhook marketplace sering dikirim ulang → tanpa guard ini edit Accurate redundant + notif Telegram dobel); FAILED tidak di-skip (retry tetap jalan).
  - **Sweep rekonsiliasi faktur (nightly 22:00 WIB, `accurate-daily-invoice-sweep`)** — menyapu ulang `accurate_daily_invoices` dalam jendela `RTS_SWEEP_LOOKBACK_DAYS` hari (**default 31 sejak 2026-07-26**, maks 90; dulu 14 — jendela 14 hari melewatkan tetesan drift settle marketplace ~2–4 minggu, terukur faktur 1–9 Jul kurang ~Rp15,7 jt tak terjaring). `snapshotUnchanged` (hash lokal `qty_hash`/`lines_hash`) meng-skip hari tak-berubah **nol call Accurate** → memperlebar jendela murah. **Faktur TERKUNCI Retur Penjualan kini DI-APPEND, bukan di-skip (2026-07-26)**: Accurate menolak *full-replace* (baris dirujuk retur, error *"Tidak dapat menghapus Rincian Faktur Penjualan…"*) tapi **menerima penambahan baris** (edit-by-id **tanpa** `_status:delete` — terbukti prod merge-mode) → `appendMissingInvoiceLines` menghitung selisih qty via **NILAI** (baris komponen-bundle @Rp0 punya bucket harga beda → otomatis diabaikan; ukur pakai nilai, BUKAN unit), guard kepemilikan (`det.CustomerNo==shop.AccurateCustID` → faktur manual/salah-ikat di-skip), lalu menambah baris yang kurang — **retur & baris lama utuh, tanpa kaskade**. Idempoten: persist hash pasca-append → sweep berikutnya skip. Worst-case (bila Accurate ternyata replace-mode) = save ditolak atomik, faktur tak berubah. Sebelumnya faktur terkunci yang drift **di-skip** → drift menumpuk senyap (terukur **55 faktur auto Juli kurang ~Rp21 jt**, diperbaiki manual via `cmd/driftscan --append-all` 2026-07-26 lalu mekanismenya diproduktifkan ke sweep ini). Grounded: `SweepDailyInvoice`/`appendMissingInvoiceLines`/`buildInvoiceSnapshot` (`accurate_rts_usecase.go`), `accurate_daily_invoice_sweep.go`, tes `accurate_rts_sweep_test.go` (`TestSweepAppendsToReturnLockedInvoice`/`…OwnershipGuard`) & `accurate_rts_usecase_test.go` (`TestAppendMissingInvoiceLines*`).
  - **Monitoring/retry**: `GET /accurate/daily-invoices` (filter shop/channel/date/date_from/date_to/status, paginated) + `POST /accurate/daily-invoices/:id/retry` (sinkron, re-snapshot + kirim ulang). FE: [[APP - Web ERP]] tab `integration-accurate/auto-sync` + halaman `integration-accurate/panduan` (panduan finance).
    - **Detail faktur** `GET /accurate/daily-invoices/:id` (`GetDailyInvoiceDetail`) + export `GET /accurate/daily-invoices/export` (`ListDailyInvoiceDetails`) mengembalikan `lines` (baris agregat) + `breakdown` (per SKU-listing, bundle di-expand jadi komponen) + `orders` (order sumber). **`buildInvoiceBreakdown` kini memakai resolver base_price yang SAMA dengan baris faktur (fix 2026-07-23, belum deploy)** — sebelumnya memakai harga order marketplace (`it.Price`), sehingga modal FE (`invoice-detail-modal.tsx` merender `breakdown` sebagai tabel utama + Subtotal/Total) menampilkan mis. **269000** untuk bundle `PJB-003 + PJB-006 + PJB-005` (`INV/2026/07/06/030-BH`) padahal faktur di Accurate benar **270000** (base_price master). **DISPLAY-only**: faktur di Accurate SELALU benar (dikirim via `makeBasePriceResolver`, terverifikasi baca langsung dok Accurate 79765 = 270000); harga marketplace tetap tampil di bagian "Order Sumber" modal. Konsekuensi: harga marketplace beda (promo) untuk SKU sama **tidak lagi** memecah baris breakdown — kolaps ke satu baris di base_price, cocok dengan baris faktur. Pola sama dengan HIGH-E retur ("angka di layar = angka yang dibukukan"). Grounded: `buildInvoiceBreakdown`/`GetDailyInvoiceDetail`/`ListDailyInvoiceDetails` (`accurate_rts_usecase.go`), tes `TestBuildInvoiceBreakdownUsesResolvedBasePrice` (`accurate_invoice_breakdown_test.go`).
  - **Guard anti-dobel**: manual send `SALES_INVOICE` (`/summary/reports/:id/send/accurate`) yang rentangnya beririsan faktur auto-sync `SENT` → **409** + daftar `{date_wib, invoice_number, line_count}`; bypass `?force=true` (audit `slog.Warn`). INCOME/SALES_RETURN tidak di-guard — income/settlement tetap jalur manual.
  - Diverifikasi ke **schema OpenAPI resmi Accurate** (`account.accurate.id/open-api/json.do`; prod langsung `zeus.accurate.id`, mode API Token Bearer+HMAC) — detail di [[External - Accurate]].
  - **Auto-Sync Retur (`RTS_RETURN_ENABLED=true` di prod; Shopee & TikTok Fase B ✅ JALAN 2026-07-16)** — `SyncOrderReturn` (`usecase/accurate_rts_usecase.go`) membukukan **Retur Penjualan** ke Accurate terhadap faktur harian sumbernya. ⚠️ Rangkaian perbaikan besar 2026-07-16/17 (harga baca-dari-faktur, itemNo, fallback pointer, HIGH-C IMPORTED, cutover split, sanity over-retur, tanggal accepted-seragam, kredensial TikTok, guardian TikTok) **belum ter-deploy** per 2026-07-17. **Dipicu dari ingestion retur** (`shopee_new_usecase.go`, setelah `SetOrderReturn` sukses — bukan dari transisi status, karena retur **parsial** membuat `order.status` tetap `COMPLETED` sehingga tak pernah memicu jalur status). Idempoten by `return_sn` (fallback `order_id`) via koleksi **`accurate_daily_returns`** (unique index `(shop_id, channel, dedupe_key)` + lock `locked_until`, re-check status pasca-lock; status baris **PENDING/SENT/FAILED/SKIPPED**). Nomor retur (`RTR/...`, generator sama dgn faktur) **di-persist SEBELUM kirim** — gagal persist → **batalkan kirim** (cegah re-fire meng-generate nomor baru → retur GANDA). *Koreksi 2026-07-15: premis lama "Accurate tak punya edit/hapus retur" **KELIRU** — schema OpenAPI resmi memuat `/api/sales-return/delete.do` (+ `list.do`, `detail.do`). Retur **bisa** dihapus, jadi retur ganda bukan lagi kerusakan permanen; desain persist-before-send tetap dipertahankan (mencegah tetap lebih murah dari memperbaiki), tapi model risikonya lebih ringan dari yang didokumentasikan sebelumnya.* SKU di-resolve `accurate_products` → `product_code`. **HIGH-E — harga retur = resolver harga FAKTUR (✅ fix 2026-07-15)**: `buildReturnDetailItems` dulu memakai **harga order marketplace**, padahal faktur memakai **`makeBasePriceResolver`** (base_price master Product, keputusan finance PR #391) → retur membalik nilai yang TIDAK PERNAH dicatat faktur. Terbukti live: `RTR/2026/07/06/001-KY+GB` membalik `PJK-008` **@140.000** sementara fakturnya mencatat **@98.000** → penjualan berkurang **Rp42.000 kelebihan**. Kini retur memakai resolver + hari-kirim yang **identik** dgn faktur → harga otomatis cocok; fungsi detail (modal FE) ikut diperbaiki agar angka di layar = angka yang dibukukan. ⚠️ Retur yang terlanjur terkirim dgn harga lama perlu koreksi finance (`sales-return/delete.do` tersedia). **HIGH-E disempurnakan 2026-07-16 — harga DIBACA dari dokumen faktur, bukan dihitung ulang** (`makeInvoicePriceResolver`): dua perhitungan atas data yang bisa bergerak (riwayat harga terisi di antara hitung faktur & hitung retur) berselisih diam-diam (faktur 06:11 → 98rb, retur dihitung ulang 06:21 → 140rb). Kini `pushReturnToAccurate` memanggil `GetSalesInvoiceDetail(accurate_invoice_id, invoice_number)` → baca `unitPrice` per baris langsung dari faktur. Dua bug turunan ikut dibereskan: **(a) `itemNo` faktur SELALU kosong** — respons `detail.do` menaruh kode di `item.no` (bersarang), tag lama `json:"itemNo"` tak pernah terisi → begitu harga dibaca per item, retur jadi **Rp 0**; kini `SalesInvoiceDetailLine.UnmarshalJSON` memetakan `item.no`. **(b) fallback pointer faktur basi** — `accurate_invoice_id` bisa menunjuk faktur yang di-buat-ulang (id lama mati, nomor sama); `GetSalesInvoiceDetail` kini coba id dulu → **fallback ke nomor** bila id ditolak (terbukti prod: id 79309 basi, faktur hidup 79767 nomor `INV/2026/07/08/002-KY+GB`). Faktur tak terbaca = **batalkan kirim** (bukan tebak harga); modal FE tak lagi 500 tapi tampilkan `price_note`. **Guard cutover retur = HARI KIRIM ≥ `autoSyncReturnCutoverDateWIB` "20260701"** (⚠️ **basis diubah tanggal-retur → hari-kirim 2026-07-19**, [[ADR - 0024 Retur Gerbang Payout + Tanggal per-Solution]] #3; TERPISAH dari cutover faktur "20260710") → `SyncOrderReturn`/`RetryDailyReturn`/`RecordReturnFetchFailure` menilai `dateWIB` (hari-kirim = tanggal faktur sumber), retur ikut era faktur; order kirim < 1 Jul = ranah manual finance. **Gerbang payout (ADR-0024)**: retur ⟺ payout≈0 — order payout>0 (ship ≥ 10 Jul) diserap Auto Sync Income (receipt), retur di-skip cegah dobel.
    - **Model GRUP: 1 dokumen per (faktur sumber + tanggal retur)** (✅ deploy 2026-07-17, [[ADR - 0016 Retur Grouped per Faktur + Tanggal Retur]]) — bukan lagi 1 dokumen per `return_sn`. `dedupe_key` = `<invoice_number>|<YYYYMMDD tanggal-retur>` (`returnGroupKey`); anggota di `Members []ReturnGroupMember`; `lines_hash` menjaga idempotensi. Rebuild = **hapus dok lama + book fresh** (hanya bila hash berubah); gagal hapus → batalkan book; setelah hapus sukses `accurate_return_id` **langsung di-clear** (kalau tidak, book gagal → retry menghapus dok yang sudah tiada → grup **macet permanen**). Mode campuran → header `PARTIALLY_RETURNED`. **Faktur WAJIB bagian kunci** — Accurate `sales-return/save.do` cuma punya **satu** `invoiceNumber` di header & `detailItem` tanpa rujukan faktur per baris → retur lintas-faktur **mustahil** digabung. Karena faktur dikunci per hari-kirim, peleburan jarang terjadi (**2 dari 74** dok di prod) — dibatasi granularitas faktur, bukan kode. **Guard anti-dobel by-MEMBER** (`GetByMember`, index `shop_channel_member_returnsn`) tak bergantung `dedupe_key`: kunci memuat tanggal, jadi tanggal bergeser = kunci bergeser = risiko dokumen ganda. Migrasi dok lama: `cmd/returnconsolidate`.
    - **APPEND-first retur (✅ 2026-07-26)** — `rebuildAndSendGroup` kini, bila dok retur SUDAH ada (`accurate_return_id>0`) **dan** perubahannya **murni penambahan** (tak ada override gudang `warehouse_items`, tak ada baris berkurang, representasi bundle sama di kedua sisi), **menambah baris via edit-by-id** (`SaveSalesReturnRequest.ID` + baris baru tanpa `_status:delete`) alih-alih **hapus+bangun-ulang** — menghilangkan **jendela dokumen-hilang** + risiko nyangkut (akar insiden kaskade 2026-07-26 di [[ADR - 0025 Log Sumber vs Input WMS + Stempel Penginput]]). Header `returnStatusType` dikirim ulang (komposisi grup berubah parsial→penuh ikut ter-update). Selain itu (override gudang / ada pengurangan / representasi bundle beda / append ditolak Accurate) → **fallback** ke delete+rebuild lama → worst-case = perilaku lama yang menangani SEMUA kasus. `returnAppendDelta` menentukan syarat via basis `(itemNo, harga-dibulatkan)`: representasi bundle beda (satu sisi punya komponen @Rp0, sisi lain tidak) → muncul selisih negatif → `pureAdd=false` → rebuild (aman). Grounded: `rebuildAndSendGroup`/`returnAppendDelta` (`accurate_rts_usecase.go`), `SaveSalesReturnRequest.ID` (`clients/accurate_client.go`), tes `accurate_rts_return_append_test.go`.
    - **Gerbang PAYOUT — retur ⟺ order `payout ≈ 0`** (✅ 2026-07-18, [[ADR - 0024 Retur Gerbang Payout + Tanggal per-Solution]]) — refund Shopee = penyesuaian INCOME/escrow, **bukan** dokumen retur. Auto Sync Income/receipt (`buildReceiptPayload`) **sudah** menyerap refund untuk order **payout>0** (baris Potongan Penjualan, akun discount 4003) & melunasi piutang penuh; order **payout≈0** di-skip receipt → kode itu mengarahkan koreksi **lewat retur**. Membukukan retur untuk order payout>0 = **DOBEL** (revenue balik 2×; terukur 7 RTR SENT dobel, 92 FAILED payout>0 = ranjau bila di-retry). `returnPayoutGate` (reuse `payoutOf` **identik** receipt): CANCELLED→book; income belum settle (`income.paid_at` nil)→**DEFER**; settled payout>0 **& ship ≥ 10 Jul** (post-cutover)→**SKIP** (baris `SKIPPED`, Auto Sync Income yang urus); selain itu→book. ⚠️ **syarat cutover WAJIB**: receipt sendiri skip ship < 10 Jul → pra-cutover payout>0 TETAP book (kalau di-skip, pembalikan hilang). Dipasang di `SyncOrderReturn` (auto) + `RetryDailyReturn` (manual tolak payout>0/belum-settle). Retur DEFER di-book saat escrow cair — `triggerReturnAfterSettlement` di `syncEscrowReleaseWindow` (refetch branch, `shopee_new_usecase.go`), idempoten fire-and-forget. **Income/Auto Sync Income sync TIDAK diubah** (ia benar). **Cleanup ✅ SELESAI 2026-07-19** (`cmd/returndescope`): descope **1565 retur hari-kirim < 1 Jul** (1007 dok Accurate dihapus) + void **1 dobel-sejati** `RTR/2026/07/16/006-BH`. Dobel-sejati ternyata **cuma 1** (bukan ~7 — analisis awal lupa receipt punya cutover sendiri; kondisi dobel = payout>0 **DAN** ada receipt SENT; pra-cutover payout>0 = receipt skip → BUKAN dobel, sudah kedescope). Edge barang-balik `solution=0` payout>0 (stok tak balik → under-stock, stok-opname backstop) → **nunggu finance**.
    - **Gerbang GUDANG — retur barang-balik DITAHAN sampai gudang scan** (✅ live; **TERBUKTI end-to-end prod 2026-07-22**; [[ADR - 0025 Log Sumber vs Input WMS + Stempel Penginput]]) — retur `solution=0` (barang balik, termasuk cancel COD) yang lolos gerbang payout **tidak langsung dibukukan**: `holdForWarehouse` menandainya **PENDING** sampai barang sampai gudang, discan, dan kondisi fisiknya (Reuse/Rework/Reject) diinput lewat `POST /accurate/daily-returns/warehouse-confirm` (dipanggil manufacture setelah form "Input Masuk Return Dari Ekspedisi"). **Kondisi fisik MENENTUKAN pembukuan** — membalik [[ADR - 0022 Retur via Sales Return per Mode + Keep Invoice Line]] ("tak perlu tunggu barang fisik"): membukukan saat marketplace menyetujui = menebak qty yang belum dilihat siapa pun. **Ketiga kondisi sama-sama menambah stok** Accurate (baris `RETURNED`); label murni informasi dashboard, penyusutan barang rusak = finance manual (keputusan 21 Juli 2026). Gerbang ditempatkan **paling akhir** (setelah baris dibuat & member terdaftar) supaya PENDING **terlihat di FE** — cabang skip yang keluar sebelum baris dibuat = retur hilang senyap. Refund-only (`solution=1`) **tak** ditahan (barangnya tak pernah balik). Konfirmasi: warehouse-confirm men-**akumulasi** kondisi per `(order_id, SKU)` (manufacture men-spawn 1 panggilan per SKU) dengan **baca-ulang row DI DALAM lock**; grup di-rekey ke **tanggal gudang** saat konfirmasi, dan `warehouse_items` ikut pindah bila grup digabung. **Lookup lewat MEMBER (`order_id`), bukan `dedupe_key`** (`GetByMemberOrderIDAny`) — gudang tak pernah memegang kunci grup (`<faktur>\|<YYYYMMDD>` sejak [[ADR - 0016 Retur Grouped per Faktur + Tanggal Retur]]) dan kunci itu **berubah** saat re-key; ini fix kritis 22 Juli (sebelumnya lookup by-key selalu meleset → gerbang **inert** sejak deploy). Tiga bug kelas "isi salah" (lookup meleset, akumulasi saling-timpa, barang hilang saat grup digabung) difix + mutasi-terverifikasi 22 Juli.
    - **`cmd/returnrecon` — audit READ-ONLY rekonsiliasi retur** (✅ 2026-07-22, on-demand) — untuk tiap retur SENT dalam rentang, ambil dokumen Retur Penjualan **nyata** dari Accurate lalu bandingkan dengan kebenaran **independen**: unit yang diklaim marketplace diretur + `booked_total` tercatat. Sengaja **tak** memakai `warehouse_items` sbg acuan (kelas bug di atas merusaknya sendiri). Tiga flag: `DOC_MISSING`, `VALUE_MISMATCH` (keras) + `UNDER_CLAIM` (lunak "tinjau" — dok < klaim; penerimaan parsial itu sah). Bukan job harian (butuh 1 fetch Accurate/retur → rate-limit) — sesuai keputusan penulis cron anomali. Audit 22 Juli: **555 retur SENT seminggu → 100% cocok, nol selisih, UNDER_CLAIM nol false-positive**.
    - **Sweep rekonsiliasi retur (nightly 22:30 WIB, `accurate-daily-return-sweep`, ✅ 2026-07-26)** — jaring pengaman **analog sweep faktur** untuk event booking retur yang KELEWAT (webhook drop / sync skip → retur menggantung PENDING/FAILED selamanya): `SweepDailyReturns` menyapu retur **PENDING/FAILED** dalam jendela `RTS_RETURN_SWEEP_LOOKBACK_DAYS` hari (default 31, maks 90) & membukukan ulang lewat `RetryDailyReturn` (yang menghormati gerbang gudang + gerbang payout + cutover + APPEND-first). Retur **bergerbang-gudang** (barang-balik belum konfirmasi, ADR-0025) & **pra-cutover** (era manual finance) **di-SKIP** — MENUNGGU, bukan gagal. Terjadwal **22:30** (setelah sweep faktur 22:00 → retur dibukukan setelah faktur sumber rampung). Verifikasi run pertama prod 2026-07-26: 321 PENDING barang-balik ter-skip benar (menunggu gudang), 0 mis-booking. Grounded: `SweepDailyReturns` (`accurate_rts_usecase.go`), `worker/tasks/accurate_daily_return_sweep.go`, registrasi `main.go`, tes `accurate_rts_return_append_test.go` (`TestSweepDailyReturnsSkipsGatedAndCutover`).
    - **Tanggal retur TikTok = `order_update_date`** (✅ 2026-07-17, [[ADR - 0017 Tanggal Retur TikTok via order_update_date]], source `order_updated`) — **bukan** `update_time` record retur, yang **maju 7–10 hari** karena ikut tersentuh settle refund (**24 dari 49** dok TikTok salah tanggal; sudah dikoreksi via `cmd/returnredate`). **Shopee kini per-solution** (✅ 2026-07-18, [[ADR - 0024 Retur Gerbang Payout + Tanggal per-Solution]]): refund-only (`solution=1`)→`requested_at` (anti-drift — `updated_at` Shopee JUGA melar median 3 hari, premis "accepted bersih" [[ADR - 0023 Retur Tanggal Accepted-Seragam + Cutover Terpisah]] #1 terbantah); barang-balik (`solution=0`) + `return_tracking` RECEIVED → arrival (`last_event_at`); fallback `updated_at`→`requested`→`shipped`. `returnEraDateWIB` kini **didelegasikan** ke `resolveReturnTransDate` (dulu menyalin urutannya → gate cutover bisa menilai tanggal berbeda dari yang dibukukan). **Jebakan zona**: driver Mongo mengembalikan `time.Time` **UTC**; `Format()` langsung atasnya membuat retur dini-hari-WIB (≥17:00 UTC) **mundur sehari** (14 dok terdampak, 1 **lompat bulan**) → wajib `util.WIBDayStart(...)`. **Nomor RTR** kini dari **tanggal retur** (dulu hari-kirim → `RTR/2026/07/06/001` untuk dokumen bertanggal 12/07; menyesatkan finance saat rekonsiliasi).
    - **HIGH-C DICABUT (2026-07-17)** — lihat [[ADR - 0018 Faktur Permanen - Semua Pembalikan via Retur]]. Guard ini men-skip retur pada faktur **SENT** dengan alasan *"baris jual hilang saat re-snapshot → retur = pengurangan ganda"*. Premis itu **mati** sejak faktur dibuat **permanen** (`OnOrderCancelledOrReturned` no-op + `listSnapshotOrders` tanpa filter status): barisnya **tak pernah hilang**, jadi Retur Penjualan adalah **satu-satunya** pembalikan — untuk **semua** faktur, `SENT` maupun `IMPORTED`. Status faktur **tak lagi relevan** di keputusan ini. Kembarannya di `RetryDailyReturn` (menolak retry order RETURNED) ikut dicabut. **Dampak terukur**: selama guard basi ini hidup, **6 retur TikTok** di faktur auto-sync tak pernah terbukukan → penjualannya menggantung; sudah di-backfill via `cmd/returnrebook`. ⚠️ **Pelajaran**: 3 tes mengunci guard ini lengkap dgn alasan meyakinkan, dan tetap **hijau** setelah line-drop dicabut — tes menegakkan premis lama = tes **melindungi bug**.
    - **SANITY over-retur (✅ 2026-07-17, ganti anti-dobel returnQuantity; status → FAILED 2026-07-18)**: guard grup `groupWouldOverReturn` menolak retur HANYA bila total retur (pending grup + qty sudah-diretur di faktur pada first-book) akan **melebihi qty terjual** di faktur (mustahil, pasti dobel/error) — hanya saat item memang ada di faktur (`sold>0`). **Ditandai `FAILED` (bukan `SKIPPED`)**: over-retur = ANOMALI (retur dobel / retur nyasar ke faktur salah) yang minta cek manual → harus **terlihat merah** di UI; `SKIPPED` (netral, abu-abu, tak-perlu-aksi) justru menyembunyikannya dari orang yang harus memeriksanya. Retry sah — begitu data dobel dibersihkan, ia lolos. Menggantikan guard lama "returnQuantity>0 → skip" yang **salah-tolak retur SAH kedua** atas SKU sama (dipicu pembukuan kita sendiri, permanen). Anti-dobel inti tetap **dedupe `return_sn`** (retur yang sama tak mungkin dikirim dua kali). Batas diketahui: faktur harian tak menandai baris per-order (`onlineOrderId` kosong), jadi dua retur identik SKU+qty dari order berbeda tak bisa dibedakan — diterima karena `return_sn` sudah menjaga inti. **Alasan penolakan Accurate kini tersimpan** (`accurateReject` → `last_error`): sebelumnya respons `{s:false, d:[...]}` dibuang jadi string generik *"accurate menolak retur (status false)"*, sehingga baris FAILED tak bisa didiagnosa dari UI/DB sama sekali — pesan asli Accurate (mis. *"Pelanggan pada Retur dan Faktur berbeda"*) itulah yang akhirnya mengungkap insiden salah-ikat 6–9 Jul.
    - **Retur MENIRU pajak faktur sumber (✅ 2026-07-18)** — `SaveSalesReturnRequest.Taxable` (tanpa `omitempty`, `false` **wajib** terkirim eksplisit) di-set `= srcInv.Taxable`; `SalesInvoiceDetail` kini mem-parse `taxable`. Tanpa field ini Accurate memakai default **master pelanggan** (BH "Kena Pajak" ON, KY+GB OFF) → retur -BH kena **PPN 11% yang faktur tak punya**, membalik lebih dari yang penjualan catat. Pola sama persis dgn faktur yg sudah kirim `taxable:false` (baris **Protokol edit** di atas). **Kebijakan finance: retur TIDAK kena pajak** (faktur jual semua `taxable=false`). ⚠️ **Historis (BELUM dikoreksi, ranah finance):** ≥291 retur auto `-BH` (Juli, ≥Rp8 jt PPN palsu, ~13–17/hari) terlanjur terbukukan kena PPN sebelum fix — koreksi via `sales-return/save.do` (`taxable:false`) menunggu keputusan finance (kemungkinan sudah masuk laporan PPN/SPT).
    - **`accurate_return_id` di-persist setelah kirim sukses (✅ 2026-07-18)** — `SaveSalesReturnResponse.ParseSavedID()` (baca `r` lalu fallback `d`; `Data` tetap `any` agar `accurateReject` tak pecah) → `SetGroupResult(...savedID)`. Sebelumnya retur membuat dokumen di Accurate lalu **lupa id-nya** (baris SENT ber-`accurate_return_id:0`), sehingga koreksi retur (protokol edit menuntut id header) hanya bisa lewat berburu dokumen manual. Id tak terbaca → baris **tetap SENT** (jangan turunkan ke FAILED: dokumen sudah ada → retry = retur GANDA).
    - **Status `SKIPPED` (✅ 2026-07-18)** — sengaja TIDAK dibukukan & itu BENAR, **hanya** untuk retur bertanggal era manual finance (pra-cutover retur). Dibedakan dari `FAILED` ("butuh perhatian" → memancing Retry & alarm) supaya merah di UI hanya berarti benar-benar gagal. FE [[APP - Web ERP]]: badge **"DILEWATI"** abu-abu, tanpa tombol Retry (`lib/return-status.ts`, dipakai bersama tabel+modal). Guard HIGH-C (order RETURNED) yang sempat pakai status ini sudah **DICABUT** (lihat HIGH-C); over-retur **bukan** SKIPPED (lihat SANITY over-retur).
    - **Keterangan dokumen (`description`) = `<no. faktur> <label refund>` (✅ 2026-07-18, kebijakan finance)** — dulu `Retur <faktur> (<tgl>)`. Label diturunkan dari komposisi member grup: **barang balik** (`solution=0`, `RETURNED`) → `return refund`; **refund-only** (`solution=1`, barang tak balik, `NOT_RETURNED`) → `refund dana`; **campur** keduanya → `return refund (campur: sebagian barang balik, sebagian refund dana)`. Dinilai dari `hasReturned`/`hasNotReturned` **bukan** `headerStatus` — `PARTIALLY_RETURNED` juga bisa muncul dari retur parsial murni barang-balik (`anyPartial`), yang tetap `return refund`, bukan "campur". Cancel pasca-kirim disintesis `solution=0` → otomatis `return refund`. Penanda anomali tanggal (lihat *Penanda ANOMALI*) tetap **di-append di belakang** bila `date_source == shipped`. Grounded: `accurate_rts_usecase.go` (`rebuildAndSendGroup`), `clients/accurate_client.go` (`SaveSalesReturnRequest.Description`), tes `accurate_return_group_test.go` (`TestSyncOrderReturn_DescriptionRefundLabel`).
    - **HIGH-F — TANGGAL dokumen retur = tanggal retur DISETUJUI marketplace, SERAGAM (✅ 2026-07-15; kebijakan disederhanakan 2026-07-17 — lihat [[ADR - 0023 Retur Tanggal Accepted-Seragam + Cutover Terpisah]])**: dulu `transDate` = **hari kirim** (`date_wib`) → retur ter-**backdate** (disetujui 14 Jul, dibukukan 6 Jul) dan bisa menabrak **periode akuntansi yang sudah ditutup**. Hari kirim tetap dipakai — tapi **hanya untuk MENCARI faktur sumber**. **Kebijakan 2026-07-17 (accepted-seragam)**: tanggal = **`accepted`** (retur disetujui marketplace, `update_time`) untuk SEMUA retur. Sempat "arrival dulu" (barang fisik tiba via reverse-tracking) tapi `arrival` **hampir tak pernah tersedia** (terukur 1 dari 23; Shopee butuh ePOD, **TikTok tak punya reverse-tracking sama sekali**) & bikin faktur retur **tak seragam** → accepted-semua lebih bersih, mudah direkonsiliasi, & sejalan prinsip "retur = cermin status marketplace, kedatangan fisik diurus service lain". **Infrastruktur arrival dicabut** (`arrivalResolver`/`SetReturnArrivalResolver`/injeksi `main.go`; `GetReturnArrival`/`ArrivalTime` ditinggalkan dorman). Urutan `resolveReturnTransDate` (timestamp marketplace via `time.Unix()`): **1) `accepted`** (kebijakan, hampir selalu) → **2) `requested`** (`create_time`, fallback bila accepted hilang) → **3) `shipped`** (hari kirim — **ANOMALI**, timestamp marketplace hilang total; tak boleh terjadi utk retur ACCEPTED). Tanggal + `date_source` **di-persist saat baris dibuat**. **Baris lama ber-`trans_date` nil kini DIHITUNG ULANG** (accepted), tak lagi jatuh ke hari kirim (✅ 2026-07-16 — sebelumnya `RTR/2026/07/08/001-KY+GB` terekap 08/07 padahal disetujui 14/07 karena baris lama trans_date kosong → kode jatuh ke hari kirim; kini `pushReturnToAccurate` recompute saat kosong).
    - **Tanggal kedatangan parsel** (`clients/shopee_returns_client.go`): **`v2.returns.get_reverse_tracking_info`** → `ShopeeReverseTrackingPayload.ArrivalTime()`. Deteksi "sampai" memakai **`epod_image_list`** (electronic Proof of Delivery) — penanda **terstruktur**; `tracking_description` sengaja TIDAK dipakai (teks bebas kurir & berbahasa lokal, mis. "Pesanan tiba di alamat tujuan" → rapuh). `validation_type=warehouse_validation` → ambil **segmen ke-2** (`post_return_logistics_tracking_info`, gudang→seller) karena stok kita bertambah saat sampai di **kita**, bukan di gudang Shopee. **Tak semua retur punya data ini** (dok Shopee): hanya **Normal RR + reverse logistics terintegrasi** — In-transit RR (`type=1`), Return-on-the-Spot (`type=2`), kurir non-integrated, & refund-only **tak punya** → fallback wajib. Dipanggil **hanya saat membukukan** `solution=0` (bukan tiap ingest) → +1 call API, volume kecil (`epReverseTracking` di APIStats). Resolver di-**inject** via `SetReturnArrivalResolver` (memutus circular dependency: usecase marketplace sudah bergantung pada RTS untuk memicu booking).
    - **Penanda ANOMALI (disesuaikan 2026-07-17)**: HANYA bila `date_source == shipped` (jatuh ke hari kirim = timestamp retur marketplace hilang, tak boleh normal terjadi). Ditandai di **DUA tempat**: **Keterangan dokumen Accurate** (`description`, di-append di belakang label refund — lihat bullet *Keterangan dokumen*) & **badge amber "cek tanggal"** di [[APP - Web ERP]] (`hasApproxDate`). `accepted`/`requested` **TIDAK ditandai** — itu tanggal SAH menurut kebijakan accepted-seragam, bukan perkiraan. (Sebelum 2026-07-17 penanda menyala utk `solution=0` non-arrival; kini arrival tak dipakai lagi, jadi penanda dipersempit ke anomali sungguhan.)
    - **BATAS SERVICE (penting, keputusan 2026-07-15)**: auto-retur **hanya mencerminkan status marketplace**. **Kebenaran fisik stok gudang BUKAN urusan service ini** — ditangani service lain (+ koreksi stok-opname). Karena itu `get_return_detail`/`is_arrived_at_warehouse` **sengaja TIDAK dipakai** sbg guard restock: `solution=0` tetap dibukukan `RETURNED` walau barang belum terbukti sampai. `get_reverse_tracking_info` dipakai **murni untuk TANGGAL**, bukan validasi keberadaan barang. *Jangan menambahkan validasi barang-fisik di service ini.*
    - **Gate + mode** (`resolveReturnMode`/`deriveReturnMode`, enum diverifikasi ke OpenAPI Accurate) — dibukukan hanya saat retur **DISETUJUI** (`ACCEPTED`; konfirmasi Finance: tak perlu tunggu barang fisik sampai gudang). Mode dari `solution`+`partial`: **refund-only** (`solution=1`, barang tak balik) → detail+header **`NOT_RETURNED`** (dana balik, **stok tidak** bertambah); **barang balik** (`solution=0`) → detail **`RETURNED`** (restock), header **`PARTIALLY_RETURNED`** bila parsial else **`RETURNED`**. Mode kosong/tak tentu → baris **`FAILED`** tanpa kirim (jangan pernah kirim enum kosong).
    - **Anti double-reduction** (menggantikan pendekatan lama `FULL_VIA_INVOICE`, by data prod: 39% retur penuh tetap `COMPLETED`) — buku Retur Penjualan selama **baris jual masih ada** (order `SHIPPED`/`COMPLETED`); bila order **sudah `RETURNED`** saat sync → **skip** (`order.Status==RETURNED` guard; pembalikan sudah lewat drop snapshot). Bila order yang sudah punya Retur Penjualan `SENT` **kemudian** flip `RETURNED`, snapshot faktur **mempertahankan barisnya** (lihat *Model faktur*) → Retur Penjualan jadi satu-satunya pembalik. Baris non-SENT di-push ulang pakai **mode tersimpan** (`detail_status`/`return_status`) walau status marketplace kini lewat `ACCEPTED`; gate `ACCEPTED` hanya menahan pembuatan baris **baru**. Keputusan: [[ADR - 0022 Retur via Sales Return per Mode + Keep Invoice Line]].
  - **Monitoring/retry retur**: `GET /accurate/daily-returns` (filter shop/channel/date/status + **`search`** = cari by `order_id` **atau** `return_sn`, partial & case-insensitive, `regexp.QuoteMeta` krn nomor bisa memuat karakter regex; alias `order_id`; **export memakai filter yang sama** agar hasil unduhan cocok dgn layar — 2026-07-15; paginated) + `GET /accurate/daily-returns/:id` (detail: header + baris item kode-Accurate + order sumber) + `GET /accurate/daily-returns/export` (Excel 2-sheet) + `POST /accurate/daily-returns/:id/retry` (sinkron; kirim ulang baris FAILED — memakai mode tersimpan/di-derive; order sudah `RETURNED` **ditolak**: dibalik via koreksi faktur). FE: [[APP - Web ERP]] tab `integration-accurate/auto-sync-return`.
  - **Belum (retur)**: **exactly-once belum tertutup** — respons `sales-return/save.do` = `{s,d}` tanpa id, jadi response hilang pasca-commit bisa dobel atau stuck `FAILED`; nomor stabil (persist-before-send) menekan risiko. *Koreksi 2026-07-15: penyebabnya **bukan** "Accurate tak punya get/list retur" — schema resmi memuat `/api/sales-return/{list,detail,delete}.do`; yang belum ada adalah **implementasinya di client kita**. Artinya penutupan exactly-once (cek pre-send by nomor + hapus duplikat) **bisa dikerjakan sekarang**, tak perlu "verifikasi Accurate" lagi.* **TikTok (Fase B DIWIRE 2026-07-14, ✅ JALAN 2026-07-16)**: webhook type-12 `RETURN_OR_REFUND_REQUEST_COMPLETE` → `TikTokClient.SearchReturns` (`/return_refund/202602/returns/search`) ambil detail item → `mapTikTokReturnToOrderReturn` (`return_id`→SN, `seller_sku`→SKU, `return_sub_line_items`→qty, `REFUND`/`can_buyer_keep_item`→refund-only Solution 1, `COMPLETE`→`ACCEPTED`) → `SetOrderReturn` → `SyncOrderReturn` **SEBELUM** `MarkReturned`. Di `tiktok_direct.go` `handleReturn`, gated `RTS_RETURN_ENABLED`. **AKAR "0 dari 95 retur TikTok" ketemu 2026-07-16 — BUKAN scope/re-auth**: `FetchAndSetTikTokReturn` lama membaca kredensial dari `credsRepo.GetCredential(platform="tiktokshop")` + `GetOrRefreshToken(shopID)` — koleksi `credentials`/`platform_tokens` **TAK PERNAH DIISI** (0 dokumen) padahal 16 toko & 299rb order tersinkron normal; ia ditulis utk model kredensial yang tak dipakai sistem ini (error asli: *"get app_key: mongo: no documents in result"*). Fix: baca kredensial lewat jalur yang SAMA dgn sinkron order — `tiktokShopRepo.ListAuthorizedShops(shopID)` → `GetAccessToken(credential.ID)` + `shop.Cipher`. Terbukti live (retur `solution=0`/`1` terambil benar). **Kegagalan fetch kini TERCATAT** (`RecordReturnFetchFailure` → baris FAILED terlihat di UI, bukan warning senyap yang hilang bersama log). **`handleReturn` kini juga memicu `OnOrderCancelledOrReturned`** setelah `MarkReturned` (dulu tidak → faktur hari kirim tak di-snapshot ulang, penjualan retur tak terbalik untuk order tanpa order-susulan). **Guardian recovery** menutup celah webhook-meleset (lihat Background Workers).
- **Sampel → Penyesuaian Persediaan (✅ live 2026-07-13, PR #375)** — order sampel TikTok (field resmi `is_sample_order` → `transaction_orders.is_sample`; Shopee tak punya program sampel) DIKECUALIKAN dari faktur sales (filter `ExcludeSample` di semua snapshot) dan dicatat sebagai **Penyesuaian Persediaan** Accurate (`item-adjustment/save.do`, ADJUSTMENT_OUT, 1 dokumen/toko/hari kirim, keterangan `Sample Tiktok <toko>`, akun ikut default preferensi, nomor IA auto-number). Ketahanan meniru faktur: koleksi `accurate_daily_adjustments` + lock + edit protokol + skip-unchanged + notif Telegram 📦/🚨. Endpoint `GET /accurate/daily-adjustments`, `GET /:id` (detail: header+baris+order sampel sumber, PR #389), `POST /:id/retry`, `POST /sync?shop_id&channel&date` (backfill). Backfill 391 order + IA.2026.07.00005–00008 terverifikasi.
- **Summary otomatis harian (✅ live 2026-07-13, PR #381)** — worker `auto-summary-report` 22:30 WIB membuat `transaction_summary_reports` status **`AUTO`** per (toko, hari WIB) dari faktur auto-sync SENT (InvoiceNo = nomor faktur harian; upsert idempoten). AUTO TIDAK dikirim ke Accurate (guard `send/:service`) dan TIDAK menstempel order — raw export report AUTO me-resolve order via query hari-kirim (sampel tetap tampil). Detail FE: item report AUTO dibangun on-the-fly dari pesanan terkirim (agregasi SKU+harga) dan kartu Total diisi worker dari snapshot baris faktur (PR #385). Kartu Total juga dihitung on-the-fly bila worker belum mengisi + Created By "Auto-Sync" (PR #387); FE membaca Total dari meta.total_amount endpoint items → cabang AUTO mengisinya + CreatedBy on-the-fly di GET report (PR #388). **rescache diikat versi build (PR #386)**: cache respons tersimpan di Redis dan selamat lintas deploy — prefix key kini memuat modtime binary sehingga image baru otomatis meng-invalidasi seluruh cache lama (kasus nyata: respons kosong versi lama tersaji pasca-deploy #385 sampai di-flush manual `srv:integration:rescache:*`).
- **Summary otomatis RETUR (✅ 2026-07-25)** — cermin RETUR dari `auto-summary-report` supaya retur tampil di halaman Summary Return (`transaction_summary_reports` tipe **`SALES_RETURN`** status **`AUTO`**), bukan cuma tab Auto-Sync Retur. Dua lapis: (a) **realtime** — hook di `rebuildAndSendGroup` (tepat setelah retur ditandai `SENT`) memanggil `RebuildReturnSummaryReport(shop, channel, hari-retur WIB)` best-effort (dibungkus recover — gagal/panik menyegarkan ringkasan TAK menggagalkan booking yang sudah sukses; pola sama `SyncOrderReturn`); (b) **worker rekonsiliasi** `auto-summary-report-return` **22:40 WIB** (setelah faktur 22:30), lookback env `AUTO_SUMMARY_RETURN_LOOKBACK_DAYS` default 30 (= backfill sejak cutover pada run pertama), enumerasi 2-sisi (dok retur SENT + report AUTO existing → union) supaya hari yang kini **kosong** karena redate ikut dibersihkan. `RebuildReturnSummaryReport` meng-upsert report per (toko, channel, hari-retur) dari SEMUA dok `SENT` hari itu — `TotalItem`=ΣLineCount, `TotalAmount`=ΣBookedTotal, `SalesReturnNo`=daftar (invoice, return_number); bila hari kosong → soft-delete report AUTO basi (`_id` uuid, aman re-insert; koleksi tanpa unique index → report MANUAL+AUTO boleh koeksis). `UpsertAutoSummaryReport` kini ikut menyimpan `sales_return_no`. **Detail item (`GET /summary/reports/:id/items`)**: `buildAutoSummaryItems` dulu SELALU merakit item dari order *shipped/completed* (basis faktur) — untuk report RETUR window-nya = tanggal RETUR (bukan hari kirim), jadi query nihil → detail kosong + Total Rp0. Diperbaiki 2026-07-25: cabang `SALES_RETURN` merakit item dari **baris retur** dok anggota report (query dok `SENT` di rentang trans_date → `ListDailyReturnDetails` → agregasi per SKU+harga; `aggregateReturnSummaryItems` murni + test). Total card FE ikut benar (baca `meta.total_amount` endpoint items). Grounded: `usecase/accurate_return_summary.go`, `worker/tasks/auto_summary_report_return.go` (+ `_test.go`), `interface/http/transaction_handler.go` (`aggregateReturnSummaryItems`), `main.go`.
- **Ringkasan retur `/return/all` (✅ 2026-07-25)** — `GET /accurate/daily-returns/stats?channel&date_from&date_to` (satu pipeline agregasi `$match` = `buildListQuery` yang SAMA dgn `List`, jadi angka **rekonsiliasi** dgn tabel Auto-Sync Retur): `order_count` (Σ member, dok pra-Members=1), `item_qty` (Σ qty_reuse+rework+reject di `warehouse_items` = barang fisik discan gudang; refund-only/belum-discan=0), `refund_total` (Σ refund_amount), + breakdown penanganan gudang `menunggu_gudang`/`sudah_discan`/`terbukukan`/`gagal` (per `last_status` + `warehouse_confirmed_at`). SKIPPED/pra-cutover ikut terhitung (konsisten dgn tabel yang menampilkan baris "DILEWATI"). FE: [[APP - Web ERP]] `integration-accurate/return/all`. Grounded: `repository/AggregateReturnStats`, `interface/http/accurate_daily_return_handler.go` (`ReturnStats`).
- **Export raw multiple (✅ live 2026-07-13, PR #382/#383)** — `GET /transactions/summary/reports/orders/export-multiple?channel&shop_ids&date_from&date_to` → satu Excel, 1 sheet per toko (nama dari config Accurate, sanitasi Excel), baris urut shipped_at; kolom Accurate Number per hari-kirim order; maks 30 toko / 62 hari. Refactor `buildRawExportSheet` (single-report tak berubah perilaku). Catatan proses: #382 sempat nyangkut (merged ke base yang sudah masuk main) → diangkut PR #383. **Harga = base_price master (✅ 2026-07-26)**: kolom **"Unit Price"/"Amount"** memakai `ItemBasePrice` master Product (via `fetchAccurateProductCodesAndNames` → `accurateUseCase.GetProduct`), **BUKAN** harga marketplace — supaya **cocok dengan faktur Accurate** (yang juga base_price, keputusan finance PR #391). Shopee (`buildRawTransactionOrderShopeeRow`) menyusul TikTok yang sudah begitu sejak awal; harga marketplace **tetap tersedia** di kolom "Harga Awal"/"Harga Setelah Diskon"/"Total Harga Produk" (`ModelOriginalPrice`/`ModelDiscountedPrice`) → nol data hilang, format header finance-Java tak berubah (test `matchesFinanceJava` tetap hijau — hanya NILAI kolom harga yang berubah). ⚠️ base_price = master **saat ini** (bukan efektif per hari-kirim `item_price_histories` seperti faktur) → **identik** untuk SKU tanpa perubahan harga, bisa beda tipis untuk SKU yang harganya baru berubah (konsisten dgn perilaku TikTok). Grounded: `transaction_handler.go` (`buildRawTransactionOrderShopeeRow`/`buildRawTransactionOrderTiktokRow`/`fetchAccurateProductCodesAndNames`), tes `transaction_handler_shopee_export_test.go`.
- **Stok Accurate lokal (✅ live 2026-07-11, PR #340/#342/#344)** — koleksi `accurate_stocks` (salinan stok per item): diisi cron `accurate-stock-refresh` tiap 10 mnt (`list-stock.do` paginated, override-able via worker config DB) + webhook Accurate `POST /webhooks/services/accurate` (tipe `ITEM_QUANTITY`/`STOCK_MUTATION`; payload raw selalu ke `webhook_logs`, parser defensif `extractStockEvents` — format resmi belum terdokumentasi, cron = jaring pengaman). Endpoint FE: `GET /accurate/stocks/list` (search; param `only_products` default true = filter `item_no ∈ distinct(accurate_products.product_code)` — aset non-jualan tersaring) + `GET /accurate/stocks/listing` (per SKU listing: komponen bundle + `mapped` flag per komponen + stok efektif = floor(min(qty/qty_per_unit)); sku noise `^\d{13,}$` (id TikTok legacy) tersaring di `ListDistinctSKUs`). Notif Telegram RTS di-rename: "Faktur Accurate Terkirim (shipped)" / "Faktur Accurate GAGAL" / "Retur Penjualan Accurate". Setup manual (pending): daftarkan URL webhook `https://erp.bharatainternasional.com/ext/webhook/accurate` di portal developer Accurate.

### ICC Account Mapping (marketing leader + staff ICC)

> Grounded ke `internal/interface/http/icc_mapping_handler.go`. Lihat [[Sales - ICC Account Manager Mapping]].

- `GET /icc/mappings/me` — mapping milik staff ICC yang sedang login; **tanpa** `RequireMarketingLeader`; filter otomatis dari `BIP-Employee-ID` header
- `GET /icc/mappings` — daftar mapping aktif; `RequireMarketingLeader`; filter: `employee_id`, `tiktok_shop_id`, `tiktok_advertiser_id`, **`team`** (isolasi per departemen), `is_active` (default `true`)
- `POST /icc/mappings` — buat mapping; **`team` auto-fill dari `BIP-Department` header**; `tiktok_shop_id` dan `tiktok_advertiser_id` opsional (minimal satu wajib diisi); enrich nama dari `tt_shop_authorized_shops` & `tt_business_advertisers`; 409 jika duplikat
- `PATCH /icc/mappings/:id` — update `is_active` / `notes`
- `DELETE /icc/mappings/:id` — hapus (hanya jika `is_active=false`; else 409)
- `GET /icc/mappings/available-shops` — toko belum di-assign (pool global, tidak difilter per tim)
- `GET /icc/mappings/available-advertisers` — advertiser belum di-assign; pipeline `$unwind → $group by advertiser_id` untuk deduplikasi

Koleksi `icc_account_mappings`. Field baru (Phase 3): **`team string`** (diisi otomatis dari `BIP-Department`). Index: partial unique `(tiktok_shop_id != "", is_active=true)` + `(tiktok_advertiser_id != "", is_active=true)` — guard `!= ""` mengizinkan baris tanpa shop/advertiser; compound `(employee_id, is_active)`.

### Marketing Team & Shop ACL (admin only)
- `/marketing/teams` — CRUD tim marketing (gated `RequireIntegrationAdmin` = supervisor/admin module integration)
- Anggota tim: assign/unassign member (`/marketing/teams/:id/members`)
- **Shop ACL**: assign/unassign toko ke tim (`/marketing/teams/:id/shops`) — kontrol akses toko per tim marketing

### Fulfillment (WMS Bridge — internal)

> Endpoint internal untuk WMS Tinggarjaya. Dilindungi oleh gateway key global (`ValidateGateway`) — tidak ada middleware tambahan. Grounded: `usecase/warehouse_bridge_usecase.go`, `interface/http/warehouse_bridge_handler.go`.

- **`POST /fulfillment/ship-batch`** — batch ship order lintas platform. Body: `[{order_id, channel, shop_id, package_id}]`. **Non-all-or-nothing**; tiap item `success`/`awb?`/`error?`. TikTok = `tiktokUC.ShipOrder(packageID, shopID)`; Shopee = `shopeeUC.ShipOrder(orderSN, storeID)`. **Kredensial pakai model AKTIF**: `GetAuthorizedShop(shopID)` → `GetAccessToken(shop.CredentialID)` + env `TIKTOK_SHOP_APP_KEY/SECRET` (jalur lama `getStoreCreds`/`GetOrRefreshToken` membaca koleksi `credentials`/`platform_tokens` yang **tak pernah diisi** → RTS TikTok tak pernah jalan; fixed 2026-07). **Shopee "not eligible for rescheduling" → diperlakukan sukses** (order Shopee masuk WMS saat PROCESSED = pengiriman sudah diatur; jalur normal, bukan error).
- **`POST /fulfillment/labels`** — ambil shipping label per order. Body: `[{order_id, channel, shop_id, package_id}]`. Paralel max 5 worker. Response per item: `status` (READY|PROCESSING|FAILED), `url` (TikTok), `pdf_data` (Shopee base64).
  - **TikTok**: `GetShippingDocument` tipe **`SHIPPING_LABEL_AND_PACKING_SLIP`** (A6 PDF — resi + detail produk, sama seperti Seller Center) → parse `doc_url`.
  - **Shopee**: 4-langkah — (0) `GetShippingDocumentParameter` ambil **`suggest_shipping_document_type`** (tipe beda per toko: `NORMAL_AIR_WAYBILL` vs `THERMAL_AIR_WAYBILL`; tipe salah → download gagal `shipping_document_should_print_first`) + `GetTrackingNumber`; (1) `CreateShippingDocument` (sertakan `tracking_number` + `shipping_document_type`); (2) **poll berulang** `GetShippingDocumentResult` (segera + max 3× jeda 2 dtk; response field `result_list`); (3) `DownloadShippingDocument` bila READY. `shipping_document_type` konsisten di ketiga langkah. PROCESSING → FE auto-retry.
- **`POST /fulfillment/labels/merged`** — sama dengan `/labels` tapi menggabungkan semua PDF READY jadi **satu PDF** via `mergeLabelPDFs` (pdfcpu `MergeRaw`): TikTok URL diunduh, Shopee `pdf_data` langsung; non-PDF/gagal dilewati. Response `{pdf: base64, included: [order_id...], results}`. Untuk cetak batch 50–100 resi (satu tab, satu print). Grounded: `usecase/label_merge.go`.

### Items / Master Catalog
- CRUD lengkap: `/items/list`, `/items/sku/:sku`, history, create, bulk, bundle, variations, patch, delete

### Workers / Jobs
- `/jobs/status`, `POST /jobs/:name/trigger`, status/history/config/enable/disable
- `GET /health`

### Background Workers (cron)
- **Sync TikTok Shop orders** — **4× sehari** 00:00/08:00/16:00/23:00 WIB (`0 0 0,8,16,23 * * *`); window **48 jam** overlap by `create_time` (default `AddDate(0,0,-2)` di `tiktok_usecase.go`). *Re-enabled 2026-07-09 dengan jadwal diperbarui — sebelumnya 1× di 01:00 dan sempat disabled sejak Jun 2026 (era itu diganti cron host `tiktok-sync.sh`; script host di bip-vps DINONAKTIFKAN 2026-07-11 karena redundan + rusak port/key — worker ini pemilik tunggal sync order sekarang).*
  - **Status jujur (✅ fix 2026-07-23)** — sebelumnya `Run()` **selalu `return nil`** apa pun yang terjadi di dalam fan-out per-toko (`SyncOrders` rekursif via `WaitGroup` bersama, fire-and-forget, tanpa jalur balik error), jadi `worker_history.status` tetap "success" walau fetch order TikTok gagal total — pola sama dengan bug GMS Shopee yang sudah pernah diperbaiki (lihat §*Observability & Ketahanan Shopee* → *Anti-burst GMS + status jujur*). Insiden kredensial 2026-07-16 (0 dari 95 retur terbukukan, lihat §*Auto-Sync Retur* → *TikTok*) bisa terulang dengan gejala yang sama (semua terlihat sukses) sampai fix ini. **Perbaikan**: error collector titip lewat **context** (`usecase/tiktok_sync_errors.go`) — `SyncOrders`/`SyncOrderDetail` tetap fire-and-forget (concurrency model TIDAK dirombak, risiko tinggi utk cron produksi), tapi 5 titik yang tadinya cuma `slog.Error(...); return` sekarang JUGA melapor ke collector. Task mengumpulkan hasil per-toko (`ttShopSyncResult`) → `summarizeTTShopResults` (pola sama `summarizeShopResults` Shopee) → `Run()` return non-nil HANYA bila ADA toko yang benar-benar gagal, **tanpa** menghentikan toko lain. Grounded: `usecase/tiktok_sync_errors.go`, `usecase/tiktok_usecase.go` (`SyncOrders`/`SyncOrderDetail`), `worker/tasks/tt_shop_sync_orders.go` (`summarizeTTShopResults`).
- TikTok Business master-data — harian 00:00
- GMV-Max report (historis) — harian 01:00
- **GMV-Max report today** (intraday) — **4× sehari** 00:00/08:00/16:00/23:00
- Integration report — harian 02:00
- Video performance TikTok Shop — 02:30
- **Sync ulasan marketplace** (`sync-reviews`) — harian **06:45** (`0 45 6 * * *`): Shopee komentar incremental + snapshot recompute; TikTok distribusi bintang kumulatif per produk (1 call/produk, throttle 500ms) + enrichment nama produk. Lihat §Ulasan Marketplace
- **Affiliate orders sync** — 4× sehari (`0,8,16,23`), loop credential→authorized-shop, error isolated per-shop
- **Affiliate status refresh** — mingguan (Minggu 02:00): re-pull window 89 hari agar `settlement_status` + `actual_commission` lama ter-update
- **Affiliate commission validation** — harian 04:00: cross-check komisi SETTLED vs finance statement lokal (`tt_shop_transaction_by_orders`), stempel `validation_status`. Nol call TikTok API
- **Income reconciler TikTok** — tiap jam **:30, hanya 06:30–23:30 WIB** (skip dini hari — quota habis buat sync harian besar, breaker sering trip; antrian persisten di DB jadi order malam terambil run 06:30). Batch 600 order/run, limiter 5 req/s + circuit breaker 36009002 (5 fail beruntun → abort run). Klasifikasi hasil refetch: `fetched` (settlement masuk) / `not_available` (belum cair, re-scan tanpa bakar attempt; min-age 3 hari) / `pending` (error teknis, attempt++ cap 10). Ringkasan tiap run disimpan ke koleksi `reconciler_run_stats` → dipakai endpoint `settlement/sync-status` untuk baris info FE settlement
- **Shopee escrow reconciler** — tiap jam :00
- Webhook-consumer — tiap 5 detik
- ~~Desty-credential refresh — tengah malam~~ (dicabut 2026-07-12)
- Sync Shopee performance — 2 AM
- **Shopee escrow reconciler** — per jam (`0 0 * * * *`): re-fetch escrow order yang **RILIS** (via `get_escrow_list`) → koreksi `income` stale (fee difinalisasi setelah COMPLETED, mis. komisi AMS). Cursor per-toko by `release_time`, skip-if-match, breaker. Lihat *Reconcile income/escrow stale* di Observability.
- **Shopee order guardian** — `sync-shopee-orders` **tiap 4 jam** (`0 0 */4 * * *` = **6× sehari** 00/04/08/12/16/20 WIB, seed): **jaring pengaman** order Shopee bila push/webhook tak tersimpan dengan benar. Tarik-ulang order berkala dengan window **trailing-overlap** (default **8 jam**, env `SHOPEE_ORDER_GUARDIAN_LOOKBACK_HOURS`; filter `create_time`; window ≈ (N+1)×4j menahan N run gagal beruntun — mis. 8j tahan 1, 24j tahan 5) via `SyncOrders` (app **ERP_SYSTEM** `2032314` berkuota independen; breaker per-run + pacing + honest-partial + APIStats). **Sequential per-toko**, shared breaker, **status jujur** (`summarizeOrderResults` → `worker_history` reflektif; toko yang belum diproses saat rate-limit dilaporkan "not-processed", di-heal run berikutnya). Hook **Auto-Sync Accurate** **ikut terpicu** dari jalur cron ini: `OnOrderToShip`/`OnOrderCompleted` (faktur; dedup guard `SENT`+hash cegah edit redundan) **dan** `OnOrderCancelledOrReturned` (booking **Retur Penjualan** cancel-pasca-kirim — usecase guardian **di-wire `accurateRTSUseCase`**, `shopee_new_usecase.go` ~909) → menutup gap faktur order yatim **dan** menjadi **go-forward untuk cancel-after-ship** yang ke-flip di dalam jendela trailing (menutup catatan "⚠️ Guardian belum" di [[ADR - 0018 Faktur Permanen - Semua Pembalikan via Retur]]; batal di luar jendela tetap perlu `cmd/cancelreturnbackfill`). **Toggle live tanpa deploy** via endpoint job existing: `POST /jobs/sync-shopee-orders/{disable,enable}` (persist `worker_configs.enabled`), `POST /jobs/sync-shopee-orders/trigger` (backfill/recovery manual), `PUT /jobs/sync-shopee-orders/config` (retune schedule/retry/timeout). Menutup gap: order Shopee dulu **100% andalkan webhook** (`ShopeePushProcessor`) — beda TikTok yang punya webhook **+** cron `sync-tt-shop-orders`. ✅ **deployed 2026-07-22** (dikonfirmasi user; sebelumnya di-kode + unit-test sejak 2026-07-14). Grounded: `worker/tasks/shopee_orders_sync_task.go` (+ `_test.go`), `main.go` (registrasi, retry default 3× framework), `.env.example`; jadwal ringkas: [[IT - Background Jobs & Schedulers]].
- **Shopee return guardian** — `sync-shopee-returns` **tiap 2 jam** (`0 0 */2 * * *`, seed): menjalankan `SyncReturns` per toko lewat usecase yang **wired `rtsUseCase`**, sehingga ingestion memicu `SetOrderReturn` → `go SyncOrderReturn` → **membukukan Retur Penjualan** yang eligible. **Menutup bug prod (didiagnosa live 2026-07-15)**: transisi retur → **`ACCEPTED`** (satu-satunya state yang bisa dibukukan) nyatanya di-ingest oleh jalur yang **TIDAK membawa `rtsUseCase`** (mis. `cmd/ordersync` mengoper `nil`) → `order.return` jadi ACCEPTED di DB tapi trigger booking **tak pernah menyala** → **0 Retur Penjualan meski `RTS_RETURN_ENABLED=true`**. Terukur: `accurate_daily_returns` beku **11 jam** (isi cuma 1 baris FAILED) sementara faktur auto jalan normal; order `2607060EEVQ0AH` (KY, kirim 06 Jul, retur ACCEPTED, faktur sumber `INV/2026/07/06/014-KY+GB` **SENT** — semua gate `SyncOrderReturn` lolos) → **0 baris** `accurate_daily_returns`. Webhook code-29 memang datang (86 log) tapi umumnya di status **awal** (create/PROCESSING) yang benar belum boleh dibukukan; saat retur akhirnya ACCEPTED, update-nya datang dari batch nil-wired. Window **trailing 60 HARI** by `create_time` (env `SHOPEE_RETURN_GUARDIAN_LOOKBACK_DAYS`, default 60, clamp 120) — retur "menua" berminggu-minggu sebelum ACCEPTED (create Juli → accepted minggu berikutnya), jadi window jam-an ala guardian order takkan menangkapnya; residual: retur yang di-create >60 hari sebelum ACCEPTED tak ter-relist (naikkan env bila perlu). **Sequential per-toko** (timeout 10m/toko, 30m/run), shared breaker + APIStats, **status jujur** (`summarizeReturnResults` → `worker_history`; rate-limit stop bersih, toko sisa "not-processed"). Aman diulang: booking di-gate `RTS_RETURN_ENABLED` + **idempoten** (dedupe `return_sn`, persist-before-send, skip bila `SENT`) → run tumpang-tindih tak dobel-buku; sekaligus jadi **mekanisme backfill** retur tertunda. **Toggle live tanpa deploy**: `POST /jobs/sync-shopee-returns/{disable,enable}`, `POST /jobs/sync-shopee-returns/trigger` (backfill manual), `PUT /jobs/sync-shopee-returns/config`. ⚠️ **sudah di kode + unit-test, belum deploy**. Grounded: `worker/tasks/shopee_returns_sync_task.go` (+ `_test.go`), `main.go` (registrasi), `.env.example`; jadwal ringkas: [[IT - Background Jobs & Schedulers]].
- **TikTok return guardian** — `recover-tiktok-returns` **tiap 2 jam** (`0 30 */2 * * *`, seed; ✅ 2026-07-16): cermin guardian Shopee, tapi **recovery-based** — TikTok tak punya pencarian retur by-waktu, jadi memindai order TikTok **RETURNED** dalam jendela (env `TIKTOK_RETURN_RECOVER_LOOKBACK_DAYS`, default 30, clamp 90), mengisi data returnya via `FetchAndSetTikTokReturn` (search by order_id bila belum ada), lalu men-drive `SyncOrderReturn` — semua pengaman ada di sana (cutover retur, HIGH-C sadar-IMPORTED, sanity over-retur, dedupe-SENT; idempoten). **Menutup celah struktural**: retur TikTok dulu **hanya** lewat webhook type-12, tanpa penyapu — sekali webhook meleset/fetch gagal (bug kredensial 2026-07-16: 0 dari 95), retur hilang senyap. Gagal fetch dicatat sbg baris FAILED (terlihat di UI), satu order rusak tak menghentikan run. **Toggle live**: `POST /jobs/recover-tiktok-returns/{disable,enable,trigger}`, `PUT .../config`. ⚠️ **belum deploy**. Grounded: `worker/tasks/tiktok_returns_recover_task.go` (+ `_test.go`), `usecase/tiktok_return_usecase.go` (`RecoverReturns`), `main.go`.
- **Affiliate Shopee AMS** — `sync-shopee-affiliate-performance` (03:00; v1 snapshot per-affiliate + v3 product/content, isolated) & `sync-shopee-affiliate-conversion` (05:00; v2 ledger order-level + validasi komisi bulanan). Detail: bullet *AMS affiliate v1/v2/v3* di bawah.
- **WMS resi bridge** — **`sync-resi-wms` tiap 10 menit** (`0 */10 * * * *`): pull resi-feed TikTok (`/tiktok/shop/orders/resi-feed`, shift dari `rts_time`) + Shopee (`/shopee/orders/resi-feed`, shift dari `update_time`) via **watermark per-channel** di koleksi `sync_cursors` (`resi-wms`, `resi-wms-shopee`, cursor by `update_time`) → **push batch** ke manufacture `POST /resi/sync-batch` (upsert by `no_resi`). Didahului **`sync-shopee-tracking` (:05 & :35)** yang mengisi AWB Shopee via `logistics.get_tracking_number` untuk order shippable tanpa resi (Shopee tak menyimpan AWB di order-detail). **Fix sumber order (2026-07-13):** task dulu query `shopee_order_details` **by `shop_id`**, tapi field itu `omitempty`+int64 → **absen di ~88% dokumen** (shop_id set Mei 0%/Jun 3%/Jul 71%) sehingga task **buta ke mayoritas order** → coverage AWB mentok **~12%** (bukan batasan Shopee — `get_tracking_number` **terbukti kembalikan AWB untuk semua umur** order termasuk Mei). Kini task ambil order dari **`transaction_orders`** (shop_id string, selalu terisi), terbaru-dulu + skip yang sudah ber-AWB (`BackfillTrackingByOrderSNs`, breaker honest-partial). **Backfill one-off** (`cmd/ordersync MODE=trackingbackfill`, 8 shop, paced 5 req/s) mengisi **46.400 AWB** (100% success) → coverage **12% → ~99%** (Mei 100%/Jun 98%/Jul 100%). Grounded: `usecase/shopee_new_usecase.go` (`SyncShopeeTracking`/`BackfillTrackingByOrderSNs`/`ProbeTracking`), `repository/shopee_repo.go` (`GetOrderTracking` kini teruskan `ErrNoDocuments`/`SetOrderTracking`). Konsumen = WMS Master Resi [[Microservices - Manufacture Service]]; jadwal ringkas: [[IT - Background Jobs & Schedulers]].
  - **Bug watermark `SetOrderTracking` (✅ fix 2026-07-22/23, [[ADR - 0027 Status Sinkron Resi Terpisah dari Update_Time Marketplace]])**: `SetOrderTracking` men-`$set` `tracking_number` **tanpa** membarui `update_time` — begitu watermark `sync-resi-wms` sudah lewat timestamp lama order itu, AWB yang baru terisi **tak pernah tertangkap lagi**, permanen. Terbukti live: resi Shopee `SPXID064036697197` (order `260715Q6Q7MSWG`) ada & valid tapi tak muncul di Master Resi. Fix cepat: `SetOrderTracking` kini ikut membarui `update_time`. Fix struktural: `SetOrderTracking` di-`repository/shopee_repo.go`.
  - **`resi_sync_state` + jaring pengaman `resi-wms-safety-net` (✅ deployed & diverifikasi prod 2026-07-23, [[ADR - 0027 Status Sinkron Resi Terpisah dari Update_Time Marketplace]])**: koleksi baru `resi_sync_state` (satu dokumen per `(channel, order_id)` → `tracking_number` terakhir tersinkron) — **terpisah** dari dokumen order (order TikTok diganti PENUH tiap re-sync via `ReplaceOne`, field tambahan di sana akan hilang). Task baru **`resi-wms-safety-net` tiap 2 jam** (`0 0 */2 * * *`, pola sama `recover-tiktok-returns`/`sync-shopee-returns`) menyapu SEMUA order ber-AWB (abaikan cursor `update_time` sama sekali), diff terhadap `resi_sync_state` (`resiSyncStragglers` — order tanpa entri, atau AWB beda dari tercatat), push ulang stragglernya **per-chunk** (`resiWMSBatchLimit`=1.000, `MarkSynced` per-chunk — bukan sekali di akhir; ditemukan perlu saat first-run TikTok 214.734 straggler gagal dengan push sekali-jalan, 413/connection-reset). Cap sapuan **500.000** (dinaikkan dari 200.000 — TikTok sendiri sudah 213.302 order-ber-AWB) + **dilog eksplisit** bila tersentuh (cursor sapuan ephemeral — cap kecil bisa jadi blind-spot permanen sendiri). Jalur cepat (`sync-resi-wms`) ikut menulis `resi_sync_state` tiap sukses push. Backfill 1x order yang terlanjur macet: `cmd/resiwmsbackfill` (dry-run default, `--apply`). ⚠️ Gap terbuka: loop per-chunk belum mencocokkan `Synced` count balasan manufacture terhadap `len(chunk)` — detail di ADR-0027 §Consequences. First-run prod: 213.293/214.734 TikTok tersync, Shopee 57.683/57.683. Grounded: `domain/repository/resi_sync_state_repository.go`, `infrastructure/repository/resi_sync_state_repo.go`, `worker/tasks/resi_wms_safety_net_task.go`, `worker/tasks/sync_resi_wms.go` (`processChannel` kini juga `MarkSynced`).
- Redis queue digunakan untuk task summary-report
- Optimasi concurrency: global semaphore + sequential processing untuk mitigasi rate limit API marketplace
- **Notifikasi worker**: semua worker kirim Telegram otomatis via hook manager level — `WithOnJobError` → `SendError` saat gagal (setelah semua retry habis); `WithOnJobEnd` → `Send` saat sukses. Dikecualikan dari notif sukses: `webhook-consumer` & `sync-resi-wms` (frekuensi tinggi). 19 job ter-cover tanpa konfigurasi per-task. PR #322.
- ⚠️ **Jadwal task = DB source of truth**: schedule di-SEED sekali ke Mongo `workers.worker_configs` saat registrasi pertama; setelahnya **nilai DB yang berlaku** — mengubah `Schedule()` di kode TIDAK berpengaruh sampai dokumen `worker_configs` di-update manual (`db.worker_configs.updateOne({_id:"<job>"},{$set:{schedule:"..."}})`; efektif live tanpa restart). Boot log WARN bila schedule DB ≠ kode. Pause task sementara: tanam lock di `workers.worker_locks` (`_id: lock:<job>`, `expires_at` = auto-lepas). Insiden 4 Jul 2026: jadwal income-reconciler diubah di kode tapi prod tetap pakai config lama. Detail: `services/integration/internal/worker/README.md`
- ✅ **Tanggal cair (statement time)** — *merged (PR #355/#357) & LIVE; `income.paid_at` terisi ±258rb order per 2026-07-13; halaman Pencairan FE tersedia*: client `GetStatementTransactions` (`/finance/202501/statements/{id}/statement_transactions`, param wajib `sort_field=order_create_time`, verified live) → `statement_time` di raw finance + `income.paid_at` di unified + koleksi baru `tt_shop_statements` (batch payout: tanggal cair, payable, status — kacamata mutasi bank). One-off `cmd/statementenrich` sudah di-apply (sisa ±10,6rb order settled tanpa `paid_at` per 2026-07-13 — backfill lanjutan bila perlu). FE halaman Pencairan (`/statements` + `/:id/orders`) sudah ada. 1 statement ≈ ratusan order → enrich hemat quota.
- ⚠️ **Kategori akuntansi income + bridging receipt 6-key** — *merged ke main (branch `feat/payout-detail-fields`); catatan: keputusan finance 2026-07-12 untuk receipt OTOMATIS memilih net-polos (tanpa DetailDiscount/kv — lihat spec `bip-erp/docs/specs/2026-07-11-accurate-receipt-payment-status-design.md`), sehingga 6-key dipakai jalur receipt MANUAL (Sistem B); kv prod masih kosong*: `TransactionIncome` +4 kategori (platform commission, transaction fee, tax, adjustment); transform Shopee full-mapping (commission_fee yang dulu hilang, ongkir tak lagi hardcode 0, ssd dobel-hitung difix) & TikTok dikategorikan dari `fee_tax_breakdown` (konvensi beban POSITIF — fix bug tanda lama); `NormalizeIncomeIdentity` validasi `GMV − Σkategori = settlement` (residual→adjustment, test 4 kasus). Receipt Accurate = **6 key KV** (5 lama + `settlement-adjustment`; komisi/trxfee digulung ke service-fee, tax+adjustment+refund ke settlement-adjustment; basis paymentAmount = TotalOriginalPrice) — balance > granularitas GL, rincian tetap di field income & raw. +`GET /accurate/gl-accounts` (bagan akun live buat dropdown Config; koleksi `accurate_kv_configs` prod masih KOSONG — finance wajib isi 6 akun sebelum cutover). One-off `cmd/incomeretransform` (dry-run default) re-transform income historis ke konvensi baru — WAJIB sebelum fitur dipakai (data lama campur tanda).
- ⚙️ **Auto Sales Receipt — guard cutover hari-kirim (PR #465, 2026-07-16)**: receipt mengalokasikan order ke faktur harian by HARI-KIRIM (`allocateInvoices`, dipakai Shopee+TikTok). Order shipped **pra-cutover** (`WIBDateKey(shippedAt) < autoSyncCutoverDateWIB=20260710`, era faktur MANUAL finance) → **skip** (jangan cari faktur, jangan FAILED) — akar 22 receipt FAILED "faktur belum dibuat" (hari-kirim Jun–9 Jul). **Balance dijaga**: bila ada order pra yang diskip → `cheque = Σpayout(order pasca) LANGSUNG` (BUKAN `escrow−skippedPayout`: identitas Σpayout≈escrow tak persis, pembulatan order-vs-wallet terbukti prod selisih Rp1.050 > toleransi Rp1 `verifyReceiptBalance`); tanpa order pra → cheque asli (`d.Amount`/`payable`, jaga regresi). Semua order pra → status **`SKIPPED`** (enum baru, bukan FAILED, nol jurnal — income era manual dibukukan finance). Cutover hari-kirim = konstanta (fakta historis), beda sumbu dari `receipt-cutover-date` KV (batas hari-CAIR).
- ✅ **Auto Sales Receipt — `paymentAmount` = BASE PRICE (model Sistem B, PR #526, 2026-07-17/18)**: `buildReceiptPayload` mengisi field Accurate **`paymentAmount` (kolom "Bayar") = base price porsi order** (`basePorsi` dari `priceByInvoice`), BUKAN net settlement/payout — meniru receipt Sistem B (income-sync lama) yang finance anggap benar (terverifikasi ke Accurate `BKK/001/06/26/BMRI-122` baris faktur `INV/2026/05/28/009-BH`: `paymentAmount`=270.000=base, `discountAmount`=64.709=Σbeban, `invoicePayment`=205.291=kas net; 16/16 baris invariant `base − Σdiscount = net = payout`). `DetailDiscount` (Diskon) = fee riil per akun + **Potongan Penjualan penyeimbang** (akun discount 4003) sehingga **Σ DetailDiscount = basePorsi − payout**; net kas per baris = `paymentAmount − Σ DetailDiscount` = **payout**. `chequeAmount` = Σ payout (nilai TAK berubah). `verifyReceiptBalance` kini cek **`Σ(paymentAmount − Σ DetailDiscount) == cheque`** (net kas, bukan Σ paymentAmount). **Guard basePorsi gagal** (faktur bundle SKU komponen `unitPrice` 0 / SKU order tak match `itemNo` faktur → basePorsi 0) → net timpang → **FAILED** (nol jurnal salah). Prod aman: `allocateInvoices` selalu isi `priceByInvoice` dari `GetSalesInvoiceDetail` faktur SENT. Retry 89 receipt (10–17 Jul) EDIT in-place — Bayar berubah settlement→base, cheque tetap, tak duplikat. **Sisa 4–5 receipt FAILED** = order **era-manual pra-10 Jul dalam statement Juli** (basePorsi tak terhitung; contoh 004-BH 334/335 order pra-10, 0 bundle) — konsisten income 1-9 Jul manual finance; receipt sehat (spt `INC/2026/07/17/017-BH`) skip order pra-cutover dari cheque → SENT balance. *(Menggantikan basis `paymentAmount = TotalOriginalPrice` / "net-polos" di catatan 6-key line atas.)*
- ✅ **Auto Sales Receipt — adjustment→akun admin (KV `settlement-adjustment`, 2026-07-17)**: `total_adjustment` (residual identitas income, ~1.250/order) dipindah dari akun Potongan Penjualan (4003) ke **akun admin (6112, `service-fee`)** dgn set KV `settlement-adjustment=6112` (tanpa deploy) → laporan per-akun cocok Excel finance (admin gabung adjustment). Retry 89 receipt menerapkannya (verified `INC/2026/07/15/009`: admin 6112 naik +adjustment, baris 4003 tinggal potongan murni). Mapping akun receipt via `loadReceiptAccounts` pakai **KV key lama** (`service-fee`=6112, `shipping-cost`=6114, `discount`=4003, `affiliate-commision`=6113, `advertising-cost`=6107, `other-income`=8001, `insurance-fee`=6122, `tax-pph`=6121), bukan `settlement-*` (mayoritas kosong).
- ✅ **Auto Sales Receipt — rekening tujuan = KAS TOKO per-toko (2026-07-18)**: `accurate_shops.bank_account_no` per toko diubah dari bank riil (129xxx BNI CV) ke **KAS TOKO** (akun BANK 13xxxx: 131=tiktok/132=shopee/133=tokopedia/134=lazada, KTB Beautyhacks/KTG Glowbooster/KTK Kyura — sumber `COA KAS TOKO.xlsx`, 40 akun) → uang cair MP mengendap di kas toko dulu; withdrawal riil finance = jurnal transfer kas toko→bank terpisah (finance tak seolah-withdrawal tiap saat). Config via FE Config Accurate (field "Rekening Bank", dropdown GL `accountType=CASH_BANK`). Mapping = keputusan FINANCE (nama shop DB ≠ COA, auto-map salah ~40%); 24 shop terisi. Retry 89 receipt (10–17 Jul) EDIT in-place → jurnal re-post bank-riil→kas-toko (verified bankNo Accurate 13xxxx); ≥18 Jul auto kas toko.
- ⚙️ **Backfill faktur order semua-cancel (2026-07-17)**: faktur harian dipicu webhook SHIPPED (`OnOrderToShip`); (shop, hari WIB) yang **SEMUA order-nya CANCELLED** (order langsung batal tanpa event SHIPPED sampai) → snapshot tak terpicu → faktur tak lahir → order cancel tak muncul di sales (kontradiksi #511 FAKTUR PERMANEN). Fix per-kejadian: `POST /accurate/daily-invoices/backfill` (`BackfillPreCutoverInvoice`, terima era-auto meski namanya "PreCutover") — 5 kombinasi 12 Jul di-backfill (INV/2026/07/12/009–013-KY+GB); 75/75 order cancel+shipped ≥10 Jul kini masuk faktur. **Gap**: masih manual per-kejadian, sweep otomatis (deteksi shop-hari punya order shipped_at tanpa faktur) belum ada.
- **One-off CLI recovery** (`cmd/backfill`, `cmd/settledfeed`; dry-run default, idempotent, `--apply` untuk tulis ke DB): `backfill` = migrasi field `income_status` doc lama + re-insert order raw-only (order_date dari `create_time` asli — summary group by order_date); `settledfeed` = feed order affiliate SETTLED → by-order finance fetch (interleave per-shop, adaptive backoff 36009002 cool-down 5/10/15s, newest-first). Dipakai recovery Jul 2026: coverage finance affiliate-SETTLED 15,7% → 100% (~78rb order, ~26 jam)

### Gross Profit per Product (modul profit)

> ⚠️ Bagian ini ada di **working branch `feat/gross-profit` (sudah di-push ke origin) yang BELUM di-merge/deploy**. Ubah marker ke ✅ setelah merge. Deploy note: drop index unik lama `sku_1_tiktok_item_id_1_product_name_1` di `product_sku_mappings` (index baru menambah `master_sku`). Spec: `bip-erp` sibling `docs/superpowers/specs/2026-07-03-gross-profit-submodule-design.md`.

Rumus per produk/SKU (TikTok) — **"Laba Sejati"** (blueprint dashboard marketing):
`GMV − biaya marketplace (breakdown) − promo = NET SETTLEMENT`, lalu `NET SETTLEMENT − HPP − iklan = LABA KONTRIBUSI` (retur = kolom informasi).

- **Sumber komponen**: settlement per-SKU dari `tt_shop_transaction_by_orders` (GMV `subtotal_before_discount`, promo `seller_discount`, fee breakdown komisi/affiliate/proses/ongkir, retur refund, `settlement_amount`; identitas kolom eksak, residual di `other`) — join `sku_id→seller_sku` via `tt_shop_orders.line_items` (per-order → kamus global → fallback); qty & bucket **per hari WIB** dari `transaction_orders` (grain assembly = `(SKU, hari, channel)`, bucket `%Y-%m-%d`); iklan = GMV Max `metrics.cost` per `dimensions.item_group_id` (= ID produk; `item_id` = level kreatif — verified) dengan split prorata revenue untuk listing multi-claim; HPP = **presisi TANGGAL order** (`SelectEffectiveCost`: `product_costs` dengan `effective_from` terbaru ≤ tanggal order) — order lama pakai HPP lama, order baru pakai HPP baru walau HPP berubah di tengah bulan. Rate **estimasi** settlement tetap diagregasi per **bulan** (`monthOf(day)`) supaya sampel rate tidak terlalu tipis.
- **Estimasi order belum settle**: rate komponen/GMV agregat per bulan dari data settled → komponen order pending diestimasi; `estimated_share` dilaporkan per baris & agregat (transparansi, terganti otomatis saat settlement masuk).
- **Mode cair** (`?mode=settled`, default FE): bucket sales diganti angka settlement actual (`SettledOnlySales` — revenue=GMV settled, qty=qty settled, bucket belum cair dibuang) → seluruh breakdown murni actual tanpa estimasi; iklan diprorata porsi cair per SKU (`applySettledAdsScale`) karena spend harian tak mengenal status settle — **plus** iklan/laba/margin tiap periode HPP (`Periods[]`, lihat butir *Pecah per periode HPP*) diprorata ulang agar konsisten dengan induk. Mode default (tanpa param) = semua order.
- **Dibayar Pembeli** (`buyer_paid`): `total_payment` order (uang dibayar buyer = setelah diskon + ongkir, SEBELUM potongan komisi TikTok) diprorata ke SKU dengan share `items.total` — konsisten cara fee di-split. = kolom "Order Amount" export Semua Pesanan TikTok (verified match). Bucket settlement-only (order yatim) tanpa total_payment → buyer_paid 0.
- **Klasifikasi Komisi**: `fee_commission` = platform + referral + **dynamic commission + flash-sale + voucher-xtra** (dynamic terverifikasi prod > platform commission, Jun 2026: 313jt vs 226jt — sebelumnya nyangkut di residual Proses); `fee_affiliate` = organik + partner + **affiliate ADS commission**; `fee_process` = residual: cashback program + biaya infrastruktur tetap (`vn_fix_infrastructure_fee`) + mall service fee + pajak/lainnya (porsi fluktuatif antar periode). **Pecahan informasi** dibawa breakdown & tampil di kartu KPI, kolom tabel (grup − BIAYA MARKETPLACE), dan sub-baris waterfall panel: `fee_comm_dynamic` (komisi dinamis; dasar = commission − dynamic) + `fee_proc_cashback/infra/mall` (lainnya = process − 3) — semua subset, ikut Add/Scale/estimasi/projection.
- **Arus dana** (`GET /profit/cash-flow`): kartu **Sudah Cair** (Σ `income.total_settlement_amount` order `income_status=fetched` — actual) vs **Uang Gantung** (COMPLETED belum settle — estimasi `GMV × rate_toko`, shrinkage `(net+global×K)/(gmv+K)` K=100jt, window belajar order settled 21/45 hari terdekat; backtest Apr+Mei total ±3,0%, per-toko 4–6%) + rincian toko×bulan + `accuracy_30d` (track-record rumus dihitung on-the-fly). Granularitas repo per (toko, hari WIB) — `AggregateCashFlowDaily`. Kartu Sudah Cair juga menghitung **order yatim** (settlement tercatat tapi order tak pernah masuk pipeline — 9.625 order drift order-list historis; `AggregateOrphanSettledDaily` anti-join) supaya konsisten dengan tabel; tooling pemulihan: `cmd/orphanbackfill` (tarik detail by-ID batch 50, dry-run default).
- **Bundle**: SKU multi-komponen dialokasikan ke produk komponen dengan rasio nilai HPP (revenue/fee/net di-share; HPP exact per komponen; qty = qty_bundle × qty_per_unit); **HPP komponen ikut presisi tanggal** (`SelectEffectiveCost` per tanggal order, sama seperti produk tunggal — komponen bundle pun pecah per periode HPP-nya sendiri); daftar `bundles` terpisah di response sebagai view informasi (overlap dengan products, jangan dijumlah).
- **Pecah per periode HPP** (`ProductProfit.Periods []ProductProfitPeriod`): bila HPP satu produk **berubah di tengah rentang tanggal**, baris produk dipecah **per periode HPP** — bukan per hari (`SplitDaysByHPPPeriod`: batas periode = tiap `effective_from`). Induk = TOTAL (jumlah semua periode), `Periods[]` = rincian per periode (terisi hanya bila >1 periode). **SEMUA kolom** dipecah per periode (qty/GMV/net/HPP/iklan/laba/margin) — **tidak dirata-rata**. Produk yang HPP-nya tak berubah = 1 periode → `Periods` kosong (tak dipecah). Konsumen FE render induk "Total" + sub-baris expandable per periode. Grounded: `domain/entity/profit.go` (`SplitDaysByHPPPeriod`, `ProductProfitPeriod`, `SelectEffectiveCost`), `usecase/profit_usecase.go` (`dayAsTime`, `accPeriod`), `usecase/profit_settlement.go` (`monthOf`, prorata Periods mode cair).
- **Koleksi baru (integration_db)**: `product_costs` (HPP per produk, riwayat per `effective_from`, `source: upload|accurate` — siap sync Accurate fase berikut) + `product_sku_mappings` (kunci unik `(sku, tiktok_item_id, product_name)`; 1 SKU boleh multi-baris: multi-toko per item_id & bundle per komponen dengan `qty_per_unit`; HPP dihitung sekali per komponen distinct).
- **Arsitektur hitung**: aggregate on-read (pipeline Mongo + fungsi murni `AssembleProductProfit` di usecase, unit-tested) — tanpa ETL/snapshot baru.
- **Identitas produk = SKU master** (mis. `PJB-002`): HPP, roll-up produk, dan dedup komponen di-join per `master_sku`; NAMA hanya alias/tampilan (case-insensitive, boleh berubah antar upload — yang tampil nama dari HPP terbaru). Baris mapping per product listing: `master_sku` sama = alias (HPP sekali), beda = komponen bundle. Terminologi: **Product Listing** = teks SKU penjual di order; **SKU** = SKU master.
- **Input HPP**: upload xlsx finance — format baru `SKU | Produk | HPP/Pcs` (header by-nama, posisi bebas) ATAU format lama 2 kolom (SKU di-resolve dari nama sekali saat upload; sisanya legacy by-nama). Baris berawalan CONTOH dilewati. Plus **CRUD satuan manual** dari UI (`POST/PUT/DELETE /profit/costs[/:id]`, `source: manual`). Authz upload/mapping/CRUD: `supervisor|admin` modul `integration` ATAU `finance`; lainnya view-only.
- **Seed mapping 2 fase**: fase 1 dari master `items` (komponen bundle + qty); fase 2 match SKU liar (tak terdaftar master) ke nama HPP finance via normalisasi/token (auto kalau kandidat tunggal, `suggestions` kalau ganda — indikasi bundle).
- **Transparansi data bolong**: response bawa `pending_income_items` (order COMPLETED belum settle), `unmapped_skus`, flag `missing_hpp` — tidak ada angka diam-diam.
- **Filter**: all / per account (credential TikTok Shop → resolve authorized shops) / per toko (`shop_id`) / per produk / per SKU (iklan level SKU = prorata share revenue, granularitas asli GMV Max per item_id).
- **Multi-channel (Shopee ditambahkan, 2026-07)**: engine kini menghitung TikTok DAN Shopee **terpisah**, bukan digabung — produk yang sama di 2 channel tampil sebagai 2 baris beda (`ProductProfit.Channel`), kunci komposit `SKU-atau-nama + Channel` konsisten di seluruh pipeline (sales, settlement, bundle, unmapped). **Shopee settlement** berbeda struktur dari TikTok: tidak ada koleksi settlement per-SKU terpisah (padanan `tt_shop_transaction_by_orders`-nya TikTok) — income Shopee menempel di level ORDER (`transaction_orders.income`), diprorata ke tiap item by kontribusi revenue (`entity.AggregateShopeeSettlementDocs`). Field `income_status` **tidak pernah diisi** utk order Shopee oleh proses aktif manapun — verified live: 100% order Shopee prod & dev = `""` (`ResolveIncomeStatus` hanya dipanggil dari path sync/webhook **TikTok**, tak pernah dari Shopee); satu-satunya sumber nilai `not_applicable` yang mungkin nampak di sebagian order Shopee lama adalah migrasi **one-off** `cmd/backfill` (dijalankan sekali historis, BUKAN reconciler yang berjalan terus — lihat koreksi di §*Reconcile income/escrow stale* di atas). Karena itu, status "settled" Shopee di seluruh engine (termasuk Cash Flow di bawah) dicek via `income != nil`, BUKAN `income_status`. **Iklan Shopee** sengaja TIDAK dihitung terpisah per-SKU (`TotalAdvertisingCost` diabaikan saat agregasi settlement Shopee) karena biaya iklan Shopee sudah inline di `income.total_settlement_amount` — menghitung lagi akan dobel-hitung (selaras keputusan channel-aware [[ADR - 0008 Profit Engine Join via item_group_id]]).
- **Cash Flow (Sudah Cair / Uang Gantung / Rincian per toko)**: `GET /profit/cash-flow` + `GET /profit/cash-flow/orders` — kartu **Sudah Cair** (settlement actual, angka pasti) vs **Uang Gantung** (order COMPLETED belum settle, estimasi ± via rasio shrinkage yang dipelajari dari order settled 21 hari terakhir per toko, fallback 45 hari), plus rincian per toko×bulan + indikator akurasi historis rumus (backtest order yang dulu gantung, kini settle). **Channel-aware** (`channel=TIKTOK|SHOPEE`, default TIKTOK bila kosong): definisi "settled" ikut aturan settlement per-channel di atas. Dua konsep TikTok-only secara **struktural** (bukan sekadar belum sempat dikerjakan): (1) **order yatim** (`AggregateOrphanSettledDaily` — settlement tercatat tapi order tak pernah masuk pipeline) berasal dari drift koleksi settlement TERPISAH TikTok yang tak punya padanan di Shopee, jadi di-skip total utk Shopee; (2) **join akun/kredensial** di panel rincian order gantung — `ShopeeCredential` menyimpan `shop_id_list` (1 kredensial → banyak toko), kebalikan `Credential` TikTok yang 1 toko → 1 kredensial, jadi Shopee tak punya join sesederhana TikTok (`AccountID`/`AccountName` dikosongkan, `account_id` query param diabaikan bila `channel=SHOPEE`). **Temuan empiris** (mongosh, prod+dev, beberapa rentang tanggal): Shopee mencairkan nyaris seketika saat order COMPLETED (0 order gantung dari puluhan ribu order diuji) — beda jauh dari TikTok yang genuinely lag 7–14 hari; kartu Uang Gantung karenanya wajar tampil kecil/nol utk Shopee, bukan indikasi data hilang. Grounded: `usecase/profit_cashflow.go`, `infrastructure/repository/profit_repo.go` (`AggregateCashFlowDaily`/`ListHangingOrders`/`ListShopNames`), `interface/http/profit_handler.go`. Rute lengkap: [[API - Integration Service]].
- Konsumen: [[APP - Web ERP]] halaman `/integration/gross-profit`. Rute lengkap: [[API - Integration Service]].

### Ulasan Marketplace (Review Analytics) ✅ (PR #564/#566/#567 merged & LIVE; #568 feed global — PR terbuka saat ditulis)

- **Tujuan**: analitik rating & ulasan untuk brand — **bukan** modul balas ulasan (balas tetap di Seller Center). Sumber data per channel **asimetris by design** (hasil riset API resmi 2026-07-19): **Shopee** = teks ulasan penuh baca-saja (`GET /api/v2/product/get_comment`, cap **500 komentar/query** via OpenAPI → flag `backfill_truncated`); **TikTok** = TIDAK ada API teks ulasan sama sekali — hanya distribusi bintang **KUMULATIF all-time** per produk via Analytics (`/analytics/202509/shop_products/{id}/performance` → `ratings[]`; rentang tanggal TIDAK berpengaruh pada ratings — temuan harness mengikat).
- **Koleksi** (`integration_db`): `marketplace_reviews` (komentar Shopee per `comment_id`, teks/media/balasan), `product_rating_snapshots` (agregat per produk per tanggal WIB; SHOPEE = harian sejati `is_cumulative:false`, TIKTOK = kumulatif per tanggal sync `is_cumulative:true`), `review_sync_states` (status per toko), `review_product_names` (cache nama produk `{channel}:{product_id}`).
- **Aturan agregasi kritis**: SHOPEE = SUM antar snapshot harian; TIKTOK = **snapshot terbaru per produk** (SUM = double-count); tren TikTok di FE = **delta antar snapshot kumulatif**. Snapshot Shopee **di-recompute dari DB** untuk rentang tanggal tersentuh (bukan dari fetch parsial — fix bug undercount kritis, `recomputeShopeeSnapshots`).
- **Enrichment nama**: nama toko dari `shopee_shop_infos` + `tt_shop_authorized_shops` (query-time); nama produk dari order (`transaction_orders.items` / `tt_shop_orders.line_items` — field TikTok `store_id`) + **fallback API produk resmi** utk produk tanpa order (Shopee `get_item_base_info` batch 50; TikTok `GET /product/202309/products/{id}`, throttle 500ms, cap 200/toko/run). Coverage 856/856 produk per 2026-07-19.
- **Endpoint** `/reviews/*` (summary, products, products/:id/trend, comments, sync-status) — rincian & param filter: [[API - Integration Service]]. Filter: `rating_avg` (bucket floor(avg)), `with_star`, `min_reviews`, `has_text`/`has_media`/`unreplied` (Shopee), comments global lintas toko (`shop_id` opsional + join `product_name`/`shop_name` — PR #568).
- **Worker** `sync-reviews` harian **06:45 WIB** (digeser dari 02:30 — hindari bentrok quota API dengan rumpun sync tengah malam), lock + notif Telegram, error per-toko terisolasi. Sync perdana manual 2026-07-19: 26 toko (8 Shopee ERP_SYSTEM + 18 TikTok), 3.731 review + 676 snapshot, 0 error. `internal/cmd/cron.go` = dead code (jalur hidup = worker framework).
- **Harness** `cmd/reviewtest`: mode live-test (`shopee|tiktok|tiktok-list`), smoke (`syncone|synctt`), `backfillnames`. Konsumen FE: [[APP - Web ERP]] `/integration/reviews` (2 tab Ringkasan|Komentar).
- Grounded: `internal/usecase/review_sync_usecase.go` + `review_query_usecase.go`, `internal/infrastructure/repository/review_repo.go`, `internal/infrastructure/clients/{shopee_client_new,tiktok_client}.go`, `internal/worker/tasks/sync_reviews.go`, `internal/interface/http/review_handler.go`.

### Observability & Ketahanan Shopee

> ⚠️ Bagian ini ada di **working branch yang BELUM di-merge/deploy** (prod masih menjalankan kode lama). Didokumentasikan agar tidak hilang; ubah marker ke ✅ setelah merge.

- **Notifikasi success-rate API Shopee per-run** — setiap cron `Sync Shopee performance` (02:00) **dan** endpoint sync manual (`/shopee/orders/sync`, `/shopee/gms/*/sync`) mengirim ringkasan **success-rate per-endpoint + overall** via Telegram di akhir tiap run. Penghitung **in-memory per-run** (dibawa di `context`) — tanpa DB, tanpa payload/PII. Tujuan: visibilitas success-rate tanpa bergantung dashboard Shopee yang delay.
- **`auth_partner` selalu 0% success di dashboard = expected (bukan bug)** — di **API Statistics** Shopee Open Platform, endpoint **`v2.public.auth_partner`** (path `/api/v2/shop/auth_partner`) konsisten tampil **0% success rate** di ketiga app (ERP_SYSTEM `2032314` / ADS_SERVICE `2032638` / AMS `2038141`). Sebabnya: `auth_partner` **bukan** call API server-to-server ber-JSON, melainkan **URL otorisasi berbasis browser** (OAuth-style) yang **dibuka merchant di browser** → Shopee membalas **redirect (302)** ke halaman login/consent, **bukan** respons API sukses. Dashboard menghitung "success" hanya untuk respons API 2xx valid, jadi redirect di-klasifikasi **fail** → 0%. Kode kita **hanya menyusun string URL** lalu menyerahkannya ke pemanggil (browser); **tak pernah HTTP-call** endpoint ini. **Indikator auth benar-benar sehat** = **`get_access_token` + `refresh_access_token` ~100%** (token exchange `/api/v2/auth/token/get` + siklus refresh jalan) — kalau `auth_partner` benar-benar gagal, mustahil `get_access_token` bisa 100% (tak ada `code` untuk ditukar). Fail-count kecil (mis. 12/5/10 pada 01–03 Jul 2026) = jumlah percobaan **(re-)otorisasi/re-auth shop** di periode itu, bukan call otomatis berulang. Grounded: `clients/shopee_client_new.go` (`GenerateAuthURL`, `GetAccessToken`), `usecase/shopee_new_usecase.go` (`GetAuthorizeURL`).
- **Ketahanan refresh & sync Shopee** (akar masalah restriction rate-limit Shopee, lihat [[LOG - Shopee API Rate Limit Request]]): distributed lock refresh-token per-shop (Redis) cegah race antar-instance + **circuit breaker** yang menghentikan run begitu Shopee balas rate-limit (`error_limit`, `error_too_many_request`, atau `ads_rate_limit_shop_api` — throttle per-shop pada endpoint ads/GMS) (cegah death-spiral kuota) + retry simpan credential + timeout independen per-shop + observability (kegagalan GMS di level *usecase* di-log & di-return). ⚠️ **Gap (diperbaiki oleh hardening di bawah)**: status **worker** `sync-shopee-performance` tetap `success` walau semua GMS gagal (task `return nil` per-toko), sehingga `worker_history` tak mencerminkan kegagalan — ini yang membuat "cron jalan tapi data GMS tak masuk" tak terlihat sejak ~24 Jun 2026.
- **Webhook order ingest — TikTok vs Shopee** (jalur "direct"/push, registry `(platform, source) → processor`): keduanya kini menarik order detail dari API platform, tapi beda strategi. **TikTok direct** (`TiktokDirectProcessor`) **selalu** tarik order detail dari API TikTok tiap webhook (bisa buat order baru). **Shopee push** (`ShopeePushProcessor`, code 3) kini **create-on-missing**: order yang sudah ada → update status saja (0 call API); order baru → tarik order detail dari Shopee (buat order). Sebelumnya Shopee-push hanya update-status & **error** bila order belum ada. "Lepas dari [[External - Desty]]" **tercapai 2026-07-12** — processor Desty (TikTok & Shopee) tidak diregistrasi lagi. **Jaring pengaman order Shopee** = cron `sync-shopee-orders` (lihat *Background Workers*): tarik-ulang order berkala (window trailing-overlap) bila push tak tersimpan — menyamakan Shopee dengan TikTok (webhook + cron), analog `SyncReturns` sebagai jaring pengaman webhook retur — yang **baru benar-benar terjadwal sejak worker `sync-shopee-returns` (2026-07-15)**; sebelumnya `SyncReturns` hanya bisa dipanggil manual/CLI, sehingga jaring pengaman retur **de facto tak pernah ada**.
- **Rate-limit gate Shopee** (proses-wide, in-memory): begitu ada call Shopee balas `error_limit`, gate menyala sampai **00:00 UTC+8** (reset kuota Shopee). Webhook order-fetch cek gate **sebelum** call — bila menyala, **skip + defer** (retry setelah reset) agar tidak menambah call gagal saat kuota habis. Push code ≠ 3 / tanpa `order_sn` → di-acknowledge tanpa error.
- **Dual-app credential per toko (routing per `account_type`)** ✅ live — tiap toko Shopee kini bisa punya DUA kredensial: **ADS_SERVICE** (app "Ads Bharata", partner `2032638`) untuk **GMS** (`get_gms_*`), dan **ERP_SYSTEM** (app "One Bharata", partner `2032314`) untuk **order/escrow/push** (`get_order_*`, `get_escrow_detail`). Sebab: kategori app Shopee memisahkan **Ads vs ERP secara desain** — tak ada satu app yang punya izin GMS + Payment/escrow sekaligus. Resolusi kredensial sadar-API-group (`getAndRefreshCredentialByType`): GMS→ADS_SERVICE, order/escrow→ERP_SYSTEM; **degrade gracefully** bila salah satu cred belum ada (skip tanpa fail). Index unik compound `(shop_id_list, account_type)` mengizinkan kedua cred hidup berdampingan; `/shopee/accounts/list` mengembalikan `account_type` + `shop_id_list` (frontend menggabung 2 cred jadi 1 kartu per toko). Grounded: `usecase/shopee_new_usecase.go`, `repository/shopee_repo.go`.
- **AMS affiliate — `account_type` KETIGA (tri-app)** ✅ live (spike terverifikasi prod 2026-07-03) — menyusul dual-app, ditambah **`AMS`** (app "Affiliate Bharata", partner `2038141`, kategori Shopee **"Affiliate Marketing Solution Management"**). AMS OpenAPI (`/api/v2/ams/*`, modul 127) **HANYA** bisa dipanggil app kategori ini — ADS (`2032638`)/ERP (`2032314`) **tak bisa** (sama seperti Ads-vs-ERP: kategori app fixed). **v1 hybrid sync-ke-DB** ✅ live (deploy + Sync manual terverifikasi prod 2026-07-03): **performa disimpan**, rekomendasi tetap **proxy**. Endpoint: **`GET /shopee/affiliate/performance?shop_id=&start_date=&end_date=`** (yyyymmdd; **baca DB** `shopee_affiliate_performance`, di-roll-up per-affiliate; default 30 hari s/d kemarin) + **`POST /shopee/affiliate/sync?shop_id=&start_date=&end_date=`** (backfill manual: tarik AMS `get_affiliate_performance` **1 panggilan per hari** — API agregat per-periode tanpa breakdown harian — paginate `has_more` page_size ≤20, upsert snapshot harian) + **`GET /shopee/affiliate/recommended?shop_id=&limit=`** (`get_recommended_affiliate_list`, proxy live). **Koleksi `shopee_affiliate_performance`**: grain `shop_id`+`affiliate_id`+`date`, `_id`=`<shop>_<aff>_<yyyymmdd>` (idempoten), dimensi kanonik **Day/AllChannel/ConfirmedOrder**; agregasi menjumlah metrik aditif + **ROI dihitung ulang = ΣSales/ΣKomisi** (bukan rata-rata). Worker harian **`sync-shopee-affiliate-performance`** (03:00 WIB, window mundur 3 hari, lihat [[IT - Background Jobs & Schedulers]]). Route sadar-account-type (`getAndRefreshCredentialByType(shopID, AMS)`); money field AMS = **string desimal**, `roi` bisa `"--"` → di-parse ke `float64` (`parseAMSMoney`). Live 908963392 → **507 affiliate** (top "Shopee Media Network" Rp16,1jt/162 order/ROI 19). ⚠️ **Buyer count** (`total_buyers`/`new_buyers`) dijumlah antar-hari = **buyer-hari, bukan pembeli unik** (AMS tak beri ID pembeli → tak bisa dedupe; akurat hanya untuk periode 1 hari) — di-UI diberi label "Buyer (akum.)" + tooltip. **Sync di-trigger ops** (bukan tombol user — dihilangkan dulu sambil pantau reliabilitas): via `POST /affiliate/sync` (jalan **async/background**, balas langsung `status:running`; hasil `written`+success-rate dilaporkan via **notifier** saat selesai — karena backfill bisa menit-an & bakal kena timeout gateway kalau sinkron) atau trigger job. Optimasi akurasi: tiap panggilan AMS **di-retry 4× backoff eksponensial** (1s/2s/4s) → success-rate = data yang benar-benar tertangkap; success-rate pakai infra **APIStats** yang sama dgn GMS; sync **lanjut-per-hari saat 1 hari gagal** (bukan abort total; worker trailing-window self-heal). Pacing 300ms/panggilan (anti `ads_rate_limit`). Catatan volume: 908963392 ~507 affiliate = ~26 halaman/hari → backfill 30 hari ~4 mnt (didominasi pacing API), worker trailing-3-hari ~23 dtk/toko. Frontend: **Marketing Insight → Affiliate**, toggle **Shopee/TikTok** (default Shopee), view Shopee = pemilih toko (akun ber-cred AMS) + **date-range (read-only atas snapshot)** + KPI + tabel per-affiliate + kartu Rekomendasi. Catatan: app AMS "No access to Sensitive Data" **tak** menghalangi data performa affiliate. Grounded: `clients/shopee_ams_client.go`, `usecase/shopee_new_usecase.go` (`GetAffiliatePerformance`/`GetRecommendedAffiliates`/`SyncAffiliatePerformance`/`ListAffiliatePerformance`), `domain/entity/shopee_affiliate.go` (`AggregateAffiliatePerformance`), `infrastructure/repository/shopee_affiliate_repo.go`, `interface/http/shopee_affiliate_handler.go`, `worker/tasks/shopee_affiliate_sync_task.go`, FE `features/marketing-insight/affiliate/`. Ditunda: v3 (content/product); v2 (order-level + validasi komisi) **kini live prod** — lihat butir *AMS affiliate v2* berikut.
- **AMS affiliate v2 — order-level konversi + validasi komisi (native/bill-based)** ✅ **live prod** (deploy + backfill manual terverifikasi 2026-07-04; GATE OK — app AMS punya akses endpoint §6, `get_performance_data_update_time` mengembalikan `last_report_date`). **Terverifikasi live 908963392 (2026-07-04):** Juni → **1.677 konversi / 546 affiliate** (`get_conversion_report` 30/30); **11 bill** bulanan (`202508`–`202606`, Online/Seller, ~6–15jt/bln) → **17.536 baris ter-link** (`get_validation_report` 40/40, 100%). Melanjutkan v1 (agregat per-affiliate): menyimpan **ledger order-level** dari `get_conversion_report` (grain `shop_id`+`order_sn`+`item_id`+`model_id`) ke koleksi baru **`shopee_affiliate_conversion`**, dan **tagihan komisi bulanan** dari `get_validation_list` ke **`shopee_affiliate_validation_bill`**; `get_validation_report` menautkan tiap bill ke baris ledger (stamp `validation_id`/`validation_month`/`campaign_source` + status; conversion re-sync sengaja **tak menimpa** field bill). **Validasi native Shopee** (bukan join finance-statement lokal ala TikTok — escrow = settlement seller, bukan komisi affiliate; sumber kebenaran = API AMS). **Rekonsiliasi**: total bill (online `total_amount`, else Σ offline) vs Σ `item_brand_commission` baris ter-link → status `MATCHED` (<Rp1) / `DISCREPANCY` / `NO_LEDGER`. Status per-baris (derive saat baca dari `verified_status`+ada bill): `BILLED`/`INVALID`/`VALID_UNBILLED`/`PENDING`. Endpoint: **`GET /shopee/affiliate/conversions?shop_id=&start_date=&end_date=&affiliate_id=&verified_status=&order_status=`** (yyyymmdd; baca `shopee_affiliate_conversion`, default 30 hari s/d kemarin, + rollup per-affiliate) + **`POST /shopee/affiliate/conversions/sync`** (backfill background, balas `status:running`, hasil via notifier) + **`GET /shopee/affiliate/validations?shop_id=`** (bills + rekonsiliasi) + **`POST /shopee/affiliate/validations/sync`** (background). Worker harian **`sync-shopee-affiliate-conversion`** (05:00 WIB — clear dari GMS 02:00/v1 03:00/TikTok-validate 04:00; window di-gate ke `get_performance_data_update_time` = `[last-2d..last]`, fallback trailing-3-hari; lihat [[IT - Background Jobs & Schedulers]]). Batasan API yang ditangani: **`page_no*page_size ≤ 10000`** — **konversi** di-iris **per-hari** WIB (page_size 500; hari >10k di-**log jujur**, belum iris sub-jam), sedangkan **`get_validation_report` di-query per-bill atas window PLACEMENT `[bulan M−2 .. M−1]`** (`billPlacementWindow`) — order di-place **~2 bulan sebelum** bulan-tagihan M; Shopee tolak window **di dalam/di atas** M (`place_order_time_end too late`) atau **>~2 bulan sebelum** (`place_order_time_start too early`); bill ~2rb baris < plafon. Waktu AMS non-padded `2006/1/2 15:04:05` (fallback ke hari query bila gagal parse → baris tak hilang), tanggal update `2006-01-02`; money conversion/report = **string desimal**, validation_list = **float**; **`price`/`purchase_value` JUGA string desimal rupiah** (doc AMS salah tulis `int`/cents — terverifikasi live: `price=97500`→Rp97.500, komisi `7600`→~8% masuk akal; di-parse `parseAMSMoney`, TIDAK dibagi 100); `deduction_status`/`deduction_method`/`ams_deduction_time` **hanya filter, tak ada di response** → status ditagih datang dari bill. Reuse pola v1: pacing 300ms, retry 4× backoff (1s/2s/4s), APIStats success-rate (stats-only, tak trip gate order/GMS), per-shop isolation, skip toko tanpa cred AMS. **Frontend**: *Marketing Insight → Affiliate → tab Shopee* kini punya sub-tab **Performa (v1) / Conversions / Validasi** — Conversions = KPI + tabel order-level (badge status) + tab rollup per-affiliate + modal detail komisi; Validasi = kartu bill + rekonsiliasi; **read-only** (tanpa tombol sync, ikut precedent v1). Grounded: `clients/shopee_ams_conversion_client.go`, `usecase/shopee_affiliate_v2_usecase.go` + `shopee_affiliate_v2_validation_usecase.go` (`billPlacementWindow`), `entity/shopee_affiliate_conversion.go` + `shopee_affiliate_validation.go`, `repository/shopee_affiliate_conversion_repo.go` + `shopee_affiliate_validation_repo.go`, `interface/http/shopee_affiliate_conversion_handler.go`, `worker/tasks/shopee_affiliate_conversion_sync_task.go`, FE `features/marketing-insight/affiliate/` (`shopee-affiliate-conversions-view.tsx`, `shopee-affiliate-validations-view.tsx`, `use-fetch-shopee-affiliate-conversions.ts`, `types/shopee-affiliate-v2.ts`). Ditunda: v3 (product/content) **kini live prod** — lihat butir *AMS affiliate v3* berikut.
- **AMS affiliate v3 — product + content analytics (agregat-snapshot, pola v1)** ✅ **live prod** (2026-07-04; **terverifikasi 908963392**: product `get_product_performance` 3/3, content `get_content_performance` 6/6, read-back OK). Dua dimensi analitik AMS §5 terakhir. **Product** ≈ **klon `get_affiliate_performance` (v1)** — metrik identik (`sales`,`items_sold`,`orders`,`clicks`,`est_commission`,`roi`,`total_buyers`,`new_buyers`) tapi key `item_id`+`item_name`; kanonik **AllChannel** (1 call/hari), koleksi baru **`shopee_affiliate_product_performance`** (grain `shop_id`+`item_id`+`date`; agregasi Σ aditif + ROI=ΣSales/ΣKomisi). **Content** = performa per Shopee Video/Live, koleksi **`shopee_affiliate_content_performance`** (grain `shop_id`+`content_id`+`channel`+`date`; fields `content_title`,`post_time`,affiliate,`products`,`views`,`likes`,`comments`,`sales`,`orders`,`items_sold`); channel **WAJIB pilih** `ShopeeVideo`/`LiveStreaming` (tak ada AllChannel) → **2 call/hari** (loop 2 channel). **Endpoint**: **`GET /shopee/affiliate/products?shop_id=&start_date=&end_date=`** + **`POST /shopee/affiliate/products/sync`** + **`GET /shopee/affiliate/contents?shop_id=&start_date=&end_date=&channel=`** + **`POST /shopee/affiliate/contents/sync`** (baca-DB / background; **sengaja dibedakan** dari GMS `/shopee/gms/item-performance` — beda sumber: AMS = komisi affiliate per-produk). **Worker**: **tak ada worker baru** — worker v1 **`sync-shopee-affiliate-performance` (03:00) di-extend** menambah product+content per-toko, **ISOLATED** (kegagalan v3 tak mengubah status/worker_history v1; muncul di `v3-problems` notifier); lihat [[IT - Background Jobs & Schedulers]]. **Kuirk kontrak terverifikasi live** (kali ini doc AMS akurat soal TIPE, TAPI menyesatkan soal SEMANTIK): `get_content_performance` `views`/`likes`/`comments` = **KUMULATIF lifetime** (terbukti: nilai **identik** di semua daily-row satu content; doc "within selected time range" MENYESATKAN) → di-agregasi **MAX**, bukan SUM (kalau di-sum → reach ter-gandakan); `sales`/`orders`/`items_sold` memang **per-hari** → SUM. `post_time` = **int64 unix asli** (doc benar); `sales`/`est_commission`/`roi` = string (`parseAMSMoney`, `roi` "--"→0). Kanonik **Day/ConfirmedOrder**, `page_size` max **20** (kecil spt v1). Reuse pola v1: pacing 300ms, retry 4× backoff, APIStats (const `epProductPerf`/`epContentPerf`), per-shop isolation, trailing-window, skip toko tanpa cred AMS. **Frontend v3** ✅ live: *Marketing Insight → Affiliate → tab Shopee* kini punya sub-tab **Product** (KPI + tabel per-produk: sales/order/komisi/ROI/buyer) & **Content** (channel toggle Semua/ShopeeVideo/LiveStreaming + KPI + tabel; `views`/`likes`/`comments` di-tampilkan sbg **lifetime** dgn label + tooltip agar tak salah-tafsir per-periode); **read-only**. Grounded FE: `features/marketing-insight/affiliate/` (`shopee-affiliate-product-view.tsx`, `shopee-affiliate-content-view.tsx`, `use-fetch-shopee-affiliate-analytics.ts`, `types/shopee-affiliate-v3.ts`, `utils/shopee-affiliate-v3.ts`). Grounded: `clients/shopee_ams_analytics_client.go`, `usecase/shopee_affiliate_product_usecase.go` + `shopee_affiliate_content_usecase.go`, `entity/shopee_affiliate_product.go` + `shopee_affiliate_content.go` (`AggregateProductPerformance`/`AggregateContentPerformance`), `repository/shopee_affiliate_product_repo.go` + `shopee_affiliate_content_repo.go`, `interface/http/shopee_affiliate_analytics_handler.go`, `worker/tasks/shopee_affiliate_sync_task.go` (di-extend). Ditunda: AMS campaign management (§3–4 AMS OpenAPI: open/targeted campaign, set komisi, invite affiliate) belum di-scope.
- **Anti-burst GMS + status jujur (hardening 2026-07-01)** ⚠️ **sudah di kode, belum deploy** — perbaikan akar penyebab data GMS berhenti masuk sejak ~24 Jun 2026. **Akar masalah** (terverifikasi via `worker_history` prod + Shopee console access-log partner `2032638`): call `/ads` (`get_gms_item_performance` / `get_gms_campaign_performance`) yang di-**burst** per toko (fan-out goroutine per-hari/campaign) memicu `ads_rate_limit_shop_api`; **run-wide breaker** lalu meng-abort seluruh run pada throttle pertama; dan kegagalan **ditelan** worker (status `success` palsu). **Perbaikan** (tanpa retry/backoff): **(1) anti-burst** — call `/ads` per toko kini **diserialkan + di-pace** (`adsCallPacing` 1500 ms via `paceAdsCall`), menggantikan fan-out goroutine; konkurensi worker **3 → 2**. **(2) Status jujur** — worker mengagregasi hasil per-toko (`summarizeShopResults`) → `worker_history.status=failed` + ringkasan `ok / rate-limited / failed` saat partial/rate-limited (sentinel `ErrShopeeRateLimited`), menutup gap di atas. **(3) Hemat kuota** — `get_shop_info` **tak lagi** dipanggil saat refresh token (shop info sudah terisi saat auth `ExchangeAccessToken`), membebaskan kuota harian app Ads untuk call GMS. Order **tidak** terdampak karena app terpisah (partner `2032314`). Grounded: `worker/tasks/shopee_sync_task.go`, `usecase/shopee_new_usecase.go` (`SyncGMSItemPerformances` / `SyncGMSCampaignPerformances`, `paceAdsCall`, `ErrShopeeRateLimited`).
- **Escrow income (`get_escrow_detail`) via One** ✅ live — untuk order **COMPLETED**, `SyncOrderDetail` menarik escrow (net settlement setelah fee) dan mengisi field `income` transaction order. **Fix bug decode (2026-07-01)**: Shopee mengirim `order_income.tenure_info_list` sebagai **array**, tapi dimodelkan struct tunggal → decode gagal 100% (income tak pernah terisi); diperbaiki jadi `[]ShopeeTenureInfo` (`clients/shopee_client_new.go`, `entity/shopee.go`) → escrow tervalidasi terisi di prod. **Backfill histori** ✅ (live, dijalankan 2026-07-01): `GET /shopee/orders/escrow/backfill?shop_id=&limit=` (`BackfillEscrow`) memilih order completed lokal ber-`income` null (filter `TransactionOrderFilter.IncomeEmpty`) → escrow-only tanpa re-list, batch 50, breaker bersama, **resumable**. Toko 908963392 ter-backfill penuh: **1.308/1.308** order completed kini ber-`income` (looped `limit=40`, ~29 putaran, tanpa rate-limit). **Escrow-capture on completion (2026-07-02):** `ShopeePushProcessor` dulu pada push code 3 untuk order **existing** cuma update status (escrow tak pernah ditarik saat completion → income null menumpuk); kini pada transisi → **COMPLETED** ia memanggil `SyncOrderDetail` (tarik escrow → isi `income`), re-read agar income tak ke-timpa update status, `CompletedAt` terjaga, defer (`ErrDelayed`) saat rate-limit/auth, non-fatal saat escrow belum siap; status non-COMPLETED tetap 0 API-call (`shopee_push.go`). **shopee_income di order-detail:** `GET /transactions/orders/:id` untuk order Shopee **COMPLETED** kini mengekspos objek **`shopee_income`** (2 seksi ala Seller Center: **Rincian Penghasilan** seller + **Pembayaran Pembeli**) dari escrow tersimpan (rekonsiliasi persis: seksi seller = `escrow_amount`, seksi buyer = `buyer_total_amount`); nil bila non-completed → FE fallback ke PaymentSummary. **Order-detail enrichment (2026-07-09):** seksi seller kini menambah **`seller_return_refund`** (Jumlah Pengembalian Dana — escrow menyimpan **negatif**, di-flip ke magnitude positif agar UI render sebagai potongan) & **`campaign_fee`** (Biaya Program Hemat Biaya Kirim); response juga mengekspos **`item_returns[]`** (status retur per-item: `ordered_quantity`/`returned_quantity`/`refund_amount`/`return_status` — join `order.items` × `order.return.items` by `(item_id, sku)` via `buildItemReturns`, **driven dari return-items** agar dua variasi ber-`item_id` sama tak dobel-hitung), **`return_information`** (reason/status dari `order.return`, dulu `{}`), dan **`histories`** (status timeline dari `ListHistoryByOrderID`). FE [[APP - Web ERP]] merender: badge "N Return/Refund" per item, daftar "Returned Items", timeline "Status History" (**sebelumnya kosong** — hook FE tak me-map `histories`), + 2 baris income baru; mapper murni + unit-test `transactions/lib/detail-mappers.ts` (`mapStatusHistory`/`mapItemReturns`). Baris income tambahan **display-only** (Total Penghasilan tetap dari `escrow_amount`, tak double-count). **Refund amount di list + detail (2026-07-11):** kolom **Refund** di list transaksi (`/transactions/orders/list`) & field **Refund Amount** di detail menampilkan `order.return.refund_amount` — **FE-only**: list endpoint sudah mengembalikan `entity.TransactionOrder` utuh ber-`return` (repo `List` tanpa projection), jadi tak ada perubahan BE. Beda konsep dari `seller_return_refund` (potongan sisi income seller dari escrow). **Tracking Number + Shipping Type di detail (2026-07-13):** `renderShopeeTransactionDetail` dulu cuma set `ShippingProvider` (= `orderDetail.ShippingCarrier`) → Tracking Number & Shipping Type tampil "-"; kini juga set `TrackingNumber` (`orderDetail.TrackingNumber`) & `ShippingType` (= `ShippingCarrier`, Shopee tak punya field jenis terpisah). FE sudah merender ketiganya — hanya BE yang tak mengisi. Tracking terisi bergantung task `sync-shopee-tracking` (lihat *WMS resi bridge*). Grounded: `interface/http/shopee_income.go`, `transaction_handler.go` (`renderShopeeTransactionDetail`, `buildItemReturns`); FE `transactions/constants/transactions.tsx` (kolom Refund), `components/detail/return-information.tsx`.
- **Reconcile income/escrow stale (release-driven, 2026-07-09)** ✅ **live** — **akar masalah**: escrow di-fetch **sekali saat COMPLETED** (provisional), tapi Shopee memfinalisasi fee **belakangan** — khususnya **komisi AMS afiliasi** (`order_ams_commission_fee`) masuk saat **dana dilepas** (~menit setelah COMPLETED) → `income` tersimpan **kelebihan** (order `260701H6MVQEA4`: tersimpan `escrow_amount` 161.387 vs Shopee final 147.339, selisih **persis Rp14.048** = komisi AMS yang belum terpotong; `order_ams_commission_fee` tersimpan 0). **Tak ada webhook escrow** di katalog push (order_status tetap COMPLETED → code-3 tak menyala), jadi dipakai **cron `shopee-escrow-reconciler`** (per jam): `get_escrow_list(release_time_from,to)` = **sinyal rilis nyata** (grounded, bukan tebak umur) + `payout_amount` final → per order rilis, bila `payout_amount` ≠ settlement tersimpan (`settlementMatches`, toleransi Rp1) → `RefetchOrderEscrow`: fetch escrow **fresh** (**bypass repo-first** `SyncOrderDetail` yang pakai escrow stale) → `SetOrderIncome` (**targeted `$set`**, tak sentuh `income_status` — field itu murni domain reconciliation **TikTok** (`ResolveIncomeStatus`, hanya dipanggil dari path sync/webhook TikTok); utk Shopee, `income_status` tak pernah diisi proses aktif manapun (verified live: 100% order Shopee = `""`) — nilai `not_applicable` yang mungkin nampak di sebagian order Shopee lama HANYA sisa migrasi one-off historis `cmd/backfill`, bukan reconciler yang berjalan terus; lihat koreksi §*Gross Profit per Product*). Window rilis **di-chunk ≤15 hari** (`splitOrderWindows`; cap sama seperti order/return list) agar lookback awal (45 hari, cursor per-toko + overlap 1 jam) tak kena batas & stuck. Reuse breaker/gate/pacing; pola meniru **income-reconciler TikTok**. **Live-confirmed 2026-07-09**: order 260701 terkoreksi **161.387→147.339** (AMS 0→14.048); cron jalan, cursor advance per-toko. **Catatan**: shop diproses **sekuensial + timeout 10 mnt** → backlog awal (order stale sejak backfill Mei/Jun) tersapu **bertahap lintas beberapa run** (skip-if-match bikin re-run makin ringan; shop berat bisa menunda shop setelahnya — follow-up: round-robin/cap per-shop). Grounded: `worker/tasks/shopee_escrow_reconciler.go` (`escrowReconcileFrom`), `usecase/shopee_new_usecase.go` (`SyncEscrowReleases` / `syncEscrowReleaseWindow` / `RefetchOrderEscrow` / `settlementMatches`), `clients/shopee_escrow_list_client.go` (`GetEscrowList`), `repository/transaction_repo.go` (`SetOrderIncome`).
- **Manual order sync histori panjang (chunking ≤15 hari)** ⚠️ **sudah di kode, belum deploy** — `GET /shopee/orders/sync` kini bisa menarik histori panjang (mis. ~3 bulan) dalam satu panggilan. `get_order_list` Shopee membatasi jendela `time_from`/`time_to` ke **≤15 hari**, jadi rentang dipecah otomatis ke sub-window ≤15 hari (`splitOrderWindows`, span 14 hari, urut **terbaru→terlama**, kontigu & disjoint) dan tiap window disync via `syncOrdersWindow`, berbagi **satu circuit breaker per-run** + pacing antar-window (`paceOrderCall` 400 ms; terpisah dari `paceAdsCall`/GMS). **Sengaja TIDAK memakai gate proses-wide `IsShopeeRateLimited()`**: gate itu juga di-trip oleh app **Ads** (GMS, partner `2032638`), sedangkan order memakai app **ERP_SYSTEM** (partner `2032314`) berkuota independen — mengikat order ke gate akan membuat throttle GMS **salah-memblokir** order sync yang masih punya kuota (breaker per-run mencerminkan respons app ERP yang benar). **Honest-partial**: bila breaker trip di tengah run → `SyncOrders` return `ErrShopeeRateLimited`, handler balas **HTTP 200 "partial"** (bukan 500) + ringkasan **success-rate per-endpoint** (`APIStatsSummary`) di body agar sync manual bisa dipantau; idempoten & resumable (upsert). Shop tanpa kredensial ERP → skip **satu kali** (sentinel di-propagasi, bukan re-cek tiap window). Param query: `shop_id` (wajib), `time_from`/`time_to` (unix) **atau** `days` (mis. `days=90`); guard rentang **maks 400 hari**. **Validasi backfill Juni+Mei 2026** (dijalankan 2026-07-03 via harness one-off `cmd/ordersync`, chunk ~7 hari; endpoint HTTP sendiri **belum deploy**): **8 toko × 2 bulan**, **~38.600 call** order+escrow **~100%**, **tanpa sekali pun rate-limit** dalam sehari (kuota harian app ERP tak sampai tersentuh) — beberapa order escrow sempat gagal karena blip jaringan lalu di-fix via re-run chunk (repo-first → hanya escrow yang kurang di-call ulang). **April belum ditarik.** Mengonfirmasi kuota harian app **ERP_SYSTEM** (`2032314`) lega untuk order+escrow (kontras app Ads `2032638` yang kronis ter-throttle sejak ~24 Jun). Grounded: `usecase/shopee_new_usecase.go` (`SyncOrders` / `syncOrdersWindow` / `splitOrderWindows` / `paceOrderCall` / `orderListWindow`), `interface/http/shopee_handler.go` (`SyncOrders`).
- **Shopee Returns ingestion (return/refund + parsial)** ⚠️ **di kode + tervalidasi live, endpoint belum deploy** — menutup gap data order status terminal: **tanggal proses** return/refund & **return parsial** (order COMPLETED bisa punya return **sebagian item** yang `order_status`-nya tetap COMPLETED → mustahil terdeteksi tanpa Returns API). `GET /shopee/returns/sync` (+ `GET /shopee/returns/list` baca DB) menarik dari **Returns API** `get_return_list` via app **ERP_SYSTEM** (`2032314`; izin "ERP System" terkonfirmasi di dok Shopee **dan** live). **List-primary**: `get_return_list` sudah memuat `item[]` (qty parsial) + `refund_amount` + `create_time`/`update_time` → **tak perlu** `get_return_detail` per return (hemat kuota; detail hanya untuk refund per-item shop whitelisted "Partial Qty RR"). Window `create_time` **≤15 hari** (batas sama seperti order) → reuse `splitOrderWindows`; pagination `page_no`/`page_size≤100` (bukan cursor). Disimpan ke koleksi **`shopee_returns`** (keyed `return_sn`, idempoten) + direfleksikan ke order via **`SetOrderReturn`** ke sub-dokumen `order.return` (**targeted `$set`** — tak tertimpa order-sync yang menulis `items`/`status` tapi bukan `return`, dan sebaliknya). **`Partial` dihitung dari KUANTITAS** (`Σ returned_quantity` vs `Σ order qty`, `isPartialReturn`) — **bukan** dari uang: refund < harga-sebelum-diskon akan **salah-cap** retur penuh ber-diskon sebagai parsial. Reuse pengaman order (breaker per-run, gate, pacing, honest-partial `ErrShopeeRateLimited` → HTTP 200 partial). **Validasi live (2026-07-07)**: shop 908963392 Juni → **15 return tersimpan**, decode 0-error, izin app OK. **Realtime via webhook code 29** ✅ **di kode, belum deploy** — `ShopeePushProcessor` kini **dispatch by code**: `return_updates_push` (code 29) → `handleReturnPush` → fetch `get_return_detail(return_sn)` → **reuse ingestion** via `SyncReturnBySN` (idempoten → tahan **repeat / out-of-order** push karena selalu fetch state terkini; cek gate → `webhook.ErrDelayed` defer bila kuota habis). **Gated on config** (bukan kode): subscribe app **ERP_SYSTEM** ke `return_updates_push` di Shopee console via **`app_type=one`** (`SHOPEE_ONE_WEBHOOK_KEY`, app "One Bharata") + deploy. `SyncReturns` tetap jadi **jaring pengaman** (webhook tak dijamin terkirim/urut) — sejak 2026-07-15 dijalankan **terjadwal** oleh worker `sync-shopee-returns` (lihat *Background Workers*), bukan lagi hanya manual/CLI. **Penting**: jalur ingestion yang mengoper `rtsUseCase = nil` (mis. `cmd/ordersync MODE=returns`) **tidak** memicu booking retur — hanya `SetOrderReturn`; pemicu booking hanya menyala lewat usecase yang wired (worker terjadwal, webhook code-29, atau `GET /shopee/returns/sync`). Grounded: `clients/shopee_returns_client.go`, `usecase/shopee_new_usecase.go` (`SyncReturns` / `syncReturnsWindow` / `SyncReturnBySN` / `isPartialReturn` / `transformClientReturn`), `webhook/processor/shopee_push.go` (`handleReturnPush`, dispatch code 3/29), `entity/shopee.go` (`ShopeeReturnDetail.TransformToOrderReturn`), `entity/transaction.go` (`TransactionReturn`), `repository/shopee_repo.go` + `transaction_repo.go` (`SetOrderReturn`), `interface/http/shopee_handler.go` (`SyncReturns` / `ListReturns`).
- **`shipped_at` Shopee = `pickup_done_time` + backfill order null (2026-07-17)** ⚠️ **di kode, belum deploy** — `shipped_at` adalah **dasar bucket faktur auto-sync Accurate** (1 faktur per toko per **hari-kirim** WIB; snapshot `listSnapshotOrders` memfilter tepat field ini). Sumbernya **`pickup_done_time`** (waktu pickup asli kurir) yang diminta di `response_optional_fields` `get_order_detail` lalu dipetakan `TransformFromShopee` sebagai nilai **otoritatif** (fallback proxy `update_time` untuk status PROCESSED/SHIPPED/COMPLETED bila pickup 0). Terverifikasi live 2026-07-17: **100% (3.992/3.992)** order Juli ber-`shipped_at` memakai pickup asli (≠ `update_time`) — Shopee benar mengembalikannya. **Bug simpan (diperbaiki):** `SaveOrderDetail` menulis via `$set` daftar-field manual dan **`pickup_done_time` luput** → **0 dari 64.470** dokumen `shopee_order_details` menyimpannya. Tak merusak `shipped_at` (transform membaca objek in-memory, bukan DB), tapi tanggal pickup otoritatif hilang untuk audit/retur + memecah quality-gate backfill; kini `pickup_done_time` ditambahkan ke `$set`. **Silent-drop `shipped_at` null:** order SHIPPED/COMPLETED ber-`shipped_at` null tak masuk bucket faktur mana pun → hilang diam-diam. Populasi = **±7.339 order** SHOPEE (record di-sync **sebelum** `pickup_done_time` diminta; nyaris semua pra-cutover 10 Jul). Guard baru: hook completed di `SyncOrderDetail` kini **WARN** saat COMPLETED ber-`shipped_at` nil (dulu skip senyap). **Tool backfill:** `cmd/ordersync MODE=shippedatbackfill` (DRY-RUN default; `MANUAL_SYNC_CONFIRM=yes` menulis) me-refetch `get_order_detail` untuk order null-shipped per toko (chunk 50, throttle 300 ms, breaker-aware, skip-if-fixed) → isi `shipped_at`; quality-gate memisah **otoritatif (pickup)** vs **proxy (uncertain)** — hanya otoritatif yang layak auto-faktur. Rebuild faktur **terpisah & retur-safe** (pra-cutover via `BackfillPreCutoverInvoice`, auto-era via retry; **EXCLUDE** hari-kirim order ber-Retur Penjualan SENT agar tak flip FAILED — lihat [[ADR - 0022 Retur via Sales Return per Mode + Keep Invoice Line]]). Prosedur lengkap: [[RUN - Backfill shipped_at Shopee]]. Grounded: `entity/transaction.go` (`TransformFromShopee`), `repository/shopee_repo.go` (`SaveOrderDetail`), `usecase/shopee_new_usecase.go` (`SyncOrderDetail`), `usecase/accurate_rts_usecase.go` (`listSnapshotOrders` / `BackfillPreCutoverInvoice`), `cmd/ordersync` (`MODE=shippedatbackfill`).

## Belum Diimplementasikan / Catatan

- **Target kirim selain Accurate** mengembalikan `501` ("service integration not implemented yet"); enum service yang tersedia baru `ACCURATE`.
- Route direct-push Accurate (`/transactions/summary/create|send|status`) handler-nya sudah **LENGKAP**, tetapi route-nya **DI-COMMENT** — digantikan alur summary-report `/send/:service`.
- Route webhook Desty langsung (`/webhook/desty`, `/webhooks/desty`) **di-comment** — digantikan `/webhooks/services/desty`.
- CronManager lama **fully disabled** — fungsinya dipindah ke worker framework.
- **Lazada** hanya disebut di kode cron yang sudah disabled — **TIDAK ada client Lazada** (placeholder/partial).
- **Meta Ads** — **TIDAK ada client Meta Ads** di backend; konsepnya baru eksis sebagai role/setting manual di FE Finance/Incentive. Rencana integrasi: lihat §Meta Ads di atas.
- **KiriminAja** — **TIDAK ada client KiriminAja** sama sekali; 0 referensi di kode. Rencana integrasi (shipping/logistics API): lihat §KiriminAja di atas.
- TODO kecil: indexing `time.Time`.

## Dependencies & Integrasi

- [[CORE - API Master Gateway]] — entry point / routing ke service
- [[External - Accurate]] — bridging finansial (Accurate Online, HMAC-signed): Sales Invoice / Sales Return
- [[Microservices - Insentive Service]] — konsumen data iklan (laporan TikTok/Shopee, + Meta Ads rencana)
- [[Microservices - TikTok Shop Service]] — webhook landing
- [[DB - Overview and Notes]] — MongoDB & Redis (queue prefix `srv:integration`)

External lain: TikTok Shop, TikTok Business/Ads, Shopee, Telegram (notifier error/job). [[External - Desty]] (middleware order) — soft-disabled 2026-07-12.

## Dokumen Terkait

- [[External - Desty]] — vendor middleware orkestrasi order (soft-disabled 2026-07-12)
- [[RUN - Onboarding Meta Ads]] — langkah operasional pembuatan akun/app Meta Ads (rencana)
- [[RUN - Onboarding KiriminAja]] — langkah operasional partnership & API key KiriminAja (rencana)
- [[Finance - Incentive]] — konsumen data konversi/CPA Meta Ads untuk skema insentif ADV Meta
- [[Sales - Marketplace Integration]] (konsep sisi marketing)
- [[Finance - Bridging App]]
- [[Sales - GMV Creative]]
- [[Vendor - CRM]]
- [Referensi API — docs-api-greget](https://docs-api-greget.vercel.app/) — REST API lengkap (129 endpoint, Nextra)
- [[IT - Background Jobs & Schedulers]] — cron + webhook dispatcher service ini (sync TikTok/Shopee; konsumsi webhook tiap 5 dtk)
