## Deskripsi

*Microservice **manufacture-service** — WMS manufaktur (warehouse/produksi): master bahan baku & produk, stok, transaksi in/out, formula/BOM, produksi, material order, PO marketing/procurement, proposal koreksi, dan audit log. Master (bahan, produk, formula) dikelola **native via web** (CRUD penuh). Sync **Google Sheets sudah dihapus** dari service ini — sumber master kini: **bahan & stok dari [[External - Accurate]]** (read-only), **barang jadi & bundle dari data HPP** integration, **formula dari import Excel**. Data operasional (transaksi) **sumber kebenaran di MongoDB**; **angka stok** mengikuti Accurate. Implementasi nyata dari konsep [[Manufacture - Stock & Material Management]].*

- **Stack**: Go + Fiber v2 + MongoDB (driver resmi) + shared-library; di belakang [[CORE - API Master Gateway]] (`/api/manufacture/*`), auth via header gateway (lihat [[CORE - SSO Flow]]).
- **Path di repo**: `bip-erp/services/manufacture/` · models: `bip-erp/shared-library/models/manufacture/models.go` · FE modul: `erp-frontend/src/features/manufacture/`.
- **Port**: `6978` (`MANUFACTURE_SERVICE_PORT`). **Database**: `manufacture_db` (Mongo per-service, **replica set 1-node** `rs-manufacture` + keyfile auth).
- **Status**: ✅ Implemented (Fase 1 master/stok/transaksi/formula sync jalan; entitas produksi/PO/proposal ada di kode).
- **API**: [[API - Manufacture Service]].

## Endpoint / Fitur (Sudah Diimplementasikan)

Daftar rute lengkap di [[API - Manufacture Service]]. Ringkas:

- **Master bahan** — list/detail + CRUD manual (`source:"manual"` → kebal stale-marking) + `PATCH status` + **`POST /master-bahan/align-accurate`** (migrasi kode lama → kode Accurate, sekali per environment) + **`POST /master-bahan/sync-accurate`**: tarik master bahan dari Accurate, menggantikan import Excel (`ImportMaster`/`rowsToMaster` sudah dihapus). Filter penting: hanya kode berprefix bahan (`BBK`/`BBO`/`BKK`/`BKO`), **atau** kode di daftar kurasi `bahanNonPrefix` (15 kode PP- bahan: PLASTIK SHRINK/ALKOHOL 70%/SILLICA GEL — sejak 18 Juli 2026, environment baru cukup sekali klik sync tanpa registrasi manual; tambah bahan PP- baru = tambah baris kurasi), **atau** kode yang sudah ada di master — daftar item Accurate juga memuat aset (`PK-` kursi kantor, `PG-` palet), mesin (`PP-` campuran: ALKOHOL & SILLICA GEL bersama MESIN FILLING), sample (`SOT-`) dan barang jadi (`PJ*`). Kategori & satuan di-seed dari prefix **saat insert saja**; tak pernah menghapus.
- **Master produk & bundle** — CRUD manual + **`POST /master-product/sync-hpp`** (barang jadi + mapping bundle dari data HPP integration) + **`POST /master-product/align-hpp`** (migrasi kode produk lama → master_sku HPP, kembaran align bahan; kode lama jadi `aliases` di MasterProduct) + `GET /sku-mapping` (listing SKU → komponen). **Bundle = virtual**: stok hanya ada di level komponen. Sync produk kini menarik DUA sumber: barang jadi ber-cost dari HPP **plus** daftar kurasi `produkNonHPP` (18 barang jadi sah yang tak di-cost HPP — `BRL`/`F001`/`SAV001`/… — ditarik dari feed Accurate, sejak 18 Juli 2026), jadi environment baru (prod) genap 62 FG sekali klik tanpa registrasi manual. Align tetap mengenali ke-18 kode itu kanonik, bukan "kode lama".
- **Stok** — snapshot per kode + `POST /stok/reconcile` (rekonsiliasi snapshot vs transaksi) + `GET /stok/sektor` (stok riil per gudang: utama/tinggar/sadewa, breakdown jenis barang → satuan; konsumen = card Status Sektor di Dashboard FE) + **`POST /stok/sync-accurate`**: stok barang jadi **dan** bahan disamakan dengan Accurate (arah baca; arah tulis = fitur push di bawah, [[ADR - 0015 Push Pergerakan WMS ke Accurate]]) + **`POST /stok/align`**: bersihkan baris stok/saldo awal **yatim** (kode tak ada di master mana pun — `DeleteMaster` tak menghapus snapshot stok, dan sync tak pernah menghapus; berbahaya laten karena mutasi stok pakai `$inc` sehingga kode yang dibuat ulang mewarisi angka basi).
- **Push WMS → Accurate** ([[ADR - 0015 Push Pergerakan WMS ke Accurate]]) — pergerakan internal (konsumsi bahan, masuk-kembali sisa, Input Gudang FG, koreksi proposal) didorong sbg **Penyesuaian Persediaan harian per arah**: pemindai watermark atas ledger `manufacture_transaksi` → outbox `manufacture_accurate_push` → `POST /accurate/push` (`?dry_run=true` pratinjau) + loop 10 mnt; inspeksi `GET /accurate/push/outbox`. Kill switch `ACCURATE_WMS_PUSH` — **AKTIF di dev LOKAL sejak 18 Juli 2026** (aman: kredensial lokal `accurate-disabled.invalid`; uji hidup 1 gram BBK-001: pindai → grup `ADJUSTMENT_IN` → konversi 1 g = 0.001 kg benar → tulis gagal 502 → outbox tetap PENDING & diulang — gagal-aman terbukti). **Dev VM & prod masih MATI** (kredensialnya pembukuan sungguhan; menyalakan = koordinasi finance). ✅ **Jalur tulis-sukses PERTAMA tereksekusi di prod 2026-08-09**: `2026-08-01|ADJUSTMENT_IN → Accurate IA.2026.08.00017`, dan `manufacture_accurate_push_day` untuk pertama kalinya memuat dokumen hari (sebelumnya hanya berisi `_watermark`).
  - ⚠️ **Akar kegagalan sebelumnya: format `transDate`** (diperbaiki PR [#1102](https://github.com/bip-itteam-internal/bip-erp/pull/1102)). manufacture menyimpan tanggal ISO `yyyy-MM-dd` (`wibDate` memakai layout `2006-01-02`) dan integration meneruskannya **mentah** ke Accurate, yang hanya menerima `dd/MM/yyyy` → `Invalid field value for field "transDate"`. Bukan gangguan sesekali: **setiap** push gagal sejak fitur hidup, 83 penolakan tercatat dalam tujuh hari terakhir saja. Kegagalannya tak pernah berhenti sendiri karena outbox mengulang selamanya — **format yang salah tetap salah pada percobaan ke-83**. Konvensi `dd/MM/yyyy` sudah dipegang konsisten di jalur Accurate lain (`journal_handler`, `anggaran_handler`, `ppn_masukan_refresh`, `accurate_daily_invoice_handler`); jalur WMS saja yang terlewat. Normalisasi ditaruh di **batas menuju Accurate** (`PushWmsAdjustment` di integration), bukan di manufacture, supaya pemanggil berikutnya ikut terlindungi; tanggal tak dikenali kini ditolak **sebelum** permintaan dikirim. Grounded: `integration/internal/usecase/accurate_wms_adjustment.go` (`normalisasiTransDateAccurate`), `manufacture/accurate_push.go` (`wibDate`).
  - ⚠️ **Galat berikutnya terungkap setelah format benar** — 2 dokumen masih PENDING dengan penolakan **berbeda**: `Nilai biaya barang "Serum GB 7 Active Ingredients" diset nol… Anda tidak memiliki hak untuk melakukan penyesuaian dengan biaya nol`. Ini **HPP nol di master Accurate + hak akses user API**, bukan bug kode — perlu tindakan di Accurate, bukan perubahan di service ini. Contoh nyata bahwa satu kegagalan bisa menyembunyikan kegagalan berikutnya di baliknya.
- **Saldo awal bulanan** — **menu sidebar sendiri** (Master & Referensi → "Saldo Awal Bulanan", slug `/manufacture/saldo-awal`, tab `saldo_awal`; dipindah dari sub-tab Stock Management 21 Juli 2026, akses mengikuti Stok = admin gudang RM/FG + PPIC/SPV). Snapshot stok tiap awal bulan, **idempoten per bulan** (foto pertama yang dipakai; ticker boot + tiap 6 jam menyusul bila service mati saat pergantian bulan — konsekuensinya angka = stok saat putaran pertama berjalan, bukan tepat tengah malam tgl 1). **Saldo akhir kini TERSIMPAN** di baris bulan itu (`qty_akhir`+`akhir_at`, 21 Juli 2026): ticker memperbaruinya tiap putaran dari stok terkini selama bulan berjalan ("berjalan"), lalu **dikunci saat bulan berganti** — snapshot saldo awal bulan baru sekaligus menuliskannya ke bulan sebelumnya sehingga saldo akhir bulan M **selalu identik** dengan saldo awal bulan M+1 (menghindari dua sumber kebenaran yang bisa berselisih, sekaligus menyembuhkan diri bila service mati di pergantian bulan). `POST /saldo-awal/snapshot` juga menyegarkan saldo akhir berjalan (tanpa itu tombolnya tak berguna di bulan yang snapshot awalnya sudah ada). Dokumen lama tanpa `qty_akhir` → FE jatuh balik ke turunan snapshot BULAN BERIKUTNYA; kolom **Perubahan = akhir − awal** (naik = +hijau, turun = −merah; sebelumnya awal − akhir sehingga stok naik tampil negatif — diperbaiki 21 Juli 2026). Bulan yang belum ditutup (snapshot bulan depan belum ada) menampilkan **"—"**, bukan 0. Dua tombol dengan tugas BERBEDA (dinamai ulang 21 Juli 2026 karena keduanya dulu berbunyi "snapshot/potret" dan membingungkan): **Segarkan Sekarang** = perbarui saldo akhir berjalan seketika tanpa menunggu putaran 6 jam; **Potret Ulang Saldo Awal** (PPIC/SPV, bulan berjalan saja) memanggil `POST /saldo-awal/snapshot?force=true` yang membuang snapshot bulan berjalan lalu memotret ulang dari stok terkini; diperlukan sekali sesudah stok pindah sumber ke Accurate (snapshot lama berisi angka era test dan idempotensi per-bulan membuatnya tak pernah memperbaiki diri). **Revisi Saldo Awal** (28 Juli 2026): tombol Revisi per baris mengajukan proposal PPIC→SPV (`original_tx.tipe="SALDO_AWAL"`, `ref=bulan`) yang saat APPLIED meng-update `qty` baris itu — perubahan saldo awal lewat approval, bukan edit langsung. **Bulan tertutup kini di-gate `manufacture_edit_grant`** (4 Agustus 2026): tombol Revisi hanya aktif bila akun punya grant **Buka Kunci Edit WMS** untuk bulan itu (bulan berjalan tetap bebas) — konsisten dengan Koreksi transaksi ledger; IT bisa memberi akses **langsung dari menu ini** lewat panel `EditGrantPanel` yang muncul untuk IT SPV saat membuka bulan tertutup. Approval revisi tetap PPIC→SPV. **Import hasil SO** (`POST /saldo-awal/import-so` pratinjau + `/import-so/commit`, PPIC/SPV): upload Excel hasil Stock Opname → panel rekonsiliasi (SO vs `qty_akhir` sistem, baris beda di-highlight & wajib keterangan) → commit men-set `qty_akhir`+`akhir_locked=true` sehingga **Saldo Akhir = hasil SO** dan **ticker tak menimpanya** (`perbaruiSaldoAkhir`/`kunciSaldoAkhirBulanLalu` melewati baris `akhir_locked`; `?force=true` snapshot ditolak 409 bila ada baris terkunci — melindungi rekonsiliasi SO dari terhapus).
- **Laporan Tarikan Mutasi** (`GET /mutasi?from=&to=`, menu sidebar "Tarikan Mutasi" di Master & Referensi, tab `mutasi`, 29 Juli 2026) — kartu mutasi per kode `master_bahan` untuk periode terpilih (preset Hari ini/Minggu ini/Bulan ini + rentang): **saldo awal · masuk Gudang RM · keluar produksi · saldo akhir**. Agregasi di backend (`mutasi.go`, inti pure `aggregateMutasi` teruji): saldo awal = snapshot `saldo_awal_bulanan` bulan `from` + net mutasi sebelum `from`; masuk = Σ INBOUND; keluar = Σ OUTBOUND ber-`grupDokumen RM-OUT-BOM`; saldo akhir = awal+masuk−keluar (turunan). **Batas periode WIB**; tanggal mutasi dari `metadata.created_at` (bukan `detail.date` yang sering kosong). FE `MutasiView.tsx` + export Excel; akses admin gudang RM/FG + PPIC/SPV (read-only). Caveat jujur di UI: saldo akhir ≠ stok live yang mengikuti Accurate. Dari support ticket "sistem mutasi gudang offline" (interpretasi laporan; lapisan penuh lokasi+AR masih TBD).
- **Transparansi Substitusi RM** (`GET /substitusi?from=&to=`, menu "Transparansi Substitusi RM" di Master & Referensi, tab `substitusi`, 29 Juli 2026) — dari support ticket "substitusi produk / transparansi kegunaan RM": bila bahan formula habis (mis. AQUA) lalu diganti bahan serupa (Air RO/PDAM), pemakaian itu **direkam eksplisit** & **transparan ke Finance** untuk rekonsiliasi selisih Accurate. **Perekaman**: di Form Keluar ke Produksi, bahan **tambahan luar BOM** bisa ditandai "Pengganti untuk <bahan formula A>" + alasan → metadata `detail.substitusiDari`/`substitusiDariNama`/`substitusiAlasan` menumpang objek `baseTx` → masuk `detail` proposal → **diwariskan ke transaksi saat APPLIED** (`proposal.go` menyalin `original_tx.detail`). **Laporan** (`substitusi.go`, inti pure `mapSubstitusi` teruji): transaksi ber-`detail.substitusiDari` dalam periode (WIB) dipetakan A (asli, dari detail) → B (pengganti, = `kode_bahan`). Hanya substitusi **APPLIED** yang tampil (pending ada di menu Persetujuan); `tanggal` = tanggal approval (stok berkurang saat approval — konsisten seluruh laporan WMS). FE `SubstitusiView.tsx` read-only + export Excel; akses admin gudang RM/produksi + PPIC/SPV + **Finance read-only** (ditambah ke `TAB_WMS_FINANCE`, seperti tab Gudang FG). Tanpa integrasi Accurate baru — stok B tetap terdorong via push existing; laporan memberi "kenapa"-nya.
- **Sistem Mutasi Gudang — Fase 1** (menu "Master Lokasi" + "Mutasi Gudang" di Master & Referensi, tab `master_lokasi`/`mutasi_gudang`, 29 Juli 2026) — dari support ticket "sistem mutasi gudang offline" (status 1–3). **Master lokasi** (`manufacture_lokasi_gudang`, CRUD, GUDANG/TOKO+konsinyasi) + **dokumen mutasi antar-lokasi** (`manufacture_mutasi_gudang`) dengan siklus **DRAFT → DIKIRIM (`/kirim`) → DITERIMA (`/terima`, isi qty_terima)** — guard transisi bersyarat (MatchedCount→409). **Saldo per-lokasi** (`GET /lokasi-stok`, inti pure `saldoLokasi` teruji) diturunkan dari dokumen: masuk (DITERIMA ke tujuan) − keluar (DIKIRIM/DITERIMA dari asal). **Prinsip inti: layer pelacakan — dokumen mutasi TIDAK menyentuh `manufacture_stok`/transaksi/Accurate** (total stok global ikut Accurate, tak berubah; saldo per-lokasi = net-mutasi, bukan stok absolut). Grounded: `lokasi_gudang.go`, `mutasi_gudang.go` (+`saldoLokasi` test); FE `MasterLokasiView.tsx`/`MutasiGudangView.tsx`. Akses admin gudang RM/FG + PPIC/SPV.
- **Sistem Mutasi Gudang — Fase 2 (AR/Konsinyasi)** (status 4–5, 29 Juli 2026) — barang **DITERIMA di toko konsinyasi** = **piutang** sampai toko membayarkan qty-nya. **Pembayaran** (`POST /pembayaran-konsinyasi`, qty parsial agregat per toko, `manufacture_pembayaran_konsinyasi`) dgn **overpay-guard** (tolak bila qty_bayar > sisa piutang; akumulasi dalam-request). **Piutang diturunkan** (`GET /piutang-konsinyasi`, inti pure `piutangKonsinyasi` teruji): per kode `diterima (mutasi DITERIMA ke toko) − terbayar = piutang`; status **AR**(>0)/**LUNAS**(≤0). Qty-based, **TIDAK menyentuh Accurate** (Finance tetap akunting Rp di Accurate). FE `PiutangKonsinyasiView.tsx`; akses admin gudang RM/FG + PPIC/SPV + **Finance read-only** (`TAB_WMS_FINANCE`; form pembayaran disembunyikan). Grounded: `konsinyasi.go` (+`piutangKonsinyasi` test). **Menuntaskan Tiket 1B (Fase 1+2).** Retur konsinyasi (barang tak laku dikembalikan) = TBD.
- **Saldo awal bulanan** — `manufacture_saldo_awal_bulanan`: snapshot stok tiap awal bulan (terpisah dari master barang), dibuat otomatis oleh goroutine ticker (saat boot + tiap 6 jam, idempoten per bulan — service mati saat tanggal 1 terbackfill saat nyala) atau manual via `POST /saldo-awal/snapshot`.
- **Transaksi** — INBOUND/OUTBOUND; mengubah stok **atomik** (Mongo transaction). Field `created_by_name` **distempel server** dari header `BIP-Username` (asalnya JWT lewat gateway), BUKAN dari body — supaya nama penginput di log WMS tak bisa dipalsukan browser dan tak perlu lookup master karyawan (`metadata.created_by` hanya menyimpan employee_id). Kosong pada dokumen sebelum field ini ada. **Cakupan stempel diperluas 21 Juli 2026** ([[ADR - 0025 Log Sumber vs Input WMS + Stempel Penginput]]) ke seluruh jalur tulis manual: `transaksi` (termasuk `transaksi_fg` & tiga titik pembuatan di `production.go`), `production_log`, `resi`, `material_order`, `marketing_po`, `procurement_po`, dan **`proposal`**. Jalur proposal ditambahkan menyusul: transaksi yang lahir dari approval deviasi BOM tak ikut distempel di putaran pertama sehingga kolom "Diinput oleh" di Keluar Produksi akan `—` **selamanya, termasuk data baru**. `Proposal.CreatedByName` distempel saat pengajuan lalu diwariskan ke transaksi saat `APPLIED` — yang tersimpan adalah nama **pengaju**, bukan approver. Nilai dari body **selalu ditimpa** (diuji: body `created_by_name:"PALSU"` + header `BIP-Username: budi.santoso` → tersimpan `budi.santoso`). **Resi hasil sync marketplace sengaja TIDAK distempel** (`upsertResiFeed`) — datanya bukan diketik orang; ada/tidaknya nama sekaligus jadi penanda manual vs sync di FE Master Resi. Validasi kode menerima **master bahan baku ATAU produk jadi** (`master_product`) → transaksi barang jadi (FG in/out) tidak lagi ditolak. OUTBOUND dengan stok tidak cukup ditolak 422 (**stok tidak boleh minus**). `PATCH /transaksi/:id/status` mengubah `detail.status` (alur terima SJ Kirim Produk: IN TRANSIT → DELIVERED). Field UI tambahan (Qty QC, PIC, produk jadi, formula, dll) disimpan apa adanya di field fleksibel `detail`.
- **Formula/BOM** — CRUD penuh (create/update/delete, termasuk `PUT /formula/:id`) + `POST /formula/import` (upload `.xlsx` layout tab NEW FORMULA, upsert by `product_name`). File sumber masih memakai **kode bahan lama (mnemonik)** → dipetakan ke kode kanonik lewat `bahanResolver` (kanonik → alias → nama); kode tak dikenal dilaporkan, tidak dibuang diam-diam.
- **Produksi** — order produksi (konsumsi stok) + production-log (catatan tanpa konsumsi).
- **PO & Proposal** — marketing-po, procurement-po (status workflow + audit), proposal approve/reject. Proposal dipakai untuk **deviasi pemakaian fisik vs teori BOM** pada keluar-produksi: FE membuat proposal `PENDING_PPIC` (transaksi belum ada), approval dua tahap PPIC → SPV; saat `APPLIED` backend insert transaksi + potong stok dengan filter kondisional `stok_sekarang >= qty` (ditolak 409 bila akan minus). **Jenis ketiga (28 Juli 2026): revisi Saldo Awal** — proposal ber-`original_tx.tipe="SALDO_AWAL"` (`ref=bulan`); saat `APPLIED` cabang khusus meng-update `manufacture_saldo_awal_bulanan` (`qty`) alih-alih transaksi/stok, dalam satu `RunTransaction`.
- **Bukti Selisih RM** ✅ (18 Juli 2026) — `manufacture_selisih_rm`: log bukti selisih bahan baku antara yang **dikirim** gudang RM dan **diterima** produksi (kode/nama/satuan, qty_dikirim, qty_diterima, keterangan, `foto[]`=minio_object key, tanggal WIB). `GET/POST /selisih-rm` (+`?date`, `?status`) & `DELETE /selisih-rm/:id`; tanpa alur approval, form bebas. **Dua sisi dicatat terpisah** (20 Juli 2026, keputusan user): gudang `POST /selisih-rm` (sisi dikirim, status `MENUNGGU`) → produksi `PATCH /selisih-rm/:id/terima` memilih catatan kiriman itu dan mengisi qty/foto/keterangan penerimaan (status `SELESAI`); penerimaan tanpa catatan kirim lewat `POST /selisih-rm/terima` (`dikirim_dicatat=false`, selisih tak dihitung). Penerimaan ganda ditolak lewat filter `diterima_dicatat != true`. Dokumen lama (satu form dua qty) dinormalisasi saat `GET` jadi `SELESAI`+kedua sisi tercatat — tanpa migrasi. **Pelapor**: `pelapor_nama`/`pelapor_posisi` & `penerima_nama`/`penerima_posisi` didenormalisasi dari header `BIP-Fullname`/`BIP-Position` saat tulis (tak perlu panggil employee-service); dokumen lama jatuh balik ke `metadata.created_by` di FE. **FE**: dua tab (Dikirim/Diterima) dengan form masing-masing, filter tanggal berupa dropdown daftar tanggal yang ada (default **hari ini** WIB, ditandai "(hari ini)"; opsi "Semua Tanggal"), kolom waktu memuat jam, satuan bahan tampil sebagai suffix di kotak qty, kolom Pelapor/Penerima + Posisi. **Foto**: dikompres di browser (Canvas maks 1600px + JPEG q0.7, tanpa dependensi) → presigned **file-service** (`manufacture/selisih-rm`, ditambah ke `validatePutPresigned` allowlist) → MinIO; service ini hanya simpan key. Menu top-level di sidebar (bawah Pengawasan). Akses FE: **posisi Security** + role **admin gudang RM/FG** + PPIC/SPV (tab `selisih_rm`; pengecualian posisi Security di `bolehAksesTabWmsUntukUser` + push modul WMS di sidebar utk Security tanpa role manufacture — pola sama Finance→Gudang FG).
- **Audit log** — `GET /audit-log` (siapa mengubah apa, per aksi/target) + `GET /audit-log/rekap?bulan=YYYY-MM` (agregasi aktivitas CRUD per user untuk **KPI otomatis**; batas hari/bulan pakai **WIB (UTC+7)**, respons ber-flag `truncated` bila entri bulan >20k).
- **Resi — Master Retur Ekspedisi (bridge marketplace)** — `manufacture_resi` (upsert by `no_resi` unik) untuk auto-fill scan form Return & Keluar FG, kini **terisi otomatis** dari order marketplace lewat integration: manufacture bisa **pull** (`POST /resi/sync-tiktok` → `/tiktok/shop/orders/resi-feed`; `POST /resi/sync-shopee` → `/shopee/orders/resi-feed`) atau menerima **push** batch (`POST /resi/sync-batch`) dari scheduler integration `sync-resi-wms` (tiap 10 mnt, watermark per-channel). Tiap resi membawa `status_pesanan`, `tanggal_rts` & `shift` gudang, dihitung saat resi terbuat dari waktu ready-to-ship (WIB): **Shift 1** 08–16 · **Shift 2** 16–24 · **Shift 3** 00–08 (menutup 24 jam; tak ada lagi kategori "Luar Jam"). FE **Master Resi** paginasi **per-hari** (nomor urut reset per hari), filter marketplace + **order status kanonik** (To Process/To Ship/Shipped/Completed/Returned/Cancelled, sama seperti modul Integration) + shift, dan **export Excel** (modal filter + pemilih kolom: Order Id/No Resi/SKU/Nama Produk/Qty + opsional). Detail sisi produsen: [[Microservices - Integration Service]]; jadwal: [[IT - Background Jobs & Schedulers]].

- **Feed Retur Marketplace (tab Return Dari Ekspedisi)** ✅ — `GET /returns` menyajikan retur marketplace **beserta status pencatatan gudang**, sumber tabel tab *Return Dari Ekspedisi* di Gudang Barang Jadi. Retur ditarik dari integration `GET /transactions/returns` lewat **HTTP + gateway key** (`getFromIntegration`, sesuai [[ADR - 0002 Database-per-Service]]), lalu di-join **di kode Go** dengan `manufacture_transaksi` — `integration_db` & `manufacture_db` beda cluster Mongo sehingga `$lookup` mustahil. **Kunci join = `order_id`, bukan `dedupe_key`** (diubah 22 Juli 2026): satu grup retur = 1 faktur + 1 tanggal dan bisa berisi **beberapa order** yang tiap paketnya datang terpisah; join per grup membuat pencatatan paket pertama langsung menghijaukan baris paket kedua — dan baris hijau tak bisa diklik, jadi paket kedua tak akan pernah bisa dicatat. Record lama (pra-fitur, tanpa `returnOrderId`) jatuh balik ke `dedupe_key` supaya riwayatnya tak hilang. Status (`BELUM_DICATAT`/`SUDAH_DICATAT`/`TANPA_BARANG_BALIK`) **diturunkan saat baca, tidak disimpan**, supaya tak bisa melenceng dari kenyataan; `meta.belum_dicatat` = sinyal tunggakan Finance (FE kini menghitung sendiri aktif vs "basi" >14 hari dari feed, `meta.belum_dicatat` tak lagi dipakai FE tapi tetap disajikan endpoint). Record gudang ditandai `detail.returnKey` (= `dedupe_key`) **dan `detail.returnOrderId`** (= order paket), index sparse `detail_return_key_idx`. Rincian kontrak: [[API - Manufacture Service]]. **`no_resi` dilengkapi backend** (21 Juli 2026) dengan mencocokkan `order_id` → `manufacture_resi.nomor_pesanan` + index `nomor_pesanan_idx`: feed marketplace tak membawa nomor resi, dan sebelumnya FE mengisi kolom resi form dengan `return_sn` (nomor RETUR) sehingga resi tak pernah terbaca. Bila order tak ada di Master Resi, `no_resi` kosong dan FE membiarkan kolomnya kosong + meminta admin men-scan — mengisi nomor yang salah diam-diam menghasilkan catatan retur yang tak bisa dijoin. **Resi parsel BALIK (22 Juli 2026)**: `marketplaceReturn` kini meneruskan `reverse_tracking_no`/`reverse_logistics_status`/`reverse_channel` dari feed integration (sumber Shopee `get_return_detail`, lihat [[Microservices - Integration Service]]). Untuk retur, FE **mengutamakan `reverse_tracking_no`** (resi paket yang benar-benar diterima gudang) di form; `no_resi` (resi FORWARD) hanya cadangan. **Scan gudang mencocokkan ke `reverse_tracking_no` DULU** (resi paket balik tak ada di Master Resi) sebelum lookup Master Resi — inilah yang membuat scan paket retur bekerja.
- **Kirim-ulang konfirmasi retur + penanda "belum sampai Accurate"** ✅ (2026-08-12, [[ADR - 0025 Log Sumber vs Input WMS + Stempel Penginput]] amandemen) — konfirmasi gudang dikirim **fire-and-forget** (`go kirimKonfirmasiRetur`), jadi kegagalannya cuma jadi satu baris log dan returnya **macet selamanya**: sweep malam melewatinya (mengira menunggu barang) dan Retry menolaknya. Satu-satunya jalan pulih selama ini adalah **menyimpan ulang form**, yang membuat transaksi baru dan **menambah stok WMS dua kali**. Terukur prod 2026-08-11: **20** retur macet karena scan tercatat MANUAL (tak tertaut) + **32** sudah tertaut tapi POST tak sampai. **`POST /returns/konfirmasi-ulang`** — **supervisor-only** (`requireWmsSupervisor`; ia membukukan ke Accurate, setara push Accurate — admin gudang FG & finance ditolak, ada tesnya), **dry-run default** (menulis butuh `?apply=1`), **lingkup wajib** (`resi=a,b,c` / `dari`&`sampai` / `catatanSaja=1`) + `limit` (maks 500) + `skip` (paginasi). Menautkan dan/atau mengirim ulang **di atas transaksi yang SUDAH ada** — tak pernah membuat transaksi, tak pernah menyentuh stok; itulah bedanya dari "simpan ulang form". Putusan `putuskanKonfirmasi(tertaut, keadaan)` memakai `accurate_booking` yang sudah ada: tak ditemukan / sudah discan di integration → lewati; `pre_cutover` → **tautkan saja, JANGAN kirim** (grupnya memang tak pernah ada, POST pasti gagal); selain itu (termasuk `booked`) → tautkan-dan-kirim (belum tertaut) atau kirim-saja (sudah tertaut). ⚠️ **Koreksi (🟡 2026-08-12, kode selesai — BELUM deploy):** versi awal menyamakan `booked` dengan `pre_cutover` (dua-duanya "tautkan saja") — keliru, `booked` adalah status GRUP bukan status order ini, dan menyamakannya adalah akar bug under-book baru (~1.451 order/2.519 unit, terpisah dari insiden 30 Juli). Detail penuh + fix di FE (`takPerluApproval`) yang jadi akar sebenarnya: [[ADR - 0025 Log Sumber vs Input WMS + Stempel Penginput]] amandemen "`booked` bukan beres". Scope `catatanSaja=1` menjaring populasi ini; short-circuit lama yang mempercayai stempel `detail.catatanSaja` sebelum evaluasi dihapus (mempercayainya = mempercayai bug yang sama). `kirimKonfirmasiRetur` kini **mengembalikan error** supaya laporan bisa menyebut pengiriman yang gagal; jalur otomatis dari form tetap mengabaikannya secara eksplisit. Penanda **`konfirmasi_tertunda`** pada feed `/returns` diturunkan **saat baca** dari `scanned_gudang` milik integration (bukan stempel lokal — stempel hanya bisa bicara tentang kejadian sesudah dipasang, sehingga 5.868 baris lama akan tampak mencurigakan tanpa dasar; diturunkan dari seberang, data lama langsung benar **tanpa backfill**). ⚠️ **Jalur kelima pemicu pembukuan retur** — setelah ingest marketplace, webhook, sweep malam, dan konfirmasi gudang otomatis — dan satu-satunya yang dipicu manusia. ⚠️ Query-nya (`detail.noResi` / `metadata.created_at`) **tak beririsan index** dan `manufacture_transaksi` memuat seluruh mutasi gudang → tiap panggilan memindai koleksi; terbatas karena berlingkup, ber-`limit`, dan dipicu manusia (bukan cron). Index sengaja belum ditambah — menambahnya sebelum terukur = menebak. Grounded: `retur_konfirmasi_ulang.go`, `konfirmasiTertunda` (`returns.go`), tes `retur_konfirmasi_ulang_test.go`/`returns_test.go`/`rbac_test.go`.
- **Paginasi feed retur diparalelkan + fallback 1-order (✅ 2026-07-22/23)** — `fetchAllReturns` dulu menarik SEMUA halaman `/transactions/returns` secara **berurutan** (sampai 10 round-trip network+Mongo bergantian) tiap tab dibuka — lambat saat backlog besar (terukur prod: 17.575 order lolos filter retur). Kini ambil halaman 1 dulu, baca `total_page` (dihitung backend dari COUNT Mongo asli, bukan ditebak dari ukuran batch — `capReturnPages`), sisanya ditarik **paralel** (maks 5 sekaligus, `returnFetchConcurrency`); semantik `truncated` & cap 2.000 (`returnFetchMaxPages`) tak berubah. **Truncation terbukti bikin false-negative, bukan cuma lambat**: order lama (shipped Juni, baru cancel 21 Juli) valid & dikenal sistem tapi jatuh di luar 2.000 baris yang termuat (sort terbaru-dulu) → gudang scan resi-nya dapat "tak cocok dengan retur marketplace mana pun" padahal sistem sudah mengenalnya (kasus nyata JX9768809830/order `584662560780813370`). **Fix**: `GET /returns/lookup?order_id=X` — pencarian TERTARGET 1 order (proxy `/transactions/returns?order_id=X` milik integration, tak menyentuh cap paginasi sama sekali) dipanggil FE sebagai fallback saat pencocokan in-memory gagal, sebelum menyerah dengan pesan "tak cocok". Grounded: `returns.go` (`fetchReturnsPage`/`capReturnPages`/`LookupReturnByOrderID`), `returns_test.go` (`TestCapReturnPages`); FE `GudangBarangJadiView.tsx` (`fallbackLookupOrder`, `terapkanReturnCocok` — satu fungsi dipakai jalur normal maupun fallback supaya keduanya tak pernah berbeda perlakuan).
  - ⚠️ **Cap 2.000 kini terlampaui jauh — batas pengaman berubah jadi batas kehilangan data** (terukur prod 6 Agustus 2026). `returnFetchPageSize = 200` × `returnFetchMaxPages = 10` memberi plafon 2.000, dan komentar konstantanya beralasan *"2.000 retur jauh di atas volume nyata"*. Asumsi itu **terbantah**: `countDocuments` atas filter feed yang sama (`{$or: [return ada, CANCELLED + shipped_at ada]}`) memberi **25.787** baris — layar menampilkan **7,8%**, dan setiap angka turunannya (badge "PERLU DICATAT", ketiga label tab) lebih kecil dari kenyataan. Pertumbuhannya cepat: 17.575 (22 Juli) → 25.787 (6 Agustus), **+47% dalam 15 hari**, jadi plafon berapa pun akan terlampaui lagi. FE sudah berhenti menyajikan angka itu seolah pasti (awalan `≥`, lihat [[APP - Web ERP]]), tapi **kebenarannya belum diperbaiki**.
  - **Menaikkan plafon saja bukan solusi — paginasi `skip` degradasi kuadratik** (terukur prod 6 Agustus 2026). Halaman 1 (`skip 0, limit 500`) = **260 ms**; halaman 52 (`skip 25.000, limit 500`) = **3.345 ms**, 12,9× lebih lambat, karena Mongo menelusuri 25.000 dokumen dulu. Menjumlahkan 52 halaman ≈ **95 detik kerja Mongo** (~19 detik pada `returnFetchConcurrency = 5`), belum termasuk HTTP, decode, dan join ke `manufacture_transaksi`. Keyset/cursor membuat tiap halaman murah tapi **tak bisa diparalelkan** (halaman N+1 butuh kursor halaman N) → 52 × 260 ms ≈ 13,5 detik berurutan. Kesimpulan: **sapuan penuh per-request tak layak dalam bentuk apa pun**; baris dan hitungan harus dipisah jalur — baris via keyset pagination (hanya halaman yang dilihat), hitungan via worker berkala yang menyimpan angka kecil. Keduanya **belum dikerjakan**; yang kedua butuh ADR sendiri (toleransi basi pada angka yang dipakai Finance). Catatan desain: `limit` yang diminta masih 200 padahal integration mengizinkan **500** ([[Microservices - Integration Service]] `/transactions/returns`).
- **Mode catatan-saja untuk retur pra-cutover, atau order booked yang SUDAH tercermin di dokumen (✅ 2026-07-22; syarat `booked` dikoreksi 🟡 2026-08-12, kode selesai — BELUM deploy)** — retur `"pre_cutover"` (kirim sebelum cutover retur, sengaja di luar Accurate — lihat [[Microservices - Integration Service]]), atau retur `"booked"` yang order INI-nya sendiri sudah punya baris di dokumen (`scanned_gudang === true`, per-order dari integration), TETAP bisa dibuka form-nya & dicatat kondisi fisiknya (Reuse/Rework/Reject → dashboard + stok WMS gerak), TAPI transaksi yang lahir dari situ **tidak** memicu POST `warehouse-confirm` ke integration (`kirimKonfirmasiRetur` di-skip via `returnCatatanSaja(tx)` — flag `catatanSaja` dikirim FE di `detail`, diputuskan `takPerluApproval` di `retur-filter.ts`). ⚠️ **Sebelum 2026-08-12**, syaratnya cuma `accurate_booking === "booked"` tanpa cek `scanned_gudang` — grup bisa SENT dari member LAIN sementara order ini belum pernah discan sama sekali, dan flag itu tetap ikut ditulis `true`: gudang mencatat kondisinya, tapi konfirmasi tak pernah dicoba dikirim. Itu akar bug under-book ~1.451 order/2.519 unit (bukan gerbang backend seperti insiden 30 Juli — ini di titik keputusan FE saat form disimpan). Detail: [[ADR - 0025 Log Sumber vs Input WMS + Stempel Penginput]] amandemen "`booked` bukan beres". Mencegah dua salah: membukukan ulang retur yang sudah SENT DAN order ini sudah tercermin (rebuild sia-sia), dan mencoba warehouse-confirm untuk retur pra-cutover yang memang tak punya grup di `accurate_daily_returns` (akan gagal). Banner form menjelaskan status ini eksplisit, bukan diam. Grounded: `retur_konfirmasi.go` (`returnCatatanSaja`), `transaksi.go` (guard di `CreateTransaksi`), `retur-filter.ts` (`takPerluApproval`, erp-frontend).
- **Export laporan retur (`GET /returns/export`, ✅, server-side — TIDAK terdokumentasi sebelum 2026-08-20)** — men-generate `.xlsx` langsung di backend (`excelize`), dipanggil tab Return (modal export, `doExport` di FE). Spine ditentukan `scope`: `RECORDED` (default, dari `manufacture_transaksi` yang gudang catat, sumbu tanggal = `metadata.created_at`) atau `ALL` (dari feed retur integration, sumbu tanggal = tanggal retur). Kolom dipilih via `?cols=` (CSV key) + `granularity` (`RETUR`/`SKU`); katalognya (`returnExportColumns`, `returns_export.go`) **SENDIRI, terpisah dari FE** (`EXPORT_COLUMNS`, `retur-export.ts`) — migrasi dari implementasi client-side lama (`buildBarisExport`, komentar kode masih menyebutnya) tidak menyatukan kedua katalog jadi satu sumber. ⚠️ **Konsekuensi ditemukan langsung oleh user (✅ 2026-08-20)**: kolom baru ditambah di FE tanpa menyentuh sini terkirim lewat `?cols=` tapi **diam-diam dibuang** `selectExportColumns` (kunci tak dikenal) — file hasil download tampak "kolom terpilih" di modal tapi kosong di isinya. Perubahan kolom KE DEPAN wajib menyentuh KEDUA sisi. Pengaman baris: `exportReturnRecordedMaxRows` (3.000) & `exportReturnAllMaxRows` (5.000). Grounded: `returns_export.go` (`ExportReturns`, `nilaiKolomExport`, `selectExportColumns`), tes `returns_export_test.go`; detail insiden & fix: [[ADR - 0025 Log Sumber vs Input WMS + Stempel Penginput]].
- **Status kesehatan sync marketplace (`GET /returns/sync-health`, ✅ 2026-07-23)** — proxy tipis ke `GET /jobs/:name/status` milik integration (framework worker sudah mencatat riwayat run, lihat [[Microservices - Integration Service]] §*Workers/Jobs* — tak ada mekanisme observability baru) untuk 3 job: `sync-tt-shop-orders`, `sync-shopee-orders`, `sync-resi-wms`. Tujuan: operator gudang tahu APAKAH sync sedang sehat **sebelum** menyimpulkan "resi/order ini tidak ada" — jawaban yang benar untuk kekhawatiran "cron gagal diam-diam" TANPA menambah panggilan API marketplace live (itu justru penyebab insiden outage rate-limit GMS Shopee sebelumnya). FE hanya menampilkan banner saat **ada** job bermasalah (diam saat sehat — cegah alert fatigue). Grounded: `sync_health.go` (`deriveSyncHealth`, `GetReturnSyncHealth`), `sync_health_test.go`; FE `retur-filter.ts` (`sejakWaktu`).

> **Performa sync (18 Juli 2026):** ketiga endpoint sync (`master-bahan/sync-accurate`, `stok/sync-accurate`, `master-product/sync-hpp`) memakai **`BulkWrite` unordered** (helper `bulkUpsert`/`upsertModel`) — sebelumnya loop `UpdateOne` per-item (~1000+ round-trip Mongo berurutan tiap klik) yang di prod (VPS, RTT jaringan) memakan detik-detik; kini 1 round-trip per koleksi (~0.1s/sync di uji lokal). Bukan "async" — kerjanya dibuat benar-benar cepat, bukan disembunyikan.

> **Paginasi resi (18–20 Juli 2026):** `manufacture_resi` puluhan ribu baris & tumbuh terus (feed marketplace tiap 10 mnt). `GET /resi` polos mengirim semuanya (~8MB lokal) → Master Resi lambat. Ditambah `?date`/`?since` + `GET /resi/days` + index `tanggal_rts`/`tanggal_pesanan`; `/resi?date=` = 57KB (**140× lebih kecil**). **Master Resi kini ambil per-hari dari server**: `/resi/days` (satu baris per tanggal) mengisi navigasi hari, isi hari ditarik saat tanggal dibuka — browser tak lagi memegang seluruh koleksi. **Scan resi** (form Retur & Keluar FG) memakai `GET /resi/lookup/:resi` ke DB, bukan mencari di memori. `GET /resi` kini **wajib berparameter** (`q`/`date`/`from-to`/`since`) — mode "kirim seluruh koleksi" dihapus & ditolak 400, karena itulah sumber lambatnya. Urutan baca dikunci `_id` naik supaya Master Resi dan fitur "Ambil Rentang" Gudang FG melihat nomor urut per-hari yang sama. Sisa: jendela `?since` masih dipakai Gudang FG, ditandai untuk dihapus setelah konsumen terakhirnya pindah ke lookup/days.

> **Scroll horizontal WMS (21 Juli 2026):** batang scroll horizontal ada di *dasar* kontainer tabel — pada tabel tinggi dasarnya jatuh di luar layar sehingga user harus menggulung halaman sampai bawah dulu baru bisa menggeser ke samping. Perbaikan di dua sisi: (1) `globals.css` aturan bersarang `.wms-root` — `::-webkit-scrollbar` didefinisikan eksplisit (mematikan overlay/auto-hide) + `overflow-x: scroll` supaya batang selalu hadir di semua menu, berlaku ke ~28 kontainer tanpa menyunting satu per satu; (2) `manufacture-app` menghitung tinggi tiap kontainer saat render = tinggi layar − posisi atas − 24px (dibatasi `max-h-[…]` kelasnya bila lebih kecil, lantai 140px), memakai **ResizeObserver** + listener `resize` — bukan `requestAnimationFrame`, karena rAF tak dijalankan browser saat tab tak terlihat sehingga pengukuran pertama terlewat. Halaman `/manufacture/kpi` (di luar manufacture-app) diberi penanda `wms-root` sendiri.

> **Asal data (`source`) — 21 Juli 2026:** sync dulu tak pernah mengisi field `source`, sehingga semua baris kosong dan UI melabelinya **"Sheet"** (peninggalan era Google Sheets yang sudah dihapus) — label yang menyesatkan. Sync kini menstempel setelah bulk upsert: master bahan → `accurate`; produk ber-cost HPP → `hpp`; produk daftar kurasi dari Accurate (`produkNonHPP`) → `accurate`. Baris `manual` (dibuat lewat web) **tak ditimpa** (filter `source != manual`). Baris lama yang bukan hasil kedua sync (di local dev: 40 BAHAN KEMAS + 2 KERTAS di `master_product`, kode mnemonik lama spt "AJ"/"BR", di-import 13 Juli pra-Accurate) `source`-nya kosong. **Master Barang menampilkan BAHAN BAKU & BAHAN KEMAS dari `master_bahan`** (bukan dari `master_product`): keduanya di-sync ke `master_bahan` (prefix BBK/BBO baku, BKK/BKO kemas), sedangkan Master Barang membaca `master_product`. Chip BAHAN BAKU/KEMAS di FE menggabungkan baris `master_bahan` (edit melompat ke Database Master), dedupe by kode. Kolom Sumber baris itu membaca **`source` asli** master_bahan → tampil **"Accurate"** (bukti data dari sync); "Master Bahan" hanya fallback bila `source` kosong. Tanpa ini, di prod (yang `master_product`-nya tak punya baris kemas legacy) chip BAHAN KEMAS tampak kosong meski datanya sudah tersinkron. **Dedup baris legacy — 22 Juli 2026:** baris bahan kemas/baku legacy di `master_product` (source kosong) dulu menang dedupe by-kode & tampil **"—"**, menutupi versi Accurate-nya. Kini FE mencocokkan **nama ternormalisasi** legacy vs `master_bahan`: yang **duplikat** (nama sama, di local 14 dari 40 kemas) dibuang → versi Accurate yang tampil; yang **tak ada padanan Accurate** (26 kemas + 2 kertas) tetap tampil tapi dilabeli **"Legacy"** (bukan "—") + tooltip "perlu ditambahkan ke Accurate lalu sync ulang" — supaya barang yang belum ada di Accurate justru kelihatan, bukan hilang. Non-destruktif (murni FE, `master_product` tak diubah). Hasil: chip BAHAN KEMAS = 241 Accurate + 26 Legacy, **0 baris "—"**.

## Model Data (`manufacture_db`)

19 collection (prefix `manufacture_`), grounded ke `models.go`:

- `manufacture_master_bahan` — master bahan baku. `_id` = **kode item Accurate** (`BBO-030`, `BKK-090`), bukan lagi mnemonik internal. Field: `aliases` (kode mnemonik lama, mis. `PEGA` — dipakai importer memetakan file Excel yang masih berkode lama), `faktor_stok_accurate` (pengali qty stok Accurate per-item), nama, kategori, satuan, min_stok, status app-managed, `source:"manual"`.
- `manufacture_master_product` — master produk/bahan kemas (kode, jenis_barang, kategori_produk, status, `source`, `stale`).
- `manufacture_sku_mapping` — mapping listing SKU → komponen (`_id` = listing SKU, `is_bundle`, `components[]{kode,nama,qty}`), di-sync dari HPP. Dasar pemecahan bundle di form Return/Keluar FG.
- `manufacture_stok` — snapshot stok per kode (bahan **dan** barang jadi). Angkanya ditimpa `POST /stok/sync-accurate`.
- `manufacture_transaksi` — transaksi in/out + `detail` (bson.M, field UI tambahan).
- `manufacture_formula` — resep/BOM (product_name + ingredients[kode_bahan, nama, qty_needed]); `kode_bahan` menunjuk kode kanonik master bahan.
- `manufacture_sync_log` — riwayat sync/import (tipe: `master_bahan_sync_accurate`, `stok_sync_accurate`, `sync_hpp`, `formula_import`).
- `manufacture_production_log` (+`detail`) · `manufacture_material_order` · `manufacture_marketing_po` (punya `items[]` = `MarketingPOItem`, satu PO banyak barang; field top-level `product_sku/name/order_qty` tetap diisi = item pertama + total qty demi kompatibilitas pembaca lama — ditambahkan 22 Juli 2026) · `manufacture_procurement_po` (+`detail`, menampung daftar `items` saat 1 permintaan berisi banyak bahan) · `manufacture_proposal`.
- `manufacture_saldo_awal_bulanan` — snapshot saldo awal per bulan (`_id` = `kode|YYYY-MM`, kode, nama, bulan, `qty` = saldo awal, `qty_akhir`+`akhir_at` = saldo akhir) — memisahkan saldo awal bulanan dari data master barang. **Field kunci SO** (`akhir_locked`, `akhir_source="SO"`, `akhir_keterangan`): baris yang saldo akhirnya berasal dari import Stock Opname **dikunci** dari overwrite ticker.
- `manufacture_edit_grant` — izin buka kunci edit riwayat WMS per-akun (`_id` = `employee_id|bulan`, `employee_id`, `username`, `bulan`, `granted_by`, `expires_at?`, `created_at`). Diberikan/dicabut IT lewat panel **Buka Kunci Edit WMS** (komponen FE bersama `EditGrantPanel`: dropdown karyawan manufacture via `/api/employee/list` + Employee ID otomatis mengikuti nama). **Satu grant (`employee_id|bulan`) kini membuka DUA surface** untuk akun+bulan itu (4 Agustus 2026): tombol **Koreksi Input** transaksi ledger (`HistoryView`) **dan** tombol **Revisi Saldo Awal** (`SaldoAwalView`) — keduanya cek helper yang sama (`bolehKoreksiTransaksi`). Panel tersedia di **dua menu**: tab Log Aktivitas (Audit) & menu Saldo Awal Bulanan (muncul untuk IT SPV saat membuka bulan tertutup, bulan terkunci ke bulan yang sedang dilihat). Bulan berjalan selalu bebas tanpa grant.
- `manufacture_lokasi_gudang` — master kode lokasi (`_id`=kode, `nama`, `tipe` GUDANG/TOKO, `konsinyasi`, `alamat`, `aktif`) untuk sistem mutasi gudang (Fase 1).
- `manufacture_mutasi_gudang` — dokumen mutasi antar-lokasi (`_id`=`MUT-YYYYMMDD-xxxx`, `tanggal`, `lokasi_asal`, `lokasi_tujuan`, `status` DRAFT/DIKIRIM/DITERIMA, `items[]{kode,nama,qty_kirim,qty_terima,satuan}`, `pic_kirim/pic_terima`, `dikirim_at/diterima_at`). **Layer pelacakan — TIDAK menyentuh `manufacture_stok`/transaksi/Accurate.**
- `manufacture_pembayaran_konsinyasi` — pembayaran qty konsinyasi (Fase 2; `_id`=`BAYAR-YYYYMMDD-xxxx`, `toko`, `tanggal`, `items[]{kode,nama,qty_bayar,satuan}`, `keterangan`, `pic`). Piutang **diturunkan** (mutasi DITERIMA ke toko − Σ pembayaran), tak disimpan.
- `manufacture_audit_log` — jejak perubahan oleh user (dari header `BIP-Employee-ID`/`BIP-Username`). ⚠️ `Target` hanya berisi kode/ID (mis. `tx.KodeBahan`), **bukan `_id` dokumen** — join balik dari audit ke satu baris log tertentu tidak andal; itu sebab nama penginput didenormalisasi ke `created_by_name`, bukan diambil dari audit log.
- **`created_by_name`** (bukan koleksi — field lintas-model) — nama user penginput hasil stempel server, ada di `Transaksi`, `ProductionLog`, `Resi`, `MaterialOrder`, `MarketingPO`, `ProcurementPO`, dan `Proposal` (diwariskan ke transaksi saat APPLIED). Pendamping `metadata.created_by` yang hanya menyimpan employee_id. Pola sama seperti `SelisihRM.PelaporNama`. Sengaja **tidak** ditambahkan ke `common.Metadata` di shared-library — itu akan memaksa rebuild seluruh service di monorepo untuk kebutuhan satu modul.
- `manufacture_resi` — master resi/AWB retur ekspedisi (`no_resi` **unik**, `nama_market`, `nama_toko`, `nomor_pesanan`, `status_pesanan`, `tanggal_pesanan`, `tanggal_rts`, `shift`, `ekspedisi`, `items[]{sku,nama_barang,qty_total}`) — sumber auto-fill scan form Return & Keluar FG. **Terisi otomatis** dari order TikTok/Shopee via resi-bridge (upsert by `no_resi`), tak lagi bergantung input manual.

## Keputusan Teknis (grounded)

- **Stok = opening-balance-as-transaction + reconcile**: mutasi stok pakai **Mongo multi-document transaction** (butuh replica set; itu sebab `manufacture_db` dijadikan RS 1-node + keyfile). Catatan: sejak stok bahan & barang jadi di-sync dari Accurate, **snapshot tak lagi murni "jumlah semua transaksi"** — sync menimpanya dengan angka Accurate; transaksi WMS menyesuaikan stok di antara dua sync.
- **Sumber master pindah dari Google Sheets ke Accurate/HPP**: sync Sheets **dihapus** dari service (tak ada lagi pemanggilan Sheets API; `source:"sheet"` tak dipakai). Bahan & stok ← Accurate, barang jadi & bundle ← data HPP integration, formula ← import Excel. Env `SHEETS_*` (SA key dkk) sudah dibuang dari compose (16 Juli 2026) — tak ada kode yang membacanya. Panel FE-nya kini `SyncMasterPanel.tsx` (rename dari `GoogleSheetsSync.tsx` — nama lama sudah bohong).
- **Kode bahan = kode item Accurate** (bukan mnemonik internal): kode lama disimpan di `aliases`. Perlu karena file Excel formula MASIH memakai kode lama — tanpa pemetaan alias, tiap import menghidupkan kembali kode lama dan memutus rujukan ke stok Accurate. Resolusi importer: kanonik → alias → **nama** (fallback nama sekaligus menyembuhkan kode rusak akibat parsing Excel, mis. `1,2` terbaca `1`).
- **Migrasi kode = endpoint, bukan skrip**: penyelarasan kode lama → kode Accurate dikerjakan `POST /master-bahan/align-accurate` (idempoten, `?dry_run=true` utk melihat rencana). Dibuat setelah ketahuan bahwa migrasi ad-hoc via mongosh hanya berlaku di satu mesin: di environment yang belum diselaraskan, sync menyisipkan kode Accurate di samping baris berkode lama → **master menumpuk** (terukur: 453 → 926 baris, 441 di antaranya barang yang sama). Karena itu sync kini **tak mungkin menduplikasi** — bila ada baris lama bernama sama, insert dilewati dan dilaporkan di `perlu_align`.
- **Align ≠ sync soal cakupan pencocokan**: sync menarik item Accurate MASUK ke master → wajib dibatasi prefix bahan (kalau tidak, kursi kantor & mesin ikut masuk). Align memetakan baris yang **sudah pasti bahan** ke kode Accurate-nya → memakai seluruh daftar item; membatasi prefix di sini justru menggagalkan ALKOHOL 70% (`PP-014`) & PLASTIK SHRINK (`PP-038`/`PP-071`).
- **Konversi satuan stok Accurate (wajib, bukan kosmetik)**: Accurate menyimpan **bahan curah dalam KG** sedangkan WMS memakai **GRAM** (formula `qty_needed` juga gram). Tanpa konversi ×1000, sync "berhasil" tapi diam-diam mengecilkan stok bahan 1000× dan perhitungan kebutuhan produksi ikut salah. Dasar: rasio stok_lama(GRAM)/qty_accurate untuk 178 bahan bermedian **1002** (kuartil 966–1056); PCS/LITER/ROL/KG berasio ~1. `accurate_stocks` **tidak membawa satuan**, jadi satuan diambil dari master bahan WMS.
- **Faktor stok per-item dipisah dari satuan** (`faktor_stok_accurate`, menang atas aturan satuan): ada bahan yang satuan Accurate-nya tak bisa disimpulkan dari satuan WMS — mis. `BBO-056` CANGKANG KAPSUL, Accurate memakai **ribuan butir** sedangkan WMS memakai PCS (faktor 1000). Menandainya `GRAM` cuma demi memicu ×1000 memang membuat angka MRP benar, tapi labelnya salah (kapsul terhitung sebagai bahan curah bergram di dashboard) — karena itu faktor dibuat eksplisit dan bisa diatur via `PUT /master-bahan/:kode`.
- **`UpsertMetadata` menuntut metadata LAMA saat update** (bug diperbaiki 21 Juli 2026): helper shared-library memutuskan create-vs-update dari `CreatedAt == nil`, jadi pemanggil yang mengirim `common.Metadata{}` saat update selalu dihitung "create" — `created_by` tertimpa si pengubah dan `updated_by` tak pernah terisi. Tiga pemanggil salah, ketiganya benar-benar menulis hasilnya ke `$set` (bukan nilai yang dibuang): (1) `UpdateResi` mengambil metadata dari **body client** — praktis selalu kosong, dan berarti client bisa mengarang `created_by`; kini dibaca dari dokumen di DB. (2) `upsertResiFeed` — **tiap sync menimpa `created_at`/`created_by` ribuan resi lama** dengan pemicu sync terakhir; kini `$set` untuk `updated_*` + `$setOnInsert` untuk `created_*`. (3) `ImportFormula` — import ulang menimpa pembuat formula asli; pola sama. Diuji: dibuat user A → diubah user B (sambil menyisipkan `metadata.created_by` palsu di body) → `created_by` tetap A, `updated_by` jadi B, nilai palsu diabaikan; lalu disentuh `/resi/sync-batch` → `created_by` tetap A. ⚠️ **Data yang terlanjur tertimpa sync lama tidak bisa dipulihkan** — nilai aslinya tak tersimpan di mana pun. Catatan: pada dokumen **baru** hasil sync, `updated_*` ikut terisi sama dengan `created_*` karena bulk upsert tak bisa membedakan insert vs update di muka.
- **Tagged logging** `[Manufacture <Event>]` untuk identifikasi di monitoring Docker.
- **Audit log** merekam user login terakhir yang mengubah data di tab mana pun (header gateway).
- **Field `detail` (bson.M) sebagai blob fleksibel**: model Go inti (transaksi, production-log, procurement-po) minimal, sedangkan UI punya banyak field tambahan (QC, PIC, checklist, daftar bahan). Field tersebut disimpan apa adanya di `detail` agar log/ledger/arsip tetap utuh setelah reload — tanpa menambah kolom per-field di model.
- **`_id` production-log WAJIB string (bug 404 diperbaiki 3 Agu 2026)**: `ProductionLog.ID` bertag `bson:"_id,omitempty"`. Bila pembuat tak mengisi id, `omitempty` membuangnya → Mongo menstempel `_id` **ber-tipe ObjectID**, padahal `DeleteProductionLog`/`UpdateProductionLog` mencari `_id` sebagai **string** (dari URL) → query string tak pernah cocok dgn key ObjectID → **404 "log tidak ditemukan"** (log tak bisa dihapus/diedit). Dua jalur pembuatan sempat meninggalkan id kosong: `CreateProduction` (order produksi konsumsi-stok) tak pernah set id, dan `CreateProductionLog` mempercaya `_id` dari body (bisa kosong). Perbaikan: helper `newProductionLogID()` (prefix `PRD-` + ObjectID hex, string, unik) dipakai di **kedua** jalur — `CreateProduction` selalu, `CreateProductionLog` sebagai jaring-pengaman saat body tak berisi `_id`. Dokumen lama ber-`_id` ObjectID dirapikan lewat migrasi mongosh (ObjectID → nilai hex sama tapi tipe string) agar bisa dihapus. Pelajaran senada dgn "migrasi kode = endpoint" di atas: environment yang belum dimigrasi (mis. prod VPS) tetap menyimpan doc ObjectID sampai skrip dijalankan di sana.
- **Item master manual kebal sync**: master-bahan **dan master-product** buatan WMS (`source:"manual"`) dikecualikan dari penandaan `stale` saat sync, jadi tidak hilang. Sync bersifat **upsert murni** — kode yang tak ada di sumber dibiarkan, tak pernah dihapus.
- **Mapping gudang = per-master** (tidak ada kolom gudang per baris): kode master bahan = **Gudang Utama** (bahan baku+kemas); kode master barang = **Gudang Tinggar** (kemas+barang jadi); **Gudang Sadewa** (titipan) dihitung dari net transaksi bergudang-simpan "sadewa". Kode yang terdaftar di dua master terhitung di dua gudang (aproksimasi; stok per-gudang sejati masih TBD).
- **Aturan stok & approval keluar-produksi**: stok **tidak boleh minus** (blokir di FE + validasi 422 backend + filter kondisional saat proposal APPLIED). Approval PPIC→SPV terpicu oleh **deviasi fisik pemakaian vs teori BOM** (bukan oleh kekurangan stok). **Tambah/hapus bahan (28 Juli 2026)**: di Form Bahan Keluar ke Produksi, sesudah pilih formula operator bisa **menghapus** bahan BOM atau **menambah** bahan di luar BOM — keduanya dihitung deviasi (`hasBomDeviation(usedIngs) || ada bahan formula dihapus`) sehingga **seluruh** pengeluaran masuk jalur proposal PPIC→SPV (tak ada potong stok langsung); bahan tambahan bertugas `qty teori 0` sehingga selalu deviasi.
- **Rantai alur FE saling tereferensi & anti-duplikat**: keluar-produksi membuat `grupDokumen` (RM-OUT-BOM) → Laporan Hasil Produksi mengambil grup (menyimpan `grupDokumen`; grup terpakai disembunyikan) & Qty Masuk Gudang menciptakan stok FG (`PROD-<batch>`) → Kirim Produk mengambil laporan (menyimpan `refLaporanProduksi`; laporan terpakai disembunyikan; OUTBOUND, SJ IN TRANSIT) → Input Gudang FG menerima SJ (PATCH status DELIVERED; INBOUND + gudang simpan Utama/Tinggar/Sadewa) → Masuk Kembali mengembalikan sisa bahan per grup via checklist. 
- **Akses modul WMS**: dua lapis FE. (1) `ManufactureGuard`: butuh system role `manufacture` (supervisor IT super-akses). (2) **Matriks akses per menu** (`features/manufacture/akses.ts`, keputusan user 17 Juli 2026) — dipakai sidebar (menu terlarang disembunyikan) + gerbang tab (URL langsung → "Menu tidak tersedia") + halaman KPI. Role granular SUDAH dibuat user di Master Data (17 Juli, penulisan berspasi: "admin gudang RM"/"admin gudang FG"/"admin produksi" — pencocokan dinormalisasi huruf-kecil + non-alfanumerik→underscore): Gudang RM=admin gudang RM · L. Hasil Produksi=admin produksi · Gudang FG=admin gudang FG · Manajemen Stok/Bundle/M.Resi=admin gudang FG+admin gudang RM · Order & Dokumen=admin produksi+admin gudang RM+admin gudang FG · Cetak SJ (Print Center)=admin gudang RM · Pengawasan (Persetujuan/Audit/Laporan/KPI) + semua akses=PPIC & SPV · **panel Sync Master khusus PPIC & SPV** (`bolehSyncMaster` — tombol disembunyikan bagi admin). **Tombol sync dipecah per-domain (22 Juli 2026)** lewat prop `tampilkan` di `SyncMasterPanel`, tiap menu pegang satu domain: **Master Barang → Sync Master Bahan** (Accurate) · **menu Stok → Sync Stok** (Accurate; toggle "Sync Stok" di header, sisanya murni Database Master) · **menu Bundle → Sync Barang Jadi & Bundle dari HPP** (`BundleView`, sudah ada). Tombol "+ Produk" manual di Master Barang disembunyikan (`TAMPILKAN_TOMBOL_TAMBAH_PRODUK=false`) — penambahan master lewat sync, bukan input manual. (Sebelumnya, 21 Juli, ketiga tombol sempat digabung di satu panel di menu Master Barang.) · warehouse staff tanpa role manufacture. · Menu turunan Master & Referensi (`master_barang`, `saldo_awal`) = akses mengikuti Stok — ⚠️ pelajaran: tab baru WAJIB didaftarkan di **MATRIKS_TAB_WMS *dan* SLUG_KE_TAB_WMS**; `master_barang` sempat hanya ada di sidebar+SECTION_TO_TAB sehingga menunya tampil tapi diklik jadi "Menu tidak tersedia" (diperbaiki 21 Juli 2026) · **Bukti Selisih RM** (`selisih_rm`, menu top-level bawah Pengawasan) = admin gudang RM/FG **+ posisi Security** (lintas-modul, dari field position). Card "Kirim FG ke Gudang Jadi" berketerangan urutan: diinput setelah admin produksi mengisi Laporan Hasil Produksi. `staff` (role lama) = **TANPA akses WMS** (keputusan user 17 Juli sore, menggantikan aturan interim gabungan-admin) — akun PPIC/gudang/produksi yang masih ber-role staff wajib dimigrasikan ke role granular/`ppic` (role `ppic` juga sudah ada di Master Data). Tombol approve tetap `ppic`/`supervisor`. Backend kini menegakkan cermin matriks per-endpoint (`rbac.go`, 2026-07-29 — lihat bullet ✅ RBAC di Catatan). · **Modul `wms_viewer`** (2026-08-05): system-role monitoring untuk user **luar WMS** — read-only **Gudang FG saja** (termasuk tab Return Dari Ekspedisi + export xlsx; tombol Export memang tak digate read-only); pola lintas-modul seperti Finance→Gudang FG (`TAB_WMS_VIEWER` di FE `akses.ts` + push sidebar + `tabViewerReadOnly` di `rbac.go`); pemegangnya TANPA role manufacture sehingga otomatis read-only (`isManufactureReadOnly`); key modul dibuat sendiri di Master Data (System Role), nilai role bebas asal tak kosong. ⚠️ pre-existing: `/manufacture/kpi` ter-redirect ke `/dashboard` utk akun uji (sedang diselidiki terpisah).
- **FE**: modul `manufacture` di [[APP - Web ERP]] (Next.js, `erp-frontend/src/features/manufacture`), 10 view (gudang bahan baku/jadi, stok, produksi, PO, KPI, dll). Fitur terbaru: tab **Master Barang** kini **CRUD produk penuh** (badge sumber Manual/Sheet), kartu **Formula BOM** punya tombol edit; modul **Pusat Laporan & KPI** menambah tab **KPI Otomatis (rekap CRUD)** — skor per user dari audit log (indikator berbobot ala menu KPI Scoring, batas WIB, prorate bulan berjalan); menu **Audit History** dipisah tab **Ledger Transaksi** vs **Log Aktivitas User** + pagination + filter tanggal. **Kunci edit riwayat bulan tertutup (28 Juli 2026)**: tombol "Koreksi Input" pada transaksi bermonth `< bulan berjalan` dikunci ("🔒 Terkunci") kecuali akun user punya `manufacture_edit_grant` aktif untuk bulan itu (helper `period-lock.ts`); transaksi tak bertanggal tak dikunci. Panel **"Buka Kunci Edit WMS"** (khusus `isItSupervisor`) di tab Log Aktivitas: IT memberi/mencabut akses edit per-akun+bulan lewat `/edit-grant` — enforcement FE-gating (backend belum cek role). Konversi field **dua arah**: tulis (legacy camelCase → Go snake_case + `_id`), baca (Go snake_case → legacy camelCase) lewat mapper di container — tanpa ini create gagal validasi (`id`/`product_sku` wajib) & arsip tampil kosong. **Konsolidasi menu "Master & Referensi" (29 Juli 2026)**: 10 item → **4 menu grup dgn bar sub-tab** — Analisa Stok (Stok/Saldo Awal/Tarikan Mutasi) · Master Data (Barang/Bundle/Resi/Lokasi) · Mutasi Gudang & Konsinyasi (Dokumen Mutasi/Piutang) · Transparansi Substitusi RM (sendiri). Config `tab-groups.ts` (pure, teruji) jadi satu sumber; **view/route/akses per-tab TIDAK berubah** (deep-link lama tetap jalan); sidebar menampilkan item grup bila ≥1 member boleh & URL diarahkan ke member pertama yang boleh (mis. Finance mendarat di Piutang); bar sub-tab di `manufacture-app` pakai predikat akses yang sama. Ikon 4 menu dibedakan (BarChart3/Package/Truck/RefreshCw).

## Belum Diimplementasikan / Catatan (TBD)

- Perhitungan kebutuhan→kekurangan otomatis dari rencana produksi (Fase 2 di [[Manufacture - Stock & Material Management]]) — sebagian (formula ada, planning belum penuh).
- Multi-gudang/lokasi + karantina ED, stock opname digital — masih konsep.
- **Integrasi [[External - Accurate]] DUA ARAH sudah dibangun** ([[ADR - 0015 Push Pergerakan WMS ke Accurate]]): read = sync stok; write = Penyesuaian Persediaan harian (anti dobel-hitung: gerakan marketplace/supplier di-SKIP karena sudah lewat Sales Invoice/Return & Faktur Pembelian). Arah tulis masih di belakang kill switch `ACCURATE_WMS_PUSH` — baru aktif di dev lokal; VM dev/prod menunggu koordinasi finance.
- **8 kode bahan belum diselaraskan** ke kode Accurate — ambigu, sengaja tidak ditebak: `SLG` (Accurate salah ketik `PP-036 SILLICA GEL`), `PS SRK 7/8/9/10/12 CM` (nama "PLASTIK SRING", Accurate punya varian ukuran+merek yang tak terputuskan otomatis — fuzzy match sempat salah pilih 9→19 CM dan 10/12→20 CM), `RF150` (BOTOL VIVIDENT 150 ML, tak ada di Accurate), `TEST-01` (data testing). Dilaporkan tiap sync via `bahan_tanpa_padanan_accurate`.
- **`#N/A` di formula** DR FAY SERUM & HAIR & BODY WASH — error di file NEW FORMULA sumber, bukan bug kode.
- **Temuan audit kode** (authz, race stok, mapper FE, seed atomik) terdaftar di [[Manufacture - Issue Code Audit 2026-06]] — perbaikan terjadwal.
- ✅ **RBAC per-endpoint sudah ditegakkan di backend** (`rbac.go`, 2026-07-29) — menutup gap lama "service tak memeriksa role sama sekali". Tiap rute kini bergerbang middleware yang mencerminkan `MATRIKS_TAB_WMS` (FE `akses.ts`): `requireTabRead`/`requireTabWrite` per tab (GET=baca, tulis=write), `requireWmsSupervisor` untuk aksi PPIC/SPV-only (Sync Master, Rencana Produksi, push Accurate, edit-grant, audit-log), `requireBatchCreate`/`requireBatchApprove` untuk Dokumen Produksi Batch, plus gerbang Sadewa. Role dibaca dari header `BIP-System-Roles`/`BIP-Position` (di-stempel gateway dari JWT) dan **dinormalisasi** (spasi→underscore) agar cocok dengan role berspasi di Master Data — beda dari `common.checkRole` yang exact-match. Pengecualian lintas-modul ikut ditegakkan: Finance read-only di `finished_goods`/`substitusi`/`piutang_konsinyasi`, posisi Security baca+tulis `selisih_rm`, modul `wms_viewer` read-only di `finished_goods` (`tabViewerReadOnly`, 2026-08-05). PPIC/SPV manufaktur & IT supervisor/admin = super-akses. `POST /transaksi` tanpa role manufaktur kini **403**. Tabel matriks di Go dijaga sinkron manual dengan FE. Lihat B9 di [[Manufacture - Issue Code Audit 2026-06]].
  - ⚠️ **`GET /transaksi` — gerbang pertama yang menilai ENDPOINT + LINGKUP DATA, bukan tab** (`requireLedgerRead`, ✅ 2026-08-10). Dengan ini `rbac.go` **tak lagi cermin satu-lawan-satu** matriks tab FE: ada peran yang boleh membaca **sebagian isi** sebuah endpoint tanpa boleh membuka tab pemiliknya — dan itu tak bisa diungkapkan `MATRIKS_TAB_WMS`. **Sebabnya:** `/transaksi` ledger generik yang dipakai banyak tab, dulu dipagari tab `stock`. Padahal isi tab **INPUT WMS** (Gudang FG) dirender **sepenuhnya dari dokumen transaksi** — resi, tanggal, market, kode & nama produk, qty Reuse/Rework/Reject, penginput, gudang asal — tanpa menyentuh master maupun stok. Akibatnya Finance, `wms_viewer`, **dan admin gudang Sadewa** (role modul `warehouse`, tak pernah lolos matriks tab) melihat tab itu **kosong 0** padahal datanya ada; untuk Sadewa bahkan pada halaman Retur Warehouse Sadewa yang datanya mereka input sendiri. **Dua jalur:** berhak tab `stock` → **ledger penuh** (perilaku lama, tak berubah); Finance / `wms_viewer` / `sadewaBolehAkses` → lolos **BACA** tapi handler **memaksa saringan baris retur** (`tipe = INBOUND` **dan** `detail.namaMarket` ber-`$nin [null, ""]`) — definisi sama dengan `filteredF3` di FE. `$nin`, bukan `$exists`+`$ne:""`: yang terakhir **meloloskan field bernilai null**. Saringan ditulis **TERAKHIR** di `buildTransaksiFilter` sehingga menimpa parameter query — saringan yang bisa dilonggarkan pemanggil bukan pengaman (ada tes yang mengirim `?tipe=OUTBOUND`). Penandanya `c.Locals` dengan **default MENYEMPIT**: bila rute suatu saat salah dipasang, admin melihat data lebih sedikit (kelihatan, mengganggu) alih-alih Finance diam-diam menerima seluruh mutasi gudang. **Menulis tak berubah** — `POST /transaksi` tetap `requireTabWrite("stock")`, keempat peran tersaring tetap 403 (ada tesnya masing-masing). Grounded: `requireLedgerRead`/`bolehLedgerPenuh` (`rbac.go`), `buildTransaksiFilter` (`transaksi.go`), `rbac_test.go` (`TestWmsRbacGates`, `TestBuildTransaksiFilter`).
- ⚠️ **Qty `Reject` pada retur ikut menambah stok FG** (form kirim `qty` = reuse+rework+reject; `deltaStok` INBOUND menambah penuh) — padahal reject seharusnya ke scrap. Belum diperbaiki; rincian & keputusan yang ditunggu ada di catatan Inbox *Temuan - Reject retur menambah stok FG*.

## Dependensi & Integrasi

- [[CORE - API Master Gateway]] (routing + inject header identitas) · [[CORE - SSO Flow]] (auth).
- [[Microservices - Employee Service]] — sumber identitas/role (audit).
- [[GA - Procurement System]] — kekurangan → pengadaan (rencana).
- [[External - Accurate]] — **sumber master bahan & angka stok (read-only)**, diakses lewat [[Microservices - Integration Service]] (`/accurate/stocks/list`, salinan lokal `accurate_stocks`); manufacture tak pernah memanggil Accurate langsung. Lihat [[ADR - 0001 Akuntansi via Accurate]].
- [[Microservices - Integration Service]] — data HPP (barang jadi & bundle) + resi-feed marketplace + **feed retur** (`GET /transactions/returns`, dikonsumsi `GET /returns`) + status job (`GET /jobs/:name/status`, dikonsumsi `GET /returns/sync-health`).
- **Env `INTEGRATION_MODULE_URL`** wajib untuk semua jalur di atas (feed retur, `/resi/sync-*`, sync HPP). Ditambahkan ke `manufacture-service` di `docker-compose.yml` bersama feed retur — **sebelumnya tidak ada**, padahal `resi.go` & `sync_hpp.go` sudah memakainya (tanpa env ini mereka balas *"INTEGRATION_MODULE_URL belum diset"*).

## Dokumen Terkait

- [[Manufacture - Stock & Material Management]] (konsep/desain) · [[API - Manufacture Service]] (endpoint)
- [[DB - Overview and Notes]] · [[APP - Web ERP]]
- [[ADR - 0025 Log Sumber vs Input WMS + Stempel Penginput]] (pemisahan log sumber/input + stempel penginput)
- [[ADR - 0027 Status Sinkron Resi Terpisah dari Update_Time Marketplace]] (kenapa `resi_sync_state` terpisah dari dokumen order — relevan karena Master Resi service ini yang mengonsumsi hasilnya)
