## Deskripsi

*Employee Service adalah master data karyawan sekaligus **sumber kebenaran (source of truth) untuk autentikasi & onboarding** di seluruh ekosistem bip-erp. Service ini menangani login (username/employee_id, PIN, biometric), registration, password hashing, serta mengelola data KPI, contract, BPJS, dan vacation quota. Selain itu ia berperan sebagai feed data lintas-service yang dikonsumsi oleh service lain. Ini adalah service terbesar dan paling lengkap (~85 route).*

- **Stack:** Go + Fiber v2 + MongoDB (replica set)
- **Path:** `services/employee`
- **Status**: ✅ Implemented penuh — service terbesar & paling lengkap, tanpa stub berarti · ✅ **multi-perusahaan (tenant)**: fondasi + direktori/agregat karyawan sudah ter-scope (F2-A, PR #659); sisa catatan kecil di bawah & [[ADR - 0029 Multi-Tenant Presensi Row-Level company_id]] · ⚠️ **akun pihak luar (vendor/mitra)**: fondasi data, gerbang masa berlaku, pemberian hak (2 sumbu), dan UI **lengkap & live**; sisa catatan: PR #972 (`/me` akun luar) belum ikut ter-deploy (lihat grup Akun pihak luar) · ✅ **resign / non-aktif karyawan**: live di produksi 2026-08-05 ([[ADR - 0035 HR Menonaktifkan Akun lewat Catatan Resign]]); ⚠️ diukur ulang 2026-09-21: **12 dokumen resign**, tetapi dari **35 akun non-aktif hanya 11** yang punya catatan — 24 kepergian tanpa tanggal, dijaga penjaga backlog yang merged 2026-09-21 (belum deploy). Catatan lama "0 dokumen, belum dipakai" sudah usang

## Endpoint / Fitur (Sudah Diimplementasikan)

**Auth & Registration (sumber SSO)**
- `POST /auth/login` — login via username atau employee_id, dengan device-hardening + akun bypass, mengembalikan PayloadJWT ke gateway
- `POST /auth/login-pin`, `POST /auth/login-biometrics` — login alternatif via PIN / biometric
- `GET /auth/refresh` — refresh token
- `POST /auth/verify-pin` — verifikasi PIN
- `POST /onboarding/register` — registrasi karyawan baru (temporary password → set username/password/PIN)
- Internal: `GET /internal/auth/user/:username`, `GET /internal/auth/employee/:employee_id`, `POST /internal/auth/change-password`, `GET /internal/auth/roles`, `POST /internal/auth/disable`
- Password & PIN di-hash via `auth.EncryptPassword`

**Account Admin (IT-staff, `RequireITStaff`)** — grup `/account/*` (operasi admin akun langsung di Employee Service)
- `PATCH /account/active-status` — set status aktif/nonaktif akun. Isinya kini `terapkanStatusAkun` (`account_status.go`), **satu-satunya** tempat `is_active` ditulis, dipakai bersama jalur resign milik HR ([[ADR - 0035 HR Menonaktifkan Akun lewat Catatan Resign]]). Kontrak HTTP-nya tak berubah: 404 untuk akun tak ada, 400 bila status sudah sama. Helper itu mengembalikan `berubah=false` **tanpa** galat untuk kasus terakhir, karena bagi cron resign itu no-op yang sah — keputusan galat-atau-bukan ditinggalkan ke pemanggil
- 🔜 **Jejak status akun** (branch `feat/employee-status-akun-bertanggal`, belum merge): tiap perubahan `is_active` yang benar-benar terjadi menulis satu baris `account_status_log`. Body `active-status` menerima `reason` opsional; alasan kosong disimpan bertanda `(tidak disebutkan)` selama masa transisi tiga tahap (BE menerima opsional → FE mewajibkan → BE menolak kosong). ⚠️ Field itu WAJIB ada di struct request: `VerboseRequestCheck` memakai `DisallowUnknownFields`, jadi field yang tak terdaftar membuat permintaan dibalas **400 `json: unknown field`** sebelum handler jalan — bentuk kegagalan yang tak terlihat di test fungsi murni mana pun. Rinciannya di bagian Resign di bawah
- `PATCH /account/forget-device` — **revoke semua registered device & web browser** (set `inactive`) **tanpa** mengubah password/PIN; untuk kasus ganti perangkat / lepas tautan device lama. Respon: "All devices and browsers revoked"
- `PATCH /account/reset` — **hard reset** akun: kosongkan username/PIN, password sementara = `employee_id`, `has_registered=false`, revoke device/browser, role dipertahankan → karyawan onboarding ulang
- `GET /account/roles`, `PATCH /account/roles` — baca / set `system_roles`
> Catatan: sebagian operasi akun juga diorkestrasi via [[CORE - IT Orchestrator]] (mis. reset-password, roles); `/account/forget-device` & `/account/reset` adalah endpoint **langsung** Employee Service (gated IT-staff).

**Master Divisi — 🔜 belum merge & belum deploy** (branch `feat/employee-master-divisi`, 2026-09-21; [[ADR - 0113 Actual vs Planning MPP Dihitung Sistem per Bulan, Berpijak pada Jejak Keluar Bertanggal]] keputusan 6)
- Koleksi `master_divisi` (`key`, `name`, `urutan`, `company_id`, `metadata`), indeks **unik** `{company_id, key}` — kedua field selalu terisi, jadi bukan kasus indeks unik atas field ber-`omitempty`. Departemen menunjuknya lewat `master_department.divisi_key` (kosong = belum dipetakan, keadaan yang sah).
- `GET/POST/PUT/DELETE /master/divisi[/:key]`. Baca ter-scope `EffectiveCompanyID` dan berbentuk `{data,count}` sama persis dengan `/master/departments`; tulis digerbang `common.RequireHRISOrITSupervisor`. `DELETE` **ditolak 409** selama masih ada departemen yang menunjuknya, beserta jumlah pemakainya — tanpa itu dokumennya hilang sementara `divisi_key` menunjuk key yatim.
- **KEY tak bisa diubah lewat `PUT`**: ia yang ditunjuk departemen. Ganti nama cukup lewat `name`. `urutan` bertipe **pointer** di body, sebab `verboseCheckFields` hanya memeriksa field string — tanpa itu body tanpa `urutan` melemparkan divisi ke puncak tiap laporan diam-diam.
- **Dibaca modul lain lewat `GET /data-type/divisi`**, jalur yang sudah dipakai `useDataTypes`, bukan endpoint baca baru. `?detail=true` memberi key + urutan untuk pemanggil yang perlu MENYIMPAN `divisi_key`. ⚠️ Cabang ini memakai `EffectiveCompanyID`, **bukan** `perusahaanReferensi` seperti cabang `department` di sebelahnya: yang terakhir juga membuka data perusahaan lain bagi pemegang `recruitment.cross_company`, dan penjaga `TestPerusahaanReferensiHanyaDiCabangBacaRekrutmen` menuntut tiap pemanggil baru diputuskan, bukan dirapikan ikut tetangganya.
- **Seed empat divisi (Commercial · Operational · Supporting · Management) hanya untuk perusahaan yang belum punya satu pun divisi**, dan ikut dipanggil saat `POST /master/companies` membuat tenant baru. ⛔ Bukan upsert per-key tiap boot: bentuk itu menghidupkan kembali divisi yang sengaja dihapus HRD, membatalkan sendiri `DELETE` di modul yang sama tanpa satu pun galat. Juga bukan pola `seedMasterDepartments` yang berhenti begitu KOLEKSINYA tak kosong, sebab itu membuat tenant kedua tak pernah kebagian.
- `PUT /master/departments/:key` memakai `ReplaceOne`, jadi aturan "field yang tak dikirim dipertahankan" kini tinggal di satu fungsi murni (`gabungFieldDepartemenTakDikirim`): `supervised_by`, `supervision_label`, `position_items`, dan **`divisi_key`**. Sebelum diangkat ke sana, tiap field baru yang lahir akan lupa dijaga dan hilang senyap saat form lama menyimpan. ⚠️ `roles` dan `positions` sengaja belum ikut dijaga (form selalu mengirimnya); dicatat di komentar fungsinya supaya kelalaian itu tak terbaca sebagai keputusan.

**Master Data — Departments & System Roles**
- `GET /master/departments`, `GET /master/departments/:key` — list / detail department (key, name, positions, roles)
- `POST /master/departments`, `PUT /master/departments/:key`, `DELETE /master/departments/:key` — CRUD department (`RequireHRISOrITSupervisor`: supervisor/admin HRIS **atau** IT — halaman Master Data di-link dari menu IT, sebelumnya HRIS-only sehingga akun IT 403; fix 17 Juli 2026)
- `GET /master/system-roles` — list system roles (feature-based: insentive, integration, dll)
- `POST /master/system-roles`, `PUT /master/system-roles/:key`, `DELETE /master/system-roles/:key` — CRUD system role (`RequireITSupervisor`: supervisor/admin IT saja — kelola role sistem di bawah IT sejak 9 Juli 2026, commit `b9f85de5`). FE menyembunyikan tombol kelola bila role tak berhak (lihat-saja).
- Seed otomatis: `seedMasterData()` meng-insert default departments (~10) dan system roles (~2) saat collection kosong
- Data-type endpoint (`GET /data-type/:dt`) sekarang membaca dari collection `master_department` / `master_system_role` (sebelumnya hardcoded di source code)
- Model: `MasterDepartment` (key, name, positions[], roles[]) dan `MasterSystemRole` (key, name, roles[]) di `shared-library/models/employee/master_data.go`
- Frontend: halaman CRUD di `/hris/master-data` (tabs Departments + System Roles)

**Legal — Register Perizinan, Kontrak & Dispute (⚠️ live di kode, runtime pending)**
- Workspace posisi Staf Legal, di-host di service ini (`services/employee/legal_{perizinan,kontrak,dispute}.go`, `RegisterLegalRoutes`). Di-host di sini — bukan service `legal` baru — agar tak menambah modul/URL gateway (menghindari panic `ValidateInternalURL`).
- CRUD `/legal/licenses`, `/legal/contracts`, `/legal/disputes`, masing-masing dengan filter query. Dipanggil FE lewat `/api/employee/legal/*`. Sejak 2026-08-13 digerbang `gateSecretary(secretary.legal.view|work|manage)`; `RequireLegalStaff`/`RequireLegalSupervisor` tinggal sebagai fallback kill-switch di tiap rute.
- Model `LegalLicense`/`LegalContract`/`LegalDispute` (`shared-library/models/employee/models.go`), collection `legal_license`/`legal_contract`/`legal_dispute`. Department `legal` **dicabut dari `DefaultDepartments`** (tak pernah lahir di dev maupun prod); haknya kini dari modul `secretary` plus peran yang diturunkan dari jabatan `Legal` di Kesekretariatan — lihat [[CORE - RBAC dan Permission Set]].
- Unggah PDF memakai endpoint generik `POST /upload` (`minio.UploadSingleHandler`) yang sudah ada. Frontend `/legal/{perizinan,kontrak,dispute}` + alert H-90/H-60. Detail: [[QA - Register Perizinan & Sertifikasi]].

**R&D Regulatory — Registrasi izin & pipeline produk (⚠️ live di kode, runtime pending)**
- Workspace posisi R&D Regulatory, di-host di service ini (`services/employee/{rnd_regulatory.go,rnd_product.go}`, `RegisterRnDRoutes`). Pola sama dengan Legal.
- CRUD `/rnd/registrations` (Register NIE/BPOM/Halal) & `/rnd/products` (Papan Pengembangan Produk). FE lewat `/api/employee/rnd/*`. Sejak 2026-08-13 digerbang `gateSecretary(secretary.rnd.view|work|manage)` — lapisan izin PERTAMA untuk kesepuluh rute ini; `RequireRnDStaff`/`RequireRnDSupervisor` tinggal sebagai fallback.
- Model `RnDRegistration`/`RnDProduct`, collection `rnd_registration`/`rnd_product`. Department `rnd` **dicabut dari `DefaultDepartments`**, sama seperti `legal`; haknya dari modul `secretary` plus peran dari jabatan `QA RND`. Detail: [[QA - R&D Regulatory (Registrasi & Pipeline Produk)]].

**Quality — CAPA, Incoming Inspection, Antrean Release Batch (⚠️ live di kode, runtime pending)**
- Workspace divisi Quality, di-host di service ini (`services/employee/{quality_capa.go,quality_incoming.go,quality_batch.go}`, `RegisterQualityRoutes`). Pola sama dengan Legal/R&D.
- CRUD `/quality/capa`, `/quality/incoming`, `/quality/batch-releases` — GET/POST/PUT gate `RequireQualityStaff`, DELETE gate `RequireQualitySupervisor`. FE lewat `/api/employee/quality/*`.
- Model `QualityCAPA`/`QualityIncoming`/`QualityBatchRelease`, collection senama. Department `quality` sudah ada di seed; role key `quality`. CAPA dipakai bersama R&D. Detail: [[QA - Quality Operasional (CAPA, Incoming, Batch Release)]].
- **Komplain QC dari Marketing** (`quality_complaint.go`, collection `quality_complaint`) juga di-host di sini, dan gerbangnya BERBEDA dari register Quality lain: baca **`gerbangBacaKomplainQC`**, tulis **`gerbangAjukanKomplainQC`** (POST dan PUT; yang mengubah tetap harus pengaju sendiri atau SPV/admin IT lewat `bolehUbahKomplain`), vonis `RequireQualityStaff`, hapus `RequireQualitySupervisor`, daftar kategori `gerbangIdentitasKomplainQC` (identitas saja). Yang dinilai tidak mengisi nilainya sendiri. Kabar `komplain-qc-diajukan`/`-divalidasi` lewat `quality_complaint_notify.go`. ✅ Daftar **kategori tertutup** miliknya sendiri beserta rute baca `GET /quality/complaints/kategori` merged 2026-09-18 (bip-erp #1968), terverifikasi DEV lewat gateway, PROD 2026-09-21. Rute dan aturannya: [[API - Employee Service]] bagian *Quality — Komplain QC dari Marketing*. ⚠️ Kedua gerbang komposit itu **baru sejak 2026-09-18**; sebelumnya rutenya memakai `RequireQualityOrMarketing` dan `RequireMarketingStaff` langsung, rinciannya di blok berikut.

**Komplain QC: lapisan akses sendiri (`quality_complaint_akses.go`, BARU 2026-09-18)**
- **Kenapa ada.** Gerbang lama bersandar penuh pada PERAN, dan tak satu pun daftar perannya memuat `insentive: icc`. Diukur PROD 2026-09-16 (catatan di kode): keempat puluh Account Specialist berperan persis itu dan **nol** di antaranya punya peran brand, jadi orang yang justru pencatat komplainnya adalah satu-satunya orang yang tak bisa mencatatnya. [[ADR - 0103 Satu Pintu Komplain Produk, Unit Tujuan Diturunkan dari Kategori]] keputusan 4 menyuruh gerbang tulis register ini mengikuti register gudang; berkas ini menjalankannya **berikut gerbang bacanya**, dan keduanya wajib datang bersamaan: membuka tulis tanpa membuka baca menghasilkan orang yang boleh mengajukan lalu kena 403 saat membuka daftarnya sendiri, persis cermin masalah yang diperbaiki [[ADR - 0099 Komplain dari Ulasan Marketplace Dirutekan per Departemen lewat Register Komplain yang Ada]] di sisi gudang.
- **`gerbangAjukanKomplainQC`** (rute TULIS): peran marketing (`common.IsMarketingStaff`) **ATAU** pemegang toko. Jalur peran **tidak disempitkan sedikit pun**, sebab perubahan ini menambah orang dan tak seorang pun boleh kehilangan akses karenanya.
- **`gerbangBacaKomplainQC`** (rute BACA): peran QC (`common.HasQualityRole`), peran marketing (`common.IsMarketingStaff`), **izin** `akuntoko.komplain.work` (`common.PermAkuntokoKomplainWork`, paket "Marketing: Pemegang Akun Toko"), **ATAU** pemegang toko. Predikatnya **dikomposisikan**, bukan menyalin daftar peran: keduanya dibangun dari daftar yang sama dengan `RequireQualityOrMarketing`/`RequireMarketingStaff`, jadi penambahan tier di kemudian hari ikut sendiri.
- **`gerbangLewatKepemilikanQC`** adalah jalur terakhir yang dipakai kedua gerbang, dan **URUTAN pemeriksaannya mengikat**: peran dan izin dibaca dari **HEADER lebih dulu** supaya hak yang sudah tertulis di token tak membayar panggilan lintas service; **identitas kosong dijatuhkan ke gerbang peran yang sudah ada** (`RequireMarketingStaff` untuk tulis, `RequireQualityOrMarketing` untuk baca), bukan diteruskan ke resolver, supaya permintaan tanpa `BIP-Employee-ID` tidak berakhir **500** padahal yang kurang identitasnya; **integration padam dijawab 500 bersebab, GAGAL TERTUTUP**, sebab 403 berarti "sudah diputuskan, kamu tak berhak" dan mengatakan itu saat sistem justru GAGAL memutuskan tak dapat dibedakan pemakainya, sementara daftar kosong lebih buruk lagi (kabar baik yang palsu). Penolakannya diserahkan ke gerbang peran supaya pesan tolak register ini tetap satu bentuk dengan rute lain.
- ⚠️ **BEDA PENTING dari register komplain gudang, dan ini yang wajib dibaca sebelum meniru salah satunya ke yang lain: di sini kepemilikan toko cuma IZIN MASUK, BUKAN cakupan baris.** `QualityComplaint` tak punya `shop_id` sama sekali dan `order_ref`-nya masih teks bebas, jadi penyempitan barisnya dikerjakan **`company_id` saja**. Konsekuensinya **siapa pun yang lolos gerbang baca melihat SELURUH komplain QC perusahaannya**, termasuk milik brand lain dan termasuk bila ia tak memegang satu toko pun. Di gudang tidak begitu: di sana `cakupanTokoKomplain` menyempitkan `shop_id` sehingga nol toko berarti nol baris ([[Microservices - Warehouse Service]]). Keputusan sadar, bukan kelalaian; begitu produk dan pesanan diisi server dari data pesanan, jalur ini wajib ditinjau ulang bersama penyempitan barisnya.
- ⚠️ Karena itu pula **TIDAK ADA memo `c.Locals` di sini**, tak seperti `cakupanKepemilikan` milik warehouse. Di sana resolver dibutuhkan dua kali (gerbang, lalu handler yang menyusun filter); di sini paling banyak **sekali** per permintaan, jadi memo hanya akan jadi mesin yang tak menggerakkan apa pun.
- **Tiga fungsi murni** menyertainya, dipisah karena seluruh pemanggilnya menyentuh Mongo sehingga aturannya tak akan pernah teruji bila tinggal di dalam handler: **`filterKomplainQC`** (`_id` + `company_id` + `status` opsional; `company_id` menahan tenant lain mengubah/memvonis/menghapus, `status` menutup race simpan-versus-validasi dan vonis ganda), **`filterUbahKomplainQC`** (di atasnya plus kunci optimistik `metadata.updated_at`, penutup lost-update dua PUT serentak), dan **`setUbahKomplainQC`** (daftar-putih field yang boleh disunting PUT; `company_id`, `status`, `verdict`, `reason`, `validated_by`, `validated_at`, dan `_id` tak pernah ikut di perintah tulisnya).
- **Resolver kepemilikan tokonya dipakai BERSAMA warehouse-service**: `common.TokoICCMilik` di `shared-library/common/toko_icc.go`, memanggil `GET /icc/mappings/me` milik [[Microservices - Integration Service]]. Di service ini ia dipasang sebagai `var ambilTokoICCQC` supaya bisa diganti di test. ⛔ **Menyentuhnya berarti menyentuh dua service**, jadi naikkan employee-service dan warehouse-service bersama; sisi gudangnya di [[Microservices - Warehouse Service]].
- **Tidak ada env baru**: `INTEGRATION_MODULE_URL` sudah terpasang di blok `employee-service` pada `docker-compose.yml` (dipasang lebih dulu untuk sumber KPI piutang dan rekonsiliasi aset GA). Env ini **tidak** masuk `InternalURL` yang divalidasi saat boot, jadi kosong tidak membuat service panik. ⚠️ **Tetapi di `docker-compose.dev.yml` env itu belum ada di blok employee-service** (per 2026-09-18; blok `warehouse-service` di berkas yang sama punya). Di lingkungan yang memakai compose dev, jalur kepemilikan karena itu gagal dengan `INTEGRATION_MODULE_URL kosong` → **500**, sementara jalur peran dan izin tetap jalan.
- **Indeks BARU `{company_id, status}`** untuk `quality_complaint`, dipasang saat boot lewat `ensureTenantIndexes` (`services/employee/company.go`, dipanggil dari `master_data.go`) dan **non-unik**: register ini tak punya kunci alami yang wajib tunggal (`order_ref` masih teks bebas), jadi unik justru akan menolak komplain kedua yang sah. Daftarnya memang dibaca dengan `{company_id[, status]}` persis dalam bentuk itu; tanpa indeks ia collection scan. Catatan di kode: diukur 2026-09-18 koleksinya masih nol dokumen di PROD maupun DEV, jadi memasangnya sekarang murah. ⚠️ Indeksnya **tidak menanam satu pun nilai status**, jadi perubahan kosakata di bawah tidak menyentuhnya.

**Komplain QC: kosakata nilai jadi KODE (T12)** — kode selesai **2026-09-21**, **belum merge**. Keputusannya [[ADR - 0103 Satu Pintu Komplain Produk, Unit Tujuan Diturunkan dari Kategori]] keputusan 12 dan 13.
- Status dan verdict jadi `menunggu_validasi`/`valid`/`ditolak`, tingkat keparahan jadi daftar tertutup `ringan`/`sedang`/`berat`. Sebelumnya kalimat Indonesia berkapital yang dirender mentah oleh frontend.
- **`severity` dicabut dari `medanUbahKomplainQC` DAN dikosongkan paksa saat POST.** Dua sisi, bukan satu: mencabutnya dari daftar putih PUT saja meninggalkan pengaju bisa menentukannya sekali di saat mengajukan, sehingga gerbang di rute sunting jadi penjaga pintu yang jendelanya terbuka. Rute `validate` satu-satunya penulisnya, menerimanya opsional dengan daftar tertutup; kosong berarti "tidak diubah" supaya klien lama tak diam-diam menghapus nilai QC lain.
- Blok paksa field-server POST diangkat jadi fungsi bernama **`paksaFieldServerKomplainQC`**. Alasannya bukan kerapian: seluruh pemanggilnya menyentuh Mongo, jadi sebelum diangkat menghapus salah satu barisnya tidak membuat satu pun test merah. Pola yang sama dengan ketiga fungsi murni di atas.
- **Lima field salinan ulasan** masuk `QualityComplaint` (`bukti_foto`, `ulasan_comment_id`, `ulasan_channel`, `ulasan_teks`, `ulasan_bintang`), bernama persis seperti register gudang. ⛔ **Tapi tidak dibersihkan di sini**, berbeda dari gudang yang menjepit bintang 1..5 dan mem-`TrimSpace` teksnya: handler POST mem-`BodyParser` langsung ke model, jadi kelimanya permukaan tulis klien apa adanya. Kelasnya sama dengan `company_id` di baris atas, hanya belum ditimpa. Kelimanya tak pernah masuk daftar putih PUT, jadi tak bisa disunting sesudah diajukan.
- ⛔ **Konsekuensi deploy: tak ada urutan yang aman untuk rute `validate`.** Kedua sisi memeriksa `verdict` ketat, jadi klien lama ke backend baru dan klien baru ke backend lama sama-sama dibalas 400. Ini pecah-kontrak, bukan penambahan field, sehingga konvensi "BE sebelum FE" tidak menolong. Tidak ada migrasi data; jumlah dokumen PROD diukur ulang tepat sebelum deploy, dan bila masih nol employee-service dan frontend dinaikkan berdekatan. Konsumen `shared-library/models/employee` hanya service ini, jadi tak ada service lain yang ikut naik. Tanpa env baru, tanpa kategori inbox baru.

**Procurement — Kontrak Vendor & Register Penghematan (⚠️ live di kode, runtime pending)**
- Register non-Accurate untuk posisi Procurement, di-host di service ini (`services/employee/{procurement_kontrak.go,procurement_saving.go}`, `RegisterProcurementRoutes`) — **terpisah** dari [[Microservices - Procurement Service]] yang berbasis Accurate.
- CRUD `/procurement/contracts` (kontrak vendor non-inventory, alert H-60) & `/procurement/savings` (cost saving = harga acuan − jadi) — gate `RequireProcurementStaff`/`RequireProcurementSupervisor`. FE lewat `/api/employee/procurement/*`.
- Model `ProcurementContract`/`ProcurementSaving`, collection `procurement_contract`/`procurement_saving`. Department `procurement` sudah ada di seed; role key `procurement`.

**Training Program (HRIS) — ⛔ SUDAH PINDAH ke [[Microservices - Learning Service]]**
- Sejak **LMS Fase 0** (2026-08-06, PR [#1020](https://github.com/bip-itteam-internal/bip-erp/pull/1020)), seluruh modul pelatihan pindah ke service `learning`: berkas `services/employee/training.go` beserta `shared-library/models/employee/training.go` **dihapus**, dan empat entri Training di struct `Collections` ikut dicabut. Data 4 koleksi sudah dipindah ke `learning_db` di dev maupun produksi.
- Alasan pindah: LMS menuntut kelas tatap muka dan belajar mandiri berbagi satu angka progres, sehingga keduanya harus tinggal di satu service ([[ADR - 0002 Database-per-Service]]).
- **Yang tersisa di service ini**: endpoint `GET /master/departments/:key` kini juga dipakai `learning` untuk memverifikasi `department_key` lewat panggilan internal. Endpoint itu menyaring `company_id`, sedangkan kueri langsung yang dulu dipakai tidak — penyempitan yang disengaja, dampaknya nol selama seluruh data Training milik `BIP`. Detail: [[Microservices - Learning Service]].
- ⚠️ **Di produksi rute lama `/training/*` masih terdaftar** karena `employee-service` sengaja tidak di-rebuild saat cut-over (menghindari ikut mendorong perubahan orang lain ke produksi). Koleksi Training lama juga masih ada di `employee_db` sebagai jalan pulang. Keduanya tidak dipakai siapa pun.

**CRUD Employee Data**
- CRUD personal data, personal-documents, work data, work-documents, schedule, dan system-auth
- Transaksi create/update employee (multi-collection, `RequireHRISStaff`) lengkap dengan existence/completeness checks
- `GET /birthdays?month=1-12` — karyawan **aktif** (`$lookup` `system_authentication` + `is_active=true`) yang ulang tahun per bulan (default bulan berjalan); `$lookup` `work_data` untuk posisi/departemen + umur saat ini, urut per tanggal; hanya field aman (tanpa NIK/KK/alamat). Filter bulan & umur dihitung di Go (`date_of_birth` bisa string ISO, bukan BSON Date) + konversi ke WIB (data disimpan midnight WIB sebagai UTC)

**Aggregation / HRIS**
- `GET /v2/internal/aggregate/employees` — join multi-collection + paginated
- `GET /v2/internal/aggregate/employees/summary`, `/it`
- `GET /internal/aggregate/employee/:id`
- `GET /internal/export/all`
- **Saringan direktori & export SATU SUMBER** (`employee_filter.go`, 🟡 **belum merge**, branch `feat/employee-export-filter`). `/v2/internal/aggregate/employees` dan `/internal/export/all` kini berangkat dari `kandidatKaryawan()` yang sama, jadi berkas unduhan berisi baris yang persis sama dengan tabel di layar. Sebelumnya export **tidak menyaring apa pun**: pipeline-nya hanya membatasi `work_data.company_id` dan tak pernah meng-`$lookup` `system_authentication`, sehingga ia bahkan tak punya bahan untuk mengetahui siapa yang aktif. Detail kontrak query: [[API - Employee Service]].
	- Filter **opsional, default tanpa saring**. Itu yang menjaga [[Microservices - Payroll Service]] — pemanggil `/internal/export/all` tanpa query sama sekali — tidak berubah perilakunya.
	- ⚠️ **Dua perubahan perilaku yang disengaja.** Galat Mongo saat menyusun kandidat kini **dipropagasi jadi 500**; sebelumnya ditelan lalu saringannya dilewati, sehingga kegagalan baca justru MELEBARKAN hasil — dan galat isolasi tenant membatalkan penyaringan perusahaan sepenuhnya, artinya direktori bisa membalas karyawan lintas-perusahaan. Kedua, `$match` kandidat dipasang **tanpa syarat**: kandidat kosong berarti nol baris, bukan "tanpa saringan".
	- ⛔ **`/v2/internal/aggregate/employees/summary` TIDAK ikut**: ia mematok `is_active: true` dan mengabaikan seluruh query, jadi tiga kartu ringkasan di layar tak bergerak saat filter berubah. Belum diputuskan apakah itu memang yang diinginkan. **TBD.**

**KPI**
- `GET /kpi`, `GET /kpi/dashboard`, `POST /kpi`
- Templates CRUD
- `GET /me/kpi-score` — mode grafik 12 bulan
- Departemen yang satu tim tampil sebagai satu kelompok, dari master data. Detail: [[HRIS - Key Performance Index]]

**Nilai otomatis metrik KPI** — ✅ **live di produksi**; mesinnya deploy 1 Agustus 2026 (PR #843/#857/#866) dan metrik otomatis pertama menyala 6 Agustus 2026. Catatan lama "branch `feat/kpi-auto-value`, belum merge & belum deploy" sudah tidak berlaku ([[ADR - 0032 Kepemilikan kpi_score dan Batas Pengumpul Metrik]], analisis: [[HRIS - Otomasi Skor KPI]])
- `KPIMetric` bertambah `key` (identitas stabil), `auto` (konfigurasi), serta `auto_value`/`auto_basis`/`auto_cakupan`/`auto_at` (hasil hitung). Semuanya `omitempty`, jadi 70 template & 406 skor lama terbaca apa adanya.
- **Kosakata sumber disamakan dengan frontend** (`otomatis`/`semi`/`manual` di `erp-frontend/src/features/finance/posisi/lib/status-sumber.ts`), bukan padanan baru. `SumberMetrik` **memaksa `manual` selama belum ada `auto_value`**, dan `semi` bila cakupan tim belum 100% — meniru `tampilanSumberKpi()` di FE supaya sumber angka tak bisa berbohong dari sisi mana pun.
- `GET /kpi/auto-values?employee_id=&period=&template_id=` — pratinjau usulan sistem, **read-only**, gerbang sama dengan `POST /kpi`. `POST /kpi` menstempel `auto_*` ke snapshot saat submit (dihitung di situ, karena `ReplaceOne` menimpa seluruh dokumen).
- Dua rumus: `rata_skor_tim` dan `persen_capai_target`, keduanya dibatasi 100 mengikuti pola `(realisasi/target)` di [[Microservices - Insentive Service]]. Cakupan `department` memakai `SupervisedDepartments` dari master data (HRGA menyatu karena master datanya bilang begitu); cakupan `team` memakai `work_data.supervisor_id`.
- **Hitungan yang gagal tidak diisi angka**: `auto_value` dibiarkan kosong dan alasannya ditulis ke `auto_basis`, sehingga sumbernya jatuh ke `manual`. Nilai 0 akan menekan skor penilai tanpa dasar dan tak terbedakan dari tim yang memang berskor 0. Karyawan yang dinilai juga dikeluarkan dari daftar timnya sendiri.
- ⚠️ **Target kini berlapis tiga, dan `TargetPeriode` sudah TIDAK ADA** (PR [#1051](https://github.com/bip-itteam-internal/bip-erp/pull/1051) merged 6 Agustus 2026, **belum di-deploy**). `KPIAutoConfig` bertambah **`TargetPerKaryawan map[string]float64`** berkunci `employee_id`, dan `TargetPeriode(cfg, periode)` **diganti** `TargetBerlaku(cfg, periode, employeeID)` di `shared-library/models/employee/kpi_reduksi.go`. Urutan menangnya dari yang paling khusus: **per karyawan → per periode → target umum**; yang tak terdaftar jatuh ke lapis berikutnya, **bukan nol**. Pemeriksaannya memakai bentuk dua-nilai (`t, ada :=`), bukan membandingkan hasil dengan nol, sebab **target 0 sah** untuk arah `turun` ("zero accident", "selisih 0%") dan memperlakukannya sebagai "tak diisi" akan diam-diam mengembalikan target umum yang bukan maksud pengisinya. `hitungDariCuplikan` di `kpi_team.go` karena itu menerima `employeeID`, dan seluruh pemanggilnya (`kpi_auto.go`) meneruskannya.
	- Alasannya bukan kenyamanan konfigurasi melainkan **ketimpangan nyata**: metrik nominal seperti profit tak dapat dinilai dengan satu garis, sebab Juli 2026 mencatat rentang **570×** antar toko ICC (Rp342.585.503 sampai **minus** Rp3.471.743). Target seragam meloloskan pemegang toko besar dan menggagalkan sisanya tanpa ada kaitannya dengan kinerja.
- ✅ **Target dari sumber** ([[ADR - 0079 Target Profit Satu Pintu di Insentif, KPI Membacanya]] T2, PR [#1775](https://github.com/bip-itteam-internal/bip-erp/pull/1775), **live di prod 2026-09-07 22:16 WIB**, terverifikasi dengan data sungguhan). `kpi_target_sumber.go` menampung registry `DaftarkanTargetSumber(sumber, tempat, metrik...)`, `targetUntukCuplikan` (satu-satunya tempat mesin memilih target: `Cuplikan.TargetSumber` untuk metrik terdaftar, `TargetBerlaku` untuk sisanya; terdaftar tanpa target = "belum dapat dihitung", bukan bawaan), dan `targetMetrik` (target yang tampil untuk `/kpi/auto-values`, `/kpi/score`, `/me/kpi-score`, dulu tiga pemanggil `TargetBerlaku` terpisah). Di shared-library: `Cuplikan.TargetSumber` dan `KPIMetric.AutoTarget` (`auto_target`, ikut snapshot beku; `BersihkanNilaiOtomatis` membuangnya saat simpan template). Pendaftar pertama `insentif_profit`/`profit` dari field `target` baris `/profit-dashboard`. Detail dan transisinya di [[HRIS - Otomasi Skor KPI]] §Target profit dibaca dari insentif. ⚠️ **T5 PR A (bip-erp [#1783](https://github.com/bip-itteam-internal/bip-erp/pull/1783), live prod 2026-09-08 11:29 WIB)**: `validasiAutoKonfig`/`validasiAutoTemplate` di berkas yang sama memilih validator per registry saat `POST /kpi/templates` dan pratinjau — metrik terdaftar divalidasi `ValidateKPIAutoConfigTargetDariSumber` (shared-library; target terisi ditolak) dan pada masa transisi targetnya **dibuang** (`BersihkanTargetOtomatis`) alih-alih ditolak, sampai layar Atur Target berhenti mengirimnya (PR B erp-frontend [#1484](https://github.com/bip-itteam-internal/erp-frontend/pull/1484)) dan penolakan 400 dinyalakan (PR C bip-erp [#1788](https://github.com/bip-itteam-internal/bip-erp/pull/1788), sekaligus mencabut rute `/kpi/target-marketing*`, `Collections.KPITargetMarketing`, dan `POST /kpi/auto-values/pratinjau`); ketiganya live prod 2026-09-08 11:29 WIB (biner terverifikasi: memuat `ValidateKPIAutoConfigTargetDariSumber`, tanpa `kpi_target_marketing.go`).
- Migrasi `migrateKPIMetricKeys()` mengisi `metrics[].key` idempoten saat boot (pola `migrateDepartmentPositionItems`); `label` tidak disentuh dan `kpi_score.template` **tidak** dimigrasi karena snapshot beku adalah inti desain penilaian.
- Index baru `kpi_score{employee_id, period}` di `ensureTenantIndexes` (koleksi itu di produksi sebelumnya hanya punya `_id_`).
- Tak menghasilkan apa pun sampai HR mengisi konfigurasi `auto` pada metrik lewat `POST /kpi/templates`; tidak ada nama posisi yang di-hardcode. **Konfigurasi pertama diisi 6 Agustus 2026** untuk tiga metrik Tech Development. Frontend sudah ada: modal Score KPI menampilkan usulannya (erp-frontend #831/#834, prod 6 Agustus 19:02 WIB), dan layar diagnostik menyusul (#843).

**Registry sumber data KPI** — ⚠️ merged ke `main` (`ad4e9e35` registry, `264cd45a` sumber Kaizen lewat PR [#1029](https://github.com/bip-itteam-internal/bip-erp/pull/1029)), **belum diverifikasi lewat gateway hidup**

- `DaftarkanSumber(<nama>, fn)` mendaftarkan pengambil angka dari service lain. Batasnya [[ADR - 0032 Kepemilikan kpi_score dan Batas Pengumpul Metrik]]: employee-service boleh **menarik** data, tapi tetap **pemilik tunggal** `kpi_score`. Sumber hanya melaporkan `Cuplikan` mentah; seluruh aturan penilaian (reduksi, arah target, pembatasan 100) tinggal di `shared-library` dan tak bisa dilewati sumber mana pun.
- ⚠️ **Galat bertipe "belum dapat dihitung", berlaku untuk sumber mana pun** (`kpi_galat_sumber.go`, bip-erp PR [#1866](https://github.com/bip-itteam-internal/bip-erp/pull/1866) merged 2026-09-14, **live di DEV**, dan ada di biner `Employee-Service` produksi 2026-09-15 (`grep -ac 'belum dapat dihitung: '` = 5, kontrol string karangan 0); uji gateway dev di butir Deploy `nilai_inspeksi_satgas`). Galat apa pun dari sumber semula ditulis ke `AutoBasis` berprefiks `gagal mengambil data: ` (`PrefiksGagalSumber`), dan prefiks itu dibaca `gagalKarenaSumber`: satu metrik yang cocok membuat `/kpi/auto-scores` mengosongkan skor otomatis **orangnya** (`rakitBarisOtomatis`), bukan cuma metrik itu, dan `adaSumberGagalTanpaNilai` memakai predikat yang sama untuk memutuskan apakah skor seseorang ditampilkan. Sumber yang tahu datanya **memang belum ada** (mis. temuan menunggu cek ulang sampai tanggal tertentu) tak punya cara menyebut alasannya selain `Cuplikan` kosong, yang jatuh ke kalimat umum "belum ada satu pun pengukuran pada periode ini". Kini sumber boleh mengembalikan `belumDapatDihitung(format, ...)`. `terapkanOtomatis` mengenalinya lewat `adalahBelumDapatDihitung` (`errors.As`, jadi boleh dibungkus `%w`) dan menulis `belum dapat dihitung: ` (`PrefiksBelumDapatDihitung`) beserta alasan persisnya; galat lain tetap `PrefiksGagalSumber`. Metrik itu jatuh ke `manual` tanpa menyembunyikan skor otomatis lain. Dikunci `kpi_auto_belum_dihitung_test.go`, termasuk galat yang dibungkus dan kontrol negatif bahwa galat biasa tetap terbaca sumber gagal. Pemakainya `nilai_inspeksi_satgas` (PR #1866) dan `pelatihan` (PR [#1895](https://github.com/bip-itteam-internal/bip-erp/pull/1895): tanpa peserta, tanpa kelas bertautan post-test, belum ada yang mengerjakan, dan responden evaluasi di bawah 3; Tahap 3b (merged 2026-09-17, bip-erp PR #1934) menambah "belum ada peserta dengan pre-test dan post-test lengkap"), lihat di bawah.
- ✅ **`uptime_sistem` kini punya dua metrik** (`uptime` dan `downtime`; PR [#1069](https://github.com/bip-itteam-internal/bip-erp/pull/1069) merged, prod di-deploy 7 Agustus 2026 dan metrik `Network` dipindah ke `downtime` — ⚠️ **dikembalikan lagi ke `uptime` arah naik target 90 pada 26 Agustus 2026 lewat layar Atur Target**, jejaknya di `kpi_template_audits`; lihat [[HRIS - Otomasi Skor KPI]]), dipilih lewat `KPIAutoConfig.Metrik` seperti `kinerja_tiket`. `downtime` ada karena `uptime` tak dapat membedakan apa pun: realisasinya 99,8–99,9 sementara rasio dibatasi 100, jadi metriknya bernilai penuh setiap bulan bahkan dengan target 99,5. Dipasangkan `arah: turun` dan target berupa **anggaran** downtime bulanan (0,5% untuk SLA 99,5%), angkanya baru bergerak: 1% jatuh ke 50, 2% ke 25. **Metrik kosong tetap berarti `uptime`** — konfigurasi produksi yang sedang hidup tak menyebut metrik, dan mewajibkannya akan mematikannya diam-diam. Keduanya membaca agregat yang sama sehingga tak ada panggilan HTTP tambahan.
- Sumber terdaftar saat ini: `uptime_sistem` (`kpi_sumber_uptime.go`), **dua sumber Kaizen** (`kaizen_ide_diajukan` dan `kaizen_ide_diterapkan`), `skor_tim` (membaca `kpi_score` sendiri), `kinerja_toko` (`kpi_sumber_kinerja_toko.go`, PR [#1042](https://github.com/bip-itteam-internal/bip-erp/pull/1042), menarik dari [[Microservices - Marketing Analytics Service]]), `kinerja_ar` (`kpi_sumber_ar.go`, merge ke `main` 11 Agustus 2026, commit `71db0de7`, menarik dari [[Microservices - Integration Service]]), `kinerja_ap` (`kpi_sumber_ap.go`, menarik dari [[Microservices - Procurement Service]]), ✅ **`varians_anggaran`** (`kpi_sumber_varians_anggaran.go`, di `main` sejak 13 Agustus 2026, menarik dari [[Microservices - Integration Service]]; tiga metrik `varians_absolut_persen` / `varians_persen` / `persen_terpakai`), ✅ **`kinerja_cost_control`** (`kpi_sumber_cost_control.go`, 14 Agustus 2026, menarik dari finance-service; metrik `rekomendasi_efisiensi`), ✅ **`forecast_kas`** (`kpi_sumber_forecast_kas.go`, 14 Agustus 2026, menarik dari [[Microservices - Integration Service]]; metrik `akurasi_forecast_kas`), ✅ **`admin_non_ops`** (`kpi_sumber_admin_nonops.go`, menarik dari [[Microservices - Integration Service]]; metrik `penurunan_yoy_persen`, + 🟡 `rasio_beban_non_ops_persen` di PR [#1548](https://github.com/bip-itteam-internal/bip-erp/pull/1548) **belum merge**), dan ✅ **`kinerja_sales_admin`** (`kpi_sumber_sales_admin.go:23`, PR [#1254](https://github.com/bip-itteam-internal/bip-erp/pull/1254); status "belum merge" sudah usang, kode ADA di `origin/main` per 2026-09-12 dan sudah terpasang di template prod aktif "KPI Sales Admin", menarik dari [[Microservices - Integration Service]]; metrik `penjualan_tuntas_cutoff_persen`), dan 🟡 **`kinerja_affiliate`** (`kpi_sumber_affiliate.go`, branch `feature/workspace-position` — **belum merge ke `main`/prod**, terverifikasi di DEV 2026-08-22, menarik dari [[Microservices - Marketing Analytics Service]]; metrik `conversion` / `gmv` / `komisi` / `affiliate_aktif`, atribusi per-individu lewat `icc_affiliate_accounts` — dipakai untuk ICC pemegang akun), dan 🟡 **`kinerja_affiliate_tim`** (`kpi_sumber_affiliate_tim.go`, branch `feature/workspace-position`; metrik `conversion` / `affiliate_aktif` **PER-TIM-CHANNEL** TikTok/Shopee via endpoint `/kpi/kinerja-affiliate-tim` — inilah yang dipakai template staf Affiliate Acquisition), dan ✅ **`kinerja_live`** (`kpi_sumber_live.go`, PR [#1479](https://github.com/bip-itteam-internal/bip-erp/pull/1479) & [#1486](https://github.com/bip-itteam-internal/bip-erp/pull/1486) merged 2026-08-27 — **deploy belum diverifikasi**; menarik dari [[Microservices - Marketing Analytics Service]]; empat metrik `conversion` / `conversion_rate` / `add_to_cart_rate` / `avg_viewing_duration` untuk template **Host Live**). Ini sumber KPI per-individu untuk staf **Affiliate Acquisition** (KPI = penilaian individu); scope bawaan `individu`, dan **nol order adalah nilai sah** — berbeda dari metrik rasio `kinerja_toko` yang menggalatkan penyebut nol.
- ✅ **`kinerja_tiket`** (`kpi_sumber_tiket.go`, PR [#1055](https://github.com/bip-itteam-internal/bip-erp/pull/1055) merged; `Employee-Service` + `Task-Management-Service` produksi di-recreate 6 Agustus 2026 ±19:00 WIB) menarik dari `GET /kpi/ticket` [[Microservices - Task Management Service]] lewat env `TASK_MANAGEMENT_MODULE_URL` + `TASK_MANAGEMENT_SERVICE_KEY`. Tiga metrik dari satu agregat, dipilih lewat `KPIAutoConfig.Metrik`: `ontime`, `csat`, `selesai_persen`.
	- ⚠️ **Metrik keempat `selesai_dinilai`** (bip-erp PR [#1962](https://github.com/bip-itteam-internal/bip-erp/pull/1962) + erp-frontend PR [#1638](https://github.com/bip-itteam-internal/erp-frontend/pull/1638), merged 2026-09-17, live di DEV dan terverifikasi lewat gateway 2026-09-17, belum deploy PROD). Untuk metrik kerusakan template `Building and Maintenance Staff` (GA): **perbaikan berhasil = tiket selesai yang sudah dinilai PENGAJU**, dibagi tiket ditugaskan. Membaca field baru `csat_bukan_penangan` dari [[API - Task Management Service]], jadi nilai yang diberikan penangan tiket itu sendiri tidak dihitung.
		- Cuplikan: `Nilai` = bintang pengaju **ditambah 0 untuk sisa tiket ditugaskan**, `Populasi` = tiket ditugaskan, formula baku `rasio_ambang`. Nol pengganti itu disengaja: Nilai kosong ditolak `hitungDariCuplikan` sebagai "belum ada pengukuran", sehingga tanpa nol orang yang tak menyelesaikan apa pun lolos ke isian manual alih-alih 0%.
		- **Penjaga konfigurasi** (`periksaKonfigMetrikTiket`): formula wajib `rasio_ambang` dan ambang = bintang minimal **1..5**. Ambang 0 meloloskan nol pengganti (semua orang 100%), rumus lain merata-ratakan bintang bercampur nol; keduanya tanpa galat bila tak dijaga. Penjaganya jalan saat MENGHITUNG, belum saat menyimpan template (ditunda, 2026-09-17). Konfigurasi yang salah baru terlihat sebagai `auto_basis` "gagal mengambil data: ambang metrik selesai_dinilai adalah bintang minimal 1 sampai 5, ..." di skor dan Otomasi KPI, karena `POST /kpi/auto-values/pratinjau` sudah dicabut (ADR 0079 §6).
		- Field absen (task-management versi lama) = galat biasa yang menyebut task-management, jadi **deploy task-management dulu**. Galat biasa digolongkan gagal sumber dan menyembunyikan skor otomatis total orang yang memakai metrik ini sampai task-management naik.
		- Tiket selesai yang belum dinilai siapa pun menurunkan cakupan (metrik `semi`) dengan catatan "N tiket selesai masih menunggu nilai pengaju", supaya penilai KPI tak menyimpan skor beku sebelum pengaju menilai. Rincian mendapat baris kelima "Dinilai pengaju selain penangan" hanya bila field terkirim.
		- **Terverifikasi DEV 2026-09-17 lewat gateway** (`GET /api/employee/kpi/auto-values` dengan template uji berstatus arsip yang dihapus sesudahnya; biner kedua service diukur baru lebih dulu): tiket yang dinilai penangannya sendiri memberi `csat_bukan_penangan: []` dan 0 dari 3 = 0%; satu nilai pengaju non-penangan dari 5 tiket selesai memberi 20% berstatus `semi` dengan catatan "4 tiket selesai masih menunggu nilai pengaju"; ambang 0 dibalas `auto_gagal_sumber: true` berpesan ambang; bulan tanpa tiket dibalas "belum dapat dihitung" dengan `auto_gagal_sumber: false`; kunci layanan salah dibalas 401.
	- ⚠️ **Bulan tanpa tiket** (PR yang sama, merged 2026-09-17, belum deploy PROD): `kinerja_tiket` dan `kinerja_tiket_divisi` kini mengembalikan `belumDapatDihitung`, bukan galat biasa. Sebelumnya galat biasa digolongkan gagal sumber dan mengosongkan **seluruh** skor otomatis orangnya, bukan cuma metrik tiket; untuk posisi yang hanya menerima 2-3 tiket sebulan, bulan tanpa tiket menyembunyikan skornya utuh. `kpi_auto_scores_test.go` sudah lama memakai fixture berprefiks belum-dapat-dihitung untuk kasus ini sementara sumbernya tak pernah sesuai.
	- **Penyebut tiap metrik sengaja berbeda, dan itu yang menentukan adil-tidaknya angka.** `ontime` memakai populasi tiket **terukur** (punya tenggat dan waktu selesai), bukan tiket selesai, sebab mengisi tenggat pekerjaan supervisor saat triase; cakupannya lalu ditimpa ke `terukur/selesai`. `csat` justru memakai cakupan **bawaan** dengan penyebut tiket selesai, sehingga 2 rating dari 6 tiket melaporkan 33% dan metriknya `semi` — rata-rata dari dua penilai memang bukan suara satu tim.
	- **`csat` tidak mengonversi skala.** KPI yang berbunyi "rating 10 dari 1-10" dinyatakan sebagai `target: 5` di konfigurasi, jadi tak ada konversi skala di kode mana pun.
	- Yang gagal dihitung selalu **galat berkalimat**, tak pernah nol: tak ada tiket ditugaskan, belum ada yang selesai, tak satu pun bertenggat, atau belum satu pun dinilai pemohonnya.
- ✅ **`kinerja_tiket_divisi`** (`kpi_sumber_tiket_divisi.go`, merged [#1427](https://github.com/bip-itteam-internal/bip-erp/pull/1427)+[#1428](https://github.com/bip-itteam-internal/bip-erp/pull/1428) 2026-08-25, **live di prod 2026-08-28** bersama task-management dan frontend; diverifikasi `grep -ac` pada `/service` dengan kontrol negatif → 0, lalu dipanggil sungguhan sampai `409`-nya terbaca) menarik dari `GET /kpi/space-group` untuk metrik KPI **tingkat tim**. Enam metrik = dua kelompok space × tiga ukuran: `support_selesai_persen`·`support_ontime`·`support_csat`·`development_selesai_persen`·`development_ontime`·`development_csat`.
	- ⛔ **Sengaja TIDAK memakai `idAnggotaCakupan`.** Cakupannya daftar **space**, bukan daftar orang, dan yang menentukannya data di [[Microservices - Task Management Service]] (`space.division` + `space.kpi_group`). Cakupan `team` akan membuang tiket yang belum ditugaskan kepada siapa pun dan tiket milik akun non-aktif — di produksi Juli 2026 masing-masing **14** dan **24** tiket di space pengembangan — sehingga angka Leader **membaik persis ketika timnya menelantarkan tiket**, tanpa galat dan tanpa gejala. `KPIAutoConfig.Scope` karena itu dinyatakan **tidak berpengaruh** di katalog, supaya form menyembunyikan pemilihnya alih-alih menampilkan isian yang diabaikan.
	- **Perhitungannya memakai ulang `katalogMetrikTiket`** milik `kinerja_tiket`, jadi aturan cakupan, penyebut, dan pesan galatnya hanya punya satu salinan; yang berbeda cuma dari mana muatannya diambil.
	- **Badan galat dari task-management diteruskan apa adanya**, tak seperti `kinerja_tiket` yang hanya menyebut kode status. Justru di sinilah pesannya menentukan: `409` berarti `space.kpi_group` belum diisi, dan yang membacanya di layar Otomasi KPI perlu tahu bahwa yang kurang ada di modul Manajemen Tugas, bukan bahwa timnya tak mengerjakan apa pun.
	- **Kosakata `support`/`development` hidup di tiga tempat** (task-management, employee-service, frontend) dan itu diterima sadar: nilai yang keliru dijawab `400` berisi daftar yang sah, jadi gagalnya **terbaca**, berbeda dari kategori inbox yang memang harus di `shared-library` karena gagalnya senyap.
- **`GET /kpi/score`** ✅ (`kpi_score_tersimpan.go`, PR yang sama, sudah di produksi) membaca skor yang **sudah tersimpan** satu orang satu periode. Ada karena `POST /kpi` menimpa dokumen skor dengan `ReplaceOne`, dan modal yang kolomnya terisi otomatis bisa mengganti penilaian lama tanpa disadari. Kontraknya di [[API - Employee Service]].
	- 🟡 **Template efektif untuk periode belum dinilai + karyawan mengunggah laporan sendiri** (bip-erp PR [#1406](https://github.com/bip-itteam-internal/bip-erp/pull/1406) + erp-frontend PR [#1195](https://github.com/bip-itteam-internal/erp-frontend/pull/1195), keduanya **belum merge**). Posisi accounting dinilai lewat laporan yang diunggah karyawan lalu dinilai 1–100 × bobot — mesin penilaian manual dan `kpi_evidence` (yang **sudah** mengizinkan unggah atas nama sendiri: `caller == employeeID` lolos tanpa RBAC, daftar dipersempit ke unggahan sendiri) sudah ada; yang hilang cuma pintunya di KPI Saya. Dua prasyarat backend: (1) pratinjau `/me/kpi-score` membawa `template_id` dan memilih template lewat `templateUntukPratinjau` (penetapan → riwayat, jatuh ke kandidat pertama menurut nama — bukan `FindOne` sembarang, yang untuk Junior Accountant CV/PT bisa menempelkan laporan ke template yang tak pernah dibuka penilai); (2) `/kpi/score` mengisi `template_id` **efektif** saat belum dinilai, karena daftar bukti disaring per `template_id` dan modal yang membuka template berbeda dari tempat karyawan mengunggah tak memperlihatkan satu pun file. Dengan itu **tiga tempat** memilih template dengan aturan yang sama: KPI Saya, kolom Otomasi, modal penilai. Keputusan: **menilai = menyetujui** — tak ada status approve per-file, sebab menyimpan skor sudah membekukan snapshot beserta `dinilai_oleh`/`dinilai_pada`. ⚠️ Celah yang **belum** ditangani: cakupan bukti di tabel Scoring (`metrics_with_evidence`) dihitung hanya untuk `scoredIDs` dari snapshot skor, jadi karyawan yang belum dinilai tak memberi sinyal apa pun di daftar walau sudah mengunggah — penilai baru melihatnya saat membuka modal. Layak PR tersendiri.
- 🟡 **`akurasi_aset_ga`** (`kpi_sumber_aset.go`, branch `feat/kpi-akurasi-aset`, **belum merge**) menilai **akurasi input aset staff GA** vs Accurate ([[ADR - 0037 Rekonsiliasi Aset GA dengan Accurate untuk KPI]] Fase 2). Memakai **scope baru `individu`** (menilai orang itu sendiri per `metadata.created_by`, bukan agregat tim/departemen — ditambahkan ke `KPIAutoScopes` di `shared-library`). Menarik `/items` + `/category-mapping` ([[Microservices - Inventory Service]]) + `/accounting/fixed-assets` ([[Microservices - Integration Service]]) — **endpoint yang sudah ada, kedua service itu tak diubah** — lalu menghitung skor per-item dengan **util murni** `shared-library/models/inventory` (`ComputeFieldScore`, aturan **ERP-only=100**). URL modul (`INVENTORY_MODULE_URL`/`INTEGRATION_MODULE_URL`) dibaca `os.Getenv` langsung, **bukan** `InternalURL` yang divalidasi boot (pola sama dengan Kaizen). Formula `rata_rata`, target 100, arah naik.
- ✅ **`kinerja_ar`** (`kpi_sumber_ar.go`, sudah di `main` sejak 11 Agustus 2026) menarik posisi piutang **HISTORIS per akhir bulan** dari [[Microservices - Integration Service]] (`GET /transactions/orders/piutang/tren`), bukan posisi *live* — bedanya menentukan: pipeline live menghitung umur relatif hari ini, sehingga tak dapat menilai bulan yang sudah berakhir. Katalognya memuat **empat** metrik (`kpi_sumber_ar.go:181-233`): `piutang_lewat_14_persen`, `piutang_lewat_60_persen`, `piutang_61_90_persen`, `piutang_lewat_90_persen`. ⚠️ **`piutang_61_90_persen` dan `piutang_lewat_90_persen` SALING LEPAS dan hanya bermakna dibaca berpasangan** (`kpi_sumber_ar.go:193,199`): keduanya sama-sama menghabiskan bucket `piutang_lewat_60_persen`, jadi memasang salah satu tanpa yang lain membuat piutang yang memburuk dari 70 hari ke 100 hari terbaca MENGECIL padahal makin parah (terukur prod 2026-09-01: 77% piutang macet justru yang paling tua, `>90` Rp7,14 jt berbanding `61-90` Rp2,16 jt). `piutang_lewat_90_persen` menggalatkan, bukan 0, bila integration-service belum mengirim posisi >90 hari, sebab cache Redis 10 menit `/piutang/tren` bisa menyajikan bentuk lama tepat sesudah deploy (`kpi_sumber_ar.go:219-232`). ✅ **`piutang_lewat_90_persen` sudah dikonfigurasi di template prod aktif "AR Staff 2026"** (bobot 0,50, target 5, arah turun; diukur prod 2026-09-12), lihat [[HRIS - Matriks KPI per Departemen]]. Metrik ini **departemen**, bukan per orang — seluruh toko dikelola bersama tim AR Sales, tak ada pembagian yang dapat diukur per karyawan. Total nol membuat rasio galat (bukan 0), sebab kedua metrik arahnya turun dan 0 berarti nilai sempurna. Menarik **sementara** langsung dari integration-service (bukan lewat fasad finance-service) karena jalurnya sudah terpasang dan terpakai oleh `akurasi_aset_ga`.
- ✅ **`varians_anggaran`** (`kpi_sumber_varians_anggaran.go`) menarik agregat varians anggaran vs realisasi OPEX dari [[Microservices - Integration Service]]. Dinamai menurut **DATA**-nya, bukan menurut posisi, karena ia dikonsumsi **empat posisi** sekaligus. Metrik `varians_absolut_persen` ada khusus untuk target DUA ARAH seperti "varians ≤ ±5%": mesin KPI hanya mengenal satu arah, jadi bentuk yang benar adalah selisih **mutlak** dengan arah makin-kecil-makin-baik. Memakai varians bertanda memberi nilai **sempurna** pada realisasi yang hanya separuh anggaran — penyimpangan 50% yang terbaca sebagai penghematan cemerlang.
	- 🟡 **Disempitkan ke departemen karyawan yang dinilai** (bip-erp PR [#1546](https://github.com/bip-itteam-internal/bip-erp/pull/1546), branch `feat/kpi-varians-departemen`, commit `e5cc5470`, **sudah di-review & disetujui, belum merge ke `main`**). Sebelumnya sumber ini memanggil `GET /accounting/anggaran/varians?tahun=&bulan=` **tanpa** parameter `departemen`, sehingga keempat posisi FAT dinilai atas angka OPEX **se-perusahaan** yang identik satu sama lain. Kini path memuat `&departemen=<url.QueryEscape(k.Karyawan.Department)>`.
	- **Alasan mengambil dari karyawan yang dinilai, bukan dari pemanggil**: penilaian KPI selalu tentang orang yang dinilai — pemanggilnya bisa HR, atasan, atau si karyawan sendiri, dan ketiganya harus menghasilkan angka yang sama. Ini sejajar dengan pola `kpi_sumber_cost_control.go:109-110` yang sudah benar sejak awal.
	- ⛔ **Departemen kosong menjadi GALAT sebelum request keluar**, bukan "semua departemen". Membiarkannya lolos akan diam-diam mengukur seluruh perusahaan lagi — persis bug yang sedang diperbaiki. Dikunci test yang membuktikan `dipanggil == false`.
	- ⚠️ **`varians_terdefinisi: false` tetap galat terbaca, bukan `Cuplikan` kosong atau 0.** Penting justru karena metrik ini **berarah turun**: pada arah turun 0 adalah nilai SEMPURNA, jadi menelan galat jadi 0 memberi skor penuh untuk periode yang anggarannya belum disinkron.
	- ⚠️ **Prasyarat data yang bisa membatalkannya**: `saringVarians` mencocokkan `anggaran_opex.departemen` dengan `work_data.department` sebagai **string persis**. Irisan kosong = metrik gagal hitung untuk setiap karyawan; itu pekerjaan data, bukan kode. **TBD** — irisan kedua daftar belum diperiksa ke produksi.
	- ⚠️ Sumber ini **tidak memanggil `DaftarkanScopeSumber`**, sebelum maupun sesudah PR ini. Penyempitannya **tanpa syarat** `Auto.Scope` (preseden yang sama dengan `kinerja_cost_control`), jadi `scope` di konfigurasi tetap tak berpengaruh. Lihat [[HRIS - Matriks KPI per Departemen]] §Finance Supervisor.
- ✅ **`kinerja_cost_control`** (`kpi_sumber_cost_control.go`) mencacah rekomendasi efisiensi dari **finance-service** (`GET /internal/kpi/cost-control`), lihat [[API - Finance Service]]. Dinamai menurut **POSISI** — kebalikan `varians_anggaran` — karena divisi FAT punya banyak posisi dengan metrik masing-masing dan sumber per-metrik akan melahirkan selusin sumber berisi satu angka. Didaftarkan **bermetrik sejak metrik pertamanya** supaya metrik berikutnya cukup menambah satu entri. ⚠️ Dokumen ini sempat memperkirakan Fase 1b (akurasi forecast kas) akan menumpang di sini; **tidak jadi** — ia mendapat sumbernya sendiri, lihat `forecast_kas` di bawah. ⚠️ Butuh env **`FINANCE_MODULE_URL` pada employee-service**; tanpanya metrik gagal hitung **dengan sebab yang terbaca** (bukan diam-diam nol) dan service tetap hidup — dikunci `TestFinanceModuleURLKosongMenghasilkanGalatTerbaca`.
- ✅ **`admin_non_ops`** (`kpi_sumber_admin_nonops.go`) menarik `GET /accounting/admin-nonops/kpi` dari [[Microservices - Integration Service]] — agregat tiga akun beban admin & non-operasional (`Beban Iuran & Sumbangan`, `Beban Bank`, `Beban Entertainment Adum`, daftar `DaftarAkunAdminNonOps`). **Sumber sendiri, bukan metrik tambahan di `varians_anggaran`/`forecast_kas`**, dengan alasan yang sama dengan keduanya: endpoint integration-service yang berbeda dan agregat baseline-vs-realisasi yang khas metrik ini. Env `INTEGRATION_MODULE_URL` dibaca `os.Getenv` langsung — env kosong menggalatkan **satu metrik** dengan pesan yang menyebut namanya, bukan memanikkan service.
	- ✅ **`penurunan_yoy_persen`** memasok metrik KPI **Cost Control #2** "Penurunan biaya admin/non operasional minimal 2% YoY" (bobot 10%). Membandingkan realisasi periode ini dengan **baseline bulan yang sama tahun sebelumnya × pengali 1,25** (kebijakan Finance). `penurunan_terdefinisi: false` menjadi **galat**, bukan 0, dengan **dua sebab dan dua pesan berbeda** (baseline belum tersalin vs realisasi belum ditarik) — kalimat yang benar untuk satu sebab menyesatkan untuk sebab lainnya.
	- 🟡 **`rasio_beban_non_ops_persen`** (bip-erp PR [#1548](https://github.com/bip-itteam-internal/bip-erp/pull/1548) + erp-frontend PR [#1332](https://github.com/bip-itteam-internal/erp-frontend/pull/1332), branch `feat/kpi-rasio-non-ops`, **belum merge**) memasok metrik **F3 SPV Finance** "Kontrol Beban Non-Operasional ≤ 2%" (bobot 20%). Formula `rata_rata`, target 2, **arah turun**, satuan persen.
	- **Kenapa metrik BARU di sumber yang SUDAH ADA, bukan sumber baru:** pembilangnya persis sama — tiga akun yang sudah ditarik `admin_non_ops`. Yang berbeda hanya penyebutnya. Sumber baru berarti menarik agregat yang sama dua kali dari endpoint yang sama.
	- ⛔ **Kenapa `penurunan_yoy_persen` BUKAN jawabannya**, meski hidup di sumber yang sama dan namanya sama-sama berbau "beban non-ops": keduanya **besaran yang berbeda**. Yang satu membandingkan realisasi dengan **baseline tahun lalu**; yang ini membandingkan realisasi dengan **pendapatan periode itu sendiri**. Menyamakan keduanya menjawab pertanyaan yang salah. Pertanyaan "≤ 2% dari APA" memang belum pernah dijawab sampai keputusan SPV 31 Agustus 2026: **penyebut = pendapatan periode**, dari baris top-total yang menyebut "pendapatan" pada `GET /accounting/profit-loss`.
	- ⚠️ **`rasio_terdefinisi: false` menjadi GALAT, bukan 0** — sama mengikatnya dengan `penurunan_yoy_persen`. Justru **lebih** genting di sini karena metrik ini berarah **turun**: pada arah turun 0 adalah nilai **terbaik**, jadi menelan galat jadi 0 memberi skor **sempurna** untuk periode yang laba-ruginya belum tersalin dari Accurate.
	- ⚠️ Sumber ini **tidak memanggil `DaftarkanScopeSumber`** dan tidak menyentuh `k.Karyawan` sama sekali — angkanya **se-perusahaan**, `scope` di konfigurasi tak berpengaruh. Konsekuensinya SPV Finance dan Cost Control dinilai atas angka yang identik untuk metrik dari sumber ini. Disengaja; lihat [[HRIS - Matriks KPI per Departemen]] §Finance Supervisor.
- ✅ **`forecast_kas`** (`kpi_sumber_forecast_kas.go`) menarik akurasi breakdown mingguan anggaran vs realisasi dari [[Microservices - Integration Service]] (`GET /accounting/anggaran/mingguan/kpi`), memasok metrik KPI Cost Control #4 **"Forecast cashflow mingguan dengan akurasi ≥ 95%"** (bobot 15%). **Sumber sendiri, bukan metrik keempat di `varians_anggaran`** meski keduanya membaca service yang sama: seluruh metrik di sumber itu lahir dari SATU muatan (`/anggaran/varians`) dan katalognya sengaja didaftarkan bersama peta penghitungnya supaya keduanya tak bisa menyimpang. Menyisipkan metrik yang memanggil endpoint lain mengubah satu peta menjadi dua jalur yang harus diingat orang berikutnya. Akurasi yang **tak terdefinisi** (anggaran periode itu belum diunggah) menjadi **galat**, bukan nilai nol — nol akan tercatat sebagai skor terburuk untuk periode yang datanya memang belum masuk. ⚠️ Butuh env **`INTEGRATION_MODULE_URL`**, yang **sudah ada** di compose dan sudah terisi di container produksi, jadi tak menuntut `--force-recreate`.
	- ✅ **Metrik ketiga `retur_lewat_14_persen`** ditambahkan 12 Agustus 2026, **live di produksi** untuk "Penanganan retur di platform atau Expedisi" (KPI AR Retur, bobot 0,3). Ia **bercabang ke sumber lain** — `GET /accurate/daily-returns/kpi/penanganan` — bukan posisi piutang, sehingga dispatch-nya dipisah sebelum lookup katalog fungsi.
	- **Jam mulai = `trans_date`, bukan saat barang sampai gudang.** Rancangan pertama memakai konfirmasi gudang sebagai jam mulai dan terbukti **tidak mengukur apa pun**: jeda scan-gudang→pembukuan dikerjakan worker otomatis dalam rata-rata **9,6 jam** dengan maksimum **5,4 hari**, sehingga ambang 14 hari tak pernah tersentuh dan nilainya 100 setiap bulan (Juli & Agustus 2026 sama-sama 100). Ini pola yang sama dengan metrik `uptime` yang dulu diganti `downtime`. Setelah jam mulai dipindah ke tanggal retur, angkanya bergerak: Juli **27,6%** (nilai 18,1), Agustus **33,1%** (nilai 15,1).
	- **Yang belum jatuh tempo bukan telat.** Retur yang belum selesai tetapi usianya belum 14 hari hanya menurunkan **cakupan** (`semi`), tidak dihitung telat — memvonisnya berarti menilai orang atas tenggat yang belum tiba. Batas tenggatnya dikirim pemanggil, bukan dihitung di pipeline, supaya deterministik.
	- **Penanda selesai = `last_status == "SENT"`, BUKAN keberadaan `last_sent_at`.** Stempel itu juga tercetak pada percobaan kirim yang gagal dan tertinggal pada retur yang sudah di-*unbook*; produksi memuat **21 `PENDING` + 1 `FAILED`** yang punya stempel itu, dan memakainya sebagai penanda membuat retur yang masih menggantung terhitung beres.
	- ✅ **Metrik keempat `retur_tuntas_cutoff_persen`** (ditambahkan 14 Agustus 2026, **live di produksi**) untuk "Pencatatan retur penjualan" (bobot 0,5). Metrik lama mengukur ketepatan waktu INPUT manusia; pencatatannya kini otomatis, sehingga yang tersisa diukur adalah **ketuntasannya pada tenggat tutup buku** — berapa persen retur periode itu terbukukan sampai tanggal 3 bulan berikutnya, persis bunyi KPI-nya. **Arahnya NAIK**, berlawanan dengan tiga metrik lain di sumber ini. Penyebutnya Total (bukan Dinilai) dan tak mengenal cakupan parsial SETELAH tenggatnya lewat; periode tanpa retur **digalatkan, bukan 0**, sebab pada metrik arah-naik 0 adalah nilai terburuk dan itu akan menghukum orang atas bulan yang kebetulan tanpa retur.
	- ✅ **Realtime + Rincian breakdown** (bip-erp PR [#1252](https://github.com/bip-itteam-internal/bip-erp/pull/1252), merged 17 Agustus 2026; erp-frontend PR [#1066](https://github.com/bip-itteam-internal/erp-frontend/pull/1066)). Gerbang penolakan **bulan berjalan** (400) pada `GET /accurate/daily-returns/kpi/penanganan` **dihapus** — terukur bahwa menahannya sampai bulan tutup tidak menstabilkan angkanya (Juli dibaca 1 Agustus memberi 9,6% telat, dibaca 15 Agustus memberi 34,7% — selisih 25 poin atas pekerjaan yang sama), jadi keadilannya sepenuhnya dipindah ke cakupan `semi`, bukan penolakan sepihak. `retur_tuntas_cutoff_persen` ikut dapat cakupan **proporsional** selama tenggat `batas_cutoff` periode itu SENDIRI belum lewat (`cakupanTuntasCutoffBerjalan`), supaya bulan berjalan tak terlihat 100% meyakinkan padahal belum pernah diuji tenggat sungguhannya. Ditambah `Cuplikan.Rincian []RincianBaris{label,nilai}` generik (`shared-library/models/employee/kpi_reduksi.go`) yang mengalir lewat `HasilOtomatis` → `KPIMetric.AutoRincian` → `auto_rincian`/`rincian` di `/kpi/auto-values` dan `/me/kpi-score` — breakdown terstruktur di balik kalimat `basis`, dipakai popup "Lihat rincian" di modal Score KPI dan laman KPI Saya erp-frontend. `Cuplikan.PopulasiNyata` (fix hasil `/review`) membedakan Populasi=1 company-wide (retur, uptime — jargon "(populasi 1)" disembunyikan) dari Populasi=1 tim `skor_tim` yang kebetulan beranggota satu orang sungguhan (tetap disebut, sebagai peringatan sampel kecil).
- ✅ **`kinerja_sales_admin`** (`kpi_sumber_sales_admin.go:23`, bip-erp PR [#1254](https://github.com/bip-itteam-internal/bip-erp/pull/1254); status "belum merge" sudah USANG, kode terverifikasi ADA di `origin/main` 2026-09-12). Sumber TERPISAH dari `kinerja_ar`, sebab AR Staf memegang **tiga** template KPI berbeda (Piutang, Retur, Sales Admin — bukan satu posisi satu template). Satu metrik: `penjualan_tuntas_cutoff_persen` untuk "Pencatatan Penjualan" (bobot 0,5), menarik dari `GET /accurate/daily-invoices/kpi/pencatatan` [[Microservices - Integration Service]]. Rumus dan arahnya identik dengan `retur_tuntas_cutoff_persen` (NAIK, penyebut Total tanpa cakupan parsial setelah tenggat lewat, periode kosong digalatkan bukan 0) — bedanya cuma satuan sumbernya, faktur harian Auto-Sync, bukan retur. **Lebih sederhana dari retur**: faktur tak punya masalah `trans_date`-ditimpa-scan-gudang, sebab `date_wib` adalah kunci dokumennya sendiri sejak dibuat, jadi seluruh perhitungan cukup satu pipeline Mongo di integration-service tanpa reduksi per-order di Go. `IMPORTED` dihitung tuntas, `VOIDED` dikecualikan dari penyebut. Reuse `cakupanTuntasCutoffBerjalan` dan `formatRibuan` dari `kpi_sumber_ar.go` (paket sama, `services/employee`). "Rekonsiliasi stok penjualan" (bobot 0,3) tetap manual, masih menunggu kesepakatan definisi selisih, keputusan HR sama seperti metrik retur. ✅ Konfigurasi `auto` metrik ini sudah terpasang di template prod aktif "KPI Sales Admin" (bobot 0,50, target 100, arah naik, scope department; upd 2026-08-20, diukur prod 2026-09-12), lihat [[HRIS - Matriks KPI per Departemen]]. Catatan lama yang menyebut pengisian ini masih tertunda sudah usang; apakah biner yang berjalan di container prod memuat kode sumber ini belum diukur terpisah (TBD).
- ⚠️ **`kinerja_ap`** (`kpi_sumber_ap.go:31,36`, PR [#1178](https://github.com/bip-itteam-internal/bip-erp/pull/1178); status "belum merge, belum deploy" sudah USANG, kode terverifikasi ADA di `origin/main` 2026-09-12) menarik tren ketepatan waktu pembayaran hutang dari [[Microservices - Procurement Service]] (`GET /faktur/pembayaran-tren`). Satu metrik: `pembayaran_tepat_waktu_persen` — persentase faktur jatuh tempo bulan tsb yang dibayar pada/sebelum `dueDate`, dihitung procurement-service dari data live `purchase-invoice/list.do` Accurate (cache 1 jam). `TepatWaktuPersen` nil (tak ada faktur yang bisa dinilai) diteruskan sebagai galat, bukan Cuplikan kosong. Env terpisah `PROCUREMENT_SERVICE_KEY` + `PROCUREMENT_MODULE_URL` (dua lapis gerbang di sisi procurement, pola sama `kinerja_tiket`/`uptime_sistem`). ⚠️ **Belum dikonfigurasi di template prod mana pun** (diukur prod 2026-09-12: tak ada template KPI Finance yang memuat sumber `kinerja_ap`); apakah biner prod yang berjalan sudah memuat kode ini juga belum diukur terpisah (TBD). Dasar keputusan & angka pengukuran lapangan: [[HRIS - Otomasi Skor KPI]].
- ✅ **`realisasi_ap`** (`kpi_sumber_realisasi_ap.go:24,28,30,37`, terverifikasi ADA di `origin/main` 2026-09-12) mengukur pekerjaan AP atas **PENGAJUAN INTERNAL** (procurement), BEDA dari `kinerja_ap` yang mengukur ketepatan waktu faktur VENDOR terhadap `dueDate`; dua populasi berbeda, dua pertanyaan berbeda. Menarik `GET /pengajuan/kpi-ap` milik [[Microservices - Procurement Service]] (`services/procurement/kpi_realisasi_ap.go:243`). Tiga metrik, masing-masing menjawab satu baris sheet KPI: `pembayaran_30menit_persen` (bobot 40), `ketepatan_nominal_persen` (bobot 50), `bukti_terunggah_persen` (bobot 10, "Membuat bukti transaksi untuk requester dan arsip"). Ambang 30 menit BUKAN angka sistem, melainkan keputusan sheet KPI, ditulis konstanta bernama `ambangMenitBawaan` (`services/procurement/kpi_realisasi_ap.go:120`). Pembilang bukti dihitung dari bukti transfer TERUNGGAH, bukan DISETUJUI: yang dinilai pekerjaan AP, bukan kecepatan senior accounting membuka layarnya (`services/procurement/kpi_realisasi_ap.go:32-36,142-144`). Penyebut nol (belum ada pengajuan yang melewati tahap bayar AP) jadi GALAT, bukan nol, lewat `persenDariPenyebut` (`kpi_sumber_realisasi_ap.go:100-106`). ⚠️ **Field `populasi_kosong` dikirim procurement tapi TIDAK dibaca sisi employee**: pembedaan "tanpa pekerjaan" dari "nol" sudah tercapai lewat galat penyebut-nol di atas, jadi field itu saat ini redundan pada jalur ini. ⚠️ **Belum dikonfigurasi di template prod mana pun** (diukur prod 2026-09-12). Lihat [[HRIS - Matriks KPI per Departemen]] baris Account Payable dan [[Finance - FAT Persona]].
- ⚠️ **`kinerja_tax`** (`kpi_sumber_tax.go:22`, terverifikasi ADA di `origin/main` 2026-09-12) dinamai menurut **POSISI** (Tax Officer), sejajar `kinerja_cost_control` dan `biaya_variabel_produksi` di bawah, sebab divisi FAT punya banyak posisi dengan metrik masing-masing. Dua metrik: `pelaporan_tepat_waktu` (bobot matriks 65) dan `dokumen_terarsip` (bobot 35); **bobotnya sendiri TIDAK ada di kode**, hidup di `KPIAutoConfig.Weight` yang diisi HR (`kpi_sumber_tax.go:19-21`). Menarik `GET /internal/kpi/tax` dari finance-service (`services/finance/routes.go:109`, handler `services/finance/pajak_kpi.go`), kontrak field `kewajiban_jatuh_tempo`/`kewajiban_tepat_waktu`/`kewajiban_arsip_lengkap`. Penyebutnya kewajiban pajak yang tenggat efektifnya jatuh pada periode itu; kewajiban yang **belum lapor tetap masuk penyebut**, dan ketepatan waktu dihitung **terpisah** dari kelengkapan arsip supaya satu kelalaian tidak dihukum dua kali (`services/finance/pajak_kpi.go:42-58`). Penyebut nol menghasilkan **Nilai kosong** (bukan 0, bukan 100) lewat `rasioKewajiban` (`kpi_sumber_tax.go:62-84`): 100 memberi nilai penuh untuk bulan tak dinilai, 0 menghukum atas ketiadaan pekerjaan. ⚠️ **Belum dikonfigurasi di template prod mana pun** (diukur prod 2026-09-12; template aktif "KPI Tax Officer" masih 8 metrik manual, lihat [[HRIS - Matriks KPI per Departemen]]), dan **sumber datanya kosong**: koleksi `pajak_kewajiban` di `finance_db` tercatat 0 dokumen (diukur prod 2026-09-12), sehingga metrik ini akan menggalatkan sekalipun sudah dikonfigurasi.
- ✅ **`ceklis_kpi`** (`kpi_sumber_ceklis.go:33`, terverifikasi ADA di `origin/main` 2026-09-12) menilai KPI **Senior Accounting** lewat daftar periksa sudah/belum, bukan angka dari transaksi: dinilai tanggal 1 sampai 5 oleh Internal Audit (laporan) dan SPV FAT (arsip). ⚠️ **Didaftarkan lewat `GrupSumberUmum`, bukan `GrupSumberFinance`** (`kpi_sumber_ceklis.go:286`): sumbernya sendiri lintas departemen, satu-satunya pemakai yang terverifikasi di prod hari ini adalah Finance. `KPIAutoConfig.Metrik` diisi **UID BUTIR** dari form-builder (bukan judul form atau urutan pertanyaan, keduanya bisa berubah tanpa satu pun galat), ditarik dari `GET /internal/kpi-checklist/metrics` lewat `FORM_BUILDER_MODULE_URL` (memo 30 detik). Empat keadaan hasil (`kpi_sumber_ceklis.go:178-283`): diceklis Sudah = 100, diceklis Belum = 0, belum diceklis dan tenggat belum lewat = **Nilai kosong** (menunggu penilaian, cakupan jatuh ke `semi`), belum diceklis dan tenggat sudah lewat (tanggal 5 **inklusif**, `kpi_sumber_ceklis.go:39`) = 0. `has_form:false` membedakan "belum dikonfigurasi" dari "gagal ambil"; hanya yang kedua jadi metrik gagal hitung. ✅ **Sudah terpasang di template prod aktif "KPI SENIOR ACCOUNTING UPDATE"** (3 metrik ber-auto, upd 2026-09-02; diukur prod 2026-09-12), lihat [[HRIS - Matriks KPI per Departemen]]. ⚠️ **Belum diverifikasi (TBD)**: dua metrik pertama template itu ("Menyusun laporan keuangan secara akurat" bobot 0,60 dan "Menyusun laporan keuangan tepat waktu" bobot 0,30) menunjuk **UID butir yang sama** (`6a98359398ef90bb78e9a366`, diukur prod 2026-09-12); apakah ini disengaja atau salah konfigurasi belum diperiksa.
- ⚠️ **`biaya_variabel_produksi`** (`kpi_sumber_biaya_variabel.go:19`, terverifikasi ADA di `origin/main` 2026-09-12) memasok baris **F3 "Kontrol ketat biaya produksi variabel"** (bobot 10) pada KPI **SPV Manufacture**, bukan posisi Finance mana pun. **Grupnya tetap `GrupSumberFinance`** (`kpi_sumber_biaya_variabel.go:132`) karena pengelompokan katalog mengikuti **ASAL DATA** (dicatat dan ditutup Finance), bukan siapa yang dinilai; pola yang sama dengan `varians_anggaran` yang dibaca empat posisi sekaligus (lihat di atas). Menarik `GET /internal/kpi/biaya-variabel` dari finance-service (`services/finance/routes.go:69`, handler `services/finance/biaya_variabel_handler.go`). Satu metrik `biaya_variabel_vs_tahun_lalu`: biaya periode ini sebagai **persentase** terhadap bulan yang **SAMA** tahun lalu (bukan bulan sebelumnya, sebab produksi bermusim), target 100 arah turun. Reduksi **`rata_rata`, BUKAN `rasio_ambang`**: Cuplikan-nya sudah membawa satu persentase jadi dengan populasi 1, dan `rasio_ambang` akan membuat biaya yang membengkak 130% dari tahun lalu berskor SEMPURNA (`kpi_sumber_biaya_variabel.go:115-131`). Cuplikan kosong (bukan 0, bukan galat keras) untuk dua keadaan berbeda: tahun lalu belum punya catatan pembanding, atau periode ini belum punya catatan (`kpi_sumber_biaya_variabel.go:58-67`). ⚠️ **Belum bisa menghasilkan angka apa pun**: koleksi `cost_biaya_variabel_produksi` di `finance_db` belum ada (diukur prod 2026-09-12). Lihat [[HRIS - Matriks KPI per Departemen]] dan [[Finance - FAT Persona]].
- ✅ **`kedisiplinan_absensi`** (`kpi_sumber_kedisiplinan.go`, PR [#1379](https://github.com/bip-itteam-internal/bip-erp/pull/1379) merged 22 Agustus 2026, **naik di prod dan dev hari itu juga dan diverifikasi lewat probe perilaku**) menarik `GET /kpi/attendance` dari [[Microservices - Attendance Service]] lewat env `ATTENDANCE_MODULE_URL` + **`ATTENDANCE_SERVICE_KEY`** (env baru; wajib terpasang di **dua** blok compose dengan nilai sama, satu mengirim satu memeriksa). Dua metrik dari satu agregat: `ketepatan_waktu` (persen hari kerja yang tercatat Tepat Waktu) dan `kelengkapan_catatan` (persen hari yang statusnya sudah diputuskan, penyebutnya hari-kerja + sisa `Pending`).
	- **Penyebut "hari kerja" DIPAKAI ULANG dari `statusTakDihitung` milik kartu Kehadiran**, bukan ditulis tandingannya. Angka yang menilai orang dan angka yang dilihat orang tidak boleh berangkat dari dua daftar terpisah. Yang berbeda hanya pembilangnya: kartu menghitung *Hadir* (tepat waktu + terlambat), metrik ini *Tepat Waktu* saja.
	- **Dua sub-metrik karena dua pertanyaan berbeda.** `Administrasi 1` menilai ketuntasan administrasi personalia, `Kedisiplinan` menilai perilaku karyawan. Keduanya berangkat dari agregat yang sama tetapi penyebutnya berbeda, dan dikunci test yang membuktikan keduanya menjawab angka **berbeda** untuk baris yang sama.
	- **Populasi = jumlah orang dalam cakupan, bukan yang punya angka.** Orang tanpa hari kerja tercatat keluar dari `Nilai` tetapi tetap di penyebut, supaya metrik yang berdiri di atas tiga orang dari tiga puluh terbaca setengah-terisi. Cakupan besar dipecah 100 id per panggilan; kegagalan satu batch menggagalkan seluruh metrik, sebab rata-rata dari sebagian cakupan adalah angka yang tampak wajar untuk pertanyaan yang tak ditanyakan siapa pun.
- ✅ **`pelatihan`** (`kpi_sumber_pelatihan.go`, bip-erp PR [#1895](https://github.com/bip-itteam-internal/bip-erp/pull/1895) merged 2026-09-15, **image produksi 2026-09-15 13:21 WIB**, diverifikasi dari dalam container; DEV belum dideploy per 2026-09-15, sedangkan per 2026-09-17 biner Employee-Service DEV sudah memuat sumber ini beserta dua metrik ADR 0102, lihat butir status di bawah) menarik `GET /kpi/pelatihan` dari [[Microservices - Learning Service]] lewat env `LEARNING_MODULE_URL` + **`LEARNING_SERVICE_KEY`** (env baru, bernilai sama di blok compose learning-service dan employee-service; keduanya dibaca `os.Getenv` langsung, bukan `InternalURL`). Grup `sdm`, cakupan baku `perusahaan` (juga `team` dan `department`, lewat `idAnggotaCakupan` yang mengeluarkan penilainya sendiri), formula `rata_rata` untuk **keenam** metriknya: empat sejak Tahap 3b (bip-erp PR #1934, merged 2026-09-17) ditambah `kesesuaian_materi_skala10` dan `kenaikan_kpi_peserta_persen` dari [[ADR - 0102 Kesesuaian Materi Dinilai Peserta dan Dampak Pelatihan dari Kenaikan Skor KPI]] (bip-erp PR [#1951](https://github.com/bip-itteam-internal/bip-erp/pull/1951), merged 2026-09-17 08:49 UTC, merge commit `c14f51b7`). Konstanta metrik di `kpi_sumber_pelatihan.go:56-63`, `DaftarkanFormulaSumber` di `:300-307` (baris diukur di `origin/main` 2026-09-17 sesudah merge itu; seluruh rujukan baris `kpi_sumber_pelatihan.go` di bab ini ikut diukur ulang saat itu):
	- `kehadiran_peserta_persen`: satu nilai 0 atau 100 **per pendaftaran** di kelas `Completed` yang tanggal selesainya jatuh di bulan periode (WIB). Rata-ratanya persis hadir dibagi terdaftar, jadi rincian `Kelas selesai` / `Pendaftaran peserta` / `Hadir` / `Tidak hadir` menjumlah ke realisasinya untuk cakupan apa pun. Populasi = pendaftaran, bukan seluruh karyawan cakupan: yang tak didaftarkan ke kelas mana pun tak punya kehadiran untuk diukur, dan menghitungnya membuat metrik ini `semi` selamanya.
	- `skor_post_test_persen`: skor terbaik (persen) tiap peserta kelas yang bertautan course, dari percobaan yang sudah dikirim dan tidak dibatalkan. Yang belum mengerjakan keluar dari `Nilai` tetapi tetap di populasi, dan baris rinciannya berfrasa `(tak ikut dihitung)`. ⚠️ Koreksi 2026-09-17: catatan lama "dipasangkan `rasio_ambang` ambang 70" keliru terhadap kode; rumus seluruh metrik sumber ini (kini enam) didaftarkan server sebagai `rata_rata` (`DaftarkanFormulaSumber`, `kpi_sumber_pelatihan.go:300-307`), dan layar Atur Target tidak menawarkan `rasio_ambang` untuknya (dicatat di [[HRIS - Matriks KPI per Departemen]]). Metrik "Skor Penilaian Training > 70" juga sudah didefinisikan ulang sebagai kenaikan skor KPI peserta ([[ADR - 0102 Kesesuaian Materi Dinilai Peserta dan Dampak Pelatihan dari Kenaikan Skor KPI]]).
	- ✅ `peningkatan_post_test_persen` (Tahap 3b, bip-erp PR [#1934](https://github.com/bip-itteam-internal/bip-erp/pull/1934) merged 2026-09-17; `cuplikanPeningkatanPostTest`, `kpi_sumber_pelatihan.go:438-499`): persen peserta wajib post-test yang skornya **NAIK** dari pre-test. Nilai 100 bila `naik_dari_pre` `true`, 0 bila `false` (`:462-467`), jadi rata-ratanya persis "berapa persen yang meningkat". Peserta ber-`naik_dari_pre` `null` (belum punya pre atau post) **keluar dari `Nilai` tetapi tetap di `Populasi`** (`Populasi` = seluruh peserta wajib, `:459-461,482-485`), dengan `Catatan` "x dari y peserta wajib post-test punya pembanding pre-test" bila tak lengkap (`:495-497`). Tanpa kelas bertautan materi, atau tanpa satu pun peserta yang lengkap pre dan post, menjadi `belumDapatDihitung`; pesan yang kedua sengaja menyebut **pre-test** sebagai penyebab paling sering (`:469-481`). Skor yang turun untuk semua orang tetap bernilai 0%, bukan galat, dan metrik ini tak membaca besar selisihnya. Dikunci `TestCuplikanPeningkatanPostTest`, `TestCuplikanPeningkatanSemuaTurunTetapNolPersen`, dan `TestCuplikanPeningkatanBelumDapatDihitung` (`kpi_sumber_pelatihan_test.go:333-420`). Label, keterangan, dan satuannya di frontend baru ada sejak erp-frontend PR [#1631](https://github.com/bip-itteam-internal/erp-frontend/pull/1631) (merged 2026-09-17), lihat [[REF - Penamaan Metrik & Sumber KPI]].
	- ⛔ **Aturan pemakaian dua metrik post-test (sebelumnya hanya tertulis di komentar Go, `kpi_sumber_pelatihan.go:43-50`).** `skor_post_test_persen` mengukur **PENGUASAAN akhir** (nilai absolut), `peningkatan_post_test_persen` mengukur **EFEKTIVITAS pelatihan** (berapa persen peserta yang benar-benar bertambah kemampuannya). Yang baru **tidak menggantikan** yang lama, dan arti metrik lama sengaja **tidak diubah** supaya template yang sudah berjalan tak berganti makna diam-diam. Keduanya bisa bergerak **berlawanan**: kelas berisi peserta yang sudah mahir menghasilkan skor tinggi dengan peningkatan rendah. HR memilih yang dipakai lewat template (keputusan pemilik proses 2026-09-16). **Jangan dijumlahkan** atau dirata-rata menjadi satu angka: yang satu rata-rata skor, yang lain persen orang, jadi gabungannya tak mengukur apa pun.
	- **Kontrak `naik_dari_pre` dengan learning: tiga keadaan.** `true` naik, `false` tidak naik, `null` belum bisa dinilai; cerminnya di sini pointer (`BarisPendaftaranPelatihanKPI`, `kpi_sumber_pelatihan.go:88-106`) dan **`null` tak boleh ditekan jadi `false`**, sebab orang yang belum mengerjakan ujiannya lalu terhitung gagal belajar. `TestKontrakMuatanPelatihanSamaDenganLearning` kini juga mengunci `skor_pre_persen` dan `naik_dari_pre`, termasuk bahwa baris `null` tetap `null` (`kpi_sumber_pelatihan_kontrak_test.go:47-64`). Aturan perhitungannya milik learning (`naikDariPrePersen`), lihat [[Microservices - Learning Service]].
		- ⚠️ Pengurai hanya menggalatkan kunci `data.pendaftaran` dan `data.evaluasi` yang hilang (`kpi_sumber_pelatihan.go:215-229`); kunci `naik_dari_pre` yang **absen** (learning versi lama tanpa field itu) terurai sebagai `null` yang sama. Jadi bila employee-service naik lebih dulu daripada learning, metrik ini berbunyi "belum ada peserta yang punya pre-test DAN post-test" untuk semua orang tanpa galat transport. Naikkan kedua container bersamaan.
		- ⚠️ **Rincian metrik ini bersarang tiga tingkat**, sedangkan `RincianBaris` cuma `{label, nilai}` (`shared-library/models/employee/kpi_reduksi.go:94-97`). Barisnya `Peserta wajib post-test` ⊃ `Dapat dinilai (pre dan post lengkap)` ⊃ `Skornya meningkat` / `Tidak meningkat`, ditambah `Pre atau post belum lengkap (tak ikut dihitung)` (`kpi_sumber_pelatihan.go:486-493`). Frontend (`klasifikasiRincian` di `erp-frontend` `src/features/hris/kpi/lib/rincian-baris.ts`) hanya mengenal tiga jenis rata (komponen, hasil `= `, pengecualian), sehingga keempat baris pertama dirender sejajar sebagai komponen dan hubungan himpunan-bagiannya hilang. Menunggu field `jenis` pada `RincianBaris`, keputusan yang sama dengan [[ADR - 0086 Metrik Live Lintas Channel Digabung Satu Angka, Rincian Tetap per Channel]] §7.
	- `kepuasan_trainer_skala10`: rata-rata empat aspek evaluasi (skala 1-5) dikali 2. Keputusan 2026-09-15: formulir tetap 1-5, konversinya di sumber. Di bawah **3 responden gabungan** menjadi `belumDapatDihitung`; agregat yang mustahil (di luar 4..20 per responden) menjadi galat biasa.
	- Tanpa peserta, tanpa kelas bertautan post-test, atau belum ada yang mengerjakan (untuk metrik peningkatan: belum ada yang lengkap pre dan post) menjadi `belumDapatDihitung` berkalimat, bukan 0. Cakupan dipecah 100 id per panggilan dan satu batch gagal menggagalkan metrik. Kunci `data.pendaftaran` atau `data.evaluasi` yang hilang di jawaban learning menjadi galat, bukan daftar kosong, dan galat transport dibersihkan dari URL ber-`?key=`.
	- **Ambang 3 responden adalah fakta milik learning** (`MinRespondenEvaluasi`). Sumber ini menerapkannya ulang atas gabungan batch, dan kesamaannya dikunci `TestAmbangRespondenSamaDenganLearning` yang membaca konstanta itu dari berkas learning. Kontrak muatan juga tidak disalin: `TestKontrakMuatanPelatihanSamaDenganLearning` mengurai literal `contohMuatanKPIPelatihan` dari test learning, dan test learning menjamin literal itu lahir dari handler sungguhan.
	- ✅ `kesesuaian_materi_skala10` (T3 [[ADR - 0102 Kesesuaian Materi Dinilai Peserta dan Dampak Pelatihan dari Kenaikan Skor KPI]], bip-erp PR [#1951](https://github.com/bip-itteam-internal/bip-erp/pull/1951) merged 2026-09-17; `cuplikanKesesuaianMateri`, `kpi_sumber_pelatihan.go:532-568`): rata-rata jawaban peserta atas pertanyaan "seberapa sesuai materi pelatihan ini dengan pekerjaan Anda", yaitu `jumlah_nilai / responden` dari objek `kesesuaian` kiriman learning, **skala 1-10 apa adanya, tanpa konversi** (`:557-559`). Beda dari kepuasan trainer yang dikali 2 dari skala 1-5.
		- Satu angka perusahaan: `Populasi` 1 **tanpa** `PopulasiNyata` (`:560-561`), sama dengan kepuasan trainer. `Catatan` "skala 1-10 dari jawaban peserta, terpisah dari kepuasan trainer" (`:562`), rincian `Responden kesesuaian materi` / `Rata-rata jawaban (skala 1-10)` (`:563-566`).
		- Di bawah **3 jawaban gabungan** (`minRespondenEvaluasiPelatihan`, ambang yang sama dengan evaluasi trainer tetapi dihitung atas `kesesuaian.responden`, bukan responden evaluasi) menjadi `belumDapatDihitung` yang menyebut periodenya (`:546-550`).
		- `kesesuaian` **nil** (learning versi lama, atau salah satu batch tak membawanya) menjadi **galat BIASA** yang menyebut learning-service, bukan `belumDapatDihitung` (`:539-545`): menyamarkannya sebagai "belum ada jawaban" membuat HR menagih peserta untuk masalah deploy.
		- Agregat di luar `1..10` per responden (`jumlah_nilai` di bawah `responden` atau di atas `responden × 10`) menjadi galat biasa, sebab datanya ada tetapi tak mungkin benar (`:551-556`).
		- Sengaja **tidak membaca `Evaluasi`** sama sekali. Dikunci `TestCuplikanKesesuaianMateri` (`kpi_sumber_pelatihan_test.go:463-531`), termasuk subtest kontrol hitung ganda: evaluasi trainer yang berbeda jauh tak menggeser angkanya sedikit pun.
	- ⛔ **`kesesuaian_materi_skala10` SEJAJAR dengan `kepuasan_trainer_skala10`, bukan bagiannya** (sebelumnya hanya tertulis di komentar Go, `kpi_sumber_pelatihan.go:35-38`). Kesesuaian menilai **MATERI**, kepuasan menilai **TRAINER**. **Jangan dijumlah, jangan dirata-rata bersama, dan jangan dibagi `× 4` bersama evaluasi trainer**: jawaban yang sama akan terhitung dua kali di template yang sama tanpa satu pun galat. Akhiran `_skala10` yang sama di kedua nama bukan izin untuk menggabungkannya; asal skalanya pun berbeda (satu pertanyaan 1..10 lawan empat aspek 1..5 dikali 2).
	- ✅ `kenaikan_kpi_peserta_persen` (T4 ADR 0102, PR #1951 yang sama; `cuplikanKenaikanKPIPeserta`, `kpi_sumber_pelatihan.go:633-724`): persen pendaftaran peserta **hadir** yang skor KPI-nya **naik** dari bulan sebelum kelas dimulai ke bulan sesudah kelas selesai.
		- **Jeda dua bulan.** Periode **P** menilai kelas yang selesai di **P-2**. Geserannya satu-satunya entri peta `jedaBahanPelatihan` (`:65-69`), dibaca closure sumber untuk menggeser periode bahan sebelum memanggil learning (`:334-341`); kalimat rincian tetap menyebut periode yang DINILAI (`:353-354`). Alasannya: skor bulan sesudah selesai (P-1) baru hampir pasti tersimpan saat P dinilai (`:31-33`). **Metrik lain tetap membaca periode P dan tak menyentuh `kpi_score`** (`:346-352`), dikunci subtest kontrol di `TestSumberPelatihanKenaikanMembacaKelasDuaBulanSebelumnya` (`kpi_sumber_pelatihan_test.go:761-826`).
		- **Pembanding**: skor `kpi_score` bulan sebelum bulan mulai kelas (geser -1) lawan bulan sesudah bulan selesai kelas (geser +1), dihitung di satu tempat, `bulanPembandingKenaikan` (`:584-594`), yang dipakai pembaca skor maupun penyusun nilai supaya keduanya tak bisa membandingkan bulan berbeda. Kelas multi-bulan karena itu berjarak lebih dari dua bulan. Bulan kelas (`bulan_mulai`/`bulan_selesai`) dikirim learning, lihat butir bahan kontrak di bawah.
		- **Hanya peserta HADIR, dihitung per PENDAFTARAN kelas** (keputusan pemilik proses 2026-09-17, `:633-642`). Kenaikan skor orang yang tak mengikuti pelatihan bukan dampaknya, dan kehadiran sudah diukur metriknya sendiri, jadi yang tak hadir tak masuk `Nilai` maupun `Populasi`. Orang yang ikut dua kelas dinilai dua kali, masing-masing dengan bulan kelasnya sendiri.
		- **Naik = 100, sama atau turun = 0, tanpa ambang** (`:670-675`).
		- Skor salah satu bulan tak ada, atau bulan kelas tak terbaca: **keluar dari `Nilai`, tetap di `Populasi`** (`Populasi` = pendaftaran hadir, `PopulasiNyata`, `:717-720`), pola yang sama dengan `peningkatan_post_test_persen`.
		- Tak ada peserta hadir, atau tak satu pun pendaftaran hadir punya pembanding lengkap, menjadi `belumDapatDihitung` yang menyebut bulan kelas (`:677-681,688-692`). **Seluruh** pendaftaran hadir tanpa bulan kelas menjadi **galat biasa** yang menyebut learning-service, karena hampir pasti learning versi lama (`:682-687`).
		- `Catatan` selalu menyebut bulan kelas yang dinilai, lalu kedua bulan pembanding bila hanya ada satu pasangan bulan (bila lebih, kalimat umum "tiap kelas"), ditambah "x dari y peserta hadir dapat dibandingkan" bila tak lengkap (`:694-702`). Rincian `Peserta hadir` / `Dapat dibandingkan (skor dua bulan ada)` / `Skor KPI naik` / `Tidak naik (sama atau turun)` / `Skor KPI bulan pembanding belum ada (tak ikut dihitung)`, ditambah `Bulan kelas tak terbaca (tak ikut dihitung)` hanya bila ada (`:704-716`). Barisnya bersarang seperti rincian peningkatan post-test, jadi catatan `RincianBaris` datar di atas berlaku juga di sini.
		- **Pembaca skor**: `ambilSkorPembandingPelatihan` (`:596-631`) lewat variabel `skorKPIPelatihan = skorPeriodeMap` (`:71-73`), pembaca yang sama dengan sumber `skor_tim` (`kpi_auto.go:363-386,432`), sehingga aturan "satu dokumen per (employee_id, period)" tak punya salinan kedua. Hanya pendaftaran hadir yang berbulan kelas yang dibaca skornya, dikelompokkan **satu kueri per periode**. Galat baca `kpi_score` diteruskan sebagai **galat biasa**, bukan `belumDapatDihitung` (`:347-351`): menelannya membuat semua peserta tampak tanpa skor dan HR menagih orang yang salah.
		- Dikunci `TestGeserPeriodeKPI`, `TestBulanPembandingKenaikan`, `TestCuplikanKenaikanKPIPeserta`, `TestCuplikanKenaikanKPIPesertaMenyebutBulanPembanding`, `TestCuplikanKenaikanKPIPesertaBelumDapatDihitung`, `TestAmbilSkorPembandingPelatihan`, dan `TestSumberPelatihanKenaikanMembacaKelasDuaBulanSebelumnya` (`kpi_sumber_pelatihan_test.go:543-826`).
	- ⚠️ **Aturan pemakaian `kenaikan_kpi_peserta_persen`.** (1) **Bukan bukti sebab-akibat**: ia mengukur apakah kinerja peserta bergerak naik sesudah pelatihan, bukan bahwa pelatihan penyebabnya (`kpi_sumber_pelatihan.go:32-33`). (2) Angka periode P adalah hasil kelas P-2; membacanya sebagai hasil pelatihan bulan P keliru, dan itu sebabnya `Catatan` selalu menyebut bulan kelasnya. (3) Yang dihitung pendaftaran HADIR, jadi angka ini tak ikut turun karena ketidakhadiran; ketidakhadiran dibaca dari `kehadiran_peserta_persen`. (4) Skor KPI antar bulan tidak selalu sebanding (template bisa berganti, banyak skor diketik manual) dan peserta tanpa skor keluar dari hitungan, sehingga populasi kecil bisa membuat angkanya melompat; keduanya diterima sadar di ADR 0102 bagian Consequences. ⚠️ **Gap terhadap ADR**: ADR itu menuntut kedua keterbatasan tersebut disebut di rincian metrik, tetapi `Catatan` dan `Rincian` di kode (`kpi_sumber_pelatihan.go:694-716`) tidak menyebut pergantian template maupun skor yang diketik manual.
	- **Status kedua metrik ADR 0102 per 2026-09-17**:
		- ✅ **Merged**: bip-erp PR [#1945](https://github.com/bip-itteam-internal/bip-erp/pull/1945) (T1-T2: jawaban `kesesuaian_materi` dan kontrak `/kpi/pelatihan`) 07:51 UTC, lalu PR [#1951](https://github.com/bip-itteam-internal/bip-erp/pull/1951) (T3-T4: kedua metrik) 08:49 UTC, merge commit `c14f51b7`.
		- ✅ **DEV, sekitar 15.50-16.05 WIB**: gerbang biner Employee-Service memuat kedua nama metrik (kontrol `gofiber` ada, string karangan 0), dan lewat gateway DEV `GET /api/employee/kpi/sumber-katalog` membalas sumber `pelatihan` dengan enam metrik di atas; tanpa token dibalas 401.
		- ⚠️ **Belum diverifikasi: perhitungan kedua metrik lewat template di DEV.** DEV tak punya template ber-blok `auto` untuk metrik ini, dan `POST /kpi/auto-values/pratinjau` sudah dicabut ([[ADR - 0079 Target Profit Satu Pintu di Insentif, KPI Membacanya]] §6), jadi perhitungannya baru terbukti unit test.
		- 🟡 **PROD belum di-deploy** (deploy prod dijalankan manusia).
		- **Urutan deploy**: `learning-service` dan `employee-service` **naik bersama**; erp-frontend **sesudah** learning, sebab learning lama mengabaikan `kesesuaian_materi` yang dikirim layar penilaian dan jawabannya hilang tanpa galat. Bila employee-service naik tanpa learning, `kesesuaian_materi_skala10` bergalat "naikkan learning-service" dan `kenaikan_kpi_peserta_persen` bergalat bulan kelas tak terbaca (bila ada peserta hadir), sementara empat metrik lama tetap jalan.
		- Frontend: label, keterangan dua locale, dan satuan ketiga metrik (`kesesuaian_materi_skala10`, `kenaikan_kpi_peserta_persen`, dan `peningkatan_post_test_persen` yang sebelumnya tanpa label) di erp-frontend PR [#1631](https://github.com/bip-itteam-internal/erp-frontend/pull/1631), merged 2026-09-17 09:19 UTC (diukur `gh pr view`); status deploy frontend belum diukur di dok ini. Checklist empat-tempatnya di [[REF - Penamaan Metrik & Sumber KPI]].
	- ✅ **Bahan kontrak dari learning untuk T3/T4 [[ADR - 0102 Kesesuaian Materi Dinilai Peserta dan Dampak Pelatihan dari Kenaikan Skor KPI]]** (bip-erp PR [#1945](https://github.com/bip-itteam-internal/bip-erp/pull/1945), **merged 2026-09-17** 07:51 UTC; baris di bawah diukur ulang di `origin/main`). Pengurai dan penggabung batch yang kini dibaca kedua metrik di atas.
		- Cermin `BarisPendaftaranPelatihanKPI` membaca `bulan_mulai` dan `bulan_selesai` (bulan WIB `YYYY-MM`, dihitung learning; `kpi_sumber_pelatihan.go:100-105`). `""` = tanggal kelas kosong atau learning versi lama.
		- `BahanPelatihanKPI.Kesesuaian` adalah **pointer** `{responden, jumlah_nilai}` (`kpi_sumber_pelatihan.go:132,221`). Kunci `kesesuaian` yang **absen** terurai `nil` = **tidak tersedia**, dan sengaja **bukan galat** seluruh sumber: menjadikannya galat memadamkan kehadiran, skor post-test, dan kepuasan yang sudah live hanya karena learning belum di-rebuild. Metrik kesesuaian sendiri yang menolak dengan pesan jelas (`kpi_sumber_pelatihan.go:539-545`). Nol responden dari learning baru tetap terbaca **tersedia berisi nol**.
		- Penggabungan batch **menjumlah** `responden` dan `jumlah_nilai`, dan hasilnya **tidak tersedia bila satu batch saja tak membawa kuncinya** (`kpi_sumber_pelatihan.go:238-269`): angka dari sebagian cakupan tampak wajar untuk pertanyaan yang tak ditanyakan siapa pun. Pendaftaran dan evaluasi tetap digabung penuh.
		- ⛔ **`kesesuaian` SEJAJAR dengan `evaluasi`, bukan bagiannya.** Jangan dijumlahkan ke `evaluasi.jumlah_nilai`, ke pembagi `× 4`, maupun ke `kepuasan_trainer_skala10`: jawaban yang sama akan terhitung dua kali di template yang sama, tanpa galat. Skalanya pun beda (1..10 satu pertanyaan lawan 1..5 empat aspek). Ambang 3 responden untuk metrik kesesuaian berlaku atas `kesesuaian.responden` gabungan, bukan atas responden evaluasi (`kpi_sumber_pelatihan.go:546`).
		- Dikunci `TestAmbilPelatihanBerbatchMenjumlahKesesuaian`, `TestAmbilPelatihanBerbatchKesesuaianTakLengkapJadiTakTersedia`, `TestAmbilPelatihanLewatHTTPKesesuaianAbsenBukanGalat`, dan pemeriksaan field baru di `TestKontrakMuatanPelatihanSamaDenganLearning`.
		- Urutan deploy T1-T2: bebas, karena kedua arah kompatibel mundur (learning lama → kunci absen terbaca tidak tersedia; employee lama mengabaikan kunci baru). Naik bersamaan baru wajib saat metrik T3/T4 dipasang, dan sejak PR #1951 merged 2026-09-17 keadaan itu **sudah berlaku**: kedua service wajib naik bersama (lihat butir status di atas).
	- ⚠️ **Belum dikonfigurasi di template mana pun** (template `People and Development` nol blok `auto`, diukur produksi 2026-09-15). Data produksi juga belum cukup: satu-satunya kelas yang berpeserta masih `Scheduled`, jadi periode Agustus menjawab `pendaftaran` kosong. Lihat [[HRIS - Matriks KPI per Departemen]] bab Training & Perfomance Officer.
- ✅ **`rencana_pelatihan`** (`kpi_sumber_rencana_pelatihan.go`, bip-erp PR [#1903](https://github.com/bip-itteam-internal/bip-erp/pull/1903) **merged 2026-09-16**, diukur `gh pr view` 2026-09-17; status deploy belum diukur ulang di dok ini) sumber **TERPISAH** dari `pelatihan` di atas, bukan metrik keempatnya (`kpi_sumber_rencana_pelatihan.go:22-25`): penyebutnya butir rencana **PERUSAHAAN**, bukan karyawan, sehingga satu-satunya cakupan yang bermakna adalah perusahaan — menaruhnya di dalam `pelatihan` akan menawarkan `team`/`department` untuk metrik yang mengabaikan keduanya. Melayani metrik "Terlaksananya kegiatan training & performance officer sesuai dengan rencana" (template `People and Development`, key `training-development`, `kpi_sumber_rencana_pelatihan.go:16-18`). Satu angka tanpa sub-metrik (`DaftarkanSumber`, bukan `DaftarkanSumberBermetrik`, `kpi_sumber_rencana_pelatihan.go:175`), grup `sdm` (`:172`), formula `rata_rata` (`:174`) — dikunci `TestKatalogSumberRencanaPelatihan` (`kpi_sumber_rencana_pelatihan_test.go:27-47`). Menarik `GET /kpi/rencana-pelatihan?periode=&company_id=&key=` dari [[Microservices - Learning Service]] (`services/learning/kpi_rencana_pelatihan.go:126`) lewat env **`LEARNING_MODULE_URL`** + **`LEARNING_SERVICE_KEY`** — sama persis dengan yang dipakai `pelatihan` di atas, bukan env baru (`kpi_sumber_rencana_pelatihan.go:55,59`), header `BIP-Gateway-ID` dari `INTERNAL_GATEWAY_KEY` (`kpi_sumber_rencana_pelatihan.go:72`).
	- ⚠️ **Cakupan selain `perusahaan` BUKAN ditolak saat template disimpan, melainkan diam-diam tak berpengaruh saat dihitung** — beda dari `kontrak_karyawan`/`turnover_karyawan` di bawah yang menggalatkan dirinya sendiri bila `Cfg.Scope != perusahaan`. Sumber ini mendaftarkan `perusahaan` sebagai satu-satunya cakupan (`DaftarkanScopeSumber(..., Perusahaan, Perusahaan)`, `kpi_sumber_rencana_pelatihan.go:171`) tapi TIDAK memanggil `DaftarkanScopePengaruh`, dan fungsi sumbernya (`kpi_sumber_rencana_pelatihan.go:175-181`) tidak membaca `k.Cfg.Scope` sama sekali — dikunci `ScopeBerpengaruh(SumberRencanaPelatihan) == false` (`kpi_sumber_rencana_pelatihan_test.go:44-46`). Validator umum `ValidateKPIAutoConfig` hanya menolak scope di luar enum global (`department`/`team`/`individu`/`tim_icc`/`perusahaan`), bukan scope yang tak didukung sumber tertentu (`shared-library/models/employee/kpi_source.go:251-256`), jadi template yang menyimpan scope lain untuk metrik ini akan tetap tersimpan dan tetap dihitung company-wide tanpa galat.
	- **Status pelaksanaan DIHITUNG learning-service, tidak disimpan** (`statusButirRencana`, `services/learning/models_rencana.go:144-172`) — fungsi yang sama dipakai layar Rencana Pelatihan, sehingga KPI dan layar itu tak mungkin berselisih. Kelas ditautkan lewat `Training.PlanItemID`, hanya kelas berstatus **Completed** yang diperiksa. Keputusan 2026-09-16: kelas Completed yang `EndDate`-nya jatuh **sebelum** awal bulan target sama sekali TIDAK menghitung butir itu (`models_rencana.go:155-158`); yang jatuh **di dalam** bulan target (awal WIB ≤ `EndDate` < awal bulan berikutnya) → **terlaksana** (`models_rencana.go:160-161`); yang jatuh **sesudah** bulan target → **terlaksana_terlambat** (`models_rencana.go:163,166-167`); tak ada kelas Completed yang lolos dan bulan target sudah lewat → **belum_terlaksana** (`models_rencana.go:165,168-169`); bulan belum lewat → **direncanakan** (`models_rencana.go:171`).
	- **Penyebut per bulan = terlaksana + terlaksana_terlambat + belum_terlaksana; butir DIBATALKAN keluar dari penyebut** (`cuplikanRencanaPelatihan`, `kpi_sumber_rencana_pelatihan.go:107-138`). Pembilang = jumlah butir **terlaksana** (nilai 100 per butir); terlambat dan belum bernilai 0 — rata-ratanya persis terlaksana ÷ direncanakan (`kpi_sumber_rencana_pelatihan.go:130,140-146`). Penyebut nol jadi `belumDapatDihitung`: seluruhnya dibatalkan → *"seluruh %d butir rencana pelatihan %s dibatalkan; tak ada butir yang bisa diukur"*, tak ada butir sama sekali → *"belum ada butir rencana pelatihan untuk %s; susun di menu Rencana Pelatihan"* (`kpi_sumber_rencana_pelatihan.go:131-137`). Status pelaksanaan yang tak dikenal dari learning menjadi galat BIASA, bukan `belumDapatDihitung`, supaya status baru yang belum dikenal di sini tak diam-diam menurunkan angka seseorang (`kpi_sumber_rencana_pelatihan.go:122-127`).
	- **`Rincian` selalu lima baris**: `Butir direncanakan` / `Terlaksana` / `Terlaksana sesudah bulannya` / `Belum terlaksana` / `Dibatalkan (tak ikut dihitung)` — HANYA baris `Dibatalkan` berpenanda `(tak ikut dihitung)`, sebab butir terlambat tetap di penyebut, bukan dikecualikan (`kpi_sumber_rencana_pelatihan.go:151-158`, dikunci `TestRincianRencanaHanyaDibatalkanYangBerpenandaPengecualian`, `kpi_sumber_rencana_pelatihan_test.go:92-108`). **`Catatan`** hanya terisi bila ada butir berstatus `direncanakan` (bulan berjalan): *"bulan berjalan: %d butir masih bisa terlaksana sampai akhir bulan"* (`kpi_sumber_rencana_pelatihan.go:160-162`).
	- ⚠️ **Belum di `main`, belum deploy, belum dikonfigurasi di template mana pun** — PR #1903 masih terbuka per 2026-09-16; seluruh perilaku di atas baru berlaku begitu kodenya merge dan container `Employee-Service` + `Learning-Service` naik bersamaan (pola sama `pelatihan`/`kedisiplinan_absensi`: dua sisi kunci layanan harus terpasang serentak). Lihat [[HRIS - Matriks KPI per Departemen]] bab Training & Perfomance Officer.
- ✅ **`turnover_karyawan`** (`kpi_sumber_turnover.go`, PR yang sama) memakai ulang `riwayatTurnover` — fungsi yang sama yang menggambar kartu Turnover di halaman Resign — dan hanya menambah pemuatan bahannya dari `companyID` alih-alih `*fiber.Ctx`. Satu metrik `turnover_persen`. **Cakupan selain `perusahaan` DITOLAK**, bukan diam-diam diperlakukan sebagai perusahaan: rekonstruksi headcount berjalan mundur dari keadaan sekarang dan hanya sah untuk perusahaan utuh, sebab perpindahan antar departemen tak tercatat sebagai keluar maupun masuk sehingga turnover per departemen akan menghitung mutasi sebagai pengunduran diri. Periode di luar `MaksBulanRiwayatTurnover` (6 bulan) juga ditolak. Bulan dicocokkan lewat **label**, bukan indeks. Headcount nol digalatkan, sebab `hitungTurnover` mengembalikan 0 di situ demi menghindari NaN dan nol itu menjadikan tenant baru tampak punya retensi sempurna.
- ⚠️ **`kontrak_karyawan`** (`kpi_sumber_kontrak.go`, PR yang sama) membaca koleksi **`employee_contract`**, BUKAN salinan `work_data.contract_ending`: salinan itu hanya menyimpan kontrak terakhir tanpa jejak, sehingga "sudah diperpanjang" tak dapat dibedakan dari "belum jatuh tempo". Dua metrik: `perpanjangan_tepat_waktu` (selisih hari antara berakhirnya kontrak dan saat penerusnya dicatat; positif = tepat waktu, dipasangkan `rasio_ambang`) dan `sisa_hari_kontrak` (keadaan tunggakan, ada karena bulan tanpa kontrak jatuh tempo membuat metrik proses tak bersampel sama sekali). **Menjawab SEBAGIAN `Administrasi 3`**: kontrak karyawan baru tak punya tenggat tercatat, jadi ketepatannya tak dapat dinilai tanpa mengarang tenggatnya. Kontrak yang karyawannya sudah tak aktif keluar dari penyebut; kontrak yatim (masih aktif, kontrak berakhir, tanpa pengganti) justru tetap dihitung gagal; kontrak `Migrated` keluar dari nilai tetapi tetap di populasi.
- ✅ **Cakupan `perusahaan`** ditambahkan ke `KPIAutoScopes` di PR yang sama, memakai jalur yang sama dengan `department` lewat helper `idAktifDariFilterWorkData` supaya aturan siapa-yang-dihitung tak punya dua salinan. Membatalkan butir "yang sengaja tidak dilakukan" di [[ADR - 0048 Skor KPI Otomatis Penuh Dibekukan Sistem]]. Dijaga test bahwa **setiap** cakupan di katalog lolos validator: katalog dirender lewat refleksi atas `KPIAutoScopes` sementara validator memakai daftar-izin yang ditulis tangan, jadi cakupan baru bisa tampil sebagai pilihan lalu ditolak saat disimpan.
- ⚠️ **`rekrutmen`** (`kpi_sumber_rekrutmen.go`, bip-erp PR [#1967](https://github.com/bip-itteam-internal/bip-erp/pull/1967) merged 2026-09-18, merge commit `df81baba`) — ✅ **live DEV 2026-09-18 dan PROD 2026-09-21**, env terisi dan terverifikasi lewat gerbang, TETAPI metriknya **belum dipasang di template mana pun**, jadi sumber ini belum menilai siapa pun. ⚠️ Diukur PROD 2026-09-21 sebelum dipasang: periode Agustus 3 requisisi / 3 slot dan September 8 requisisi / 8 slot, **nol** di antaranya punya kandidat Hired yang tertaut lewat lowongan, dan cakupan buffer **0%** (nol kandidat berstatus Buffer atas 6 rencana MPP, kritikal 0 dari 3). Artinya memasang metriknya hari ini menerbitkan **0%** di tempat lembar HRD menulis 83,3 dan 70 — selisih itu soal DATA yang belum terisi di modul Recruitment, bukan rumusnya, dan wajib dibicarakan dengan HRD lebih dulu. Melayani template `Recruitment` (Recruitment & Onboarding) dan metrik "Menjamin Ketersediaan Tenaga Kerja / 100% Pemenuhan Rekruitment < 30 hari" di `Supervisor HRGA`; sampai sumber ini hidup, kedua template mengetik angka pemenuhan yang sama secara terpisah (Agustus 2026: 83,3 di keduanya, `kpi_sumber_rekrutmen.go:22-31`). Grup **`sdm`**, formula **`rata_rata`** untuk **keenam** metriknya, dan — beda dari `rencana_pelatihan` di atas — ia **memanggil `DaftarkanScopePengaruh`** (`:126`). **Cakupan HANYA `perusahaan`**: cakupan lain digalatkan di dalam fungsi sumbernya (pola `kontrak_karyawan`/`turnover_karyawan`), sebab rekrutmen, buffer, review, dan retensi dinilai atas seluruh perusahaan dan data rekrutmen tak punya departemen perekrut untuk disaring (`:158-167`). **Target, arah, dan bobot tidak ada di sini** — tetap diisi HR lewat template. Dikunci `TestKatalogSumberRekrutmen` dan `TestSumberRekrutmenMenolakCakupanDanMetrikTakDikenal` (`kpi_sumber_rekrutmen_test.go:42-98`).
	- **Enam metrik** (`:34-55`), bersandar pada dua ambang yang hidup **di kode**: `tenggatPemenuhanHari` = **30** (batas "< 30 hari" pada bunyi metrik di KEDUA template; bagian dari definisi metrik, bukan target) dan `masaProbationBulan` = **3** (Peraturan Perusahaan tak mengaturnya; keputusan pemilik proses 2026-09-17, satu tempat).
		- `pemenuhan_tepat_waktu` — per **SLOT** requisition (`jumlah_dibutuhkan`, minimal 1) yang tenggatnya (tanggal persetujuan + 30 hari) jatuh di periode: 100 bila hire ke-*i* terjadi kurang dari 30 hari sesudah persetujuan, selain itu 0. **Slot yang belum terisi dihitung gagal**, supaya lowongan yang macet ikut menurunkan angkanya.
		- `pemenuhan_tepat_waktu_kritikal` — sama, hanya requisition yang posisinya **kritikal di MPP tahun persetujuannya**; penandanya datang dari recruitment-service, tidak dihitung ulang di sini.
		- `buffer_mpp_persen` — total kandidat **Buffer** ÷ total rencana MPP tahun periode, dihitung recruitment-service dengan rumus yang sama dengan layar MPP.
		- `buffer_mpp_kritikal_persen` — sama, posisi kritikal saja.
		- `review_evaluasi_tepat_waktu` — per kontrak **PKWT (Evaluasi)** yang berakhir di periode: 100 bila review onboarding karyawan itu sudah **DIPUTUSKAN** paling lambat tanggal akhir kontraknya (hari akhir kontrak sendiri masih boleh dipakai memutuskan). Keputusan dicari **di dalam masa kontrak yang sedang dinilai**, bukan yang terawal saja: tanpa batas bawah, review kontrak evaluasi SEBELUMNYA memenuhi kontrak perpanjangannya, dan kontrak kedua dapat 100 tanpa seorang pun menilainya (`:411-446`).
		- `retensi_masa_probation` — per karyawan yang pada suatu hari di periode masih dalam **3 bulan pertama** masa kerjanya: 0 bila ia resign **SUKARELA** di periode itu dan masih di dalam masa tersebut, selain itu 100. Target 100 setara "turnover masa probation 0%".
	- ⭐ **Yang DIKECUALIKAN ikut terbit di `Rincian`, dan itulah yang membuat angka rendah bisa dibaca**: `Slot belum jatuh tempo`, `Requisition tanpa jejak persetujuan`, `Hired tanpa lowongan di periode` (pemenuhan); `Posisi Buffer tanpa padanan MPP` (buffer); `Kontrak belum berakhir` dan `Karyawan keluar sebelum kontrak berakhir` (review evaluasi); `Tanggal masuk tak terbaca` (retensi) — seluruhnya berpenanda **`(tak ikut dihitung)`**. Orang yang sudah keluar tak lagi menuntut review, dan menghitungnya menghukum perekrut atas hal yang tak bisa dikerjakan (`:473-475`).
	- **Keadaan yang menghasilkan `belumDapatDihitung`, bukan galat** (metriknya jatuh ke `manual` tanpa mengosongkan skor otomatis orang itu, lihat butir galat sumber di atas): periode belum berjalan; belum ada slot requisition yang tenggat 30 harinya jatuh **dan sudah lewat** di periode; belum ada rencana MPP tahun itu; buffer diminta untuk periode selain **bulan berjalan atau bulan lalu** — status Buffer kandidat **tak punya riwayat**, yang terbaca selalu keadaan hari ini, jadi periode yang lebih lama tak bisa dihitung ulang tanpa angka hari ini menyamar sebagai angka bulan tersebut (`:279-296`); tak ada kontrak PKWT (Evaluasi) yang berakhir dan sudah lewat; tak ada karyawan dalam masa probation pada periode itu.
		- ⛔ **Gerbang periodenya dijalankan SEBELUM recruitment dipanggil** (`:169-186`, dikunci `TestSumberRekrutmenGerbangPeriodeSebelumMengambilData`): galat biasa mengosongkan SELURUH skor otomatis orang itu, jadi recruitment yang tak terjangkau tak boleh mengubah "belum dapat dihitung" menjadi galat.
	- **Memanggil [[Microservices - Recruitment Service]] lewat HTTP**: `GET /kpi/rekrutmen?periode=&company_id=` dan `GET /kpi/review-evaluasi?company_id=&employee_id=` (dipecah batch **100** id; batas di recruitment 200, dan satu batch gagal menggagalkan semuanya — hasil sebagian akan terbaca sebagai review yang tak pernah ada). Env **`RECRUITMENT_MODULE_URL`** + **`RECRUITMENT_SERVICE_KEY`**, klien ber-batas **15 detik** sendiri supaya recruitment yang tersendat tak menahan `GET /kpi`, header `BIP-Gateway-ID` dari `INTERNAL_GATEWAY_KEY` (recruitment memasang `ValidateGateway` untuk seluruh rutenya). Kontrak rutenya di [[API - Recruitment Service]].
		- ⚠️ **URL-nya SENGAJA dibaca `os.Getenv` langsung, bukan lewat `InternalURL`** yang divalidasi saat boot dengan panic (`:648-650`): pengumpul metrik KPI tidak boleh menjadi syarat hidupnya employee-service. Pola yang sama dengan Kaizen dan `akurasi_aset_ga`. Env kosong = **satu metrik** gagal hitung dengan nama env-nya disebut, bukan service mati.
		- **Memo hasil 30 detik per (periode, perusahaan)** (`:597-644`): EMPAT metrik (pemenuhan, pemenuhan kritikal, buffer, buffer kritikal) meminta muatan yang **identik**, jadi tanpa memo satu orang menerbitkan empat panggilan HTTP bermuatan sama — dan saat recruitment tersendat, empat kali batas 15 detik melewati batas gateway 30 detik sehingga SELURUH halaman KPI membalas **502**, bukan cuma metriknya yang gagal. TTL pendek supaya requisition yang baru disetujui cepat terlihat; **galat TIDAK di-memo**, dan pengambilannya dilakukan di LUAR mutex supaya penilaian lain tak antre di belakang satu permintaan yang mungkin timeout.
		- Kunci `data.requisisi` / `data.coverage` / `data.review` yang **hilang** menjadi galat, bukan daftar kosong (diurai lewat pointer, `:685-698`): kunci hilang yang terbaca sebagai kosong mendarat di "belum ada requisition" dan tampak persis seperti bulan tanpa rekrutmen. Galat penguraian URL sengaja **tidak memuat URL-nya** karena URL itu memuat kunci layanan, dan galat transport disaring `common.GalatTransportAman`.
		- **Test kontraknya membaca literal muatan APA ADANYA dari berkas test recruitment**, tidak menyalinnya (`kpi_sumber_rekrutmen_kontrak_test.go:19-30`, sumber `services/recruitment/kpi_rekrutmen_test.go`): field yang di-rename di recruitment membuat test di sana tetap hijau sementara employee-service membaca field itu sebagai nol, dan skor rekrutmen seseorang turun tanpa satu pun galat.
	- ✅ **`retensi_masa_probation` seluruhnya data LOKAL** (`work_data.join_date` + `employee_resign`), jadi ia tetap terhitung walau recruitment tak terjangkau — dikunci `TestSumberRekrutmenRetensiTakBergantungRecruitment`. Resignnya dibaca lewat `muatResignAppliedPerusahaan` yang kini **dipakai bersama** dengan `turnover_karyawan` (`kpi_sumber_turnover.go:109-115`), supaya "resign yang benar-benar terjadi" hanya punya satu kueri. `join_date` bertipe campur (BSON Date dan string) sehingga disaring di Go, bukan di kueri; yang tak terbaca masuk baris pengecualian, bukan diam-diam hilang.
	- ⚠️ **Env-nya wajib dipasang di DUA blok compose dengan nilai yang SAMA** — `employee-service` (`docker-compose.yml:292-293`, yang mengirim) dan `recruitment-service` (`:751`, yang memeriksa); kosong = kedua rute KPI recruitment menolak (gagal tertutup; kunci yang berbeda antar-blok terbaca sebagai **401**). Karena env dibaca saat container **dibuat**, kedua container wajib `--force-recreate`, bukan `restart`. Lihat [[RUN - Deploy Microservices bip-erp]].
- ✅ **`GET /kpi/auto-overview`** (`kpi_auto_overview.go`, PR [#1066](https://github.com/bip-itteam-internal/bip-erp/pull/1066) merged 6 Agustus 2026, prod di-deploy 7 Agustus 2026 bersama `API-Gateway` karena perubahannya menyentuh `noCacheRoutes`) menjawab pertanyaan yang tak bisa dijawab registry ini sendiri: dari seluruh metrik satu departemen, mana yang sudah dinyalakan, mana yang menghasilkan angka, dan mana yang macet beserta sebabnya. Lima status per metrik (`otomatis`/`semi`/`manual`/`belum`/`tanpa_karyawan`); dua terakhir khusus layar ini karena keadaan tingkat template dan tingkat departemen yang tak pernah dialami satu metrik satu orang.
	- **Memanggil `terapkanOtomatis` yang sudah dipakai `/kpi/auto-values`, bukan menyalin rumusnya** — layar diagnostik yang berhitung sendiri akan berbohong justru saat perbedaannya paling penting.
	- **Dibatasi 10 orang per metrik** (`maksEvaluasiOverview`, meniru `maksPratinjauKaryawan`): sumber ber-HTTP dipanggil berurutan per karyawan, dan timeout-nya menumpuk persis saat sumbernya mati — yaitu saat layar ini dibuka. `karyawan_aktif` tetap menyebut jumlah sesungguhnya.
	- **`department` opsional** (backend memilih yang pertama berhak) dan **badan 403 membawa `departemen_tersedia`**, supaya pemanggil yang tak berhak atas satu departemen bisa pindah alih-alih buntu. Rutenya dikeluarkan dari `noCacheRoutes` gateway karena cache 3 menit membuat tombol Segarkan berbohong. Kontrak: [[API - Employee Service]] · layar: [[APP - Web ERP]].
- ⚠️ **`GET /kpi/auto-scores`** (`kpi_auto_scores.go`) menghitung skor otomasi **per karyawan** satu departemen — Σ(bobot × usulan) — untuk kolom "Otomasi" di daftar KPI Scoring. Sudah **live di produksi tanpa pernah didokumentasikan**; catatan di bawah menggambarkan keadaan sesudah perbaikan PR [#1275](https://github.com/bip-itteam-internal/bip-erp/pull/1275) (belum merge).
	- 🔴 **Satu posisi TIDAK selalu satu template, dan versi pertama menganggapnya begitu.** Rute ini semula memetakan `department + position` ke SATU template dan menyimpan yang terakhir terbaca (`FindMany` **tanpa sort**, jadi urutannya tak dijamin). AR Staff Finance memegang **tiga** template — `KPI AR Piutang`, `KPI AR Retur`, `KPI Sales Admin` — sehingga seorang staf AR Retur bisa dihitung dengan metrik AR Piutang. Bukan angka basi melainkan angka dari template yang salah. `POST /kpi/templates` sendiri mewajibkan `name` justru untuk membedakan template seposisi, jadi keadaan ini kelas satu, bukan anomali.
	- **Template efektif diturunkan dari riwayat `kpi_score`** — template pada skor periode TERAKHIR karyawan itu, sah hanya bila masih termasuk kandidat posisinya sekarang. Aturan ini **tidak baru**: persis yang dipakai `employee_count` di `GET /kpi/templates`, dan memakainya ulang menjaga kedua layar menjawab "template siapa" dengan cara yang sama. ⚠️ Sejak PR [#1298](https://github.com/bip-itteam-internal/bip-erp/pull/1298) riwayat **turun jadi lapis KEDUA** — lihat bab penetapan template di bawah.
	- **Multi-template tanpa riwayat penentu = `template_ambigu`, TIDAK dihitung dengan tebakan.** Angka salah yang tampak wajar lebih berbahaya daripada kolom kosong beralasan — pola yang sama dengan prinsip "sistem tidak mengarang angka" di [[HRIS - Otomasi Skor KPI]]. Ambiguitas hanya dilaporkan bila posisi itu **memang punya otomasi**; tanpa penjaga itu `Beauty Hacks / Buzzer` (dua template: `BUZZER BHS` + template uji `Buzzer` berisi metrik `contoh`, keduanya tanpa `auto`) akan memunculkan peringatan yang tak dapat ditindaklanjuti penilai — mengubah cacat data master jadi teguran di layar.
	- Memanggil `metrikOtomatisUntuk` yang sama dengan `/kpi/auto-values`, bukan menyalin rumus. Dibatasi `maksKonkurensiOtomasi` = 8 panggilan serentak, sebab sebagian sumber memanggil service lain lewat HTTP dan satu departemen besar akan membanjiri service seberang persis saat layarnya dibuka.
	- Rutenya **dikeluarkan dari `noCacheRoutes`** oleh PR yang sama. Ia sempat luput saat rute ini ditambahkan padahal alasannya identik dengan tiga rute otomasi lain — bukti bahwa daftar itu perlu diperiksa tiap kali rute otomasi baru lahir.
- 🟡 **Penetapan template KPI — menghentikan tebakan** (`kpi_assignment.go`, `kpi_assignment_routes.go`, `kpi_assignment_migrate.go`; PR [#1298](https://github.com/bip-itteam-internal/bip-erp/pull/1298), **belum merge**). Menurunkan jawaban "template siapa" dari riwayat memang membetulkan kolom Otomasi, tetapi riwayat **menengok ke belakang** sehingga selalu telat satu periode: orang yang baru berpindah pekerjaan dihitung dengan metrik pekerjaan LAMANYA sampai penilaian pertamanya di posisi baru — dan angkanya terlihat meyakinkan tanpa ada yang menandainya. Akarnya, tak ada satu pun tempat yang mencatat "template KPI orang ini X".
	- **Resolusi jadi TIGA LAPIS**: penetapan eksplisit → riwayat `kpi_score` → ambigu. Penetapan yang menunjuk template **di luar kandidat posisi sekarang** diabaikan, bukan dipaksakan; karena sebuah template hanya milik satu (departemen, jabatan), penetapan basi mustahil terpakai — **netral secara konstruksi**. Itu pula sebabnya pelepasan penetapan saat mutasi sengaja TIDAK dikerjakan: yang tersisa hanya baris mati di koleksi, dan menyentuh jalur mutasi demi kerapian bukan tukar-tambah yang sepadan mengingat pelepasan `supervisor_id` di sana pernah terlewat satu arah dengan akibat senyap.
	- **Berperiode (`berlaku_mulai` `YYYY-MM`), bukan nilai terkini yang ditimpa.** Penilaian dikerjakan per bulan dan biasanya baru awal bulan berikutnya; menimpa saat diganti akan membuat pergantian pada September diam-diam mengubah hitungan Agustus yang belum sempat dinilai. Mengganti = **menambah baris**, riwayat tersimpan — pola pergantian leader `icc_leaders`, dan sekelas dengan `kpi_posisi_periode.go` yang sudah menyelesaikan persoalan kembar untuk *jabatan* per periode.
	- **Tak jadi beban pengisian**, lewat tiga jalur: backfill saat boot dari riwayat `kpi_score` (idempoten, pola `migrateKPIMetricKeys`) · penilaian pertama menyemai sendiri (`semaiPenetapanDariPenilaian`, best-effort, **tak pernah menimpa** yang sudah ada) · layar penetapan hanya untuk pengecualian. `berlaku_mulai` pada backfill memakai periode **tertua** tiap rentetan template yang sama, bukan terakhir — memakai yang terakhir membuat penilaian ULANG bulan lampau jatuh ke ambigu padahal jawabannya ada di riwayat itu sendiri.
	- ⚠️ Backfill memakai `InsertMany` **tak-berurut**: penjaga idempotensinya "koleksi masih kosong", jadi penyemaian yang berhenti di tengah karena satu bentrok akan DILEWATI selamanya pada boot berikutnya — separuh terisi tanpa gejala. Bentrok mungkin sebab index `{employee_id, period}` pada `kpi_score` **tidak unik**.
	- 🔴 **RBAC diperiksa per KARYAWAN pada jalur tulis** (`masalahOtorisasiPenetapan`), bukan sekali atas `department` yang dikirim pemanggil. Ditemukan lewat `/review`: versi pertama menggerbangi diri dengan nama departemen dari badan permintaan, sehingga supervisor satu departemen dapat menyertakan `employee_id` departemen lain dan mengubah dasar penilaian orang di luar wewenangnya. Larangan yang sama sudah tertulis pada `POST /kpi/auto-values/pratinjau` ([[ADR - 0031 Prefix internal Bukan Batas Keamanan]]). Keputusannya diangkat jadi fungsi murni penerima predikat supaya teruji tanpa Mongo maupun Fiber.
	- Kontrak: [[API - Employee Service]] · koleksi: [[DB - Overview and Notes]] · layar: [[APP - Web ERP]].
- ✅ **Sumber `insentif_profit`** (live prod 2026-08-26) — **DUA** metrik `profit` dan `retur_persen`, dibaca dari `GET /profit-dashboard` di [[Microservices - Insentive Service]]. KPI **berhenti menghitung profit sendiri**: rumus dan periodenya diatur SK Direktur 010/011 dan hidup di satu tempat. Scope `individu`→`icc`, `team`/`tim_icc`→`leader`, `department`→`supervisor`. Delapan template marketing memakainya. TARGET tetap dari template KPI, bukan dari insentif — metrik `pencapaian_target` sempat ada lalu dibuang karena melahirkan target kedua. 🟡 **Diputuskan dibalik 2026-09-07** ([[ADR - 0079 Target Profit Satu Pintu di Insentif, KPI Membacanya]], belum di kode): target metrik `profit` dibaca dari baris `/profit-dashboard` (konsep "target dari sumber"), template tak lagi menyimpannya, snapshot beku menyimpan target yang dipakai.
	- ✅ **`retur_persen` PINDAH ke sini dari `kinerja_toko`** (2026-08-27, PR [#1462](https://github.com/bip-itteam-internal/bip-erp/pull/1462); migrasi boot `migrateKPIReturKeInsentif` mengganti `auto.sumber` 8 template, **hanya sumber** — formula/target/arah/scope tak disentuh). Sebabnya: `kinerja_toko.retur_persen` membaca `|income.total_refund|` dari mart, dan di TikTok field itu **hanya terisi pada order CANCELLED** — yang justru dibuang saat mart dibangun. Akibatnya retur toko TikTok di KPI **selalu nol**: terukur prod 2026-08-26 tim Ade Rp35,4 juta / 370 order retur di insentif vs **Rp0** di KPI, skor retur 100/100 untuk tim ber-rasio 6%. Versi insentif membaca retur yang benar-benar dibukukan (`accurate_daily_returns` status SENT), per order, definisi yang sama dengan gerbang 7%. Sekaligus menyamakan POPULASI: scope `team` di `kinerja_toko` = bawahan langsung HRIS (yang dinilai sendiri **dikecualikan** padahal memegang toko terbesar; anggota tanpa mapping dilewati diam-diam), sedangkan `insentif_profit` memakai baris leader utuh.
	- **Retur belum terbukukan menurunkan CAKUPAN, bukan menggagalkan** — `retur_count / (retur_count + retur_gagal_booking)` → status `semi` + catatan. Dihitung dari ANGKA (`retur_gagal_booking`, dipaparkan insentive di PR #1462), bukan diurai dari kalimat peringatan. ⚠️ Sisa yang diketahui: baris ber-`retur_count` 0 tetapi `retur_gagal_booking` > 0 memberi rasio 0% dan skor 100 — terukur 3 orang di Agustus, ketiganya justru akan **gugur** bila returnya terbukukan. Belum diperbaiki (keputusan pemilik produk: ditunda).
	- 🟡 **`mode=bergeser`** (PR [#1503](https://github.com/bip-itteam-internal/bip-erp/pull/1503), belum merge) — adaptor ini SENGAJA meminta jendela periode yang berbeda dari insentif: order yang uangnya cair setelah cutoff ikut dihitung di periode berikutnya alih-alih hangus. Inilah **satu-satunya tempat** perbedaan itu lahir; hilangnya parameter itu membuat KPI diam-diam kembali ke aturan hangus tanpa galat apa pun. Lihat §Dua jendela periode di [[Microservices - Insentive Service]].

> ⚠️ **Gerbang peringatan `insentif_profit` mencocokkan PROSA, dan itu pernah mematikan seluruh KPI marketing.** `peringatanYangMenggagalkan` menyaring peringatan insentif lewat potongan kalimat dengan penjaga **fail-closed** — apa pun yang tak dikenal menggagalkan penilaian. Ketika insentive menambah kalimat baru "beban gaji tidak lengkap — dasar upah BPJS belum diisi…" (commit `2057201f`), kalimat itu muncul di SETIAP baris marketing dan mematikan **kedua** metrik untuk 8 template sekaligus (insiden 2026-08-27, hotfix PR [#1480](https://github.com/bip-itteam-internal/bip-erp/pull/1480)). Penjaga fail-closed-nya BENAR dan dipertahankan — ia yang membuat kegagalan terlihat alih-alih diam-diam memberi skor dari biaya yang dianggap nol. Yang belum tertutup: kontraknya masih kalimat bebas, jadi kalimat baru berikutnya akan mengulanginya. Perbaikan yang direncanakan: insentive mengirim `peringatan_rinci: [{kode, teks, dampak}]` dan employee membaca `dampak`, bukan menebak dari prosa.

- ✅ **Mode monitoring parsial** (`kumpulkanOtomatisParsial`, live prod 2026-08-26) — `GET /kpi?sertakan_otomasi=1&parsial=1` mengikutkan orang yang baru sebagian metriknya terukur, ditandai `parsial: true` + `metrik_terukur`. Tanpa ini layar bulan berjalan kosong total begitu satu sumber belum siap: terukur prod, 29 anggota Kyura dan **nol** yang tampil, padahal 14 dari 31 ICC ROAS-nya sudah terisi. ⚠️ `layakDifinalisasi` TIDAK ikut dilonggarkan — cron finalisasi tetap menuntut lengkap, karena membekukan skor setengah jadi mencatat angka yang lebih kecil dari kenyataan sebagai sejarah permanen.

⚠️ **`kinerja_toko` kini punya DELAPAN metrik**: `revenue`, `ads_cost`, `gross_profit`, `jumlah_video`, `roas`, `roas_bersih`, `retur_persen`, `profit_bersih` — dipilih lewat `KPIAutoConfig.Metrik`, katalognya `map[string]func(KinerjaTokoMart) (float64, error)` di `kpi_sumber_kinerja_toko.go`. `jumlah_video` **sengaja tidak menggalatkan nol**, berbeda dari metrik rasio di sebelahnya: seorang ICC memang dapat tidak menerbitkan video sebulan penuh, dan itu justru keadaan yang hendak diukur — menggalatkannya menyembunyikan kinerja terburuk sebagai "gagal hitung". Sebaliknya `roas`, `roas_bersih`, dan `retur_persen` **menggalatkan penyebut nol**, sebab angka apa pun yang dikarang di situ akan dibaca sebagai penilaian atas orangnya. Sumbernya mendaftar lewat **`DaftarkanSumberBermetrik`** (bukan `DaftarkanSumber`) sehingga daftar metriknya ikut masuk katalog yang dibaca frontend — tak ada daftar kedua yang bisa ketinggalan.
  - **Riwayat penamaan**: enam metrik pertama masuk lewat PR [#1049](https://github.com/bip-itteam-internal/bip-erp/pull/1049) (merged 6 Agustus 2026) dengan nama `roi`/`roi_bersih`. Sejak Agustus 2026 (branch `feature/workspace-position`, commit `619eeafd`) dinamai ulang **`roas`/`roas_bersih`** — rumus `revenue/ads_cost` memang ROAS, bukan ROI; nama lama tetap jalan lewat `aliasMetrikUsang`. Metrik kedelapan **`profit_bersih`** (`net_settlement − hpp − ads_cost − retur − opex`, commit `a51c5216`) **boleh bernilai negatif** — kerugian adalah keadaan yang hendak diukur, jadi tak dijepit ke nol. Kedua perubahan **selesai di dev, belum di prod**. Detail & sumber opex: [[HRIS - Otomasi Skor KPI]] bab "Perubahan Agustus 2026".
- ✅ **`kinerja_live` — KPI Host Live (`kpi_sumber_live.go`, merged 2026-08-27)**. Empat metrik dari [[Microservices - Marketing Analytics Service]] `/kpi/kinerja-live`. Bobot & target diisi HR di template; angka contoh yang diputuskan pemilik metrik: `conversion_rate` bobot 70 target 2%, `avg_viewing_duration` bobot 20 target 60 **detik**, `add_to_cart_rate` bobot 10 target 5%. ⚠️ **Yang terpasang di prod berbeda dari angka contoh itu** (diukur `employee_db` 2026-09-15): template `Host Live Kyura` dan `Host Live Beautyhacks` memakai bobot yang sama (0,7 · 0,1 · 0,2) tetapi target **7** untuk `conversion_rate` dan **7** untuk `add_to_cart_rate` (durasi tonton tetap 60), seluruhnya scope `individu`. Komentar di `kpi_sumber_live.go` dan `kpi_live.go` masih mencatat 2% dan 5% sebagai keputusan pemilik metrik. Target milik HR, jadi prod yang berlaku; apakah 7% untuk add to cart memang dimaksud **TBD**, karena deskripsinya identik dengan conversion rate. Rincian template dan keadaan skor: [[HRIS - Matriks KPI per Departemen]] bab Host Live.
	- **Reduksi BERBEDA per metrik**, dan bedanya menentukan benar-salahnya angka: `conversion` cacahan → `jumlah_nilai`; ketiga metrik rasio & durasi → `rata_rata`. Menjumlahkan persentase antar periode memberi 7%+8%=15% — bukan capaian apa pun, dan target akan selalu terlampaui hanya karena bulannya berjalan. Didaftarkan lewat peta `perMetrik` pada `DaftarkanFormulaSumber`, bukan satu formula untuk seluruh sumber.
	- **Klik nol MENGGALATKAN kedua rasio**, sementara `conversion` (cacahan) tetap boleh nol. Rasio tanpa penyebut tak terdefinisi, dan menyajikannya sebagai 0% terbaca "host gagal total mengonversi" padahal tak seorang pun mengklik produknya — pola sama dengan `roas` di `kinerja_toko`. Pembedanya field `ada_klik` dari service sumber.
	- ⚠️ **Scope `individu` terdaftar tetapi TIDAK dihormati sumbernya** — ia selalu menghitung per-departemen (departemen diambil dari `work_data` orang yang dinilai). Memilih "Individu" di template menghasilkan angka identik dengan "Departemen", sementara layar KPI menampilkan badge "Individu"; terlihat di produksi 2026-08-27 pada template *Host Live Kyura*. Scope itu didaftarkan sebagai persiapan penilaian per-orang lewat `/live-shifts`, yang **belum bisa** karena koleksinya masih 0 dokumen. **TBD**: tutup pilihannya atau buat sumbernya menghormati scope. ✅ **Ditutup 2026-09-09** (butir scope `individu` di bawah). ⚠️ Warisannya tersimpan permanen: skor Agustus 2026 dibekukan 1 September, sebelum penutupan itu, sehingga tiap host menerima angka tim departemennya (Kyura 7 host 94,18, Beauty Hacks 3 host 90,29). Lihat [[HRIS - Matriks KPI per Departemen]] bab Kyura › Host Live.
	- ⚠️ **`add_to_cart_rate` melaporkan 0% untuk keadaan yang belum terukur** — per 2026-08-27 nol dari 2.365 sesi Agustus punya field `add_to_cart` karena kode pengambilnya baru merge. Field-nya pointer (nil = belum diambil) tetapi pembedaan itu belum dipakai di sisi KPI. **TBD**: galatkan seperti klik nol. ✅ **Ditutup oleh bip-erp #1840 (di PROD sejak 2026-09-11)**: metrik ini kini menggalatkan saat `ada_klik_keranjang` false, yaitu tak ada klik dari sesi yang data keranjangnya terukur (butir rasio berpasangan di bawah).
	- **Departemen kosong di `work_data` menggalatkan**, bukan mengembalikan nol — nol terbaca sebagai "tak live sebulan penuh" dan menuduh orangnya atas data induk yang belum lengkap.
	- ✅ **Scope `individu` berjalan penuh sejak 2026-09-09**, termasuk ketiga metrik rasio ([[ADR - 0063 Siaran Serentak Dicatat sebagai Sesi Terpisah per Akun]] §8). Sebelumnya jalur individu mematok `ada_klik: false` sehingga `conversion_rate` dan `add_to_cart_rate` menggalatkan dan `avg_viewing_duration` selalu nol — dan karena ketiganya berbobot 70+10+20 di template Host Live, **100% bobotnya mustahil terisi**. Terukur prod 2026-09-09 sebelum perbaikan: ketiga rasio nol untuk seluruh 9 host.
	- **Tiga penjaga scope individu, semuanya menerbitkan galat berkalimat alih-alih nol** (`penjagaIndividu`): belum ada shift tercatat; shift ada tapi belum satu pun terjodohkan dengan siaran TikTok (sync harian belum menyusul atau `akun_live` salah ketik); dan penyebut rasio di bawah `minKlikRasioIndividu` = **100 klik**, ambang yang diturunkan dari ukuran prod ~123 klik per jam siaran sehingga kira-kira setara satu jam. Ambang itu **tidak** menyentuh `conversion` yang cacahan.
	- **Cakupan diisi khusus jalur individu** (`cakupanIndividu`) = shift terjodohkan ÷ total shift, sehingga metrik yang berdiri di atas sebagian shift berstatus `semi`. Tanpa itu cakupan bawaan selalu 1/1 karena sumbernya membawa satu angka realisasi. Jalur departemen sengaja `nil` — ia tak mengenal shift.
	- **Rincian jalur individu kini menampilkan bahan rasio** (klik, masuk keranjang, tayangan) yang sudah benar-benar dihitung, plus jam yang diberi label **jam dinding** untuk membedakannya dari penjumlahan per sesi.
	- ✅ **Rasio berpasangan** (bip-erp #1840, di PROD sejak 2026-09-11; employee-service naik 15:32 WIB, sebelum marketing-analytics 15:33, sesuai urutan). `KinerjaLiveData` menerima `orders_berklik`, `klik_keranjang`, `views_bertonton`, dan `ada_klik_keranjang` sebagai **pointer**: nil berarti marketing-analytics versi lama yang belum mengirimnya, dan jatuh ke bahan lama (`pembilangKonversi`, `penyebutKeranjang`, `penyebutTonton`, `adaKlikKeranjang`) supaya layar sama dengan sebelum perubahan, bukan menampilkan nol sebagai fakta. Ambang `minKlikRasioIndividu` kini membaca penyebut rasio yang sedang dinilai (`penyebutKlik`: klik berkeranjang untuk `add_to_cart_rate`). Rincian menampilkan pasangan pembentuk rasio dengan label `Konversi — pesanan dibayar (klik terukur)`, `Klik produk (keranjang terukur)`, dan `Tayangan (durasi tonton terukur)`. ⚠️ **Deploy employee-service lebih dulu** daripada marketing-analytics; alasannya di [[Microservices - Marketing Analytics Service]] § KPI Host Live.
	- ⚠️ **Pembulatan ke bawah per sesi menekan pembilang rasio di jalur individu** (belum diperbaiki, ditemukan 2026-09-15). `bahanTraffic.tambah` (`services/marketing-analytics/kpi_live.go`) mengalikan tiap cacahan sesi dengan porsi waktu irisan lalu memotongnya `int64(nilai × porsi)` **per sesi**, sebelum dijumlah. Klik yang berjumlah ribuan hampir tak terpengaruh, tetapi pesanan dan masuk keranjang per sesi kecil, sehingga tiap sesi terjodoh bisa kehilangan hampir satu cacahan (0 sampai kurang dari 1). Biasnya selalu ke bawah, jadi `conversion_rate` dan `add_to_cart_rate` per orang terbaca lebih rendah daripada rasio siaran yang ia bawakan. Terlihat jelas pada host bervolume kecil: satu host Beauty Hacks per 15 September 2026 membawa GMV porsi Rp704.929 dari 19 shift terjodoh, tetapi `conversion` (pesanan porsinya) **0**, begitu pula `orders_berklik`. Cacahan `conversion` terpotong dua kali: `RingkasShift` (`live_shift_penjualan.go`) memotong per sesi, lalu `kpi_live_individu.go` membagi bulat ke jumlah host. Jalur departemen (porsi 1) tidak terkena. Besarnya pada host bervolume tinggi **belum diukur**; karena conversion rate host Kyura berada di bawah target 7, bias ini langsung mengurangi skor. **TBD**: akumulasi sebagai pecahan, bulatkan sekali di akhir.
	- ⚠️ **Ambang `minKlikRasioIndividu` membuat host bervolume kecil tak pernah dibekukan otomatis.** Ambang 100 klik diturunkan dari akun brand bervolume tinggi. Pada host yang klik porsinya di bawah itu, kedua rasio "belum dapat dihitung" sementara durasi tonton tetap terhitung, dan karena `layakDifinalisasi` menuntut seluruh metrik punya angka, orangnya jatuh ke penilaian manual. Terukur per 15 September 2026: dua host Beauty Hacks yang shift-nya terjodoh hanya membawa 34 dan 30 klik, dan seluruh siaran host departemen itu baru 262 klik. Rincian per host: [[HRIS - Matriks KPI per Departemen]] bab Beauty Hacks › Host Live. **TBD** pemilik metrik: ambang per departemen, atau menerima penilaian manual.
	- ⛔ **Scope `department` tidak menghasilkan angka tim** (ditemukan 2026-09-15). Sumber ini selalu mengirim `employee_id` ke `/kpi/kinerja-live` (`ambilKinerjaLiveMart(k.Periode, department, k.Karyawan.EmployeeID)`, satu-satunya pemanggil), sementara marketing-analytics memilih jalur per orang semata karena `employee_id` terisi, tanpa membaca scope (`HandlerKPIKinerjaLive`, `kpi_live.go`). Dengan kode `origin/main` sekarang, jalur departemen marketing-analytics tak pernah dicapai sumber KPI. Memilih `department` di template tetap memberi angka per orang, dan yang berubah justru merugikan: `penjagaIndividu` dan `cakupanIndividu` hanya aktif pada `individu`, sehingga host tanpa shift jatuh ke galat "tak ada klik produk", rasio dari segelintir klik ikut dinilai, cakupan selalu terbaca penuh, dan rincian berlabel "total tim" untuk angka milik satu orang. Komentar `DaftarkanScopePengaruh` di berkas yang sama masih menyatakan `department` menghitung total tim, jadi komentar dan perilakunya berselisih, dan test sumber ini memalsukan `ambilKinerjaLiveMart` sehingga tak pernah melewati percabangan itu. Skor Agustus 2026 yang identik per departemen dibekukan 2026-09-01, sebelum berkas jalur per orang (`kpi_live_individu.go`) lahir 2026-09-02, jadi bukan bukti scope `department` bekerja. **TBD**: kirim `employee_id` hanya saat scope `individu`, atau cabut `department` dari scope yang terdaftar. Dampak ke keputusan KPI September: [[HRIS - Matriks KPI per Departemen]] bab Kyura › Host Live, temuan 6.
	- ⚠️ **Penolakan penjaga tercatat sebagai kegagalan sumber, bukan "belum dapat dihitung"** (ditemukan 2026-09-15). `penjagaIndividu` mengembalikan `fmt.Errorf` biasa, bukan galat bertipe belum-dapat-dihitung, sehingga `terapkanOtomatis` memberinya prefiks `gagal mengambil data:` ([[HRIS - Otomasi Skor KPI]] § Tiga sebab kegagalan, tiga tanggapan berbeda). Terlihat di `GET /kpi/auto-scores` prod per 15 September: "belum ada shift live tercatat", "klik produk pada siaran orang ini baru 34 (ambang 100)", dan "2 shift tercatat tapi belum ada yang terjodohkan" seluruhnya tampil sebagai `gagal_sumber`. Akibatnya `auto_score` dua host Beauty Hacks yang durasi tontonnya sudah terhitung (1 dari 3 metrik) sengaja dikosongkan, dan layar terbaca seperti gangguan sistem padahal marketing-analytics sehat. **TBD**: bungkus ketiga galat penjaga sebagai belum-dapat-dihitung.
	- ⚠️ **Host Shopee belum punya sumber otomatis, dan skornya akan kosong** — HR perlu tahu sejak awal, bukan menemukannya saat menilai. ⛔ **Dok ini sebelumnya menyebutnya "permanen"; itu KELIRU (dikoreksi 2026-09-10).** Koneksinya kini **sudah berdiri**: 2 toko terotorisasi ke app Livestream, dan panggilan `livestream.get_session_metric` sudah lolos otentikasi (lihat § koreksi di [[Microservices - Marketing Analytics Service]]). Yang **tetap** menghalangi cuma satu, tetapi menentukan: **tak ada cara mengenumerasi sesi milik sendiri** — endpoint hanya menjawab bila `session_id`-nya disebutkan, dan modul itu tak punya daftar sesi. Ditambah `orders` Shopee memuat order belum-bayar serta batal sehingga tak sebanding dengan `sku_orders` TikTok yang dipakai metrik ini. Jadi kosongnya masih benar hari ini — alasannya saja yang berbeda, dan berbeda itu menentukan apakah orang berikutnya berhenti bertanya atau tidak.
- ⚠️ **`kesiapan_live`: KPI Live Support (`kpi_sumber_kesiapan_live.go`, ✅ PROD 2026-09-17)**, bip-erp #1916 + #1927 + #1932, image `employee-service` 2026-09-17 08:12:44 WIB. Menarik `GET /kpi/kesiapan-live` dari [[Microservices - Marketing Analytics Service]] berkunci `MARKETING_ANALYTICS_SERVICE_KEY` (tak ada env baru). Keputusannya [[ADR - 0101 Kesiapan Siaran Dicatat Host saat Mulai sebagai Dasar KPI Live Support]]. ⚠️ Catatan statusnya: **belum ada template yang memasang blok `auto`-nya**, dan belum ada sesi yang membawa `pendukung[]` karena MyBharata belum dirilis.
	- **Dua sub-metrik**, `device_lengkap_persen` dan `display_sesuai_persen`, menjawab 2 dari 4 metrik template `Host Live Support Kyura` (bobot 0,30). Reduksi `rata_rata` keduanya, **tanpa metrik baku** (berbeda dari `kinerja_live` yang jatuh ke `conversion`), scope **hanya `individu`**, grup `GrupSumberMarketing`.
	- ⛔ **Sumber TERPISAH dari `kinerja_live`, bukan sub-metrik di sana.** `kinerja_live` menyelesaikan orang lewat `host[]`; yang ini lewat `pendukung[]`. Satu sumber dengan dua aturan atribusi yang dipilih menurut nama metrik gagal sebagai 200 berisi nol baris.
	- **Persen `null` dari marketing-analytics jadi GALAT, bukan nol** (`penjagaKesiapan`). Nol terbaca "alatnya tak pernah lengkap" dan menghukum orangnya atas pertanyaan yang belum pernah diajukan; presedennya `Add to Chart` bernilai 0 di bab `kinerja_live` di atas.
	- **Tiga cabang penjaga, sebab di hulu lebih dulu**: `toko_diminta` bernilai nol menyebut toko dan ICC Management (bukan menyalahkan orangnya), lalu "belum ada sesi yang mencatatnya sebagai pendukung", lalu "belum satu pun mengisi ceklis". `toko_diminta` bertipe pointer: nil dari marketing-analytics versi lama **tidak** dibaca nol.
	- **`CakupanPersen` = sesi berceklis ÷ sesi total**, wajib diisi karena sumber ini membawa satu angka sehingga cakupan bawaan selalu 1/1.
	- ⛔ **Penjaga posisi dua lapis** (`memegangPosisiLiveSupport`), diperiksa **sebelum** marketing-analytics dipanggil: `position_key` kanonik **atau** `position` mentah, aturan yang sama dengan `filterPosisiLiveSupport`. Satu lapis tak cukup: satu-satunya pemegang posisi Live Support di produksi ber-`position_key` `videographer`. Batasnya: penjaga ini **tidak** mencegah host berkolusi dengan Live Support sungguhan, karena sumber ini tak melihat isi `pendukung[]` tiap sesi.
	- ⛔ **Galat transport disaring `common.GalatTransportAman`.** URL yang dirakit berakhir `&key=`; tanpa penyaring, `*url.Error` membawa kunci layanan ke `AutoBasis`, tersimpan di Mongo dan dirender di dialog KPI. Berkas ini sempat jadi satu-satunya dari 20 sumber KPI yang tidak menyaringnya (diperbaiki #1927).
	- **Test kontrak mengurai rekaman JSON respons apa adanya**, bukan struct Go; kontrol negatifnya (satu nama field diubah camelCase) merah dengan `SesiBerceklis = 0, mau 38`.
- 🟡 **`karya_live_support`: KPI Live Support untuk tema dan teaser (`kpi_sumber_karya_live_support.go`), merged 2026-09-17 18:13 WIB, belum deploy** (bip-erp [#1955](https://github.com/bip-itteam-internal/bip-erp/pull/1955) `feat/employee-karya-live-support`). Menarik `GET /kpi/karya-live-support` dari [[Microservices - Marketing Analytics Service]] berkunci `MARKETING_ANALYTICS_SERVICE_KEY` (tak ada env baru). Keputusannya [[ADR - 0106 Tema dan Teaser Live Support Disetor dan Diputus Penyetuju Departemen sebagai Dasar KPI]].
	- **Dua metrik**, `tema_disetujui` dan `teaser_disetujui`, menjawab 2 metrik template `Host Live Support Kyura` yang tak punya sumber lain (bobot 0,70). ⚠️ Pasangkan mengikuti LABEL, bukan key: `tema_disetujui` ke "Membuat tema hostlive perbulan" (key `kualitas-tema`), `teaser_disetujui` ke "Membuat cuplikan konten (Teaser)" (key `kuantitas-tema`). Tanpa metrik baku, scope **hanya `individu`**, grup `GrupSumberMarketing`.
	- **Reduksi `jumlah_nilai` atas satu cacahan** (`Nilai: [n]`, `Populasi: 1`), pola `jumlah_video` dan lost time injury. ⛔ **Bukan `jumlah_unit`**: reduksi itu menghitung `len(Nilai)`, jadi satu cacahan berapa pun selalu terbaca 1. Spesifikasi awalnya menulis `jumlah_unit`; dikoreksi saat implementasi.
	- **Nol disetujui = nilai 0 yang SAH**, bukan galat, berbeda dari `kesiapan_live` yang nolnya berarti belum ditanya: tak menyetor apa pun adalah hasil kerja yang memang dinilai. Setoran yang **menunggu** tak dihitung tetapi **selalu tersebut di `Catatan`** ("x disetujui, y menunggu tinjauan, z ditolak"), supaya skor rendah karena peninjau belum membuka antrean tak terbaca sebagai Live Support yang tak bekerja.
	- **Kunci ringkasan yang HILANG jadi galat, bukan nol** (`disetujui`/`menunggu`/`ditolak` bertipe pointer; `data` absen atau `null` juga galat). Nol disetujui menurunkan skor, jadi bentuk respons yang rusak tak boleh menyamar sebagai "belum menyetor".
	- **Penjaga posisi dua lapis** `memegangPosisiLiveSupport` yang sama dengan `kesiapan_live`, diperiksa sebelum marketing-analytics dipanggil. Galat transport disaring `common.GalatTransportAman`. Kontrol negatif dijalankan untuk penjaga posisi, penyebutan menunggu di `Catatan`, penolakan kunci hilang, dan reduksi.
- ⚠️ **`GET /kpi/sumber-katalog`** (`kpi_katalog.go`, PR [#1053](https://github.com/bip-itteam-internal/bip-erp/pull/1053) merged 6 Agustus 2026, **belum di-deploy**) memaparkan seluruh isi dropdown konfigurasi: sumber beserta sub-metriknya, formula, arah, dan scope. Ada supaya daftar pilihan hidup **hanya di kode Go**; frontend yang menuliskan daftarnya sendiri membuat tiap sumber baru menuntut perubahan di dua tempat — persis ketergantungan yang registry ini hendak hapus. Formula/arah/scope dibaca **lewat refleksi** atas struct anonim di `shared-library` (`nilaiStructAnonim`), bukan literal, karena literal berarti tambahan di shared-library tak pernah muncul di dropdown dan gejalanya cuma pilihan yang hilang. Menggerbangi dirinya sendiri dengan `RequireKPIDepartmentRBAC` yang sama dengan `POST /kpi/templates` ([[ADR - 0031 Prefix internal Bukan Batas Keamanan]]). Kontrak: [[API - Employee Service]].
- ⚠️ **`POST /kpi/auto-values/pratinjau`** (`kpi_auto.go`, PR yang sama, **belum di-deploy**) menghitung konfigurasi yang **belum tersimpan** untuk maksimal 10 karyawan sekaligus. Ada karena salah konfigurasi tidak menimbulkan galat, hanya angka salah yang tampak wajar — sebaran beberapa orang pada periode lampau langsung menunjukkan target terlalu rendah (semua 100), terlalu tinggi, atau arah terbalik (semua 0). Read-only meski `POST`: metode itu dipakai karena konfigurasinya datang dari body. **RBAC diperiksa per karyawan yang diminta**, bukan sekali per departemen; memeriksa satu saja menjadikannya jalan pintas membaca nilai KPI departemen lain. Perhitungannya memakai jalur `terapkanOtomatis` yang sama, bukan salinan rumus.
- ⚠️ **Jejak perubahan konfigurasi otomatis** (`kpi_template_audit.go`, PR yang sama, **belum di-deploy**) mencatat tiap blok `auto` yang berubah saat template disunting, ke koleksi **terpisah** `kpi_template_audits` (bukan larik di dalam template — riwayat yang menumpang dokumen induk membuatnya membesar tanpa batas). Satu dokumen per metrik yang berubah: `template_id`·`metric_key`·`metric_label`·`config_lama`·`config_baru`·`diubah_oleh`·`diubah_at`. Perbandingannya `reflect.DeepEqual`, bukan `==` maupun hasil marshal, karena `KPIAutoConfig` memuat peta (`target_per_periode`, `target_per_karyawan`) dan pointer (`nilai_minimum`) yang tak dapat dibandingkan langsung. Pencocokan metrik lama-baru lewat `key` lebih dulu, `label` sebagai cadangan.
	- 🔴 **Belum ada endpoint maupun UI untuk MEMBACANYA.** Jejaknya hanya terbaca lewat akses Mongo langsung, dan **belum ada index `{template_id, diubah_at:-1}`** sehingga penelusuran per template memindai seluruh koleksi. Selama itu belum ada, fitur ini menulis riwayat yang praktis tak terbaca siapa pun. **TBD**: rute baca + index.
- **Dua sumber Kaizen, bukan satu**, karena [[HRIS - Matriks KPI per Departemen]] memakai dua redaksi berbeda: sebagian menghitung ide yang **diajukan**, sebagian yang benar-benar **diterapkan**. Menggabungkannya berarti salah satu departemen dinilai dengan angka yang bukan miliknya.
- Ditarik dari `GET /internal/kaizen/metrics` milik [[Microservices - Form Builder Service]] lewat env **`FORM_BUILDER_MODULE_URL`** (sudah ada di `docker-compose.yml`). Dibaca `os.Getenv` langsung, **bukan** lewat map yang divalidasi `ValidateInternalURL` — entri kosong di map itu memanic seluruh service.
- `has_program:false` dari form-builder membedakan "perusahaan ini belum menjalankan programnya" dari "gagal mengambil data". Hanya yang kedua dilaporkan sebagai metrik gagal hitung; yang pertama bukan kegagalan.
- 🟡 **`program_culture`** (`kpi_sumber_culture.go`, branch `feature/workspace-position`, **belum merge/prod**) menarik `GET /internal/culture/metrics` dari [[Microservices - Form Builder Service]] lewat `FORM_BUILDER_MODULE_URL` — pola identik Kaizen (memo 30s, gerbang kunci layanan). Grup **SDM**, formula baku `rata_rata`, **tanpa sub-metrik dan tanpa scope**. Melayani metrik `culture` ("Skor Program Culture") pada template aktif `HR Organizational Development` (Culture & Industrial). `MetrikCulture{Komposit[], Jumlah}`; Cuplikan: satu **skor komposit** (blueprint 30/30/40, dihitung form-builder) per program, `Nilai` = daftar komposit, Populasi = jumlah program, `PopulasiNyata` true; reduksi `rata_rata` → skor KPI officer. **Data kosong digalatkan** (jatuh `manual`, bukan 0). Mekanisme lengkap (skor komposit, jenis program, distribusi pilar): [[ADR - 0066 Modul Kelola Program Culture]] + [[Microservices - Form Builder Service]].
- ⚠️ **`nilai_inspeksi_satgas`** (`kpi_sumber_inspeksi_satgas.go`, bip-erp PR [#1866](https://github.com/bip-itteam-internal/bip-erp/pull/1866) merged 2026-09-14, **live di DEV, PROD belum deploy**; buktinya di butir Deploy) menarik nilai inspeksi Satgas 5R & K3 **per orang** dari `GET /internal/satgas/metrics?period=&company_id=&key=` milik [[Microservices - Form Builder Service]]. Keputusannya [[ADR - 0090 Inspeksi Satgas 5R dan K3 di Form Builder dengan Nilai dari Cek Ulang Terakhir]] §8: employee-service tetap satu-satunya penulis `kpi_score` ([[ADR - 0032 Kepemilikan kpi_score dan Batas Pengumpul Metrik]]), dan sumbernya untuk dipasang HR pada metrik Office Boy `Kebersihan 3` dan Security `Kerapihan dan kebersihan Pos`. Grup **SDM** karena grup mengikuti ASAL DATA (inspeksi OD & Industrial Relation di bawah HR), walau yang dinilai Office Boy dan Security; dikunci `kpi_katalog_grup_test.go`. Scope `individu` satu-satunya, formula baku `rata_rata` atas satu nilai per periode, tanpa sub-metrik (`DaftarkanSumber` biasa). Prosedur umumnya: [[RUN - Menambah Metrik KPI Otomatis]].
	- **Aturan nilainya TIDAK dihitung di sini.** Kiriman terakhir, rumus skala, batas cek ulang, dan gabungan antar form dihitung sekali di form-builder (`satgas_nilai.go`); sumber ini hanya memetakan jawabannya ke `Cuplikan`, sebab menghitung ulang akan melahirkan definisi kedua yang menyimpang dari rekap Satgas. Nilai final diteruskan sebagai **satu** nilai (`Populasi` 1). `CakupanPersen` diisi **hanya** bila orangnya sasaran lebih dari satu form (`forms_dinilai / forms_total`), supaya nilai dari 1 dari 2 form tak terbaca selengkap nilai seluruh formnya.
	- **Cara menarik.** Env `FORM_BUILDER_MODULE_URL` dan env baru **`FORM_BUILDER_SERVICE_KEY`**, keduanya dibaca `os.Getenv` per panggilan, **bukan** lewat map yang divalidasi `ValidateInternalURL`; env kosong menggalatkan metrik dengan pesan yang menyebut nama env-nya. Rutenya digerbang **kunci layanan** di query `key`, bukan `BIP-Gateway-ID` ([[ADR - 0031 Prefix internal Bukan Batas Keamanan]]: gateway memasang header itu untuk setiap permintaan ber-JWT, sementara muatannya nilai per orang). Header gateway tetap dikirim karena form-builder memasang `ValidateGateway` untuk seluruh rutenya. Klien 10 detik. Memo single flight 30 detik (`kpi_memo_hilir.go`) berkunci `perusahaan|periode`, sehingga satu pengambilan melayani semua orang yang dinilai bersamaan; kegagalan tak disimpan memo.
	- **Pesan galat HTTP.** `401` dijawab kalimat yang menyuruh memeriksa `FORM_BUILDER_SERVICE_KEY` di form-builder dan employee-service. Status non-200 lain meneruskan badan galat form-builder lewat `pesanGalatTiketGrup`, supaya sebab yang bisa ditindak (mis. `500` kiriman form mencapai batas baca, beserta judul formnya) sampai ke HR, bukan cuma kodenya. Galat transport dibungkus `common.GalatTransportAman`: URL-nya memuat `?key=`, dan galat ini disimpan ke `AutoBasis` lalu dirender di dialog KPI.
	- **Muatan sempit**: `{has_form, period_key, orang[{employee_id, nilai, forms_dinilai, forms_total, ada_kiriman, menunggu_cek_ulang, batas_cek_ulang, form_tanpa_skala}]}`, tanpa nama, departemen, jabatan, atau rincian per form. Karena itu pesannya menyebut "karyawan ini", bukan nama orang.
	- **Klasifikasi** (`cuplikanNilaiSatgas`, fungsi murni), dua kelas yang sengaja dibedakan:
		- **Belum dapat dihitung** (galat bertipe, lihat butir galat bertipe di bagian awal registry ini): belum ada form Satgas yang berjalan pada periode itu (`has_form:false`); temuan masih menunggu cek ulang, dengan tanggal batasnya disebut; kiriman terakhir tak menjawab pertanyaan skala; belum ada inspeksi. Metrik itu saja jatuh ke `manual`, skor otomatis lain orangnya tetap tampil. Temuan yang menunggu cek ulang sengaja tak diberi angka: `POST /kpi` menyalin nilai otomatis saat penilai menyimpan, jadi angka temuan yang telanjur tersimpan tak ikut berubah saat cek ulang masuk.
		- **Galat biasa** (`PrefiksGagalSumber`, menyembunyikan seluruh skor otomatis orang itu lewat `gagalKarenaSumber`): karyawan bukan sasaran form Satgas mana pun; form tanpa pertanyaan skala (judul formnya disebut); form-builder tak terjangkau, kunci salah, atau non-200. Salah konfigurasi dan kerusakan harus dibetulkan orang, jadi tak boleh tersamar sebagai data yang wajar belum ada.
	- **Kontrak dua sisi.** Literal `contohMuatanNilaiSatgas` dikunci di form-builder (`TestKontrakMuatanNilaiSatgas` dan `TestMuatanNilaiSatgasSempit` di `satgas_metrics_test.go`) dan di sini per field (`TestKontrakJawabanNilaiSatgas`). `TestKontrakNilaiSatgasSamaDenganFormBuilder` **membaca literal milik form-builder langsung dari berkas test-nya** lalu membandingkannya dengan salinan di sini: tanpa itu, kunci yang di-rename di form-builder beserta literalnya tetap hijau di kedua suite, sementara employee-service membaca field itu sebagai nilai nol.
	- ⚠️ **Kalimat alasannya dibaca frontend.** erp-frontend (PR [#1555](https://github.com/bip-itteam-internal/erp-frontend/pull/1555) merged 2026-09-14, dibangun manual di DEV hari itu; sarannya belum dilihat di layar, baru unit test) memilih saran di layar Otomasi KPI dengan mencocokkan potongan kalimat ini (`caraMengatasiKey`), dan salinan literalnya ada di `auto-overview-view.cara.test.tsx`. Mengubah kalimat di berkas Go wajib memperbarui salinan itu; lihat [[REF - Penamaan Metrik & Sumber KPI]].
	- ⚠️ **Deploy**: `FORM_BUILDER_SERVICE_KEY` wajib bernilai sama di **dua** blok compose (`employee-service` dan `form-builder-service`, di `docker-compose.yml` maupun `docker-compose.dev.yml`). Form-builder membacanya sekali saat proses naik dan kunci kosong **menutup** rutenya (`401` untuk semua pemanggil), jadi penambahan env ini menuntut `--force-recreate` kedua container, bukan `restart`. DEV 2026-09-14: kuncinya belum ada di `.env` dev saat merge, lalu diisi dan kedua container dibuat ulang `--force-recreate` dengan kunci sama; biner employee-service dev memuat kode T3. `.env` PROD belum diukur, PROD belum deploy. Uji lewat gateway dev 2026-09-14 (`GET /api/employee/kpi/auto-values`, template KPI uji Office Boy bersumber `nilai_inspeksi_satgas`, data uji sudah dibersihkan): temuan yang masih bisa dicek ulang berbunyi "belum dapat dihitung: temuan Satgas atas karyawan ini pada periode 2026-09 masih menunggu cek ulang sampai 5 Oktober 2026"; Office Boy tanpa kiriman "belum dapat dihitung: belum ada inspeksi Satgas atas karyawan ini pada periode 2026-09"; sesudah cek ulang skor 5, `auto_value` 100; ketiganya `auto_gagal_sumber: false`. Skor otomatis lain orangnya tetap tampil saat periode tanpa form Satgas baru dibuktikan unit test.

**Cakupan supervisi antar-departemen**
- `master_department` punya `supervised_by` (key departemen induk) + `supervision_label` (nama pendek kelompok, mis. `HRGA`). Di-seed & di-migrasi idempoten saat startup (hanya mengisi bila field belum pernah ada, jadi nilai yang diatur admin tak tertimpa).
- Service ini **sumber kebenaran** relasi tersebut: mengisi klaim JWT `supervised_departments` saat login, dan melayani `/list?type=supervisor` dengan urutan telusur departemen sendiri → induk.
- Konsep, aturan, dan konsekuensinya: [[HRIS - Organization Structure]]

**Jenjang jabatan — ✅ live di produksi 2026-08-03** (PR [#952](https://github.com/bip-itteam-internal/bip-erp/pull/952), `60e77d53`)
- Koleksi baru `master_job_level` (`key`, `name`, `rank`) + `position_items[].level_key` di `master_department`. Lima tingkat bawaan: Pelaksana(10) · Senior/Officer(20) · Leader(30) · Supervisor(40) · Direktur(50). Model & helper di `shared-library/models/employee/master_data.go` (`JobLevel`, `DefaultJobLevels`, `RankOf`, `MinJarakRank`), handler di `services/employee/job_level.go`.
- ⚠️ **Penanda ORGANISASI, BUKAN sumbu hak akses** ([[ADR - 0030 RBAC Tiga Sumbu dengan Hak Menempel di Posisi]]). `permission_resolve.go` & `permission_exceptions.go` **tidak boleh menyebutnya sama sekali**, dan itu dikunci `TestJenjangBukanSumbuHakAkses` yang membaca kedua berkas itu — pola sama dengan `internal_routes_guard_test.go`. Dijaga dari sumbernya karena pelanggarannya tak akan membuat satu pun test fungsional gagal.
- **Rank renggang** supaya tingkat baru (mis. Manager ber-rank 45) cukup satu entri di `DefaultJobLevels()` + deploy, tanpa menyentuh dokumen jabatan/karyawan mana pun. Jarak minimalnya dikunci uji.
- `seedJobLevels()` menyisipkan **per key**, bukan skip-if-nonempty seperti `seedMasterDepartments`/`seedMasterSystemRoles`/`seedPermissionSets` — pola itu menuntut fungsi `migrate*` sendiri tiap ada key baru, karena dev & prod tak pernah kosong. Nama yang sudah diubah HR tak ditimpa.
- **GLOBAL**, tanpa `company_id`, beda dari `master_department`. Index unik `key` di `ensureTenantIndexes`.
- `GET /master/job-levels` · `PUT /master/departments/:key/positions/:positionKey/level` (`RequireHRISOrITSupervisor`, sama dengan `menu-hidden`). Frontend: tab **Jenjang Jabatan** di `/hris/master-data`.
- **Belum menghasilkan apa pun sampai HR mengisi**: verifikasi prod 2026-08-03 menunjukkan 5 jenjang ter-seed dan **0 dari 79 jabatan** berjenjang. Aturan & konsekuensinya: [[HRIS - Organization Structure]] · [[HRIS - Career & Promotion]]

**Resign / Non-Aktif Karyawan — ✅ live di produksi 2026-08-05** (PR [#1009](https://github.com/bip-itteam-internal/bip-erp/pull/1009); [[ADR - 0035 HR Menonaktifkan Akun lewat Catatan Resign]], konsep: [[HRIS - Personalia]])
- Koleksi baru `employee_resign` (`employee_id`, `company_id`, `category`, `effective_date`, `reason`, `file`, `status`, `applied_at`, `account_deactivated`, `cancelled_at`, `cancel_reason`, `metadata`). Model + enum di `shared-library/models/employee/models.go`, handler di `services/employee/resign.go` & `resign_file.go`.
- **Koleksi sendiri, BUKAN field status di `work_data`.** Koleksi itu sudah menyimpan dua salinan kontrak yang butuh empat penjaga agar tak menyimpang; salinan ketiga mengulang pola yang sama. `system_authentication.is_active` tetap satu-satunya sumber status aktif, dan dokumen resign menjelaskan **mengapa** — inilah "catatan terminasi" yang diminta [[HRIS - Attrition]].
- Lima kategori tetap di kode (`ResignCategories`): Mengundurkan Diri · PHK · Pensiun · Kontrak Berakhir · Meninggal Dunia. Bukan master data, jadi penambahan menuntut deploy.
- Tiga status **disimpan**, bukan diturunkan dari tanggal: `scheduled` (akun masih hidup) · `applied` (akun sudah mati) · `cancelled`. Menurunkannya dari `effective_date` saja membuat catatan yang dibatalkan tetap terbaca non-aktif, dan membuat cron kehilangan sifat idempotennya bila ada hari terlewat.
- `GET /resign` (daftar berpaginasi, filter `category`/`status`/`department`/`search`) · `GET /resign/employee/:employee_id` (riwayat) · `POST /resign` · `PATCH /resign/:id` · `POST /resign/:id/cancel` · `POST|GET /resign/:id/file`. Seluruhnya `RequireHRISStaff` + isolasi tenant lewat `EffectiveCompanyID`; `POST` menolak karyawan dari perusahaan lain sebelum menyentuh akunnya.
- **Tanggal efektif = hari PERTAMA non-aktif**, dinormalkan ke tengah malam WIB saat disimpan (`normalisasiTanggalEfektif`). Frontend mengirim RFC3339 yang zonanya tergantung cara tanggal dirakit di browser, jadi tanpa normalisasi dua orang yang memilih tanggal sama bisa berujung hari mati berbeda. Fallback saat tzdata tak terbaca adalah **+07:00 tetap, bukan UTC** — UTC akan mematikan akun tujuh jam lebih cepat dari yang dimaksud.
- ⚠️ **Nilai nol tanggal dijaga eksplisit.** `verboseCheckFields` di `shared-library/validation` hanya memeriksa field bertipe **string**, jadi `effective_date` yang hilang dari body lolos sebagai `time.Time` nol. Merakitnya ulang lewat `time.Date` menghasilkan 1 Januari tahun 1 yang bukan lagi nilai nol Go, sehingga cek `IsZero` tak berbunyi dan — karena tahun 1 sudah lewat — akun langsung mati alih-alih request dibalas 400. `normalisasiTanggalEfektif` karena itu mengembalikan nilai nol apa adanya.
- **Cron harian 00:10 WIB** (`cronTerapkanResign`) menyapu `status=scheduled AND effective_date <= akhir hari ini`. Batas atas akhir hari, bukan waktu saat cron jalan, supaya catatan bertanggal hari ini tidak terlewat sampai besok. Hanya `scheduled` yang disapu, dan itulah yang membuatnya idempoten sekaligus menyapu tunggakan hari yang terlewat. Tanggal hari ini atau mundur diterapkan **seketika** saat `POST`, karena HR sering baru mencatat setelah orangnya keluar.
- **Pembatalan hanya menghidupkan akun yang dimatikan catatan itu sendiri**, ditandai `account_deactivated` yang diisi dari nilai balik `terapkanStatusAkun`. Tanpa itu, akun yang sudah dinonaktifkan IT lebih dulu tetap membuat catatan berstatus `applied`, dan HR yang membetulkan salah input akan menghidupkan kembali akun yang sengaja dimatikan IT. Alasan pembatalan **wajib** hanya bila pembatalan itu membuka kembali akses.
- Koreksi (`PATCH`) hanya untuk catatan `scheduled`. Mengubah tanggal catatan yang sudah `applied` tidak menghidupkan kembali akunnya, jadi dokumen dan kenyataan akan berbeda tanpa ada yang tahu; jalannya lewat batalkan lalu catat ulang.
- Dokumen pendukung menerima PDF, gambar (JPG/PNG), dan Word (DOC/DOCX), cap **4 MB** yang dipaku [[Microservices - File Service]]. Dinilai dari ekstensi **terakhir**, jadi `surat.pdf.exe` ditolak. Objek lama dihapus setelah `$set` berhasil, pola sama dengan lampiran kontrak.

**Jejak status akun & penjaga backlog — 🔜 belum merge & belum deploy** (branch `feat/employee-status-akun-bertanggal`; [[ADR - 0113 Actual vs Planning MPP Dihitung Sistem per Bulan, Berpijak pada Jejak Keluar Bertanggal]], perluasan [[ADR - 0035 HR Menonaktifkan Akun lewat Catatan Resign]])
- **Masalah yang diselesaikan**: `system_authentication` tak punya satu pun stempel waktu, jadi penonaktifan yang tak lewat menu Resign tak bisa ditanggalkan sama sekali. Diukur PROD 2026-09-21: **35 akun non-aktif, hanya 11 punya catatan resign, 24 tanpa tanggal** (dokumen resign ada 12, satu milik orang yang masih aktif). Akibatnya rekonstruksi headcount akhir Juni terbaca 188 sementara lembar HRD menulis 158 — dan angka itu dipakai menilai tim rekrutmen.
- Koleksi baru `account_status_log` (`employee_id`, `company_id`, `sebelum`, `sesudah`, `alasan`, `sumber`, `resign_id`, `pelaku`, `created_at`), indeks `{employee_id:1, created_at:-1}`. `sebelum`/`sesudah` disimpan **keduanya**: tanpa nilai lama, baris dari permintaan no-op tak bisa dibedakan dari penonaktifan sungguhan. `company_id` **tanpa** `omitempty` supaya akun luar (yang memang tak punya `work_data`) tetap punya field itu.
- **Empat pintu, semuanya kini berjejak.** `terapkanStatusAkun` menuntut argumen jejak (alasan, sumber, pelaku, id resign) sehingga pintu baru tak bisa dikompilasi tanpa menyebutkannya. Sumbernya tiga: `it` (jalur IT), `resign` (terapkan & batalkan), `akun_luar` (`DELETE /external-accounts/:employeeID` — pintu **ketiga** yang baru ketahuan lewat pemindai sumber, sebelumnya menulis `is_active` langsung). Pintu **keempat**, `PUT /update/:employee_id/system-auth`, meneruskan map bebas ke `$set` dan karena itu **menolak 400** bila body memuat `is_active`, menunjuk `PATCH /account/active-status`.
- Jejak ditulis **di dalam** `terapkanStatusAkun`, sesudah early-return no-op: permintaan yang tak mengubah apa pun bukan peristiwa, dan menulisnya akan memajukan "non-aktif sejak" seseorang tiap kali cron menjalani ulang tanggal yang sama.
- `GET /resign/non-aktif-tanpa-catatan` — penjaga akun non-aktif yang belum punya catatan keluar, digerbang sama dengan `GET /resign`. **Nol baris adalah ambang** [[ADR - 0113 Actual vs Planning MPP Dihitung Sistem per Bulan, Berpijak pada Jejak Keluar Bertanggal]] sebelum riwayat headcount bulan lampau dibuka. ⚠️ Jejak status akun **tidak** menggantikan catatan keluar: jejak menjawab "kapan saklarnya ditekan", catatan resign menjawab "orang ini berhenti, karena apa" — akun bisa mati tanpa orangnya berhenti, jadi baris yang sudah punya jejak pun tetap muncul sampai HR mencatatnya.
- **Penjaga di test, bukan di niat**: pemindai AST menolak penulisan `is_active` di luar `account_status.go` (daftar-izin satu berkas, `func.go`, karena itu `work_document`), menuntut `terapkanStatusAkun` benar-benar memanggil penulis jejaknya, menuntut tiap literal jejak menyebut alasan & sumber, dan menahan rute literal penjaga agar tak tertelan saudara ber-`:param`. Masing-masing berpasangan dengan kontrol positif supaya pemindai yang diam-diam berhenti membaca tidak lolos sebagai hijau.
- Index: `{employee_id, status}` · `{company_id, effective_date}` · `{status, effective_date}` (dipakai sapuan cron).
- Frontend: `/hris/resign` di menu **Personalia** ([[APP - Web ERP]]).
- ⚠️ **Karyawan non-aktif lenyap, bukan sekadar tersembunyi**: setidaknya enam kueri di service ini menyaring `is_active: true` diam-diam (`/list?type=employee`, agregat direktori, dsb), jadi karyawan yang resign di tengah bulan hilang dari laporan absensi dan basis payroll bulan itu juga. **Belum ditangani.**
- ⚠️ **Verifikasi produksi 2026-08-06**: `employee_resign` **0 dokumen**, akun aktif **183**. Fitur sudah live tapi belum dipakai, jadi angka apa pun yang diturunkan darinya masih nol.

**Promosi & Mutasi Karyawan — ⚠️ merged ke `main` 2026-08-10** (PR [#1142](https://github.com/bip-itteam-internal/bip-erp/pull/1142), `a9a323dd`; [[ADR - 0044 Mutasi Antar-Tenant Mempertahankan employee_id]], konsep: [[HRIS - Career & Promotion]])
- Koleksi baru `employee_movement` (`employee_id`, `company_id`, `type`, snapshot `from_*`, tujuan `to_*`, `effective_date`, `reason`, `file`, `status`, `applied_at`, `cancelled_at`, `cancel_reason`, `metadata`). Model di `shared-library/models/employee/movement.go`; handler di `services/employee/mutasi.go`, `mutasi_routes.go`, `mutasi_file.go`, `mutasi_calendar.go`.
- **Satu koleksi untuk promosi DAN mutasi**, dibedakan `type`: `Promosi` · `Mutasi` · `Mutasi Antar-Perusahaan`. Keduanya peristiwa yang sama bentuknya, dan memisahkannya jadi dua koleksi berarti dua salinan mesin yang sama.
- ⚠️ **`employee_id` TIDAK diterbitkan ulang saat pindah tenant**, sehingga prefix ID (`BIP-`, `ELT-`) berhenti menandakan perusahaan dan tinggal jadi jejak asal-mula. Sumber kebenaran tenant tetap `work_data.company_id`. Alasan & konsekuensinya: [[ADR - 0044 Mutasi Antar-Tenant Mempertahankan employee_id]].
- **Jenis dipilih HR, tidak pernah disimpulkan dari jenjang.** Nol dari 79 jabatan punya `level_key`, jadi menyimpulkannya dari `RankOf` berarti menyimpulkan dari data kosong. `level_key` tetap disnapshot sebagai jejak dan tetap **bukan** sumbu hak akses ([[ADR - 0030 RBAC Tiga Sumbu dengan Hak Menempel di Posisi]]).
- Tiga status **disimpan** (`scheduled`/`applied`/`cancelled`), tanggal efektif dinormalkan tengah malam WIB, **cron harian 00:15 WIB** (`cronTerapkanMutasi`) — lima menit **setelah** cron resign, supaya karyawan yang tanggal resign dan mutasinya sama sudah non-aktif lebih dulu dan tidak ikut dipindahkan. Pola dari [[ADR - 0035 HR Menonaktifkan Akun lewat Catatan Resign]].
- ⚠️ **Satu perbedaan sengaja dari resign**: hanya `scheduled` yang memblokir catatan baru. Resign juga memblokir `applied` karena orang cuma berhenti sekali; orang dimutasi berkali-kali, dan menyalin aturan resign apa adanya akan mengunci setiap karyawan dari mutasi **kedua** selamanya tanpa satu pun galat.
- **`company_id` catatan = perusahaan ASAL**, sebab semua pembacaan menyaring `EffectiveCompanyID` dan catatan yang distempel tujuan akan lenyap dari layar HR yang membuatnya. Daftar, riwayat, dan lampiran dibaca dari **dua arah** (`company_id` ATAU `to_company_id`) supaya sisi tujuan melihat orang yang masuk.
- **Departemen & jabatan tujuan diverifikasi ke `master_department` perusahaan TUJUAN** (`resolveTujuan`); `key` unik per perusahaan, dan ELT masih nol departemen sehingga tanpa penjaga ini mutasi pertama ke sana mendaratkan orang di departemen yang tidak ada. `position_key` diambil dari master, tak pernah dikirim frontend.
- **`work_data` ditulis lewat `$set` yang disusun sendiri** (`perubahanWorkData`/`pengembalianWorkData`), BUKAN lewat `executeEmployeeUpdateTransaction` (bisa mengosongkan `company_id`) maupun rute update parsial (sumber korupsi tipe BSON). `employment_type`/`contract_ending` **tak pernah** ikut — keduanya milik modul kontrak (`fieldKontrak`), dan itu dikunci uji.
- **`supervisor_id` dilepas saat departemen berubah**, sebab `validasiAtasan` menolak atasan beda perusahaan dan atasan langsung adalah relasi departemen. Nilai lamanya disnapshot di `from_supervisor_id` supaya pembatalan punya jalan pulang; tanpa itu ia hilang permanen. Promosi dalam departemen yang sama tak menyentuhnya.
- **Pembatalan mengembalikan posisi dari snapshot**, dan filternya menyebut posisi TUJUAN sehingga hanya karyawan yang benar-benar masih duduk di sana yang dikembalikan — sekelas dengan `account_deactivated` pada resign, yaitu tak membatalkan efek yang bukan miliknya.
- **Catatan yang mustahil terlaksana ditutup otomatis** (`alasanBatalOtomatis`): karyawan yang keburu non-aktif sebelum tanggal efektif membuat catatannya dibatalkan cron dengan alasan tercatat. **Hanya** untuk sebab itu; posisi yang tak cocok tetap dicoba lagi karena sebabnya bisa pulih, dan galat infrastruktur tak pernah menutup apa pun.
- `GET /mutasi` · `/mutasi/types` · `/mutasi/companies` · `/mutasi/employee/:employee_id` · `POST /mutasi` · `PATCH /mutasi/:id` · `POST /mutasi/:id/cancel` · `POST|GET /mutasi/:id/file`. Baca digerbang `gateHris(PermHrisView, …)`, tulis `gateHris(PermHrisWork, …)`. Kontrak: [[API - Employee Service]].
	- `/mutasi/companies` mengembalikan hanya perusahaan AKTIF (`key`·`name`·`code`), bentuk yang dibutuhkan form pemilih. Alasan yang tercatat saat rute ini dibuat (PR [#1143](https://github.com/bip-itteam-internal/bip-erp/pull/1143)), bahwa `/master/companies` digerbang `RequireITSupervisor` sehingga staf HR buntu di layar, ⚠️ **tak cocok dengan kode sekarang** (diverifikasi 2026-09-11, `services/employee/master_data.go:354-374`): `GET /master/companies` tanpa gerbang peran tapi ikut mengembalikan perusahaan nonaktif, dan hanya `POST` yang digerbang (`RequireCentralAdmin`).
- **`supervisor_id` dilepas DUA ARAH saat departemen berubah** (`pindahDepartemen`): atasan si karyawan sendiri, DAN orang-orang yang menunjuk dia sebagai atasan. Arah kedua sempat terlewat dan akibatnya senyap — `bawahanAktif` menyaring `supervisor_id` **plus** `company_id`, jadi Leader yang pindah departemen di dalam satu perusahaan tetap mengagregasi skor tim yang sudah bukan timnya tanpa satu pun galat. Daftar bawahan disnapshot ke `from_subordinate_ids` **saat diterapkan** (bukan saat dicatat, sebab daftarnya bisa berubah di antaranya) supaya pembatalan punya jalan pulang; pengembaliannya hanya menyentuh yang atasannya masih kosong.
- **Penilaian KPI memakai jabatan PERIODE, bukan jabatan hari ini** (`kpi_posisi_periode.go`). `POST /kpi` semula membandingkan template dengan jabatan terkini, dan itu pecah begitu modul ini ada: karyawan yang pindah 1 September membuat penilaian Agustus-nya mustahil diisi benar. Jawabannya diambil dari `employee_movement`; patokannya jabatan pada **akhir** periode, sebab penilaian bulanan harus punya satu jawaban. Gagal baca riwayat jatuh ke perilaku lama, bukan memblokir penilaian. Detail: [[HRIS - Key Performance Index]].
- **Prasyarat perpindahan — PONDASI, belum gerbang** (`mutasi_prasyarat.go`). Aturan yang dituju: naik jabatan/pindah posisi menuntut post-test LMS lebih dulu. LMS-nya belum ada, jadi **belum ada satu pun pemeriksa terdaftar dan tak ada yang tertahan**. Menyambungkannya nanti = `DaftarkanPrasyarat("post_test", fn)`, meniru idiom `DaftarkanSumber`. Dua sifat dikunci uji: tanpa pemeriksa **tak ada yang tertahan**, dan pemeriksa yang **gagal dihubungi tak menahan** (kalau menahan, satu service mati mengunci pencatatan mutasi seluruh perusahaan). Hasilnya disimpan di `prasyarat[]`, dan pelewatan **wajib beralasan berikut pelakunya**.
- **Notifikasi `employee-moved`** (`mutasi_notify.go`, PR [#1145](https://github.com/bip-itteam-internal/bip-erp/pull/1145), **merged**) dikirim setelah perpindahan BENAR-BENAR diterapkan: ke karyawannya, dan untuk lintas tenant juga ke supervisor HR perusahaan **tujuan**. Karena approval sengaja ditunda, jalur ini adalah **satu-satunya** yang memberi tahu perusahaan penerima; perusahaan tujuan tanpa supervisor HR karena itu di-log eksplisit. Kalimatnya menyatakan orangnya sudah masuk, **bukan** meminta persetujuan, dan menyebut bahwa kontrak baru perlu diterbitkan. ⚠️ Kategori baru = naikkan `Employee-Service` **dan** `Notification-Service` bersama; pasangan mobile-nya my-bharata [#110](https://github.com/bip-itteam-internal/my-bharata/pull/110).
- Index: `{employee_id, status}` · `{company_id, effective_date}` · `{to_company_id, effective_date}` · `{status, effective_date}` (sapuan cron). `ensureMutasiIndexes` dijaga `mongodb.DB == nil` karena ia dipanggil saat **registrasi rute**, sehingga panik `GetCollection` akan membuat service gagal naik sama sekali.
- **Feed kalender `movement`** (`mutasi_calendar.go`) menumpang `/internal/calendar-feed` yang sudah ada — tanpa provider baru, tanpa perubahan frontend. Lingkup **personal saja**; catatan `cancelled` tak pernah muncul, `applied` tetap muncul. Detail: [[Microservices - Calendar Service]].
- ⚠️ **Belum diverifikasi lewat gateway hidup, dan produksi belum di-deploy.** 509 test `services/employee` + 140 `shared-library` hijau dan 21 service build bersih, tapi seluruhnya lokal. Frontend belum ada, sehingga `deep_link` feed kalender menunjuk halaman yang belum dibangun.

**Ringkasan turnover — ✅ merged & live di produksi** (konsep: [[HRIS - Attrition]])
- `GET /resign/summary` (gerbang `gateHris(PermHrisView, RequireHRISStaff)`, isolasi tenant) menyuplai kartu statistik di halaman Resign **dan** kartu turnover di tab Ringkasan/HRD Supervisor Ringkasan Divisi HRGA ([[APP - Web ERP]]): `keluar` · `keluar_sukarela` · `masuk` · `headcount_awal` · `headcount_kini` · `turnover_persen` · `target_persen`.
- Merge `a9064d66` (2026-08-06), disusul `8463cbe0` (2026-08-08) yang menyempitkan **pembilang turnover jadi pengunduran diri saja** (penyebutnya tetap seluruh headcount). Verifikasi biner container `Employee-Service` produksi **2026-08-10**: string `resign/summary` ada, kontrol negatif kosong. Catatan lama "branch `feat/employee-turnover`, belum merge & belum deploy" **sudah tidak berlaku**.
- **`/resign/summary` tetap tanpa parameter bulan, dan itu masih disengaja.** Tak ada riwayat headcount di service ini, jadi headcount awal bulan **direkonstruksi** (`aktif sekarang + keluar − masuk`). Bulan lampau menuntut penguraian mundur berlapis yang tiap langkahnya menambah galat, dan hasilnya terlihat pasti padahal tidak. Endpoint ini menjawab "sekarang bagaimana", dan jawabannya tak boleh bergantung pada rekonstruksi apa pun.
- `GET /resign/summary/riwayat?bulan=N` — ⚠️ **branch `feat/hris-ringkasan-divisi`, belum di-push/PR/merge/deploy.** Deret turnover & headcount beberapa bulan ke belakang untuk bagan tren di Ringkasan Divisi HRGA. Gerbang sama (`gateHris(PermHrisView, RequireHRISStaff)`), tiap entri berisi `bulan` · `keluar` · `keluar_sukarela` · `masuk` · `headcount_awal` · `headcount_akhir` · `turnover_persen`; respons membawa `target_persen` · `rekonstruksi: true` · `maks_bulan`. Kontrak: [[API - Employee Service]].
	- **Ini MEMBALIK keberatan di butir sebelumnya, dan pembalikannya sadar.** Keberatannya masih benar dan tidak dicabut; yang berubah adalah **konsumennya**. Penolakan itu lahir saat satu-satunya pemakai adalah kartu angka, dan angka tunggal yang salah tak punya penawar apa pun. Bagan tren menjawab pertanyaan berbeda — **arah**, bukan nilai — dan arah bertahan terhadap galat kecil yang sama.
	- **Tiga pengaman supaya kelonggarannya tak melebar sendiri**: panjang deret dijepit **keras** di 6 (`MaksBulanRiwayatTurnover`, bukan bawaan yang bisa dinaikkan lewat query, sebab tiap lapis mundur mewarisi galat lapis sebelumnya); respons menandai diri `rekonstruksi: true`; dan **bulan terakhir deret wajib sama persis dengan `/resign/summary`**, dipatok uji. Menaikkan batasnya hanya sah bersama snapshot bulanan yang sungguhan, bukan dengan mengubah konstanta.
	- Rumusnya **memakai ulang** `hitungKeluar` · `headcountAwal` · `hitungTurnover` yang sama dengan `/summary`, bukan menyalinnya: dua salinan rumus turnover pasti menyimpang, dan yang dipercaya pemakai adalah endpoint yang kebetulan dibukanya lebih dulu.
	- ⚠️ **`headcount_akhir` entri TERAKHIR berarti "sekarang", bukan "akhir bulan"** — bulannya belum selesai, dan definisinya sengaja disamakan dengan `headcount_kini` di `/summary` supaya kedua endpoint tak berbeda pendapat. Konsumen yang menyandingkannya dengan bulan-bulan sebelumnya membandingkan potongan waktu yang lebih pendek: sah untuk melihat arah, menyesatkan sebagai angka akhir.
	- **Jepitan `bulan` diangkat jadi `bulanRiwayatValid`** karena dipakai DUA kali untuk satu permintaan: menentukan batas bawah kueri Mongo, dan menentukan panjang deret. Bila keduanya menjepit sendiri-sendiri lalu salah satu berubah, kuerinya memuat rentang berbeda dari yang digambar — dan gejalanya cuma bulan terjauh berisi nol, yang di layar tak terbedakan dari bulan yang memang sepi.
	- `join_date` **tidak** disaring di kueri (tipenya campur date dan string; kueri rentang Mongo hanya mengenai yang bertipe date), sedangkan `effective_date` boleh disaring di kueri karena selalu ditulis kode kita sendiri. Alasan lengkapnya sama dengan `/summary`.
	- ⚠️ **Belum diverifikasi lewat gateway.** 6 uji baru hijau dan `go build` bersih, tapi seluruhnya lokal. Yang wajib dibuktikan sebelum diklaim selesai: `GET /api/employee/resign/summary/riwayat?bulan=6` membalas 6 entri **dan** bulan terakhirnya cocok dengan `GET /api/employee/resign/summary` — kalau keduanya berbeda pendapat untuk bulan yang sama, rekonstruksinya salah.
- Rumus **rata-rata headcount** (`keluar / ((awal+akhir)/2) × 100`), ambang `TargetTurnoverBulananPersen = 5.0` **dikirim lewat respons** dan tidak disalin ke frontend: dua salinan angka ambang pasti menyimpang, dan akibatnya kartu berwarna aman untuk angka yang menurut server sudah lewat batas.
- `hitungTurnover` menjaga pembagian nol bukan demi kerapian angka: **NaN dan Inf tak bisa di-marshal `encoding/json`**, jadi tanpa penjagaan itu endpoint membalas 500 alih-alih angka. Tenant yang baru dibuat persis punya nol karyawan aktif.
- `keluar` dihitung dari `effective_date` (bukan `applied_at`) dan hanya status `applied`.

**Surat Peringatan (SP1/SP2/SP3) — ⚠️ selesai di branch `feat/employee-surat-peringatan`, belum di-push/PR/merge/deploy** (konsep & aturan: [[HRIS - Disciplinary (Surat Peringatan)]])
- Koleksi `employee_warning`, terpisah dari `work_data` dengan alasan yang sama seperti `employee_resign`: riwayat sanksi adalah rangkaian peristiwa, bukan satu field status.
- **Status TIDAK disimpan** — dan ini **sengaja berbeda** dari resign & mutasi yang menyimpannya. Keduanya wajib menyimpan karena cron-nya punya efek samping (mematikan akun, memindahkan jabatan) dan perlu membedakan "belum diterapkan" dari "sudah". Hangusnya SP tak berakibat apa pun, ia cuma berhenti dihitung; menyimpan statusnya justru menaruh kebenaran jumlah SP aktif pada cron yang berjalan tepat waktu. Diturunkan saat baca dari `revoked_at` dan `expires_at`, dengan tag `bson:"-"` pada field `Status` supaya tak ada dua sumber kebenaran. **Konsekuensinya**: penyaringan `?status=` harus mengungkapkan definisi itu lagi sebagai kueri Mongo (`filterStatusSP`), dan yang menjaga keduanya sinkron adalah test, bukan tipe.
- **`expires_at` DISIMPAN** (bukan dihitung ulang tiap baca) karena 6 bulan hanya tertulis eksplisit untuk SP2 (Pasal 55); SP1 & SP3 belum dipastikan HRD, dan menyimpan per catatan berarti koreksi nanti tak menuntut migrasi. Harinya **dijepit ke hari terakhir bulan tujuan**: `time.AddDate` menormalkan 31 Februari jadi 3 Maret, sehingga SP akhir bulan diam-diam berlaku beberapa hari lebih lama daripada yang tertulis di suratnya.
- **Gerbang baca TIGA arah** (HR · diri sendiri · atasan atas departemen bawahannya) memakai `common.SupervisedDepartmentsStrict`. Memakai `SupervisedDepartments` yang ber-fallback membuat **setiap karyawan** tampak supervisor departemennya sendiri dan bisa membaca SP rekan-rekannya; kelas yang sama sudah menggigit di feed kalender (PR [#1047](https://github.com/bip-itteam-internal/bip-erp/pull/1047)). Permintaan non-HR yang bukan dirinya sendiri dan tanpa klaim supervisi ditolak **sebelum** menyentuh database, sehingga tak ada jalan menuju pencarian departemen tanpa klaim supervisi yang nyata. Dikunci uji kontrol negatif lewat Fiber sungguhan.
- **Usulan SP1** memanggil [[Microservices - Attendance Service]] `GET /internal/late-recap`; hitungan telat **tidak disalin** ke sini. Periodenya 26-25 (periode payroll), sama dengan yang sudah dilihat karyawan di [[APP - MyBharata]]. Attendance tak terjangkau dibalas **503 berisi `error` tanpa kunci `data`**, bukan daftar kosong: daftar kosong terbaca "tak ada yang terlambat" dan HR tak punya cara tahu fiturnya rusak.
- **Penerbitan MANUAL**, meski peraturan menulis telat 3x "otomatis memicu" SP1 — penyimpangan sadar, sebab tarik log fingerprint masih manual sehingga angka telat sebuah periode belum tentu lengkap saat dibaca.
- **Pencabutan atomik**: `revoked_at: nil` ikut di filter `UpdateOne`, bukan cuma diperiksa dari hasil `FindOne`. Tanpa itu dua pencabutan bersamaan sama-sama lolos penjaga dan sama-sama menulis, sehingga karyawannya menerima **dua** notifikasi untuk satu surat. **Tidak ada `PATCH /warnings/:id`** sama sekali: surat yang sudah terbit dan diberitahukan tak boleh bisa ditulis ulang tanpa bekas.
- **Notifikasi `warning-issued` & `warning-revoked`**. Dua kategori, bukan satu, karena MyBharata memilih label/warna/ikon dari kategori dan menumpangkan pencabutan pada kategori penerbitan membuat kabar "SP dicabut" tampil dengan rupa sanksi baru. ⚠️ Kategori baru = naikkan `Employee-Service` **dan** `Notification-Service` bersama. ⚠️ **Pasangan mobile-nya BELUM ada**: `notification_type.dart` belum mengenali keduanya, jadi notifikasinya jatuh ke `default` dan tampil berlabel "Sistem" dengan ikon server.
- Index: `{employee_id, issued_date}` · `{company_id, issued_date}` · `{company_id, revoked_at, expires_at}` (hitungan SP aktif). `ensureWarningIndexes` dijaga `mongodb.DB == nil` karena dipanggil saat **registrasi rute**.
- Aturan berkas lampiran **dipakai ulang apa adanya** dari jalur resign (`validateResignFile`, `resignContentType`, `kpiEvidence*`): kebutuhannya identik dan menyalinnya hanya melahirkan aturan kedua yang akan menyimpang.
- ⚠️ **Belum diverifikasi lewat gateway hidup, belum merge, produksi belum di-deploy.** Seluruh test `services/employee`, `services/attendance`, dan `shared-library` hijau, tapi seluruhnya lokal.

⚠️ **Jebakan lintas-fitur: tanggal di koleksi karyawan tersimpan DUA TIPE.** Hitungan produksi 2026-08-06 pada `work_data.join_date`: **145 dokumen bertipe `date`, 61 bertipe `string`**. Kueri rentang Mongo (`$gte`/`$lt`) **hanya mengenai yang bertipe date**, karena perbandingan antar-tipe BSON terjadi di bracket terpisah — 30% data lolos **tanpa galat apa pun**. Penyaringan tanggal untuk koleksi ini karena itu wajib dikerjakan **di Go** lewat `resolveTanggalFleksibel` (`services/employee/tanggal.go`, diangkat dari endpoint ulang tahun yang sudah lebih dulu kena masalah sama pada `date_of_birth`).
Pengecualiannya kolom yang **selalu ditulis kode kita sendiri** — mis. `employee_resign.effective_date` yang lewat `normalisasiTanggalEfektif` — tipenya dijamin Date sehingga aman disaring di kueri.

**Akun pihak luar (vendor/mitra) — ✅ merged & live di prod** (PR [#956](https://github.com/bip-itteam-internal/bip-erp/pull/956), dilanjut #960/#961/#968/#971/#972). Prosedur operasionalnya: [[RUN - Onboarding Akun Eksternal (Vendor & Mitra)]].
- ⚠️ **Satu PR belum ikut ter-deploy.** Verifikasi biner produksi 2026-08-05 (`strings /service` di container `Employee-Service`): `gerbangAkunEksternal`, `companyIDAkun`, `muatKatalogRole`, `rapikanRoleModul`, `izinAkun` **ada**; `profilAkunLuar` (PR #972, `/me` akun luar) **tidak ada** — image dibangun 2026-08-04 15:18, commit-nya masuk 15:32. Frontend yang bergantung padanya sudah live, jadi vendor mendarat di dashboard karyawan berisi tanda hubung dan namanya tampil sebagai ID akun. Akses ke modul **tidak** terpengaruh. Perbaikan = rebuild + restart service, tanpa perubahan kode.
- Koleksi baru `external_account` (`employee_id`, `full_name`, `organization`, `email_address`, `phone_number`, `company_id`, `sponsor_employee_id`, `valid_until`, `purpose`, `metadata`) + field `account_type` di `system_authentication` (kosong = `employee`). Model & helper di `shared-library/models/employee/external_account.go`, handler di `services/employee/external_account.go` + `external_account_routes.go`.
- **Kredensialnya menumpang `system_authentication`**, bukan koleksi kedua — supaya login, SSO, device, dan `permission_sets` tetap satu jalur. Yang membedakan hanya `account_type`.
- **Tidak dijadikan record karyawan** karena model karyawan menuntut NIK, KK, agama, status pernikahan, golongan darah, NPWP, dua nomor BPJS, rekening bank, dan `fingerprint_id` — untuk orang luar sebagian besar bukan kebetulan kosong melainkan memang tak boleh dikumpulkan. Sebaliknya tiga hal yang justru dibutuhkan (asal organisasi, penanggung jawab internal, masa berlaku) tak punya tempat di `work_data`/`personal_data`.
- ⚠️ **Menutup fail-open `company_id`.** `resolveCompanyID` berpangkal pada `work_data`, dan akun luar tak punya `work_data`, jadi sebelumnya nilainya jatuh ke `DefaultCompanyID` dan tiap vendor diam-diam tercatat sebagai tenant **BIP** tanpa galat apa pun. `companyIDAkun` (`services/employee/external_account.go`) mengambilnya dari `external_account.company_id` untuk akun luar. Dasar isolasinya: [[ADR - 0029 Multi-Tenant Presensi Row-Level company_id]].
- **Masa berlaku ditegakkan di EMPAT jalur token**: `/auth/login`, `/auth/login-pin`, `/auth/login-biometrics`, dan `GET /auth/refresh`. Yang terakhir menentukan — refresh menerbitkan token baru tanpa kredensial, jadi menjaga tiga jalur login saja berarti akun kedaluwarsa bisa memperpanjang dirinya sendiri tanpa batas.

	> **Keempat jalur itu kini menanggung TIGA hal sekaligus**, dan tiap penambahan wajib menyentuh keempatnya: masa berlaku akun luar, `Permissions` (izin efektif dari paket akun + posisi, `permission_resolve.go`), dan sejak 2026-08-09 `SystemRoles` yang **diturunkan dari jabatan** lewat `peranEfektif` (`peran_dari_jabatan.go`, lihat [[ADR - 0043 Peran Sistem Diturunkan dari Jabatan]]). Memasang di sebagian jalur saja menghasilkan kegagalan yang paling sulit dilacak: orang melihat menu berbeda tergantung **cara ia masuk**. Komentar penjaga sudah ditinggalkan di `main.go` tepat di atas baris `Permissions`.

	Konsekuensi yang perlu diingat saat men-debug: `system_authentication.system_roles` di database **tak lagi menceritakan seluruh kebenaran**. Yang berlaku adalah hasil `peranEfektif`; bacalah lewat `GET /auth/refresh` atau panel **Peran vs Jabatan**, bukan dari koleksi mentah.
- **Fail-closed dua tempat**: `ValidUntil` kosong dianggap **kedaluwarsa** (bukan berlaku selamanya), dan `NormalizeAccountType` memetakan nilai tak dikenal ke `external` (bukan `employee`).
- **Tak bocor ke agregat karyawan** — diperiksa, bukan diasumsikan: `companyEmployeeIDs` berangkat dari `work_data.company_id` dan daftar karyawan dari `personal_data`; akun luar tak punya keduanya, jadi otomatis terkecualikan dari Total Karyawan, direktori, dan bagan organisasi.
- `GET|POST /master/external-accounts` · `PUT|DELETE /master/external-accounts/:employeeID` (`RequireITSupervisor` — menerbitkan kredensial untuk orang di luar perusahaan lebih dekat ke memberi kunci daripada ke mengelola data HR, jadi sengaja **lebih ketat** dari master data lain). Penanggung jawab wajib & diverifikasi ada di `work_data`; `DELETE` menonaktifkan (`is_active=false`), tidak menghapus. ID berprefiks `EXT-` mengikuti pola `master_company.code`; index unik `employee_id`.
- **Akun terbit lengkap dengan kata sandi** (PR #968): sandi acak dibuat **sebelum** apa pun ditulis — bila sumber acak bermasalah, tak ada dokumen yang terlanjur masuk. Dikembalikan **sekali** di respons `POST` (`temporary_password`); yang tersimpan cuma hash-nya. Sengaja tidak dikirim otomatis lewat email/WA: pembuatnya yang menyerahkan, sehingga jelas siapa yang bertanggung jawab bila bocor. Akun terbit `HasRegistered: true` (bukan alur onboarding karyawan, yang cuma ada di aplikasi mobile) dengan `Username` = ID akun dan `SystemRoles` = map **kosong**, bukan nil — nil ter-marshal jadi `null`, dan `system_roles: null` pernah melumpuhkan frontend di produksi lewat `JSON.parse("null")`.
- `POST /master/external-accounts/:employeeID/reset-password` — terbitkan ulang kata sandi. **Sengaja terpisah** dari `PATCH /account/reset` milik karyawan, yang menyetel `is_active: true` sebagai efek samping: memakainya untuk akun luar akan **menghidupkan kembali vendor yang sengaja dinonaktifkan**. Endpoint ini tak menyentuh `is_active` sama sekali. Tak ada jalur "lupa kata sandi" mandiri untuk akun luar — pemulihan lewat email berarti mempercayai kotak masuk di luar kendali perusahaan.
- ⚠️ **Fail-open ticket yang ditemukan menyusul & sudah ditutup.** Klaim awal bahwa akun luar tanpa paket "tak bisa berbuat apa-apa" **TIDAK benar**: fallback tier di service konsumen dipicu oleh klaim `permissions` yang **absen**, dan `TicketTierDefault` memberi izin staff kepada tier apa pun yang tak dikenal (termasuk tier kosong) — jadi tiap akun vendor diam-diam mendapat **6 izin ticket** (`view`, `create`, `comment`, `attach`, `checklist.manage`, `status.update`) dengan reach `own`. Terverifikasi lewat uji, bukan pembacaan kode. Ditutup `izinAkun` (`services/employee/permission_resolve.go`) yang **selalu** mengisi klaim akun luar dengan penanda tanpa-hak `account.$type.external`, sehingga klaimnya jadi ADA-tapi-kosong dan setiap fallback yang berpatokan "klaim absen" otomatis tertutup — termasuk modul yang belum ditulis. Menambal `TicketTierDefault` saja akan menutup lubang hari ini dan membiarkan lubang sama terbuka di modul berikutnya. Dikunci uji di **kedua** sisi: `permission_akun_luar_test.go` (klaim terbit tak kosong) + `akun_luar_gate_test.go` di task-management (konsumen benar-benar menolak). Jalur karyawan sengaja tak tersentuh — klaim karyawan tanpa paket tetap kosong, kalau tidak seluruh fallback tier mati sekaligus.
- **UI: tab Akun Eksternal di `/hris/master-data`** (erp-frontend PR [#777](https://github.com/bip-itteam-internal/erp-frontend/pull/777)) — daftar, buat, perpanjang, nonaktifkan. Ditaruh sebagai tab (bukan halaman sendiri) mengikuti Kelola Perusahaan yang juga digerbang IT tapi tinggal di area HRIS. Statusnya dibedakan **tiga**: nonaktif / kedaluwarsa / aktif, karena dua yang pertama sama-sama menutup akses tapi pemulihannya beda. Aturan kedaluwarsa disalin persis dari backend termasuk "tanggal kosong = kedaluwarsa"; melonggarkannya membuat layar menampilkan Aktif untuk akun yang ditolak saat login.
- **UI hak akses & kredensial** (erp-frontend, lanjutan #777): dialog **"Hak Akses Vendor"** dengan **dua tab** — *Paket Hak* & *Role Modul*. Dipisah tab, bukan ditumpuk satu daftar: keduanya sumbu yang benar-benar berbeda, dan menumpuknya membuat orang menyangka mencentang di satu tempat sudah cukup padahal modul tujuannya membaca yang lain. Dipasang di layar ini, bukan di panel Assign pada tab Permission Sets, karena panel itu mengisi pilihannya dari daftar karyawan (`personal_data`/`work_data`) sehingga tak pernah memuat akun luar. Ditambah dialog **"Kredensial Akun Vendor"** (sekali tampil, dengan tombol salin) dan tombol **Terbitkan Ulang Kata Sandi**.
- **Beranda & sidebar khusus vendor** (erp-frontend): dashboard karyawan menampilkan Kehadiran/Jadwal/Pengumuman yang semuanya berangkat dari data tak dimiliki akun luar — layar berisi deretan tanda hubung terbaca sebagai aplikasi rusak, bukan sebagai batas yang disengaja. Diganti `VendorDashboard` (organisasi, penanggung jawab, masa berlaku, status akses); saat belum punya hak, petunjuknya menyebut **nama** penanggung jawabnya, bukan "hubungi admin". Sidebar: akun luar hanya melihat Dashboard dari grup `erp` — item Manajemen Tugas & Onboarding lolos karena modul `erp` bertanda publik (aturan untuk karyawan yang belum diberi role, bukan untuk orang luar) lalu semuanya ditolak backend, dan menu yang tak satu pun bisa dibuka lebih buruk daripada tak ada menu. Status akun luar dibaca dari `account_type` di profil, **bukan** ditebak dari awalan `EXT-`. ⚠️ Keduanya bergantung PR #972 yang belum ter-deploy.
- **Daftar membawa status kredensial** (PR [#961](https://github.com/bip-itteam-internal/bip-erp/pull/961), stacked di atas #960): `is_active`/`has_registered` digabung **saat baca** dari `system_authentication` lewat tipe respons `AkunLuarDenganStatus`, sengaja TIDAK diduplikasi ke `external_account` — dua salinan kebenaran yang sama pasti menyimpang, dan yang dipercaya saat login tetap `system_authentication`.
- ✅ **Pemberian hak akses — dua sumbu, endpoint terpisah** (PR #968 & #971; menggantikan catatan lama "role/permission ditunda"). Akun baru tetap terbit **tanpa hak modul apa pun**; hak dipasang eksplisit lewat salah satu dari dua sumbu, dan **memilih sumbu yang salah tidak menghasilkan galat apa pun** — vendor cuma tetap tertolak:
  - `GET|PUT /master/external-accounts/:employeeID/permission-sets` → `permission_sets` (modul ber-RBAC: ticket, payroll, finance, monitoring, procurement). Key di luar `master_permission_set` ditolak: paket fantom tak pernah menghasilkan izin tapi tampil terpasang di layar, dan pemasangnya menyangka vendor sudah bisa bekerja.
  - `GET|PUT /master/external-accounts/:employeeID/system-roles` → `system_roles`, untuk modul yang menggerbang di situ — **warehouse** salah satunya. Tanpa endpoint ini vendor yang butuh modul semacam itu tak bisa diberi apa-apa lewat layar mana pun.
- **Tiga hal yang sengaja lebih ketat daripada jalur karyawan** (`PATCH /account/roles`/`/account/permission-sets`): gerbangnya `RequireITSupervisor` (bukan `RequireITStaff`); akun diperiksa milik perusahaan pembaca (jalur karyawan menegakkannya lewat `work_data` yang tak dimiliki akun luar); dan key modul **beserta nilai role**-nya divalidasi ke master data — jalur karyawan menyimpan apa pun yang dikirim, sehingga role salah ketik tersimpan diam-diam lalu tak pernah cocok dengan gerbang mana pun. `rapikanRoleModul` + `muatKatalogRole` (`external_account_routes.go`) menyalin aturan penimpaan `master_department` → `master_system_role` persis seperti pengisi dropdown di layar; **katalog yang gagal dibaca menolak segalanya**, bukan meloloskan segalanya. Role `group` (admin Bharata Group) dibuang paksa — ia membuka override lintas-perusahaan, kebalikan dari maksud akun yang dikurung ke satu tenant.
- **Berlakunya setelah login ulang.** Izin dirakit saat token **terbit**, jadi perubahan di kedua endpoint baru terasa setelah vendor login ulang atau token 72 jamnya diperbarui — sama seperti permission-set karyawan, bukan perilaku khusus akun luar. Respons menyebutkannya eksplisit.

**Penerbit penanda "menu terkunci"** (`menu_terbatas.go`, branch `feat/oneForAll`, **sudah merge** — berkasnya ada di `origin/main` per 2026-08-10) — service ini juga yang memutuskan sebuah **menu terbatas** sedang aktif atau tidak. Saat token terbit, `izinAkun` menanyakan apakah paket menu (mis. `menu_finance_laporan`) sudah dipasang ke **minimal satu** akun atau posisi; bila ya, penanda `$menulock.<kunci>` ikut ditempel ke klaim **setiap orang**. Fakta itu global — tak bisa disimpulkan dari klaim satu orang — sehingga harus dihitung di sini, bukan di konsumen. Hitungannya di-cache 60 detik; keduanya (akun & posisi) dihitung karena hak datang dari dua arah.

⚠️ Penanda **hanya menumpang pada klaim yang sudah tak kosong**, dan itu bukan kehati-hatian berlebih: `effectiveTicketPermissions` di task-management memutuskan fallback tier dari "klaim **ada**", bukan "klaim memuat izin ticket" seperti payroll/monitoring/procurement. Menempelkan penanda ke karyawan tanpa paket akan mengubah klaimnya dari kosong jadi berisi dan **melenyapkan seluruh hak tiket mereka tanpa pesan apa pun** — kelas kegagalan yang sama yang dimanfaatkan `account.$type.external` di atas, hanya kali ini korbannya karyawan. Dikunci uji `menu_terbatas_test.go` (termasuk sifat malasnya: klaim kosong tak boleh sampai menyentuh Mongo). Konsekuensinya ada celah pada akun tanpa paket; besarnya belum terukur — lihat [[ADR - 0039 Menu Terbatas Default Terbuka sampai Di-assign]].
- **`/me` menjawab akun luar dari data pendampingnya** (PR #972, `profilAkunLuar`): seluruh jalur `/me` berangkat dari `work_data`/`personal_data` yang tak dimiliki vendor, sehingga jawabannya kosong dan layar jatuh ke ID akun. Cabang akun luar ditaruh **paling atas**, sebelum cabang `collection` mana pun. Nama field meniru profil karyawan supaya layar lama tetap bekerja; tambahannya `account_type`, `organization`, `sponsor_name`, `valid_until`. `department`/`position` sengaja **dibiarkan kosong** — mengisinya dengan nama vendor membuat orang luar terbaca seperti karyawan di layar mana pun. ⚠️ Belum ter-deploy, lihat catatan deploy di atas.
- Menonaktifkan akun **tidak** memutus token yang sudah beredar (JWT TTL 72 jam, revoke masih placeholder — [[CORE - SSO Flow]] §Catatan & Keterbatasan).
- ⚠️ **Jalur hak-dari-posisi mati untuk vendor**: `positionSetKeys` langsung berhenti bila `work_data.Department` kosong, dan akun luar tak punya `work_data`. Hak vendor hanya bisa datang dari pemasangan eksplisit per-akun (`permission_sets` atau `system_roles`). Menyusul dari itu, akun vendor **tak muncul** di layar "Siapa Boleh Apa"/pengecualian karena `companyEmployeeIDs` berpangkal pada `work_data` — blind spot audit yang justru jadi alasan layar itu dibuat. **TBD.**

**Self-Service (`/me`)**
- Profile, kpi-score, vacation, payroll-approx
- Photo get/upload

⚠️ **`GET /me/payroll-approx` kini YATIM: nol konsumen kode** (diukur 2026-09-01 dengan grep seluruh workspace). Satu-satunya pemakainya adalah [[APP - MyBharata]], dan bagian "Perkiraan" di sana dibuang bersama perombakan halaman Slip Gaji; [[APP - Web ERP]] tak pernah memakainya. Endpointnya **sengaja tidak dihapus** — itu keputusan tersendiri yang belum diambil — tetapi dicatat di sini supaya pembaca berikutnya tak menyangka ia masih melayani siapa pun.

Yang **tidak** ikut mati: `/payroll-supplement` di [[Microservices - Attendance Service]], karena [[Microservices - Payroll Service]] memanggilnya **langsung** saat menghitung run, bukan lewat proxy `payroll-approx` ini.

**Vacation**
- `GET /vacation`, `POST /vacation/quota`
- `POST /vacation/decrement` — dipanggil oleh attendance
- ⛔ **`work_data.vacation.quota` BUKAN jatah tahunan**, melainkan **sisa setelah Cuti Bersama dipotong** (12 hari Pasal 15 dikurangi 7 hari kerja Cuti Bersama 2026 = 5). Pemotongan itu dikerjakan HR di luar sistem lalu hasilnya diketik. Jangan menjumlahkannya dengan jatah Cuti Bersama, dan jangan menyimpulkan perusahaan hanya memberi 5 hari.
- ⚠️ **Tak ada penerbit hak sama sekali.** `POST /vacation/quota` (manual, per orang) satu-satunya penulis `quota`; `cronResetAnnualLeave` hanya menyetel `used=0` bagi yang `quota > 0` sehingga karyawan berkuota 0 dilewati selamanya. Akibatnya 15 karyawan bermasa kerja >12 bulan tak bisa mengajukan cuti (ditolak 400 `VACATION_QUOTA_UNSET` di attendance). Rencana penggantinya: [[ADR - 0061 Jatah Cuti Tahunan Terbit Otomatis di Ulang Tahun Kontrak]] (koleksi baru `vacation_ledger`, `work_data.vacation` turun jadi salinan ringkas). Detail domain: [[HRIS - Leave Request]] §Kuota Cuti Tahunan.
- ⛔ **Tanggal di koleksi karyawan bertipe CAMPUR, dan sumbernya masih hidup.** Diukur 2026-08-29: `work_data.join_date` 114 `date` / 95 `string`, `contract_ending` 158 / 51, `personal_data.date_of_birth` 196 / 13. MongoDB membandingkan antar-tipe di bracket terpisah, jadi filter rentang (`$gte`/`$lt`) **melewati dokumen bertipe string tanpa satu pun galat**.

	Penyebabnya bukan data lama: 31 dokumen berpindah dari `date` ke `string` antara 2026-08-06 dan 2026-08-29 sementara koleksinya cuma tumbuh 3. Rantainya — frontend mengirim JSON (tak punya tipe tanggal) → `orchestrator/hris/partial_update.go` meneruskan `map[string]any` apa adanya → employee-service men-`$set` map itu mentah lewat `buildUpdateSet` dan dua rute `POST /create/*`. Berkas orchestrator itu sendiri benar; ia dibuat 2026-07-30 untuk memperbaiki bug lain (round-trip struct menimpa `company_id`, `vacation`, `photo` dengan nilai nol), hanya tidak mengurus tipe tanggal.

	Penawar BACA sudah ada dan tetap dipakai: `resolveTanggalFleksibel` (`services/employee/tanggal.go`), yang membaca BSON Date maupun string. Penyaringan tanggal untuk koleksi ini karena itu dikerjakan **di Go, bukan di kueri** — lihat `resign.go` dan `kpi_sumber_turnover.go` yang memakai struct bayangan ber-`JoinDate interface{}`.

	Perbaikan sumbernya ada di branch `feat/employee-koersi-tanggal` (belum merge): koersi ke `time.Time` sebelum `$set` di empat pintu berbasis map, plus migrasi boot-time. Lihat [[ADR - 0061 Jatah Cuti Tahunan Terbit Otomatis di Ulang Tahun Kontrak]] §10.

	`employee_contract.start_date` bersih (**225 dari 225** `date`), jadi pakai itu bila butuh tanggal yang bisa langsung dibandingkan di kueri.

**Contract / BPJS / Analysis**
- Endpoint contract, BPJS, dan analysis (`RequireHRISStaff`)
- **Kontrak kerja (riwayat PKWT/PKWTT)**: rute `/contract*` (daftar, ringkasan, riwayat, tambah, koreksi, lampiran PDF) dirinci di [[API - Employee Service]] §Kontrak Kerja; rencana e-signing di [[HRIS - Kontrak Kerja Elektronik (e-Signing & e-Meterai)]]. Kode: `contract.go`, `contract_file.go`, `contract_summary.go`, `contract_migrate.go`.
	- Koleksi `employee_contract` adalah sumber kebenaran; perpanjangan = dokumen baru, status dihitung saat baca. Kontrak pertama dibentuk di dalam transaksi create-employee (`kontrakPertama`, `func.go`) dan `work_data` diturunkan darinya; karyawan lama dibuatkan satu kontrak `migrated: true` oleh migrasi saat boot.
	- ⛔ **Salinan `work_data.employment_type` + `contract_ending` hanya ditulis modul kontrak** (`segarkanSalinan`). Tiga pintu tulis `work_data` lain membuang keduanya secara senyap lewat `fieldKontrak` (`partial_update.go`): `buildUpdateSet` (`PUT /update/:employee_id/work` & `/personal`), `POST /create/:employee_id/work`, dan `workDataSetTanpaFieldTerkunci` (dipakai `executeEmployeeUpdateTransaction`). Pintu tulis `work_data` baru wajib melewati penjaga yang sama. Dibuang senyap, bukan ditolak 400, supaya form lama tak mati selama jendela rilis BE-sebelum-FE. Kepemilikan: [[REF - Kepemilikan Data]].
	- ⚠️ Nomor kontrak (`nomorKontrak`) diurut **per karyawan**, jadi bisa sama antar-karyawan; formatnya belum dikonfirmasi HR.
	- ✅ **Pengingat kontrak habis: merged 2026-09-11 (bip-erp PR #1851), live DEV 2026-09-11 dan PROD 2026-09-12** (jalan PROD pertama 2026-09-13 07:00 WIB belum dibaca). `contract_pengingat.go`, cron harian 07:00 WIB (`cronPengingatKontrak`, didaftarkan di `cron.go` dengan galat `AddFunc` diperiksa, dibungkus `defer recover`): ringkasan inbox `reminder` per supervisor HR per perusahaan dan pesan H-14 ke atasan langsung. Jejak kiriman di koleksi baru `employee_contract_pengingat` (index unik `contract_id + tahap + penerima_id`, dibuat `ensurePengingatKontrakIndexes` di `RegisterContractRoutes`); hanya kiriman yang berhasil yang dicatat, lewat `kirimInboxKategoriGalat` (`warning_notify.go`), varian `kirimInboxKategori` yang mengembalikan galat. Env **`PENGINGAT_KONTRAK_SAAT_BOOT=true`** menjalankan job sekali saat boot, **khusus verifikasi DEV** (dibaca saat container dibuat, jadi `--force-recreate`). Perilaku lengkapnya: [[HRIS - Kontrak Kerja Elektronik (e-Signing & e-Meterai)]] §Pengingat Kontrak Habis.
	- **Aturan status kontrak disatukan (PR #1851)**: `klasifikasiKontrak` menghitung dalam tanggal WIB dan dipakai daftar, ringkasan, riwayat (`statusKontrak` tinggal adaptor), serta pengingat. Hari berakhir berstatus `ending`, `expired` mulai keesokan harinya; `ending_duration` di `GET /contract` ikut tanggal WIB.
	- `penerimaHRPerusahaan` (`mutasi_notify.go`) tidak menyaring `is_active`. Pengingat kontrak menyaringnya sendiri; pemberitahuan mutasi lintas perusahaan belum, jadi bisa sampai ke supervisor HR yang sudah keluar.
	- ⚠️ **Dokumen PKWT masa transisi (T18): selesai di branch `feat/employee-dokumen-pkwt` 2026-09-12, belum merged.** `contract_pkwt.go` (isi dokumen + daftar kekurangan, fungsi murni), `contract_pkwt_gaji.go` (klien payroll), `contract_pkwt_teks.go` (teks template HR), `contract_pkwt_pdf.go` (`github.com/go-pdf/fpdf` v0.9.0, dependensi baru service ini), `contract_pkwt_handler.go` (tiga rute `/contract/:id/pkwt*`, didaftarkan di akhir `RegisterContractRoutes` bersama `ensureDokumenKirimPKWTIndexes`). Env baru **`PAYROLL_MODULE_URL`** dibaca `os.Getenv` langsung dan **sengaja tidak masuk `InternalURL`** yang divalidasi `ValidateInternalURL` saat boot: kosong berarti kolom gaji kosong dan HR diberi tahu, bukan service mati. Izin `payroll.view` dinilai di sini (`common.IzinPayrollEfektif`) sebelum memanggil payroll, karena `InternalRequest` tidak meneruskan `BIP-Permissions`. Jejak kiriman di koleksi `employee_contract_dokumen_kirim`. Galat transport ke notification dilepas dari URL-nya sebelum di-log, karena URL itu memuat `?key=` layanan. Rute: [[API - Employee Service]] §Kontrak Kerja; perilaku: [[HRIS - Kontrak Kerja Elektronik (e-Signing & e-Meterai)]] §Masa transisi.
- `GET /bpjs` — nomor kepesertaan KS & KT per karyawan. Dua mode: **single** (`?employee_id=`, respons datar) dan **list** (paginasi + `search` nama/NIK + filter `department`). Hanya karyawan **aktif** (`system_authentication.is_active`) dan dibatasi perusahaan pemakai. **Hanya baca** — tak ada endpoint tulis; nomor BPJS diubah lewat `PATCH /work-data` milik [[CORE - HRIS Orchestrator]] yang bersifat partial.
	- Sempat **yatim**: lengkap sejak lama tapi tak punya pemanggil sama sekali. Kini dikonsumsi menu **Kelola BPJS** di [[APP - Web ERP]] (dikoreksi 2026-08-06).
- Device/browser management + FCM token

**Feed Lintas-Service**
- `GET /list?type=fcm-token|supervisor|employee|phone-number|vacation|host-live-candidate|live-support-candidate` — dipakai attendance & notification, dan MyBharata untuk pemilih sesi live. ⚠️ Diralat 2026-09-17 ke daftar di `services/employee/list_type_pesan.go` (`origin/main`): versi sebelumnya baris ini menyebut `department`, yang **tak pernah punya case** di switch handler. Itu ketidakcocokan yang sama persis dengan yang dicatat komentar di sana sebagai "menyesatkan pemanggil selama berbulan-bulan".
	- `host-live-candidate`: pemegang posisi `posisiHostLive` (Host Live **dan** Live Support), untuk pemilih host/co-host.
	- `live-support-candidate` (✅ PROD 2026-09-17, bip-erp #1927): pemegang posisi `posisiLiveSupport` saja, untuk pemilih "Live Support yang bertugas" ([[ADR - 0101 Kesiapan Siaran Dicatat Host saat Mulai sebagai Dasar KPI Live Support]]). Sengaja **tidak** memakai ulang daftar di atas walau daftar itu sudah memuat Live Support: memakainya membuat host bisa memilih host lain sebagai pendukung. Kedua daftar berbagi `filterPosisi` dua lapis (`position_key` kanonik atau `position` mentah, `host_live_kandidat.go`).
	- ⚠️ `semuaTypeValid` di `list_type_pesan_test.go` **ditulis tangan**. Terukur saat `live-support-candidate` ditambahkan: membuang nilai itu dari kedua pesan produksi membuat test `MemuatSemuaNilaiType` tetap hijau karena daftarnya belum memuatnya. Case baru wajib ditambahkan di sana juga.
	- ⛔ **`platform=web_browser` dulu tidak menyaring apa pun** (2026-08-20, PR [#1311](https://github.com/bip-itteam-internal/bip-erp/pull/1311)). Cabang mobile menyaring `device.Status == "active"`, cabang browser tidak — dan `WebBrowser` bahkan **tak punya field `Status`** meski `/account/forget-device` sudah lama menulis `web_browser.N.status = "inactive"` ke field yang tak pernah dibaca siapa pun. Selama tak ada yang mengirim push ke browser akibatnya nol; begitu push web menyala, **mencabut sebuah browser tidak menghentikan notifikasi ke browser itu**, termasuk browser bersama dan perangkat karyawan yang sudah keluar.
	- Penyaringnya **`Status != "inactive"`, BUKAN `== "active"`**. Seluruh baris yang tersimpan sebelum field itu ada tidak punya `status`; menuntut `"active"` akan mematikan push untuk SEMUA orang tanpa gejala apa pun selain "kok sepi". Dipusatkan di `WebBrowser.Aktif()` supaya tak ada pembaca yang menafsirkannya sendiri, dan kedua arahnya dikunci test.
- `POST /web-browser` — daftarkan/perbarui browser (upsert per `device_id`) berikut `fcm_token`-nya; dipakai erp-frontend saat orang menyalakan notifikasi desktop.
	- **Daftarnya dibatasi 10 terbaru** lewat update pipeline `$ifNull` + `$sortArray` + `$slice`, satu operasi atomik. Komentar di kode sendiri sudah lama mengakui daftarnya "akan membengkak", dan token browser jauh lebih cepat busuk daripada token ponsel (hapus data situs, profil baru, incognito).
	- ⚠️ **Dua bug yang membuat endpoint ini mustahil berhasil**, keduanya diperbaiki PR [#1325](https://github.com/bip-itteam-internal/bip-erp/pull/1325) dan keduanya ditemukan dengan menjalankannya di dev, bukan dari test:
		- Menambah field **string** apa pun ke `WebBrowser` tanpa `validate:"optional"` langsung membuat seluruh pendaftaran dibalas **400**, karena struct ini merangkap bentuk request dan `verboseCheckFields` menolak setiap string kosong. Berlaku untuk field baru berikutnya juga.
		- `web_browser` bernilai **`null`** (bukan array) di banyak dokumen, dan `$push` menolaknya dengan **500** yang tak dicatat di log mana pun. Diuji berdampingan: `$push` polos versi lama pun gagal, jadi ini bug lama — fitur push cuma yang pertama memicunya.
- `GET /sync/work-schedules` — dipakai cron attendance; kini **enrich `department`** (dari `work_data`) untuk guard swap same-department ([[ADR - 0006 Swap Jadwal Same-Department]])
	- **`roster_enabled` hidup di sini dan mengalir SATU ARAH** (PR #1012, [[ADR - 0036 Roster Harian Menimpa Jadwal Dasar]]). `SyncCollection` melakukan `DeleteMany({})` lalu `InsertMany` seluruh koleksi tiap 30 menit, jadi menyetel saklar itu di database attendance **akan terhapus pada tik berikutnya tanpa galat**. Setel di database employee.
	- Dokumen `work_schedule` di sini **tidak menyimpan** `department` maupun `position` — keduanya di-enrich saat dibaca. Menyaring dengan keduanya (mis. untuk menyalakan roster massal) mencocokkan nol dokumen; turunkan `employee_id` dari `work_data` lebih dulu.
	- `PUT /internal/schedule/factory-update` me-`ReplaceOne` dokumen utuh dari keluaran factory. `pertahankanSaklarRoster` membaca dokumen lama dan membawa `roster_enabled` maju; tanpa itu saklar hilang permanen tiap HR memindahkan jadwal, karena **tidak ada** jalur yang mengisinya ulang (beda dari `department`/`position`).
- `GET /qr/:employee_id`, `GET /check-unique/:field/:value`, `GET /data-type/:dt`
- MinIO uploads: `POST /upload`, `POST /upload/multiple`
- Cron aktif
- `GET /internal/mpp-vacancies` (`mpp_vacancies.go`, gerbang `requireInternalKey`): posisi kosong dari `employee_resign` (SELURUH kategori, SELURUH perusahaan) untuk MPP di [[Microservices - Recruitment Service]]; endpoint tak menerima identitas pemanggil, jadi penyaringan hak akses per perusahaan dikerjakan di sisi recruitment lewat field baris, bukan di sini. ⚠️ Tiap baris kini membawa **`company_id`** (`work_data.company_id` orang yang keluar; kosong dianggap BIP via `perusahaanAtauDefault`) dan `level_key` dicari di `master_department` PERUSAHAAN orang itu (bip-erp #1853, live prod 2026-09-12). Tanpa field ini, posisi kosong perusahaan lain terbaca BIP dan rencana tenaga kerjanya salah alamat (lihat [[ADR - 0092 Rekrutmen Lintas Perusahaan lewat Paket Izin]] §Yang dibayar: urutan deploy mengikat, employee-service tak boleh tertinggal dari recruitment-service). ⚠️ **Merged 2026-09-17 (bip-erp [#1961](https://github.com/bip-itteam-internal/bip-erp/pull/1961); deploy belum diverifikasi)**: `kumpulkanVacancies` kini menyaring `saringResignPosisiKosong()` = status `scheduled` + `applied` (resign `cancelled` tak lagi jadi posisi kosong; terjadwal sengaja ikut supaya HR merencanakan pengganti sebelum orangnya keluar, berbeda dari ringkasan resign dan KPI turnover yang hanya `applied`), `lengkapiResign` menempelkan `resign_id`, `status`, `resign_dibuat_pada` (dari timestamp `_id`), dan `tanggalEfektifWIB` memformat tanggal efektif di zona WIB (tanggal disimpan tengah malam WIB lewat `normalisasiTanggalEfektif`, dibaca driver sebagai UTC 17:00 sehari sebelumnya). Konsumennya satu: recruitment-service, yang mencocokkan keputusan HR per resign.

## Belum Diimplementasikan / Catatan

- Tidak ada stub berarti — ini service paling lengkap dalam ekosistem bip-erp.
- **Allowlist proyeksi agregat menjebak.** Respons detail karyawan dibangun lewat `JoinCollectionSpecify` dengan daftar field eksplisit di `services/employee/aggregate_projection.go`; field yang tidak disebut di sana **tidak pernah sampai ke frontend**, dan responsnya tetap tampak wajar sehingga tak ada galat yang muncul. `roster_enabled` sempat gagal total karena ini (PR #1012). Endpoint LIST memakai mekanisme berbeda (refleksi tag bson) sehingga tidak terkena.
- **Cakupan supervisi: pakai versi ketat untuk gerbang TULIS.** `common.SupervisedDepartments` **jatuh ke `[departemen sendiri]` untuk siapa pun** bila klaimnya kosong, sehingga gerbang yang memakainya meloloskan setiap karyawan untuk departemennya sendiri, bukan hanya supervisornya. `common.SupervisedDepartmentsStrict` / `CanManageDepartment` (PR #1012) membaca header mentah tanpa fallback. Sejak PR itu `resolveSupervisedDepartments` juga berhenti mengosongkan cakupan berukuran 1, sehingga supervisor berdepartemen tunggal **perlu login ulang** (TTL token 72 jam) agar klaimnya terbit. `ApproveHandover` di [[Microservices - Inventory Service]] masih memakai pola lama dan berlubang dengan cara yang sama.
- Department CRM sudah dihapus (di-merge ke BeautyHacks/Kyura); role warehouse belum aktif.
- **Departments, positions, dan system roles** sudah dimigrasikan dari hardcoded source ke **MongoDB master data** (`master_department`, `master_system_role`). Dapat dikelola via CRUD endpoint atau frontend `/hris/master-data`.
- `common.Roles` (tipe system_roles di JWT/MongoDB) diubah dari Go struct dengan fixed field menjadi `map[string]Role` — mendukung penambahan department/role tanpa ubah kode. Format serialisasi JSON/BSON tidak berubah (backward compatible).
- Backward compat string constants (`DeptHR`, `DeptGA`, dll) tersedia di `shared-library/models/employee/master_data.go` untuk consumer yang compare department name.
- **Multi-perusahaan (tenant)** ([[ADR - 0029 Multi-Tenant Presensi Row-Level company_id]]): **fondasi** `WorkData.CompanyID` (di-default BIP saat create via `defaultWorkCompany`), master `Company` (`master_company`) + CRUD `/master/companies` (⚠️ **koreksi**: `GET` tanpa gerbang peran, `POST` `RequireCentralAdmin`, tak ada `PUT`/`DELETE`, bukan `RequireITSupervisor` seperti sempat tercatat di dok ini; diverifikasi `services/employee/master_data.go:354-363`), `resolveCompanyID` + join `master_company` di `/me` (objek `company{key,name,code}`) dan di respons login `new_user` (sapaan onboarding mobile). `/list?type=fcm-token` dan `/list?type=supervisor` menyaring `?company_id=`.
	- ⚠️ **Override berizin untuk data referensi rekrutmen, sempit** (`perusahaanReferensi`, `services/employee/perusahaan_referensi.go`; bip-erp #1853, live prod 2026-09-12). `EffectiveCompanyID` di atas TETAP hanya menghormati `?company=` untuk admin pusat; klaim "berlaku di semua keadaan" yang sempat tercatat untuknya di dok ini sudah tidak akurat sejak varian ini ada. `perusahaanReferensi` = `EffectiveCompanyIDDenganIzin(c, recruitment.cross_company)`, dipakai HANYA di cabang baca `/data-type/position` (mode `all`/`grouped`/per-department), `/data-type/headcount`, `/data-type/department-groups`, `/data-type/department`, dan `/list?type=employee`. Jumlah pemanggil per berkas dikunci `TestPerusahaanReferensiHanyaDiCabangBacaRekrutmen` (7 di `main.go`, 1 di `perusahaan_referensi.go`); menambah pemanggil baru berarti memperluas wewenang izin itu ke data yang belum diputuskan, jadi keputusan, bukan perapian. Direktori lengkap, KPI, kontrak, dan seluruh jalur TULIS tetap terkunci `EffectiveCompanyID`/`CompanyID` biasa.
	- ⛔ **`/list?type=employee` untuk perusahaan LAIN yang dibuka izin itu (bukan admin pusat, bukan perusahaan sendiri) disempitkan lagi** (`gerbangDaftarLintasIzin` + `sempitkanProyeksiLintasIzin`, berkas yang sama): query `role_system`/`role_value`/`with_supervisor`/`include_external`/`include_inactive` (nilai apa pun) ditolak **403**; `username`/`phone_number`/`photo` dikosongkan dari proyeksi (kunci tetap ada, nilai kosong). Ditolak, bukan diabaikan: filter yang diabaikan diam-diam membalas daftar lebih luas dari yang diminta, dan akun luar dimuat per perusahaan pemakai sehingga akan tercampur ke daftar perusahaan lain. Admin pusat dan daftar perusahaan sendiri tak berubah. Kontrak lengkap: [[API - Employee Service]] · keputusan: [[ADR - 0092 Rekrutmen Lintas Perusahaan lewat Paket Izin]].
	- **Direktori & agregat karyawan: ✅ ter-scope (F2-A, PR #659).** Helper `companyEmployeeIDs(c)` (`services/employee/company.go:19`) membatasi jalur yang berangkat dari `personal_data`/`system_authentication` (dua koleksi itu tak punya `company_id`, jadi sumber kanonis = `work_data.company_id`). Cakupan: `/v2/internal/aggregate/employees`, `/summary`, `/it`, `/list?type=employee`, `/internal/export/all`, `/view`, plus sub-list sensitif headcount, KPI, `kpi/dashboard`, contract, BPJS, analysis, `/birthdays`, vacation. Index `{company_id, employee_id}` + `master_department.company_id` dipasang idempoten saat boot (`ensureTenantIndexes`) sehingga lookup jadi covered query.
	- **Departemen per-perusahaan** (`master_department.company_id` + scope `/data-type/department`, `/position`, `/master/departments` via `EffectiveCompanyID`): **live di main via PR #652**, memperbaiki PR #649 yang sempat ter-orphan (merged ke branch stacked yang sudah mati). Departemen & posisi kini per-perusahaan (migrasi backfill BIP); perusahaan baru mulai kosong. Supervisi/RBAC (`allMasterDepartments`, `system-list`) sengaja **tetap global** (config BIP-central).
	- **Katalog jadwal data-driven (PR #661)**: `resolveScheduleType` membaca `company_work_schedule` / `company_group_rotation` yang di-sync ke `employee_db` (fallback katalog hardcoded), dipakai di create-employee + `PATCH /employees/work-schedule`. Tanpa ini, `schedule_id` milik perusahaan lain (mis. `ELT-REGULAR`) ditolak "invalid schedule type".
	- **Jalur update parsial diperbaiki (2026-07-30, branch `fix/employee-partial-update` — belum merge)**: `PUT /update/:employee_id/work` dan `/personal` menerima body sebagai map lalu men-`$set` isinya, jadi pemanggil wajib mengirim HANYA field yang diubah. [[CORE - HRIS Orchestrator]] dulu melanggarnya (round-trip struct) sehingga tiap simpan form menimpa `company_id`, `vacation`, dan `photo` dengan nilai nol. Kini orchestrator meneruskan map, dan audit distempel di sini lewat dot-notation `metadata.updated_at`/`updated_by` (`services/employee/partial_update.go`) supaya `metadata.created_*` tak tertimpa. Detail: [[ADR - 0029 Multi-Tenant Presensi Row-Level company_id]] §Perbaikan jalur edit karyawan.
	- **Sisa catatan**: `executeEmployeeUpdateTransaction` memakai `$set: update.WorkData` **tanpa** `defaultWorkCompany` (`services/employee/func.go:205`) sedangkan `WorkData.CompanyID` ber-tag `bson:"company_id"` tanpa `omitempty`, jadi payload update yang tak menyertakan `company_id` menimpanya jadi `""` (jalur create sudah aman). **Jalur ini terpisah dari rute update parsial di atas dan BELUM diperbaiki.** Pengelompokan KPI (`kpi_group`) & supervisor-lookup masih berbasis **nama departemen**, jadi dua perusahaan dengan nama departemen sama bisa ter-merge.
	- **Korupsi tipe BSON (belum diperbaiki)**: karena rute update parsial men-`$set` nilai mentah dari JSON, tanggal tertulis sebagai **string** dan angka sebagai **double**, padahal model memakai `time.Time`/`int`. Terverifikasi DB dev 2026-07-30: **31 dari 179** `work_data` (`join_date`, `contract_ending`, `fingerprint_id`, `metadata.created_at`) dan **6** `personal_data` (`date_of_birth`). Dokumen itu gagal didekode ke struct; belum ada normalisasi maupun migrasi.

## Dependencies & Integrasi

- **MongoDB** — penyimpanan utama; collections: `personal_data`, `personal_document`, `work_data`, `work_document`, `employee_contract`, `employee_contract_pengingat`, `employee_contract_dokumen_kirim` (T18, di branch), `work_schedule`, `company_work_schedule`, `system_authentication`, `external_account`, `kpi_score`, `company_holiday`, `master_department`, `master_system_role`, `master_company`, `master_job_level`, `employee_warning` (⚠️ belum merge), `legal_license`, `legal_contract`, `legal_dispute`, `rnd_registration`, `rnd_product`, `quality_capa`, `quality_incoming`, `quality_batch_release`, `procurement_contract`, `procurement_saving`. Lihat [[DB - Overview and Notes]].
- **MinIO** — client langsung untuk upload foto & dokumen.
- [[Microservices - Attendance Service]] — memanggil `POST /vacation/decrement`, mengonsumsi feed `/list` dan cron `/sync/work-schedules`.
- [[Microservices - Notification Service]] — mengonsumsi feed `/list` (fcm-token, supervisor, dll).
- [[Microservices - File Service]] — terkait penyimpanan dokumen/file.
- [[Microservices - Form Builder Service]]: dibaca sumber KPI yang menarik data form lewat `FORM_BUILDER_MODULE_URL` (lihat Registry sumber data KPI). ⚠️ `nilai_inspeksi_satgas` (PR [#1866](https://github.com/bip-itteam-internal/bip-erp/pull/1866) merged 2026-09-14, live di DEV; kodenya ada di biner `Employee-Service` dan `Form-Builder-Service` produksi 2026-09-15) menambah env `FORM_BUILDER_SERVICE_KEY`, bernilai sama di blok compose kedua service. ⛔ **Di produksi env itu belum diisi** (tak ada barisnya di `.env`, panjang 0 di kedua container; diukur 2026-09-15), jadi sumbernya gagal tertutup di sana.
- [[Microservices - Learning Service]]: dibaca sumber KPI `pelatihan` lewat `GET /kpi/pelatihan`, env `LEARNING_MODULE_URL` + `LEARNING_SERVICE_KEY` (bernilai sama dengan blok learning-service; terisi dan hash-nya cocok di kedua container produksi 2026-09-15).
- [[Microservices - Recruitment Service]]: dibaca sumber KPI `rekrutmen` lewat `GET /kpi/rekrutmen` + `GET /kpi/review-evaluasi`, env `RECRUITMENT_MODULE_URL` + `RECRUITMENT_SERVICE_KEY` (bernilai sama dengan blok recruitment-service). ⛔ **Belum di-deploy dan env-nya belum diisi** di DEV maupun produksi per 2026-09-18 (bip-erp PR [#1967](https://github.com/bip-itteam-internal/bip-erp/pull/1967) merged hari itu), jadi sumbernya gagal tertutup sampai kedua container naik dengan `--force-recreate`.
- [[Microservices - Payroll Service]] — dibaca dokumen PKWT masa transisi (T18, di branch 2026-09-12): `GET /employee-salary/:employeeId` + `GET /salary-components` lewat env `PAYROLL_MODULE_URL`, identitas HR pemanggil diteruskan.
- [[CORE - API Master Gateway]] — menerima PayloadJWT dari endpoint auth (SSO).
- [[CORE - HRIS Orchestrator]] — mengonsumsi aggregate, KPI, contract, BPJS, dan vacation.
- [[CORE - IT Orchestrator]] — mengonsumsi aggregate `/it` dan system-auth roles.

## Dokumen Terkait

- [[APP - MyBharata]]
- [[HRIS - Key Performance Index]]
- [[HRIS - Payroll]]
- [[IT - Background Jobs & Schedulers]] — cron service ini (sync schedule harian 04:00, reminder KPI tgl 1, reset cuti 1 Jan)
