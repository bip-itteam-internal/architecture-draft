## Deskripsi

*`hris-dashboard` ("One Bharata") adalah portal web internal ERP/HRIS terpadu Bharata Group — dashboard berbasis role untuk mengelola HR, attendance, finance/insentif, integrasi marketplace, IT, dan GA. Selain itu, web ini juga berperan sebagai **SSO Identity Provider** untuk aplikasi lain (mis. Task Manager).*

- **Stack**: Next.js 16 (App Router) + React 19 + TypeScript; shadcn/ui + Radix + Tailwind v4; TanStack Query + axios; react-hook-form + zod; recharts (grafik); leaflet (peta lokasi absensi); `@uiw/react-md-editor` (article); **react-i18next** (i18n dua bahasa → [[ADR - 0010 Internasionalisasi (i18n) Dua Bahasa]])
- **Path**: `erp-frontend` (repo `erp-frontend`), branch `feature/sso-task-manager-menu`
- **Status**: ✅ Implemented (aktif dikembangkan)
- **Multi-perusahaan** ([[ADR - 0029 Multi-Tenant Presensi Row-Level company_id]]): halaman **Kelola Perusahaan** `/hris/companies`, **pemilih perusahaan admin pusat** (`CompanySwitcher`, kini di **footer sidebar**, kirim `?company` ke presensi), **Buat Karyawan** pilih-perusahaan-dulu + prefix Employee ID ikut perusahaan + kandidat rekrutmen khusus BIP, **Kelola Departemen** per-perusahaan (BE PR #652 sudah live di main), **Kelola Shift + Rotasi + Mesin Fingerprint** per-perusahaan di `/hris/schedule` (pemilih perusahaan bersama; shift editor 7-hari, rotasi shift bergilir, mesin fingerprint gate-IT; BE #656/#658/#657, FE #501), **Kelola Network** (WiFi WFO) ikut switcher sehingga admin pusat bisa mendaftarkan WiFi kantor perusahaan lain (BE #663), **direktori & agregat karyawan** ikut perusahaan terpilih (F2-A, BE #659 + #660), dan **toggle "Bharata Group"** di form artikel untuk broadcast lintas-perusahaan (BE #662, khusus admin pusat).

## Autentikasi & Role

- Login `employee_id` + password → `POST /auth/login` (gateway); `token`, `system_roles`, dll disimpan sebagai cookie. Bearer token auto-attach; proactive refresh via `/auth/refresh` (<60 detik sebelum expiry); interceptor 401 retry sekali lalu redirect `/login`.
- **SSO Identity Provider** (mint one-time code via `POST /auth/sso/ticket`):
	- *Inbound handoff*: jika login dengan query `?redirect_url=...`, setelah login di-redirect balik ke app tujuan dengan `?code=...`
	- *Outbound launch*: ⚠️ **tak ada lagi tombol/menu peluncur** di sidebar. Yang tersisa di kode hanya penanganan `redirect_url` di `app/login/page.tsx` (allowlist: `NEXT_PUBLIC_SSO_ALLOWED_ORIGINS`, subdomain `bharatainternasional.com`, localhost, dan `NEXT_PUBLIC_TASK_MANAGER_APP_URL`), jadi app tujuan yang harus mengarahkan user ke ERP lebih dulu
- Role dari cookie `system_roles` (map `module → role | role[]`). Module: `erp, finance, insentive, hris, kpi, it, ga, secretary, beauty_hacks, kyura, manufacture, quality, procurement, integration, legal, rnd`. Role: `admin, supervisor, security`, dan khusus insentif `adv_leader, adv_marketplace, adv_meta`.
- **Role gating**: sidebar menampilkan module sesuai key `system_roles` (`erp` publik); menu KPI butuh `supervisor`; Guestbook butuh `ga:security`; create Article + seluruh card Splash butuh `admin`; sub-item Incentive di-gate per role.
- **Pengelompokan menu**: sub-kategori memakai **menu induk bersarang** yang bisa dibuka-tutup, **bukan** field `group` (yang merender header section datar). `group` pernah dipakai lalu ditolak karena header-nya membuat menu di bawahnya tampak ikut masuk ke grup itu; keputusan 2026-07-29, dikunci `components/layout/od-menu.test.ts`. Kategori HRIS kini punya 7 induk: **Komunikasi · Guestbook · Personalia · People Development · Pengembangan Organisasi · Recruitment · Payroll**, plus **Dashboard** dan **Analisis SDM** yang sengaja dibiarkan berdiri sendiri di tingkat atas sebagai pintu masuk sekali klik. Pengelompokan ini murni tampilan: tak menyentuh key `system_roles` maupun `perm`, jadi tak mengubah hak akses siapa pun.
	- **Tampilan menu per posisi + Pengaturan bertab** (branch `feat/pengaturan-menu-posisi`, **belum merge**): `/hris/system-setup` dulu menumpuk `DepartmentsManager` dan `AttendanceSettingManager` secara vertikal, kini **tiga tab** (Departemen & Jabatan · Aturan Presensi · **Tampilan Menu**). Menunya ikut berganti nama dari "System Setup" jadi **"Pengaturan"** karena isinya melebihi setup dan HRIS punya dua "System Setup" yang membingungkan.
		- Tab Tampilan Menu memilih menu yang tampil per jabatan lewat centang. **Centang berarti TAMPIL, yang disimpan justru kebalikannya** (`menu_hidden`); pembalikan itu ditaruh di satu tempat karena "centang = tampil" lebih mudah dipahami pengelola, sedangkan menyimpan-yang-disembunyikan yang membuat menu baru otomatis muncul. Ini **setelan tampilan, bukan hak akses** — detail & pembedaannya dari `permission_sets` di [[CORE - RBAC dan Permission Set]].
		- Daftar centangnya **diturunkan** dari `menus` yang sama dengan sidebar (`daftarMenuUntukPengaturan`), bukan disalin: salinan berarti tiap menu baru harus didaftarkan dua kali, dan yang biasanya terjadi adalah centang untuk menu yang sudah tak ada plus menu baru yang tak pernah bisa disembunyikan. Url yang hadir di dua menu jadi **satu** centang, karena penyembunyian memang bekerja atas url (contoh nyata `/hris/guestbook/late-employees` = "Karyawan Terlambat" di HRIS dan "Tim Terlambat" di Portal Saya).
	- **Induk menu tipis diratakan** (`sidebar-menu-shape.ts`, branch yang sama): induk dengan **kurang dari 3 anak yang terlihat** dibubarkan dan anaknya naik ke tingkat atas. Ambangnya dari **jumlah anak yang terlihat, BUKAN role** — Leader ber-role `staff` bisa saja melihat banyak menu dan sebaliknya, jadi menyandarkan bentuk sidebar pada nama role akan salah persis di kasus penting. Dijalankan paling akhir supaya yang dihitung adalah jumlah yang benar-benar terlihat pemakai itu.
	- **Menu KPI di Portal Saya kini juga untuk Leader** (branch `feat/kpi-leader-portal`, **belum merge**): dulu hanya supervisor. Gerbangnya **keberadaan bawahan langsung**, bukan nama jabatan maupun jenjang, dengan aturannya diekstrak ke `components/layout/kpi-menu.ts` agar bisa dites tanpa merender sidebar (pola `tools-menu.ts`). Halaman `/portal/kpi` melayani dua peran: supervisor melihat seluruh departemennya, Leader melihat bawahan langsungnya; **supervisor menang** bila keduanya. Pengisian datanya lewat menu baru **HRIS → Personalia → Atasan Langsung** (`/hris/employee/atasan-langsung`), alat isi-massal per departemen dengan tombol "isi yang kosong" memakai supervisor departemen. Detail: [[HRIS - Key Performance Index]] dan [[HRIS - Organization Structure]].
	- **Atasan Langsung dibalik jadi pilih-atasan-lalu-centang-bawahan** (branch `feat/atasan-pilih-bawahan`, **belum merge**). Bentuk awalnya meminta atasan satu per satu untuk tiap karyawan, jadi satu departemen berarti membuka dropdown belasan kali. Tiap baris kini menyebut **akibatnya** sebelum disimpan (`akan ditambahkan` · `akan dipindah dari X` · `akan dilepas`), karena mencentang orang milik Leader lain mencabutnya dari Leader itu dan itu tak boleh senyap. Berpindah atasan dengan perubahan menggantung meminta konfirmasi; tanpa itu, sekali salah sentuh dropdown seluruh centang hilang dan pekerjaannya diulang. Karyawan non-aktif ditandai dan terkunci — `is_active` datang dari BE dan **opsional bagi FE** (tak-ada berarti aktif), mengikuti konvensi "FE fallback aman bila field baru belum ada".
		- **Dua koreksi setelah dipakai di produksi hari yang sama** (branch `feat/atasan-sembunyikan-nonaktif`, **belum merge**): karyawan non-aktif kini **disembunyikan** (jumlahnya disebut lewat baris keterangan) alih-alih ditandai — di Tech Development 5 dari 11 baris ternyata orang yang sudah resign; dan pemilih departemen memakai **`grouped: true`** sehingga **HRGA** muncul sebagai satu pilihan berisi 30 orang. Yang kedua menuntut BE mengenali label grup lebih dulu, jadi **deploy BE sebelum FE bukan anjuran**: FE yang mengirim `HRGA` ke BE lama dijawab daftar kosong, dan itu kegagalan **senyap** tanpa error. Catatan pemakaian: HRGA berada di **urutan terakhir** daftar pilihan (bukan abjad), dan Human Resource serta General Affair **tak lagi ada terpisah**.
	- **Bagan Organisasi** (`/hris/org-chart`, menu di Personalia tepat setelah Atasan Langsung, branch `feat/org-chart`, **belum merge**): pohon HTML bersarang **tanpa pustaka bagan** — `recharts` tak punya tata letak pohon, dan menambah dependensi khusus untuk empat tingkat simpul tak sepadan. Departemen terbuka, isinya **terlipat secara bawaan**; 204 kartu sekaligus membuat bagan lebih sulit dibaca daripada daftar. Ditaruh bersebelahan dengan Atasan Langsung karena keduanya membaca data yang sama: yang satu mengisinya, yang satu menampilkannya. Detail keputusan (kerangka departemen, penjaga siklus, penempatan Direktur): [[HRIS - Organization Structure]].
		- ⚠️ **Halaman ini pernah gagal total di produksi** karena lingkaran render: `const { data: rows = [] }` membuat array baru tiap render, array itu jadi dependensi `useEffect`, efeknya memanggil `setState`, berulang tanpa henti. Saat itu **`tsc`, `eslint`, dan 1424 uji semuanya hijau** — tak satu pun bisa melihatnya karena halaman itu tak punya uji render sama sekali. Penawarnya dua: keputusan dipindah ke `lib/penetapan-atasan.ts` supaya bisa diuji tanpa React, dan **tiap komponen baru wajib punya minimal satu uji yang benar-benar merendernya**.
	- Penyesuaian setelah dipakai di dev (PR #631, **belum merge**): induk **Laporan** dibatalkan karena isinya cuma satu menu analisis dan membungkusnya hanya menambah klik; **Analisis SDM** kembali berdiri sendiri, dan **laporan absensi** (`/hris/report`) pindah ke **Personalia** karena itu urusan administrasi kepegawaian sehari-hari. Menu **Slip Gaji Saya** dicabut — halamannya (`/hris/payroll/my-payslips`) tetap ada, tapi menunya berada di kategori `hris` sehingga **tak pernah terlihat karyawan biasa**; bila slip mandiri kelak mau dibuka untuk semua karyawan, tempatnya di kategori **Portal Saya** (`erp`), bukan HRIS.
- **Akses menu WMS (matriks per posisi)**: akses tiap menu `/manufacture/*` ditentukan **matriks posisi tunggal** `src/features/manufacture/akses.ts` (`MATRIKS_TAB_WMS`), dipakai **tiga penjaga** yang wajib seragam: sidebar (menu terlarang disembunyikan, grup kosong ikut hilang), gerbang tab `manufacture-app` (akses langsung via URL → "Menu tidak tersedia"), dan `ManufactureGuard` (menilai izin per-tab dari URL). PPIC & supervisor = semua akses; `admin gudang FG` → Gudang FG + Stok/Bundle/M.Resi; `admin gudang RM` → Gudang RM + Order & Dokumen; `admin produksi` → L. Hasil Produksi + Order & Dokumen; `staff` **tanpa akses WMS**. Role diketik bebas di Master Data → dicocokkan dalam bentuk kanonik (huruf kecil, spasi→underscore).
- **Konsolidasi menu WMS + active-state**: beberapa tab `/manufacture/*` digabung jadi **satu entri sidebar** lewat `src/features/manufacture/tab-groups.ts` (`TAB_GROUPS`) — mis. grup **"Mutasi Gudang & Konsinyasi"** = tab *Dokumen Mutasi* (`/manufacture/mutasi-gudang`) + *Piutang Konsinyasi* (`/manufacture/piutang-konsinyasi`). `resolveItem` di `components/layout/sidebar.tsx` mematok `url` entri ke **member pertama yang boleh** (mis. Finance yang cuma boleh Piutang mendarat di Piutang, bukan Dokumen Mutasi). ⚠️ Karena satu entri mewakili beberapa path, active-state di `navigation.tsx` **tak boleh** cuma `pathname === url` (dulu bug: di tab Piutang tak ada menu ter-highlight). Perbaikannya: `resolveItem` melampirkan **`activePaths`** = semua path member yang boleh (`pathsGrupBoleh`), lalu `navigation.tsx` (`cocokAktif`) menandai aktif bila `pathname === url` **ATAU** `activePaths.includes(pathname)`. Menu non-WMS tanpa `activePaths` → jatuh ke perilaku lama `pathname === url`.
- **Finance → Gudang FG (read-only)**: role `finance` **tanpa** role `manufacture` mendapat key `manufacture` di sidebar; izinnya **satu pengecualian lintas-modul di dalam matriks** (`TAB_WMS_FINANCE = ["finished_goods"]`, lewat `bolehAksesTabWmsUntukUser`) — bukan daftar terpisah, supaya akses menu WMS tetap satu sumber. Hasilnya Finance hanya melihat & membuka *Gudang FG* untuk memantau tab *Return Dari Ekspedisi* (acuan penyesuaian stok Accurate); tab lain tetap "Akses Ditolak". Di halaman itu mereka **read-only** (`isManufactureReadOnly`): tombol input diganti badge *Hanya Lihat*, baris feed tak bisa diklik, ketiga submit dijaga. Aturan read-only sengaja "tanpa role `manufacture` → read-only" (bukan menyebut `finance`), jadi role apa pun yang kelak diberi tab WMS otomatis read-only. `itSupervisor` dikecualikan. ⚠️ Semua ini gating **frontend**: manufacture-service tak memeriksa role sama sekali (lihat [[Microservices - Manufacture Service]]).
- **Quality & R&D → WMS (read-only)**: pola sama Finance. `system_roles.quality` boleh membuka *Dokumen Produksi Batch* (`batch_record`), *Hasil Produksi* (`production`), *Gudang RM* (`raw_materials`) — sumber Pre-Check/CPPB/IPC/Incoming. `system_roles.rnd` boleh *Master Produk/BOM* (`master_barang`), *Gudang RM* (`raw_materials`), *Analisa Stok* (`stock`) — sumber BOM & simulasi HPP. Pengecualian lintas-modul `TAB_WMS_QUALITY` / `TAB_WMS_RND` di `features/manufacture/akses.ts` (via `bolehAksesTabWmsUntukUser`), **read-only** (`isManufactureReadOnly`). Link-nya ditaruh langsung di blok menu `quality`/`rnd` (grup "Data Produksi (WMS)" / "Data WMS") — bukan lewat modul `manufacture`, sehingga tak tersaring filter WMS di sidebar (yang hanya jalan untuk `mod === "manufacture"`). Beda dari **sub-role** manufacture `qc`/`rnd` di `MATRIKS_TAB_WMS[batch_record|rekon_mo]` yang membaca `system_roles.manufacture` (bukan modul departemen). Lihat [[QA - Quality Operasional (CAPA, Incoming, Batch Release)]] & [[QA - R&D Regulatory (Registrasi & Pipeline Produk)]].
- **Procurement → WMS (read-only)**: pola sama. `system_roles.procurement` boleh membuka *Material Order & PO* (`orders_po`), *Gudang RM* (`raw_materials`), *Analisa Stok* (`stock`), *Rencana Produksi/PPIC* (`production_plan`) — dasar keputusan "sisa hari stok" & PO mendesak. `TAB_WMS_PROCUREMENT` di `features/manufacture/akses.ts`, read-only. **Tulis** PO material tetap butuh role `manufacture` (mis. `admin_gudang_rm`). Grup menu "Gudang & Material (WMS)" di blok `procurement`. - **Legal → Procurement (read-only)**: grup menu "Referensi Procurement" di blok `legal` → **Kontrak Vendor** (`/procurement/kontrak`) & **Master Pemasok** (`/procurement/pemasok`), untuk penalti/klaim vendor & rujukan kontrak. Kontrak Vendor (register `employee-service`): backend GET `RequireProcurementReadOrLegal` (procurement|legal|IT-sup); tulis tetap `RequireProcurement*` → Legal **read-only** (FE menyembunyikan create/edit/hapus + badge *Hanya Lihat* bila tanpa role procurement); `proxy.ts` mengecualikan `legal` khusus `/procurement/kontrak` (bukan `/penghematan`). Master Pemasok = tab di modul `/procurement` (main), digerbang `ProcurementGuard` yang kini juga meloloskan `legal` (view; tulis digerbang backend Procurement). Sisa singgungan Legal (dispute→HRIS, pengingat→Task Management, somasi→`/sekre/surat` yang belum ada) belum ditautkan.
- **Menu terbatas (whitelist per akun)** — branch `feat/oneForAll`, **belum merge**. Menu yang isinya tak pantas dilihat seluruh divisi bisa dipersempit ke akun tertentu tanpa menyentuh izin modulnya. Pemakai pertama: **Accounting** (`/finance/accounting`, posisi keuangan seluruh perusahaan). Bentuknya **default terbuka**: selama paket "Menu: Laporan Keuangan" belum dipasang ke siapa pun, menu & halaman tampil seperti biasa bagi role `finance`/IT; begitu ada ≥1 akun di-assign, hanya mereka yang bisa. Satu aturan dipakai dua tempat — `bolehAksesMenu` (`utils/menu-terbatas.ts`) dibaca `bolehMenu` untuk item sidebar (`perm: "menu.finance.laporan"`) dan `MenuTerbatasGuard` (`components/menu-terbatas-guard.tsx`) untuk halamannya, menggantikan `FinanceModuleGuard` di halaman itu. ⚠️ `fallback`-nya wajib cerminan aturan lama (di sini `useFinanceFallback`), kalau tidak menyalakan fitur diam-diam mengubah siapa yang melihat menu padahal belum ada yang di-assign. Penyembunyian di FE tetap kenyamanan; gerbang backend-nya parsial — lihat [[ADR - 0039 Menu Terbatas Default Terbuka sampai Di-assign]].
- **Akun "integration-only" (allowlist env)**: username yang terdaftar di `NEXT_PUBLIC_INTEGRATION_ONLY_USERS` (comma-separated) dibatasi **hanya** melihat section Integration di sidebar — modul publik (`erp`/`hris`/`manufacture`) maupun section turunan Integration (INTEGRATION ACCURATE & INSIGHT) ikut disembunyikan — dan setelah login langsung diarahkan ke `/integration/ads-analytics`. Helper `isIntegrationOnlyUser` (`src/utils/access.ts`) dipakai di `components/layout/sidebar.tsx` & `app/login/page.tsx`. Tujuan: akun review/test marketplace (mis. Shopee).

## Modul / Fitur (Sudah Diimplementasikan)

**ERP (publik)**
- Dashboard: ringkasan kehadiran pribadi (clock in/out, overtime/telat/cuti), ringkasan karyawan, jadwal shift, widget helpdesk (`POST /feedback`)
- Article + Splash (admin): kelola artikel (md-editor) & gambar splash
- Guestbook view (role security). ⚠️ Menu peluncur SSO ke app Task Manager eksternal **sudah tidak ada** di `sidebar-menus.tsx`; nama "Task Management" di sidebar kini merujuk portal tugas internal (lihat bagian **Task Management** di bawah)

**HRIS** (module paling lengkap)
- Employee: CRUD penuh, filter/search, summary, export Excel, status registrasi myBharata, halaman detail
- Attendance: tabel clock in/out + ikon metode + preview geolokasi (Leaflet), update manual, summary
- **Roster** (`/hris/roster`, menu di Personalia, PR #805 · [[ADR - 0036 Roster Harian Menimpa Jadwal Dasar]]): grid **orang × tanggal** satu bulan untuk mengisi jadwal Host Live yang tak berpola. Sel terisi menampilkan jamnya atau penanda Libur; sel kosong menampilkan jadwal dasar dengan warna samar. Penyuntingan lewat popover (jam bebas, Libur, catatan, pintasan shift). **Seleksi baris & kolom** supaya sebulan untuk 30 host tak berarti ~900 klik; kiriman dipecah jadi batch di bawah batas 500 sel milik server, dengan umpan balik kemajuan. Tanggal lampau read-only dan **tak bisa terpilih lewat mekanisme mana pun**. Pilihan departemen disaring ke cakupan supervisi pemakai, mencerminkan gerbang backend. Keempat keadaan dibedakan tampilannya — memuat, gagal memuat (pesan asli server + tombol coba lagi), tak ada departemen dalam cakupan, dan departemen yang memang belum punya karyawan roster — supaya "kosong" tak pernah ambigu.
	- Saklar **`roster_enabled`** menumpang form edit jadwal karyawan yang sudah ada, bukan halaman baru. Pemilih jadwal dasar **tidak pernah disembunyikan** saat saklar menyala, karena jadwal dasar tetap wajib sebagai tempat jatuh. Saklar hanya dikirim bila nilainya benar-benar berubah — pengaman agar field yang hilang di sisi server tak lagi berarti mematikan roster orang.
	- **Menyalakan saklar tidak menulis ulang jadwal** (PR #809). Jadwal yang berlaku dibaca `schedule_id ?? group_id` (karyawan `pattern` menyimpannya di `group_id`, dan form yang cuma membaca `schedule_id` membuat kotaknya kosong lalu validasi menolak), dan jadwal hanya ditulis bila pemakai benar-benar menyentuhnya. Tanpa keduanya, HR yang sekadar menyalakan roster dipaksa memilih ulang jadwal dari dropdown berisi delapan varian grup Host Live bernama mirip — salah pilih berarti mengubah jadwal kerja orang itu.
	- **Tombol Panduan** di header halaman roster (erp-frontend #811) memakai ulang komponen `InfoSheet`; konten dwibahasa di `src/content/guides/roster-guide.ts`. Menjelaskan dua hal yang tak terbaca dari layar: alurnya "pilih sel DULU, baru Terapkan" (tombolnya membuka pengisian, bukan menyimpan), dan bahwa batas mengubah jadwal **bukan soal jam** melainkan keadaan — tak ada aturan H-1 maupun batas 12 jam; yang menutup pintu adalah orangnya sudah tap masuk, tanggalnya sudah lewat, atau entrinya sudah berstatus cuti/dinas.
- Contract (+ filter **Bulan Berakhir** di panel Filter, default bulan berjalan → `ending_month`), Schedule (kalender + holiday CRUD), Vacation (kuota/terpakai/sisa), Report (bulanan + export Excel), Fingerprint (device management), **Dashboard HRM** (gaya ERPGo: 8 KPI cards + widget Department Distribution/Quick Actions/Employees on Leave/Missing Attendance/Recent Requests/Upcoming Agenda/Announcements — grounded ke endpoint nyata; agregasi absensi hari-ini via `/entries`) + **Analisis SDM** (`/hris/analysis`, demografi pindahan dari dashboard lama), KPI
	- **Jadwal Kerja & Laporan Kehadiran ikut pola satu-card** (✅ **live di produksi 2026-08-05**, PR [#806](https://github.com/bip-itteam-internal/erp-frontend/pull/806)) — keduanya dua layar HRIS terakhir yang belum memakainya sama sekali.
		- **`/hris/schedule`**: shift, rotasi, dan mesin fingerprint dulu bertumpuk vertikal sebagai **grid kartu**, sehingga satu layar memuat tiga judul, tiga tombol tambah, dan kartu sebanyak datanya — membandingkan dua shift berarti membandingkan dua kartu yang isinya tersusun ke bawah. Ketiganya kini `Table` + `Banner` di kartu yang sama, dipilih lewat **`CustomTabs`** di atas tabel (pola halaman Kandidat). Shift memakai **satu kolom per hari, tujuh kolom**, supaya perbedaan antar-shift terbaca dengan menyusuri satu kolom ke bawah. Preview kalender tetap kartu terpisah di bawah: ia alat lihat, bukan daftar yang dikelola.
			- Aturan tab diekstrak ke `features/hris/schedule/lib/tabs.ts` agar bisa diuji tanpa merender Radix (pola `kpi-menu.ts`). Tab **Mesin Fingerprint tidak DISUSUN** untuk non-IT, bukan disusun lalu disembunyikan lewat CSS — tab yang tetap dirender masih bisa difokus keyboard dan masih terbaca screen reader. `tabJadwalYangSah` menjaga tab aktif selalu menunjuk sesuatu yang ada, karena penanda IT-nya FALSE pada render pertama (`mounted && isItMember(...)`) lalu berubah, dan tanpa penjaga itu yang tampil adalah halaman kosong tanpa penjelasan.
		- **`/hris/report`**: satu-satunya layar berkepala tabel **hitam** (`bg-black dark:bg-black`) di seluruh aplikasi, dengan pembungkus & sel ber-`border-foreground`, dan toolbar rakitan tangan di `CardAction` (pemilih bulan, dua `Select`, kotak cari, tombol unduh). Semua override dibuang; toolbar pindah ke `Banner` (pencarian & filter dari `tableState`, pemilih bulan dan tombol unduh di slot `actions`). Halaman menyusut **501 → 219 baris**: dialog unduh jadi komponen sendiri, dan penyusun baris matriks pindah ke helper sehingga **tabel di layar dan berkas yang diunduh berangkat dari satu fungsi** (dua penyusun terpisah berarti isi unduhan bisa menyimpang tanpa ada yang menyadarinya).
			- Layar ini **tetap tidak memakai `Table` bersama**: ia matriks **karyawan × tanggal** dengan kolom yang tumbuh mengikuti jumlah hari dan sel berwarna per status, sedangkan komponen itu merender satu baris per record dari daftar kolom tetap. Yang diseragamkan susunan kartu dan gaya tabelnya, bukan komponennya.
			- ⚠️ **Cascade departemen → posisi hilang saat pindah ke `Banner`.** Versi lama mereset posisi tiap departemen berubah; `FilterTable` menerapkan seluruh filter sebagai satu objek dan tak punya jalan mereset satu field dari luar, jadi posisi lama menempel dan tabel menyusut jadi **nol baris tanpa pesan apa pun**. Ditambal dengan mengabaikan posisi yang bukan milik departemen terpilih. **Sisa yang belum beres**: panel filter masih MENAMPILKAN posisi lama itu sebagai terpilih walau tak lagi diterapkan, karena `FilterTable` menyemai draft-nya sekali saat mount dan tak menerima perubahan dari luar. **TBD.**
	- **Isi dashboard menyesuaikan posisi** (branch `feat/dashboard-hris-per-posisi`, **belum merge**): satu halaman `/hris/dashboard`, komposisi seksinya ditentukan `features/hris/dashboard/lib/position-view.ts`. **Supervisor/admin HRIS** dapat versi terlengkap (8 kartu + sebaran departemen + aksi cepat + 5 widget + funnel rekrutmen). Status supervisor sengaja diambil dari **tingkat role**, bukan nama posisi, supaya rename posisi tak mencabut aksesnya; nama posisi hanya membedakan sesama staf (`Personalia`, `Recruitment & Onboarding`, `Training & Performance Officer`, `Culture & Industrial`). Nama tak dikenal jatuh ke preset `default` yang tetap berguna, bukan layar kosong.
	- ⚠️ **Preset `ga`/`security`/`office-boy` ADA tapi belum aktif.** Ketiganya menuntut `system_roles.hris` untuk sampai ke menu HRIS, dan posisi itu tak punya (verifikasi dev 2026-07-30: `GA Staff` 2 orang `ga:staff`, `Security` 7 orang `ga:security`, `Office Boy` 4 orang **`system_roles` kosong**). Artinya **13 dari 17 orang HRGA belum terlayani** dashboard ini. Melayani mereka perlu keputusan terpisah: beri akses HRIS, atau buat dashboard sendiri di bawah `/ga/`.
	- **Penyaringan ini kenyamanan tampilan, BUKAN kontrol akses.** `/hris/dashboard` tak dijaga middleware; penjaga sebenarnya tetap 403 dari backend. Jangan tertipu `src/proxy.ts` yang mencantumkan route itu sebagai "private" — berkas itu tak pernah dipasang (lihat §Belum Diimplementasikan).
- **Recruitment**: **Dashboard** (KPI + hiring funnel %-konversi + donut status kandidat & onboarding progress + kalender interview; agregasi FE) — sejak branch `feat/dashboard-hris-per-posisi` **bukan menu tersendiri lagi**: komponennya disematkan di Dashboard HRIS untuk posisi `Recruitment & Onboarding` dan supervisor (prop `embedded` menurunkan judulnya `h1`→`h2`), dan route `/hris/recruitment/dashboard` jadi **redirect** ke `/hris/dashboard` agar tautan lama tak mati — plus Job Requisitions + Candidates + Interviews + Candidate Assessment + System Setup (master ATS) + **Panduan in-app** (modal onboarding section `/hris/recruitment`: auto-muncul pertama kali, tombol Paham/Ingatkan-nanti persist `localStorage`, tombol Panduan di header khusus section; 6 tab + flowchart alur; dwibahasa) → [[Microservices - Recruitment Service]]
  - **Onboarding Checklist** (✅ 2026-07-26, `/hris/recruitment/onboarding`): menu HR 2-tab (Template + Onboarding Berjalan) untuk kelola template tugas onboarding karyawan baru + mulai onboarding (pilih karyawan + template → tetapkan PIC per item + start_date + buddy) + pantau progres. Menu **"Tugas Onboarding Saya"** (Portal Saya) = sisi PIC menandai tugasnya selesai. Menu lama **"Candidate Onboarding" di-rename → "Review Onboarding"** (= Performance Review masa evaluasi) agar tak rancu. Reuse Combobox 2-baris + `useListEmployees`. Backend: [[Microservices - Recruitment Service]] (increment "Onboarding Checklist (rebuild)").
- **Resign / Non-Aktif Karyawan** (`/hris/resign`, grup **Personalia** tepat setelah Contract, ✅ **live di produksi 2026-08-05**, PR [#803](https://github.com/bip-itteam-internal/erp-frontend/pull/803)): tabel catatan resign berpaginasi (filter kategori · status · departemen · pencarian nama), form Tambah/Perbaiki, dan panel detail berisi pembatalan + lampiran dokumen. Menyimpan form ini **mencabut akses karyawan ke ERP dan MyBharata**, jadi peringatan konsekuensinya dirender **di dalam form**, bukan sebagai konfirmasi kedua sesudahnya. Header dan footer dialog sengaja dibuat diam dan hanya badan formnya yang bergulir: dengan `DialogContent` bawaan yang `overflow-auto`, peringatan itu dan tombol yang menjalankannya sama-sama terdorong ke bawah lipatan.
	- Combobox "Pilih Karyawan Aktif" memakai `useListEmployees` yang sudah ada; endpoint di baliknya (`/list?type=employee`) sudah menyaring `is_active`, jadi karyawan non-aktif memang tak muncul tanpa penyaringan tambahan di FE.
	- Tombol **Perbaiki** hanya muncul untuk catatan berstatus `scheduled`, mengikuti penolakan server atas koreksi catatan yang sudah berlaku. Form-nya dibuka sebagai modal **terkendali di tingkat halaman** dan panel detailnya ditutup lebih dulu: menaruh `Dialog` di dalam `Sheet` berarti dua overlay Radix bersarang yang saling berebut focus trap.
	- Panel detail menurunkan barisnya dari data terbaru (menyimpan ID, bukan salinan baris), karena ia sendiri yang mengunggah dokumen dan membatalkan catatan — memegang salinan membuatnya menampilkan keadaan sebelum aksinya sendiri.
	- Kunci i18n `hris.resign.*` lengkap di **kedua** locale, dijaga uji kelengkapan (`schema.test.ts`). Kategori resign diterjemahkan penuh karena istilah HR, bukan istilah teknis. Backend & keputusannya: [[Microservices - Employee Service]] · [[ADR - 0035 HR Menonaktifkan Akun lewat Catatan Resign]] · konsep: [[HRIS - Personalia]].
	- Ikut dihapus di branch yang sama: `features/hris/employee/components/delete.tsx` + `hooks/use-delete.ts` — **kode mati**, komponennya tak dirender di halaman mana pun dan hook-nya menembak `DELETE /api/hris/employees/:id/multi` yang tak punya handler di [[CORE - HRIS Orchestrator]].
	- **Kartu statistik turnover** (branch `feat/hris-resign-stat`, **belum merge & belum deploy**): empat kartu di dalam kartu tabel — keluar bulan ini, headcount awal bulan, **headcount saat ini**, dan **turnover berjalan** berwarna menurut ambang. Disuplai `GET /resign/summary`; rumus & keterbatasannya di [[HRIS - Attrition]].
		- Labelnya menyebut "Saat Ini" dan "Berjalan", **bukan "Akhir Bulan"**: bulannya belum selesai, dan menamainya akhir-bulan di pertengahan bulan membuat angka sementara terbaca sebagai angka final.
		- Nilai yang belum ada tampil **tanda hubung dan nada netral**, bukan `0%` hijau. Nol adalah angka sah yang berarti "tak ada yang keluar"; tanda hubung berarti "belum tahu". Menyamakan keduanya menghapus perbedaan yang paling penting di halaman ini, mengingat koleksinya masih kosong.
		- Keterangan permanen di bawah kartu menyebut bahwa hitungan hanya mencakup yang tercatat lewat menu ini. Tanpa itu `0%` terbaca sebagai "turnover kita nol" padahal artinya "belum ada yang dicatat".
		- `StatSummary` mendapat prop **`tone`** opsional (`default`/`success`/`danger`). Tanpa prop itu tampilannya persis seperti sebelumnya, jadi 9 pemakai lain tak berubah.
		- ✅ Field `color` yang **mati** sudah dibuang dari **Kelola Karyawan** dan **Kehadiran** (PR [#812](https://github.com/bip-itteam-internal/erp-frontend/pull/812)). Keduanya mengirim `color: "text-primary"` padahal `StatItem` tak pernah punya field itu, jadi selama ini dibuang diam-diam dan kartunya seragam primary semua. Sejak `tone` ada, membiarkannya justru menyesatkan pembaca berikutnya.
			- ⚠️ **Bukan semua `color` di repo ini mati.** Dari enam berkas yang memakainya, hanya dua di atas yang mengalirkannya ke `StatSummary`. Sisanya (`integration/inventories`, `warehouse/DashboardView`, beberapa komponen insentif) memakai komponen kartu yang **berbeda** dan `color`-nya memang didukung di sana — jangan ikut dibersihkan tanpa memeriksa komponen tujuannya lebih dulu.
- **Pengembangan Organisasi** (grup sidebar OD, ⚠️ 2026-07-29): menu **Community of Interest** berisi **Jadwal Club** (`/hris/community/schedule`, jadwal rutin sepekan 4 klub Bharata Club Community) + **Dokumentasi Kegiatan Club** (`/hris/community/documentation`, galeri kegiatan per klub) + **Ulang Tahun** (`/hris/birthdays`, **dipindah** ke grup ini dari daftar menu HRIS teratas; route & halaman tidak berubah). ⚠️ Data klub & dokumentasi masih **konstanta di frontend** (belum ada service/storage; unggah foto belum ada, isinya data contoh dan ditandai badge "Data contoh"). Header grup sidebar kini ikut i18n (`navigation.tsx` merender `tr(section.group)`). Detail: [[HRIS - Pengembangan Organisasi (Community of Interest)]]
- **Payroll** (`/hris/payroll/*`) → [[Microservices - Payroll Service]]: **Pengaturan Gaji** (`/salary-setup`, reuse `SystemSetupLayout`, **5 tab**: Komponen Gaji, BPJS, Pajak & PTKP, **Perlakuan Kehadiran**, **Badan Usaha**) · **Gaji Karyawan** (`/employee-salary`, halaman tersendiri, bukan lagi tab) · **Payroll Run** (`/runs`, `/runs/:id`) · **Slip Gaji Saya** (`/my-payslips`). Tab **Perlakuan Kehadiran** = master dibayar/dipotong per status absensi (data milik [[Microservices - Attendance Service]] `payroll_status_treatment`, memengaruhi payout).
	- **Penjaga salah input Upah BPJS** (branch `feat/payroll-upah-bpjs-guard`, **belum merge**): field `upah_bpjs` di form Gaji Karyawan dulu hanya berpenjelasan **tooltip**, yang baru muncul saat ikon info di-hover, jadi praktis tak terbaca. Akibatnya di production field itu terisi **nominal potongan**, bukan dasar upah (lihat catatan di [[Microservices - Payroll Service]]). Kini: `description` permanen di bawah label, **estimasi nominal potongan karyawan** yang ikut berubah saat upah/centang program diubah (`bpjsEmployeeEstimate`, cermin `computeBpjsEmployee` BE termasuk `salary_cap`), dan banner amber bila Upah BPJS di bawah Gaji Pokok. Peringatan saja, **tidak memblokir simpan**, karena ada kasus sah (paruh waktu, dasar di-cap ke UMK). Bila config BPJS gagal dimuat, estimasi menampilkan `-`, bukan `Rp 0`: nol terbaca sebagai hasil hitung sah dan justru bisa menuntun HR mengganti angka yang sudah benar. Register Gaji Karyawan juga dapat **kolom "Est. Potongan BPJS"** memakai fungsi yang sama, plus ikon amber per baris: penjaga di form hanya menolong saat satu orang diedit, sedangkan kekeliruannya terjadi pada 163 karyawan sehingga HR perlu memindai semuanya sekaligus.
	- **Pemilih Badan Usaha Penggaji jadi `Combobox` bercari** (branch `feat/payroll-badan-usaha`, **belum merge**): master badan usaha berisi **41 entri** dengan nama nyaris kembar (lihat [[Microservices - Payroll Service]]), yang mustahil dibedakan dengan menggulir `select` polos. `buildCompanyOptions` mengurutkan abjad (bukan urutan datang dari API) dan menandai entri default.

**KPI (shared)** — scoring per departemen + template CRUD (**per posisi**) + filter periode/status; HR lihat semua, supervisor hanya divisinya. Modal **Score KPI menyaring dropdown template ke posisi karyawan** yang dinilai — submit ditolak backend (`400`) bila posisi template ≠ posisi karyawan (bila belum ada template posisi tsb → diarahkan membuat dulu). Detail: [[HRIS - Key Performance Index]]

**Task Management** (`src/features/task-management/`, menu **Task Management**, rute `/task-management/*`) — portal permintaan/tugas lintas-divisi; backend [[Microservices - Task Management Service]]:
- **Rename 2026-07-28**: menu & modul ini sebelumnya bernama **Support Ticket** dengan rute `/support-ticket/*`. Istilah user-facing kini **Tugas** (id) / **Task** (en); nama modul dibiarkan English sebagai istilah teknis ([[ADR - 0010 Internasionalisasi (i18n) Dua Bahasa]]). Rute lama tetap hidup lewat **redirect 302** di `next.config.js` (`/support-ticket/:path*` → `/task-management/:path*`). Namespace i18n `taskManagement`, gating menu di `components/layout/task-management-menu.ts` (fungsi teruji, bukan lagi cocokkan judul menu di `sidebar.tsx`). **Tidak menyentuh backend**: endpoint tetap `/api/task-management/*` dan role sistem tetap `ticket`. ⚠️ Nama tampilan role di **Master Data → System Roles** masih "Support Ticket" karena berasal dari master-data employee-service (`master_system_role`), bukan dari kode FE.
- **Ajukan Tugas** (pilih departemen → Space, Judul/Deskripsi **auto-kapital huruf pertama** sentence-case, lampiran ≤ **4 MB**), **Tugas Saya** (tab *Tugas Saya* / *Ditugaskan* / *Tugas Tim* supervisor), **Permintaan Masuk** (triage supervisor: **approve** dgn assign penangan / **reject** dgn alasan — **approval hanya di web**), detail tugas (status inline, checklist, komentar).
- **Kelola Space** (dulu "System Setup") — CRUD Space per divisi. **RBAC** di-anchor ke role sistem `ticket`: **admin ticket** → semua divisi; **supervisor divisi** (termasuk IT-spv) → **HANYA divisinya** (selaras BE `canSuperviseDivision`). **Izin checklist** hanya assignee/supervisor (pemohon read-only). Editor prioritas punya input **Target resolusi (jam)** per prioritas (`resolution_hours`; kosong = fallback 72 jam), dikirim saat create/update Space.
- **Admin per space ✅** (FE PR [#818](https://github.com/bip-itteam-internal/erp-frontend/pull/818) + BE [#1027](https://github.com/bip-itteam-internal/bip-erp/pull/1027), **merged 2026-08-06**; ⚠️ belum diuji lewat gateway, prod belum di-deploy) — orang yang ditunjuk memegang sebuah space, walau tier-nya staf dan walau dari departemen lain. Keputusannya di [[ADR - 0038 Hak Per-Objek Admin Space Task Management]]:
	- Bagian **Admin Space** tersendiri di Kelola Space (`space-detail-view.tsx` seksi `admins`, disunting lewat `space-section-dialog.tsx`) dan di wizard buat-space (`space-form-view.tsx`). Isinya combobox karyawan, sama seperti daftar izin akses.
	- **Gating menu tak bisa dibaca dari token.** `bolehKelolaTaskManagement(systemRoles, itSupervisor, adminSpace)` menerima parameter ketiga yang diambil `sidebar.tsx` dari `GET /spaces/my-roles`, sebab hak ini menempel di dokumen space, bukan di `system_roles`. Tanpa itu admin space bertier staf tak akan pernah melihat menu **Permintaan Masuk**, **Laporan Tim**, dan **Kelola Space**.
	- Layar detail tugas memutuskan tombol triase dari `task.space.admins` yang **dititipkan BE di dalam objek tugas**, jadi tak perlu permintaan tambahan per tugas.
- **Tipe permintaan + akses space ✅** (langkah wizard **Tipe & Akses**, PR [#744](https://github.com/bip-itteam-internal/erp-frontend/pull/744) + BE [#929](https://github.com/bip-itteam-internal/bip-erp/pull/929)):
	- `TypeEditor` mengelola daftar tipe per space (mis. Perbaikan Bug, Penambahan Fitur). Di **Ajukan Tugas** tipe dipilih sebelum Judul, dengan keterangan **di dalam dropdown**. Muncul **hanya** bila space punya tipe; daftar kosong berarti pilihan itu tak dirender sama sekali. Supervisor bisa membetulkan tipe di **Permintaan Masuk** (tersimpan langsung, tak menunggu Setujui).
	- Sakelar **Batasi akses space** + daftar centang departemen & combobox orang (mengambil **seluruh** karyawan, bukan hanya divisi space). Daftar izin hanya dikirim saat space `restricted`. Penyaringan sesungguhnya di BE — FE tak menyaring sendiri.
	- ⚠️ `normalizeTypes` **wajib** menyertakan `id` tipe yang sudah ada; tanpa itu BE membuat id baru dan tugas lama kehilangan tipenya tanpa galat.
	- ⚠️ Space lama mengirim `"visibility": ""` (string kosong, **bukan** null), sehingga `?? "public"` tak berlaku. Dibaca lewat `visibilityOrDefault` yang abai huruf besar-kecil agar sama dengan `strings.EqualFold` di BE (PR [#746](https://github.com/bip-itteam-internal/erp-frontend/pull/746)).
- **Pertanyaan per tipe permintaan ✅** (FE PR [#800](https://github.com/bip-itteam-internal/erp-frontend/pull/800) + BE [#989](https://github.com/bip-itteam-internal/bip-erp/pull/989); **LIVE di dev & prod 2026-08-05**, dan seluruh 35 tipe Tech Development sudah terisi 154 pertanyaan) — menjawab keluhan bahwa permintaan yang masuk ke Tech Development tak jelas isinya:
	- Di **Kelola Space → Tipe & Akses**, tiap tipe punya tombol **Pertanyaan (n)** yang membuka editornya sendiri (`type-field-editor.tsx`); satu panel terbuka pada satu waktu supaya daftar tipe tetap terbaca. Tujuh jenis jawaban: teks pendek, teks panjang, angka, tanggal, dropdown, pilihan tunggal, pilihan ganda.
	- Di **Ajukan Tugas**, tipe yang punya pertanyaan **menggantikan** editor Deskripsi dengan daftar pertanyaan itu (`type-field-inputs.tsx`) plus satu **Catatan tambahan** opsional. Tipe tanpa pertanyaan tetap memakai editor markdown lama, jadi fitur ini bisa digulirkan per tipe.
	- Jawaban dirakit jadi **markdown** oleh `lib/type-fields.ts` dan dikirim sebagai `description` seperti biasa — tak ada perubahan kontrak. Detail & aturannya: [[Microservices - Task Management Service]].
	- **Label pertanyaan sengaja TIDAK lewat i18n**: isinya data yang diketik supervisor, setara nama space dan nama tipe. Hanya kerangka UI-nya yang dua bahasa ([[ADR - 0010 Internasionalisasi (i18n) Dua Bahasa]]).
	- `OptionsEditor` **dipakai ulang** dari `features/form-builder`, bukan ditiru, karena bentuk nilainya memang sama (array string).
	- ⚠️ Sama gentingnya dengan aturan `id` di atas: `normalizeTypes` **wajib** meneruskan `fields`, dan membedakan absen (jangan diubah) dari array kosong (hapus semua). Setiap bagian Kelola Space mengirim ulang seluruh daftar tipe, jadi menghilangkannya berarti pertanyaan terhapus saat menyimpan bagian lain.
	- ⚠️ Tanggal ditulis apa adanya (`YYYY-MM-DD`) di markdown, **tidak dilokalkan**: isinya dibaca berbulan-bulan kemudian oleh orang dengan bahasa antarmuka berbeda. Pemilih tanggalnya (`DateStringPicker`) tetap tampil sesuai bahasa aktif.
- **SLA & lifecycle tugas**: badge SLA response/resolution di daftar & detail (state `on_track`/`warning`/`breached`/`met`/**`held`**). Saat **triage/approve**, `due_date` **auto pre-fill** = hari ini + target resolusi prioritas terpilih (tetap bisa diedit). Aksi **Tahan (Hold)** / **Lepas** & **Buka kembali (Reopen)** di detail tugas, selaras endpoint BE `hold`/`unhold`/`reopen` (PR #226 UI Hold/Reopen, #227 SLA per-priority).
- **CSAT (kepuasan)**: requester menilai tugas berstatus **Done** (⭐ 1–5 + komentar, **wajib bila ≤ 2**) via widget di detail (`star-rating.tsx`); sudah dinilai → tampil **read-only** (handler/supervisor ikut lihat). Kartu **CSAT** (rata-rata /5 · %puas · jumlah respons) di halaman Report (`/report/csat`). Requester-only submit; selaras endpoint BE `POST /tasks/:id/csat`. (Web PR #233, BE PR #343.)
- **Automation (per-space)**: di **Kelola Space** ada toggle **Auto-assign (round-robin)** + input **Auto-tutup setelah (hari)** (0 = nonaktif), dikirim saat create/update space. Di **triage/approve**, assignee jadi **opsional** bila space auto-assign aktif & punya member (hint "di-assign otomatis"). Selaras field BE `auto_assign`/`auto_close_days`. (Web PR #236, BE PR #352.)
- **Laporan Tim** (`/task-management/team-report`, menu **Laporan Tim** — supervisor, admin, **atau admin space**; gate `bolehKelolaTaskManagement`; beda dari halaman **Laporan** yang pribadi) — dashboard tim/divisi: **KPI** (Total/Open/Selesai/Backlog — *Open & Backlog kecualikan tugas Ditolak* — on-time SLA response/resolusi, CSAT, reopen rate), **tren** masuk vs selesai per hari (recharts), **Sebaran per Space**, **Kepatuhan SLA** (overall + per divisi), **Kinerja Anggota** (total/done/avg response/avg resolusi jam/reopened), **CSAT** (rata-rata + distribusi ⭐1–5), **Tugas Lewat SLA** (klik baris → detail). **Export Excel** per-tabel + **Print/PDF**; filter rentang tanggal. Konsumsi endpoint report divisi BE (`admin-stats`, `summary-by-department`, `timeline`, `sla`, `manpower-performance`, `sla-breaches`, `csat` — lihat [[Microservices - Task Management Service]]). i18n **id+en**; **status tugas tetap mentah** (custom per-space, tak diterjemah); loading `Skeleton`, error per-seksi. (Web PR #262, BE PR #392.)

- **Pemohon terlihat, tab pindah ke komponen bersama** (PR erp-frontend #690, ✅ **merged 2026-08-01**): daftar tugas dapat kolom **Pemohon** (`requestor_name`) di semua tab dan ikut ter-export Excel; detail tugas menampilkan **Pemohon** dan **No. Telepon**. Keduanya **tanpa perubahan backend**, karena `requestor_name` dan `phone` sudah inline di struct `Task` dan ikut ter-emit lewat `TaskPopulated`, sehingga `GET /tasks/filter` maupun `GET /tasks/:id` sudah mengirimnya hari ini. Tab *Tugas Saya* / *Ditugaskan* / *Tugas Tim* kini memakai komponen bersama **`CustomTabs`** (`components/ui/custom-tabs.tsx`, varian `underline`, sudah dipakai Recruitment) menggantikan `Tabs`/`TabsList`/`TabsTrigger` mentah; helper lokal `TabCount` dihapus karena pill badge-nya duplikat dengan `renderBadge` bawaan. `DetailRow` di kartu detail pindah dari `justify-between` + nilai rata kanan ke **grid dua kolom** (lebar label tetap, nilai rata kiri), sebab di kartu sidebar yang sempit label panjang seperti "Penanggung jawab" dan nilainya saling mendorong sampai patah jadi dua baris rata kanan yang sulit dibaca.

**Finance** — menu role `finance` (grounded ke `sidebar-menus.tsx` `financeMenus` + halaman `app/(main)/finance/*`). Kategori sidebarnya bernama **FAT — Finance, Accounting & Tax**: itu nama divisinya, dan seluruh isi kategori (AR, AP, Accounting, Jurnal, Pencairan, KPI) adalah anggotanya — "FINANCE" saja menyempitkan divisi jadi salah satu dari tiga fungsinya.
- **Ringkasan Divisi / Dashboard FAT** (`/finance`, ✅ 2026-08-01): mengikuti purwarupa `Dashboard_FAT_Bharata.html`. **Sebelas tab posisi** (Ringkasan · Supervisor FAT · AR Leader · AR Staf · Senior/Junior Accounting · Accounting Payable · Cost Control · Tax Officer · Accounting CV · Kamus) memakai `CustomTabs`, **bukan sidebar kedua** — sidebar posisi sempat dipasang lalu dicabut karena berdampingan dengan sidebar utama dan entri "Ringkasan Divisi" tampil dua kali menunjuk rute sama. Hanya tab aktif yang dirender supaya hook tab lain tak ikut menembak backend. Rute `/finance/posisi/*` **dipertahankan** (disinkronkan lewat `?posisi=`) mengikuti preseden `/finance/piutang/*` agar tautan lama tak putus.
	- **Tab disaring menurut posisi pengguna** (`posisi/lib/tab-untuk-posisi.ts`): staf melihat Ringkasan + tabnya sendiri + Kamus; supervisor (`isAnySupervisor`) dan anggota IT melihat semuanya dengan penanda "tab saya". `position` dibaca dari cookie sesi. **Arah gagalnya sengaja terbuka** — posisi yang tak terpetakan menampilkan semua tab, bukan mengunci; ini kontrol tampilan, penjaga sebenarnya `FinanceModuleGuard` + backend.
	- **Elemen tanpa sumber data DITANDAI, bukan diisi nol.** Dari 93 elemen, ~47 masih bertanda karena master data/modulnya belum ada (pajak, forecast kas, tutup buku, maker-checker, register penghematan, janji bayar, rekonsiliasi bank vs GL). Fungsi murni `tampilanMetrik` membuang nilai pada status `"belum"` **berapa pun isi yang dikirim pemanggil**, dan `tampilanSumberKpi` menurunkan klaim "otomatis" jadi "manual" bila aktualnya belum bersumber — purwarupa menandai ~70 baris KPI sebagai otomatis padahal tak satu pun terpetakan ke angka nyata.
	- Papan skor KPI per posisi **dihapus** (2026-08-01): seluruh kolom Aktual/Skor kosong, dan KPI sudah punya modulnya sendiri di `/finance/kpi`. Tabel "Tampilan ERP yang dibutuhkan" dan "Aksi di ERP" juga dihapus — isinya spesifikasi untuk tim IT, bukan data operasional.
- **Anggaran** (`/finance/anggaran`, ✅ 2026-08-01): master anggaran OPEX per akun × departemen × bulan. Unggah Excel + form koreksi satu baris. Hasil unggah menampilkan **alasan tiap penolakan beserta nomor barisnya** — unggahan yang menolak separuh baris tanpa menyebut barisnya lebih buruk daripada gagal total. Backend: [[API - Integration Service]] `/accounting/anggaran*`
- **Piutang** (`/finance/piutang/*`): tabel accounts-receivable + filter overdue/in-transit. Empat sub-halaman: **All**, **TikTok Shop**, **Shopee**, dan **Uang Gantung** (dana marketplace yang belum cair). Halaman **All** = KPI aging gabungan 3 kartu (Gabungan/TikTok/Shopee) dengan **filter waktu opsional** (2026-07-23): rentang tanggal (preset **Semua** default = tanpa filter/semua outstanding, **Bulan Ini**, **Bulan Lalu**, **Kustom**) + **basis tanggal** (**Tgl Kirim** `shipped_at` default / **Tgl Order** `order_date`; toggle disabled saat "Semua"). Bucket aging (0–14/15–60/>60 hari) **tetap** dihitung dari `shipped_at`; rentang hanya mempersempit populasi. Backend: [[API - Integration Service]] `GET /transactions/orders/piutang/summary`
- **Incentive** (`/finance/incentive/*`) → [[Microservices - Insentive Service]]: **Dashboard** (stats/tren + bulk approve), **My Incentive**, **Payroll Calculator** (`/incentive/calculator`, kalkulator insentif), **Settings** (2 tab: Master KPI CRUD + Master Integration = mapping employee ↔ platform/campaign)
- **Pencairan** (`/integration/payouts`): overview + tabel pencairan dana marketplace (Shopee terpisah), backend [[Microservices - Integration Service]]
- **KPI** (`/finance/kpi`): halaman KPI yang sama dengan [[HRIS - Key Performance Index]], **dikunci ke departemen Finance** (`KpiPageContent lockedDepartment="Finance"`) — appraisal KPI per-karyawan Finance, bukan skor insentif marketing
- **Integrasi Accurate** (sub-kategori sidebar): role `finance` melihat **seluruh blok Accurate Bridging** (Sales/Income/Return Shopee & TikTok, Auto-Sync, Kas Toko, Auto-Sync Retur, Auto Sync Income, Stok Accurate, Panduan Accurate) — identik dengan role `integration_accurate`. Sejak konsolidasi, blok ini muncul di FINANCE di bawah satu **sub-kategori collapsible "Integrasi Accurate"** — bukan lagi item rata yang terasa duplikat dengan kategori INTEGRATION ACCURATE. Satu sumber array `accurateBridgingMenus` (`components/layout/sidebar-menus.tsx`); versi finance di-map menambah field `group: "Integrasi Accurate"` (`accurateBridgingMenusGrouped`) yang dirender jadi header seksi collapsible di `components/layout/navigation.tsx`. **Anti-duplikat**: kategori INTEGRATION ACCURATE berdiri sendiri hanya di-push untuk user yang punya role `integration` **tetapi tanpa** `finance` (`components/layout/sidebar.tsx`) — user dengan kedua role melihat blok ini **sekali** saja di bawah FINANCE. **Detail per menu Accurate** didokumentasikan di bagian **Integration** di bawah (sengaja tidak diduplikasi di sini).

**IT** — Employee (aktivasi akun, reset password), Network (WiFi SSID/MAC CRUD)
**GA** — Inventory (QR, repair history, assignment)

**Form Builder** (`src/features/form-builder/`, rute `/form-builder*`, ✅ **merged 2026-08-01** — PR #680 kelola, #682 editor bertab, #683 analisa; kepemilikan per departemen PR [#691](https://github.com/bip-itteam-internal/erp-frontend/pull/691) **open**) — alat kelola form dinamis **per departemen**, sekarang aktif untuk HRGA dan IT; backend [[Microservices - Form Builder Service]], konsep [[IT - Form Builder]]:
- **Satu rute dipakai dua kategori.** Menu didaftarkan di kategori `it` **dan `hris`**, keduanya menunjuk `/form-builder` yang sama. Backend menyaring form per `owner_department`, jadi tiap tim hanya melihat miliknya tanpa dua halaman kembar yang harus dijaga sinkron. Dikunci `components/layout/form-builder-menu.test.ts`.
- **Kategorinya `hris`, bukan `ga`** (PR #691). `KATEGORI_UTAMA_DEPARTEMEN` di `sidebar-kategori.ts` menyatakan menu General Affair memang tinggal di kategori `hris`; selama dipasang di `ga`, orang HR dan GA hanya melihat Form Builder bila kebetulan punya peran `ga`.
- **Daftar departemen pemilik ditanyakan ke server** lewat `GET /me/capability`, tidak disalin ke FE. Daftar itu konfigurasi backend; menyalinnya berarti menu tampil untuk orang yang setiap aksinya ditolak server begitu tim menambah departemen. `lib/owner-module.ts` yang dulu menebak dari cookie `system_roles` **dihapus**.
- **Daftar** (`/form-builder`): Banner + MainTable, filter status, kolom penanda gerbang presensi, aksi terbitkan/tutup/hapus. Tombol transisi diturunkan dari `lib/lifecycle.ts` yang mencerminkan satu larangan backend — form terbit tak bisa mundur ke draft — supaya tombol yang pasti ditolak server tak pernah muncul.
- **Builder** (`/form-builder/new`, `/form-builder/[id]`): sembilan tipe pertanyaan, urutan bisa digeser, editor opsi, sasaran (semua / per departemen / per karyawan), dan pengaturan gerbang presensi. Halaman tersendiri, bukan modal — isinya tak muat nyaman di `ActionForm` yang dipakai form dua-field.
- **Tipe form + sasaran penilaian** (PR [#722](https://github.com/bip-itteam-internal/erp-frontend/pull/722), **merged & live di dev 2026-08-02**): tipe dipilih di tab Informasi Umum (Survei · Penilaian Karyawan · Pengajuan · Checklist) dan jadi **tab penyaring** memakai `CustomTabs` yang sudah ada. Penyaringannya di **server** lewat query, bukan di klien atas hasil satu halaman — penyaringan klien membuat tab "Penilaian" tampak kosong hanya karena seluruh form penilaian kebetulan ada di halaman kedua. Memilih "Penilaian Karyawan" membuka bagian **Sasaran Penilaian** di tab Pengaturan DAN menyembunyikan gerbang presensi, karena backend memang menolak gabungan keduanya.
	- Daftar jabatan untuk sasaran disusun dari **master data departemen yang sudah diambil halaman ini** (`master_department.positions`), bukan endpoint baru. Jabatan bernama sama di beberapa departemen digabung jadi satu pilihan berketerangan departemen pemakainya — tanpa itu "Admin" di lima departemen terlihat seperti lima pilihan identik.
	- Kolom **"Sudah Mengisi"** menampilkan jumlah **orang**, bukan jumlah jawaban; jumlah jawabannya jadi keterangan kecil. Pada form penilaian satu orang menghasilkan banyak jawaban, dan menampilkan 740 terbaca seolah empat kali lipat karyawan perusahaan sudah mengisi.
	- Kolom **Judul dibatasi lebarnya** dan dipotong `truncate`. `line-clamp` baru memotong SETELAH lebar kotaknya ditentukan, sedangkan tabel berlebar otomatis mengambil lebar dari isi terpanjang — satu deskripsi panjang menarik kolom Judul melar sampai kolom Pemilik, Status, dan tombol aksi terdorong keluar layar.
- **Pemilih karyawan ber-search** (PR [#723](https://github.com/bip-itteam-internal/erp-frontend/pull/723), **merged**): kotak teks Employee ID yang diketik tangan diganti `Combobox` **bersama** (bukan tiruannya) yang menampilkan nama, jabatan, dan departemen. Berlaku di sasaran pengisian maupun sasaran penilaian. Mengetik kode seperti `BIP-0001-01-20` menuntut pemakai menghafalnya, dan satu huruf keliru menghasilkan sasaran yang diam-diam tak pernah cocok dengan siapa pun sampai form terbit dan tak ada yang mengisinya. Employee ID tetap ikut di keterangan agar bisa dicari; id yang tak dikenal master data tetap ditampilkan apa adanya supaya form lama tak diam-diam berubah sasarannya.
- **Editor tak menuntut gulir balik** (PR [#696](https://github.com/bip-itteam-internal/erp-frontend/pull/696), **open**): tombol Tambah Bagian/Pertanyaan **menempel di puncak** daftar saat digulir dan **diulang di bawahnya**; item baru disisipkan **tepat setelah baris yang sedang digarap** (meniru Google Forms) lalu digulir ke layar dan judulnya difokuskan. Memindahkan tombolnya saja tak cukup — kalau item tetap ditumpuk di ujung, pengelola menekan tombol di tempat baru lalu tetap menggulir jauh untuk mengisinya. Tombol **Simpan pindah ke `AdminFooter`**, bilah bawah yang sudah dipakai `triage-view`; di tempat lamanya (sebaris dengan tab) ia terbawa naik saat daftar panjang digulir.
- **Keterangan ujung skala** (PR [#693](https://github.com/bip-itteam-internal/erp-frontend/pull/693)): dua isian opsional untuk tipe `scale`, dibatasi 40 karakter mengikuti backend. Dikosongkan jadi `undefined`, **bukan string kosong**, supaya `omitempty` backend benar-benar membuangnya alih-alih menyimpan keterangan hampa. `pruneFieldForType` ikut membuang keduanya saat tipe berpindah dari `scale` — pola yang sama sudah dipakai untuk `options` dan rentang angka, karena backend menolak atribut yang menempel di tipe yang tak memilikinya dan error itu tak terlihat asalnya di layar.
- **Bagian** (PR [#692](https://github.com/bip-itteam-internal/erp-frontend/pull/692)): tombol "Tambah Bagian" menyisipkan pemisah yang dirender `SectionRow` — komponen tersendiri tanpa pemilih tipe, tanpa saklar wajib, tanpa editor opsi. Menumpangkannya ke baris pertanyaan berarti setengah kendali di layar tak berlaku dan harus disembunyikan satu per satu, dan setiap yang lolos disembunyikan menghasilkan payload yang ditolak backend. **`section` sengaja tak masuk pemilih tipe**: kalau ia jadi pilihan tipe, pertanyaan yang sudah diisi atributnya bisa berubah jadi pemisah tanpa peringatan. Penomoran melewati bagian (`lib/sections.ts`) supaya nomor di editor cocok dengan nomor yang dilihat pengisi.
- **Susunan pertanyaan terkunci** begitu form punya jawaban, DAN saat jumlah jawaban belum diketahui. Menebak ke arah "boleh" berarti pengelola menyunting lalu kehilangan pekerjaannya di `409`.
- **Mode `block` memunculkan peringatan tegas**: halaman pengisian di [[APP - MyBharata]] kini **sudah ada**, tapi `form-builder-service` belum jalan di prod dan attendance-service masih pra-merge, jadi karyawan yang tertahan belum tentu punya cara menyelesaikannya.
- **Tanggal gerbang dikonversi ke RFC3339 offset WIB** (`lib/gate.ts`), tanggal selesai jadi `23:59:59`. Backend membandingkan jendela inklusif di kedua ujung; tanggal selesai yang dikirim sebagai awal hari akan mematikan gerbang pada dini hari tanggal itu dan hari terakhir praktis hilang tanpa terlihat.
- **Sasaran per departemen memakai ulang** `useFetchDepartments` milik Master Data, bukan endpoint sendiri. Sasaran per karyawan sementara diketik sebagai Employee ID per baris — combobox bersearch menuntut endpoint pencarian karyawan yang belum ada.
- **RBAC**: menu **tanpa** `perm` permission-set, karena backend form-builder masih memakai RBAC tier lama (`system_roles` key `it`/`ga`). Memasang `perm` akan menyembunyikan menu dari orang yang justru diizinkan server.
- `/api/form-builder/` masuk `COMPANY_SCOPED_READS` di `lib/axios.ts`, jadi daftar form ikut **CompanySwitcher** admin pusat.
- i18n domain `formBuilder` lengkap di `id.ts` + `en.ts` ([[ADR - 0010 Internasionalisasi (i18n) Dua Bahasa]]).
- **Editor bertab** (PR #682): Informasi Umum · Pertanyaan · Pengaturan Form. Tab dirender kondisional (bukan disembunyikan CSS) sehingga nilai form tetap utuh saat berpindah, dan **simpan yang gagal memindahkan tab lebih dulu** ke tab bermasalah paling awal — tanpa itu modal error menunjuk field yang tak ada di layar. Tombol kembali memakai `SidebarBackButton` bersama, bukan tombol sendiri di badan halaman.
- **Halaman analisa jawaban** (`/form-builder/[id]/analytics`, PR #683) — meniru bentuk Google Form dengan tiga tab **Ringkasan · Pertanyaan · Individu**, plus export CSV. Dibuka lewat ikon grafik di kolom Aksi:
	- **Ringkasan**: kartu angka (`StatSummary` yang sudah ada), tren harian, dan rekap per pertanyaan menurut tipenya. Kartunya **dua kolom** mulai layar lebar — satu kartu selebar halaman per pertanyaan membuat batang melar sampai ujung dan form belasan pertanyaan jadi gulungan sangat panjang.
	- **Pertanyaan**: pilih satu pertanyaan, lihat seluruh jawabannya beserta nama pengisi. **Individu**: satu responden per layar; indeksnya global sehingga navigasi melintasi batas halaman (200 per halaman) tanpa pemakai sadar ada paginasi.
	- ⚠️ **Identitas responden ditampilkan penuh** (nama, departemen, jabatan). Keputusan sadar pemilik produk, konsekuensi & alasannya di [[IT - Form Builder]]. **Pengecualian**: form penilaian ber-`anonymous` dikosongkan identitas penilainya oleh backend sebelum sampai ke sini.
	- **Tab "Yang Dinilai"** (PR [#724](https://github.com/bip-itteam-internal/erp-frontend/pull/724), **merged 2026-08-02**) — hanya muncul pada form penilaian. Menjawab pertanyaan yang tak dijawab satu pun tab lama: bukan "apa jawaban orang-orang", tapi **"bagaimana nilai tiap orang"**. Tabelnya berisi nama, jabatan, jumlah penilai, dan rata-rata per pertanyaan bernilai angka. Yang belum dinilai siapa pun tampil sebagai **tanda hubung, bukan nol** — menampilkan 0 membuat orang yang justru terlewat terlihat seperti yang terburuk. Kartu "Karyawan Mengisi" berganti jadi **"Penilai Selesai"**, yang menghitung penilai yang menyelesaikan SELURUH daftarnya.
	- **Bentuk chart ditentukan `lib/chart-form.ts`, bukan selera**: **pie** hanya untuk pilihan **TUNGGAL** beririsan 3–6, selebihnya batang. **Checkbox selalu batang** karena satu orang boleh memilih beberapa sehingga irisannya berjumlah >100% dan pie akan berbohong soal proporsi (Google Form pun begitu). Dua irisan juga batang — pie dua irisan hanyalah satu angka yang dibungkus lingkaran. Bar chart berseri tunggal memakai **satu warna** (kategorinya sudah dinamai sumbu, warna berbeda hanya menyiratkan identitas yang tak ada) dan **tanpa legend**.
	- ⚠️ **Palet pie `--fb-seri-1..6` DIVALIDASI, jangan diganti tanpa uji ulang.** Menambah pie berarti fitur ini memakai palet **kategorikal**, jadi paletnya dijalankan lewat validator skill `dataviz` alih-alih dikira-kira. Hasilnya menolak token bawaan: **`--chart-4` (#ffb900) dan `--chart-5` (#fe9a00) di tema terang hanya berjarak ΔE 7,4 untuk penglihatan NORMAL** (ambang 15) sehingga dua irisan itu tak terbedakan siapa pun, dan `--chart-3` di bawah ambang chroma sehingga terbaca abu-abu. Token `--fb-seri-*` di `globals.css` lolos seluruh pemeriksaan di kedua tema. Peringatan kontras <3:1 di tema terang dijawab **legend berlabel + jumlah + persentase tertulis**, jadi identitas irisan tak pernah bersandar warna saja. Ini juga alasan token tersendiri: `--chart-1..5` berganti hue antar tema, dan di pie warna menandai identitas opsi.
	- **Penanda `truncated` dan penyembunyian `response_rate` diteruskan sampai ke layar** persis seperti yang dikirim backend; keduanya menjaga agar hasil terpotong atau sasaran-tak-diketahui tak tampak seperti angka utuh.
	- **Export lewat blob**, bukan `<a href>`: endpoint-nya butuh Bearer token yang hanya dipasang interceptor axios. Nama berkas dibersihkan di klien (`lib/filename.ts`) karena `Content-Disposition` backend tak terpakai di jalur ini.
- **Belum ada**: **halaman isi form untuk karyawan** — pengisian memang direncanakan lewat [[APP - MyBharata]], bukan web.
**Integration (marketplace ↔ Accurate)** — Ads Analytics (Shopee/TikTok), Ads Management, Auto-Approve log, Marketplace log, Master Data (Product, Config Accurate), Shipment Setting, OAuth; `integration-accurate`: Sales / Income / Return Shopee & TikTok-Shop + **Auto-Sync** + **Auto-Sync Retur** + **Panduan Accurate** — kategori sidebar INTEGRATION ACCURATE ini kini hanya tampil untuk user `integration` **tanpa** role `finance`; yang punya `finance` melihat blok sama sebagai sub-kategori "Integrasi Accurate" di FINANCE (lihat bagian Finance)
- **Auto-Sync** (`/integration-accurate/auto-sync`, ✅ 2026-07-11): monitoring faktur harian auto-sync RTS→Accurate — tabel (tanggal WIB, toko, channel, invoice, status badge SENT/FAILED/PENDING, lines, error) + filter range tanggal (Dari–Sampai), dropdown Toko (nama+ID, auto dari master shops via `useFetchShops`, ter-filter channel), status; baris FAILED highlight + tombol **Retry** (sinkron ke `POST /accurate/daily-invoices/:id/retry`); refetch 60s. Modal detail faktur (form ala Accurate) menampilkan **Departemen & Proyek** per toko (PR FE #240 — nilai dari Config Accurate via `GET /accurate/settings/shops`, sumber yang sama dgn yang dikirim auto-sync ke baris faktur; toko tanpa config = "-"). Seksi **Penyesuaian Sampel** di bawah tabel faktur (PR FE #254): dokumen Penyesuaian Persediaan (IA) per toko-hari utk order sampel gratis TikTok — tanggal, toko, nomor IA, status, baris, error, Retry (`/accurate/daily-adjustments`); klik baris → modal detail dokumen (baris barang keluar + pesanan sampel sumber, PR FE #260). Halaman Summary Shopee & TikTok: badge status **AUTO** ("dari Auto-Sync", tombol Send/Delete Sales disembunyikan) + tombol **Export Multiple** (multi-toko + rentang hari kirim → 1 Excel, sheet per toko via endpoint `export-multiple`). Backend: [[Microservices - Integration Service]] bagian Auto-Sync RTS.
- **Auto-Sync Retur** (`/integration-accurate/auto-sync-return`, ✅ Shopee & TikTok JALAN 2026-07-16; **Lazada ditambahkan 2026-07-28** — dropdown channel kini di-map dari `MARKETPLACE_CHANNELS` (sumber tunggal) alih-alih union `"SHOPEE" | "TIKTOK"` yang ditulis tangan, sehingga menambah channel tak lagi menyentuh halaman ini): monitoring retur auto-sync (Retur Penjualan) → Accurate — tabel (**Tanggal = tanggal RETUR terjadi** (`trans_date`) dgn hari kirim tampil kecil sbg konteks — sebelum 2026-07-15 kolom ini menampilkan hari kirim, membuat retur "tak ketemu" saat dicari by tanggal retur; **badge amber "cek tanggal"** (2026-07-17, dulu "tanggal perkiraan") HANYA bila `date_source == "shipped"` = timestamp retur marketplace hilang, anomali — accepted/requested tak ditandai karena itu tanggal sah kebijakan accepted-seragam; tooltip menjelaskan asal tanggal; toko, channel, No Retur, faktur sumber, status badge **SENT/FAILED/PENDING/SKIPPED** — **SKIPPED** tampil abu-abu **"DILEWATI"** tanpa tombol Retry (2026-07-18; sengaja dilewati & benar, bukan kegagalan → merah hanya utk yang benar-benar perlu perhatian; error-nya pun abu-abu "Alasan dilewati:" bukan merah). **Dua sebab DILEWATI** (sebab kedua sejak 2026-08-05): retur era manual pra-cutover, **atau** order ber-payout>0 yang refundnya sudah diserap Auto Sync Income — yang kedua barisnya murni jejak supaya keputusan itu tak lagi tak terlihat. Karena baris jejak tak pernah punya nomor retur, kolom **No Retur** jatuh ke **nomor pesanan** saat `return_number` kosong (sebelumnya "-" → baris mustahil dikenali dari daftar), lines, refund, error) + **form "Cari Pesanan"** (input search: nomor pesanan **atau** nomor retur, debounce 400ms, tombol clear — 2026-07-15) + filter tanggal/toko/channel/status; baris FAILED highlight + **Retry**; **kolom Error klik → dialog teks-penuh bisa di-copy** (2026-07-16; pesan Accurate sering panjang, terpotong di sel); export Excel (**ikut filter search**); modal detail (item kode-Accurate + order sumber; **`price_note`** amber bila harga tak dapat ditentukan krn faktur sumber tak terbaca — modal tak lagi 500); refetch 60s. Kolom **Mode**: "Barang dikembalikan" (restock) / "Barang tidak dikembalikan" (refund-only). *Status `FULL_VIA_INVOICE`/`REFUND-ONLY` + tombol "Push ke Accurate" **sudah tidak ada** — dihapus [[ADR - 0022 Retur via Sales Return per Mode + Keep Invoice Line]] yang menggantikan [[ADR - 0012 Retur Refund-only Push-all]].* Backend: [[Microservices - Integration Service]] bagian Auto-Sync Retur. Grounded: `app/(main)/integration-accurate/auto-sync-return/page.tsx`, `features/integration/accurate/hooks/use-fetch-daily-returns.ts`.
- **Auto Sync Income** (`/integration-accurate/receipts`, ⚠️ jalan dgn catatan): monitoring **Bukti Terima Kas** otomatis — saat marketplace mencairkan uang ke saldo toko, sistem mencatatnya di Accurate lengkap dgn rincian beban & rekening kas toko (**pencatatan, bukan pemindahan uang**; penarikan ke bank tetap manual finance lewat Seller Center — marketplace tak membuka API pemicu penarikan). Tabel: tanggal WIB, toko (nama di-resolve dari master shops), channel, nomor bukti terima, nilai cheque, status badge **SENT/FAILED/PENDING**, badge **"koreksi (n)"** per baris (satu fetch `/accurate/receipt-adjustments` lalu digrup client-side per (channel, toko, tanggal) — tanpa endpoint baru; void tak dihitung, amber bila ada TERSUSUL/INVALID); baris FAILED + **Retry**; modal detail; export Excel; refetch 60s. **Form "Cari Penerimaan"** (2026-08-04, meniru Auto-Sync & Auto-Sync Retur): nomor bukti terima / nomor statement TikTok / **nomor pesanan**, debounce 400ms, tombol clear, **ikut ke export**. Nomor pesanan di-resolve server jadi kunci penerimaan (detail mekanisme + caveat EXACT-match: [[API - Integration Service]]). ⚠️ **Filter tanggal sebelumnya inert-lalu-merusak**: halaman meneruskan nilai `<input type="date">` (`YYYY-MM-DD`) apa adanya ke API yang membandingkannya dgn `date_wib` (`YYYYMMDD`) — memilih "Sampai Tanggal" mengosongkan tabel, "Dari Tanggal" saja tak menyaring apa pun; **difix 2026-08-04 di handler** (normalisasi tanggal), FE tak berubah. ⚠️ Opsi **PENDING** pada dropdown status **tak pernah cocok apa pun** (field `last_status` absen sampai percobaan kirim pertama) — belum diperbaiki. Backend: [[Microservices - Integration Service]] bagian Auto Sales Receipt. Grounded: `app/(main)/integration-accurate/receipts/page.tsx`, `features/integration/accurate/hooks/use-fetch-receipts.ts`.
- **Summary Return** (`/integration-accurate/return/{all,shopee,tiktok,lazada}`, ✅ 2026-07-25; **lazada 2026-07-28**): sekawan dgn Summary Sales, kini terisi data auto-sync. Tabel **shopee/tiktok** membaca `transaction_summary_reports` tipe `SALES_RETURN` status `AUTO` — diisi worker `auto-summary-report-return` + hook realtime (lihat [[Microservices - Integration Service]] "Summary otomatis RETUR"). Halaman **`/return/all`** = kartu ringkasan (bukan tabel) via `GET /accurate/daily-returns/stats`: **Ringkasan Retur** (Jumlah Order · Jumlah Barang fisik discan · Nilai Pengembalian — gabungan + rincian **per channel**; sejak 2026-07-28 kartu ini berbasis daftar `MARKETPLACE_CHANNELS` — `useQueries` satu query per channel, `sumReturnStats` variadik, judul kartu ikut daftar channel — bukan lagi prop `tiktok`/`shopee` yang ditulis tangan) + **Penanganan Gudang & Pembukuan** (Menunggu Discan / Sudah Discan / Terbukukan / Gagal), difilter rentang **tanggal retur** (`trans_date`) via DatePicker; refetch 60s. Sumber satu koleksi (`accurate_daily_returns`) → angka **rekonsiliasi** dgn tab Auto-Sync Retur. **Beda peran dari Auto-Sync Retur**: yang ini ringkasan/gambaran besar; Auto-Sync Retur = meja kerja per-baris (retry/error/export). Grounded: `app/(main)/integration-accurate/return/all/page.tsx`, `features/integration/accurate/return/hooks/{use-return-all-page,use-fetch-return-stats}.ts`.
- **Stok Accurate** (`/integration-accurate/stok`, ✅ live 2026-07-11, PR #230/#232/#234/#235): pembanding stok read-only, 2 tab + keterangan per-tab — "Stok Gudang" (semua item + qty dari salinan lokal; toggle **"Hanya produk jualan"** default ON; badge pembaruan **Realtime** (webhook) / **Berkala (10 menit)**; cari; auto-refresh 60s) + "Stok per Listing" (5 kolom berlabel: SKU Listing | Komponen | **Isi per Paket ×N** | **Stok Gudang** | **Stok Efektif**; komponen per baris rowspan; badge **"belum termapping"** utk komponen tanpa link kode Accurate — bukan stok 0; incl. cek live per-SKU). Halaman Panduan diperluas: section Stok (dua sudut pandang, contoh visual ×N & pembagian stok efektif, badge belum termapping) + penjelasan **harga 0 pada komponen paket di faktur** (persis perilaku Accurate). Teks modal detail faktur dikoreksi: "pesanan yang sudah dibawa kurir" (selaras trigger SHIPPED). **Ditautkan juga ke WMS ERP OPERATIONAL untuk monitoring** (menu top-level "Stok Accurate" di modul `manufacture` → route sama `/integration-accurate/stok`, READ-ONLY): akses via tab baru `stok_accurate` = `["*"]` (semua pemegang role WMS operasional non-staff + PPIC/SPV) di `features/manufacture/akses.ts` (`MATRIKS_TAB_WMS` + `SLUG_KE_TAB_WMS` dgn key path penuh karena `bolehUrl` hanya mengupas prefix `/manufacture/`). Aman dibuka WMS tanpa role integration: endpoint `/accurate/stocks*` tak dikunci role di gateway (`/api/:module/*` cukup JWT) maupun di integration-service. User yang punya finance TANPA manufacture tak melihatnya di WMS (`TAB_WMS_FINANCE` hanya `finished_goods`) — mereka sudah punya Stok Accurate di FINANCE.
- **Panduan Accurate** (`/integration-accurate/panduan`): halaman panduan tim finance — cara kerja auto-sync, koreksi batal/retur (faktur tak pernah dihapus), jaminan akurasi (harga promo/bundle/anti-dobel), arti status, kapan kirim manual masih dipakai.
- **Peringatan pencatatan ganda** di manual send SALES_INVOICE (halaman detail summary): backend balas 409 bila rentang beririsan faktur auto-sync → panel kuning daftar tanggal+faktur bentrok + tombol "Tetap Kirim" (force) / "Batal".
- **Ads Analytics GMV** (`/integration/ads-analytics`, TikTok+GMV): header dropdown flow: Account → Advertiser → Shop → Date Range. **Fix 2026-07-09**: (1) Shop list kini di-derive dari campaigns per advertiser (endpoint `/gmv_max/campaigns`) — bukan dari `/stores` yang return store sama untuk semua advertiser; ini memastikan tiap advertiser tampilkan shop yang benar-benar punya campaign aktif. (2) State shop di-reset ke kosong saat advertiser berganti. (3) Shop auto-select first valid option saat selection kosong/tidak valid. Data matrix (tabel performance) ditarik via `ListGMVMaxPerformanceReport` yang sebelumnya broken karena field path salah (lihat catatan di [[Microservices - Integration Service]]).
- **Transactions** — sub-navigasi dua halaman:
  - *Dashboard* (`/integration/transactions`): 4 kartu status live (Pesanan Baru/Belum Di Proses/Siap Dikirim/Pesanan Selesai) via `GET /transactions/orders/dashboard/summary`; **tabel status sinkronisasi per-toko** (`ShopSyncStatusTable` — jadwal & terakhir sync TikTok/Shopee per-toko, branch `feat/order-sync-status-card`); filter Platform + Shop + rentang tanggal kustom; grafik perbandingan 4 tab (Potensi Penjualan, Produk Terjual, Penjualan Selesai, Pesanan Selesai) dengan granularity hourly/daily otomatis via `GET /transactions/orders/summary/comparison`.
  - *Order Management* (`/integration/transactions/list`): tabel order terpadu + filter (platform, shop, status, date range, Canceled By — tampil hanya saat status = CANCELLED) + 4 kartu ringkasan (Total Revenue, Total Orders, Total Cancel, Total Return) + search global order ID + tombol Accurate Integration. Detail order: Payment Method + Due Date ditampilkan di seksi Shipping Information.
**Marketing Insight** (`/marketing-insight`, feature `src/features/marketing-insight/`) — landing card + sidebar submenu; submodul: Customer Demography, GMV Max Monitoring, **Product Performance** (`/marketing-insight/product-performance` — toggle channel Shopee(GMS)/TikTok(GMV Max); tab TikTok: KPI cards spend/revenue/ROAS/produk + tabel sortable client-side **Produk · SKU · Toko · Spend · Revenue · Orders · Cost/Order · ROAS · ROI(%)** + search + filter toko/tanggal + **Export Excel** per filter aktif; **kolom SKU** = `master_sku` produk dari BE `skus` (join `product_sku_mappings`), comma-join + truncate + tooltip, "-" bila belum ter-map; fetch `/api/integration/tiktok/business/report/gmv_max/overview`), dan **Affiliate Performance** (`/marketing-insight/affiliate`): KPI cards (GMV, komisi dibayar/estimasi, order·creator) + kartu Validasi Komisi (cross-check finance statement: tervalidasi/selisih/belum-ada-bukti/menunggu); satu tabel ber-tab (Order | Top Creator | Top Produk) dengan nama+gambar produk (enrich \$lookup `tt_business_store_products` via item_group_id); klik baris order → modal Detail Transaksi Affiliate (produk, creator, konten, model/rate komisi, est/actual, status, verdict validasi finance); filter status validasi; date range picker tanpa default (max 3 bulan); section Update Otomatis (jadwal pipeline bahasa awam + tarikan/validasi terakhir + riwayat 10 batch); fetch react-query → `/api/integration/affiliate/*` ([[API - Integration Service]]). **Catatan Filter Toko (Shopee vs TikTok)**: Untuk submodul Product Performance & Affiliate platform Shopee, filter **"All Shop"** diimplementasikan menggunakan **Client-Side Aggregation** (eksekusi paralel `Promise.all` di React Query hooks lalu di-merge di frontend) karena API Shopee mewajibkan parameter `shop_id` tunggal. Berbeda dengan TikTok GMV Max yang mendukung filter multi-toko langsung di level server.

- **Gross Profit / Laba Sejati** (`/integration/gross-profit`, feature `src/features/integration/gross-profit/`) — ✅ *live (merged + deployed)*. **Multi-channel**: toggle `[All][Shopee][TikTok][Lazada]` (All = gabung, **default**); label breakdown **channel-aware** — tiap MP pakai istilah sendiri (Shopee: Biaya Administrasi/Layanan/Transaksi/Ongkir/Voucher; TikTok: Komisi Dasar/Dinamis/Kreator/Cashback/Infra/Mall; Lazada: Komisi/Biaya Pembayaran/Affiliate/Pengiriman/Voucher; All: istilah netral generik) via `lib/breakdown-config.ts` `breakdownRows(channel)`; komponen MP-only disembunyikan di channel lain. Waterfall per-produk pakai istilah channel asli produk. Laba kontribusi per produk/SKU mengikuti blueprint dashboard marketing (`GMV − biaya MP − promo = net settlement − HPP − iklan = laba`; retur kolom informasi). 3 tab: **Profit** (kartu **Arus Dana**: Sudah Cair actual vs Uang Gantung ± estimasi + baris akurasi rumus 30 hari + collapsible rincian per toko×bulan + tombol "Lihat daftar order" → side-panel order gantung (order id/toko/akun/tgl/GMV/umur, `/profit/cash-flow/orders`) — `cash-flow-cards.tsx`, endpoint `/profit/cash-flow`; kartu ringkasan alur GMV→Net→Laba (KPI per komponen: GMV · Komisi · Affiliate · Cashback · Infra · Mall · Proses-lain · Ongkir · Promo · Net · HPP · Iklan · Retur · Laba · Margin, tiap kartu + % dinamis thd GMV); toggle mode data **Cair saja (default) | Semua order (±)** — mode cair = murni settlement actual tanpa estimasi, iklan prorata porsi cair; toggle Produk|Bundle — kedua tabel **identik struktur** via komponen bersama `profit-table-shared.tsx` (grup kolom berwarna, legend, sort, footer TOTAL, badge Aksi SCALE/JAGA/PERBAIKI/HENTIKAN; grup − BIAYA MARKETPLACE 8 sub-kolom Komisi-Dasar|Komisi-Dinamis|Affiliate|Cashback|Infra|Mall|Proses-lain|Ongkir; tanda estimasi ± hanya di sel BIAYA settlement — GMV/HPP/Iklan selalu pasti, GMV dari seluruh penjualan bukan dari settlement); tabel Bundle: kolom pertama SKU bundle + subteks komposisi, caption anti-dobel-hitung lintas tab; klik baris → panel samping waterfall Rincian Biaya (+ blok **Periode HPP** bila produk pecah periode) + tabel per-SKU qty/GMV/Net/Laba, porsi bundle expandable; produk yang **HPP-nya berubah di rentang tanggal** tampil sebagai **baris induk "Total" + sub-baris expandable per periode HPP** (chevron, `formatPeriodID` "1–14 Jul 2026", semua kolom qty→margin per periode — tidak dirata-rata; `profit-table.tsx`)), **HPP** (upload xlsx finance format `SKU|Produk|HPP/Pcs` + tambah/edit/hapus HPP manual per baris + filter nama/periode + badge Aktif/riwayat; HPP bundle terhitung read-only), **Mapping** (generate 2 fase dari master+nama finance, tabel Single|Bundle grouped per SKU, edit nama inline, panel approve saran dengan status simpan Menyimpan…/Tersimpan ✓ + guard klik ganda). Tombol **Export Excel** (tab Profit) mengekspor baris produk yang sedang tampil (ikut filter tanggal/toko/mode/search) ke `.xlsx` client-side (exceljs, kolom alur blueprint lengkap + pecahan Proses, nilai mentah; kolom **"Periode HPP"** — produk pecah periode diekspor baris TOTAL + baris per-periode via `flattenForExport`). Mutasi hanya role `supervisor|admin` modul `integration` ATAU `finance`. Backend: [[API - Integration Service]] rute `/profit/*`.

**Manufacture (WMS)** (`/manufacture/*`, feature `src/features/manufacture/`) — modul WMS manufaktur di balik `ManufactureGuard`; navigasi lewat sidebar ERP (Dashboard · Master & Referensi · Operasional Gudang · Order & Dokumen · Pengawasan · KPI). Backend: [[API - Manufacture Service]].
- **Gudang Barang Jadi (FG)** (`/manufacture/finished-goods`) — 3 tab: *Input Gudang FG (Inbound)* · *Return Dari Ekspedisi* · *Keluar FG (Marketplace Sales)*. Bisa dibuka **read-only** lintas-modul oleh pemegang role `finance` dan modul **`wms_viewer`** (system-role monitoring luar-WMS, 2026-08-05; export laporan retur tetap bisa — tombol Export tak digate read-only; ditegakkan FE `TAB_WMS_VIEWER` + backend `tabViewerReadOnly` di [[Microservices - Manufacture Service]]).
	- **Return Dari Ekspedisi** ✅ — tabelnya bersumber **retur marketplace** (`/api/manufacture/returns`), bukan lagi ledger lokal. Tiap baris berbadge **Belum Dicatat** / **Sudah Dicatat** / **Tanpa Barang Balik** (refund-only), dengan hitungan belum-dicatat di header sebagai **sinyal Finance**. Klik baris *Belum Dicatat* → modal Form Return Ekspedisi terbuka **prefilled** (SKU + qty retur marketplace sebagai acuan; pembagian Reuse/Rework/Reject tetap penilaian pengecek) dan membawa kunci join, sehingga setelah simpan baris berubah status sendiri. Baris yang **sudah** dicatat tak bisa diklik — form ini selalu membuat transaksi baru (tak ada jalur edit), jadi simpan kedua akan menambah stok dua kali. Bundle dipecah ke komponen (stok FG per komponen). Gagal memuat feed tampil sebagai **banner merah**, bukan tabel kosong — "tak ada retur" dan "gagal memuat" wajib bisa dibedakan. **Dilengkapi 22 Juli 2026** ([[ADR - 0025 Log Sumber vs Input WMS + Stempel Penginput]] #8–#10): retur barang-balik **ditahan sampai gudang scan** (gerbang backend); form punya panel konteks read-only + pengaman qty/status + **scan resi paket BALIK** (`reverse_tracking_no`, Shopee `get_return_detail`) yang cocok langsung ke retur; daftar difokuskan (aktif vs "basi" terlipat) dengan **search + filter channel** (Lazada ditambahkan ke dropdown filter & export 2026-07-28); **fallback "input NOMOR ORDER"** saat scan resi gagal kini benar-benar menjemput untuk Shopee & Lazada juga — sebelum 2026-07-28 jalur `resolve=1` dipaku ke TikTok sehingga order channel lain selalu dijawab "tak punya retur" tanpa ada yang mencoba (lihat [[Microservices - Integration Service]]); ini penting khusus Lazada karena sebagian retur Lazada tak punya resi-balik sama sekali; **export laporan xlsx fleksibel** (pilih kolom + filter + toggle per-retur/per-SKU); dan **"Nama Pengecek (PIC)" auto-isi** dari `full_name` JWT (default, bukan stempel audit).
	- Record hasil input gudang — termasuk catatan manual lama tanpa kunci join — kini tampil di sub-tab **Input WMS** (lihat di bawah), bukan lagi berbaur di tabel feed sebagai *Catatan Manual*. Tak ada data yang lenyap, hanya berpindah tempat.

- **Log sumber vs input WMS** ✅ (21 Juli 2026, [[ADR - 0025 Log Sumber vs Input WMS + Stempel Penginput]]) — log di **lima** menu dipecah dua sub-tab: data **dari sumber** vs data hasil **input gudang**. Tujuannya rekonsiliasi Finance: apa yang seharusnya masuk/keluar vs apa yang sudah dicatat.

	| Menu | Tab sumber | Tab Input WMS |
	|---|---|---|
	| Return Dari Ekspedisi | feed retur marketplace | transaksi retur WMS |
	| Keluar FG | resi marketplace **agregasi per SKU** | transaksi keluar WMS |
	| Inbound RM Harian (Gudang RM) | Procurement PO | penerimaan WMS |
	| Input Gudang FG | Surat Jalan `IN TRANSIT` | penerimaan WMS |
	| Kirim FG ke Gudang Jadi (Gudang RM) | Laporan Produksi belum dibuatkan SJ | Surat Jalan WMS |

	- **Baris sumber bisa diklik → form terbuka terisi**, meniru pola Return. Untuk Input Gudang FG & Kirim FG logika pengisian diekstrak jadi fungsi bersama (`pilihSjUntukTerima()` / `pilihLaporanProduksi()`) sehingga dropdown di dalam form & klik baris memakai satu jalur.
	- **Badge status hanya di Return** — Keluar FG & Inbound RM tak punya kunci join (transaksi tak menyimpan nomor resi/PO), jadi sengaja tanpa badge; menebak status di sana lebih berbahaya bagi Finance daripada tidak ada badge. Input Gudang FG & Kirim FG tak butuh badge: daftarnya per definisi sudah berupa tunggakan (`IN TRANSIT` / laporan tanpa `refLaporanProduksi`).
	- **Inbound RM multi-bahan** (22 Juli 2026) — form kini multi-baris: klik baris sumber PO memuat **seluruh** bahan PO sebagai baris sekaligus (Qty SJ dari qtyOrder; Lolos/Reject diisi operator), baris bisa ditambah/dihapus untuk memfilter sebelum submit, dan tiap baris valid membuat satu transaksi INBOUND terpisah. Tiap baris punya field **Nama Barang** readonly (otomatis dari master; kode tak dikenal ditandai). Menggantikan versi lama satu-bahan-per-input yang hanya mengisi supplier & tanggal. Lihat [[ADR - 0025 Log Sumber vs Input WMS + Stempel Penginput]].
	- **Tab sumber Keluar FG diagregasi per SKU** (bukan daftar per-AWB) supaya setingkat dengan tab WMS yang juga per SKU — daftar per-resi tak punya pasangan untuk dibandingkan. Bundle dipecah ke komponen (kode bundle asal tetap tampil sebagai keterangan). Disaring per tanggal RTS; 931 resi → 19 baris SKU di dev.
	- **Hanya resi `COMPLETED` + `SHIPPED`** yang terhitung keluar gudang (kanonikalisasi status di `features/manufacture/resi-status.ts`, dipakai bersama Master Resi). Aturan ditegakkan di **semua** pintu masuk form — klik baris, **Ambil Rentang**, dan **scan resi** — bukan hanya di tab sumber. Di Ambil Rentang filter dipasang saat memasukkan, bukan saat menomori, supaya nomor 1..N tetap cocok dengan Master Resi. Scan ditolak dengan alasan + status resinya, tidak dilewati diam-diam.

- **Panel "UAV Patrol / Inventory Intelligence" & grafik stok — disembunyikan di balik flag** ⚠️ (22 Juli 2026) — keduanya sempat dibongkar (21 Juli) atas alasan kejujuran (lihat di bawah), lalu **dikembalikan ke bentuk semula namun dinonaktifkan**: markup asli (Bento grid berisi grafik + panel kamera) kini dibungkus `{TAMPILKAN_GRAFIK_KAMERA && (…)}` dengan `const TAMPILKAN_GRAFIK_KAMERA = false` di `DashboardView.tsx`, jadi tak dirender sampai ada data/integrasi sungguhan. Keputusan user: simpan wujud aslinya, sembunyikan dulu — bukan hapus. Modal telemetri tetap ada namun tak terjangkau selama flag mati.
	- **Alasan pembongkaran awal (21 Juli) tetap berlaku bila flag dinyalakan tanpa data nyata:** panel UAV **sepenuhnya fiktif** — kamera hardcoded (termasuk *"Sektor B2 - Electronics Hub"* di pabrik farmasi), status ONLINE/FPS/suhu karangan, "live feed" berupa foto Unsplash (`alt="Simulated Stream"`), teks *"patroli drone berikutnya 15 menit"* tanpa penjadwal. Grafik berjudul *"Grafik Stock (30 Hari Terakhir)"* dengan toggle WEEKLY/MONTHLY padahal `monthlyData` = `weeklyData` (satu titik = stok hari ini) — snapshot stok harian tak pernah disimpan (yang ada `manufacture_saldo_awal_bulanan` per bulan + ledger). **Prasyarat sebelum flag dinyalakan:** kamera/drone butuh integrasi nyata; tren 30 hari butuh backend penyimpan snapshot stok harian — **TBD**.

- **Tabel Fast Moving tak lagi terpotong** ✅ (22 Juli 2026) — pembungkusnya diberi kelas opt-out `wms-no-clamp` yang mengecualikannya dari aturan `max-height: calc(100vh - 13rem)` di `globals.css` (`.wms-root .overflow-x-auto:not(.wms-no-clamp)`), sehingga 10 baris tampil utuh, bukan hanya ~2 dengan scroll internal.

- **Input `type=number` di WMS tak berubah saat scroll** ✅ (22 Juli 2026) — handler `wheel` global di `manufacture-app.tsx` mem-`blur()` input number yang sedang fokus begitu roda mouse diputar, menutup perilaku bawaan browser yang menaik-turunkan angka (mis. Qty SJ/Lolos/Reject di form Inbound RM) saat pengguna sekadar menggulir halaman.

- **PO Marketing multi-barang + pemisah ribuan** ✅ (22 Juli 2026) — form PO Marketing (menu Manajemen Order) kini multi-baris: satu PO memuat banyak barang (tambah/hapus baris, klik log sumber PO memuat semua bahan sekaligus). Field **Order Quantity** memakai pemisah ribuan tampilan (`30000` → `30.000`, locale id-ID; nilai disimpan digit mentah) — sekaligus lolos dari bug scroll karena jadi `type=text` + `inputMode=numeric`. Model backend `MarketingPO` dapat field **`items[]`** (`MarketingPOItem`); field top-level `product_sku/name/order_qty` tetap diisi (item pertama + total qty) demi kompatibilitas pembaca lama (agregasi Dashboard, cetak, arsip). Arsip & pratinjau cetak menampilkan seluruh barang + baris Total. Lihat [[Microservices - Manufacture Service]]. **Terkait**: baris log sumber PO Marketing di sub-tab Pengadaan bisa diklik → mengisi form Permintaan Pengadaan (PPIC) dari produk pertama PO.

- **Bukti Selisih RM — tab Log Sumber** ✅ (22 Juli 2026) — menu Bukti Selisih RM (`SelisihRmView`) mendapat tab **Sumber** berisi transaksi **Bahan Keluar ke Produksi** (`OUTBOUND` ber-`namaProdukJadi` — kejadian nyata gudang RM menyerahkan bahan ke produksi). Klik baris → form **Catat Kirim** terbuka terisi otomatis (kode+qty+keterangan konteks), tinggal tambah foto bukti. Meniru pola klik-baris-sumber [[ADR - 0025 Log Sumber vs Input WMS + Stempel Penginput]], tapi tanpa kunci join pasti (transaksi keluar tak simpan id catatan selisih) → hanya badge lunak "ada catatan cocok" (heuristik kode+qty+tanggal), bukan klaim status. Tanpa perubahan backend (pakai `POST /selisih-rm` yang ada).

- **Pratinjau cetak = kertas terang di kedua tema** ✅ (22 Juli 2026) — tiga pratinjau cetak di Manajemen Order (SPK Material Order, PO Marketing, PO Pengadaan) sebelumnya campur token tema (`bg-card`) + warna hardcoded (`text-black`) → di mode gelap jadi hitam-di-atas-gelap (tak terbaca). Diperbaiki dengan meng-scope-ulang token desain (`--card`, `--foreground`, dst) ke nilai **terang** langsung pada elemen "kertas" via **inline CSS-var** (`PAPER_LIGHT_VARS`) + kelas cadangan `.wms-print-paper` di `globals.css`. Pilihan inline sengaja: menang atas `.dark` (variabel diset pada elemen itu sendiri) **dan** aman dari HMR global-CSS yang kadang tak recompile — mekanisme sama seperti yang dipakai `.dark`.

- **Card header WMS netral & sadar tema** ⚠️ (21 Juli 2026) — enam card "Input Data …" (Dashboard ×2, Gudang RM, Gudang FG, Kirim FG, Material Order) diubah dari blok berwarna pekat (`bg-emerald-900`/`bg-slate-900`, dipatok gelap di kedua mode) menjadi `bg-card` + border yang ikut tema. **Isi card ikut dikonversi** — teks putih & `bg-white/10` di dalamnya hanya terbaca di atas card gelap, jadi mengganti latar saja akan membuat isinya lenyap di mode terang. Sekalian diperbaiki kontras mode gelap di Dashboard (26 varian `dark:`) + satu bug `bg-white/90` dengan `text-foreground` (= putih-di-atas-putih di mode gelap). Terukur: elemen berkontras <3 di Dashboard **54 → 14**.
	- ⚠️ **Sisa 14 semuanya dari `var(--manufacture)`** yang nilainya **identik di `:root` dan `.dark`** (`#065f46`, emerald-800) — hijau tua di atas latar gelap. **Belum diperbaiki** karena variabel ini dipakai dua peran: **116×** sebagai warna teks (perlu diterangkan di mode gelap) dan **90×** sebagai warna latar dengan teks putih (akan rusak bila diterangkan). Perbaikan yang benar = token teks terpisah, dan pola yang sama berlaku untuk `--hris`/`--it`/`--qc`/`--marketing` → lintas modul, keputusan tersendiri.
	- ⚠️ Ditemukan beberapa kelas Tailwind **tidak valid** yang warnanya selama ini tak pernah diterapkan: `text-teal-750`, `text-red-650`, `text-gray-450`, `text-rose-450` (shade 450/650/750 tidak ada). Sudah diganti ke shade valid.

- **Kolom "Diinput oleh"** ✅ — log WMS menampilkan `created_by_name` (stempel server dari JWT, lihat [[Microservices - Manufacture Service]]). **Sengaja dibedakan** dari field PIC/`adminPic`/`marketingPic` yang diketik manual di form — keduanya bisa orang berbeda. Data sebelum fitur ini tampil `—`. Penempatan menyesuaikan tabel: kolom sendiri di log Gudang RM/FG & Material Order, **sub-baris di kolom Tanggal** pada Laporan Hasil Produksi (tabelnya sudah 11 kolom — menambah kolom ke-12 mengulang masalah kolom terpotong yang baru diperbaiki), dan di Master Resi **hanya muncul untuk resi manual** (resi hasil sync tak punya penginput; `—` di belasan ribu baris hanya jadi kebisingan, sekaligus jadi penanda asal data).

**Procurement** (`/procurement`, satu halaman `src/app/(main)/procurement/page.tsx` dengan **sembilan tab** via `?tab=`, feature `src/features/procurement/`, gerbang `ProcurementGuard` — role `system_roles["procurement"]` atau anggota IT) — sumber tunggal daftar tab: `lib/tabs.ts` (`PROCUREMENT_TABS`, dipakai halaman tab + enam pengalih halaman lama + sidebar).
- **Tab dikelompokkan visual** ✅ (2026-07-31) — sembilan tab sejajar (Ringkasan, Tagihan Pemasok, Pesanan Pembelian, Penerimaan, Permintaan Barang, Master Pemasok, Barang & Jasa, Kamus Produk, Master Referensi) sulit dipindai mata, meniru masalah yang dihindari mockup HTML acuan modul ini lewat sidebar berkelompok (Ringkasan / Per posisi / Referensi). Halaman **per posisi** dari mockup sengaja **tidak** ditiru — permission set sudah menanganinya; yang diambil hanya pengelompokannya, dipadatkan jadi label kecil (`PEMBELIAN`, `MASTER`) + pemisah vertikal **di atas baris tab yang tetap satu baris** (keputusan pengguna, bukan sidebar terpisah). Ringkasan berdiri sendiri tanpa label kelompok. Teknis: field `kelompok` pada tiap entri `PROCUREMENT_TABS` + helper `kelompokkanTab()` (murni lapisan tampilan — nilai `value` tiap tab, `normalkanTab`, dan seluruh tautan `?tab=` lama **tidak berubah**); dirender sebagai `<span>` (bukan `role="tab"`) supaya label kelompok jelas tak-bisa-diklik; `flex-wrap` pada `TabsList` menjaga layar sempit tidak memaksa scroll mendatar halaman.
- **Master Pemasok** (`/procurement?tab=pemasok`) — tabel + cari + filter kategori/status-sync/akun-utang-kosong; form Tambah/Ubah dengan usul nomor otomatis per kategori (`PBB-061`), field baca-saja **Tipe Pemasok** saat edit. Backend: [[Microservices - Procurement Service]] bagian Master Pemasok.
- **Barang & Jasa** (`/procurement?tab=barang`, ✅ 2026-07-28) — tabel + cari + filter kategori/jenis-barang/status-sync; form Tambah/Ubah dengan usul kode otomatis (`BRG-{angka}`, **tidak per-kategori** — beda dari pemasok), toggle berjenjang **Nomor Seri/Produksi** (aktif → pilih Tipe Nomor Seri UNIQUE/BATCH + toggle Pakai Kadaluarsa) dan **Kena PPN** (aktif → pilih PPN dari katalog), dua field baca-saja saat edit (**Merek Barang**, **Tipe Persediaan**). Backend: [[Microservices - Procurement Service]] bagian Master Barang & Jasa.
- **Pembelian — cermin Accurate baca-saja** ✅ (2026-07-31, tiga tab baru) — tab **Pesanan Pembelian** (`?tab=pesanan`), **Penerimaan** (`?tab=penerimaan`), **Permintaan Barang** (`?tab=permintaan`) MENGGANTIKAN tab `po` lama (form input PO manual — koleksi `purchase_order` 0 dokumen di produksi, kode `POForm.tsx`/`POList.tsx` tetap ada tak dihapus, tinggal dimunculkan lagi bila kelak dipakai). Ketiganya baca-saja tanpa tombol buat/ubah — pesanan/penerimaan/permintaan dicatat finance langsung di Accurate, ERP menariknya otomatis (06:00 & 18:00 WIB) atau lewat tombol Segarkan di tab Ringkasan. Pencarian disaring **di peramban** atas data yang sudah dimuat (bukan request baru per ketikan) — daftar berukuran ribuan baris, sudah di memori.
  - **PesananList**: nomor, pemasok (nama + kode di bawahnya — dari objek `vendor` bersarang Accurate), tanggal, janji kirim, nilai, badge persetujuan, persen terkirim.
  - **PenerimaanList**: nomor, tanggal terima, status (dari `statusName`, field `status` tidak ada di `receive-item/list.do`), nomor pesanan tertaut, **ketepatan kirim**. Periode ketepatan (jendela 6 bulan) diambil dari respons backend (`detail_dari`/`detail_sampai`) dan ditulis eksplisit di layar — **bukan** ditebak/di-hardcode FE, supaya tak ada dua kalimat periode yang bisa menyimpang. Sel ketepatan **tidak pernah** memuat angka ketika janji kirim tak diketahui (bukan "0 hari", bukan "—") — dua kalimat alasan berbeda ("Tidak tertaut pesanan" vs "Menunggu penyegaran berikutnya") tergantung `detail_terambil`.
  - **PermintaanList**: hanya tiga kolom (nomor, tanggal, status) — `requisitionType` tidak ada di `purchase-requisition/list.do` Accurate, kolom tambahan yang selalu kosong akan terbaca sebagai data hilang padahal memang tak disediakan.
  - **Gagal memuat** pada ketiga tab menampilkan pesan galat di baris `colSpan` **di dalam rangka tabel yang tetap utuh** (kotak cari + kontrol paginasi tidak ikut hilang) — idiom disamakan dengan Tagihan Pemasok & Barang/Jasa. Sebelumnya (early return yang membuang seluruh rangka termasuk ketikan pencarian yang sedang berjalan) diperbaiki review 2026-08-01. Keadaan kosong juga dibedakan dari total `pagination.total_items` **server**, bukan panjang array halaman ini — "belum ada data tercermin" vs "tidak ada pada halaman ini" adalah dua kondisi berbeda.
  - Backend: [[Microservices - Procurement Service]] bagian Pembelian · [[API - Procurement Service]] bagian Pembelian.

**Lain** — Secretary / Quality (KPI saja), Beauty_hacks / Kyura (link eksternal Ideamiils + KPI)

**Pola satu card untuk layar daftar** ✅ (2026-08-04, PR erp-frontend [#775](https://github.com/bip-itteam-internal/erp-frontend/pull/775)) — layar daftar sebelumnya menumpuk **dua card terpisah** (banner di atas, tabel di bawah, berjarak), dan banyak di antaranya masih memasang `<h1>` sendiri **di atas** banner sehingga nama layar tertulis dua kali berturut-turut; dua halaman bahkan merender dua banner sekaligus. Kini `Table` (`src/components/table/index.tsx`) menerima prop **`toolbar`**: isinya dirender di dalam card yang sama lalu dipisah `Separator`, dan `CardHeader` dilewati supaya judul tak ganda. `Banner` mendapat mode **`bare`** (dirinya sendiri, tanpa card pembungkus). Diterapkan ke **24 layar HRIS**; Kelola Karyawan jadi acuannya.
	- **`CardContent` bawaannya `p-6 pt-0`** dan selama ini mengandalkan `CardHeader` untuk jarak atas — begitu header dilewati isinya menempel ke garis pemisah, jadi `pt-6` dipasang khusus saat `toolbar` ada.
	- **Filter dirender di LUAR pembungkus ber-padding** supaya menempel penuh ke tepi card tanpa sudut membulat; layout-nya tak lagi dipaksa tiga kolom (mengalir `flex-wrap`, tiap kontrol melebar penuh di layar sempit).
	- **Dropdown baris-per-halaman dan tombol Download pindah ke footer** `Table` (mengikuti pola Task Management), bukan dihapus per halaman — cara itu menjaga ekspor tetap ada di 17 halaman tanpa menyentuh satu per satu.
	- Bug ikutan: **placeholder filter tak pernah muncul** karena nilai kosong dipetakan ke sentinel `__all__` yang dianggap Radix sebagai sebuah pilihan; kini nilainya benar-benar `""`.
	- **Kelola Karyawan** (`/hris/employee`): kolom **Aksi** dihapus (isinya cuma satu tautan ke detail — satu kolom penuh untuk satu tautan menyempitkan kolom yang benar-benar dibaca), barisnya sendiri yang bisa diklik; status pendaftaran memakai **ikon MyBharata** asli (berwarna = terdaftar, `grayscale` = belum) menggantikan ikon generik berlatar biru yang menabrak warna aksi di sekitarnya.
	- **Dua layar sisa ikut menyusul** (✅ live 2026-08-05, PR [#806](https://github.com/bip-itteam-internal/erp-frontend/pull/806)): **Jadwal Kerja** (`/hris/schedule`) dan **Laporan Kehadiran** (`/hris/report`), keduanya sebelumnya tak memakai pola ini sama sekali. Rinciannya di bagian HRIS di atas. Setelah keduanya tinggal **KPI** yang belum ikut (lihat bawah).
	- ⚠️ **Dua jebakan komponen bersama yang ketahuan saat mengerjakannya**, dan keduanya berlaku untuk layar mana pun yang menyusul:
		- **`TableCell` tak punya garis apa pun**, dan `TableRow` cuma memberi garis BAWAH. Untuk tabel biasa itu memadai, tapi tabel bentuk MATRIKS (banyak kolom sempit sejenis) kehilangan seluruh pemisah kolomnya dan selnya jadi tak bisa ditelusuri. Layar seperti itu wajib memasang `border` sendiri di selnya — memakai token `border` bawaan, bukan warna yang dipaksakan.
		- **Kolom yang menempel (`sticky`) wajib punya latar buram sendiri.** Ini gampang terlewat karena warna latar yang dipakai sebelumnya sering merangkap dua peran sekaligus; begitu warna itu diseragamkan, isi baris terlihat menembus kolom yang menempel dan gejalanya hanya muncul saat digulir mendatar.
	- ✅ **Keadaan kosong kini ikut bahasa aktif** (PR [#812](https://github.com/bip-itteam-internal/erp-frontend/pull/812), live 2026-08-06). `Table` dulu memaku `"Data not found"` di dalam komponennya sehingga 40-an layar berbahasa Indonesia menampilkan kalimat Inggris itu apa adanya. Bawaannya sekarang kunci `common.noData` yang **sudah ada di kedua locale sejak lama** — perbaikannya karena itu mengenai seluruh layar sekaligus tanpa menyentuh satu pun di antaranya.
		- Prop **`emptyText`** untuk layar yang punya kalimat lebih menjelaskan. Tiga tabel Jadwal Kerja memakainya untuk mengembalikan kalimat yang sempat hilang saat beralih ke pola ini ("Belum ada shift untuk perusahaan ini" dan dua saudaranya). Kalimat itu menyebut **perusahaan**, dan di halaman yang datanya mengikuti `CompanySwitcher` itu justru pertanyaan pertama saat tabelnya kosong — sesuatu yang "Data tidak ditemukan" tak pernah jawab.
		- Keadaan kosong **tidak** dirender selagi tabel masih memuat; yang tampil baris skeleton. "Tidak ditemukan" di tengah pemuatan adalah kabar buruk yang belum tentu benar.
		- ⚠️ **Pesan galat punya penyakit yang persis sama dan sempat luput**: `"Internal Server Error"` juga dipaku dalam bahasa Inggris di komponen yang sama. Layar yang sebelumnya sudah menampilkan pesan terjemahan sendiri karena itu justru **memundur** ke bahasa Inggris begitu beralih ke komponen bersama — kebalikan dari maksud perpindahannya. Diperbaiki bersama KPI (PR [#813](https://github.com/bip-itteam-internal/erp-frontend/pull/813)): bawaan jadi `common.serverError`, plus prop **`errorText`** yang simetris dengan `emptyText`. Pelajarannya: saat memindahkan sebuah layar ke komponen bersama, **kedua** keadaan itu wajib dicek, bukan hanya yang kosong.
	- ✅ **KPI ikut menyusul, dan dengan itu pola satu-card dipakai SELURUH layar daftar HRIS** (live 2026-08-06, PR [#813](https://github.com/bip-itteam-internal/erp-frontend/pull/813)). KPI adalah yang terakhir tersisa dan bentuknya paling jauh dari pola: **tiga card bertumpuk** — banner sendiri, tab departemen telanjang di antaranya, lalu card departemen yang berisi empat kartu statistik dan tabel anggota. Kini satu card: banner `bare` lewat `toolbar`, tab dan ringkasan di slot `summary`, tabel di bawahnya.
		- **Tab departemen** jadi `CustomTabs` (`underline`, berbadge jumlah anggota) — sama seperti Kandidat. Satu departemen sengaja menghasilkan **nol tab**, bukan satu tab tunggal, karena tak ada yang bisa dipilih di sana; tujuh halaman KPI per-departemen plus Portal Saya selalu berada dalam keadaan itu.
		- **Dua baris tab bertumpuk sengaja dibedakan bentuknya**: pemilih Penilaian/Template memakai `pill` karena berpindah **mode halaman**, sedangkan tab departemen memakai `underline` karena **menyaring isi tabel**. Satu bentuk untuk dua arti berbeda akan terbaca sebagai satu kelompok tab yang sama.
		- **Empat `StatCard` tiruan dibuang.** Markup-nya tiruan persis `StatSummary` bersama sampai ke `p-5` dan `rounded-lg bg-primary/10 p-2 text-primary` — dan seperti biasa tiruan tertinggal: saat `StatSummary` mendapat prop `tone`, tiruannya tidak. Definisi kartunya pindah ke `features/hris/kpi/lib/stats.ts` supaya bisa diuji tanpa merender apa pun. Semua kartu bernada `default`: **ambang KPI belum pernah disepakati**, dan mewarnai kartu tanpa ambang berarti menyatakan penilaian yang tak ada dasarnya.
		- **`HashLoader` terakhir di jalur ini hilang** saat tabel template pindah ke komponen bersama; yang menggantikan adalah baris skeleton bawaannya, sesuai konvensi tim.
		- **Props `KpiPageContent` tidak berubah sama sekali**, sehingga **11 rute pemanggilnya tak disentuh satu pun** (`/hris/kpi`, `/hris/kpi/templates`, `/portal/kpi`, plus KPI milik Finance, IT, GA, Quality, Procurement, Kesekretariatan, Manufaktur). Berkasnya menyusut 702 → 335 baris dan menyisakan orkestrasi saja.

**Internasionalisasi (i18n)** — dukungan dua bahasa **Indonesia (default) / English** via `react-i18next` (`src/i18n/`); switcher di header (samping tema), pilihan disimpan cookie `lang` (dibaca SSR). Rollout **bertahap**; sudah: HRIS Ulang Tahun, KPI, Announcements, Recruitment System Setup, Payroll Pengaturan Gaji (`hris.payroll.*`), Task Management, Pengembangan Organisasi (`hris.community.*` + header grup sidebar). Aturan lengkap: [[ADR - 0010 Internasionalisasi (i18n) Dua Bahasa]]

## Belum Diimplementasikan / Catatan

- **`/integration/inventories` = STUB** (data `MOCK_INVENTORIES` hardcoded; inventory asli ada di module GA)
- **`/integration/shipment-setting`** create modal — `// TODO: wire up mutation` (tabel tampil, create belum di-wire)
- **Accurate transaction detail** — panel "Summary Coming Soon"
- **User dropdown**: item Account, Notifications, Settings *disabled*; belum ada route Settings
- **Payroll Calculator**: route ada tapi dihapus dari menu
- **Firebase/FCM push**: kode di-comment, env Firebase masih placeholder
- **Login**: "Forgot your password?" / "Contact admin" link mati; belum ada flow reset password / registrasi
- **Integration-only allowlist = UI-gating saja**: route `/integration/*` & `/dashboard` tetap bisa diakses via URL langsung (tidak dijaga middleware `proxy.ts`); akun tetap butuh role `integration` di `system_roles` agar API tidak 403. Env `NEXT_PUBLIC_*` di-inline saat build → ubah allowlist butuh rebuild/restart. Belum ada hardening middleware untuk akun ini.
- ⚠️ **`src/proxy.ts` = KODE MATI yang menyamar jadi penjaga route** (temuan 2026-07-30). Berkas itu mengekspor `proxy(request)` lengkap dengan daftar `hrisPublicRoutes` dan `hrisPrivateRoutes` (`/hris/dashboard`, `/hris/employee`, `/hris/report`, `/hris/attendance`, `/hris/fingerprint`, `/hris/schedule`, `/hris/contract`) plus `itPrivateRoutes`, dan membaca cookie `token` + `system_roles` persis seperti middleware autentikasi. **Tapi fungsinya tidak diimpor di mana pun dan tidak ada `middleware.ts` sama sekali di repo**, jadi tak satu pun route itu benar-benar dijaga di frontend. Bahayanya bukan pada route yang terbuka (backend tetap menolak 403), melainkan pada pembaca kode yang wajar mengira penjaga sudah terpasang. Perlu diputuskan: pasang sebagai `middleware.ts` yang benar, atau hapus supaya tidak menyesatkan.
- **Definisi "admin pusat" di FE beda dari BE**: `sidebar.tsx:89` masih memakai `isSupervisorOrAdmin(systemRoles?.it)` untuk memutuskan siapa yang melihat `CompanySwitcher`, sedangkan BE sudah pindah ke `system_roles.group = admin`. Akibatnya pemilih perusahaan bisa muncul untuk user yang override `?company=`-nya justru diabaikan BE, sehingga tiap perbaikan tenant-scope di FE tampak "tidak jalan" bila diuji dengan akun supervisor IT biasa. Titiknya kini tinggal satu: `/hris/schedule` tak lagi memilih perusahaan di level halaman, sebab perusahaan terpilih mengalir lewat cookie `selected_company` yang ditempelkan interceptor axios ke GET ber-scope perusahaan (`lib/axios.ts:120-128`). Detail: [[ADR - 0029 Multi-Tenant Presensi Row-Level company_id]].
- ⚠️ **Mode supervisor halaman KPI MUSTAHIL menyala** (temuan review 2026-08-06, **pra-ada** — bukan bawaan PR [#813](https://github.com/bip-itteam-internal/erp-frontend/pull/813) yang cuma memindahkan tata letak). Di `features/hris/kpi/components/kpi-page-content.tsx` tertulis `isHr = !isLocked` lalu `supervisorOnly = !isLocked && isSupervisor && !isHr`; karena `!isHr` secara definisi sama dengan `isLocked`, ekspresinya adalah `!isLocked && … && isLocked` = **selalu `false`**. Akibatnya seluruh cabangnya mati: detail karyawan tak pernah diambil sehingga `supervisorDept` selalu kosong, empat percabangan yang memakainya tak pernah terpilih, dan dua kalimat i18n (`subtitleSupervisor`, `subtitleSupervisorDept`) tak pernah tampil sekali pun. Sekelas dengan gotcha "fitur merged, deployed, dan tetap mustahil dipakai" di ingatan tim. Perbaikannya **butuh keputusan produk dulu**: siapa yang dimaksud "HR" versus "supervisor", mengingat `system_roles` berisi kode **modul** dan atasan sebenarnya tercatat di `work_data.is_supervisor` (lihat [[HRIS - Organization Structure]]). **TBD.**
- **Kunci i18n `hris.kpi.periodPrefix` kini yatim** di kedua locale — satu-satunya pemakainya (`DepartmentSection`) ikut terhapus saat KPI pindah ke pola satu card. Periodenya sendiri tidak hilang dari layar, tetap terbaca di pemilih periode pada banner. Cukup dibuang saat berkas locale disentuh berikutnya.
- **Edit Data Pekerjaan** (`/hris/employee/<id>`, tab Data Pekerjaan) — dropdown Departemen & Jabatan dulu tidak ter-scope perusahaan sehingga karyawan tenant lain disodori master data BIP; diperbaiki di branch `fix/edit-workdata-company-scope` (**belum merge**) dengan mengalirkan `work_data.company_id` ke modal. Karena perusahaan pilot bisa belum punya `master_department`, nilai tersimpan disisipkan sebagai opsi agar tak terhapus saat disimpan. Perbaikan ini hanya efektif penuh bagi admin grup (lihat butir di atas), dan **butuh BE ter-deploy lebih dulu** karena jalur simpannya ikut diperbaiki ([[CORE - HRIS Orchestrator]]).
- **Layar setelan per-jabatan tidak ikut `CompanySwitcher`** — Tampilan Menu (`/it/settings` + Pengaturan HRGA), Hak per Posisi, Jenjang Jabatan, dan Siapa Boleh Apa membaca serta menulis `master_department` tanpa meneruskan perusahaan terpilih, sehingga admin pusat yang beralih tenant tetap menyunting jabatan perusahaannya sendiri tanpa peringatan apa pun. Diperbaiki di branch `fix/setelan-posisi-per-perusahaan` (**belum merge**) dengan meneruskan `readSelectedCompany()` ke pembacaan dan penulisan, plus dua hook yang belum menerima perusahaan (`use-fetch-department-groups`, `use-fetch-permission-exceptions`). **BE tidak berubah**, sudah benar sejak PR #652. `COMPANY_SCOPED_READS` sengaja tak disentuh karena hook-nya mengirim `?company` eksplisit; menambahkan `/master/departments` ke interceptor akan ikut mengubah picker Form Builder, Pelatihan, Dokumen HRD, dan Department Integration. Detail beserta pembagian perilaku tulis (presensi `CompanyID` versus master data pengelolaan `EffectiveCompanyID`): [[ADR - 0029 Multi-Tenant Presensi Row-Level company_id]].

## Dependencies & Integrasi

- **API Gateway bip-erp** (`NEXT_PUBLIC_BASE_URL`; dev `http://localhost:6969`) — semua request lewat sini → [[CORE - API Master Gateway]]
- Konsumsi module via gateway: [[Microservices - Employee Service]], [[Microservices - Attendance Service]], [[Microservices - Notification Service]], [[Microservices - Insentive Service]], [[Microservices - Integration Service]], [[Microservices - Inventory Service]], serta operasi IT via [[CORE - IT Orchestrator]] & HR via [[CORE - HRIS Orchestrator]]
- SSO target: app Task Manager (`tasks.bharatainternasional.com`) → memicu sesi ke [[Microservices - Task Management Service]]
- CDN dokumen (`NEXT_PUBLIC_DOC_URL`), MinIO read-key per domain (employee/attendance/task/notification)

## Dokumen Terkait

- [[BASE - Enterance Point]]
- [[APP - MyBharata]]
- [[HRIS - Key Performance Index]]
- [[Finance - Incentive]]
- [[Finance - Bridging App]]
- [[GA - Inventory Management]]
- [[ADR - 0010 Internasionalisasi (i18n) Dua Bahasa]]
