> **Status**: ✅ Implemented (21 Juli 2026) — **lima** menu WMS dipecah dua sub-tab; stempel `created_by_name` aktif di semua jalur tulis manual (termasuk jalur proposal). Verifikasi visual tuntas untuk **Inbound RM**, **Kirim FG**, & **Master Resi**; Keluar FG / Input Gudang FG / Laporan Hasil Produksi menunggu pengecekan dengan role yang sesuai (`admin gudang FG`, `admin produksi`). **Follow-up 22 Juli 2026:** (a) batasan "PO multi-bahan tak mengisi form penuh" diselesaikan — form Inbound RM jadi multi-baris (klik baris PO memuat seluruh bahan); (b) **backend gerbang gudang** (retur barang-balik ditahan sampai gudang scan; Decision #8) — cakupan ADR ini diperluas dari log-split FE ke fondasi backend yang membuatnya bermakna; **terbukti end-to-end di prod**; (c) **form retur dilengkapi** (pengaman qty/status, panel konteks, scan dari daftar, retur basi terlipat; Decision #9); (d) **resi paket BALIK** diutamakan di form + scan (sumber Shopee `get_return_detail`), plus **search/filter/export laporan + auto-isi PIC** (Decision #10). **Follow-up 30 Juli 2026 (✅ deployed):** gerbang gudang **PER-ORDER** (fix under-book — konfirmasi 1 member membuka gerbang seluruh grup: 189 grup/1.437 order ~Rp142jt) + status **SEBAGIAN** turunan — lihat amandemen Decision #8. Lihat Consequences.

## Context

Log tiap menu WMS mencampur dua hal yang asalnya berbeda: **data dari sumber** (retur marketplace, resi, PO ke supplier, Surat Jalan produksi) dan **data hasil input gudang**. Di tab *Return Dari Ekspedisi* keduanya tampil berbaur di satu tabel; di menu lain sumbernya tak terlihat sama sekali. Akibatnya Finance sulit menjawab pertanyaan paling dasar: **apa yang seharusnya masuk/keluar, dan mana yang belum dicatat gudang.**

Masalah kedua: log tidak menunjukkan **siapa** yang menginput. `metadata.created_by` hanya menyimpan employee ID, sedangkan field seperti `admin_pic`, `marketing_pic`, `procurement_pic`, dan "kepala produksi" adalah **field bisnis yang diketik operator di form** — bisa berisi orang lain sama sekali dan tidak bisa dipakai sebagai jejak akuntabilitas.

Kondisi teknis yang membatasi pilihan (diverifikasi ke kode, bukan asumsi):

- Header `BIP-Username` **sudah** di-inject [[CORE - API Master Gateway]] dari JWT untuk setiap request (`gateway_request.go` menghapus header kiriman client lalu menulis ulang dari claims), tapi di manufacture-service hanya dibaca `audit.go`.
- `manufacture_audit_log` menyimpan username, tapi field `Target`-nya hanya kode/ID (mis. `tx.KodeBahan`), **bukan `_id` dokumen** — join balik dari audit ke baris log tertentu tidak andal.
- **Hanya satu feed sumber yang punya kunci join**: `GET /returns` menurunkan `record_status` dari `detail.returnKey` = `dedupe_key`. Transaksi Keluar FG **tidak** menyimpan nomor resi yang dipakai (hanya keterangan "Dari N resi master"), dan transaksi penerimaan RM **tidak** menyimpan nomor PO.

## Decision

**1. Log tiap menu dipecah dua sub-tab: SUMBER dan INPUT WMS.**

| Menu | Tab sumber | Tab input WMS |
|---|---|---|
| Return Dari Ekspedisi | feed retur marketplace (`GET /returns`) | transaksi INBOUND ber-`namaMarket` |
| Keluar FG | resi marketplace **diagregasi per SKU** (lihat §3) | transaksi OUTBOUND ber-`marketplaceSalesDetection` |
| Inbound RM Harian | Procurement PO | transaksi penerimaan ber-`picQc` |
| Input Gudang FG | Surat Jalan status `IN TRANSIT` | transaksi INBOUND ber-`gudangSimpan` |
| **Kirim FG ke Gudang Jadi** | Laporan Hasil Produksi belum dibuatkan SJ | transaksi OUTBOUND ber-`driver` (SJ) |

Sumber Inbound RM adalah **Procurement PO**, bukan Accurate — keputusan user 21 Juli 2026 setelah dipastikan tidak ada feed penerimaan barang dari Accurate (sync-nya hanya master & angka stok, bukan transaksi penerimaan). Jadi yang dibandingkan adalah *apa yang dipesan* vs *apa yang benar-benar datang & lolos QC*.

**2. Badge status "sudah/belum dicatat" HANYA di tab yang punya kunci join.**

Return Ekspedisi mempertahankan badge tiga-nilai. Keluar FG dan Inbound RM **sengaja tanpa badge status** — tanpa kunci join, status apa pun di sana adalah tebakan, dan tebakan salah tentang "barang ini sudah keluar/diterima" lebih berbahaya bagi Finance daripada tidak ada badge sama sekali.

Dua menu tidak butuh badge karena daftarnya **sudah** berupa tunggakan menurut definisi: Input Gudang FG (`IN TRANSIT` = belum diterima) dan Kirim FG (laporan produksi yang belum tertaut `refLaporanProduksi`). Kirim FG bahkan punya kunci join yang paling bersih — laporan hilang sendiri dari tab sumber begitu dipakai membuat SJ.

**3. Tab sumber Keluar FG diagregasi PER SKU, bukan daftar per-resi** (21 Juli 2026, setelah user mempertanyakan bentuk awalnya).

Alasannya bukan sekadar jumlah baris. Tab WMS mencatat **per SKU** (diagregasi lintas banyak resi), sedangkan daftar per-AWB adalah dokumen pengiriman — dua tab berbicara dalam granularitas berbeda sehingga **tak bisa dibandingkan berdampingan**, padahal itu justru tujuan pemisahan tab. Terukur di dev: 931 resi → 19 SKU untuk satu tanggal RTS.

Bundle **dipecah ke komponen** agar sebanding dengan tab WMS (transaksi menyimpan SKU komponen). Tanpa ini baris dengan qty TERBESAR justru tak pernah punya pasangan — di dev, bundle `PJG-002 + PJG-004` menyumbang 641 dari ~900 qty. Kode bundle asal tetap ditampilkan sebagai keterangan agar bisa ditelusuri balik ke listing marketplace.

**4. Hanya resi berstatus `COMPLETED` + `SHIPPED` yang terhitung keluar gudang.**

Kanonikalisasi status marketplace (6 kategori, memetakan ~24 nilai mentah TikTok/Shopee) diekstrak dari `ResiMasterView` ke `features/manufacture/resi-status.ts` supaya semua layar memakai satu definisi.

Bukan `COMPLETED` saja: pada tanggal RTS yang baru ~47% resi masih `SHIPPED` (= `IN_TRANSIT`, sudah dibawa kurir) padahal barangnya jelas sudah pergi — memfilter ke Completed membuat pencatatan **kurang**, bukan lebih akurat.

`TO_SHIP` (`AWAITING_COLLECTION`, masih menunggu kurir) dikecualikan karena asimetri jalur koreksi: kalau batal **setelah** dibawa kurir, barangnya kembali fisik dan tercatat di menu Return — stok tetap benar. Kalau batal **sebelum** diambil, tak ada barang yang kembali sehingga tak akan pernah tercatat di mana pun, dan stok yang terlanjur dipotong **tak punya jalur perbaikan**. `IN_TRANSIT` adalah titik pertama yang aman.

Status **tak dikenal tetap dihitung dan dilaporkan**, bukan disaring diam-diam — varian Shopee belum pernah terlihat di dev (semua resi TikTok), dan pengiriman yang lenyap tanpa jejak lebih berbahaya daripada baris yang minta ditinjau. Prinsip sama dengan feed retur.

Aturan ini ditegakkan di **semua** pintu masuk form, bukan hanya tab sumber: klik baris, **Ambil Rentang**, dan **scan resi**. Di Ambil Rentang filter dipasang saat *memasukkan*, bukan saat *menomori* — daftar `f4RangeList` sengaja dibiarkan utuh supaya nomor urut 1..N tetap cocok dengan penomoran Master Resi (kontrak urutan `_id`, lihat `resi.go`). Scan ditolak dengan **alasan + status resinya**, tidak dilewati diam-diam, karena operator sedang memegang paketnya.

**5. Baris sumber bisa diklik → membuka form terisi**, mengikuti pola `openReturnForm` yang sudah ada. Untuk Input Gudang FG & Kirim FG, logika pengisian diekstrak jadi fungsi bersama (`pilihSjUntukTerima` / `pilihLaporanProduksi`) supaya dropdown di dalam form dan klik baris memakai satu jalur. Di Keluar FG baris mewakili satu SKU lintas banyak resi, jadi kliknya memuat **seluruh resi tanggal RTS itu** (setara "Ambil Rentang" sekali klik) — memuat "resi baris ini" tak punya arti di tingkat agregat.

**6. Nama penginput distempel SERVER, bukan dikirim client.** Field `created_by_name` diisi handler dari `c.Get(common.Header.Username)` dan **selalu menimpa** nilai yang datang di body. Alternatif "frontend kirim username dari cookie" ditolak: nilainya dikirim browser sehingga bisa dipalsukan, dan jejak yang bisa dipalsukan bukan jejak audit.

Distempel di: `transaksi` (termasuk `transaksi_fg` & tiga titik di `production`), `production_log`, `resi`, `material_order`, `marketing_po`, `procurement_po`, dan `proposal`.

Jalur **proposal** ditambahkan belakangan (21 Juli 2026): transaksi yang lahir dari approval deviasi BOM tidak ikut distempel di putaran pertama, sehingga kolom "Diinput oleh" di Keluar Produksi akan menampilkan `—` **selamanya, termasuk data baru**. `Proposal` kini menyimpan `created_by_name` sendiri dan mewariskannya ke transaksi saat `APPLIED` — yang ditampilkan adalah nama **pengaju**, bukan approver, karena kolomnya berjudul "Diinput oleh" dan yang mengisi form adalah operator (approver sudah terekam di `ppic_approval`/`spv_approval` + audit log).

**7. Resi hasil sync marketplace TIDAK distempel.** `upsertResiFeed` sengaja dibiarkan kosong — datanya datang dari marketplace, bukan diketik orang. Mengisi nama pemicu sync akan membuat kolom "Diinput oleh" berbohong tentang ribuan baris. Konsekuensi yang diambil sebagai keuntungan: ada/tidaknya nama sekaligus menjadi penanda asal data (manual vs sync) di [[Microservices - Manufacture Service]] tab Master Resi.

**8. Backend: retur BARANG-BALIK ditahan sampai gudang mengonfirmasi** (fondasi yang membuat "belum dicatat gudang" bermakna; ✅ live, **terbukti end-to-end prod 22 Juli 2026**). Retur `solution=0` yang lolos gerbang payout ([[ADR - 0024 Retur Gerbang Payout + Tanggal per-Solution]]) **tidak** langsung dibukukan ke Accurate — `holdForWarehouse` menahannya status **PENDING** sampai form ini tersimpan, lalu manufacture memanggil `POST /accurate/daily-returns/warehouse-confirm`. **Kondisi fisik (Reuse/Rework/Reject) menentukan pembukuan** — membalik [[ADR - 0022 Retur via Sales Return per Mode + Keep Invoice Line]] ("tak perlu tunggu barang fisik"): membukukan saat marketplace menyetujui berarti menebak qty yang belum dilihat siapa pun. Ketiga kondisi **sama-sama menambah stok** (label informasi; barang rusak disesuaikan finance manual — keputusan 21 Juli). Detail mekanisme (akumulasi per `(order_id, SKU)`, lookup by-member, re-key ke tanggal gudang) + audit `cmd/returnrecon` ada di [[Microservices - Integration Service]] §Auto-Sync Retur. **Amandemen 2026-08-05 (✅ deployed 2026-08-05, commit `779b0a06`):** cadangan pencarian **by-key** di `ConfirmReturnFromWarehouse` kini mengabaikan baris `SKIPPED`, menyamakannya dengan lookup utama. Tanpa itu, scan gudang bisa **menghidupkan** retur yang sengaja tak dibukukan — entah karena refundnya sudah diserap Auto Sync Income ([[ADR - 0024 Retur Gerbang Payout + Tanggal per-Solution]]) atau karena era manual pra-cutover — dan `rebuildAndSendGroup` tak punya gerbang payout sendiri untuk menahannya. Terbukti tes regresi: 1 dokumen Retur Penjualan terbukukan dobel sebelum fix.

> **Amandemen — gerbang PER-ORDER, bukan per-grup** (✅ **deployed 2026-07-30**). `holdForWarehouse` semula membuka gerbang **seluruh grup** begitu `WarehouseConfirmedAt` terisi — cukup **SATU** paket member discan → dokumen dibukukan hanya baris member itu, member lain jadi anggota grup **SENT TANPA baris** (**under-book**; prod `RTR/2026/07/29/017-BH`: 3 order → 1 baris. Audit: **189/192 grup SENT, 1.437 order ~Rp142jt**). Bukan "SENT hantu" — dokumen ADA di Accurate, cuma kurang baris. **Fix:** gerbang menahan tiap member sampai barang **ORDER-nya sendiri** discan (`orderHasWarehouseItem` — cek `warehouse_items` ber-`order_id`), aktif hanya bila grup sudah punya rincian scan; guard cegah grup SENT turun ke PENDING saat menahan member baru (pelajaran retur-hilang 21 Juli). Grup yang **sebagian** discan ditandai **"SEBAGIAN"** — field **turunan** `WarehousePending` (member barang-balik belum discan; **refund-only `solution=1` dikecualikan** — tak perlu scan, dibukukan langsung), dihitung saat baca (list/detail/export), **tak dipersist**. Backlog tampil SEBAGIAN **otomatis tanpa backfill**; dokumen **self-heal** saat paket sisa discan (rebuild menambah baris). Presisi solution per-member hanya di export+detail (order termuat); badge list konservatif.

> **Amandemen — `WarehousePending` juga menandai grup yang BELUM discan sama sekali** (✅ 2026-08-10). Amandemen di atas mendefinisikannya sempit: penanda grup yang **sebagian** discan. Kedua fungsi penghitung karena itu berhenti lebih awal saat `warehouse_items` kosong, dengan asumsi "kosong = jalur klaim-marketplace = lengkap" — dan asumsi itu menelan kasus ketiga: grup **PENDING** yang ditahan gerbang menunggu barang datang. Justru itulah bentuk **mayoritas** backlog. Terukur prod 2026-08-06: **908 baris PENDING, 900 ditahan gerbang, NOL sebagian discan** — jadi badge SEBAGIAN **tak pernah menyala sekali pun** sejak fitur ini ada, karena keadaan yang dideteksinya praktis tak pernah terjadi (begitu cukup discan, grup langsung SENT). Lebih buruk dari diam: modal detail menurunkan kolom "Scan Gudang" dari himpunan yang selalu kosong itu, sehingga menandai **setiap** order **"✅ Sudah discan"** untuk grup yang belum satu pun discan — pernyataan salah di layar yang justru dipakai memutuskan tindakan. **Fix:** `menungguScanGudang(row)` = `last_status == PENDING` **DAN** `holdForWarehouse(row, nil)`. Syarat status **wajib**, bukan gerbang saja: nilai nol `Solution` kebetulan **0** (= barang balik), jadi grup jalur klaim yang tak pernah menyimpan solution akan lolos cek gerbang dan salah ditandai menunggu. Pencocokan teks `last_error` **ditolak** meski 900 dari 908 baris memuat kalimat penahanan — kalimatnya bisa berubah dan penandanya mati diam-diam (pola "wired tapi inert" yang sudah beberapa kali menggigit jalur retur ini). **Cadangan `row.Members`:** `warehousePendingFromOrders` (export+detail) kini menerima `row`, karena `loadMemberOrders` **melewati order tanpa retur terpakai** — untuk grup yang data returnya hilang daftar order termuat KOSONG sehingga versi presisi mengembalikan nihil, dan justru grup paling tak lengkap yang tampil "sudah discan" (terukur: **15 grup Shopee**). Sikapnya konservatif seperti jalur list: member yang tak bisa dinilai solution-nya dianggap menunggu — salah-tandai "belum discan" cuma menambah pemeriksaan, salah-tandai "sudah discan" menyembunyikan barang yang tak pernah datang. Grounded: `menungguScanGudang`/`memberOrderIDs`/`warehousePendingMembers`/`warehousePendingFromOrders` (`accurate_rts_usecase.go`), tes `accurate_return_warehouse_gate_test.go`. ⚠️ **Isi kolom "Scan Gudang" di export laporan retur ikut berubah** (dari "sudah discan" jadi "belum discan" untuk grup bergerbang) — itu koreksi, tapi export lama yang sudah tersimpan akan berbeda dari yang baru.

> **Amandemen — jalan pintas gerbang untuk PERKAKAS OPS** (✅ 2026-08-20, bip-erp PR #1304). Gerbang gudang berlaku juga di jalur **Retry** (keputusan manual tak bisa menggantikan fakta fisik) — alasan itu **tetap berlaku untuk manusia**. Tapi ia salah alamat untuk perkakas yang membukukan **ULANG** baris yang SUDAH pernah terbukukan: saat `cmd/returnsplitkind` memecah dokumen campur lama, dokumen Accurate-nya dihapus dulu, lalu bagian yang barisnya dibangun dari scan **tertahan** gerbang — dan nilainya lenyap. Terukur `RTR/2026/08/18/114-BH` **Rp3.346.500** (tertangkap guard nilai perkakas, dihentikan sebelum menyebar, lalu diremediasi utuh). **Keputusan**: `RetryDailyReturn` kini menghormati `WithSkipWarehouseGate(ctx)` — flag yang sudah dikenal `SyncOrderReturn` sejak `returnfix`. Yang berhak menyetelnya **hanya perkakas ops**; tombol Retry di FE dan sweep malam tak pernah menyetelnya, jadi cakupan gerbang untuk jalur normal **tidak berubah**. Saat memecah, gerbang dinilai **per bagian**: ber-item-scan tapi cakupan member tak penuh → gate-skip (merebuild persis baris lamanya, member tanpa item tetap tak menyumbang baris); tanpa item → **PENDING** (benar — memang belum pernah terbukukan); **refund-only** tanpa item → gate-skip (baris `NOT_RETURNED`, nol dampak stok). Grounded: `RetryDailyReturn`/`skipWarehouseGate` (`accurate_rts_usecase.go`), `cmd/returnsplitkind`.

> **Amandemen — jalur kirim-ulang konfirmasi + penanda "belum sampai Accurate"** (✅ 2026-08-12). Decision #8 mengirim konfirmasi gudang **fire-and-forget**: pencatatan fisik tak boleh dibatalkan hanya karena jaringan bermasalah. Alasannya tetap benar, tapi konsekuensinya baru terukur sekarang — kegagalan kirim hanya meninggalkan satu baris log, dan returnya **macet selamanya**: sweep malam melewatinya (mengira masih menunggu barang, benar menurut datanya) dan tombol Retry menolaknya. Satu-satunya jalan pulih adalah **menyimpan ulang form**, yang membuat transaksi baru dan **menambah stok WMS dua kali** — pemulihan yang merusak. **Terukur prod 2026-08-11**, dua populasi macet dengan sebab berbeda: **20** retur yang scan-nya tercatat **MANUAL** (resi tak tercocokkan → `returnKey` kosong → konfirmasi tak pernah dikirim) dan **32** yang sudah tertaut tapi POST-nya tak sampai. Keduanya tak bisa ditemukan dari layar mana pun; menemukannya butuh sensus manual lintas-database. **Dua perbaikan:** (1) **penanda diturunkan SAAT BACA**, bukan stempel — feed integration membawa `scanned_gudang` per-order (dari `orderHasWarehouseItem`), manufacture menurunkan `konfirmasi_tertunda` (`GoodsReturning` && sudah dicatat gudang && !`scanned_gudang` && booking bukan `booked`/`pre_cutover`), dan FE menampilkannya sebagai badge merah **"BELUM SAMPAI ACCURATE"** yang selama ini tersamar sebagai "SUDAH DICATAT" hijau. Stempel yang ditulis ke depan **ditolak** justru karena ia hanya bisa bicara tentang kejadian sesudah dipasang — 5.868 baris lama akan tampak mencurigakan tanpa dasar; diturunkan dari kebenaran di seberang, seluruh data lama langsung berlabel benar **tanpa backfill**. (2) **`POST /returns/konfirmasi-ulang`** (manufacture, **supervisor-only**, **dry-run default**, lingkup wajib disebut + `limit`): menautkan dan/atau mengirim ulang **di atas transaksi yang SUDAH ada** — tak pernah membuat transaksi, tak pernah menyentuh stok. Putusannya (`putuskanKonfirmasi`) memakai `accurate_booking` yang sudah ada, **bukan aturan baru**: `booked`/`pre_cutover` → tautkan saja, JANGAN kirim (cermin mode catatan-saja; mengirim akan me-rebuild dokumen Accurate yang sudah ada, atau gagal untuk pra-cutover). ⚠️ Ini **jalur kelima** yang memicu pembukuan retur — setelah ingest marketplace, webhook, sweep malam, dan konfirmasi gudang otomatis — dan satu-satunya yang dipicu manusia. Grounded: `retur_konfirmasi_ulang.go`, `konfirmasiTertunda` (`returns.go`), `ReturnBookingState.Scanned` (`accurate_rts_usecase.go`), tes `retur_konfirmasi_ulang_test.go` + `returns_test.go`.
> **Amandemen — `booked` bukan "beres", ia status GRUP (fix under-book baru, 🟡 kode selesai & teruji — BELUM deploy, 2026-08-12).** Amandemen di atas menyatakan sebagai aturan: `booked`/`pre_cutover` → tautkan saja, JANGAN kirim. Separuhnya keliru. `booked` berarti **grup** sudah SENT di Accurate — bukan berarti **order ini** sudah punya barisnya sendiri di dokumen itu. Satu member grup discan & terkirim cukup membuat seluruh grup `booked`; member LAIN yang belum pernah discan ikut terbaca "beres" walau barisnya sendiri tak pernah ada. Akarnya bukan di alat pemulihan, melainkan di **titik simpan form itu sendiri**: `takPerluApproval` (erp-frontend, `retur-filter.ts`) memakai premis yang sama untuk memutuskan `catatanSaja` saat form disimpan — jadi gudang mencatat kondisi barangnya (stok WMS bertambah, form tampil "beres"), tapi konfirmasi **tak pernah dicoba dikirim** ke integration sama sekali. Beda dari dua populasi 2026-08-11 di atas (kegagalan jaringan/tautan **setelah** dicoba kirim), populasi ini tak pernah masuk jalur kirim. **Terukur (sensus lintas-database, 2026-08-12): 1.451 order / 2.519 unit di 22 SKU** tanpa baris di `warehouse_items` walau grupnya SENT. Nilai rupiah **belum pasti** — harga jual dasar tak tersimpan lokal (`accurate_products`/`manufacture_master_product` tanpa field harga; `item_price_history` hanya mencakup 5 dari 22 SKU) — taksiran kasar pakai harga komponen acuan [[ADR - 0040 Retur Paket Utuh via Baris Induk Faktur]] (~Rp59.000/botol) memberi **~Rp150 juta**, ordo besar yang sebanding dengan insiden under-book 30 Juli (~Rp142jt) di atas.
>
> **Fix (tiga titik, satu premis yang sama diperbaiki di masing-masing):**
> 1. **`takPerluApproval`** (`retur-filter.ts`) — `booked` kini butuh `scanned_gudang === true` (per-order, field yang sama yang sudah dikirim backend sejak amandemen di atas, sebelumnya tak dipakai di titik keputusan ini). `pre_cutover` tetap tanpa syarat — grup itu memang tak pernah ada di Accurate.
> 2. **`konfirmasiTertunda`** (`returns.go`) — buang pengecualian `booked`; hanya `pre_cutover` yang tetap dikecualikan.
> 3. **`putuskanKonfirmasi`** (`retur_konfirmasi_ulang.go`) — pisahkan `booked` dari `pre_cutover`. `booked` + `!Scanned` kini **kirim seperti biasa** (aman: menambah baris ke grup yang sudah ada via jalur delete+rebuild yang sudah teruji sejak 22 Juli, bukan membuat grup baru — append TIDAK perlu disentuh, lihat catatan di bawah). Short-circuit lama yang mempercayai stempel `detail.catatanSaja` **dihapus** — stempel itu ditulis form berdasarkan premis yang sama yang keliru, mempercayainya membuat scope pemulihan jadi no-op untuk populasi yang justru ingin dijaring. Endpoint `POST /returns/konfirmasi-ulang` menambah scope eksplisit `catatanSaja=1` (populasi ini sudah ditandai sistem sendiri, bukan sapuan tanpa batas) + paginasi `skip`.
>
> ⚠️ **Ditemukan saat /review, ikut diperbaiki**: badge daftar retur (`GudangBarangJadiView.tsx`) memakai `accurate_booking` mentah untuk memilih badge "SUDAH TERBUKUKAN" — bukan lewat `takPerluApproval`. Akibatnya SEMUA order `booked` (termasuk yang under-book) tetap tampil "SUDAH TERBUKUKAN", dan cabang `konfirmasi_tertunda` (badge "BELUM SAMPAI ACCURATE" dari amandemen di atas) **tak pernah tercapai** untuk grup `booked` — persis dead code untuk populasi yang paling perlu ditampakkan. Digerbangi lewat variabel `catatanSaja` yang sudah benar.
>
> **Jalur append TIDAK diperluas** (koreksi rencana awal): sempat dipertimbangkan melonggarkan syarat `len(groupRow.WarehouseItems) == 0` di `rebuildAndSendGroup` (`accurate_rts_usecase.go`, integration) supaya `returnAppendDelta` ikut menilai kasus ber-`warehouse_items`. Ternyata tak perlu — begitu `WarehouseItems` terisi (kapan pun), `detail` sudah diganti total oleh `buildWarehouseDetailItems` (baris di atas guard append), dan delete+rebuild dari situ **sudah** jalur yang benar & teruji (mekanisme self-heal Decision #8/amandemen SEBAGIAN di atas, sudah menangani ratusan grup sejak 22 Juli). **Nol perubahan di `integration`** untuk fix ini.
>
> **Amandemen — `aksiKirimSaja` kirim pakai tautan BASI (bug baru, ✅ 2026-08-14).** Order yang sudah pernah `tertaut` (`putuskanKonfirmasi` → `aksiKirimSaja`) dikirim ulang pakai `detail.returnKey` **lama** yang tersimpan di gudang — bukan `DedupeKey` **segar** yang baru saja dibuktikan benar oleh `putuskanKonfirmasi` sendiri lewat lookup ke integration. Kalau grup retur di integration sudah berganti kunci sejak tautan pertama dibuat (mis. rebuild), kirim gagal `"grup retur ... tak ditemukan"` — dan kegagalan itu **tak pernah memperbaiki tautannya**, jadi percobaan berikutnya gagal **persis sama, selamanya** (bukan transien, tak sembuh sendiri lewat retry). **Ditemukan** saat memulihkan 9 order macet dari amandemen "jalur kirim-ulang" di atas: 5 pulih bersih, 4 (`260726NG1T2BXB` dkk) gagal 2× berturut dengan pesan identik. **Fix:** `jalankanKonfirmasiUlang` menyegarkan tautan (`perluTautkan`) untuk **SEMUA** aksi yang mengirim — termasuk `aksiKirimSaja`, bukan cuma `aksiTautkanSaja`/`aksiTautkanDanKirim` seperti sebelumnya. Aman & idempoten: `r` yang dipakai selalu hasil lookup segar di panggilan yang sama, jadi order yang tautannya memang masih benar cuma menulis ulang nilai yang sama. Grounded: `perluTautkan`/`perluKirim` (`retur_konfirmasi_ulang.go`), tes `TestPerluTautkan`/`TestPerluKirim`.
>
> **Amandemen — fallback Master Resi tak pernah cocok, salah field (bug baru, ✅ 2026-08-14).** Saat `detail.returnOrderId` kosong (populasi **MANUAL** — badge `!tx.returnKey` di `GudangBarangJadiView.tsx`), `KonfirmasiUlangRetur` mencoba cari nomor pesanan lewat Master Resi memakai filter `bson.M{"_id": noResi}` — padahal `_id` koleksi `Resi` adalah ObjectID Mongo-nya sendiri, sedangkan `noResi` (nomor resi manusia, mis. `JY1273931539`) tersimpan di field terpisah `no_resi`. `FindOne` karena itu **selalu** gagal cocok — cabang fallback ini efektif tak pernah bekerja sejak dibuat, bukan cuma untuk sebagian kasus. **Diverifikasi live** (`GET /resi/lookup/:resi`): **9 dari 11** resi yang tadinya dilaporkan `"resi tak membawa nomor pesanan"` TERNYATA ADA di Master Resi dengan `nomor_pesanan` valid — datanya lengkap, alat pemulihannya yang buta. Inilah salah satu penyebab utama populasi MANUAL menumpuk terus meski `POST /returns/konfirmasi-ulang` sudah ada. **Fix:** filter dibetulkan jadi `bson.M{"no_resi": noResi}`. Grounded: `retur_konfirmasi_ulang.go` (fallback lookup di `KonfirmasiUlangRetur`).
>
> **Amandemen — badge "BELUM SAMPAI ACCURATE" dipecah: macet teknis vs menunggu Seller Center (✅ 2026-08-14).** Diselidiki 4 dari 9 order (amandemen tautan-basi di atas) yang TETAP gagal `apply=1` dengan pesan identik dua kali berturut walau fix tautan-basi sudah deploy. **Ground truth Shopee live** (`returns.get_return_detail`): keempatnya masih `status: PROCESSING` — belum pernah `ACCEPTED` — sedangkan `reverse_logistics_status` SUDAH `LOGISTICS_DELIVERY_DONE` (parsel fisik SUDAH sampai di gudang seller) dan `due_date` (batas waktu seller bertindak) **sudah lewat untuk semuanya**. Bukan bug kode — `resolveReturnMode` (integration) memang hanya membukukan saat `ACCEPTED`, jadi belum ada dokumen apa pun untuk order ini; mengirim ulang percuma. Sebelumnya badge ini **menyamakan** keadaan itu dengan retur yang benar-benar macet teknis (konfirmasi gagal terkirim) — dua penyebab yang menuntut obat berlainan tampil identik. **Fix:** `konfirmasiTertunda` kini WAJIB `Status == ACCEPTED`; fungsi baru `menungguApprovalSeller` (badge amber terpisah **"MENUNGGU APPROVAL SELLER CENTER"**) menyala saat gudang sudah mencatat fisik tapi marketplace belum memutuskan (`Status` bukan `ACCEPTED` maupun `CANCELLED` — buyer batal berarti tak ada yang perlu di-approve). Obatnya manusia membuka Shopee/TikTok Seller Center, bukan retry teknis. Grounded: `konfirmasiTertunda`/`menungguApprovalSeller` (`returns.go`), tes `TestKonfirmasiTertunda`/`TestMenungguApprovalSeller`; badge di `GudangBarangJadiView.tsx`.
>
> **Amandemen — tab "Perlu Approval Seller Center" di Auto-Sync-Retur (integration + erp-frontend, ✅ 2026-08-14, paginasi dibetulkan 2026-08-15 — lihat amandemen di bawah).** Populasi yang ditandai `menungguApprovalSeller` di atas TAK PERNAH muncul di tabel Auto-Sync-Retur (halaman `integration-accurate/auto-sync-return`) — tabel itu bersumber `accurate_daily_returns`, dan `resolveReturnMode` memang belum membuat dokumen apa pun untuk retur yang belum `ACCEPTED`. Diminta user sebagai kelanjutan alami setelah badge di atas: kalau WMS sudah bisa menandai baris ini, Auto-Sync-Retur (tempat finance/ops memantau retur) juga perlu bisa melihatnya, dari sumber yang BENAR (feed order-level, bukan `accurate_daily_returns`). **Backend** (`GET /transactions/returns`, integration): query param baru `perlu_approval=1`. **FE**: tab baru di halaman Auto-Sync-Retur (`PendingApprovalTable`, `useFetchPendingApprovalReturns`) — kolom **Logistik Balik** disorot merah saat `LOGISTICS_DELIVERY_DONE` (sinyal paling kuat: barang sudah fisik sampai tapi belum diputuskan). Grounded: `pending-approval-table.tsx`, `use-fetch-pending-approval-returns.ts`.
>
> **Amandemen — `perlu_approval=1` dipindah ke query Mongo, bukan saring pasca-fetch (✅ 2026-08-15).** Versi awal amandemen di atas menyaring `rows` SETELAH `buildReturnRows` (fungsi Go `menungguApprovalMarketplace`/`filterMenungguApprovalMarketplace`, kini DIHAPUS) — beralasan `Status` bisa hasil sintesis `EffectiveReturn`, bukan field mentah. **Ditemukan user bertanya langsung**: "paginasinya di BE atau FE?" — jawabannya cacat: Mongo memaginasi ORDER dulu (mis. 20/halaman terurut tanggal), baru DISARING di Go — jadi "halaman 1" bisa berisi jauh lebih sedikit baris daripada `limit` walau total yang cocok di seluruh koleksi jauh lebih banyak (meta di-tambal `TotalNumber = len(rows)` supaya tak berbohong, tapi tetap bukan paginasi yang benar). **Ternyata aman disaring di raw field**: satu-satunya sumber `EffectiveReturn` menyimpang dari `return.status` mentah adalah sintesis cancel-pasca-kirim (`synthesizeCancelReturn`), dan itu SELALU menghasilkan `Status: "ACCEPTED"` — baris begitu tak pernah lolos filter `perlu_approval` baik dihitung dari status mentah maupun status sintesis, jadi keduanya menghasilkan himpunan YANG SAMA PERSIS. **Fix**: `TransactionOrderFilter.PendingMarketplaceApproval` (bukan digabung `HasReturn`/`IncludeCancelledReturn`) → Mongo `return.solution=0` + `return.status NOT IN [ACCEPTED, CANCELLED]` langsung di query, paginasi & `meta.TotalNumber` kini benar-benar dari Mongo. Grounded: `TransactionOrderFilter.PendingMarketplaceApproval` (`transaction_repository.go`), `buildBSONFilter` (`transaction_repo.go`), tes `TestBuildBSONFilterPendingMarketplaceApproval`.
>
> **Amandemen — filter kolom "Sumber" di tab INPUT WMS (erp-frontend, ✅ 2026-08-15).** Kolom **Sumber** tabel #1 (Decision #1) hanya menampilkan badge `TERTAUT MARKETPLACE`/`MANUAL` (`tx.returnKey` terisi/kosong) tanpa cara menyaringnya — mencari populasi MANUAL (yang jadi fokus dua amandemen di atas) berarti menggulung ribuan baris manual. Dropdown baru "Semua Sumber / Tertaut Marketplace / Manual" di sisi kotak cari tab INPUT WMS. Fungsi murni `wmsSumberFilterMatch` (`retur-filter.ts`) — `'' → semua lolos`, `'TERTAUT'`/`'MANUAL'` menyaring atas `!!returnKey`. Pesan kosong tabel dibedakan: hasil filter kosong ("Tak ada baris ber-status ...") vs benar-benar tak ada data. Grounded: `wmsSumberFilterMatch` (`retur-filter.ts`), tes di `retur-filter.test.ts`; state `wmsSumberFilter` (`GudangBarangJadiView.tsx`).
>
> **Amandemen — bug tersembunyi ditemukan: tautan `jalankanKonfirmasiUlang` nyaris TAK PERNAH tersimpan (✅ 2026-08-15).** Deploy fix Master Resi (amandemen `_id` vs `no_resi` di atas) lalu `apply=1` dijalankan atas backlog 30 hari (7.872 baris) — rekap melaporkan 194 `tautkan-dan-kirim` + 50 `tautkan-saja` "berhasil". User bertanya "ko masih banyak MANUAL" setelah itu. **Diselidiki**: filter `UpdateOne(bson.M{"_id": tx.ID}, ...)` di `jalankanKonfirmasiUlang` memakai `tx.ID` (Go **string**) — tapi `_id` yang TERSIMPAN di Mongo untuk transaksi normal (jalur `CreateTransaksi`/`transaksi_fg.go`, tanpa `_id` eksplisit di body) adalah **ObjectID** auto-generate driver, BUKAN string. BSON type-sensitive: filter string TIDAK PERNAH cocok dokumen ber-`_id` ObjectID — `UpdateOne` "berhasil" (err nil) tapi `MatchedCount=0`, diam-diam tak mengubah apa pun. **Dibuktikan**: siklus dry-run→apply→dry-run pada SATU order (584590014067148231) tetap melaporkan `tautkan-saja` persis sama tiga kali berturut; raw fetch `GET /transaksi?kode=PJG-002` mengonfirmasi `detail.returnKey` memang tak pernah tertulis. **Dampak berbeda per aksi**: `aksiTautkanSaja` (pra-cutover) — NOL efek sama sekali, badge MANUAL tak pernah berubah. `aksiKirimSaja`/`aksiTautkanDanKirim` — sisi **integration tetap benar** (kebetulan selamat: `kirimKonfirmasiRetur` memakai `tx` yang SUDAH dimodifikasi di MEMORI, bukan hasil baca-ulang DB), tapi tautan **LOKAL** (badge MANUAL vs TERTAUT MARKETPLACE) tetap tak tersimpan juga — jadi retur yang integration-nya sudah beres tampil MANUAL selamanya di WMS. Pola SAMA yang sudah pernah ditambal di `UpdateStatusTransaksi` (`transaksi.go`, komentar eksplisit menyebut bug ini) — `jalankanKonfirmasiUlang` luput sejak dibuat. **Fix**: `transaksiIDFilter` (fungsi murni) — `$or` ObjectID-bila-hex-valid + string mentah, cermin pola `transaksi.go`; `MatchedCount==0` kini mengembalikan error eksplisit (bukan diam). **Backlog 30 hari kemarin perlu di-`apply=1` ULANG setelah deploy fix ini** — batch sebelumnya sebagian besar no-op tak terdeteksi. Grounded: `transaksiIDFilter` (`retur_konfirmasi_ulang.go`), tes `TestTransaksiIDFilter`.
>
> **Amandemen — celah TikTok tracking-RETURNED tanpa cancel/return sama sekali (✅ 2026-08-20).** Setelah dua bug di atas (Master Resi, `transaksiIDFilter`) genuinely beres — diverifikasi dry-run backlog 30 hari nol `tautkan-saja`/`tautkan-dan-kirim` tersisa — user tetap melaporkan resi baru berstatus MANUAL: `584959989047461290`. **Diselidiki lewat `/tiktokapi` live** (`order.get_order_detail`, `return_refund.returns_search`): order ini **TIDAK PERNAH** `CANCELLED`/`RETURNED` di sisi TikTok (masih `IN_TRANSIT`) dan TikTok **tak punya objek retur sama sekali** — satu-satunya sinyal retur adalah **tracking kurir forward** (mis. J&T) yang sudah `tracking.status=RETURNED` (paket balik ke seller). Dibandingkan ke Shopee (query prod): **0 order Shopee cocok pola ini**, dan vocab `tracking.status` Shopee bahkan tak pernah memuat nilai `RETURNED` — platform Shopee konsisten mem-flip order jadi `CANCELLED` utk skenario COD-gagal serupa, TikTok tidak. Populasi terukur: **~634–635 order TikTok**. **Fix** (integration, `accurate_rts_usecase.go`): `effectiveReturn` dapat cabang KETIGA — TikTok + `shipped_at` ada + `tracking.status` menandakan balik → `synthesizeTrackingReturnedReturn` (mirror `synthesizeCancelReturn`: SELURUH `order.Items` balik, `Status=ACCEPTED`, `Solution=0`, resi balik = resi forward — tak ada objek retur formal jadi tak ada resi balik terpisah). Fungsi baru `trackingMarksReturned` menyamakan `RETURNED` (masih transit balik) dgn `RETURNED_RECEIVED` (sudah sampai gudang) — SENGAJA, supaya sintesis tak hilang begitu kurir maju dari satu status ke status berikutnya di rebuild berikutnya (kalau hanya `RETURNED` yang dicek, transisi ke `RETURNED_RECEIVED` bikin `effectiveReturn` balik jadi nil, seolah retur yang sudah terbukukan tak pernah ada). Tanggal dokumen: `resolveReturnTransDate` dapat sumber baru `returnDateSourceTrackingReturned` (`tracking.last_event_at`, checkpoint kurir) — dicek SEBELUM cabang `order_update_date` umum TikTok, tapi DIBATASI ke order **tanpa** sub-doc retur hidup (retur TikTok yg sungguh diketahui marketplace tetap lewat `order_update_date` yang sudah teruji anti-drift, sama sekali tak tersentuh perubahan ini). Grounded: `effectiveReturn`/`synthesizeTrackingReturnedReturn`/`trackingMarksReturned`/`resolveReturnTransDate` (`accurate_rts_usecase.go`), tes `Test_effectiveReturn_tiktokTrackingReturned_synthesizes`/`_tiktokTrackingReturnedReceived_stillSynthesizes`/`_shopeeTrackingReturned_notSynthesized`/`_requiresShipped`/`_aliveSubDocWins`, `TestSynthesizeTrackingReturnedReturn`, `TestResolveReturnTransDate_TikTokTrackingReturnedUsesLastEventAt`/`_AliveSubDocUsesOrderUpdateDate`. **Belum dilakukan**: backfill/re-sync populasi ~634-635 order historis (baru berlaku utk order yang di-rebuild/disentuh sesudah deploy) — perlu dikonfirmasi user setelah fix ini deploy.
>
> **Amandemen — `warehouse_confirmed_at` ikut tanggal SCAN, bukan tanggal konfirmasi diproses (✅ 2026-08-20).** User bertanya balik setelah amandemen "Tgl Dicatat" di atas ("tanggal scan gudang harus sync dengan tanggal di Auto Sync Retur"), dipicu contoh nyata: WMS menunjukkan resi discan `2026-08-01`, tapi dokumen Retur Penjualan-nya bertanggal `13/08/2026` (label "tgl barang discan gudang"). **Diverifikasi live**: `warehouse_confirmed_at` prod = `2026-08-13T01:56:11Z`, `date_source: "warehouse"`. **Akar**: `ConfirmReturnFromWarehouse` (integration) mengisi `konfirmasiAt := time.Now()` — waktu SERVER MEMPROSES POST `/warehouse-confirm`, bukan kapan barang fisik di-scan. Selama tak ada gangguan, keduanya sama hari; tapi konfirmasi ini fire-and-forget dan bisa MACET (akar sudah ditambal amandemen "bug tersembunyi ... jalankanKonfirmasiUlang" & "aksiKirimSaja kirim pakai tautan BASI" di atas) — begitu macet lalu baru terkirim ulang lewat pemulihan supervisor **belasan hari kemudian**, dokumen ikut bertanggal hari PEMULIHAN, bukan hari SCAN. Order ini persis kena pola itu: baris `accurate_daily_returns` dibuat `2026-07-30` (marketplace pertama terdeteksi), gudang scan fisik `01/08`, tapi konfirmasi POST-nya baru berhasil `13/08` (jendela pemulihan backlog Aug 12–14 yang sama disebut amandemen di atas). **Fix**: `manufacture_transaksi.detail.date` (tanggal `<input type=date>` form WMS — SUDAH ada di data, cuma tak pernah dikirim ke integration) kini ikut payload konfirmasi sbg `scan_date` (`kondisiReturDariTransaksi`/`kirimKonfirmasiRetur`). Integration (`ConfirmReturnFromWarehouse`) memakainya sbg `konfirmasiAt` bila terisi & valid (WIB, `scanDateFromItems`); kosong/tak valid tetap fallback `time.Now()` — backward compatible, tak ada perilaku lama yang berubah. Karena jalur konfirmasi-ulang (recovery supervisor) memanggil fungsi manufacture YANG SAMA atas `tx` yang sama, backfill/retry di MASA DEPAN otomatis ikut membawa tanggal scan asli — bukan cuma perbaikan untuk konfirmasi baru. Desain sengaja diletakkan di `ReturnWarehouseItem.ScanDate` (level item, bukan mengubah signature `ConfirmReturnFromWarehouse`) — struct literal ber-field-nama otomatis kompatibel, ~20 call-site tes yang sudah ada TAK perlu disentuh. Grounded: `scanDateFromItems`/`ConfirmReturnFromWarehouse` (`accurate_rts_usecase.go`, integration), `entity.ReturnWarehouseItem.ScanDate` (`accurate_daily_return.go`), `kondisiReturDariTransaksi` (`retur_konfirmasi.go`, manufacture), tes `TestConfirmWarehouse_ScanDate*`/`TestKondisiReturDariTransaksi_ScanDate*`. **Belum dilakukan**: backfill dokumen historis yang sudah terlanjur bertanggal-pemulihan (bukan tanggal-scan) — perlu dikonfirmasi user, populasinya belum terukur.
>
> **Amandemen — sweep otomatis konfirmasi macet, DORMANT (✅ 2026-08-20).** Tindak lanjut amandemen `warehouse_confirmed_at` di atas: user bertanya kenapa gap-nya bisa sampai 12 hari. Jawaban: konfirmasi gudang **fire-and-forget** tak punya mekanisme retry otomatis SAMA SEKALI — sweep malam & tombol Retry sama-sama sengaja melewatkan populasi ini, dan satu-satunya jalan pulih selama ini murni supervisor menjalankan `POST /returns/konfirmasi-ulang` MANUAL. "12 hari" bukan interval yang dirancang, cuma berapa lama backlog dibiarkan sebelum ada yang kebetulan menjalankan pemulihan. **Fix** (bip-erp PR #1314): logika per-transaksi `KonfirmasiUlangRetur` diekstrak ke `prosesSatuKonfirmasi` (pure refactor, satu sumber kebenaran) supaya dipakai bersama handler manual **dan** goroutine sweep baru (`retur_konfirmasi_sweep.go`, default tiap 4 jam, jendela bergulir 3 hari). **DORMANT by default** (`RETUR_KONFIRMASI_SWEEP_ENABLED != "true"`, pola sama `AUTO_ARRANGE_ENABLED` integration) — kode aman ter-deploy, perlu dinyalakan sadar lewat env. Job background PERTAMA di manufacture-service (sebelumnya nihil — lihat [[IT - Background Jobs & Schedulers]]). Grounded: `prosesSatuKonfirmasi`/`jalankanKonfirmasiUlang` (`retur_konfirmasi_ulang.go`), `mulaiKonfirmasiUlangSweep`/`buildSweepFilter`/`jalankanSweepSekali` (`retur_konfirmasi_sweep.go`), tes `retur_konfirmasi_sweep_test.go`. **Belum dilakukan**: menyalakan env di prod (perlu keputusan user eksplisit sesudah deploy).
>
> **Amandemen — verifikasi pasca-deploy `scan_date` + ukuran populasi warisan (✅ 2026-08-20).** PR #1305/#1310/#1314 (+ erp-frontend #1104) merged & ter-deploy 12:06 WIB; diverifikasi sampai BINARY (`grep -a` atas `/service`: `scanDateFromItems` di Integration; `mulaiKonfirmasiUlangSweep`/`daftar_nama_barang`/`recorded_at`/`scan_date` di Manufacture) — bukan sekadar status merge. Log boot manufacture membuktikan sweep benar-benar ter-*wire* (`sweep konfirmasi-ulang OFF`), bukan cuma ter-compile. **Fix tanggal TERBUKTI bekerja**: dari 119 dokumen yang diproses pasca-deploy, 98 membawa `warehouse_items[].scan_date`, dan **98 dari 98 punya `trans_date` PERSIS SAMA dengan tanggal scan — nol selisih**. ⚠️ **Jebakan pengukuran yang sempat menyesatkan saya**: menyaring "konfirmasi pasca-deploy" pakai `warehouse_confirmed_at` itu SALAH — sejak fix, field itu berisi TANGGAL SCAN (tengah malam WIB), bukan waktu proses, jadi konfirmasi hari-ini malah jatuh SEBELUM ambang deploy dan tampak seolah cuma 1 baris. Sumbu waktu-proses yang benar = `updated_at`. Heuristik "tengah malam WIB = berasal dari scan_date" juga tak sahih untuk data PRA-fix (107/321 pra-deploy juga tengah malam) — pembuktian yang sah adalah membandingkan langsung ke `manufacture_transaksi.detail.date`. **Populasi warisan terukur** (dok `date_source=warehouse` yang dikonfirmasi sebelum deploy, dicocokkan ke tanggal scan WMS via API `/transaksi?kode=<SKU>` 28 SKU): 1.518 dok tercocokkan, **1.023 (67%) sudah benar** (drift 0), **495 ber-drift** — 74×1 hari, 77×2–3, 83×4–7, 261×8–30, **nol >30 hari**, rata-rata 3,1 hari, dan drift SELALU positif (dokumen tak pernah lebih awal dari scan — konsisten dgn akarnya). Pecahannya: **SENT 486** (322 dalam-bulan, **164 LINTAS BULAN** — tanggalnya akan mundur ke Juli, periode yang kemungkinan sudah ditutup finance), SKIPPED 5, FAILED 4; 90 dok tak punya padanan (member MANUAL tanpa `returnOrderId`). ⚠️ **`cmd/returnredate` TIDAK cocok untuk backfill ini**: deteksinya mendelegasi ke `ResolveReturnTransDate` yang tak membaca `warehouse_confirmed_at`, dan langkah kerjanya hapus-dok → hapus-baris → `SyncOrderReturn` ulang — untuk retur barang-balik, baris yang dihapus itu MEMBAWA `warehouse_items`, sehingga sync ulang akan tertahan gerbang gudang jadi PENDING: persis pola "hapus lalu gagal bukukan ulang = pembalikan lenyap". Backfill karena itu **DITAHAN menunggu keputusan finance** (bukan sekadar keputusan teknis) dan butuh alat baru yang membawa `warehouse_items` + tanggal scan sekaligus.
>
> **Amandemen — PROBE backfill: periode Juli TERBUKTI TERBUKA, ubah-di-tempat aman & reversibel (✅ 2026-08-20).** Sebelum membangun alat backfill untuk 495 dok ber-drift, dijalankan probe 1 dokumen untuk menjawab satu-satunya pertanyaan yang tak bisa ditebak: apakah Accurate menerima tanggal yang dimundurkan ke Juli (periode mungkin sudah ditutup finance)? **Target**: `INV/2026/07/16/001-BH|20260813` (id Accurate **117774**, `RTR/2026/07/23/031-BH`, 2 member/2 item — sengaja dipilih yang terkecil). **Bukti pendukung yang menguatkan pengukuran drift**: nomor RTR-nya SENDIRI sudah menyandang **23 Juli** (nomor RTR dibentuk dari tanggal retur, [[ADR - 0016 Retur Grouped per Faktur + Tanggal Retur]]) padahal tanggal dokumennya 13 Agustus — jadi dokumen itu selama ini bernomor-Juli tapi bertanggal-Agustus, persis kebingungan yang dilaporkan user. Pola serupa terkonfirmasi di kandidat lain (`RTR/2026/07/24/...` utk scan 24 Juli, `RTR/2026/07/29/...` utk scan 29 Juli). **Hasil**: Accurate **MENERIMA** `transDate=23/07/2026` — Juli TIDAK ditutup. Seluruh verifikasi lolos: *nomor tetap · total nilai tetap · jumlah baris induk tetap · header mode tetap · taxable tetap*; hanya tanggal berubah, **keterangan pun tak tersentuh**. Lalu **dikembalikan** ke 13/08/2026 dengan hasil verifikasi sama — membuktikan perubahan ini **reversibel dua arah** dan probe tak meninggalkan jejak (prod kembali persis seperti semula). **Konsekuensi desain**: mekanisme yang benar untuk backfill adalah **UPDATE IN-PLACE lewat `sales-return/save.do`** (pola `cmd/returnketeranganfix`/`cmd/invoicetaxfix`: baca `detail.do` → kirim ulang tiap baris beserta **id + `ReturnDetailStatusType` ASLI**, hanya header yang berubah) — BUKAN hapus+buat-ulang. Bedanya menentukan: kalau save ditolak, dokumen **tetap utuh** (no-op), sedangkan hapus-lalu-gagal-bukukan-ulang = pembalikan LENYAP. ⚠️ **`returnketeranganfix` sengaja TIDAK menyentuh DB** (`trans_date`/`dedupe_key`/`warehouse_confirmed_at`), jadi memakainya sendirian menimbulkan **divergensi DB↔Accurate** yang bisa "disembuhkan" salah arah oleh rebuild otomatis (tanggal balik + nomor RTR terbakar). Alat backfill karena itu WAJIB memperbarui **kedua sisi**: Accurate in-place **dan** baris DB (`trans_date`, `warehouse_confirmed_at`, `dedupe_key` yang memuat tanggal — dengan pengecekan bentrok kunci; pada probe, kunci tujuan `...|20260723` terbukti bebas). **Belum dilakukan**: alat batch-nya sendiri + eksekusi 495 dok.
>
> **KOREKSI — sweep otomatis DIUJI ke kegagalan nyata: menolong 0 dari 5. JANGAN dinyalakan (⚠️ 2026-08-20).** Atas permintaan user ("uji dulu skenarionya sukses atau tidak"), 5 kegagalan konfirmasi NYATA hari ini (226 sukses : 5 gagal dalam ~2,5 jam, ~2%) diperiksa satu per satu terhadap rancangan sweep. Hasilnya **membantah amandemen "sweep otomatis" di atas**: tak satu pun akan tertolong. Rinciannya — **(a) 2 order TikTok** (`584704823239279969`, `584725816695424606`) dikirim **26–27 Juni**, yaitu SEBELUM `autoSyncReturnCutoverDateWIB = "20260701"`: sengaja di luar Accurate, jadi konfirmasinya **tak akan pernah berhasil** dan sweep akan mengulanginya tiap 4 jam selamanya sambil memproduksi log error palsu (persis pola "alarm yang selalu menyala berhenti dibaca" di [[IT - Background Jobs & Schedulers]]); **(b) 1 order Shopee** (`260808T813BGES`) ber-`return.status=PROCESSING` — menunggu **approval Seller Center oleh manusia**, bukan menunggu sistem (populasi tab "Perlu Approval Seller Center"); **(c) 2 kegagalan rebutan kunci** (`"grup ... sedang diproses proses lain"`) — form gudang men-spawn satu konfirmasi PER SKU secara paralel lalu saling berebut lock, TAPI sweep justru **melewatinya**: `putuskanKonfirmasi` mengembalikan `aksiLewati` saat `k.Scanned` true, dan order tsb SUDAH punya `warehouse_items` dari SKU saudaranya. Sweep menilai per-ORDER, kehilangannya per-SKU. **Cacat rancangan tambahan**: jendela `metadata.created_at >= now-3hari` punya titik buta — apa pun yang macet lebih lama tak pernah terjaring, padahal kasus pemicunya justru 12 & 28 hari; pola yang benar adalah berbasis-STATUS tanpa jendela waktu, seperti `open_order_sweep.go` warehouse-service. **Pelajaran**: sweep dibangun sebelum komposisi kegagalan diukur.
>
> **KOREKSI KEDUA — alarm "49 barang retur hilang dari pembukuan" adalah FALSE POSITIVE, ditarik (⚠️ 2026-08-20).** Dari perbandingan scan WMS vs `warehouse_items` (4.362 baris diperiksa, di luar `catatanSaja` & yang belum bergrup), 49 baris tampak "hilang" pada 32 dokumen SENT. **Diuji ke datanya: 10 dari 12 sampel ternyata KOMPONEN BUNDLE**, bukan barang hilang — SKU jual berupa gabungan (mis. `"PJG-002 + PJG-003 + PJG-006"`), gudang men-scan komponennya satu per satu, sistem mencatat sebagian sebagai wakil bundle; mencatat semuanya justru DOBEL. Termasuk kasus yang jadi dasar alarm (`585039925408532115`, SKU jual `"PJG-002 + PJG-003 + PJG-004"` — PJG-002 adalah komponen bundle yang saudaranya sudah tercatat). 2 sisanya ber-SKU jual `PJK-015` (bundle juga, hanya tak memakai konvensi nama `" + "`). **Kesimpulan: rebutan kunci menimbulkan kegagalan yang berisik di log, tapi pada kasus yang diperiksa TIDAK terbukti menghilangkan data.** Metode pembandingan scan-vs-warehouse_items **tidak sahih tanpa menormalkan bundle lebih dulu** — catat ini supaya tak diulang.
>
> **KOREKSI KETIGA — simulasi backfill: angka "495 dok" SALAH, yang benar ~90; 411 dok butuh PEMECAHAN bukan penggantian tanggal (⚠️ 2026-08-20).** Sebelum menulis alat, user meminta simulasi ("uji dulu pasti benar atau tidak") — dan simulasi itu **membatalkan rancangan awal**. Rancangan lama memakai **tanggal scan PALING AWAL** per dokumen; simulasi atas 1.791 grup kandidat membuktikan itu keliru: **411 grup (23%) berisi order yang discan di HARI BERBEDA** (mis. `INV/2026/07/16/011-KY+GB|20260813` memuat scan 28, 30, dan 31 Juli) — satu tanggal mustahil benar untuk semuanya, dan memaksakan yang paling awal akan MEMALSUKAN tanggal member lain. **Rincian simulasi**: 1.791 kandidat → 256 tanpa data scan WMS (dilewati) · **411 AMBIGU** · **1.034 sudah benar** · **90 benar-benar perlu dikoreksi** (89 SENT + 1 FAILED), dari situ **84 kunci bersih** dan **6 TABRAKAN KUNCI** (kunci tujuan sudah dipakai grup lain → butuh penggabungan, jangan diotomatiskan). Selisih dgn angka 495 sebelumnya berasal dari metode MIN yang menghitung grup multi-hari sebagai "drift" padahal tanggal benarnya tak tunggal. **Temuan yang lebih dalam**: karena kode lama menstempel WAKTU PROSES (`time.Now()`) alih-alih tanggal scan, retur yang discan di hari BERBEDA tapi diproses dalam satu batch **ikut tergabung jadi SATU dokumen**. Jadi bug ini tak hanya menggeser tanggal — ia juga **menggabungkan dokumen yang seharusnya terpisah**. Memperbaiki 411 itu berarti MEMECAH satu dokumen jadi beberapa (hapus + bukukan ulang per hari) — jalur berisiko "hapus lalu gagal bukukan ulang", bukan sekadar ubah-di-tempat yang sudah terbukti aman lewat probe. **Rekomendasi**: kerjakan **84 dok kunci-bersih** dengan ubah-di-tempat (aman, reversibel, terbukti); **6 tabrakan** & **411 ambigu** ditahan sebagai keputusan terpisah.
>
> **Amandemen — DOKUMEN RETUR YATIM: 41 dokumen, Rp 26,7 jt terbalik DUA KALI (⚠️ 2026-08-20, PR #1329).** User melaporkan dari layar Accurate `RTR/2026/08/18/085-KY+GB` (Rp 5.025.000, faktur `INV/2026/07/20/002-KY+GB`). Pencarian by-`return_number` di DB kita melaporkan "tidak ada" — dan **kesimpulan awal saya bahwa itu salah-ketik ADALAH KEKELIRUAN**: dokumen itu nyata, hidup di Accurate, hanya saja **tak terlacak sistem kita**. Ketiga baris kita untuk faktur itu menunjuk dokumen LAIN (120571 `RTR/2026/08/18/086` 55 baris · 120774 `RTR/2026/08/20/085` 2 baris · 120626 `RTR/2026/08/20/021` REFUND), diverifikasi dgn membaca dokumen langsung dari Accurate. ⚠️ **Konsekuensi metodologis yang penting**: SEMUA audit retur yang ada (`cmd/returnrecon` dll) berangkat dari baris `accurate_daily_returns` lalu membaca dokumen yang DITUNJUKNYA — arah itu **mustahil** menemukan dokumen yatim, sehingga pengukuran "hanya 2 pembalikan ganda" yang dibuat sebelumnya **TIDAK SAH** (ia hanya membandingkan antar-dokumen yang kita catat). **Perkakas baru** `cmd/returnorphan` + `AccurateClient.ListSalesReturnsByDate` (READ-ONLY, meniru pola `ListSalesInvoicesByDate` yang sudah terbukti: filter `EQUAL` per-tanggal + paginasi — sengaja tak menebak sintaks `BETWEEN` yang belum pernah dijalankan ke prod) berangkat dari **sisi Accurate** lalu mencocokkan balik. **Pemisahan yang menentukan kejujuran angka**: "tak terlacak" ≠ "yatim" — dokumen era manual finance memakai penomoran `RTR/26/..` sedangkan sistem kita SELALU `RTR/2026/..`; asumsi ini **diverifikasi ke prod** (4.674 baris kita seluruhnya `RTR/2026`, NOL `RTR/26`). Tanpa pemisahan itu angkanya melebih-lebihkan masalah **70×**. **Hasil audit 1 Jul–20 Agt**: 5.394 dokumen di Accurate · 2.470 terlacak · 2.883 era manual finance · **41 YATIM SUNGGUHAN senilai Rp 26.746.500**. Delapan terbesar berketerangan **jenis CAMPURAN** ("RETUR COD + PAYLATER", "+ OVO", "+ QRIS") → dugaan kuat sisa dokumen **pra-pemecahan-per-jenis** yang penghapusannya gagal (mekanisme gagal-hapus terbukti ada: baris FAILED berbunyi *"gagal hapus dok lama … sebelum rebuild"*). **Dugaan sebab ini BELUM diuji** — jangan diperlakukan sebagai kesimpulan. **Tindak lanjut 41 dokumen = keputusan finance, bukan otomatis.** ⚠️ **Backfill tanggal (84 dok) DITUNDA** sampai ini tuntas: mengubah dokumen yang kita lacak sementara ada dokumen yatim tak diketahui hanya memperkeruh. Grounded: `cmd/returnorphan`, `accurate_sales_return_list.go`, tes `pisahYatim`.
>
> **Amandemen — UJI SEBAB dokumen yatim: 41/41 punya penerus untuk faktur yang SAMA (⚠️ 2026-08-20).** Dugaan diuji: *yatim = dokumen lama yang tersuperseded (pemecahan per-JENIS / rebuild) tetapi penghapusannya GAGAL*. Bila benar, faktur yang sama HARUS masih kita lacak lewat dokumen lain. **Dua iterasi uji — yang pertama DIBUANG karena cacat, dan itu bagian penting dari catatan ini**: (v1) memasangkan lewat *(tanggal faktur + kode toko)* → melaporkan "41/41 punya penerus", TAPI **terlalu longgar**: satu tanggal memuat puluhan faktur per toko, sehingga uji itu hampir mustahil berkata "tidak". Uji yang tak bisa gagal bukan uji. (v2) memasangkan lewat **ID FAKTUR PERSIS**. `salesInvoice` ternyata **tidak dikirim `list.do`** (diverifikasi: seluruh 41 kosong → uji sempat "tak dapat dijalankan", BUKAN "didukung"), jadi id faktur diambil per-dokumen lewat **`detail.do` → `invoiceId`** — nama field itu **dipastikan dengan men-dump payload MENTAH**, bukan ditebak (dok 119650 → `invoiceId` 80250, cocok dgn `accurate_invoice_id` faktur `INV/2026/07/20/002-KY+GB`). **Hasil v2: 41 dari 41 yatim punya penerus untuk faktur yang SAMA; 0 tanpa penerus; 0 gagal dibaca.** Dugaan **DIDUKUNG**. Contoh terkuat: yatim `RTR/2026/08/18/085` (Rp 5.025.000) berpenerus `RTR/2026/08/18/086` + `RTR/2026/08/20/085` + `RTR/2026/07/25/020` — seluruhnya SENT atas faktur 80250 yang sama. ⚠️ **BATAS KEJUJURAN**: ini membuktikan yatim **bukan dokumen asing**, tapi **BELUM** membuktikan barisnya menduplikasi penerusnya — nilai rupiah pembalikan-ganda per dokumen menuntut **perbandingan per-baris** yang belum dikerjakan. Jangan mengutip "Rp 26,7 jt" sebagai kerugian pasti; itu nilai total dokumen yatim, bukan selisih yang terbukti dobel. Grounded: `punyaPenerus`/`SalesReturnInvoiceID` (PR #1329).

> **Amandemen — PEMBALIKAN BERLEBIH TERBUKTI: 15 baris faktur, ≥ Rp 687 rb (⚠️ 2026-08-20).** Pertanyaan "berapa rupiah yang benar-benar terbalik dua kali" akhirnya dijawab **tanpa** menyimpulkan dari perbandingan antar-dokumen versi kita. Payload mentah `detail.do` (di-*dump* dan diperiksa, **bukan ditebak** — pelajaran dari `salesInvoice` yang ternyata tak dikirim `list.do`) menunjukkan tiap baris retur membawa `salesInvoiceDetail` berisi **`quantity`** (jumlah TERJUAL di baris faktur) dan **`returnQuantity`** (AKUMULASI yang sudah diretur terhadap baris itu, lintas SELURUH dokumen, dihitung **Accurate sendiri**). Jadi `returnQuantity > quantity` adalah pernyataan pembukuan Accurate, bukan tafsiran kita. **Hasil prod** (119 baris faktur yang tersentuh 41 dokumen yatim): **5** diretur PAS · **99** diretur KURANG dari terjual · **15 DIRETUR MELEBIHI terjual** → pembalikan ganda TERBUKTI, nilai kelebihan **≥ Rp 687.000**. Terbesar: baris faktur `500442` (terjual 1, diretur **4**, lebih 3 = Rp 294.000) yang tersentuh yatim `RTR/2026/08/18/085` **dan** `RTR/2026/08/15/084` — dua yatim atas baris yang sama. ⚠️ **DUA BATAS yang WAJIB dibaca bersama angka itu, jangan dikutip terpisah**: **(1)** Rp 687 rb adalah **LANTAI, bukan total** — baris komponen bundle ber-`unitPrice` 0 (nilainya melekat di baris induk), sehingga kelebihan pada komponen tak terhitung rupiahnya; **(2)** sunyi pada 104 baris lain **TIDAK** membuktikan bersih — bila retur fisik 1 dari 5 terjual lalu dihitung dua kali, `returnQuantity`=2 masih ≤ 5 dan lolos dari uji ini. Membuktikan duplikasi penuh menuntut pembanding **retur FISIK** (WMS/marketplace), bukan Accurate semata. **Konsekuensi praktis**: angka Rp 26,7 jt (total nilai dokumen yatim) **bukan** kerugian; kerugian yang TERBUKTI sejauh ini ≥ Rp 687 rb pada 15 baris. Grounded: `SalesReturnLines`/`SalesReturnLineRef` (PR #1329).

> **Amandemen — ANGKA FINAL pembalikan ganda: 34 dokumen, Rp 25,1 jt (⚠️ 2026-08-20).** Uji `returnQuantity` sebelumnya (15 baris, ≥Rp 687 rb) ternyata **jauh understated** — ia hanya menangkap duplikasi yang kebetulan MELEWATI jumlah terjual, dan sunyi bila retur fisik 1-dari-5-terjual dihitung dua kali. Celah itu ditutup dengan uji **tumpang tindih ID BARIS FAKTUR**: bila baris faktur yang SAMA diretur oleh dokumen YATIM **dan juga** oleh penerusnya, qty pada yatim itu pembalikan berganda. ⚠️ **Sengaja memakai id baris faktur (identitas milik Accurate), BUKAN mencocokkan SKU lintas sistem** — pencocokan SKU sudah sekali menipu audit ini (gudang men-scan komponen bundle, Accurate membukukan induknya → selisih SKU tampak "barang hilang" padahal bukan; lihat koreksi false-positive di atas). **Hasil prod atas 41 yatim**: **34 TERBUKTI menduplikasi = Rp 25.144.500** · **7 TANPA tumpang tindih** = retur sah tersendiri yang sekadar tak terlacak (bukan dobel, tapi tetap tak terlacak). Terbesar: `RTR/2026/08/18/085-KY+GB` — **141 baris dobel, Rp 5.025.000**, baris fakturnya juga diretur `RTR/2026/08/18/086` + `RTR/2026/08/20/021` + `RTR/2026/08/20/085`; inilah dokumen yang dilaporkan user dari layar Accurate. **Ringkasan tiga lapis uji** (masing-masing menutup celah yang sebelumnya): (1) yatim vs terlacak → 41 dok Rp 26,7 jt *nilai dokumen*, bukan kerugian; (2) `returnQuantity` > terjual → 15 baris ≥Rp 687 rb *terbukti berlebih*, tapi lantai; (3) tumpang tindih baris faktur → **34 dok Rp 25,1 jt duplikasi sesungguhnya**. **Tindak lanjut = keputusan finance** (menghapus dokumen retur yang sudah masuk pembukuan); 7 yatim non-duplikat JANGAN dihapus — itu retur sah. Grounded: `SalesReturnLines` + uji duplikasi (PR #1329).

> **Status**: ✅ **deployed** (PR #1183, jalur kirim-ulang + fix under-book "booked bukan beres"; PR terpisah #1215 untuk amandemen tautan-basi + fallback Master Resi 2026-08-14 di atas — **belum deploy**). **Backlog under-book ~2.571 order (Aug 12–14) sudah dipulihkan** lewat `apply=1` bertahap — 5 dari 9 order dari amandemen tautan-basi pulih langsung tanpa fix kode (link masih benar), sisanya + populasi MANUAL menunggu deploy #1215. Grounded: `retur-filter.ts` (`takPerluApproval`), `returns.go` (`konfirmasiTertunda`), `retur_konfirmasi_ulang.go` (`putuskanKonfirmasi`, `perluTautkan`, `perluKirim`, `buildKonfirmasiUlangFilter`), `GudangBarangJadiView.tsx` (badge daftar retur).

**9. Form retur dilengkapi + daftar difokuskan** (22 Juli 2026). Perbaikan FE `GudangBarangJadiView` di atas fondasi #8:
- **Pengaman input**: keterangan kondisi (dropdown Rework: isi berkurang/segel terbuka; Reject: pecah/barang tidak sesuai—klaim ekspedisi) **wajib** saat qty terisi; total per baris tak boleh **melebihi** klaim marketplace (over-retur diblokir — Accurate pasti menolak), **kurang** disorot (parsial sah).
- **Panel konteks retur read-only** (order, no. retur, channel/toko, status order, alasan marketplace **yang di-humanize** dari kode mentah, nilai refund) — sebelumnya form tak menampilkan retur mana yang dicatat.
- **Field "Cancel Order" manual dihapus** — diturunkan dari `order_status` marketplace (sistem sudah tahu; ketik ulang = sumber salah).
- **Scan resi dari halaman daftar** membuka form terisi + tertaut (auto-fokus untuk scanner); pesan sukses membedakan tertaut vs catatan WMS lepas.
- **Daftar difokuskan**: default hanya retur yang bisa dikerjakan; badge memimpin angka **aktif**, retur **basi** (>14 hari, barang mungkin tak datang) diberi angka redup + dilipat di bawah. Ambang 14 hari heuristik (idealnya per-SLA channel).
- **Resi paket BALIK diutamakan** (22 Juli 2026): form mengisi resi dari `reverse_tracking_no` (label paket retur yang diterima gudang), Master Resi (forward) hanya cadangan; **scan mencocokkan ke resi-balik dulu** sebelum Master Resi. Sumber & backfill di [[Microservices - Integration Service]].

**10. Search + filter + export laporan retur, dan auto-isi PIC** (22 Juli 2026, FE-only, tab Return). Logika murni di-TDD (vitest, `features/manufacture/utils/*`):
- **Search + filter**: kotak cari (order/retur/**resi-balik**/resi-forward/toko/SKU, partial+case-insensitive, `cocokPencarian`) + filter channel; disambung ke daftar tanpa mengubah perilaku default.
- **Export laporan fleksibel** (`buildBarisExport`): modal pilih **kolom** (24 kolom termasuk resi-balik/kurir/status-logistik + Reuse/Rework/Reject + dicatat-oleh) + **filter** (rentang tanggal, channel, status catat) + **toggle granularitas per-retur / per-SKU** → **xlsx** (`exceljs`+`file-saver` dynamic import, pola `ResiMasterView`). Murni client-side dari feed yang sudah dimuat (batas truncation >2000 diberitahukan).
- **Auto-isi "Nama Pengecek (PIC)"** dari **`full_name` JWT** (cookie `token`, fallback `username`), diisi **hanya bila kosong** — DEFAULT kenyamanan, **BUKAN** pengganti stempel audit `created_by_name` (#6) yang tetap server-side & otoritatif. Ini pembedaan penting: PIC = field bisnis bisa disunting, stempel = jejak audit tak bisa dipalsukan.

> **Amandemen — kolom "Daftar Nama Barang" di export (✅ 2026-08-20 FE, ✅ 2026-08-20 BE — lihat koreksi di bawah).** Granularitas **per-retur** (default modal export) hanya membawa `daftar_sku` (kode SKU digabung koma) — operator harus buka master SKU terpisah untuk tahu nama barangnya; granularitas per-SKU sudah punya `item_name` tapi itu bukan default. Kolom baru `daftar_nama_barang` (mirror `daftar_sku`, gabung `items[].name`) ditambahkan ke `EXPORT_COLUMNS` & masuk **default terpilih** `expCols`. Grounded: `nilaiKolom`/`EXPORT_COLUMNS` (`retur-export.ts`), tes `retur-export.test.ts`; `expCols` (`GudangBarangJadiView.tsx`).
>
> **Koreksi — perbaikan di atas TIDAK CUKUP, export sesungguhnya server-side (bug ditemukan langsung oleh user setelah deploy, ✅ 2026-08-20).** `retur-export.ts` (`buildBarisExport`/`EXPORT_COLUMNS`) TERNYATA sudah lama bukan sumber nilai file — `doExport` (`GudangBarangJadiView.tsx`) memanggil **`GET /returns/export`** (manufacture), yang men-generate `.xlsx` di server sejak migrasi "Dulu dirakit client-side... Kini backend menggerakkan" (komentar sudah ada di kode sebelum amandemen ini, TAK dibaca saat menambahkan kolom di atas). Backend punya katalog kolomnya SENDIRI (`returnExportColumns`/`nilaiKolomExport`, `returns_export.go`) yang harus di-mirror manual dari FE — tidak otomatis sinkron. Akibatnya kolom baru di atas terkirim FE lewat `?cols=...` tapi **diam-diam dibuang** `selectExportColumns` (kunci tak dikenal di katalog backend) — file hasil download TETAP tanpa nama produk walau kolom "terpilih" di modal. User melaporkan langsung setelah kedua PR (FE amandemen di atas + fix "sudah deploy" sebelumnya) di-deploy: "nama produk masih ngga ada di file hasil export nya". **Fix**: `daftar_nama_barang` ditambahkan ke `returnExportColumns` + `nilaiKolomExport` (fungsi baru `namaBarangList`, mirror `skuList`: nama dari item marketplace `mr.Items[].Name`, fallback ke item kondisi gudang `cond.Items[].Name`) + daftar kolom default internal (`selectExportColumns`). **Pelajaran**: dua implementasi export (FE lama, BE baru) hidup berdampingan di kode tanpa satu sumber kebenaran skema kolom — perubahan kolom ke depan WAJIB menyentuh KEDUANYA (`retur-export.ts` **dan** `returns_export.go`), atau tulis ulang salah satunya jadi generated dari yang lain. Grounded: `returnExportColumns`/`nilaiKolomExport`/`namaBarangList` (`returns_export.go`), tes `returns_export_test.go`.
>
> **Amandemen — kolom "Tgl Dicatat" ditambah, "Tgl Update" BUKAN tanggal scan (✅ 2026-08-20).** User bertanya langsung: kolom "Tgl Update" (`updated_at`) di file export itu tanggal scan gudang? **Bukan** — `updated_at` diisi dari `updated_at` milik sub-dokumen retur MARKETPLACE (integration), field yang SAMA yang sudah diselidiki panjang untuk `resolveReturnTransDate` (lihat amandemen "celah TikTok tracking-RETURNED" di atas): melar mundur 7–10 hari (TikTok, settle refund) / ~3 hari (Shopee). Tanggal scan SESUNGGUHNYA (`returnCondition.RecordedAt`, dari `manufacture_transaksi.metadata.created_at` — sudah dipakai backend untuk SORTING internal scope `RECORDED`) **tak pernah diekspos jadi kolom sama sekali** sebelum ini. **Fix**: kolom baru `recorded_at`/"Tgl Dicatat" ditambah di KEDUA katalog (mengikuti pelajaran amandemen di atas) — backend `returnExportColumns`+`nilaiKolomExport` (bip-erp PR #1305, commit kedua) & FE `EXPORT_COLUMNS`+`nilaiKolom`+`ExportRec.recordedAt` (erp-frontend PR #1104), keduanya masuk default kolom terpilih. Kosong bila retur belum dicatat gudang (mis. scope `ALL`).

## Consequences

**Yang membaik**

- Finance bisa membandingkan sumber vs pencatatan gudang di **lima** menu, bukan hanya retur — dan di Keluar FG perbandingannya kini setingkat (per SKU vs per SKU).
- Log WMS menampilkan penginput yang tak bisa dipalsukan, terpisah jelas dari field PIC yang diketik manual.
- Baris sumber jadi titik masuk kerja (klik → form terisi), bukan sekadar tabel baca.
- Resi batal / belum diambil kurir **tidak lagi bisa masuk** form Keluar FG lewat pintu mana pun — sebelumnya angka sumber ikut menghitungnya dan form bisa menariknya, sehingga stok terpotong untuk barang yang tak pernah keluar.

**Yang harus diterima**

- **Data lama tampil `—`.** Username tak pernah disimpan sebelumnya dan tidak bisa diisi surut — audit log punya username tapi `Target`-nya tak cukup untuk join balik.
- **Tab sumber Keluar FG & Inbound RM tidak bisa menjawab "mana yang belum diinput"** sampai kunci join ditambahkan (menyimpan nomor resi/PO pada transaksi saat input). Itu perubahan terpisah dan hanya akan akurat untuk data sesudahnya.
- ~~**PO multi-bahan tidak mengisi form penuh.**~~ **Diselesaikan (22 Juli 2026):** form Inbound RM kini multi-baris — klik baris sumber PO memuat **seluruh** bahan PO sebagai baris (Qty SJ dari qtyOrder; Lolos/Reject diisi operator), baris bisa ditambah/dihapus, dan submit membuat satu transaksi INBOUND per bahan. Tiap baris menampilkan Nama Barang readonly dari master (kode tak dikenal ditandai jelas). Ini menghapus tebakan "bahan mana" — operator memfilter dari daftar lengkap, bukan mengetik ulang.
- **Daftar "SJ Belum Diterima" bukan angka pasti.** Status SJ berubah `DELIVERED` hanya bila penerimaan dibuat lewat pilihan *Terima dari Surat Jalan*; penerimaan yang diketik manual tidak menutup SJ-nya, sehingga SJ itu tetap tampil tertunggak walau barangnya sudah masuk. Perilaku pra-ada, baru terlihat sekarang karena datanya ditampilkan sebagai daftar.
- **"SEBAGIAN" bisa over-flag** (amandemen gerbang per-order). Order yang barangnya **genuinely tak balik** (hilang/`TANPA_BARANG_BALIK` — tercatat di manufacture-service, tak terlihat integration per [[ADR - 0002 Database-per-Service]]) tetap dihitung "belum discan" → grup tampil SEBAGIAN padahal sebenarnya beres. Konservatif yang **disengaja** (over-flag lebih aman dari under-book — "cek ini" > sembunyikan). Menyempurnakannya butuh status fisik dari manufacture-svc (perubahan lintas-service terpisah).
- **Tab sumber Keluar FG disaring per tanggal RTS.** `resiList` memuat jendela 60 hari (≈14 ribu record); agregasi per SKU membuat batas 300 baris yang sempat dipasang tidak lagi diperlukan (satu tanggal ≈ 19 baris) dan sudah dihapus.
- **Angka tab sumber Keluar FG bukan "seluruh resi hari itu".** Yang batal/retur dan yang belum diambil kurir dikecualikan, jadi totalnya lebih kecil dari jumlah resi mentah. Jumlah yang dikecualikan ditampilkan eksplisit di header supaya selisihnya bisa dijelaskan, bukan tampak sebagai data hilang.
- **Perilaku terhadap status Shopee belum teruji** — di dev semua resi TikTok, tak ada satu pun Shopee meski endpoint sync-nya ada. Begitu Shopee aktif di produksi, kategori kanoniknya perlu dicek sekali; status yang tak terpetakan akan ikut terhitung (dan dilaporkan merah), bukan lenyap.
- **Gerbang ini punya TANGGAL LAHIR, dan itu penting saat membetulkan data lama** (temuan remediasi 2026-08-06). Gerbang backend baru terbukti jalan **22 Juli 2026**; retur untuk order yang lebih tua **tak pernah** melewatinya, jadi ketiadaan `warehouse_items` pada dokumen lama **bukan** tanda barangnya belum discan — memang tak pernah ada mekanismenya. Konsekuensi praktis: saat membukukan ulang data lama, `--skip-warehouse-gate` **hanya sah untuk dokumen pra-22-Juli**. Dipukul-rata ke dokumen yang lebih baru, ia membukukan barang yang belum dilihat siapa pun — persis yang gerbang ini ada untuk mencegah. Rinciannya di [[ADR - 0040 Retur Paket Utuh via Baris Induk Faktur]].
- ~~**`cmd/cancelretrigger` & `cmd/returnrebook` salah membaca "belum dibukukan"**~~ **✅ diperbaiki 2026-08-06 (PR #1065).** Keduanya menyimpulkan sebuah order tak punya baris dengan mencari **satu sisi saja**, padahal sejak model grup ([[ADR - 0016 Retur Grouped per Faktur + Tanggal Retur]]) pembukuannya sering hidup sebagai **member** di baris grup order lain. Salahnya ke dua arah berlawanan: `returnrebook` (hanya `order_id`) melaporkan "gagal 41" padahal ~20 di antaranya sudah terbukukan benar; `cancelretrigger` (hanya `members.order_id`, plus menghitung baris **apa pun** termasuk `SKIPPED`/`FAILED`) menganggap order macet sudah ditangani — itu yang menyembunyikan 5 retur TikTok macet. Kini keduanya memakai satu sumber kebenaran `FilterOrderSudahDibukukan`: cocokkan `order_id` **dan** `members.order_id`, status dibatasi `SENT`/`PENDING`. Sengaja **tidak** memakai ulang `GetByMemberOrderIDAny` — lookup itu melayani gerbang anti-dobel ("jangan bikin dokumen kedua", `FAILED` pun dihitung ada), sedangkan filter ini menjawab "masih perlu dikerjakan?". Ikut dibereskan: `cancelretrigger` tak lagi memperlakukan **error query** sebagai "sudah ditangani" (bentuk lama `cerr != nil || n > 0` menghilangkan order dari daftar tanpa jejak), dan `returnrebook` memisahkan `PENDING` (menunggu scan gudang = hasil BENAR) dari gagal.
- **Retur "basi" belum punya penutupan resmi** (#9). Retur barang-balik yang barangnya tak akan datang menumpuk di "belum dicatat" selamanya — melipatnya di FE hanya kosmetik. Perlu aksi backend "tutup retur basi" (state penutupan) + keputusan finance (siapa boleh menutup, kapan). ANTREAN.
- **Stok WMS = stok Accurate untuk semua kondisi** (#8; selisih Reject **diselesaikan 22 Juli**). Backend `deltaStokTransaksi` kini menambah **seluruh qty** (Reuse+Rework+Reject) ke stok WMS — SAMA dengan yang Accurate bukukan (`RETURNED`), jadi tak ada lagi selisih otoritatif. Ini **membalik arah** temuan awal 17 Juli ("reject ke scrap"): karena Accurate membukukan ketiganya, mengurangi Reject di WMS justru menciptakan selisih permanen — satu pintu penyesuaian (finance manual, keputusan 21 Juli) lebih mudah dijaga daripada dua sistem menebak. Sisa: FE `onUpdateStock` masih menambah Reuse+Rework saja untuk update optimistik lokal, tapi itu **kosmetik & sekejap** — tertimpa `loadData()` yang mengambil stok server (semua qty). Diselaraskan di iterasi FE berikutnya.

## Dokumen Terkait

- [[Microservices - Manufacture Service]] · [[APP - Web ERP]] · [[API - Manufacture Service]]
- [[Manufacture - Stock & Material Management]] (konsep domain)
- [[ADR - 0002 Database-per-Service]] (sebab join retur dikerjakan di kode Go, bukan `$lookup`)
- [[ADR - 0015 Push Pergerakan WMS ke Accurate]] (Input Gudang FG = titik pengakuan stok FG)
