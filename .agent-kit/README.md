# ERP Agent Kit

Gaya kerja koding ber-AI yang seragam untuk tim ERP Bharata. Landasannya arsitektur
draf (`architecture-draft`). Semua dev pakai Claude Code dengan flow & tooling yang sama.

## Onboarding (3 langkah)

1. Clone `architecture-draft` + project yang digarap sebagai **sibling** di dalam folder `erp/`:
   ```
   erp/
   ├── architecture-draft/   (vault ini)
   └── <project>/            (mis. bip-erp)
   ```
2. Buka folder `erp/` di Claude Code, jalankan init sekali:
   - Windows:  `powershell -ExecutionPolicy Bypass -File architecture-draft\.agent-kit\init.ps1`
   - mac/linux: `bash architecture-draft/.agent-kit/init.sh`
3. Mulai task: `/start-task <deskripsi>`.

## Flow wajib (per task)

`/start-task` → `/plan` → `/implement` → `/review` → `/sync-docs` → `/wrap`

## Update standar

`git -C architecture-draft pull` lalu jalankan ulang init. Session-start hook akan
mengingatkan bila versi kit terpasang ≠ versi terbaru.

## Isi kit

- `commands/` — 6 slash command flow + `/ask` (recall read-only, sebut sumber) + `/skills` (cek/install skill plugin rekomendasi tim) + `/index-vault` (bangun VAULT-INDEX.json) + `/analisa-kebutuhan` (kebutuhan manajemen mentah → ADR + dok domain + daftar task; berhenti sebelum kode) + **loop otonom** `/brief`, `/kerjakan`, `/judge`, `/supervise`, `/ekstrak-skill`, `/papan-sesi` (kit ≥ 1.15.0) + `/dashboard` (kit ≥ 1.16.0) + `/kantor-agent` (kit ≥ 1.20.0).
- `agents/` — agen kustom `loop-*` (disalin `init` → `.claude/agents/`): eksekutor per domain `loop-fix`/`loop-refactor`/`loop-test`/`loop-docs`, eksekutor per lapisan `loop-fe`/`loop-be`/`loop-mobile`/`loop-devops` (kit ≥ 1.24.0), penilai `loop-judge` (read-only), `loop-supervisor`, `loop-ekstrak-skill`.
- `hooks/` — `session-start` (info flow + cek versi/staleness + daftarkan sesi), `sesi-sentuh` (UserPromptSubmit), `sesi-selesai` (SessionEnd), `pre-commit-gate` (**menolak** commit di branch default repo kode, exit 2), `antre-gate` + `antre` + `antre-lib` (antrean kerja berat lintas sesi, kit ≥ 1.30.0), skrip pendukung loop (`gerbang`, `baseline-test`, `worktree-baru`, `worktree-bersih`, `transkrip-ringkas`, `papan-sesi`, `kantor-agent` (penulis data Python + template HTML + launcher `.ps1`/`.sh`); lib bersama `sesi-lib`, `gerbang-lib`), `gerbang-kit` (gerbang vault atas test milik kit sendiri, kit ≥ 1.25.0), dan `githooks/pre-push` (dipasang lewat `core.hooksPath` ke repo kode **dan** vault).
- `baseline/` — `<repo>.json`: test yang memang merah di `origin/main`, bertanggal + ber-commit; dibandingkan `gerbang` supaya hanya kegagalan BARU yang menolak.
- `templates/brief.md` — Quick Brief Spec untuk `/brief`.
- `skills/` — skill tim (disalin `init` → `.claude/skills/`). Kini: `migrasi-tabel-hris`, `audit-keamanan`, `deploy-bip-erp`, `peta-alur`.
- `rules/` — `team-memory.md` (ingatan tim bersama; **di-import langsung** oleh CLAUDE.md dari vault via `@../architecture-draft/.agent-kit/rules/team-memory.md` — update cukup `git pull`, tak perlu re-init) plus berkas prosedur yang **tidak** di-import dan dibaca on-demand supaya tak membakar konteks tiap sesi: `plan-checklist.md` (`/plan`), `review-checklist.md` (`/review`), `wrap-completion-gate.md` (`/wrap`), `vault-retrieval.md` (pencarian vault, dipakai `/ask`, `/start-task`, `/analisa-kebutuhan`), dan `ui-checklist.md` (mutu layar dan urutan reuse komponen; dirujuk team-memory, `plan-checklist.md` §1, dan `review-checklist.md` §J).
- `templates/` — `workspace-CLAUDE.md` (jadi `erp/CLAUDE.md`).
- `init.ps1` / `init.sh` — pemasang.
- `VERSION` — versi kit.
- `docs/` — design & plan.

## Changelog

- **1.30.0**: **Antrean kerja berat lintas sesi** (rencana `.task-plans/2026-09-25-antrean-kerja-berat.md`). Diukur 2026-09-25 13.39-13.53: CPU 83-88%, RAM 26/32 GB, disk 73%, karena beberapa sesi Claude menjalankan `tsc`, `next build`, dan `vitest` (sekitar 20 worker per suite) bersamaan; satu gerbang `go test` berjalan 2 jam. Aturan "dua eksekutor wajar" di `commands/kerjakan.md` hanya prosa, dan ADR 0077 tidak menghitung prosa sebagai gerbang. Kini run PENUH (suite test, build, tsc, lint, `go build/test ./...`, `flutter test/build`) dari sesi mana pun mengantre satu per satu (`AGENTKIT_ANTRE_SLOT`, default 1): `hooks/antre-lib.ps1` memegang satu-satunya klasifikasi perintah (`Get-KelasPerintah`: `penuh`/`tertarget`/`bukan`; teks berkutip dan here-string dibuang dulu sehingga `git commit -m "pnpm test"` bukan perintah) dan kuncinya. Kunci = **named Mutex per slot, BUKAN Semaphore**: hitungan Semaphore bocor saat pemegangnya mati, sedangkan Mutex pemegang yang di-kill menjadi *abandoned* dan langsung diambil penunggu berikutnya (teruji dengan proses nyata). Pemegang memasang `AGENTKIT_ANTRE_PEGANG=<PID>` sehingga anak-anaknya tak mengambil kunci lagi (tanpa ini satu slot = deadlock). Tiga titik pasang: (1) `gerbang-lib.ps1` `Invoke-Gerbang`/`Invoke-GerbangBatas` **per langkah**, bukan sepanjang gerbang, dan waktu antre tidak masuk `durasi_detik` maupun batas waktu `dart analyze`/`flutter test`; (2) `githooks/pre-push` lewat fungsi `berat()`; (3) hook PreToolUse baru `antre-gate.ps1` yang **menolak** (exit 2) run penuh yang tak lewat `antre.ps1`, dengan pesan berisi path absolut dan perintah `run_in_background: true` (menunggu giliran bisa melampaui batas 10 menit tool); jalan keluar sadar `$env:AGENTKIT_ANTRE_LEWATI=1;`. Test tertarget (satu berkas/paket) sengaja tidak mengantre supaya siklus TDD tak menunggu suite 40 menit. Penunggu mencetak pemegang tiap 60 detik ke stderr (tanpa batas tunggu: batas akan menggagalkan gerbang karena antrean, tuduhan palsu), dan `/papan-sesi` punya bagian "ANTREAN KERJA BERAT". Pola `if` hook (9 alat x Bash/PowerShell) wajib mencakup setiap alat yang dikenali `Get-KelasPerintah`; `init.ps1` menandainya. **Mac/linux: no-op tercatat** (`antre.sh` langsung `exec`, `init.sh` tak memasang hook). Test: `tests/test-antre.ps1` baru (klasifikasi 38 kasus termasuk kontrol negatif, hook, Mutex dengan proses nyata: menunggu/abandoned/bersarang/2 slot/exit code, gerbang-lib, papan-sesi, dan `git push` sungguhan ke remote bare sandbox dengan `pnpm` palsu yang membuktikan tsc/lint/build berjalan DI DALAM antrean), dipanggil dari `test-init.ps1`; entri `if` dipatok persis. Kontrol mutasi: kunci `Invoke-Gerbang` dibuang membuat "menunggu slot" merah; `berat` dibuang dari pre-push membuat "di dalam antrean" merah (kontrol pertama sempat merah karena sebab lain, sh tak melihat `pnpm.cmd` palsu; diperbaiki dengan kembaran sh). **Butuh re-init.**

- **1.29.0**: **Gerbang kunci-ganda pada berkas Docker Compose** (brief `2026-09-24-gerbang-kunci-ganda-compose.md`, sumber bip-erp #2038). `docker-compose.yml` bip-erp kemasukan `INTEGRATION_MODULE_URL` dua kali di blok `employee-service` (2026-09-23 16:07, empat menit sesudah container terakhir dibangun), dan YAML menolak kunci ganda dalam satu mapping sehingga `docker compose` gagal parse SELURUH berkas — bukan lint, bukan test, bukan CI, bukan `pre-push` (gerbang Go hanya menyala bila `services/<x>` tersentuh, dan tak pernah `go test`) yang menangkapnya; seluruh service tetap `healthy` sampai orang pertama mencoba deploy berikutnya, berjam-jam kemudian, dan gagal. Blok baru di `githooks/pre-push` + `githooks/gerbang-compose.py` (override `construct_mapping` `yaml.SafeLoader`, mencatat SEMUA duplikat di sembarang kedalaman mapping berikut DUA nomor barisnya) menyala dari **berkas compose yang TERSENTUH** lewat satu pola (`compose_pat`), bukan dari jenis repo — bekerja di repo mana pun yang punya `docker-compose*.yml`/`compose.yaml`, tak digerbang `[ -d services ]`. Basis diff yang tak bisa ditentukan memeriksa SEMUA berkas compose yang ada (pola yang sama dengan `svcs` di blok Go saat shared-library tersentuh), dan python/PyYAML yang tak terpasang membuat gerbang GAGAL, bukan dilewati diam-diam. ⛔ **Judge menolak percobaan 1 dengan 2 kritis**, keduanya diperbaiki di rilis ini. Pertama, `VERSION` tak dinaikkan: `init` menulis `.agent-kit/VERSION` ke `.claude/.kit-version` dan `session-start` mencetak "(terkini)" bila sama, jadi sinyal yang seharusnya menyuruh tim re-init justru berkata sebaliknya — gerbangnya hidup di vault sambil menjaga nol orang, dan diukur di mesin percobaan 1 memang tak satu pun `.claude/hooks/githooks/gerbang-compose.py` terpasang. Kedua, berkas ber-`<<: *anchor` (YAML merge key, fitur Compose baku untuk extension fields/fragments — bip-erp sudah memakai KELUARGA idiom yang sama lewat `x-mongo-logging: &mongolog`, yaitu anchor + alias; diukur ke `origin/main` 2026-09-24 ia punya **nol** `<<:` hari ini, jadi ini positif palsu yang MENUNGGU, bukan yang sedang terjadi — langkah dari alias ke merge key adalah lanjutan baku pola itu) **ditolak** dengan dua pesan yang dua-duanya salah ("gagal di-parse sebagai YAML" lalu "GAGAL: kunci ganda") walau berkasnya sah tanpa keraguan: `_construct_mapping` menggantikan constructor `tag:yaml.org,2002:map` tanpa pernah memanggil `flatten_mapping` (satu-satunya yang membuang kunci `<<`), dan tag merge tak punya constructor terdaftar sehingga `construct_object` pada key node-nya jatuh ke `construct_undefined` dan melempar. ⛔ Perbaikannya BUKAN memanggil `flatten_mapping`: fungsi itu menyisipkan pasangan hasil merge DI DEPAN `node.value`, sehingga kunci lokal yang memang sengaja menimpa kunci warisan (mis. `restart` di-override per service) akan terbaca sebagai duplikat — positif palsu jenis baru. Yang benar: kunci ber-tag `tag:yaml.org,2002:merge` dilewati UTUH di loop (tak di-`construct_object`, tak dicatat), dan anchor-nya sendiri tetap diperiksa lewat jalur biasa di tempat definisinya. Dibuktikan lewat fixture (disiapkan koordinator, disalin persis) dua jebakan sekaligus: `x-common: &common` dipakai dua service lewat `<<: *common`, satu di antaranya menimpa `restart` secara lokal — LOLOS; fixture yang sama plus `image` ditulis dua kali di satu service — TETAP tertangkap, supaya perbaikan positif-palsu tidak diam-diam mematikan deteksinya. Perbaikan informasional: **satu** pola (`compose_pat`) kini sumber kebenaran tunggal "apa itu berkas compose" untuk KEDUA cabang (`find . -type f | sed 's#^\./##' | grep -E "$compose_pat"`) — sebelumnya cabang basis-diff-tak-tentu punya daftar `-iname` sendiri yang diam-diam menyimpang dua arah (`compose.dev.yml` lolos grep tapi terlewat `-iname`, `docker-compose-prod.yml` sebaliknya), dan arahnya terbalik dari yang diinginkan karena cabang itu ada justru karena daftarnya tak bisa dipercaya. Dua catatan lain didokumentasikan sebagai komentar tanpa mengubah perilaku: gerbang ini memeriksa isi WORKING TREE berkas tersentuh, bukan blob commit yang dipush; dan pesan kegagalan `pre-push` masih menyebut "kunci ganda" walau `gerbang-compose.py` bisa gagal karena sebab YAML lain. Diverifikasi lewat `git push` sungguhan ke sandbox bare-remote terisolasi (bukan bip-erp/vault, `core.hooksPath` menunjuk langsung ke source): kontrol negatif (duplikat) ditolak menyebut kunci + dua baris, kontrol positif (diperbaiki) lolos, nama berkas berbeda (`docker-compose.dev.yml`) di repo tanpa `services/`/`package.json` tetap menyala, `PATH` tanpa python sama sekali → GAGAL bukan dilewati, dan kedua fixture merge-key lolos/tertangkap persis seperti di atas — semuanya lewat push nyata, bukan simulasi. Nol positif palsu dikonfirmasi atas compose bip-erp yang SEKARANG lewat `gh api ...?ref=main` (checkout lokal 1.493 commit di belakang `origin/main` dengan perubahan tak terkait tak dipakai sebagai acuan). Test: `tests/test_gerbang.py` **61** (dari 56: duplikat bersarang dua-baris-benar, berkas bersih, merge-key+penimpaan lokal, merge-key+duplikat asli tetap tertangkap, YAML rusak sebab lain), pytest kit keseluruhan **360** (dari 355), `tests/test-init.ps1` tetap hijau. **Butuh re-init.**
- **1.28.0**: **Triase task di pintu masuk: keputusan dulu, atau langsung brief** (2026-09-23). Loop otonom terukur bekerja tetapi jarang dipakai — **16 PR** sepanjang 17 hari berbanding 548 PR terbaru di tiga repo, dan **146 artefak `/plan` berbanding 26 brief** dalam satu bulan. Sebabnya bukan loop-nya lemah melainkan ia harus **diingat**: task masuk lewat `/start-task` kecuali seseorang sengaja memilih `/brief`. Probe atas 20 rencana terakhir: **8 muat jadi brief hari ini, 8 muat sesudah satu keputusan diambil, 4 memang tidak seharusnya**. Yang membedakan ternyata **bukan ukuran** melainkan apakah keputusannya sudah diambil — task 213 dan 238 baris lintas dua repo muat karena ADR-nya sudah memutuskan, sementara task 177 baris satu repo tidak muat karena intinya justru memutuskan. Triasenya tinggal di dua tempat: tiga bulir di `rules/team-memory.md` sebagai pemicu universal (menyala juga di sesi tanpa slash command, yaitu **155 dari 173** berkas sesi yang tak pernah keluar dari tahap `mulai`), dan prosedur lengkapnya langkah 0 di `/start-task`. Gerbangnya **berbasis keyakinan**: sumber keputusan yang berhasil dibuka → `/kerjakan` langsung; sumber yang tak bisa ditunjuk atau repo/domain `(ditebak)` → brief ditampilkan dan menunggu persetujuan. Ragu berarti `ragu`, gagal-tertutup. Field `Sumber` di `templates/brief.md` **sudah ada sejak lama dan tak pernah disebut prosedurnya**; yang ditambahkan artinya, bukan fieldnya. `/kerjakan` menulis **`keputusan_lanjut`** per percobaan supaya run yang terputus bisa dibedakan dari run yang benar-benar habis tiga percobaan — diperiksa atas tiga brief yang berhenti, dua menulis `## Hasil` dengan benar dan satu masih memuat placeholder template — dan badan PR menyebut **`Dasar keputusan`** supaya yang menekan merge bisa membantah premisnya (terukur nol `reviewDecision` tercatat pada 634 PR dalam 30 hari). ⛔ **Yang TIDAK berubah, sengaja ditulis eksplisit**: judge dua lapis, `pre-commit`, `pre-push`, gerbang kelengkapan `/wrap`, batas 3 percobaan, kewenangan merge (ADR 0077 §1), dan deploy prod oleh manusia. Batas "task kecil" `/brief` juga tidak diperlebar — yang berubah cuma pintu masuknya. **Ditunda dengan alasan tertulis**: perbaikan dinamis (percobaan 2 memilih strategi berbeda) dan carry-over pendekatan yang gagal, keduanya karena n = 15 brief terlalu kecil untuk membuktikan apa pun dan keduanya akan mengacaukan metrik bila dipasang bersamaan dengan triase. **Ditolak oleh ukuran**: berhenti lebih awal saat temuan judge berulang — diperiksa atas kelima brief yang butuh lebih dari satu percobaan, temuan percobaan kedua **selalu** di `file:line` berbeda, jadi sinyalnya tidak ada. ⚠️ Dua jebakan PowerShell tertangkap TDD saat menulis gerbangnya: **backtick adalah karakter ESCAPE di pola wildcard**, jadi pola `-like` yang membungkus kata dengan backtick terurai jadi kata itu diikuti bintang HARFIAH dan tak akan pernah cocok, apa pun isi berkasnya (dipakai `.Contains()`); dan `Out-File -Encoding utf8` menulis **BOM** yang masuk ke subjek commit. ⚠️ **Review akhir menemukan lima lubang di SAMBUNGAN antar-berkas, bukan di berkasnya** — tiap berkas benar sendiri-sendiri. Dua di antaranya membatalkan mekanisme utamanya dan sudah diperbaiki di rilis ini: baris **"Flow wajib"** yang disuntikkan hook `SessionStart` ke setiap sesi ternyata **membantah** pemicu triase yang baru dipasang (kini dikualifikasi "hanya bila triase menghasilkan tidak", di `.ps1`, `.sh`, dan template CLAUDE.md), dan **`/kerjakan <teks bebas>`** menjalankan `/brief` lalu "lanjut" tanpa syarat sehingga melewati gerbang `ragu` yang baru dibuat. Tiga sisanya: `keputusan_lanjut` tak punya aturan nilai (kini dipetakan eksplisit ke keadaan; nilai tak jujur membatalkan seluruh gunanya), langkah 0 menuntut resolusi sumber "dengan perintah" tanpa menunjuk `rules/vault-retrieval.md` §1b yang hanya dirujuk di Langkah 2 yang justru dilewati, dan **jalur vault domain `docs` push langsung ke `main` tanpa PR** sehingga `Dasar keputusan` yang cuma ada di badan `gh pr create` tak pernah terbit untuk brief dokumentasi (kini wajib di badan commit vault juga). Test: `tests/test-init.ps1` **103** (dari 88), termasuk anggaran 8 baris untuk blok `team-memory.md` yang dibuktikan tidak vakum lewat kontrol negatif (2 baris ditambahkan → tepat 1 FAIL pada assertion yang diklaimnya) dan penjaga URUTAN langkah 0 yang dibuktikan lewat mutasi di memori (asli `True`, blok dipindah ke bawah `False`). Desain: `docs/2026-09-23-triase-task-design.md`. **Butuh re-init.**
- **1.27.0**: **Kantor Agent dibagi jadi pos per departemen, dan denahnya jadi gedung sungguhan** (sesi `d77f4cfa`, 2026-09-21), seluruhnya atas keputusan pemilik yang diambil bertahap sambil melihat hasilnya. Denah lama dibagi per AKTIVITAS dan masing-masing hanya ada satu (`perpustakaan`, `meja`, `server`, `rapat`, `lounge`), sehingga tak menjawab pertanyaan yang justru ingin dijawab pemilik: **sesi ini sedang mengerjakan sistem departemen mana**. Kini ada **9 pos** (HRGA, Marketing, Tech Development, Kesekretariatan, Finance, Procurement, Warehouse, Manufaktur, Quality) ditambah **Umum** untuk sesi yang belum menyentuh apa pun yang dikenali, masing-masing ruangan berpintu dengan rak buku, meja, dan papan namanya sendiri. Nama departemennya **grounded ke `deptKeyToNames`** (`bip-erp/shared-library/common/roles.go`), bukan dikarang, dengan penyimpangan yang dicatat: `Human Resource` + `General Affair` jadi `HRGA` (selaras master data, HRGA memang `supervision_label` keduanya), `Beauty Hacks` + `Kyura` jadi `Marketing`, serta `Legal` + `R&D Regulatory` melebur ke `Kesekretariatan`; `Marketing` dan `Warehouse` sendiri tak punya key di peta itu dan memasukkannya adalah keputusan kit ini. Departemen sesi ditentukan dari **path berkas yang disentuh**, dan yang ambigu (`services/insentive`, `services/integration`) sengaja TIDAK dipetakan karena robot di pos keliru tak berbunyi apa pun sementara robot di pos `Umum` menyatakan dirinya sendiri. **Bentuk gedungnya**: persegi 36x36 dengan koridor **perempatan** di tengah, tiap blok berisi tiga ruangan bersekat **kaca** yang semuanya menghadap koridor dengan **satu pintu**, pojok terjauh tiap blok jadi **taman dalam** (pojok itu tak dijadikan ruangan karena satu-satunya cara memberinya pintu adalah menembus tetangganya), dan lounge dibiarkan **terbuka**. Ruang server **menyatu dengan Tech Development** sebagai satu ruangan yang MEMANJANG dua sel, rak server di sel utara dan meja kerja di sel selatan, jadi sesi dari pos lain yang menjalankan perintah benar-benar berjalan ke sana; pos `Printing` dihapus dan lounge memanjang ke bekas selnya, yang sekaligus memindahkan jalan masuk lounge ke koridor tegak alih-alih menembus ruangan Kesekretariatan. Isi ruangan dinyatakan dalam koordinat LOKAL terhadap pintunya (`lokalKotak`), dan mukanya dipaksa selalu selatan atau timur karena proyeksi ini **tak pernah menggambar sisi utara dan barat**: aturan "hadap pintu" yang terdengar wajar justru membuat rak di ruangan berpintu utara kehilangan mukanya sama sekali dan terbaca membelakangi pintunya, persis keluhan yang muncul di ruang Quality. ⛔ **Dua cacat SENYAP ditemukan saat memverifikasinya, dan keduanya membuat fitur ini mati total sambil denahnya tetap terlihat wajar.** Pertama, `salin()` menyalin daftar field tetap dan **`pos` tidak ada di daftar itu**: `r.pos` dibaca `grupPos()` dan `klaimIdentitas()` tetapi tak pernah diisi dari data sesi, jadi SELURUH robot jatuh ke bangku cadangan dan kesembilan ruangan departemen berdiri kosong, tanpa satu pun galat. Yang menemukannya uji browser `2b`, bukan mata; sempat dijelaskan keliru sebagai "robotnya masih berjalan". Kedua, **ekor transkrip 64 KB terlalu pendek untuk menentukan pos**: ekor itu cuma memuat 26 sampai 46 kejadian dan yang terakhir hampir tak pernah berupa path berkas departemen, jadi diukur atas 8 transkrip hidup **8 DARI 8 sesi jatuh ke `Umum`** padahal kedelapannya punya departemen jelas bila dibaca lebih dalam. `pos_dari_pindai_dalam` memindai MUNDUR berbatas 4 MB, dipanggil hanya bila ekor dan cache tak punya, hasilnya diingat termasuk yang negatif (dipindai ulang baru sesudah transkrip tumbuh sepanjang satu ekor penuh); kedalaman yang benar-benar dibutuhkan terukur **1 MB untuk 6 sesi dan 2 MB untuk 2 sesi, 3 sampai 13 ms**, jauh lebih murah daripada jendela tugas latar 8 MB yang sudah dijalankan tiap tick. Sesudah keduanya diperbaiki, 6 sesi hidup di mesin ini dapat departemen nyata (Quality, Tech Development x3, HRGA, Warehouse); sebelumnya 6 dari 6 `Umum`. Test: `tests/test_kantor_agent.py` **158** (dari 117), termasuk penjaga bahwa `POS_DENAH` di template dan `POS` di penulis berisi himpunan yang sama, karena itu satu fakta di dua berkas dan menyimpangnya senyap di kedua arah; `tests/kantor-agent-browser.ps1` **51 PASS 0 FAIL** dengan 0 galat JavaScript. ⚠️ **Baseline `origin/main` diukur lebih dulu (46 PASS 0 FAIL)**, jadi kesembilan kegagalan awal terbukti lahir dari pekerjaan ini dan bukan bawaan; satu di antaranya regresi sungguhan (robot lounge dibuat menghadap timur padahal ia duduk di sofa bermuka selatan) dan sisanya patokan yang memang harus ditulis ulang. ⚠️ **Patokan koordinat yang diketik ulang di harness dibuang seluruhnya**: pusat meja rapat sempat tertinggal di `(22; 11,5)` milik tata letak lama dan menuduh kursi yang sebenarnya sudah benar, jadi geometri kini DIBACA dari halaman lewat `__KANTOR_UJI__.geometri()`. Check "pusat kamera mendarat di robot" diganti "robot masuk bingkai" + "kamera IKUT saat robotnya berjalan", karena pusat kamera memang **dijepit ke kotak denah** supaya menyorot robot di pinggir tak memperlihatkan latar kosong; melebarkan toleransi ditolak karena itu justru menghapus yang ingin dijaga. Lima kontrol mutasi terbukti merah pada assertion yang diklaimnya: nama pos di denah diubah, pemindai blok `POS_DENAH` dibutakan, potongan pindai tak disambung, pindai dibalik jadi maju, dan batas pindai diabaikan. **Butuh re-init.**

- **1.26.0**: **Robot Kantor Agent berkarakter baru, dan kamera yang mengikuti sesi yang disorot** (sesi `d77f4cfa`, 2026-09-21), keduanya atas permintaan pemilik beserta gambar referensinya. Robot tak lagi dirakit dari kotak isometrik: ia **bulat dan melayang tanpa kaki**, berbadan **putih untuk semua sesi**, dengan visor gelap lebar berisi dua mata kubah dan senyum. Perabot sengaja dibiarkan kotak supaya robot jadi tokoh yang menonjol dari latarnya. **Identitas sesi pindah dari badan ke cahaya visor**: mata dan senyum memakai warna sesi, bukan cyan tetap. Konsekuensinya disebutkan sebelum pemilik memilih, dan diterima: dua sesi berwarna mirip lebih sulit dibedakan dari jauh daripada saat badannya sendiri yang berwarna, dan badan putih di atas lantai terang menyandarkan pemisahannya pada bayangan di bawahnya. Punggung tetap membawa satu indikator kecil berwarna supaya identitas tak hilang total saat robot membelakangi kamera, karena proyeksi ini hanya memperlihatkan sisi selatan dan timur sehingga dua arah lain memang tanpa wajah. ⚠️ Tiga jebakan terukur saat menggambarnya, semuanya hanya terlihat oleh MATA dan tak satu pun akan ditangkap test: proporsi pertama membuat kepalanya pipih dan melayang jauh di atas badan sehingga terbaca sebagai **piring terbang**, bukan helm; **sweep-flag busur SVG terbalik dari dugaan yang wajar** (sumbu y menghadap ke bawah, jadi 1 melengkung ke ATAS) sehingga versi pertama tergambar **cemberut** lengkap dengan mata berbentuk mangkuk; dan busur senyum menonjol ke bawah sejauh jari-jarinya dari kedua ujungnya, jadi tanpa memperhitungkan itu ujungnya menembus bibir visor. Bayangan lantai memakai `ry` berbasis `HH`, bukan `HZ`, karena lingkaran di lantai memproyeksi jadi elips 2:1. **Kamera pengikut**: menyorot sebuah sesi, lewat kartu di panel maupun dengan mengklik robotnya, memperbesar denah ke robot itu dan **mengikutinya selagi ia berjalan** (terukur: lebar `viewBox` 1000 jadi 404, pusatnya mendarat 6px dari robotnya). Yang dianimasikan `viewBox`, bukan `transform` pada satu grup, sehingga label, gelembung, dan papan ruangan ikut membesar tanpa perhitungan terpisah; `VIEW_AWAL` dibaca dari atribut `viewBox` itu sendiri supaya angkanya tak hidup di dua tempat; kamera dijepit ke kotak denah supaya menyorot robot di pinggir tak memperlihatkan latar kosong; dan begitu sampai ia berhenti menulis atribut tiap bingkai. Lepas lewat **Esc** atau klik robotnya lagi, dengan petunjuk yang tampil selama kamera mengikuti. `setSorot()` menyatukan tiga salinan kode sorotan yang sebelumnya berdiri sendiri-sendiri. Test: `tests/kantor-agent-browser.ps1` **46 check** (dari 41), 0 galat JavaScript. ⚠️ Penghitungan mata **pindah dari warna ke KELAS** (`.mata`, `.senyum`): mematok `88ffff` akan hijau untuk implementasi apa pun sekarang warnanya mengikuti sesi, dan check-nya jadi vakum; kontrol positifnya (yang menghadap kamera bermata dua dan bersenyum) ditambahkan di baris yang sama, karena "0 mata" tanpa pembanding tak membuktikan apa-apa. Check kamera mengukur `viewBox` sungguhan dan jarak pusatnya ke robot, bukan sekadar kelas `.sorot` yang bisa terpasang sementara kameranya tak bergerak. Dua kontrol mutasi terbukti merah pada assertion yang diklaimnya: panggilan kamera dicabut, dan visor dipaksa selalu tergambar. **Butuh re-init.**
- **1.25.0**: **Dua lubang gerbang ditutup: nol gerbang bukan lulus, dan kit menundukkan dirinya pada disiplinnya sendiri** (sesi `d77f4cfa`, 2026-09-21). ADR 0077 par 1 menahan wewenang merge pada manusia **sampai ada mekanisme yang bisa menolak**, dan dua lubang membuat mekanisme itu belum utuh. **Lubang pertama**: `gerbang.ps1` menghitung `lolos` sebagai "tak ada gerbang yang gagal", dan daftar KOSONG memenuhi syarat itu, jadi repo yang jenisnya tak dikenali dinyatakan lolos tanpa satu pemeriksaan pun lalu `/judge` mengalikannya dengan verdict agen seolah lapis mesinnya sudah bekerja. Diukur hari ini, yang jatuh ke jenis `lain` bukan satu melainkan **empat**: `architecture-draft`, `mybharata-app`, `guestbook-system`, dan `consolidated-accounting-app`. Keputusannya kini bertumpu pada **jenis repo, bukan jumlah gerbang** (`Get-PutusanLolos` / `putuskan_lolos`), karena nol gerbang punya dua arti yang berlawanan: jenis `lain` berarti kita tidak tahu cara memeriksanya (**GAGAL**), sedangkan repo Go yang branch-nya cuma menyentuh README memang tidak punya yang perlu diperiksa (lolos, dengan catatan bahwa itu tidak membuktikan apa pun). Satu-satunya pengecualian adalah daftar-izin `RepoTanpaGerbang` berisi `architecture-draft`, dan daftar itu **bukan kekebalan**: gerbang yang benar-benar berjalan selalu menang. Jenis **`flutter`** baru (`pubspec.yaml`): `dart analyze` atas folder `.dart` yang tersentuh saja, berbatas waktu dan **terpisah** dari `flutter test --machine` (1200 detik, dibandingkan baseline seperti vitest dan go), karena analyze seluruh repo sudah terbukti menggantung. Daftar foldernya diringkas dua kali, dan keduanya menambah cakupan alih-alih menguranginya: folder yang sudah **tercakup induknya** dibuang (`dart analyze` bekerja rekursif, jadi `lib` bersama `lib/src/core/api` menganalisis subpohon yang sama berulang), dan bila folder daunnya masih lebih dari 20 ia **diruntuhkan ke segmen pertama** alih-alih dipotong — memotong akan membuang pemeriksaan diam-diam, persis kelas yang sedang ditutup. Diukur di `mybharata-app`: 21 folder jadi `lib test`, dan durasinya turun dari 108,6 detik (bahkan sempat menembus batas 300 detik saat mesin sibuk) jadi **37,2 detik** dengan cakupan yang justru lebih luas. Batasnya lalu dinaikkan ke 600 detik supaya ia menjaga **gantung**, bukan lambat. Deteksi **`node` diperlebar ke lockfile apa pun** dengan pelaksana dibaca dari lockfile-nya (`pnpm` / `npm run` / `yarn` / `bun run`); urutannya penting karena `erp-frontend` nyata memegang `pnpm-lock.yaml` DAN `package-lock.json` sekaligus, dan `package.json` tanpa lockfile sengaja tetap `lain` sebab pelaksananya tak bisa dibaca. **Alat yang tidak terpasang membuat gerbang GAGAL, bukan lenyap.** Dua jebakan terukur saat membangun cabang Flutter, keduanya senyap: `$p.ExitCode` sesudah `WaitForExit(ms)` terbaca `$null` sehingga `dart analyze` yang berbunyi "No issues found!" dinilai **GAGAL**, lalu `WaitForExit()` tanpa argumen (obat yang tampak benar) menunggu pipe keluaran tertutup dan proses `dart` sisa menahannya sampai batas 300 detik habis; exit code akhirnya diminta dari `cmd` sendiri lewat `/v:on` + penanda `!ERRORLEVEL!`, dan keluarannya dibaca dengan `FileShare.ReadWrite`. **Lubang kedua**: `init` sengaja mengecualikan vault saat memasang `core.hooksPath`, jadi `tests/test-init.ps1` dan pytest `Tools/` tak pernah digerbang apa pun (ADR 0077 par 4). Vault kini ikut, dan `githooks/pre-push` memanggil `hooks/gerbang-kit.py` — **satu** implementasi untuk semua OS, bukan pasangan `.ps1`/`.py`, karena python sudah jadi syarat pytest-nya sendiri. Tiga hal membuatnya tidak mengganggu pekerjaan dokumentasi: saringan path (push yang tidak menyentuh `.agent-kit/` atau `Tools/` keluar seketika, dan `Toolsmith/` tidak tertipu), penjaga rekursi `AGENTKIT_KIT_TESTS_RUNNING` karena `test-init` menjalankan `init` yang memasang hook ini, dan jalan keluar sadar `AGENTKIT_SKIP_KIT_TESTS=1` yang dicetak keras. Venv dicari di worktree **UTAMA** repo (worktree tertaut tidak punya `Tools/.venv`), dan python tanpa pytest bukan kandidat. ⛔ **Push pertama yang memicu gerbang ini menemukan jebakan yang jauh lebih berbahaya daripada yang sedang ditutup**: git mewariskan `GIT_DIR`, `GIT_WORK_TREE`, `GIT_INDEX_FILE`, dan `GIT_QUARANTINE_PATH` ke hook-nya, dan variabel itu **menang atas penemuan repo**, sehingga `git -C <folder lain>` tetap mengenai repo yang sedang di-push. `test-init.ps1` membuat repo sandbox di `%TEMP%`, tetapi seluruh perintah gitnya mendarat di worktree vault yang sedang di-push: **dua commit kosong bertambah di atas branch kerja, branch `feat/uji` lahir di repo nyata, HEAD berpindah ke `main`, dan `user.email=test@example.invalid` tertulis ke config repo** — tak satu pun terbaca sebagai galat, yang terlihat hanya test-init merah yang hijau bila dijalankan langsung. `gerbang-kit.py` kini membuang **seluruh** `GIT_*` sebelum memanggil anak (bukan daftar tertentu: yang diwariskan git bertambah antar versi), dan `test-init.ps1` sendiri **menolak jalan** bila masih melihat lingkungan itu, supaya test yang dijalankan tangan dari konteks hook berhenti alih-alih mengubah repo orang. ⛔ Push **kedua** menemukan lubang ketiga, satu lapis lebih awal dan persis kelas yang sama: `$rsha` yang dioper git ke `pre-push` adalah tip remote sesungguhnya, dan itu bisa commit yang **belum ada di sini** bila remote sudah maju; `git diff <commit asing> <lsha>` lalu gagal diam-diam, daftar berkasnya kosong, dan tiap cabang membaca kosong itu sebagai "tidak ada yang tersentuh" lalu melewati pemeriksaannya — push yang memuat perubahan `.agent-kit/` dilaporkan **"gerbang test kit DILEWATI"**. `pre-push` kini memastikan basis diff benar-benar ada (`git cat-file -e`) dan, bila tidak, **memeriksa semua**: gerbang kit dinyalakan lewat `--paksa`, dan repo Go membangun seluruh service alih-alih nol. Dua ambang `loop-kirim` dinaikkan ke 20 dan 25 detik: yang dijaga adalah "tidak menggantung", bukan kecepatan spawn (terukur 5,07 dan 9 detik saat sepi, 11,4 dan 16 detik saat sibuk), dan sejak rilis ini test itu ikut jalan di pre-push saat mesin justru sedang sibuk. Diverifikasi atas repo nyata: `guestbook-system` kini terbaca `node` berpelaksana npm (sebelumnya `lain`), `architecture-draft` lolos lewat daftar-izin dengan alasannya tercetak, `mybharata-app` terbaca `flutter` dan analyze-nya benar-benar berjalan **108,6 detik** atas 683 berkas tersentuh, dan kontrol negatif membuktikan gerbangnya tidak vakum (galat tipe sengaja → `dart analyze` exit 3 → gerbang merah; diperbaiki → exit 0 → hijau). Test: `tests/test_gerbang.py` baru (56 test), `tests/test-init.ps1` 88 (dari 73), pytest seluruhnya 314. **Paritas kedua implementasi** dikunci dua arah: `Get-JenisRepo` lawan `jenis_repo` atas fixture yang sama, dan subperintah `konstanta` yang membandingkan batas waktu, peta lockfile beserta **urutannya**, serta daftar-izin — tanpa yang kedua, nilai yang cuma diubah di satu berkas menyimpang tanpa satu pun test merah, dan yang memakainya di OS lain tidak akan tahu. **Delapan** kontrol mutasi terbukti merah pada assertion yang diklaimnya: aturan nol-gerbang dicabut di Python dan di PowerShell, deteksi `flutter` dicabut, saringan path pre-push dicabut, pembersihan `GIT_*` dicabut, penjaga `GIT_DIR` di test-init dicabut, pemeriksaan basis diff di pre-push dicabut, dan satu konstanta dinaikkan hanya di sisi PowerShell. **Butuh re-init** supaya `core.hooksPath` vault terpasang; tanpa itu gerbang vault tidak menyala dan gagalnya SENYAP.
- **1.24.0**: **Peran agent per lapisan tim IT, dan eksekusi paralel** (sesi `d77f4cfa`, 2026-09-21). Kit hanya punya eksekutor per DOMAIN, sementara `SCRUM SPECS` menyebut Development Team diisi **BE/FE/DevOps** dengan QA yang masih dirangkap Scrum Master, jadi peran di kantor tak pernah terbaca sebagai tim. Empat agen baru: `loop-fe` (erp-frontend), `loop-be` (bip-erp), `loop-mobile` (mybharata-app), dan `loop-devops` yang sengaja **tanpa `PowerShell` dan `Bash` di `tools`** sehingga ia menulis compose, workflow, skrip, dan urutan deploy tetapi tak punya alat menjalankannya; itu menjadikan aturan "deploy PROD dijalankan manusia" sebuah gerbang, bukan kalimat (ADR 0077 par 3 menuntut gerbang punya exit code, dan ketiadaan alat adalah padanannya). `/kerjakan` par 0 memilih eksekutor dari **dua sumbu, domain kali repo**; `test` tetap ke `loop-test` dan `docs` ke `loop-docs` karena keduanya lintas lapisan; repo tanpa spesialis jatuh ke cadangan `loop-fix`/`loop-refactor`. **Paralel**: `/kerjakan <a> <b>` men-dispatch dua eksekutor dalam satu pesan bila repo berbeda, kedua brief bertanda `Paralel: aman`, dan pasangan yang berbagi endpoint memuat blok `## Kontrak` identik; maksimum dua brief sekaligus karena satu mesin (terukur: dua pekerjaan Chrome bersamaan membuat satu gagal "target CDP tidak ditemukan dalam 120 detik"). `/brief` mengisi field `Paralel` (ragu berarti tidak) dan menulis blok kontrak; `templates/brief.md` menambah keduanya. Peta `PERAN` di `hooks/kantor-agent.py` bertambah enam: FE, BE, Mobile, DevOps, plus `loop-supervisor` dan `loop-ekstrak-skill` yang selama ini tampil sebagai nama mentah. Diukur sebelum dirancang (14 hari, 64 transkrip): 40 sesi memanggil Agent, **32 dari 40 tak pernah punya lebih dari satu subagent hidup bersamaan**, `loop-*` cuma 52 dari 624 panggilan, **5 dari 12 brief butuh percobaan kedua**, dan jeda PR sampai merge p50 **6 menit** di kedua repo sehingga manusia bukan hambatan. Diukur juga bahwa subagent SUDAH memegang `CLAUDE.md` dan seluruh ingatan tim di konteksnya, jadi berkas agen kini melarang membacanya ulang alih-alih menyuruhnya. Test: `tests/test-init.ps1` menambah penjaga bahwa **tiap** agen kit punya entri `PERAN` dan bahwa `loop-devops` tak punya shell; `tests/test_kantor_agent.py` 117 (dari 110). **Butuh re-init**; peran barunya belum terbaca pada giliran yang sama dengan `init`, tetapi terukur 2026-09-21 **terbaca sendiri pada giliran berikutnya tanpa restart** (`loop-fe` ditolak "not found", lalu tersedia dan diuji: ia menolak `git commit`, menolak menambah prop komponen shared, dan mengutip aturan FE yang tidak ada di berkas agennya sendiri). Restart tetap jalan yang pasti bila giliran berikutnya masih menolak.
- **1.23.0**: **Panel Kantor Agent bisa disembunyikan, bilah gulirnya tipis** (sesi `d77f4cfa`, 2026-09-16), permintaan pemilik atas halaman terpasang. Saklar `sembunyikan panel` di kanan atas header memasang `hidden` pada `<aside>` dan kelas `tanpa-panel` pada `<main>`, sehingga gridnya jadi satu kolom dan denah mengisi ruangnya (terukur di jendela 1600px: panggung 1188px jadi 1527px); isi panel tidak dibongkar, jadi kartunya kembali utuh saat ditampilkan lagi. Pilihannya diingat lewat `localStorage`, yang terukur BISA dipakai di `file://` pada Chrome (dugaan awal bahwa Chrome menolaknya dengan `SecurityError` salah, dan diperiksa sebelum ditulis); pembungkus `try` tetap ada untuk peramban dan konfigurasi yang menolak penyimpanan di `file://`. Bilah gulir panel **6px** lewat `::-webkit-scrollbar`, sementara `scrollbar-width: thin` sengaja hanya dipasang di dalam `@supports not selector(::-webkit-scrollbar)`: di Chrome `scrollbar-width` MENANG atas pseudo-element webkit dan `thin` di sana menghasilkan 10px, sedangkan aturan webkit saja 6px (diukur di peramban dengan elemen kontrol pembanding: 6px lawan 15px bawaan; `offsetWidth - clientWidth` wajib dikurangi dua border 1px milik panel, kalau tidak angkanya terbaca 8px). Harness uji berhenti memakai `--hide-scrollbars` karena flag itu membuat lebar bilah terukur **0px**, sehingga check-nya akan lolos untuk aturan CSS apa pun. Test: `tests/kantor-agent-browser.ps1` 41 check (3 baru); kontrol mutasi mematikan aturan bilah tipis dan check itu merah dengan bilah kembali 15px. **Butuh re-init.**
- **1.22.0**: **Arah hadap robot Kantor Agent** (sesi `d77f4cfa`, 2026-09-16). Robot tak lagi selalu menatap penonton: `gambarRobot` menerima arah dan merotasi badannya 90 derajat per langkah, wajah digambar lewat `sisi()` yang memutuskan sendiri apakah bidang depannya jatuh di sisi yang terlihat (`mukaTimur` menemani `mukaSelatan`), dan yang membelakangi mendapat garis engsel punggung. Proyeksi ini hanya memperlihatkan sisi selatan, timur, dan atas, jadi hadap utara dan barat memang tanpa wajah; pemilik memilih yang paling realistis dan identitas tetap terbaca dari label nama serta warna. Arah duduk mengikuti perabot (Lead pod ke mejanya, bangku pod ke meja, rak dan konsol ruang server ke utara, perpustakaan ke rak buku, lounge ke penonton, kursi ruang rapat dihitung ke pusat meja lalu dibulatkan empat arah), arah berjalan mengikuti segmen jalur dan kembali ke arah kursinya setibanya, dan kunci cache badan memuat arah supaya penggambaran ulang hanya terjadi saat berputar. Monitor pod pindah ke sisi utara meja dengan layar menghadap robot; penanda warna Lead jadi lampu di permukaan meja sisi selatan monitor, sisi yang benar-benar terlihat penonton, tetap ber-id `led-pod-<n>`. Test: `tests/kantor-agent-browser.ps1` 38 check (baru: arah per area, kedua bangku pod, arah saat berjalan, badan yang benar-benar digambar ulang saat berputar, penanda warna pod; dilihat merah dulu, ditambah dua kontrol mutasi yang masing-masing menghasilkan dua merah), 0 galat JavaScript; `tests/test_kantor_agent.py` 110 dan `tests/test-init.ps1` tetap hijau karena penulis data tak disentuh. **Butuh re-init.**
- **1.21.0**: **Kantor Agent v2** (sesi `d77f4cfa`, 2026-09-15), menutup kekurangan 1.20.0 tanpa hook baru. Sumber "sesi mana terbuka dan apakah ia bekerja" kini **registri sesi Claude Code** `~/.claude/sessions/<pid>.json` (format internal, diamati di 2.1.269: `status` busy/idle/waiting, `waitingFor`, `procStart`, `startedAt`), dicocokkan dengan `claude agents --json` (terdokumentasi, terukur 2,3 sampai 9,5 detik) tiap 60 detik lewat subprocess yang tidak memblokir tick; dua ketidakcocokan berturut-turut memasang banner. Transkrip tetap menentukan tool dan area; registri yang tak terbaca menurunkan penulis ke mode transkrip 1.20.0 dengan banner. Yang berubah bagi pembaca: sesi yang lama diam tetap tampil (terukur di workspace nyata: 6 dari 6 sesi sama dengan `claude agents`, termasuk sesi yang diam 6 jam); **menunggu izin** dibaca dari `status: waiting` + `waitingFor` dan tampil sebagai gelembung **!** yang tak ikut label ringkas dan tak pernah diredupkan (S0: registri berubah di detik yang sama dengan dialognya, hook `Notification` baru 8 detik kemudian, jadi opsi hook tidak dibangun); **tugas shell latar** yang belum selesai tampil "menunggu tugas latar" di ruang server, termasuk perintah yang dipindah ke latar karena timeout, dan tidak lagi untuk tugas yang dihentikan TaskStop (yang tak pernah mendapat `<task-notification>`) atau milik proses Claude Code yang lama; kartu menyebut asal sesi (VS Code/terminal), nama, pid, dan tombol **salin id** (clipboard API, lalu `execCommand`, lalu kolom id terpilih untuk salin manual). PID yang didaur ulang ditolak lewat `procStart` = FILETIME `GetProcessTimes` (ctypes; `os.kill(pid, 0)` di Windows adalah TerminateProcess). **Penulis tunggal** kini dikunci (`msvcrt.locking`/`fcntl.flock`): penulis kedua keluar 4 dan launcher melapor "sudah jalan" (dibuktikan dua launcher serentak di workspace nyata). `VERSI_DATA` 2; halaman tidak menafsirkan data versi lain dan banner-nya menyebut `--berhenti`. Tick p50 8 ms, p95 15 ms atas 6 sesi dengan cek silang aktif (37 tick sesudah tick pertama); tick pertama membaca jendela tugas latar 8 MB tiap transkrip (p95 tujuh tick pertama 423 ms). Test: `tests/test_kantor_agent.py` 110 (dari 72; kontrol mutasi procStart, kunci, dan enam aturan pelacak tugas latar terbukti merah), `tests/test-init.ps1` 71 (dari 69: registri palsu, dua launcher serentak), `tests/kantor-agent-browser.ps1` 27 check (izin, tugas latar, asal, tiga jalur salin dengan clipboard yang dibaca balik lewat menempel karena `readText` ditolak di `file://`, banner registri dan versi, 0 galat JavaScript). Spike Pixel Agents 1.4.1 mode tanpa hook: sesi dibaca dari transkrip dan izin hanya ditebak dari timer, setara tanda `?` 1.20.0; `~/.claude/settings.json` global tidak berubah. Dialog izin tool sungguhan diukur 2026-09-16: registri `waiting` + `waitingFor: "permission prompt"`, gelembung "!" tampil 1,7 detik kemudian dan hilang 3,1 detik sesudah disetujui. Batas: sesi yang masih tertahan di layar awal dan sesi anak (mewarisi `CLAUDE_CODE_CHILD_SESSION` dari shell tool sesi lain) tak punya entri registri sehingga tak tampil; tugas latar dilacak bertahap per transkrip mulai dari 8 MB terakhir saat berkasnya pertama terlihat (jarak awal tugas ke notifikasinya terukur p50 78 KB, p90 480 KB, maks 6,6 MB atas 430 tugas 7 hari; ekor 64 KB 1.20.0 melewatkan 239 di antaranya), jadi tugas yang dimulai sebelum jendela itu tak terlihat; `.sh` belum pernah dijalankan. **Butuh re-init**; penulis 1.20.0 yang masih hidup dihentikan dulu dengan `/kantor-agent --berhenti`.
- **1.20.0**: **`/kantor-agent`**, denah kantor isometrik sesi Claude Code yang hidup (sesi `d77f4cfa`, 2026-09-15). Tiap sesi hidup tampil sebagai robot Lead, tiap subagent sebagai robot kecil sesuai `agentType`, dan robot berpindah ke area sesuai tool yang sedang dipakai (perpustakaan, meja, ruang server, ruang rapat, lounge). Menjawab yang tak bisa dijawab `/papan-sesi`: status `aktif` di `.task-plans/sesi` terukur tak bisa dipercaya (72 dari 99 berkas aktif, hanya 7 transkripnya ditulis dalam 10 menit) dan `tahap` hanya berubah lewat slash command (61 dari 72 di `mulai`), jadi sumbernya ekor transkrip. `hooks/kantor-agent.py` satu-satunya penulis data, hanya pustaka standar Python (`ConvertFrom-Json` PS 5.1 terukur tak selesai 180 detik untuk profil yang Python selesaikan dalam 0,045 detik); `hooks/kantor-agent.template.html` satu-satunya penulis UI (SVG isometrik tanpa library); `hooks/kantor-agent.ps1`/`.sh` launcher (venv vault, lalu `py -3`, lalu `python3`; penulis tunggal dijaga lewat PID + command line proses; `-Berhenti`). Berkas, bukan layanan (ADR 0077 §5): `<script>` yang disuntik ulang terbukti membaca isi terbaru di `file://` (spike CDP, 20 kali ganti nama atomik tanpa satu pun gagal); penulis mati tampil sebagai banner basi dan format transkrip yang berubah tampil sebagai banner, bukan kantor kosong. Tick p95 93 ms atas 7 sesi hidup sesudah prasaring `os.scandir` + cache per berkas (sebelumnya median 320 ms; `os.stat` atas 167 transkrip saja 77 ms). Dalam satu batch tool paralel, hasil tool singkat sering baru tertulis setelah tool lambatnya selesai (diukur atas 25 transkrip), jadi area ditentukan tool tertunda paling awal di luar perpustakaan/meja, bukan yang terakhir. Test: `tests/test_kantor_agent.py` (72 test atas templat baris transkrip nyata yang dibersihkan, gerbang kebocoran 0 hasil; kontrol mutasi end_turn, normcase, dan perluasan ekor terbukti merah), `tests/test-init.ps1` +9 assertion kantor-agent, dan `tests/kantor-agent-browser.ps1` (manual, CDP: delapan keadaan layar, robot benar-benar tiba, pod dan warna Lead stabil, 0 galat JavaScript). Batas yang disadari: prompt izin tidak tercatat di transkrip (ditandai `?`), satu mesin, `.sh` belum pernah dijalankan. Desain: `docs/2026-09-15-kantor-agent-design.md`. **Butuh re-init.**
- **1.19.0**: skill **`peta-alur`** dan **archify** di rekomendasi `/skills` (sesi `208df0c5`, 2026-09-14). Archify (`tt-a1i/archify`, MIT) membuat peta arsitektur/alur interaktif dari spec JSON. Sengaja **tidak** di-vendor ke `skills/`: upstream masih bergerak cepat (v2.17 dev), jadi dipasang per mesin lewat `/skills` dengan `pnpm dlx skills add …`, dan langkah 4 `commands/skills.md` kini menjalankan perintah per baris tabel, bukan hanya `claude plugin install`. `peta-alur` hanya memuat keputusan khusus tim, disusun dari tiga skenario baseline tanpa skill yang menghasilkan empat kegagalan nyata: peta diusulkan disimpan di vault padahal repo vault PUBLIC; workflow 5 lane direncanakan padahal 4 lane terukur meluap 326px di 1440×900; legend workflow dibiarkan "Agent logic"/"Tool action"; dan peta yang gagal `visual-check` tetap mau dikirim saat dikejar rapat. Ketiga skenario lolos setelah skill dibaca, dan resep legend-nya dibuktikan lewat `deliver` showcase 9/9. `rules/plan-checklist.md` §5: peta Archify boleh dilampirkan sebagai tambahan diagram ASCII, tidak menggantikannya. `rules/team-memory.md` menambah penunjuk skill. **Butuh re-init** untuk dapat skill-nya, lalu `/skills` untuk memasang archify.
- **1.18.0** — **`pre-commit-gate` tak lagi men-spawn `powershell.exe` untuk SETIAP panggilan Bash/PowerShell** (brief `pre-commit-gate-overhead`, sesi `1dbabb42`, 2026-09-12). Sejak 1.15.0 matcher `PreToolUse` `Bash|PowerShell` membuat `pre-commit-gate.ps1` di-spawn untuk perintah apa pun (`Get-Location`, `ls`, dll), padahal gerbangnya cuma bertindak untuk `git commit` (`pre-commit-gate.ps1:91` `if (-not $isCommit) { exit 0 }`, sesudah spawn proses, `ConvertFrom-Json`, dan tokenisasi). `init.ps1`/`init.sh` kini memasang **dua** entri `PreToolUse` (matcher `Bash` dan `PowerShell` terpisah, "|" tak bisa dipakai di scope `if`), masing-masing memakai field `if` (permission-rule syntax, `Bash(*commit*)` / `PowerShell(*commit*)`) supaya Claude Code menyaring ISI command dan tidak men-spawn proses sama sekali bila polanya tak cocok. Pola sengaja lebar (wildcard di kedua ujung) supaya tetap men-spawn untuk `-c`/`-C` di posisi mana pun, `Git.exe`, path lengkap lewat `&`, dan commit yang dirangkai `;`/baris baru. **Logika penolakan di `pre-commit-gate.ps1` sama sekali tidak disentuh** — keenam bentuk itu ternyata sudah DITOLAK exit 2 sebelum brief ini juga; yang berubah cuma kapan skrip itu dipanggil. `tests/test-init.ps1` (60 assertion `Check(...)`, naik dari 50 di `origin/main`): enam kontrol positif baru untuk bentuk yang logikanya sudah menolak tapi belum diuji eksplisit, dan assertion bentuk `settings.json` diperbarui untuk 2-entri + field `if`. Dibuktikan tidak vakum: mutasi `-match`→`-cmatch` pada regex token `git` (`pre-commit-gate.ps1:76`) membuat tepat satu kontrol baru ('Git.exe') merah, lalu dikembalikan (`git diff` kosong sesudahnya). Assertion bentuk konfigurasi `if` (dua entri, field `if`) dipatok PERSIS lewat `-ceq` (bukan `-match` longgar) supaya bisa merah bila pola dipersempit; dibuktikan tidak vakum dengan cara sama: mempersempit `PowerShell(*commit*)` jadi `PowerShell(git commit*)` membuat tepat SATU assertion baru merah (59 PASS/1 FAIL), lalu pola dikembalikan persis semula (`git diff` pada baris pola sama dengan sebelum mutasi). Baseline **tool PowerShell di sesi hidup** sebelum perubahan: median 10× `Get-Location` = 3.740 ms (n=10, min 2.357/maks 16.959 ms; 2026-09-12 05:38-05:39 UTC; beban mesin sesaat sesudahnya diukur orkestrator: claude=9, node=37, git=3, powershell=7 proses). Diukur dari transkrip MESIN INI (bukan diverifikasi sebagai agregat seluruh tim) 13 Agu-12 Sep 2026: 27,9 ribu panggilan PowerShell/bulan (sesi utama + subagent), p50 3,9 dtk untuk perintah ringan walau cuma satu transkrip aktif — proyeksi ~7,7 jam/bulan BILA pola pemakaian mesin ini representatif untuk tim.

**Sesudah re-init nyata** (kit 1.18.0 dari worktree fix ini, ke workspace nyata, belum di-push) **dan diukur ulang di sesi Claude Code hidup** 2026-09-12 15:18:33–15:19:02 UTC: median 10× `Get-Location` = 268,5 ms (n=10, min 257/maks 680 ms; beban mesin: claude=6, node=8, git=6, powershell=4 proses) — turun 92,8% dari baseline, di bawah ambang lolos brief (≤ 2.431 ms). Catatan jujur: beban mesin berbeda antara kedua pengukuran (rentang tidak tumpang-tindih: maks sesudah 680 ms < min sebelum 2.357 ms), jadi sebagian penurunan bisa ikut dipengaruhi beban, bukan murni konfigurasi.

**Kontrol langsung di sesi Claude Code hidup** lewat tool PowerShell (repo sandbox `%TEMP%`, branch `main`, nol riwayat): kedelapan bentuk — polos, `-c` sebelum `-C`, `Commit` huruf kapital, `Git.exe`, path lengkap git.exe lewat `&`, `pushd; git add .; git commit; popd`, `git status`/`git commit`/`popd` dipisah baris baru, dan `cd <repo>; git commit; cd <erp>` — semuanya DITOLAK (pesan "DITOLAK gerbang agent-kit … branch 'main'", tanpa baris `EXIT=`); sandbox tetap nol riwayat sesudahnya. Untuk `Commit` kapital: gerbang TERBUKTI BERJALAN (ditolak) — mekanismenya (pola `if` tak peka huruf, vs jatuh ke aturan "input tak terurai") tidak dibedakan pengujian ini. Kontrol negatif: commit di branch fitur lolos (`EXIT=0`, konteks hook "Reminder sebelum commit"), `commit --dry-run` di vault `main` lolos (konteks hook "Vault: stage per-nama berkas"). Tes pembeda config (pemantau spawn WMI, bukti PENDUKUNG karena melewatkan sebagian spawn saat panggilan paralel): perintah non-commit berhenti memicu spawn `pre-commit-gate.ps1` di config baru, beda dari config lama.

**Belum diverifikasi**: entri `Bash(*commit*)` lewat tool Bash (mati di mesin dev Windows ini), sisi `.sh` (`init.sh`/`pre-commit-gate.sh`, dicerminkan manual, tak pernah dijalankan), dan perilaku field `if` di versi Claude Code lama milik rekan tim. **Butuh re-init.**
- **1.17.0** — **Kirim peristiwa loop ke papan tim** (`dev-activity-board` PR #1 `feat/loop-ingest`, merged dan di-deploy 2026-09-07; worker hanya berubah lewat `pnpm run deploy`, bukan `pnpm deploy` telanjang, oleh pemegang akun Cloudflare). `hooks/loop-kirim.ps1`/`.sh`: opt-in per mesin lewat `~/.agent-kit/loop-ingest.json` (`url`, `secret`, `mesin`), HMAC-SHA256 `X-Loop-Signature-256`, timeout 3 detik, semua galat ditelan, jeda 10 menit setelah 3 kegagalan beruntun, dan **satu baris di konteks SessionStart** bila gagal beruntun supaya tidak senyap. Hook sesi mengirim `sesi.mulai`/`sesi.sentuh` (throttle: hanya saat tahap berubah atau >5 menit)/`sesi.selesai`; `/brief` mengirim `brief.dibuat`, `/kerjakan` mengirim `judge.verdict` dan `loop.pr`. `brief_id` = 12 hex SHA-256 slug, dihitung di satu tempat (`loop-kirim.ps1 -BriefSlug`). **Judul brief, teks task, judul PR tidak pernah dikirim** (ADR 0034 §4). Panggil `loop-kirim.ps1` in-process (`&`), bukan `powershell -File`: proses baru melucuti kutip di dalam JSON. `githooks/pre-push` kini juga mengenali skrip `typecheck`. test-init: 50 assertion (loop-kirim tanpa konfigurasi no-op, URL mati tetap exit 0 dan mencatat kegagalan, id brief stabil). **Butuh re-init + restart sesi.**
- **1.16.0** — **`/dashboard`**: HTML statis bergaya World Monitor (`hooks/dashboard.ps1` + `dashboard.py`/`.sh` sebagai pengumpul data, **`hooks/dashboard.template.html` satu-satunya penulis UI**). Panel SHIPPED (PR dibuka/merged per hari, merge rate, cycle median/p95), MIX (prefiks conventional judul PR merged, stacked bar horizontal, bukan donat), HOTSPOTS (scope judul), LOOP (brief → judge → PR dari data lokal), CYCLE, BASELINE, SESI; filter rentang 7/14/30 hari dan repo di halaman (data 30 hari ditanam sebagai JSON, tanpa server); tabel kembar per bagan, tooltip crosshair, legend. Panel tanpa sumber (biaya per PR, klasifikasi risiko) ditulis di kaki, bukan diisi nol. PR ditarik `gh` **per irisan mingguan** karena bip-erp menembus batas 500 dalam 30 hari; irisan yang penuh diperingatkan. Palet dark divalidasi `validate_palette.js` skill dataviz. `papan-sesi.ps1` tidak lagi menulis HTML sendiri (satu penulis UI); `/papan-sesi` menunjuk `dashboard.html`. Dua jebakan PS 5.1 baru tercatat di kode: `ConvertFrom-Json` atas array JSON memancarkan satu objek (bongkar dengan `ForEach-Object`), dan `2>&1` atas native command mencampur stderr ke data. **Butuh re-init.**
- **1.15.0** — **AI Engineering Loop.** Keputusan pemilik 2026-09-06 (ADR 0077 revisi) membangun loop otonom sesuai dokumen rujukan, dengan substitusi: orkestrasi native Claude Code (bukan Trigger.dev), tingkatan Claude (bukan DeepSeek/Kimi/GLM), **agent berhenti di PR**. Baru: `agents/` (7 agen `loop-*`: eksekutor fix/refactor/test/docs `sonnet`, `loop-judge` `opus` read-only, `loop-supervisor` + `loop-ekstrak-skill` `fable`); command `/brief`, `/kerjakan` (worktree pendek → eksekutor → judge → perbaikan maks 2× → PR), `/judge` (gerbang deterministik **dan** agen, lolos hanya bila keduanya), `/supervise` (draft, tidak auto-apply), `/ekstrak-skill` (transkrip diringkas skrip dulu), `/papan-sesi` (tabel + HTML statis). Hook: **`pre-commit-gate` menggantikan `pre-commit-reminder`** dan benar-benar MENOLAK (exit 2) commit di branch default repo kode, matcher `Bash|PowerShell` (versi lama bermatcher `Bash` saja sehingga tidak pernah menyala di mesin PowerShell); `SessionStart`/`UserPromptSubmit`/`SessionEnd` menulis papan sesi ke `.task-plans/sesi/`; `hooks/githooks/pre-push` (tsc/lint/build, go build per service tersentuh) dipasang `init` lewat `core.hooksPath` absolut ke tiap repo kode sibling. `baseline/<repo>.json` bertanggal supaya test bisa masuk gerbang (`baseline-test`). `init` menyalin `agents/`, memasang hooksPath (melewati repo yang sudah punya, mis. husky), membuat `.task-plans/{sesi,briefs,judge}`. `tests/test-init.ps1` kini memuat **kontrol positif** (gate exit 2 di `main`) dan **negatif** (exit 0 di branch fitur, di vault); kontrol positif itulah yang menangkap `-eq` vs `-ceq` (`-c core.fsmonitor=false` pernah menimpa path repo). Desain: `docs/2026-09-06-ai-engineering-loop-design.md`. **Butuh re-init + restart sesi.**
- **1.14.0** — command **`/analisa-kebutuhan`**, peran sistem analis yang berdiri **sebelum** `/start-task` dan sifatnya **opsional**. Menerima kebutuhan manajemen mentah, memperlakukannya sebagai **solusi yang diusulkan bukan kebutuhan**, menggali lewat maksimum 7 pertanyaan, grounding lewat `Explore` paralel ke vault + tiga repo dengan lima gerbang (aturan bisnis di `mybharata-app`, klaim negatif wajib `git grep`, ukur data prod, kolom sejajar vs himpunan bagian, status dok), lalu menulis ADR + dok domain + `Workspace/ANALISA - *.md`. Ia **boleh menyimpulkan "tidak perlu dibangun"**. Prosedur pencarian vault diangkat ke `rules/vault-retrieval.md` karena command ini pemakai ketiga; `/ask` dan `/start-task` kini merujuk ke sana, perilaku tidak berubah. Ikut memperbaiki `tests/test-init.ps1` yang **sudah merah sejak `index-vault.md` dan `skills.md` ditambahkan** (mematok 7 command padahal ada 9); jumlahnya kini diturunkan dari kit, bukan angka mati. **Butuh re-init.**
- **rules-only 2026-08-25** — **tanpa bump `VERSION`, cukup `git pull`** (sengaja: hook session-start membandingkan versi, jadi bump palsu akan menyuruh semua orang re-init untuk perubahan yang tak menyentuh `commands`/`hooks`/`skills`). **Prinsip kode ditulis eksplisit: SATU FAKTA SATU TEMPAT, bukan "bikin yang reusable".** Rumusan "reusable" sengaja **tidak** dipakai karena bertabrakan dengan aturan yang sudah dibayar mahal — `FilterTable` dirender puluhan halaman dan larangan menambah tipe baru ke sana demi satu modul sudah tertulis di `team-memory.md`; agent yang disuruh "bikin reusable" akan melakukan persis hal yang dilarang itu. Yang benar-benar menggigit di sini bukan kurang reuse melainkan **satu fakta hidup di dua tempat lalu menyimpang diam-diam** (ambang KPI 75 vs 80 di dua kartu bersebelahan, `weight` ditulis dari dua layar, kategori inbox tersalin ke biner tiap service) — tak satu pun tercegah oleh menulis kode yang reusable. Tiga tempat: prinsipnya di `rules/team-memory.md` (§ Prinsip kode, di-import tiap sesi, sengaja pendek), **`review-checklist.md` §G "Satu fakta hidup di dua tempat" naik ke Pass 1 KRITIS** (section lama G–L bergeser jadi H–M) dengan lawannya **§N "Abstraksi yang kelewat dini"** di Pass 2 supaya kedua arah kesalahan tak dicampur, dan `plan-checklist.md` §1 yang kini **menuntut menyebut pola pencarian yang benar-benar dijalankan beserta jumlah kandidat** — sebelumnya bagian itu prosa yang tak bisa dibuktikan meleset, dan bagian yang boleh diisi "tidak ada" tanpa bukti akan diisi "tidak ada".
- **1.10.0** — skill **`deploy-bip-erp`**. Sengaja **bukan** salinan runbook: prosedur, perintah, dan troubleshooting tetap satu-satunya di **[[RUN - Deploy Microservices bip-erp]]**, dan skill ini menyatakan runbook yang menang bila keduanya berbeda. Yang dikerjakannya tiga hal yang selama ini ada di kepala orang dan sering terlewat: (1) **menentukan daftar container** yang harus naik, termasuk kopling lewat biner yang tak terlihat dari API — kategori inbox menuntut `notification-service` ikut, perubahan `shared-library` menuntut semua pembacanya ikut, klaim JWT menuntut `api-gateway` ikut; (2) **urutannya** (penyedia API dulu, BE sebelum FE); (3) **gerbang verifikasi** yang menolak `docker ps` dan `/health` sebagai bukti dan menuntut biner dibuktikan memuat kode baru, fitur dipicu lewat gateway, notifikasi benar-benar tiba, dan angka nol diperlakukan sebagai pertanyaan. Ditambah aturan mencurigai biner basi sebelum menuduh logika, yang sudah memakan berjam-jam pelacakan ke arah keliru. **Butuh re-init.**
- **1.9.0** — **`/plan` dapat checklist, dan skill `audit-keamanan` lahir.** `rules/plan-checklist.md` (on-demand) menambah tiga bagian yang paling sering hilang dari rencana: **Apa yang Sudah Ada** (kode/komponen/master data/resolver yang sudah menyelesaikan sebagian masalah, dipakai ulang atau dibangun tandingannya dengan alasan), **Mode Kegagalan** (tiap jalur kode baru dijawab tiga pertanyaan — ada test? ada penanganan galat? senyap atau terlihat? — dan tidak-tidak-senyap ditandai celah kritis), serta matriks memilih jenis test (unit vs test handler lewat Fiber vs panggilan manual lewat gateway) dengan aturan regresi yang mutlak. Artefak `/plan` ikut bertambah dua bagian itu. Skill baru `audit-keamanan` diadaptasi dari `/cso` gstack: gerbang keyakinan 8/10 mode harian, verifikasi aktif lewat penelusuran kode (dilarang menyerang sistem hidup), sembilan fase yang disetel untuk Go/Fiber/MongoDB plus OWASP Top 10 dan STRIDE, dan **pengecualian keras** yang mendahului segalanya — plaintext credentials di dok IT vault DISENGAJA dan tidak boleh diflag. Laporannya wajib menyebut berapa kandidat yang dibuang, supaya auditnya bisa dipercaya. **Butuh re-init**; berkas checklist-nya sendiri menyebar cukup dengan `git pull`.
- **1.8.0** — **`/wrap` dapat gerbang kelengkapan, dan `/plan` mulai menulis artefak.** Prosedurnya di `rules/wrap-completion-gate.md` (on-demand): temukan artefak rencana, ekstrak itemnya, tentukan mode verifikasi (DIFF · LINTAS-REPO · KEADAAN-LUAR), klasifikasi SELESAI/SEBAGIAN/BELUM/BERUBAH/TAK TERVERIFIKASI, lalu gerbang yang **memblokir penutupan** saat ada item BELUM dan menuntut konfirmasi **per item** (bukan borongan) untuk yang tak terverifikasi. Tiga pemeriksaan khusus bip-erp berlaku selalu: fitur backend baru wajib sekali dipanggil lewat gateway (bukan cuma unit test), angka nol yang mencurigakan diperlakukan sebagai pertanyaan, dan konsekuensi deploy dua-container ikut tercatat. `/plan` kini menulis `.task-plans/<tanggal>-<slug>.md` setelah rencana disetujui, dengan bagian **Cara Verifikasi** dan **Di Luar Lingkup** yang wajib diisi. Tanpa artefak itu gerbangnya tak punya pembanding, dan `/wrap` akan bilang begitu terang-terangan alih-alih lolos diam-diam. Dipanen dari `ship/sections/plan-completion.md` gstack (MIT); mekanisme subagent, PR body, dan `/qa-only`-nya dibuang, mode lintas-repo diperkuat karena di sini menyentuh dua repo itu keadaan normal. **Butuh re-init**; berkas gerbangnya sendiri menyebar cukup dengan `git pull`.
- **1.7.0** — **`/review` naik kelas dari 5 baris jadi prosedur berjenjang.** Checklist barunya `rules/review-checklist.md` (dibaca on-demand, bukan di-import): dua pass (kritis/informasional), **gerbang verifikasi anti false-positive** (klaim "tidak ada" wajib dibuktikan Grep, klaim "consumer tak menangani" wajib dibuktikan dengan membaca berkasnya), heuristik fix-first, dan daftar yang JANGAN dilaporkan (creds vault disengaja, merah CI erp-frontend = startup failure, `pnpm test` main tak pernah hijau). Kategorinya dipanen dari checklist review [gstack](https://github.com/garrytan/gstack) (MIT) lalu diganti padanan Go/Fiber/MongoDB/Next.js, plus gotcha bip-erp yang sudah terbukti menggigit (rute akar vs prefix gateway, `c.JSON()` sebagai nilai galat, struct request tak ikut diperbarui, kategori inbox, `PATCH` menimpa penuh, i18n dua locale, `FilterTable` draft disemai sekali). **Butuh re-init** untuk `commands/review.md`; checklist-nya sendiri menyebar cukup dengan `git pull`.
- **1.6.0** — skill tim pertama: **`/migrasi-tabel-hris`** (folder `skills/` baru, disalin `init` → `.claude/skills/`). Prosedur memindahkan halaman daftar ke struktur tabel HRIS beserta jebakan yang sudah terbukti menggigit dan gerbang verifikasi. **Butuh re-init** untuk dapat skill-nya. `rules/team-memory.md` ikut diperbarui (jebakan tabel/filter + peringatan gerbang CI erp-frontend mati) — bagian itu menyebar cukup dengan `git pull`, tanpa re-init.
- **1.4.0** — command **`/skills`**: cek skill/plugin Claude Code rekomendasi tim (superpowers, code-review, dataviz, frontend-design, deep-research) vs terpasang; tawarkan install yang kurang (user konfirmasi, agent yang install). Butuh re-init untuk dapat command-nya.
- **1.3.0** — team-memory kini **di-import langsung dari vault** (`@../architecture-draft/.agent-kit/rules/team-memory.md`) alih-alih disalin ke `.claude/rules/`; `init` tak lagi menyalin `rules/`. Efek: update ingatan tim cukup `git pull architecture-draft` — **tanpa re-run init**. (Re-init sekali untuk adopsi mekanisme baru; `.claude/rules/` lama bisa dihapus, tak dipakai.)
- **1.2.1** — `rules/team-memory.md`: tambah konvensi `pnpm`, gotchas (vault creds intentional, repo mybharata rename, RBAC `system_roles`=modul & atasan di `work_data`), perjelas auto-push (FE/mybharata tak auto-push).
- **1.2.0** — tambah `rules/team-memory.md` (ingatan tim bersama: gotchas, konvensi, sumber-kebenaran); `init` menyalin `rules/` → `.claude/rules/`; `workspace-CLAUDE.md` meng-`@import` file itu supaya ter-load tiap sesi.
- **1.1.0** — tambah `/ask`: tanya-jawab read-only grounded ke vault + kode, sebut sumber & status, sarankan `/sync-docs` bila ada gap dok.
- **1.0.1** — session-start tak lagi salah lapor "ketinggalan dari remote" saat local justru _ahead_ (kini bandingkan ke merge-base); re-run init mem-_prune_ file `commands/`/`hooks/` yang sudah dihapus di kit baru (bukan cuma menimpa).
- **1.0.0** — rilis awal: 6 command flow arch-first, hooks, init lintas-OS.
