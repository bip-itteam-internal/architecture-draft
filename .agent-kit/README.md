# ERP Agent Kit

Gaya kerja koding ber-AI yang seragam untuk tim ERP Bharata. Landasannya arsitektur
draf (`architecture-draft`). Semua dev pakai Claude Code dengan flow & tooling yang sama.

## Onboarding (3 langkah)

1. Clone `architecture-draft` + project yang digarap sebagai **sibling** di dalam folder `erp/`:
   ```
   erp/
   ├── architecture-draft/   (vault ini)
   └── <project>/            (mis. bip-erp)
   ```
2. Buka folder `erp/` di Claude Code, jalankan init sekali:
   - Windows:  `powershell -ExecutionPolicy Bypass -File architecture-draft\.agent-kit\init.ps1`
   - mac/linux: `bash architecture-draft/.agent-kit/init.sh`
3. Mulai task: `/start-task <deskripsi>`.

## Flow wajib (per task)

`/start-task` → `/plan` → `/implement` → `/review` → `/sync-docs` → `/wrap`

## Update standar

`git -C architecture-draft pull` lalu jalankan ulang init. Session-start hook akan
mengingatkan bila versi kit terpasang ≠ versi terbaru.

## Isi kit

- `commands/` — 6 slash command flow + `/ask` (recall read-only, sebut sumber) + `/skills` (cek/install skill plugin rekomendasi tim) + `/index-vault` (bangun VAULT-INDEX.json) + `/analisa-kebutuhan` (kebutuhan manajemen mentah → ADR + dok domain + daftar task; berhenti sebelum kode) + **loop otonom** `/brief`, `/kerjakan`, `/judge`, `/supervise`, `/ekstrak-skill`, `/papan-sesi` (kit ≥ 1.15.0) + `/dashboard` (kit ≥ 1.16.0).
- `agents/` — agen kustom `loop-*` (disalin `init` → `.claude/agents/`): eksekutor `loop-fix`/`loop-refactor`/`loop-test`/`loop-docs`, penilai `loop-judge` (read-only), `loop-supervisor`, `loop-ekstrak-skill`.
- `hooks/` — `session-start` (info flow + cek versi/staleness + daftarkan sesi), `sesi-sentuh` (UserPromptSubmit), `sesi-selesai` (SessionEnd), `pre-commit-gate` (**menolak** commit di branch default repo kode, exit 2), skrip pendukung loop (`gerbang`, `baseline-test`, `worktree-baru`, `worktree-bersih`, `transkrip-ringkas`, `papan-sesi`; lib bersama `sesi-lib`, `gerbang-lib`), dan `githooks/pre-push` (dipasang lewat `core.hooksPath`).
- `baseline/` — `<repo>.json`: test yang memang merah di `origin/main`, bertanggal + ber-commit; dibandingkan `gerbang` supaya hanya kegagalan BARU yang menolak.
- `templates/brief.md` — Quick Brief Spec untuk `/brief`.
- `skills/` — skill tim (disalin `init` → `.claude/skills/`). Kini: `migrasi-tabel-hris`, `audit-keamanan`, `deploy-bip-erp`.
- `rules/` — `team-memory.md` (ingatan tim bersama; **di-import langsung** oleh CLAUDE.md dari vault via `@../architecture-draft/.agent-kit/rules/team-memory.md` — update cukup `git pull`, tak perlu re-init) plus berkas prosedur yang **tidak** di-import dan dibaca on-demand supaya tak membakar konteks tiap sesi: `plan-checklist.md` (`/plan`), `review-checklist.md` (`/review`), `wrap-completion-gate.md` (`/wrap`), `vault-retrieval.md` (pencarian vault, dipakai `/ask`, `/start-task`, `/analisa-kebutuhan`), dan `ui-checklist.md` (mutu layar dan urutan reuse komponen; dirujuk team-memory, `plan-checklist.md` §1, dan `review-checklist.md` §J).
- `templates/` — `workspace-CLAUDE.md` (jadi `erp/CLAUDE.md`).
- `init.ps1` / `init.sh` — pemasang.
- `VERSION` — versi kit.
- `docs/` — design & plan.

## Changelog

- **1.18.0** — **`pre-commit-gate` tak lagi men-spawn `powershell.exe` untuk SETIAP panggilan Bash/PowerShell** (brief `pre-commit-gate-overhead`, sesi `1dbabb42`, 2026-09-12). Sejak 1.15.0 matcher `PreToolUse` `Bash|PowerShell` membuat `pre-commit-gate.ps1` di-spawn untuk perintah apa pun (`Get-Location`, `ls`, dll), padahal gerbangnya cuma bertindak untuk `git commit` (`pre-commit-gate.ps1:84` `if (-not $isCommit) { exit 0 }`, sesudah spawn proses, `ConvertFrom-Json`, dan tokenisasi). Diukur dari transkrip 13 Agu-12 Sep 2026: 27,9 ribu panggilan PowerShell/bulan (sesi utama + subagent), p50 3,9 dtk untuk perintah ringan walau cuma satu transkrip aktif — tiap 1 dtk overhead ≈ 7,7 jam/bulan gabungan tim. `init.ps1`/`init.sh` kini memasang **dua** entri `PreToolUse` (matcher `Bash` dan `PowerShell` terpisah, "|" tak bisa dipakai di scope `if`), masing-masing memakai field `if` (permission-rule syntax, `Bash(*commit*)` / `PowerShell(*commit*)`) supaya Claude Code menyaring ISI command dan tidak men-spawn proses sama sekali bila polanya tak cocok. Pola sengaja lebar (wildcard di kedua ujung) supaya tetap men-spawn untuk `-c`/`-C` di posisi mana pun, `Git.exe`, path lengkap lewat `&`, dan commit yang dirangkai `;`/baris baru. **Logika penolakan di `pre-commit-gate.ps1` sama sekali tidak disentuh** — keenam bentuk itu ternyata sudah DITOLAK exit 2 sebelum brief ini juga; yang berubah cuma kapan skrip itu dipanggil. `tests/test-init.ps1` (60 assertion `Check(...)`, naik dari 50 di `origin/main`): enam kontrol positif baru untuk bentuk yang logikanya sudah menolak tapi belum diuji eksplisit, dan assertion bentuk `settings.json` diperbarui untuk 2-entri + field `if`. Dibuktikan tidak vakum: mutasi `-match`→`-cmatch` pada regex token `git` (`pre-commit-gate.ps1:69`) membuat tepat satu kontrol baru ('Git.exe') merah, lalu dikembalikan (`git diff` kosong sesudahnya). Assertion bentuk konfigurasi `if` (dua entri, field `if`) dipatok PERSIS lewat `-ceq` (bukan `-match` longgar) supaya bisa merah bila pola dipersempit; dibuktikan tidak vakum dengan cara sama: mempersempit `PowerShell(*commit*)` jadi `PowerShell(git commit*)` membuat tepat SATU assertion baru merah (59 PASS/1 FAIL), lalu pola dikembalikan persis semula (`git diff` pada baris pola sama dengan sebelum mutasi). Baseline **tool PowerShell di sesi hidup** sebelum perubahan: median 10× `Get-Location` = 3.740 ms (n=10, min 2.357/maks 16.959 ms; 2026-09-12 05:38-05:39 UTC; beban mesin sesaat sesudahnya diukur orkestrator: claude=9, node=37, git=3, powershell=7 proses). Diukur dari transkrip MESIN INI (bukan diverifikasi sebagai agregat seluruh tim) 13 Agu-12 Sep 2026: 27,9 ribu panggilan PowerShell/bulan (sesi utama + subagent), p50 3,9 dtk untuk perintah ringan walau cuma satu transkrip aktif — proyeksi ~7,7 jam/bulan BILA pola pemakaian mesin ini representatif untuk tim.

**Sesudah re-init nyata** (kit 1.18.0 dari worktree fix ini, ke workspace nyata, belum di-push) **dan diukur ulang di sesi Claude Code hidup** 2026-09-12 15:18:33–15:19:02 UTC: median 10× `Get-Location` = 268,5 ms (n=10, min 257/maks 680 ms; beban mesin: claude=6, node=8, git=6, powershell=4 proses) — turun 92,8% dari baseline, di bawah ambang lolos brief (≤ 2.431 ms). Catatan jujur: beban mesin berbeda antara kedua pengukuran (rentang tidak tumpang-tindih: maks sesudah 680 ms < min sebelum 2.357 ms), jadi sebagian penurunan bisa ikut dipengaruhi beban, bukan murni konfigurasi.

**Kontrol langsung di sesi Claude Code hidup** lewat tool PowerShell (repo sandbox `%TEMP%`, branch `main`, nol riwayat): kedelapan bentuk — polos, `-c` sebelum `-C`, `Commit` huruf kapital, `Git.exe`, path lengkap git.exe lewat `&`, `pushd; git add .; git commit; popd`, `git status`/`git commit`/`popd` dipisah baris baru, dan `cd <repo>; git commit; cd <erp>` — semuanya DITOLAK (pesan "DITOLAK gerbang agent-kit … branch 'main'", tanpa baris `EXIT=`); sandbox tetap nol riwayat sesudahnya. Untuk `Commit` kapital: gerbang TERBUKTI BERJALAN (ditolak) — mekanismenya (pola `if` tak peka huruf, vs jatuh ke aturan "input tak terurai") tidak dibedakan pengujian ini. Kontrol negatif: commit di branch fitur lolos (`EXIT=0`, konteks hook "Reminder sebelum commit"), `commit --dry-run` di vault `main` lolos (konteks hook "Vault: stage per-nama berkas"). Tes pembeda config (pemantau spawn WMI, bukti PENDUKUNG karena melewatkan sebagian spawn saat panggilan paralel): perintah non-commit berhenti memicu spawn `pre-commit-gate.ps1` di config baru, beda dari config lama.

**Belum diverifikasi**: entri `Bash(*commit*)` lewat tool Bash (mati di mesin dev Windows ini), sisi `.sh` (`init.sh`/`pre-commit-gate.sh`, dicerminkan manual, tak pernah dijalankan), dan perilaku field `if` di versi Claude Code lama milik rekan tim. **Butuh re-init.**
- **1.17.0** — **Kirim peristiwa loop ke papan tim** (`dev-activity-board` PR #1 `feat/loop-ingest`, merged dan di-deploy 2026-09-07; worker hanya berubah lewat `pnpm run deploy`, bukan `pnpm deploy` telanjang, oleh pemegang akun Cloudflare). `hooks/loop-kirim.ps1`/`.sh`: opt-in per mesin lewat `~/.agent-kit/loop-ingest.json` (`url`, `secret`, `mesin`), HMAC-SHA256 `X-Loop-Signature-256`, timeout 3 detik, semua galat ditelan, jeda 10 menit setelah 3 kegagalan beruntun, dan **satu baris di konteks SessionStart** bila gagal beruntun supaya tidak senyap. Hook sesi mengirim `sesi.mulai`/`sesi.sentuh` (throttle: hanya saat tahap berubah atau >5 menit)/`sesi.selesai`; `/brief` mengirim `brief.dibuat`, `/kerjakan` mengirim `judge.verdict` dan `loop.pr`. `brief_id` = 12 hex SHA-256 slug, dihitung di satu tempat (`loop-kirim.ps1 -BriefSlug`). **Judul brief, teks task, judul PR tidak pernah dikirim** (ADR 0034 §4). Panggil `loop-kirim.ps1` in-process (`&`), bukan `powershell -File`: proses baru melucuti kutip di dalam JSON. `githooks/pre-push` kini juga mengenali skrip `typecheck`. test-init: 50 assertion (loop-kirim tanpa konfigurasi no-op, URL mati tetap exit 0 dan mencatat kegagalan, id brief stabil). **Butuh re-init + restart sesi.**
- **1.16.0** — **`/dashboard`**: HTML statis bergaya World Monitor (`hooks/dashboard.ps1` + `dashboard.py`/`.sh` sebagai pengumpul data, **`hooks/dashboard.template.html` satu-satunya penulis UI**). Panel SHIPPED (PR dibuka/merged per hari, merge rate, cycle median/p95), MIX (prefiks conventional judul PR merged, stacked bar horizontal, bukan donat), HOTSPOTS (scope judul), LOOP (brief → judge → PR dari data lokal), CYCLE, BASELINE, SESI; filter rentang 7/14/30 hari dan repo di halaman (data 30 hari ditanam sebagai JSON, tanpa server); tabel kembar per bagan, tooltip crosshair, legend. Panel tanpa sumber (biaya per PR, klasifikasi risiko) ditulis di kaki, bukan diisi nol. PR ditarik `gh` **per irisan mingguan** karena bip-erp menembus batas 500 dalam 30 hari; irisan yang penuh diperingatkan. Palet dark divalidasi `validate_palette.js` skill dataviz. `papan-sesi.ps1` tidak lagi menulis HTML sendiri (satu penulis UI); `/papan-sesi` menunjuk `dashboard.html`. Dua jebakan PS 5.1 baru tercatat di kode: `ConvertFrom-Json` atas array JSON memancarkan satu objek (bongkar dengan `ForEach-Object`), dan `2>&1` atas native command mencampur stderr ke data. **Butuh re-init.**
- **1.15.0** — **AI Engineering Loop.** Keputusan pemilik 2026-09-06 (ADR 0077 revisi) membangun loop otonom sesuai dokumen rujukan, dengan substitusi: orkestrasi native Claude Code (bukan Trigger.dev), tingkatan Claude (bukan DeepSeek/Kimi/GLM), **agent berhenti di PR**. Baru: `agents/` (7 agen `loop-*`: eksekutor fix/refactor/test/docs `sonnet`, `loop-judge` `opus` read-only, `loop-supervisor` + `loop-ekstrak-skill` `fable`); command `/brief`, `/kerjakan` (worktree pendek → eksekutor → judge → perbaikan maks 2× → PR), `/judge` (gerbang deterministik **dan** agen, lolos hanya bila keduanya), `/supervise` (draft, tidak auto-apply), `/ekstrak-skill` (transkrip diringkas skrip dulu), `/papan-sesi` (tabel + HTML statis). Hook: **`pre-commit-gate` menggantikan `pre-commit-reminder`** dan benar-benar MENOLAK (exit 2) commit di branch default repo kode, matcher `Bash|PowerShell` (versi lama bermatcher `Bash` saja sehingga tidak pernah menyala di mesin PowerShell); `SessionStart`/`UserPromptSubmit`/`SessionEnd` menulis papan sesi ke `.task-plans/sesi/`; `hooks/githooks/pre-push` (tsc/lint/build, go build per service tersentuh) dipasang `init` lewat `core.hooksPath` absolut ke tiap repo kode sibling. `baseline/<repo>.json` bertanggal supaya test bisa masuk gerbang (`baseline-test`). `init` menyalin `agents/`, memasang hooksPath (melewati repo yang sudah punya, mis. husky), membuat `.task-plans/{sesi,briefs,judge}`. `tests/test-init.ps1` kini memuat **kontrol positif** (gate exit 2 di `main`) dan **negatif** (exit 0 di branch fitur, di vault); kontrol positif itulah yang menangkap `-eq` vs `-ceq` (`-c core.fsmonitor=false` pernah menimpa path repo). Desain: `docs/2026-09-06-ai-engineering-loop-design.md`. **Butuh re-init + restart sesi.**
- **1.14.0** — command **`/analisa-kebutuhan`**, peran sistem analis yang berdiri **sebelum** `/start-task` dan sifatnya **opsional**. Menerima kebutuhan manajemen mentah, memperlakukannya sebagai **solusi yang diusulkan bukan kebutuhan**, menggali lewat maksimum 7 pertanyaan, grounding lewat `Explore` paralel ke vault + tiga repo dengan lima gerbang (aturan bisnis di `mybharata-app`, klaim negatif wajib `git grep`, ukur data prod, kolom sejajar vs himpunan bagian, status dok), lalu menulis ADR + dok domain + `Workspace/ANALISA - *.md`. Ia **boleh menyimpulkan "tidak perlu dibangun"**. Prosedur pencarian vault diangkat ke `rules/vault-retrieval.md` karena command ini pemakai ketiga; `/ask` dan `/start-task` kini merujuk ke sana, perilaku tidak berubah. Ikut memperbaiki `tests/test-init.ps1` yang **sudah merah sejak `index-vault.md` dan `skills.md` ditambahkan** (mematok 7 command padahal ada 9); jumlahnya kini diturunkan dari kit, bukan angka mati. **Butuh re-init.**
- **rules-only 2026-08-25** — **tanpa bump `VERSION`, cukup `git pull`** (sengaja: hook session-start membandingkan versi, jadi bump palsu akan menyuruh semua orang re-init untuk perubahan yang tak menyentuh `commands`/`hooks`/`skills`). **Prinsip kode ditulis eksplisit: SATU FAKTA SATU TEMPAT, bukan "bikin yang reusable".** Rumusan "reusable" sengaja **tidak** dipakai karena bertabrakan dengan aturan yang sudah dibayar mahal — `FilterTable` dirender puluhan halaman dan larangan menambah tipe baru ke sana demi satu modul sudah tertulis di `team-memory.md`; agent yang disuruh "bikin reusable" akan melakukan persis hal yang dilarang itu. Yang benar-benar menggigit di sini bukan kurang reuse melainkan **satu fakta hidup di dua tempat lalu menyimpang diam-diam** (ambang KPI 75 vs 80 di dua kartu bersebelahan, `weight` ditulis dari dua layar, kategori inbox tersalin ke biner tiap service) — tak satu pun tercegah oleh menulis kode yang reusable. Tiga tempat: prinsipnya di `rules/team-memory.md` (§ Prinsip kode, di-import tiap sesi, sengaja pendek), **`review-checklist.md` §G "Satu fakta hidup di dua tempat" naik ke Pass 1 KRITIS** (section lama G–L bergeser jadi H–M) dengan lawannya **§N "Abstraksi yang kelewat dini"** di Pass 2 supaya kedua arah kesalahan tak dicampur, dan `plan-checklist.md` §1 yang kini **menuntut menyebut pola pencarian yang benar-benar dijalankan beserta jumlah kandidat** — sebelumnya bagian itu prosa yang tak bisa dibuktikan meleset, dan bagian yang boleh diisi "tidak ada" tanpa bukti akan diisi "tidak ada".
- **1.10.0** — skill **`deploy-bip-erp`**. Sengaja **bukan** salinan runbook: prosedur, perintah, dan troubleshooting tetap satu-satunya di **[[RUN - Deploy Microservices bip-erp]]**, dan skill ini menyatakan runbook yang menang bila keduanya berbeda. Yang dikerjakannya tiga hal yang selama ini ada di kepala orang dan sering terlewat: (1) **menentukan daftar container** yang harus naik, termasuk kopling lewat biner yang tak terlihat dari API — kategori inbox menuntut `notification-service` ikut, perubahan `shared-library` menuntut semua pembacanya ikut, klaim JWT menuntut `api-gateway` ikut; (2) **urutannya** (penyedia API dulu, BE sebelum FE); (3) **gerbang verifikasi** yang menolak `docker ps` dan `/health` sebagai bukti dan menuntut biner dibuktikan memuat kode baru, fitur dipicu lewat gateway, notifikasi benar-benar tiba, dan angka nol diperlakukan sebagai pertanyaan. Ditambah aturan mencurigai biner basi sebelum menuduh logika, yang sudah memakan berjam-jam pelacakan ke arah keliru. **Butuh re-init.**
- **1.9.0** — **`/plan` dapat checklist, dan skill `audit-keamanan` lahir.** `rules/plan-checklist.md` (on-demand) menambah tiga bagian yang paling sering hilang dari rencana: **Apa yang Sudah Ada** (kode/komponen/master data/resolver yang sudah menyelesaikan sebagian masalah, dipakai ulang atau dibangun tandingannya dengan alasan), **Mode Kegagalan** (tiap jalur kode baru dijawab tiga pertanyaan — ada test? ada penanganan galat? senyap atau terlihat? — dan tidak-tidak-senyap ditandai celah kritis), serta matriks memilih jenis test (unit vs test handler lewat Fiber vs panggilan manual lewat gateway) dengan aturan regresi yang mutlak. Artefak `/plan` ikut bertambah dua bagian itu. Skill baru `audit-keamanan` diadaptasi dari `/cso` gstack: gerbang keyakinan 8/10 mode harian, verifikasi aktif lewat penelusuran kode (dilarang menyerang sistem hidup), sembilan fase yang disetel untuk Go/Fiber/MongoDB plus OWASP Top 10 dan STRIDE, dan **pengecualian keras** yang mendahului segalanya — plaintext credentials di dok IT vault DISENGAJA dan tidak boleh diflag. Laporannya wajib menyebut berapa kandidat yang dibuang, supaya auditnya bisa dipercaya. **Butuh re-init**; berkas checklist-nya sendiri menyebar cukup dengan `git pull`.
- **1.8.0** — **`/wrap` dapat gerbang kelengkapan, dan `/plan` mulai menulis artefak.** Prosedurnya di `rules/wrap-completion-gate.md` (on-demand): temukan artefak rencana, ekstrak itemnya, tentukan mode verifikasi (DIFF · LINTAS-REPO · KEADAAN-LUAR), klasifikasi SELESAI/SEBAGIAN/BELUM/BERUBAH/TAK TERVERIFIKASI, lalu gerbang yang **memblokir penutupan** saat ada item BELUM dan menuntut konfirmasi **per item** (bukan borongan) untuk yang tak terverifikasi. Tiga pemeriksaan khusus bip-erp berlaku selalu: fitur backend baru wajib sekali dipanggil lewat gateway (bukan cuma unit test), angka nol yang mencurigakan diperlakukan sebagai pertanyaan, dan konsekuensi deploy dua-container ikut tercatat. `/plan` kini menulis `.task-plans/<tanggal>-<slug>.md` setelah rencana disetujui, dengan bagian **Cara Verifikasi** dan **Di Luar Lingkup** yang wajib diisi. Tanpa artefak itu gerbangnya tak punya pembanding, dan `/wrap` akan bilang begitu terang-terangan alih-alih lolos diam-diam. Dipanen dari `ship/sections/plan-completion.md` gstack (MIT); mekanisme subagent, PR body, dan `/qa-only`-nya dibuang, mode lintas-repo diperkuat karena di sini menyentuh dua repo itu keadaan normal. **Butuh re-init**; berkas gerbangnya sendiri menyebar cukup dengan `git pull`.
- **1.7.0** — **`/review` naik kelas dari 5 baris jadi prosedur berjenjang.** Checklist barunya `rules/review-checklist.md` (dibaca on-demand, bukan di-import): dua pass (kritis/informasional), **gerbang verifikasi anti false-positive** (klaim "tidak ada" wajib dibuktikan Grep, klaim "consumer tak menangani" wajib dibuktikan dengan membaca berkasnya), heuristik fix-first, dan daftar yang JANGAN dilaporkan (creds vault disengaja, merah CI erp-frontend = startup failure, `pnpm test` main tak pernah hijau). Kategorinya dipanen dari checklist review [gstack](https://github.com/garrytan/gstack) (MIT) lalu diganti padanan Go/Fiber/MongoDB/Next.js, plus gotcha bip-erp yang sudah terbukti menggigit (rute akar vs prefix gateway, `c.JSON()` sebagai nilai galat, struct request tak ikut diperbarui, kategori inbox, `PATCH` menimpa penuh, i18n dua locale, `FilterTable` draft disemai sekali). **Butuh re-init** untuk `commands/review.md`; checklist-nya sendiri menyebar cukup dengan `git pull`.
- **1.6.0** — skill tim pertama: **`/migrasi-tabel-hris`** (folder `skills/` baru, disalin `init` → `.claude/skills/`). Prosedur memindahkan halaman daftar ke struktur tabel HRIS beserta jebakan yang sudah terbukti menggigit dan gerbang verifikasi. **Butuh re-init** untuk dapat skill-nya. `rules/team-memory.md` ikut diperbarui (jebakan tabel/filter + peringatan gerbang CI erp-frontend mati) — bagian itu menyebar cukup dengan `git pull`, tanpa re-init.
- **1.4.0** — command **`/skills`**: cek skill/plugin Claude Code rekomendasi tim (superpowers, code-review, dataviz, frontend-design, deep-research) vs terpasang; tawarkan install yang kurang (user konfirmasi, agent yang install). Butuh re-init untuk dapat command-nya.
- **1.3.0** — team-memory kini **di-import langsung dari vault** (`@../architecture-draft/.agent-kit/rules/team-memory.md`) alih-alih disalin ke `.claude/rules/`; `init` tak lagi menyalin `rules/`. Efek: update ingatan tim cukup `git pull architecture-draft` — **tanpa re-run init**. (Re-init sekali untuk adopsi mekanisme baru; `.claude/rules/` lama bisa dihapus, tak dipakai.)
- **1.2.1** — `rules/team-memory.md`: tambah konvensi `pnpm`, gotchas (vault creds intentional, repo mybharata rename, RBAC `system_roles`=modul & atasan di `work_data`), perjelas auto-push (FE/mybharata tak auto-push).
- **1.2.0** — tambah `rules/team-memory.md` (ingatan tim bersama: gotchas, konvensi, sumber-kebenaran); `init` menyalin `rules/` → `.claude/rules/`; `workspace-CLAUDE.md` meng-`@import` file itu supaya ter-load tiap sesi.
- **1.1.0** — tambah `/ask`: tanya-jawab read-only grounded ke vault + kode, sebut sumber & status, sarankan `/sync-docs` bila ada gap dok.
- **1.0.1** — session-start tak lagi salah lapor "ketinggalan dari remote" saat local justru _ahead_ (kini bandingkan ke merge-base); re-run init mem-_prune_ file `commands/`/`hooks/` yang sudah dihapus di kit baru (bukan cuma menimpa).
- **1.0.0** — rilis awal: 6 command flow arch-first, hooks, init lintas-OS.
