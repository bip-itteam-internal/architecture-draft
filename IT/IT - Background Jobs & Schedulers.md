## Deskripsi

*Inventaris **proses yang berjalan diam-diam** di bip-erp — cron/scheduler/worker latar belakang: apa yang jalan, kapan, di service mana, untuk apa, dan lock-nya. Grounded dari kode (`bson`/cron string disalin verbatim). Tujuannya: tak ada "sistem tersembunyi" yang tak diketahui saat debugging, perencanaan kapasitas, atau mengubah service.*

- **Status**: ✅ Implemented — 47 job terjadwal + 1 dispatcher webhook (+1 pengingat kontrak habis employee 07:00 WIB, merged 2026-09-11, **LIVE** DEV 2026-09-11 dan PROD 2026-09-12) (+1 job background PERTAMA manufacture 2026-08-20, sweep konfirmasi retur macet, **DORMANT** — lihat tabel) (+1 penjadwal internal marketing-analytics 2026-08-01, **LIVE** — service itu sebelumnya tanpa scheduler sama sekali; per audit kode 2026-08-01: +19 job integration yang sebelumnya belum tercatat — rumpun Accurate (stock/aset/opex refresh, sweep faktur/retur/receipt, receipt-sync, sample-shipping-expense, anomali retur), rekonsiliasi (statement TikTok, income Lazada, status order, retur TikTok), resi (enrich cancel/reverse, safety-net WMS), wallet-sync, seed-mapping, guardian stale-toship; +4 job integration ditambahkan 2026-07-11: affiliate Shopee AMS performance/conversion & WMS resi bridge `sync-resi-wms`/`sync-shopee-tracking`; +1 guardian order Shopee `sync-shopee-orders` 2026-07-14, **belum deploy**; +1 guardian retur Shopee `sync-shopee-returns` 2026-07-15 & +1 guardian retur TikTok `recover-tiktok-returns` 2026-07-16 — memicu booking Retur Penjualan yang selama ini tak pernah menyala, **belum deploy**; +1 sync ulasan marketplace `sync-reviews` 2026-07-19 harian 06:45, **LIVE**; +3 job Packet Tracking `sync-tracking`/`detect-stuck`/`sync-return-tracking` 2026-07-17..18 — checkpoint pengiriman forward+retur & deteksi mandek, **LIVE**). *3 job reminder attendance (koreksi/leave/tukar jadwal) + auto-ignore koreksi & tukar jadwal + pre-alokasi sadar-swap dari **PR #165** — belum rilis ke prod.* Audit kode 2026-09-12 (snapshot origin/main): **+15 job integration** yang belum punya baris tabel (`accurate-daily-invoice-gap-sweep`, `accurate-perlengkapan-cost-refresh`, `accurate-ppn-masukan-refresh`, `accurate-realisasi-nonops-refresh`, `piutang-snapshot-bulanan`, `sync-lazada-withdrawal-journal`, `reconciliation-alert`, `ringkasan-notif`, `lazada-credential-task`, `meta-credential-task`, `meta-commerce-order-poll`, `accurate-external-edit-sweep`, `accurate-snapshot-baseline-seed`, `accurate-serap-jurnal-kas`, `accurate-webhook-renew`), **+3 goroutine finance-service** (terbit masa pajak, pengingat pajak H-7/H-3, buka periode audit kertas kerja), dan **+3 goroutine procurement-service** (pengingat jatuh tempo faktur, antrean sync Accurate, impor pembelian terjadwal). Baris finance dan procurement di tabel sengaja dipersempit ke mekanisme dan file:line; rasionalnya tinggal di [[API - Finance Service]], [[Finance - Rancangan Finance Service]], [[Finance - Audit Internal]], dan [[Microservices - Procurement Service]]. ⛔ **Koreksi 2026-09-12**: `accurate-receipt-sweep` tidak pernah didaftarkan di `main.go` (`_ = tasks.NewReceiptSweepTask`); `receipt-sync` kini 5×/hari; `income-reconciler` TikTok hanya berjalan 06:30 sampai 23:30 WIB; cron harian insentive sudah dicabut 2026-07-30 bersama `cron_worker.go`.
- **Zona waktu**: semua **Asia/Jakarta (WIB)** kecuali override env `INTEGRATION_WORKER_TZ`
- **Sintaks cron**: integration pakai `robfig/cron` **6-field** (`detik menit jam tgl bln dow`); attendance/employee/notification pakai **5-field** klasik

## Job Terjadwal (time-scheduled)

| Service | Jadwal (WIB) | Untuk apa | Lock | File:line |
|---|---|---|---|---|
| employee | harian 04:00 (`0 4 * * *`) | sync company work schedule | — | `services/employee/cron.go:29` |
| employee | tgl 1 / bulan 08:45 (`45 8 1 * *`) | broadcast reminder KPI (FCM + inbox) | — | `services/employee/cron.go:34` |
| employee | harian 02:00 (`0 2 * * *`) | **finalisasi skor KPI otomatis-penuh** ([[ADR - 0048 Skor KPI Otomatis Penuh Dibekukan Sistem]]): bekukan `kpi_score` posisi yang SELURUH metriknya ber-`auto` & menghasilkan angka; jabatan per-periode. **Tanggal beku per-department** (2026-08-25): default **tgl 1**, **Finance tgl 5** (`bolehBekuDept`, relatif periode). Idempoten (`$setOnInsert`), dijalankan harian sebulan penuh supaya container mati tak melewatkan sebulan; setelah `cronTerapkanMutasi` 00:15 | — | `services/employee/cron.go:52` (`kpi_finalisasi.go`) |
| employee | 1 Jan 00:05 (`5 0 1 1 *`) | reset kuota cuti tahunan (used→0). ⚠️ **Bukan pemberi jatah** — filternya `vacation.quota > 0`, jadi karyawan berkuota 0 dilewati selamanya, dan sisa cuti hangus setahun lebih cepat dari Pasal 15. Akan **dicabut** dan diganti job **harian** idempoten oleh [[ADR - 0061 Jatah Cuti Tahunan Terbit Otomatis di Ulang Tahun Kontrak]]; job tahunan yang jatuh saat container mati melewatkan satu tahun penuh | — | `services/employee/cron.go:37` |
| employee | harian 07:00 (`0 7 * * *`) | **pengingat kontrak habis** ([[ADR - 0089 Tanda Tangan Kontrak Kerja di Sistem Sendiri, Didampingi HRD, e-Meterai Dibubuhkan HR]] §2): satu ringkasan inbox `reminder` per supervisor HR per perusahaan (masuk "segera berakhir", H-30, H-7, kontrak kedaluwarsa pada karyawan aktif per minggu ISO) + pesan H-14 ke atasan langsung. Idempoten lewat `employee_contract_pengingat` (unik `contract_id + tahap + penerima_id`), yang dicatat hanya kiriman berhasil, jadi yang gagal dicoba lagi besok; `defer recover`; galat `AddFunc` di-log. Env `PENGINGAT_KONTRAK_SAAT_BOOT=true` menjalankan sekali saat boot (DEV saja). Merged 2026-09-11 (bip-erp PR #1851); **LIVE** DEV 2026-09-11 dan PROD 2026-09-12 (jalan PROD pertama 2026-09-13 07:00 WIB belum dibaca) | — | `services/employee/cron.go` (`contract_pengingat.go`) |
| attendance | tiap 30 mnt (`*/30 * * * *`) | pre-alokasi entry absensi ~2j sebelum shift; pending→alpha saat shift mulai; perhitungkan **cuti & tukar jadwal** disetujui (swap kedua sisi via `WorkTimeFor`) | — | `services/attendance/cron.go:28` |
| attendance | tiap jam (`0 * * * *`) | auto-ignore **leave + koreksi presensi + tukar jadwal** basi (>24j) + FCM — termasuk tahap consent rekan | — | `services/attendance/cron.go:38` |
| attendance | tiap jam (`0 * * * *`) | **reminder reviewer koreksi** basi (T+18j, ~6j sblm auto-ignore) → SPV/HR FCM | — | `services/attendance/cron.go:41` |
| attendance | tiap jam (`0 * * * *`) | **reminder reviewer leave** basi (T+18j) → SPV/HR FCM | — | `services/attendance/cron.go:42` |
| attendance | tiap jam (`0 * * * *`) | **reminder tukar jadwal** basi (T+18j) → rekan/atasan/HRD FCM | — | `services/attendance/cron.go:43` |
| insentive | ~~harian 00:00 (akhir bulan: 15:00 Sn–Jm / 12:00 Sb)~~ | ⛔ **DICABUT 2026-07-30** (koreksi 2026-09-12): cron harian hitung insentif skema KPI-multiplier dihapus bersama `cron_worker.go` (dicabut SK 010/DIR/Rev-SK6/VII/2026). Yang tersisa di insentive-service hanya **pre-warm cache dashboard profit** saat boot lalu tiap 10 menit (`mulaiPrewarmProfitDashboard`), bukan cron | - | `services/insentive/main.go:712-713,2300-2301`; pre-warm `services/insentive/func.go:1379-1396` |
| manufacture | **tiap 4 jam** (`RETUR_KONFIRMASI_SWEEP_INTERVAL_HOURS`, default) — goroutine ticker, bukan cron | **sweep konfirmasi retur gudang macet** (2026-08-20, [[ADR - 0025 Log Sumber vs Input WMS + Stempel Penginput]]): konfirmasi fire-and-forget yang gagal terkirim (`kirimKonfirmasiRetur`) tak pernah punya retry otomatis — sweep ini menjaring transaksi retur 3 hari terakhir (`RETUR_KONFIRMASI_SWEEP_LOOKBACK_DAYS`) lewat `prosesSatuKonfirmasi`, KODE YANG SAMA dgn pemulihan manual `POST /returns/konfirmasi-ulang`. **JOB BACKGROUND PERTAMA di manufacture** (sebelumnya service ini nihil job — lihat catatan di bawah). **DORMANT by default** (`RETUR_KONFIRMASI_SWEEP_ENABLED != "true"`, pola sama `AUTO_ARRANGE_ENABLED`) — ⚠️ **belum dinyalakan di prod** | in-process channel-guard (manufacture single-instance) | `services/manufacture/retur_konfirmasi_sweep.go` |
| marketing-analytics | **tiap 48 jam** 03:00 (timer WIB di-arm ulang per siklus, bukan cron) | **penjadwal internal** (2026-08-01): 3 job berurutan `sync-ad-creative-link` → `sync-video-performance` → `sync-profit-attribution` (jendela 14 hari); kesehatan via `GET /jobs/status` (detak boot + ambang mati 72j); env `MARKETING_ANALYTICS_SCHEDULER_*` (ENABLED bawaan true, RUN_ON_BOOT false). **Lock in-process saja** — replika >1 = penjadwal ganda. **LIVE** | in-process `jobRegistry` | `services/marketing-analytics/penjadwal.go` |
| task-management | tiap jam (delay awal 1 mnt) | eskalasi SLA task (warning ke SPV / breach ke admin) | — | `services/task-management/reminder.go:12` |
| notification | harian 03:00 (`0 3 * * *`) | hapus `inbox` > 2 bulan | — | `services/notification/cron.go:20` |
| finance | tgl 1/bulan 00:30 WIB (dicek tiap 10 mnt, goroutine ticker; **bukan** cron manager integration) | **penerbitan masa pajak** periode berjalan, idempoten (index unique). ⚠️ Zona **WIB**, bukan UTC container; tgl 1 00:30 WIB = tgl akhir bulan lalu 17:30 UTC; salah zona = cron tak pernah menyala, senyap. Sudah didokumentasikan di [[API - Finance Service]] (SATU FAKTA SATU TEMPAT, tak diulang di sini); baris ini cuma menaruhnya di indeks jadwal lintas-service | - | `services/finance/pajak_jadwal.go:9-25` |
| finance | harian 08:00 WIB (dicek tiap 10 mnt, goroutine ticker) | **pengingat kewajiban pajak** H-7 & H-3 ke pemegang izin `finance.pajak.kelola`. Alur & tujuan (deep-link ke baris kewajiban) ada di [[Finance - Rancangan Finance Service]]; tak diulang di sini | - | `services/finance/pajak_jadwal.go:92-96`, `services/finance/pajak_notify.go:44,284` |
| finance | tgl 6/bulan 01:00 WIB (dicek tiap 10 mnt, goroutine ticker) | **buka periode audit kertas kerja** bulan SEBELUMNYA (tutup buku diasumsikan selesai tgl 5). Fakta "dibuka tanggal 6" SUDAH ada di [[Finance - Audit Internal]] §H+2; baris ini cuma menambahkan jam (01:00 WIB) & mekanisme ticker-nya, bukan menyalin ulang alasannya | - | `services/finance/audit_kertas_kerja.go:133-135,159` |
| procurement | tiap jam (goroutine ticker) | **pengingat jatuh tempo faktur** H-3 hari kerja → kategori `pengajuan-barang-perlu-dibayar` ke pemegang `budget.ap.bayar`. Sudah didokumentasikan lengkap di [[Microservices - Procurement Service]] §Pengingat jatuh tempo faktur (SATU FAKTA SATU TEMPAT, tak diulang di sini) | - | `services/procurement/jatuh_tempo_pengingat.go:15-40,110` |
| procurement | tiap 30 detik (goroutine ticker) | proses antrean kirim pemasok/barang/faktur ke Accurate (PENDING/FAILED lewat `next_retry_at`, retry backoff menaik). Sudah didokumentasikan lengkap di [[Microservices - Procurement Service]] (worker antrean sync); tak diulang di sini | - | `services/procurement/sync.go:56-57` |
| procurement | 2× sehari 06:00 & 18:00 WIB (dicek tiap 10 mnt, goroutine ticker) | **impor pembelian terjadwal** (PO/penerimaan/permintaan) dari Accurate; lantai, bukan langit-langit; impor manual tetap tersedia `POST /pembelian/impor`. ⚠️ Belum tercatat di dok Procurement Service manapun; pertama kali diinventarisasi di sini | - | `services/procurement/sync_pembelian.go:12-20,121-126` |
| integration | ~~harian 01:00~~ **dead code** | ~~refresh token Desty + retry order PENDING~~ — `CronManager` tidak pernah dipanggil dari `main.go` (verifikasi 2026-07-12) | Redis | `services/integration/internal/cmd/cron.go:34` |
| integration | **4× sehari** 00:00/08:00/16:00/23:00 (`0 0 0,8,16,23 * * *`) | sync order TikTok Shop (semua shop authorized, window **48 jam** overlap by `create_time`) | Redis `lock:sync-tt-shop-orders` (15m) | `.../worker/tasks/tt_shop_sync_orders.go:35` |
| integration | harian 00:00 (`0 0 0 * * *`) | sync master data TikTok Business | Redis `lock:sync-tt-business-master-data` (15m) | `.../worker/tasks/tt_business_master_data.go:45` |
| integration | harian 01:00 (`0 0 1 * * *`) | sync report GMV-Max | Redis `lock:sync-tt-business-gmv-max-report` (15m, conc. 5) | `.../worker/tasks/tt_business_gmv_max_report.go:33` |
| integration | harian 02:00 (`0 0 2 * * *`) | sync integration report | Redis `lock:sync-tt-business-integration-report` (15m) | `.../worker/tasks/tt_business_integration_report.go:35` |
| integration | harian 00:00 (`0 0 0 * * *`) | sync master data TikTok Shop (produk/kategori) | Redis `lock:sync-tt-shop-master-data` (15m) | `.../worker/tasks/tt_shop_master_data.go:34` |
| integration | **tiap 5 detik** (`*/5 * * * * *`) | konsumsi antrian webhook `webhook_tasks` & dispatch | Redis `lock:webhook-consumer` (5m) | `.../worker/tasks/webhook_consumer_task.go:36` |
| integration | ~~harian 00:00~~ **dicabut 2026-07-12** | ~~refresh kredensial Desty~~ — registrasi task dihapus dari `main.go` ([[External - Desty]] soft-disabled) | Redis `lock:desty-credential-task` (5m) | `.../worker/tasks/desty_credential_task.go:40` |
| integration | harian 02:00 (`0 0 2 * * *`) | sync performa Shopee GMS (item & campaign) | Redis `lock:sync-shopee-performance` (30m) | `.../worker/tasks/shopee_sync_task.go:30` |
| integration | **tiap 4 jam** 00/04/08/12/16/20 (`0 0 */4 * * *`) | **guardian order Shopee**: tarik-ulang order (window trailing-overlap 8j, env `SHOPEE_ORDER_GUARDIAN_LOOKBACK_HOURS`) sbg fallback bila push/webhook tak tersimpan; app ERP_SYSTEM, honest-partial. ✅ LIVE (diverifikasi prod 2026-09-01: `enabled=true`, run sukses) | Redis `lock:sync-shopee-orders` (60m) | `.../worker/tasks/shopee_orders_sync_task.go` |
| integration | **tiap 4 jam** (`0 0 */4 * * *`) | **guardian order Lazada** `sync-lazada-orders`: tarik-ulang order atas window **WAKTU-UBAH** (`update_after`, default 24j, env `LAZADA_ORDER_GUARDIAN_LOOKBACK_HOURS`, clamp 168j) — menutup satu-satunya pintu masuk order Lazada yang selama ini cuma webhook. Memicu hook faktur HANYA saat status bergerak (hindari badai attempts); honest-partial (kena-kuota ≠ gagal). ✅ LIVE (diverifikasi prod 2026-09-07: `job registered`, run sukses) | Mongo `lock:sync-lazada-orders` (30m) | `.../worker/tasks/lazada_orders_sync_task.go` |
| integration | **tiap 6 jam** menit :30 (`0 30 */6 * * *`) | **guardian retur Lazada** `sync-lazada-returns`: sapu retur seluruh toko lewat `BackfillReturns` (menautkan retur ke order **dan** membukukan Retur Penjualan Accurate). **Tanpa window waktu** — endpoint `reverse/getreverseordersforseller` tak menerima filter tanggal sama sekali, jadi cakupan ditentukan **paging** (batas 20 halaman × 50). Menit :30 sengaja meleset dari guardian order (:00) & income-reconciler (:45) agar kuota API tak dihantam berbarengan. Idempoten; honest-partial. ⚠️ belum deploy (PR #1753) | Mongo `lock:sync-lazada-returns` (30m) | `.../worker/tasks/lazada_returns_sync_task.go` |
| integration | **tiap 6 jam** :45 (`0 45 */6 * * *`) | **`sync-lazada-withdrawal-journal`**: bukukan penarikan dana Lazada ke Accurate sebagai Jurnal Umum (kas toko → bank); menutup celah buku besar kas toko yang sebelumnya cuma berisi Debit (dari receipt). Gerbang KV `withdrawal-journal-cutover-date` KOSONG = **NOL jurnal**, disengaja: finance sudah membukukan sebagian penarikan lewat jurnal manual, menebak tanggal mulai berisiko mencatat ganda uang yang sama | Redis `lock:sync-lazada-withdrawal-journal` | `.../worker/tasks/lazada_withdrawal_journal_task.go:35,40` |
| integration | **tiap 2 jam** (`0 0 */2 * * *`) | **guardian retur Shopee**: `SyncReturns` per toko lewat usecase **wired `rtsUseCase`** → memicu booking **Retur Penjualan** yang eligible (gated `RTS_RETURN_ENABLED`, idempoten). Menutup bug: transisi retur→ACCEPTED di-ingest jalur nil-wired → trigger booking tak pernah menyala (0 retur terbukukan). Window trailing **60 hari** by `create_time` (env `SHOPEE_RETURN_GUARDIAN_LOOKBACK_DAYS`, clamp 120) krn retur telat matang; honest-partial. ⚠️ belum deploy | Redis `lock:sync-shopee-returns` (30m) | `.../worker/tasks/shopee_returns_sync_task.go` |
| integration | **tiap 2 jam** (`0 30 */2 * * *`) | **guardian retur TikTok** `recover-tiktok-returns` (2026-07-16): recovery-based (TikTok tak punya pencarian retur by-waktu) — pindai order TikTok RETURNED (window env `TIKTOK_RETURN_RECOVER_LOOKBACK_DAYS`, default 30, clamp 90), isi data retur via `FetchAndSetTikTokReturn`, drive `SyncOrderReturn` (semua pengaman). Menutup celah: retur TikTok dulu hanya webhook, sekali meleset hilang. ⚠️ belum deploy | Redis `lock:recover-tiktok-returns` (30m) | `.../worker/tasks/tiktok_returns_recover_task.go` |
| integration | harian 05:05 (`0 5 5 * * *`) | proaktif refresh token semua Shopee shop (buffer sebelum expiry 06:00) | Redis `lock:shopee-credential-task` (30m) | `.../worker/tasks/shopee_credential_task.go:30` |
| integration | harian **02:05** (`0 5 2 * * *`) | **`lazada-credential-task`**: refresh proaktif token semua toko Lazada terdaftar (access token berlaku 7 hari, harian = margin lebar sebelum kedaluwarsa) | Redis `lock:lazada-credential-task` (10m) | `.../worker/tasks/lazada_credential_task.go:23-30` |
| integration | harian **03:20** (`0 20 3 * * *`) | **`meta-credential-task`**: perpanjang proaktif token OAuth Meta yang mendekati kedaluwarsa (umur ~60 hari) → Telegram TERPISAH untuk akun **mati** (butuh connect-ulang manual; Meta menolak memperpanjang token yang sudah lewat) vs yang **gagal sementara** (dicoba lagi besok) | Redis `lock:meta-credential-task` (10m) | `.../worker/tasks/meta_credential_task.go:25-36` |
| integration | **tiap 10 menit** offset :05 (`0 5/10 * * * *`) | **`meta-commerce-order-poll`**: tarik order Meta Commerce; POLLING karena Meta tak menyediakan webhook untuk order (dok resmi menyarankan interval 5–15 menit). **DIAM** (log Debug, bukan alarm) bila `META_COMMERCE_ACCOUNT_ID` belum dikonfigurasi; Bharata belum punya FB Shop, itu keadaan normal saat ini, bukan kegagalan | Redis `lock:meta-commerce-order-poll` (5m) | `.../worker/tasks/meta_commerce_order_task.go:27-50` |
| integration | tiap jam :00 (`0 0 * * * *`) | rekonsiliasi income/escrow Shopee | Redis `lock:shopee-escrow-reconciler` | `.../worker/tasks/shopee_escrow_reconciler.go` |
| integration | tiap jam :30, **hanya 06:30 sampai 23:30 WIB** (`0 30 6-23 * * *`; koreksi 2026-09-12, catatan lama `0 30 * * * *`) | rekonsiliasi income TikTok per order (refetch settlement) | Redis `lock:income-reconciler` | `.../worker/tasks/tt_income_reconciler.go:99-100` |
| integration | **4× sehari** 00:00/08:00/16:00/23:00 (`0 0 0,8,16,23 * * *`) | sync affiliate orders TikTok Seller | Redis `lock:sync-affiliate-orders` (15m) | `.../worker/tasks/affiliate_orders_sync.go` |
| integration | mingguan Minggu 02:00 (`0 0 2 * * 0`) | refresh status affiliate orders 89 hari terakhir | Redis `lock:refresh-affiliate-orders` | `.../worker/tasks/affiliate_orders_refresh.go` |
| integration | harian 04:00 (`0 0 4 * * *`) | validasi komisi affiliate vs finance statement lokal. ⚠️ **Tak pernah sekali pun berhasil 3 Jul–8 Agt 2026** (35 run, 35× `context deadline exceeded`) — `order_id` tak terindeks membuat tiap `UpdateMany` jadi COLLSCAN, total ~770 menit untuk timeout 30 menit; ✅ sembuh setelah index (PR #1094), run pertama pasca-deploy **25 detik** | Redis `lock:validate-affiliate-commission` | `.../worker/tasks/affiliate_commission_validate.go` |
| integration | harian 02:30 (`0 30 2 * * *`) | sync video performance TikTok Shop | Redis `lock:sync-tt-shop-video-performance` | `.../worker/tasks/tt_shop_video_performance_task.go` |
| integration | harian **22:40** (`0 40 22 * * *`) | **`auto-summary-report-return`** (2026-07-25): cermin RETUR dari `auto-summary-report` (22:30). Menyegarkan report Summary AUTO `SALES_RETURN` per (toko, channel, hari-retur) dari dok retur `SENT` + bersihkan hari yang kosong via redate; lookback env `AUTO_SUMMARY_RETURN_LOOKBACK_DAYS` default 30 (= backfill run pertama). Realtime sudah ditangani hook di `rebuildAndSendGroup` (SENT); worker ini rekonsiliasi | Redis `lock:auto-summary-report-return` | `.../worker/tasks/auto_summary_report_return.go` |
| integration | harian **06:45** (`0 45 6 * * *`) | **sync ulasan marketplace** (`sync-reviews`): komentar Shopee incremental (cap 500/query, snapshot harian di-recompute dari DB) + distribusi bintang kumulatif TikTok per produk (1 call/produk, throttle 500ms) + enrichment nama produk (order + fallback API produk). Jadwal sengaja pagi — hindari bentrok quota dgn rumpun sync 00:00–02:00. LIVE 2026-07-19 (26 toko) | Redis `lock:sync-reviews` (60m) | `.../worker/tasks/sync_reviews.go` |
| integration | **4× sehari** 00:00/08:00/16:00/23:00 (`0 0 0,8,16,23 * * *`) | sync report GMV-Max **hari ini** (intraday update) | Redis `lock:sync-tt-business-gmv-max-report-today` (15m) | `.../worker/tasks/tt_business_gmv_max_report_today.go:34` |
| integration | harian 03:00 (`0 0 3 * * *`) | sync performa affiliate Shopee AMS (v1 snapshot per-affiliate + v3 product/content) | Redis `lock:sync-shopee-affiliate-performance` (30m) | `.../worker/tasks/shopee_affiliate_sync_task.go:31` |
| integration | **tiap 30 mnt** (`0 */30 * * * *`) | **`sync-tracking`** (Packet Tracking forward): tarik checkpoint API resmi MP (Shopee `get_tracking_info` / TikTok `/fulfillment/202309/orders/{id}/tracking`) utk order non-terminal belum DELIVERED + final-sync 1× saat COMPLETED + enrich resi/provider. Isi sub-dok `tracking` | Redis `lock:sync-tracking` | `.../worker/tasks/sync_tracking.go` |
| integration | tiap jam :15 (`0 15 * * * *`) | **`detect-stuck`** (deteksi order mandek): scan **hanya SHIPPED/IN_TRANSIT** (window `order_date ≥ now-14hr`) vs threshold per-stage (config KV `stuck_thresholds`), set/clear sub-dok `stuck` + `ClearStuckOlderThan` | Redis `lock:detect-stuck` | `.../worker/tasks/detect_stuck.go` |
| integration | tiap jam :20 (`0 20 * * * *`) | **`sync-return-tracking`** (retur): Shopee `get_reverse_tracking_info` (checkpoint hub) / TikTok `Search Returns` (resi+kurir+milestone, tanpa checkpoint) → sub-dok `return_tracking`; SELALU tulis `last_sync_at` (backoff, cegah re-sync storm refund-only) | Redis `lock:sync-return-tracking` | `.../worker/tasks/sync_return_tracking.go` |
| integration | harian 05:00 (`0 0 5 * * *`) | sync konversi order-level + validasi komisi affiliate Shopee AMS (v2) | Redis `lock:sync-shopee-affiliate-conversion` (30m) | `.../worker/tasks/shopee_affiliate_conversion_sync_task.go:32` |
| integration | **tiap :05 & :35** (`0 5,35 * * * *`) | **WMS resi**: fetch AWB Shopee (`get_tracking_number`) untuk order shippable tanpa resi — dijalankan sebelum `sync-resi-wms` | Redis `lock:sync-shopee-tracking` (12m) | `.../worker/tasks/sync_shopee_tracking.go:28` |
| integration | **tiap 10 menit** (`0 */10 * * * *`) | **WMS resi bridge**: pull resi-feed TikTok+Shopee (watermark per-channel di `sync_cursors`) → push batch ke manufacture `POST /resi/sync-batch` | Redis `lock:sync-resi-wms` (8m) | `.../worker/tasks/sync_resi_wms.go:55` |
| integration | **tiap 15 menit** (`0 */15 * * * *`) | **`auto-arrange`** (pengganti auto-ship Desty, Gap 2): per channel cek gerbang window sadar-holiday (dievaluasi "sekarang": libur/≥15:00 TikTok/besok-libur Shopee → tunda), cari order **siap-diatur-kirim** (`raw_status ∈ {AWAITING_SHIPMENT, READY_TO_SHIP, RETRY_SHIP}`, termasuk COD; umur ≤7 hari; belum `arranged`/`needs_review`) → `ShipBatch` (TO_PROCESS→TO_SHIP) → tandai `arranged` / retry 3× → `needs_review`+Telegram. **⚠️ DORMANT**: hanya didaftarkan bila env **`AUTO_ARRANGE_ENABLED=true`** (default off) — aman di-deploy siapa pun; nyalakan hanya saat **cutover Desty→WMS**. MongoDB lock (LockKey) `lock:auto-arrange` (10m) | `.../worker/tasks/auto_arrange.go` |

| integration | tiap jam **:05** (`0 5 * * * *`) | **`accurate-stock-refresh`**: tarik stok bulk Accurate (`list-stock.do`, paginated) → upsert `accurate_stocks` (source=sync). Jaring pengaman di bawah webhook push. Semula tiap 10 menit; **diperjarang jadi jam-jaman 2026-07-30** (PR #798) — tiap run menarik katalog utuh yang sama (876 item identik), ±1.300 → ±216 call/hari | Redis `lock:accurate-stock-refresh` (10m) | `.../worker/tasks/accurate_stock_refresh.go:41` |
| integration | harian **21:30** (`0 30 21 * * *`) | **`accurate-daily-invoice-gap-sweep`** (2026-08-26): buat faktur untuk toko-hari yang punya order sah tapi tak pernah punya baris faktur (toko didaftarkan setelah order terkirim; prod: 5 toko/36 hari/96 order). Jendela mundur 45 hari (`RTS_GAP_SWEEP_LOOKBACK_DAYS`, clamp 90). Berjalan **sebelum** sweep faktur 22:00 supaya baris yang baru lahir ikut tersapu malam yang sama | Redis `lock:accurate-daily-invoice-gap-sweep` | `.../worker/tasks/accurate_daily_invoice_gap.go:49-50` |
| integration | harian **22:00** (`0 0 22 * * *`) | **`accurate-daily-invoice-sweep`**: sapu faktur harian yang belum terkirim/gagal → retry booking | Redis `lock:accurate-daily-invoice-sweep` | `.../worker/tasks/accurate_daily_invoice_sweep.go:76` |
| integration | harian **22:30** (`0 30 22 * * *`) | **`accurate-daily-return-sweep`**: cermin RETUR dari sweep faktur | Redis `lock:accurate-daily-return-sweep` | `.../worker/tasks/accurate_daily_return_sweep.go:48` |
| integration | ~~harian 23:15~~ (`0 15 23 * * *`) | **`accurate-receipt-sweep`**: sapu receipt (pelunasan) yang belum terkirim/gagal. ⛔ **KOREKSI 2026-09-12**: berkas task ADA tapi **TAK PERNAH didaftarkan** di `main.go` (`_ = tasks.NewReceiptSweepTask`, bukan `tasks.Register(...)`), jadi **BUKAN job aktif** dan jangan dihitung sebagai berjalan | - (tak terdaftar) | `.../worker/tasks/receipt_sweep.go:45,50`; non-registrasi: `main.go:2417` |
| integration | harian **23:50** (`0 50 23 * * *`) | **`piutang-snapshot-bulanan`**: potret posisi piutang SELURUH perusahaan (populasi sama dengan metrik KPI `kinerja_ar`); HARIAN (bukan job akhir-bulan) supaya satu hari gagal tak menghapus potret bulan itu selamanya; menimpa periode berjalan, potret terakhir sebelum ganti bulan yang menang. ⛔ Bucket `>90` **sengaja tak dijumlahkan** ke mana pun; himpunan bagian dari `>60` (`SnapshotDariAging`), menjumlahkannya menghitung rupiah yang sama dua kali | - | `.../worker/tasks/piutang_snapshot_task.go:21,47-48`; aturan bucket: `piutang_snapshot_task.go:86-92` |
| integration | ~~2× sehari 08:30 & 14:30~~ **KOREKSI 2026-09-12: 5× sehari** 08:30/11:30/14:30/17:30/20:30 (`0 30 8,11,14,17,20 * * *`) | **`receipt-sync`**: bangun Sales Receipt harian per (toko, hari-cair) dari statement/escrow; idempoten full-replace. Jadwal SEED kode di atas; jadwal aktual bisa berbeda bila ada entri `worker_configs` (lihat § Catatan & risiko operasional) | Redis `lock:receipt-sync` | `.../worker/tasks/receipt_sync.go:38,56` |
| integration | **2× sehari** 08:45 & 14:45 (`0 45 8,14 * * *`) | **`sample-shipping-expense`**: Jurnal Umum beban ongkir pesanan sampel gratis, satu jurnal per (toko, tanggal-settled). Gate KV `sample-shipping-cutover-date` (kosong = NO-OP). Sejak **2026-08-04 (PR #975)** baris jurnal membawa **departemen & proyek toko** (dulu dikirim kosong) dan baris harian **dibaca ulang setelah lock** (anti jurnal dobel, kelas bug sama dgn [[Microservices - Integration Service]] Penyesuaian Sampel); `RunDaily` tetap **tanpa** `force` — sapuan 90 harinya berjalan tiap run, memaksanya berarti menulis ulang seluruh jurnal lama tiap hari. Koreksi jurnal lama lewat **Retry** (`force`, menembus hash `total\|jumlah order`) | Redis `lock:sample-shipping-expense` | `.../worker/tasks/sample_shipping_expense_task.go:26` |
| integration | harian **02:15** (`0 15 2 * * *`) | **`accurate-fixed-asset-refresh`**: tarik master aset tetap Accurate → salinan lokal | Redis `lock:accurate-fixed-asset-refresh` (15m) | `.../worker/tasks/aset_tetap_refresh.go:64` |
| integration | harian **02:45** (`0 45 2 * * *`) | **`accurate-realisasi-opex-refresh`**: tarik realisasi OPEX Accurate → salinan lokal. Populasinya kombinasi beranggaran **DITAMBAH seluruh 57 akun komponen OPEX** di tingkat perusahaan (2026-08-13): sebelumnya hanya yang beranggaran, sehingga belanja pada akun tak beranggaran tak pernah ditarik, barisnya tak pernah lahir, dan penanda "bukan potret utuh" di layar varians **tidak menyala** — Rp 150 jt belanja Juli hilang dari layar tanpa jejak. Daftar OPEX ditambahkan **hanya pada departemen kosong** (RAPB tak punya dimensi departemen), jadi tambahannya ≤57 panggilan, bukan 56×13=728 kartesian; periode tanpa satu baris anggaran pun tetap dilewati tanpa menyentuh Accurate. Katalog yang gagal dibaca **tidak** membatalkan penarikan yang masih bisa dilakukan tetapi dilaporkan sebagai galat run | Redis `lock:accurate-realisasi-opex-refresh` (15m) | `.../worker/tasks/realisasi_opex_refresh.go:59` |
| integration | harian **03:15** (`0 15 3 * * *`) | **`accurate-realisasi-mingguan-refresh`**: tarik realisasi **6 akun kas-keluar per MINGGU** → koleksi `realisasi_opex_mingguan`, bahan metrik KPI Cost Control #4 (forecast cashflow, bobot 15%). Populasinya sempit dan tetap: 6 akun × 4–5 minggu × 2 periode = **paling banyak 60 panggilan**, seluruhnya di departemen kosong. Dijadwalkan **30 menit sesudah** penyegar bulanan (02:45), bukan berbarengan — keduanya berbagi limiter Accurate 6 req/s yang juga dipakai jalur yang menghadap pengguna, dan berbarengan membuat keduanya saling memperlambat sampai mendekati timeout. Periode tanpa anggaran kas-keluar dilewati tanpa menyentuh Accurate. ⚠️ Penarikan yang **gagal tetap DITULIS** dengan statusnya, tidak dilewati: baris yang hilang tak terbedakan dari baris yang belum pernah ditarik, dan minggu itu akan dihitung sebagai belanja nol — satu penolakan Accurate berubah jadi "minggu itu tidak membelanjakan apa pun" lalu menekan skor orang tanpa dasar. Kegagalan sebagian **bukan** galat run (barisnya sudah tersimpan benar dan run berikutnya mencoba lagi; alarm yang selalu menyala berhenti dibaca) | Redis `lock:accurate-realisasi-mingguan-refresh` (10m) | `.../worker/tasks/realisasi_mingguan_refresh.go:60` |
| integration | harian **03:30** (`0 30 3 * * *`) | **`accurate-ppn-masukan-refresh`**: tarik PPN masukan faktur pembelian dua bulan (bulan lalu + berjalan, entri lazim telat) dari Accurate (`purchase-invoice/list.do` lalu `detail.do` per id, SEKUENSIAL krn limiter 6 req/s dibagi service) → upsert salinan lokal `accurate_ppn_masukan`. Satu faktur gagal tak membatalkan sisanya tapi dilaporkan sebagai galat run, bukan senyap | Redis `lock:accurate-ppn-masukan-refresh` (15m) | `.../worker/tasks/ppn_masukan_refresh.go:25,74-75` |
| integration | harian **03:35** (`0 35 3 * * *`) | **`accurate-realisasi-nonops-refresh`**: tarik realisasi **3 akun Admin & Non-Ops** (bahan KPI Cost Control #2, "turun ≥2% YoY") untuk periode berjalan + sebelumnya + baseline YoY masing-masing (4 periode/hari, ≤9 panggilan, katalog akun by-NAMA bukan `GetProfitLossAccounts`; yang itu cuma memuat akun beraktivitas). Kegagalan **IKUT DITULIS** dengan sebabnya (baris hilang tak terbedakan dari belum-pernah-ditarik) | Redis `lock:accurate-realisasi-nonops-refresh` (5m) | `.../worker/tasks/realisasi_nonops_refresh.go:24,67-68` |
| integration | harian **04:00** (`0 0 4 * * *`) | **`accurate-perlengkapan-cost-refresh`**: isi stok DI GUDANG GA (`gudang_utama_qty`, env `PERLENGKAPAN_GUDANG` default "Gudang Utama") + Total Biaya (qty × harga pokok) tiap barang kategori Perlengkapan ber-qty(agregat)>0, via `item/detail.do` per item; terpisah dari `accurate-stock-refresh` (jam-jaman, agregat) karena qty-per-gudang+biaya cuma ada di detail. Menulis lewat `UpdateGudang` ($set bertarget) supaya tak menimpa sync jam-jaman | Redis `lock:accurate-perlengkapan-cost-refresh` (15m) | `.../worker/tasks/accurate_perlengkapan_cost_refresh.go:53,57` |
| integration | harian **07:30** (`0 30 7 * * *`) | **`check-return-anomaly`**: deteksi anomali retur (over-retur, retur tanpa faktur) → notif | Redis `lock:check-return-anomaly` | `.../worker/tasks/accurate_return_anomaly_check.go:71` |
| integration | harian **08:00** (`0 0 8 * * *`) | **`reconciliation-alert`**: evaluasi selisih uang masuk (MP↔ERP↔Accurate) untuk HARI KEMARIN (bukan hari berjalan; uang masih cair, receipt masih diproses) → Telegram bila melewati ambang KV `reconciliation-alert-threshold` (rupiah; kosong/0 = notif mati). READ-ONLY. Membalik alur lama: sebelumnya selisih baru ketahuan bila finance kebetulan membuka layar rekonsiliasi dan membandingkan manual | - | `.../worker/tasks/reconciliation_alert.go:49,55-59,83,104` |
| integration | harian **08:05** (`0 5 8 * * *`) | **`toko-tanpa-config-alert`** (2026-08-09, PR #1105): READ-ONLY — toko yang mengirim barang 30 hari terakhir tapi tak ada di `accurate_shops` → notif. Toko tak terdaftar **tak pernah "gagal"** (tak pernah dicoba), jadi omzetnya hilang dari pembukuan tanpa alarm apa pun; terukur 6 toko / Rp 30,8 jt sejak 8 Jul. **Sunyi bila nihil temuan** | Redis `lock:toko-tanpa-config-alert` (5m) | `.../worker/tasks/toko_tanpa_config_alert.go` |
| integration | harian **02:00** (`0 0 2 * * *`) | **`seed-mapping`**: bangun mapping SKU listing→master dari riwayat order + kamus channel variation; melaporkan produk master yang belum punya HPP bernama sama persis | Redis `lock:seed-mapping` | `.../worker/tasks/seed_mapping_task.go:26` |
| integration | **tiap 2 jam** :40 (`0 40 */2 * * *`) | **`enrich-cancel-resi`**: lengkapi resi order cancel (paket balik) | Redis `lock:enrich-cancel-resi` | `.../worker/tasks/enrich_cancel_resi.go:30` |
| integration | **tiap 2 jam** :50 (`0 50 */2 * * *`) | **`enrich-reverse-resi`**: lengkapi resi paket RETUR (reverse logistics) | Redis `lock:enrich-reverse-resi` | `.../worker/tasks/enrich_reverse_resi.go:31` |
| integration | **tiap 2 jam** (`0 0 */2 * * *`) | **`resi-wms-safety-net`**: jaring pengaman bridge resi — push ulang batch yang tertinggal ke manufacture `POST /resi/sync-batch` | Redis `lock:resi-wms-safety-net` | `.../worker/tasks/resi_wms_safety_net_task.go:59` |
| integration | **tiap 2 jam** :50 (`0 50 */2 * * *`) | **`ringkasan-notif`**: kirim satu laporan berisi seluruh notifikasi rutin yang ditahan `notifier.Peringkas` sepanjang siklus (~56 pesan/laporan); pasangan wajib dari lapisan yang menahan pesan; tanpa job ini pesan tertahan selamanya. Menit :50 sengaja menjauh dari puncak beban :00/:30 | Redis `lock:ringkasan-notif` (2m) | `.../worker/tasks/ringkasan_notif_task.go:24,41` |
| integration | **tiap 2 jam** :25 (`0 25 */2 * * *`) | **`reconcile-tiktok-returns`**: rekonsiliasi retur TikTok (lengkapi retur yang webhook-nya terlewat) | Redis `lock:reconcile-tiktok-returns` | `.../worker/tasks/tiktok_returns_reconcile_task.go:44` |
| integration | **tiap 6 jam** :45 (`0 45 */6 * * *`) | **`reconcile-tt-order-status`**: rekonsiliasi status order TikTok vs marketplace | Redis `lock:reconcile-tt-order-status` | `.../worker/tasks/tt_shop_reconcile_status.go:46` |
| integration | **tiap 6 jam** (`0 0 */6 * * *`) | **`sync-stale-toship`**: guardian order TO_SHIP yang menggantung | Redis `lock:sync-stale-toship` | `.../worker/tasks/stale_toship_guardian_task.go:60` |
| integration | **tiap 3 jam** :15 (`0 15 */3 * * *`) | **`tt-statement-reconciler`**: rekonsiliasi statement TikTok → income per order (menutup order yang settle via statement) | Redis `lock:tt-statement-reconciler` | `.../worker/tasks/tt_statement_reconciler.go:56` |
| integration | **tiap 2 jam** :45 mulai 01:45 (`0 45 1/2 * * *`) | **`wallet-sync`**: tarik mutasi wallet marketplace (penarikan, penyesuaian saldo) | Redis `lock:wallet-sync` | `.../worker/tasks/wallet_sync.go:41` |
| integration | **jam 06–23** :45 (`0 45 6-23 * * *`) | **`lazada-income-reconciler`**: set income per order Lazada dari statement finance (T+ variabel) | Redis `lock:lazada-income-reconciler` | `.../worker/tasks/lazada_income_reconciler.go:37` |
| integration | **tiap 6 jam** :17 (`0 17 */6 * * *`) | **`accurate-external-edit-sweep`** (Penyisir Lapis 2): baca ulang `detail.do` untuk faktur SENT dalam jendela 45 hari yang tak kebetulan tersentuh jalur kirim, deteksi edit eksternal → draf Kotak Adopsi. **LAHIR MATI**: digerbang KV `external-edit-sweeper` (bukan `worker_configs`; itu men-seed `enabled=true` otomatis ke tiap job baru, mustahil melahirkan job dalam keadaan mati lewat jalur itu). Menyalakan: set kv `external-edit-sweeper=on` | in-process, `lock:accurate-external-edit-sweep` | `.../worker/tasks/accurate_external_edit_sweep.go:45,54` |
| integration | **tiap 6 jam** :41 (`0 41 */6 * * *`) | **`accurate-snapshot-baseline-seed`**: tanam baseline `accurate_sent_snapshots` (`source=SEED`, dari `detail.do`; isi Accurate SAAT INI, bukan isi yang dikirim) untuk dokumen SENT lama yang belum bersnapshot (prod 2026-08-02: 4 dari 1.116). Tanpa baseline, deteksi edit eksternal buta. **LAHIR MATI** via kv `snapshot-baseline-seeder` (pola & alasan sama Sweep Lapis 2) | `lock:accurate-snapshot-baseline-seed` | `.../worker/tasks/accurate_snapshot_baseline_seed.go:50,60` |
| integration | **tiap 3 jam** :15 (`0 15 */3 * * *`) | **`accurate-serap-jurnal-kas`**: serap Jurnal Umum kas dari Accurate, jendela 45 hari. **LAHIR MATI** via gerbang KV `serap-jurnal-kas` yang dievaluasi di AWAL `Run`; kosong = nol serapan, konsisten dengan pola lahir-mati di atas | `lock:accurate-serap-jurnal-kas` | `.../worker/tasks/accurate_serap_jurnal_kas.go:35,43,59-60,66-76` |
| integration | harian **05:55** (`0 55 5 * * *`) | **`accurate-webhook-renew`**: perpanjang masa aktif webhook Accurate (berlaku 7 hari); HARIAN, bukan per-7-hari, supaya satu run gagal masih menyisakan enam kesempatan pulih sebelum webhook mati senyap. TANPA gerbang kv (beda dari `accurate-serap-jurnal-kas`): ongkosnya cuma 1 call/hari, dan gerbang yang lupa dinyalakan mengembalikan justru kegagalan yang hendak dicegah | Redis `lock:accurate-webhook-renew` | `.../worker/tasks/accurate_webhook_renew.go:33,63-66` |

> ~~Inisialisasi index (bukan job): `services/insentive/cron_worker.go:1550` — `ensureCronIndexes()` (TTL index `cron_locks.expires_at`) dipanggil sekali saat startup.~~ ⛔ Koreksi 2026-09-12: `cron_worker.go` sudah dihapus 2026-07-30 (`services/insentive/main.go:712-713`), dan `ensureCronIndexes` maupun `cron_locks` tidak lagi ditemukan di kode insentive origin/main.

## Event-driven (webhook dispatcher)

- **integration** — `internal/webhook/dispatcher.go`: antrian in-memory, **5 worker goroutine** default. Entry: `POST /webhooks/services/shopee` · `/tiktok` · `/accurate` · `/lazada` (publik, tanpa validasi gateway; handler `CreateWebhookLazada`, processor `LazadaPushProcessor` — order **dan** reverse-order/retur lewat pintu yang sama). Callback TikTok Shop/Shopee → di-log → di-antri per-platform → di-konsumsi job `webhook_consumer_task` (tiap 5 detik). Rate limit 20 req/dtk per platform; retry on failure; status disimpan ke Mongo. Entry `/webhooks/services/desty` **dicabut 2026-07-12** ([[External - Desty]] soft-disabled). ⚠️ **Lazada dulu tak punya cron penarik order/retur** (temuan 2026-07-28; keduanya kini ditutup — **ORDER** oleh `sync-lazada-orders` (PR #1621, ✅ live), **RETUR** oleh `sync-lazada-returns` (PR #1753, belum deploy)): sebelum itu cron khusus Lazada hanya `lazada_credential_task` & `lazada_income_reconciler`, tak ada padanan guardian `sync-shopee-orders`/`sync-shopee-returns`/`recover-tiktok-returns`, sehingga **webhook di atas adalah satu-satunya pintu masuk** order & retur Lazada — dan itu terbukti menggigit 2026-09-01 (token toko kedaluwarsa, push berhenti, **18 order lenyap 10 hari**). Sweep Accurate (faktur 22:00 / retur 22:30) **bukan** pengganti: keduanya menyapu baris PENDING/FAILED yang **sudah ada**, jadi data yang tak pernah masuk ERP tak meninggalkan jejak untuk disapu. Lihat [[Microservices - Integration Service]].

## In-process poller (goroutine, bukan cron)

- **warehouse** — dua goroutine poll di `main.go` (bukan cron manager), Redis lock via redsync:
  - **Reconciler 60s** (`reconciler.go`, `lock:warehouse-reconciler` TTL 50s) — tarik order dari integration `GET /transactions/orders/list` **by-watermark** (`order_update_date`, cursor per-status di `sync_cursors`), 4 stream `TO_SHIP`/`SHIPPED`/`COMPLETED`/`RETURNED`, batch 500 **di-paginasi sampai jendela habis** (maks 20 halaman/tick). Antrian kerja WMS + auto-close order yang diproses di luar WMS. ✅ **Deadlock cursor (saturasi batch) diperbaiki 2026-07-24** (PR #638): dulu bila ≥500 order jatuh dalam satu jendela overlap, cursor tak pernah maju dan stream macet selamanya (terjadi nyata 09:17 WIB, 514 order, macet 1j44m). Kini paginasi menguras jendela + dorong cursor bila kurasan buntu. Detail: [[Microservices - Warehouse Service]].
  - **Open-order sweep 5m** (`open_order_sweep.go`, `lock:warehouse-open-sweep` TTL 4m) — jaring pengaman **self-healing**: kumpulkan order **terbuka** (`status_wms NOT IN [HANDED_OVER, CANCELLED]`, tertua-dulu, cap 5000/tick) → tanya status terkini `?order_ids=…` → `doUpsert`. **Tanpa cursor** → menutup celah reconciler yang melewatkan update telat (order nyangkut). Idempoten; run pertama membersihkan backlog. Lihat [[Microservices - Warehouse Service]].

## Service tanpa job background

`file` · `inventory` · `tiktok-shop-service` · `api-gateway` · orchestrator `hris` · orchestrator `it`.

## Catatan & risiko operasional

- ⛔ **`workers.worker_configs` MENIMPA jadwal cron di kode — senyap, dan seluruh tabel di atas jadi tak bisa dipercaya sendirian.** Database **`workers`** (bukan `integration_db`), koleksi **`worker_configs`**, kunci `_id` = **nama job**. **68 entri per 2026-09-10.** Bila ada entri untuk sebuah job, nilai `schedule` di **database MENANG** atas yang di kode — jadi mengubah jadwal di Go **tidak berpengaruh apa pun** sampai dokumen database ikut diperbarui. Grounded: `services/integration/internal/worker/manager.go` `Register()` (`configStore.Load` → `job.Schedule = cfg.Schedule`); hal yang sama berlaku untuk `MaxAttempts`, `RetryDelay`, dan `Timeout`.

	**Terjadi 2026-09-10**: jadwal penyerapan jurnal kas diubah 04:15 → tiap 3 jam, **deploy sukses**, tapi entri database bertanggal 4 Agustus 2026 tetap memaksa **04:15**. Tak ada galat, tak ada penolakan — jadwal barunya sekadar tak pernah berlaku, dan yang membaca kode menyimpulkan fiturnya sudah berubah.

	⚠️ **Manager menyemai tiap job BARU dengan `enabled=true` ke `worker_configs` saat boot pertama.** Konsekuensinya: nilai di kode adalah **seed sekali-pakai**, bukan konfigurasi hidup. Job yang perlu lahir-mati karena mahal karena itu digerbang **kv**, bukan `worker_configs` (mis. `accurate-serap-jurnal-kas` lewat kv `serap-jurnal-kas`; komentar alasannya ada di `accurate_serap_jurnal_kas.go` & `accurate_snapshot_baseline_seed.go`).

	✅ **Yang menyelamatkan**: `manager.go:157` mencetak peringatan boot bila DB ≠ kode —
	`WARN worker: schedule from DB overrides code — code change has NO effect until worker_configs updated` (membawa `job`, `db_schedule`, `code_schedule`).
	**Baca log boot setiap kali mengubah jadwal cron**; ini satu-satunya tanda yang muncul.

	**Cara memperbaiki** (pola untuk job apa pun — `<nama-job>` = `_id` di koleksi):
	```bash
	docker exec Integration-MongoDB mongosh "mongodb://erp-mongo:<pass>@localhost:27017/workers?authSource=admin" --quiet --eval 'db.worker_configs.updateOne({_id:"<nama-job>"},{$set:{schedule:"<cron 6 field>",updated_at:new Date()}})'
	```
	Lalu `docker restart <service>` — **jadwal dibaca saat boot**, jadi update tanpa restart tak berefek sampai boot berikutnya.

	⚠️ **Pakai kutip TUNGGAL untuk `--eval`.** Dengan kutip ganda, shell mengembangkan `$set` menjadi string kosong dan perintahnya gagal **tanpa sebab yang jelas** — bentuk perintahnya terlihat benar di layar.

	Alternatif tanpa mongosh untuk job yang punya endpointnya: `PUT /jobs/:name/config` (retune schedule/retry/timeout) dan `POST /jobs/:name/{enable,disable}` — lihat [[API - Integration Service]].

- **Penumpukan jam 00:00–02:00**: banyak sync integration (master data, order, report, kredensial) menumpuk di tengah malam WIB — perhatikan saat menilai beban/quota API TikTok/Shopee. Tambahan: `sync-tt-shop-orders` kini 4× sehari (bukan 1× di 01:00) — jam 00:00/08:00/16:00/23:00 WIB.
- **Notifikasi worker**: semua worker integration kirim Telegram otomatis via hook di manager level — tidak perlu konfigurasi per-task. `WithOnJobError` → `SendError` saat gagal (setelah semua retry habis); `WithOnJobEnd` → `Send` saat sukses. Dikecualikan dari notif sukses: `webhook-consumer` & `sync-resi-wms` (frekuensi tinggi, akan spam). PR #322.
- **Job tersibuk**: `webhook_consumer` jalan **tiap 5 detik** — paling sering; pastikan lock Redis sehat agar tak dobel.
- **Riwayat run (`workers.worker_history`)** — TTL **90 hari** (index `started_at_1`, `HistoryTTL` di `worker/history.go`). Per 2026-08-09 keberhasilan `webhook-consumer` **tidak lagi dicatat** (PR [#1108](https://github.com/bip-itteam-internal/bip-erp/pull/1108)): job tiap-5-detik itu menyumbang 1.466.519 dari 1.483.909 baris — **98,8% koleksi**, seluruhnya `success`, nol kegagalan sepanjang retensi. Riwayat yang tak pernah memuat kejadian buruk tak bisa menjawab apa pun saat sesuatu terjadi. **Kegagalan tetap disimpan apa pun nama jobnya** — itu batas antara mengurangi kebisingan dan menghapus bukti. Daftar pengecualian sengaja berisi nama eksplisit, bukan aturan "semua job frekuensi tinggi": `sync-resi-wms` punya 170 kegagalan dan `accurate-stock-refresh` 6, jadi aturan berbasis frekuensi akan ikut membuang konteks "sebelum gagal, seberapa sering berhasil?". TTL **dipertahankan** — memendekkannya ke 14 hari hanya memangkas 83% sementara membuang kebisingan memangkas 99% *dan* menyimpan riwayat penuh untuk job yang riwayatnya benar-benar dibaca (endpoint `GET /jobs/:name/history`). Proyeksi: 1.483.909 baris/251 MB → 17.383 baris/3 MB.
- ⚠️ **Alarm kegagalan ADA tapi mudah tenggelam** — hook `WithOnJobError` terbukti menyala saat timeout (diuji lewat `Manager.wrapFn` maupun `Scheduler.wrap`) dan filter `Peringkas` meloloskan pesan `Worker Failed`, jadi 35 kegagalan `validate-affiliate-commission` di atas kemungkinan besar **memang terkirim** — lalu hilang di antara ratusan `✅ Worker Succeeded` per hari. Masalahnya rasio sinyal, bukan ketiadaan alarm; menambah alarm baru di atas alarm yang sudah tenggelam justru memperburuk. Catatan pencarian log: `handleErr` menulis `job timed out` untuk `DeadlineExceeded`, **bukan** `job errored` — mencari dengan kata kunci yang salah membuat kegagalan tampak seperti tak pernah dilaporkan.
- **Locking**: integration = Redis (prefix `srv:integration:lock:*`); insentive = dulu Mongo `cron_locks` (TTL 2j), tidak relevan lagi sejak cron insentive dicabut 2026-07-30. Service lain (employee/attendance/notification/task-management) **tanpa distributed lock** — aman selama **single instance**; bila di-scale horizontal, job bisa dobel.
- Backup DB mingguan ada di luar tabel ini (lihat [[IT - Backup & DR]] / [[IT - Runbooks]]).

## Dokumen Terkait

- [[IT - Monitoring System]] · [[IT - Runbooks]] · [[IT - Environment Inventory]] · [[IT - Backup & DR]]
- [[DB - Overview and Notes]] (`cron_locks`, `webhook_tasks`) · [[Microservices - Integration Service]] · [[Microservices - Insentive Service]] · [[Microservices - Employee Service]] · [[Microservices - Attendance Service]] · [[Microservices - Notification Service]] · [[Microservices - Task Management Service]] · [[Microservices - Procurement Service]]
- Finance-service **tak punya** dok `Microservices - *` sendiri, tetapi jadwalnya BUKAN cuma tercatat di sini: penerbitan & pengingat pajak ada di [[API - Finance Service]] dan [[Finance - Rancangan Finance Service]]; buka periode audit kertas kerja ada di [[Finance - Audit Internal]] §H+2. Baris finance-service di tabel di atas sengaja dipersempit ke mekanisme ticker + file:line, bukan mengulang rasionalnya (SATU FAKTA SATU TEMPAT)
- [[External - Desty]] · [[Sales - Marketplace Integration]]
- [[API - Integration Service]] — endpoint `/jobs/*` (status, histori, config, enable/disable/trigger)
- [[External - Accurate]] · [[External - Accurate Webhook]] — limiter 6 req/s dibagi seluruh service (alasan jadwal job Accurate disebar) & job `accurate-webhook-renew`
